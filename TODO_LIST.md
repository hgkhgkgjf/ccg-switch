## 本轮 PLAN 2026-06-21 small-screen edit stats target label

- 目标：让 `ChatInputStatusTabs` 小屏 fallback 的 edit row `+N` / `-N` 统计在保持紧凑可见文本的同时，通过 `title` / `aria-label` 暴露 `Edit stats: <file> · +N / -N`，避免多个文件编辑行里重复裸数字脱离文件上下文。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx` 的 `renderEditsPanel` 与可读 cc-gui `FileChangesList.tsx`；确认本项目小屏 edit row 已有 full-diff action label，但 stats group 仍只有裸 `+5` / `-2`。
- 功能点 3：补 RED 测试。验证方式：扩展 `ChatInputStatusTabs.test.tsx` 的 edits panel 用例和 key-only fallback 用例，断言 stats group 输出 `title` / `aria-label`，旧实现应因 stats group 缺少目标化标签失败。
- 功能点 4：实现最小展示修复。验证方式：新增 `chat.layout.inputStatusEditFileStats` locale key，并在 `ChatInputStatusTabs.tsx` 用 `translateWithFallback()` 为 stats group 写入 `title` / `aria-label`；不改 diff 选择、popover 关闭、编辑统计计算、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录小屏 fallback edit row stats 必须包含文件路径和增删统计上下文。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 small-screen edit stats target label

- 上轮进展与阻塞：上一轮记录称已完成小屏 MCP server transport target label；本轮代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 中存在 `getMcpServerTransportTargetLabel(server)`，MCP transport 行已写入 `title` / `aria-label`，`ChatInputStatusTabs.test.tsx` 和 `.trellis/spec/frontend/component-guidelines.md` 也有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/FileChangesList.tsx` 的文件变更行会显示文件名与 `+N` / `-N` stats；本项目小屏 `ChatInputStatusTabs` edits panel 已把文件行做成可复用 page-level diff selection 入口，并有 `Inspect full diff: <file>` action label，但 stats group 仍只有裸 `+5` / `-2`，没有自己的目标上下文。用户价值：补齐文件变更列表中 stats 信息的可访问语义，保持与 cc-gui 的文件变更可扫描能力一致。
- 本轮差距诊断（超越 cc-gui）：本项目的 composer strip 是小屏 quick-detail popover，用户会在多个 edit row 之间快速比较变更规模；裸 `+N` / `-N` 被 hover 或读屏单独读取时，无法知道属于哪个文件。用户价值：现在可直接获得 `Edit stats: src/pages/ChatPage.tsx · +5 / -2`，保留高密度扫描，同时让统计信息独立可理解。
- 本轮规划：只改小屏 edit row stats group 的 `title` / `aria-label`、两个 SSR 回归断言、一组 i18n key、前端组件规范和 Trellis 记录；不改 diff 选择、popover 关闭、编辑统计计算、Store/Rust/Tauri response。优先级中等偏高：长会话和多文件编辑中高频出现，用户价值明确，实现成本低，回归面集中在 SSR markup。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.tsx` 新增 `getEditStatsTargetLabel(edit)`，用 `translateWithFallback()` 为 edit stats group 写入 `title` / `aria-label`，可见文本仍保持紧凑 `+N` / `-N`。`src/components/chat/ChatInputStatusTabs.test.tsx` 新增 edits panel 目标标签用例，并扩展 key-only fallback 用例，覆盖 `Edit stats: src/file-1.tsx · +1 / -0` 且不暴露 `chat.layout.inputStatusEditFileStats`。`src/locales/en.json` / `src/locales/zh.json` 新增 `inputStatusEditFileStats`。`.trellis/spec/frontend/component-guidelines.md` 已记录小屏 fallback edit-row stats 必须包含目标文件和增删统计上下文。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 2 项，失败点为 edit row stats 缺少 `title="Edit stats: ..."` 和对应 `aria-label`。GREEN 定向通过：同命令通过（1 file / 25 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/chatStatusSummary.test.ts`（6 files / 79 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在。`git diff --check -- src/components/chat/ChatInputStatusTabs.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/locales/en.json src/locales/zh.json .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。目标扫描确认 `getEditStatsTargetLabel`、`inputStatusEditFileStats`、测试断言和规范条目存在。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx` 也超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察小屏 composer strip edits popover 的 hover/focus/tooltip；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 审计 `ChatInputStatusTabs` edit rows 在 selected/current 状态下的 stats 与 full-diff action label 是否需要进一步区分 `Current full diff` 和普通 stats 的读屏顺序；2) 做真实 Tauri desktop smoke：构造多文件 edits、MCP 配置、task/subagent activity、recent edit tree、history load，点测小屏 popover 和右侧 `StatusPanel` 的 tooltip、焦点顺序和可访问名称；3) 对 `StatusPanel` / `ChatInputStatusTabs` 做短 token 清单收束，核对 remaining count、history load、daemon/MCP diagnostic、edit stats 是否都已带目标；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 todo/files/subagents、MCP 配置、运行状态、状态 pill、文件改动列表/树和小屏 fallback。

## 本轮 PLAN 2026-06-21 small-screen MCP transport target label

- 目标：让 `ChatInputStatusTabs` 小屏 fallback 的 MCP server transport 行与桌面 `StatusPanel` 保持一致，在保留紧凑 `stdio` / `http` 可见文本的同时，通过 `title` / `aria-label` 暴露 `MCP server transport: <server> · <transport>`，避免协议 token 脱离 server 上下文。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx` 和可读 cc-gui `StatusPanel` / MCP 类型；确认本项目小屏 MCP server row 的 status pill 已有 server target label，但 transport token 仍是裸文本。
- 功能点 3：补 RED 测试。验证方式：扩展 `ChatInputStatusTabs.test.tsx` 的 configured MCP server details 与 key-only fallback 用例，断言 transport 文本输出 `MCP server transport: Filesystem · stdio` / `MCP server transport: GitHub · http` 的 `title` / `aria-label`；旧实现应因 transport 行缺少目标化标签失败。
- 功能点 4：实现最小交互修复。验证方式：在 `ChatInputStatusTabs.tsx` 复用现有 `chat.layout.mcpServerTransport` fallback，为 transport 行写入 `title` / `aria-label`；不改 MCP 配置加载、server enabled 逻辑、popover 行为、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录小屏 fallback MCP transport token 也必须暴露 server + transport context。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 MCP/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 small-screen MCP transport target label

- 上轮进展与阻塞：上一轮记录称已完成 `StatusPanel` activity row status pill 的目标化 `title` / `aria-label`；本轮代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 的 task/subagent row status pill 也已经复用 `getToolStatusTargetLabel(tool)`，测试里已有 `npm: npm test · Success` 与 `Agent: Review composer status strip · Pending` 覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 以 todo/files/subagents tabs/popover 展示状态；其 MCP 类型定义明确 transport 为 `stdio` / `http` / `sse` 等连接方式。本项目小屏 `ChatInputStatusTabs` MCP fallback 已列出 server name、transport/config type、enabled/disabled，但 transport 行只是裸 `stdio` / `http` / `Unknown`，不像桌面 `StatusPanel` 已暴露 server + transport context。用户价值：小屏 fallback 与桌面状态面板在同一信息上保持可访问语义一致。
- 本轮差距诊断（超越 cc-gui）：本项目在 composer 上方提供小屏 quick-detail popover，比 cc-gui 更适合窄屏快速查看 MCP 配置；但多个 server row 中重复出现 `stdio` / `http` 时，裸 token 需要用户从相邻 name 倒推归属。用户价值：hover、读屏和自动化检查能直接获得 `MCP server transport: Filesystem · stdio`，不需要依赖视觉邻近关系。
- 本轮规划：只改小屏 MCP server transport 行的 `title` / `aria-label`、两个 SSR 回归断言、前端组件规范和 Trellis 记录；不改 MCP 配置加载、server enabled 逻辑、popover 开关、Store/Rust/Tauri response。优先级中等偏高：用户价值明确、出现频率随 MCP 配置常驻、实现成本低、回归面集中在 SSR markup。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.tsx` 新增 `getMcpServerTransportTargetLabel(server)`，复用现有 `translateWithFallback()` 和 `chat.layout.mcpServerTransport` fallback，为 MCP server row 的 transport 行写入 `title` / `aria-label`；可见文本仍保持 `stdio` / `http` / `Unknown`。`src/components/chat/ChatInputStatusTabs.test.tsx` 扩展 configured MCP server details 与 key-only i18n fallback 用例，覆盖真实 transport 和 unknown fallback。`.trellis/spec/frontend/component-guidelines.md` 已记录小屏 fallback transport/config token 也必须暴露 server + transport context。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 2 项，失败点为 `title="MCP server transport: Filesystem · stdio"` 与对应 `aria-label` 缺失。GREEN 定向通过：同命令通过（1 file / 24 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts`（4 files / 63 tests）。全量前端通过：`npm test`（55 files / 504 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在。`git diff --check -- src/components/chat/ChatInputStatusTabs.tsx src/components/chat/ChatInputStatusTabs.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。目标扫描确认 `getMcpServerTransportTargetLabel`、transport `title` / `aria-label`、测试断言和规范条目存在。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx` 也超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察小屏 composer strip MCP popover 的 hover/focus/tooltip；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 审计 `ChatInputStatusTabs` edit rows 的 `+N` / `-N` 裸数字是否需要 `aria-hidden` 或 stats target label，避免小屏 edit 列表重复读出无上下文数字；2) 做真实 Tauri desktop smoke：构造 MCP 配置、task/subagent/edit activity、recent edits、history load，点测小屏 popover 和右侧 `StatusPanel` 的 tooltip、焦点顺序和可访问名称；3) 对 `StatusPanel` / `ChatInputStatusTabs` 做短 token 清单收束，核对 remaining count、history load、daemon/MCP diagnostic、edit stats 是否都已带目标；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 todo/files/subagents、MCP 配置、运行状态、状态 pill、文件改动列表/树和小屏 fallback。

## 本轮 PLAN 2026-06-21 activity row status pill target label

- 目标：让 `StatusPanel` current activity 历史列表中的 task / subagent row 状态 pill 在保持紧凑 `Success` / `Pending` / `Failed` 可见文本的同时，向辅助技术和 hover tooltip 暴露 `工具类型: 工具目标 · 状态`，避免活动历史里重复短状态词脱离对应任务。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 `renderActivityTaskRow` 与 activity history 测试；确认本项目 row 级 button 已有 jump label，但 row 内 status pill 仍缺少目标化 `title` / `aria-label`。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 recent desktop task list 与 subagent activity 用例，断言 row 内 status pill 输出 `Test: npm test -- StatusPanel · Success`、`Task: write regression coverage · Pending` 的 `title` / `aria-label`；旧实现应因状态 pill 缺少目标化标签失败。
- 功能点 4：实现最小交互修复。验证方式：复用已有 `getToolStatusTargetLabel(tool)`，写入 `renderActivityTaskRow()` 中状态 pill 的 `title` / `aria-label`；不改 row click/jump、任务分组、活动计数、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 activity history row 内 status pill 必须包含工具类型、目标摘要和状态。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 activity row status pill target label

- 上轮进展与阻塞：上一轮记录称已完成 active tool status pill 的目标化 `title` / `aria-label`；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 中存在 `getToolStatusTargetLabel(tool)`，current activity primary active tool 的 `tool-state-pill` 已写入 `title` 与 `aria-label`，对应 `StatusPanel.test.tsx` 断言和 `.trellis/spec/frontend/component-guidelines.md` 规则也存在。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 以 todo/files/subagents tabs/popover 展示活动状态，本项目右侧 `StatusPanel` 已把 recent task/subagent 活动做成可跳转的桌面列表；但 row 内 `Success` / `Pending` 状态 pill 仍缺少独立目标标签。用户价值：在本项目更强的活动历史列表里补齐可访问语义，避免状态词只依赖 row 旁边的视觉摘要。
- 本轮差距诊断（超越 cc-gui）：活动历史列表会同时出现多个 task 和 subagent row，用户经常快速扫描最近的 build/test/edit/agent 结果；旧实现 row button 已有 `Jump to tool task: <summary>`，但内部 pill 单独被读到或 hover 时仍只是 `成功` / `等待中`。用户价值：读屏、tooltip 和自动化检查能直接获得 `Test: npm test -- StatusPanel · Success` 或 `Task: write regression coverage · Pending`，不需要从父 row 推断状态归属。
- 本轮规划：只改 `renderActivityTaskRow()` 中状态 pill 的 `title` / `aria-label`、两个 SSR 回归断言、前端组件规范和 Trellis 记录；不改 row click/jump、任务/子代理分组、活动计数、active tool、Store/Rust/Tauri response。优先级中等：活动历史在长会话中高频出现，实现成本低，回归面集中在 SSR markup。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 复用已有 `getToolStatusTargetLabel(tool)`，并给 `renderActivityTaskRow()` 内的 `tool-state-pill` 写入 `title` / `aria-label`；可见状态文本和 row 级 jump label 保持不变。`src/components/chat/StatusPanel.test.tsx` 扩展 recent desktop task list 与 subagent activity 用例，分别覆盖普通 task row 和 subagent row。`.trellis/spec/frontend/component-guidelines.md` 已记录 activity-history row 内 status pill 也必须包含工具类型、目标摘要和状态。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为 activity row status pill 缺少 `Test: npm test -- StatusPanel · Success` 与 `Task: write regression coverage · Pending` 的目标化标签。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。全量前端通过：`npm test`（55 files / 503 tests，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在。`git diff --check` / `git diff --cached --check` exit 0；前者仅打印 Windows LF/CRLF 转换提示，后者无输出。目标扫描确认 activity row status pill `title` / `aria-label`、测试断言和规范条目存在。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 activity history row 的 hover/focus/tooltip；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke：构造 active tool、recent task、subagent activity、recent edits、MCP/history load，点测右侧状态面板所有短状态 pill 的 tooltip、焦点顺序和可访问名称；2) 对 `ChatInputStatusTabs` 小屏 fallback rows 做同类状态 pill target-label 审计，确认小屏任务/子代理/编辑入口和桌面右栏一致；3) 对 `StatusPanel` 的非状态短 pill 做清单收束，例如 pending/error count、MCP enabled/live status、history load cache/window chips 是否都已带目标；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、状态 pill、文件改动列表/树和小屏 fallback。

## 本轮 PLAN 2026-06-21 active tool status pill target label

- 目标：让 `StatusPanel` current activity 区域的 active tool 状态 pill 在保持紧凑 `Success` / `Pending` / `Failed` 可见文本的同时，向辅助技术和 hover tooltip 暴露 `工具类型: 工具目标 · 状态`，避免当前运行态里只读出孤立状态词。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 current activity 渲染与可读 cc-gui `StatusPanel`；确认本项目 active tool status pill 仍缺少目标化 `title` / `aria-label`。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 current activity 与 key-only i18n fallback 用例，断言 active tool status pill 输出 `aria-label="Bash: npm run build · Pending"`；旧实现应因状态 pill 缺少目标化标签失败。
- 功能点 4：实现最小交互修复。验证方式：在 `StatusPanel.tsx` 中新增 active tool status target label helper，并写入 active tool 状态 pill 的 `title` / `aria-label`；不改 activity 数据、任务选择、计数、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 current activity active tool status pill 必须包含工具类型、目标摘要和状态。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 active tool status pill target label

- 上轮进展与阻塞：上一轮记录称已完成 recent edit tree 文件状态 pill 的目标化 `title` / `aria-label`；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 中存在 `getEditStatusTargetLabel(edit)`，recent edit file row 的 `tool-state-pill` 已写入 `title` 与 `aria-label`，对应 `StatusPanel.test.tsx` 断言和 `.trellis/spec/frontend/component-guidelines.md` 规则也存在。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 以 todo/files/subagents tabs/popover 展示运行状态，本项目 `StatusPanel` current activity 已经比 cc-gui 更集中地展示 active tool、pending/error counts、recent task/subagent list；但 active tool 状态 pill 仍只有短文本 `Pending` / `等待中`，缺少独立目标标签。用户价值：继续用真实代码确认 parity 边界，并把本项目更强的当前活动摘要补齐到可访问层。
- 本轮差距诊断（超越 cc-gui）：当前活动区是用户等待长任务时最常看的运行态入口；旧实现中 active tool 的 `Bash` chip、`npm run build` 摘要和 `Pending` pill 视觉相邻，但 pill 单独被读到或 hover 时仍缺少目标。用户价值：读屏、tooltip 和自动化检查能直接获得 `Bash: npm run build · Pending`，用户不用从相邻视觉文本倒推状态属于哪个工具。
- 本轮规划：只改 active tool status pill 的 `title` / `aria-label`、两个 SSR 回归断言、前端组件规范和 Trellis 记录；不改 activity 数据、任务选择、pending/error 计数、活动历史行、Store/Rust/Tauri response。优先级中等：当前活动区在长任务中高频出现，实现成本低，回归面集中在 SSR markup。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `getToolStatusTargetLabel(tool)`，复用现有 `getToolStatusLabel()`，并给 active tool 的 `tool-state-pill` 写入 `title` / `aria-label`；可见状态文本仍保持 `Success` / `Pending` / `Failed` 或对应中文资源。`src/components/chat/StatusPanel.test.tsx` 扩展 current activity 与 key-only i18n fallback 用例，覆盖默认资源和英文 fallback。`.trellis/spec/frontend/component-guidelines.md` 已记录 active tool status pill 必须包含工具类型、目标摘要和状态。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为 active tool status pill 缺少 `Bash: npm run build · Pending` 的目标化标签。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。全量前端通过：`npm test`（55 files / 503 tests，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在。`git diff --check` / `git diff --cached --check` exit 0；前者仅打印 Windows LF/CRLF 转换提示，后者无输出。目标扫描确认 `getToolStatusTargetLabel`、active tool `title` / `aria-label`、测试断言和规范条目存在。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 current activity 的 hover/focus/tooltip；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 审计 `StatusPanel` activity task/subagent row 内部状态 pill 是否也需要同类 `工具目标 · 状态` 的独立 target label，避免活动历史列表里的 `Success` / `Pending` / `Failed` 脱离 row 目标；2) 做真实 Tauri desktop smoke：构造 active tool pending/error、recent tasks、subagent activity、recent edits、MCP 状态，点测右侧状态面板的焦点顺序、tooltip 和可访问名称；3) 对 `StatusPanel` 做短状态 pill 清单收束，核对 current activity、activity history、MCP、history load、daemon、runtime context、recent edits 是否都已有 target label；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、状态 pill、文件改动列表/树和小屏 fallback。

## 本轮 PLAN 2026-06-21 recent edit status pill target label

- 目标：让 `StatusPanel` recent edit tree 的文件状态 pill 在保持紧凑 `Success` / `Pending` / `Failed` 可见文本的同时，向辅助技术暴露 `文件路径: 状态` 的目标化 `aria-label`，避免文件树中重复短状态词脱离文件上下文。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 recent edit tree 渲染与可读 cc-gui `StatusPanel` / `FileChangesList`；确认 cc-gui 文件变更列表已有文件行状态符，本项目文件树更强但状态 pill 缺少目标化 accessible name。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` recent edit tree 与 key-only i18n fallback 用例，断言 file status pill 输出 `aria-label="src/components/chat/StatusPanel.tsx: Success"` 或中英文等价；旧实现应因状态 pill 缺少 `aria-label` 失败。
- 功能点 4：实现最小交互修复。验证方式：在 `StatusPanel.tsx` 中新增 recent edit status target label helper，并写入 file row 状态 pill；不改 edit tree 数据、文件选择、diff hover/open-file、状态计算、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 recent edit tree file status pill 必须包含目标文件路径和状态。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 recent edit status pill target label

- 上轮进展与阻塞：上一轮记录称已完成 runtime context tile value 的目标化 `aria-label`；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 中存在 `getRuntimeContextValueLabel()` 与 `sdkStatusValueLabel`，model / permission mode / reasoning / workspace / SDK tile value 已写入 `aria-label`，对应 `StatusPanel.test.tsx` 断言也存在。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/FileChangesList.tsx` 文件变更列表使用 A/M 状态符、文件名、diff 和 undo 操作；本项目 `StatusPanel` recent edit tree 已经提供文件夹树、diff hover、中心 diff 选择和 open-file，但文件行里的 `Success` / `Pending` / `Failed` 状态 pill 缺少独立目标标签。用户价值：继续基于真实代码确认 parity 边界，避免把本项目更强文件树的局部 a11y 缺口误判为 cc-gui 结构缺失。
- 本轮差距诊断（超越 cc-gui）：recent edit tree 中多个文件行会重复出现短状态 pill；旧实现的 row 本身有 `Inspect full diff: <file>`，但状态 pill 单独被读到或鼠标悬停时仍只是 `Success` / `Failed`。用户价值：读屏、tooltip 和自动化可访问性检查能直接获得 `src/components/chat/StatusPanel.tsx: Success`，用户不用从相邻视觉文本倒推状态属于哪个文件。
- 本轮规划：只改 recent edit file row 的状态 pill `title` / `aria-label`、两个 SSR 回归断言、前端组件规范和 Trellis 记录；不改 edit tree 数据、文件选择、diff hover/open-file、状态计算、Store/Rust/Tauri response。优先级中等：文件树在长历史和多文件修改中高频出现，实现成本低，回归面集中在 SSR markup。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `getEditStatusTargetLabel(edit)`，复用现有 `getStatusLabel()`，并给 recent edit file row 的 `tool-state-pill` 写入 `title` / `aria-label`，可见文本仍保持 `Success` / `Pending` / `Failed`。`src/components/chat/StatusPanel.test.tsx` 扩展 key-only i18n fallback 与 edit tree 渲染用例，覆盖英文 fallback 与默认中英文资源。`.trellis/spec/frontend/component-guidelines.md` 已记录 recent edit tree file-row status pill 必须包含文件路径和状态。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为 recent edit status pill 缺少 `aria-label="src/components/chat/StatusPanel.tsx: Success"` 或中英文等价。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。全量前端通过：`npm test`（55 files / 503 tests，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在。`git diff --check` / `git diff --cached --check` exit 0；前者仅打印 Windows LF/CRLF 转换提示，后者无输出。目标扫描确认 `getEditStatusTargetLabel`、`editStatusTargetLabel`、测试断言和规范条目存在。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 recent edit tree 的 hover/focus/tooltip；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 审计 `StatusPanel` active tool status pill 和 activity task row status pill 是否也需要同类 target label，避免 `Success` / `Pending` / `Failed` 在当前任务区脱离工具目标；2) 做真实 Tauri desktop smoke：构造多文件 edits、pending/error edits、hover diff、open-file、中心 diff selection，点测 recent edit tree 的焦点顺序、tooltip 和可访问名称；3) 对 `StatusPanel` 做一次短状态 pill 清单收束，核对 current activity、MCP、history load、daemon、runtime context、recent edits 是否都已有 target label；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看文件改动列表/树、diff 入口、状态 pill、undo/keep/discard 边界和小屏 fallback。

## 本轮 PLAN 2026-06-21 runtime context value target labels

- 目标：让 `StatusPanel` runtime context tile 的值在保持紧凑截断展示与原有 `title` 的同时，向辅助技术暴露 `Model: ...`、`Permission mode: ...`、`Workspace: ...`、`SDK: ...` 这类目标化 `aria-label`，避免密集 tile 中只读出孤立值。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 runtime context 渲染与测试；读取可访问的 cc-gui `StatusPanel`，确认 cc-gui 没有同构 runtime context tile grid，本轮属于本项目增强项。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 runtime context 与 key-only i18n fallback 用例，断言 model/mode/reasoning/workspace/SDK value 输出目标化 `aria-label`；旧实现应因 tile value 缺少 `aria-label` 失败。
- 功能点 4：实现最小交互修复。验证方式：在 `StatusPanel.tsx` 中新增 `getRuntimeContextValueLabel()` 与 `sdkStatusValueLabel`，并写入 model、permission mode、reasoning、workspace、SDK 五个 runtime value；不改 runtime context 数据来源、显示顺序、SDK tooltip、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 runtime context tile value 必须带 `字段名: 值` 的 target label。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 runtime context value target labels

- 上轮进展与阻塞：上一轮完成 `StatusPanel` daemon status value 的目标化 `aria-label`，代码中确实存在 `daemonStatusAriaLabel` 并写入 daemon 状态 span；本轮复查确认此前 History load status pill、MCP server row、current activity count、recent edit overflow、daemon reconnect、MCP/history details toggle、open-file 冒泡边界等记录项仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 运行态并排截图，以及 IDE build 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 以 todo/files/subagents tabs/popover 展示状态，没有本项目这种 provider/model/mode/reasoning/workspace/SDK runtime context tile grid，也没有同构 tile value accessible-name 缺口可直接补齐。用户价值：继续用真实代码划清 parity 边界，不把本项目运行上下文增强误判为 cc-gui 原能力。
- 本轮差距诊断（超越 cc-gui）：本项目 runtime context tile 会展示模型、权限模式、推理强度、工作区和 SDK 状态，其中模型 id、Windows 工作区路径和 SDK 名称常被视觉截断；旧实现只有可见值与 `title`，辅助技术上下文里可能只读出 `claude-sonnet-4-6`、`Auto Mode`、`C:/...`。用户价值：键盘与读屏用户能直接获得 `Model: claude-sonnet-4-6`、`Workspace: C:/guodevelop/ccg-switch`、`SDK: Claude Code SDK · Installed`，不必从视觉 tile label 反推值的含义。
- 本轮规划：只改 runtime context value 的 `aria-label`、两个 SSR 回归断言、前端组件规范和 Trellis 记录；不改 runtime context 数据来源、SDK installed 计算、tile 布局、tooltip、Store/Rust/Tauri response。优先级中等：运行上下文在发送前和排障时高频出现，实现成本低，风险集中在 SSR。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `getRuntimeContextValueLabel()` 与 `sdkStatusValueLabel`，model / permission mode / reasoning / workspace / SDK 五个 runtime tile value 现在输出 `字段名: 值` 的 `aria-label`；SDK tile 仍保留安装路径作为 `title`。`src/components/chat/StatusPanel.test.tsx` 扩展 runtime context 与 key-only i18n fallback 用例，覆盖默认资源和英文 fallback。`.trellis/spec/frontend/component-guidelines.md` 已记录 runtime context tile value 应带 target label。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为 runtime context tile values 缺少 `aria-label="Model: claude-sonnet-4-6"` 等目标化标签。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。后端快速检查通过：`cargo check --manifest-path src-tauri/Cargo.toml`。全量前端通过：`npm test`（55 files / 503 tests，仅既有 Browserslist/caniuse-lite 过期提示）。后端测试通过：`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。IDE `build_project` 120 秒超时，未返回编译结果。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在。`git diff --check` / `git diff --cached --check` exit 0；前者仅打印 Windows LF/CRLF 转换提示，后者无输出。
- 未验证项：未启动真实 Tauri 桌面观察 runtime context tile hover/focus；未手动确认 runtime context tile value 在 Accessibility tree 或屏幕阅读器中的实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 审计 `StatusPanel` 里的 active tool status pill、recent edit tree status pill 是否需要同类 target label，避免 `Success` / `Failed` 在文件树或当前任务行里脱离目标；2) 做真实 Tauri desktop smoke：切换模型/权限模式/推理强度/工作区/SDK 安装状态，点测 runtime context tile 的截断、tooltip、焦点顺序和读屏名称；3) 对 ToolBlock + StatusPanel 做一次运行态可访问性扫描，核对所有短状态 pill、自定义 row、数字 pill、图标按钮的 `title/aria-label/disabled/onKeyDown`；4) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 daemon status target label

- 目标：让 `StatusPanel` 顶部 daemon 状态值在保持短状态文案的同时，向辅助技术暴露 `Daemon: Success/Daemon offline/Daemon error` 这类目标化 `aria-label`，避免密集 key/value 行中只读出孤立状态词。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 daemon status 渲染与测试；读取可访问的 cc-gui `StatusPanel` / `FileChangesList`，确认 cc-gui 状态面板没有同构 daemon runtime row，本轮属于本项目增强项。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 key-only i18n fallback 与 daemon offline 用例，断言状态值输出 `aria-label="Daemon: Daemon offline"` 或中文等价；旧实现应因状态 span 缺少 `aria-label` 失败。
- 功能点 4：实现最小交互修复。验证方式：在 `StatusPanel.tsx` 中新增 `daemonStatusAriaLabel`，复用 `daemonLabel` 与 `daemonStatusText`，并写入 daemon 状态值 span；不改 daemon 状态判断、诊断 tooltip、重连按钮、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录桌面 `StatusPanel` daemon 状态值需要 `Daemon: <status>` 目标化 accessible name。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 daemon status target label

- 上轮进展与阻塞：上一轮完成 `StatusPanel` History load status pill 的目标化 `title` / `aria-label`，代码中确实存在 `sessionLoadStatusAriaLabel` 并写入状态 pill；本轮复查也确认此前 MCP server row、current activity count、recent edit overflow、daemon reconnect、MCP/history details toggle、open-file 冒泡边界等记录项仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 运行态并排截图，以及 IDE build 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 以 todo/files/subagents tabs/popover 展示状态，没有本项目这种 daemon runtime key/value row，也没有同构 daemon 状态值 accessible-name 缺口可直接补齐。用户价值：继续用真实代码划清 parity 边界，不把本项目 runtime 诊断增强误判为 cc-gui 原能力。
- 本轮差距诊断（超越 cc-gui）：本项目右侧状态面板会在 `Daemon` 行显示 `Success`、`守护进程离线`、`Daemon error` 这类短状态值；旧实现只有可见短文本和 diagnostic/status tooltip，辅助技术上下文里容易脱离 `Daemon` 这个目标。用户价值：键盘与读屏用户能直接获得 `Daemon: Daemon offline` / `守护进程: 守护进程离线`，在高密度状态面板中更快判断运行时是否需要恢复。
- 本轮规划：只改 daemon 状态值 span 的 `aria-label`、两个 SSR 回归断言、前端组件规范和 Trellis 记录；不改 daemon 状态分类、诊断文案、重连动作、运行时 context、Store/Rust/Tauri response。优先级中等：daemon 状态是高频运行健康信号，实现成本极低，回归面集中。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `daemonStatusAriaLabel`，复用 `daemonLabel` + `daemonStatusText`，并写入 daemon 状态值 span 的 `aria-label`；可见状态文本与原有 tooltip 保持不变。`src/components/chat/StatusPanel.test.tsx` 扩展 key-only i18n fallback 与 daemon offline 用例，覆盖英文 fallback 与中文默认资源。`.trellis/spec/frontend/component-guidelines.md` 记录 desktop `StatusPanel` daemon status value 应暴露 `Daemon: <status>`。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为 daemon 状态值缺少 `aria-label="Daemon: Daemon offline"` / 中文等价。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。全量前端通过：`npm test`（55 files / 503 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored；并行执行时出现 Cargo package cache/build directory 文件锁等待后正常完成）。IDE `build_project` 120 秒超时，未返回编译结果。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在。`git diff --check` / `git diff --cached --check` exit 0；前者仅打印 Windows LF/CRLF 转换提示，后者无输出。
- 未验证项：未启动真实 Tauri 桌面观察 daemon online/offline/error 状态；未手动确认 daemon 状态值在 Accessibility tree 或屏幕阅读器中的实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 继续审计 `StatusPanel` runtime context value / SDK status value 是否需要同类 target label，尤其是 `SDK: Claude Code SDK · Installed` 这种密集 tile 值；2) 真实 Tauri desktop smoke：触发 daemon offline/error/reconnecting，点测状态值、诊断 tooltip、重连按钮、焦点顺序和读屏名称；3) 对 ToolBlock + StatusPanel 做一次运行态可访问性扫描，核对所有短状态 pill、自定义 row、数字 pill、图标按钮的 `title/aria-label/disabled/onKeyDown`；4) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 history load status pill target label

- 目标：让 `StatusPanel` History load 诊断卡片的状态 pill 暴露 `History load: Loading/Complete/Load failed` 这类目标化 `title` / `aria-label`，避免短状态词在密集诊断行中脱离上下文。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 `sessionLoadMetrics` 渲染与对标项目可读 `StatusPanel`；确认 cc-gui 没有同构 History load metrics row，本轮属于本项目增强项。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 completed history load 与 readable fallback 用例，断言状态 pill 输出 `aria-label="History load: Complete"`、`History load: Loading`、`History load: Load failed`；旧实现应因状态 pill 缺少 `aria-label` 失败。
- 功能点 4：实现最小交互修复。验证方式：在 `StatusPanel.tsx` 中新增 `sessionLoadStatusAriaLabel`，复用 `sessionLoadMetricsLabel` 和 `sessionLoadStatusLabel`，并写入 session-load 状态 pill 的 `title` / `aria-label`；不改 session load metrics 数据、展开逻辑、缓存/窗口判断、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 session-load status pill 必须带目标化状态标签。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 history load status pill target label

- 上轮进展与阻塞：上一轮完成 `StatusPanel` MCP server row 状态 pill 的目标化 `aria-label` / `title`，代码中确实存在 `getMcpServerStateLabel()`，MCP live result 暴露 `filesystem: Online · 24ms`，enabled/disabled 暴露 `filesystem: Enabled` / `browser: Disabled`。更早的 current activity count、recent edit overflow、daemon reconnect、MCP live-check、MCP/history details toggle、recent edit diff keyboard helper、open-file 冒泡边界、ToolBlock/group action label 也仍在当前代码中存在。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 运行态并排截图，以及 IDE build 120 秒超时未返回结果。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读实现没有本项目这种 History load metrics 诊断 row，因此本轮没有直接 cc-gui 控件缺口可补齐。用户价值：继续把对标限定在真实代码，不把本项目历史加载性能诊断误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 History load card 会显示 `Loading`、`Complete`、`Load failed` 这类短状态 pill；旧实现只有可见短文本，缺少 `title` / `aria-label`，聚焦或辅助技术上下文里容易脱离 `History load` 这个目标。用户价值：用户能直接获得 `History load: Loading` / `History load: Complete` / `History load: Load failed`，在密集状态面板中更快判断历史会话加载是否完成、仍在窗口首屏阶段还是失败。
- 本轮规划：只改 History load status pill 的 `title` / `aria-label`、两个 SSR 回归、前端组件规范和 Trellis 记录；不改 session load metrics 数据、展开/收起逻辑、缓存/窗口判断、Store/Rust/Tauri response。优先级中等：历史加载诊断在长会话高频出现，实现成本低，回归面集中。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `sessionLoadStatusAriaLabel`，复用 `sessionLoadMetricsLabel` + `sessionLoadStatusLabel`，并写入 session-load 状态 pill 的 `title` / `aria-label`；可见 pill 文案保持紧凑不变。`src/components/chat/StatusPanel.test.tsx` 扩展 completed history load 与 readable fallback 用例，覆盖 complete/loading/error 三种状态。`.trellis/spec/frontend/component-guidelines.md` 已记录 session-load status pill 应带 `History load: ...` 目标标签。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为 History load 状态 pill 缺少 `aria-label="History load: Complete"` / `History load: Loading`。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。全量前端通过：`npm test`（55 files / 503 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未返回编译结果。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在。`git diff --check` / `git diff --cached --check` exit 0；前者仅打印 Windows LF/CRLF 转换提示，后者无输出。
- 未验证项：未启动真实 Tauri 桌面加载长历史会话；未手动确认 History load status pill tooltip、Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 继续审计 `StatusPanel` 中 daemon status 文本、runtime context value、SDK status value 是否需要同类 target label 或更明确 region 语义；2) 真实 Tauri desktop smoke：加载长历史会话，点测 History load windowed/loading/cache/error 状态、详情展开、tooltip、焦点顺序和读屏名称；3) 对 ToolBlock + StatusPanel 做运行态可访问性扫描，核对所有紧凑按钮、自定义 row、数字 pill、状态 pill 的 `title/aria-label/disabled/onKeyDown`；4) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 mcp server status pill target labels

- 目标：让 `StatusPanel` MCP details 中每个服务器行的 live result / enabled / disabled 状态 pill 暴露“服务器名 + 状态”的 `aria-label` / `title`，避免一屏多个短状态 pill 脱离视觉行上下文后只剩 `Online`、`Enabled`、`Disabled`。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 MCP server row 与对标项目可读 `StatusPanel` / `FileChangesList`；确认 cc-gui 没有同构 MCP server row，本轮属于本项目增强项。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 MCP live result 与 fallback 用例，断言 live result 输出 `aria-label="filesystem: Online · 24ms"`，enabled/disabled pill 输出 `filesystem: Enabled` / `browser: Disabled`；旧实现应因缺少这些 `aria-label` 失败。
- 功能点 4：实现最小交互修复。验证方式：在 `StatusPanel.tsx` 中新增 `getMcpServerStateLabel()`，MCP live result pill 使用 server name + live status + latency 作为 `aria-label`，enabled/disabled pill 使用 server name + enabled state 作为 `title` / `aria-label`；不改 MCP 配置、live check 数据、按钮 disabled、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 MCP server-row 状态 pill 应尽量包含服务器名。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 mcp server status pill target labels

- 上轮进展与阻塞：上一轮完成 `StatusPanel` current activity pending/error 数字 pill 的 label + count `aria-label`，代码中确实存在 `pendingToolCountLabel` / `errorToolCountLabel` 并写入数字 pill。更早的 recent edit overflow label、daemon reconnect、MCP live-check、MCP/history details toggle、recent edit diff keyboard helper、open-file 冒泡边界、ToolBlock/group action label 也仍在当前代码中存在。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 运行态并排截图，以及 IDE build 120 秒超时未返回结果。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `StatusPanel` / `FileChangesList` 没有本项目这种 MCP server row 和 live-check 结果，因此本轮没有直接 cc-gui 控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目 MCP 诊断面板误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 MCP details 会在多服务器列表中展示 `Online` / `Enabled` / `Disabled` 这类短状态 pill；旧实现中 live result 只有 latency `title`，enabled/disabled pill 只有可见短文本。用户价值：tooltip 和辅助技术用户能获得 `filesystem: Online · 24ms`、`browser: Disabled` 这类目标化状态，不需要从视觉行位置把状态和服务器名重新配对。
- 本轮规划：只改 MCP server-row 状态 pill 的 `aria-label` / `title`、两个 SSR 回归断言、前端组件规范和 Trellis 记录；不改 MCP 状态计算、live check 结果、服务器列表渲染结构、Store/Rust/Tauri response。优先级中等：MCP 诊断是高频故障排查入口，实现成本低，回归面集中在 SSR。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `getMcpServerStateLabel()`；MCP live result pill 现在输出 `aria-label` 为 server name + live status + latency，例如 `filesystem: Online · 24ms`；enabled/disabled pill 现在输出 `title` / `aria-label` 为 server name + enabled state，例如 `filesystem: Enabled` / `browser: Disabled`。`src/components/chat/StatusPanel.test.tsx` 扩展 MCP live result 与 fallback 用例，覆盖英文和当前中文文案。`.trellis/spec/frontend/component-guidelines.md` 已记录 MCP server-row 状态 pill 应包含服务器名。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为 MCP live result / enabled / disabled 状态 pill 缺少包含服务器名的 `aria-label`；修正中文断言后 GREEN 定向通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。全量前端通过：`npm test`（55 files / 503 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未返回编译结果。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在。`git diff --check` / `git diff --cached --check` exit 0；前者仅打印 Windows LF/CRLF 转换提示，后者无输出。
- 未验证项：未启动真实 Tauri 桌面打开 MCP details；未手动确认 MCP server row tooltip、Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 继续审计 `StatusPanel` 中 session load status pill、daemon status 文本、runtime context value 是否需要同类 target label 或 region 语义；2) 真实 Tauri desktop smoke：打开 MCP details，点测 live-check 空闲/检查中/成功/失败、服务器状态 pill、tooltip、焦点顺序和读屏名称；3) 对 ToolBlock + StatusPanel 做运行态可访问性扫描，核对所有紧凑按钮、自定义 row、数字 pill、状态 pill 的 `title/aria-label/disabled/onKeyDown`；4) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 current activity count pill aria labels

- 目标：让 `StatusPanel` 当前活动卡片右上角的 pending/error 数字 pill 在保持紧凑数字外观的同时，向辅助技术暴露“含义 + 数量”，避免只读出 `3` / `1` 这种无上下文数字。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 中 current activity count pill；确认 cc-gui 可读 `StatusPanel` / `FileChangesList` 没有同构右侧 current activity count pill，本轮属于本项目增强项。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 current activity count 用例，断言 pending/error 数字 pill 输出 `aria-label="Pending tools: 3"` / `aria-label="Failed tools: 1"`；旧实现应因只有 `title` 且无 `aria-label` 失败。
- 功能点 4：实现最小交互修复。验证方式：在 `StatusPanel.tsx` 新增 `pendingToolCountLabel` / `errorToolCountLabel`，并写入数字 pill 的 `aria-label`；不改可见文本、状态计数、活动列表、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 numeric-only current activity count pill 必须用 `aria-label` 暴露 label + count。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 current activity count pill aria labels

- 上轮进展与阻塞：上一轮完成 `StatusPanel` 最近编辑隐藏文件切换按钮的状态化可见文本 / `title` / `aria-label`，代码中确实存在 `hiddenEditFilesActionLabel`，折叠态暴露隐藏文件数量，展开态暴露返回最近编辑列表动作。更早的 daemon reconnect、MCP live-check、MCP/history details toggle、recent edit diff keyboard helper、open-file 冒泡边界、ToolBlock/group action label 也仍在当前代码中存在。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 运行态并排截图，以及 IDE build 120 秒超时未返回结果。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `StatusPanel` / `FileChangesList` 没有本项目这种右侧 current activity header 数字 count pill，因此本轮没有直接 cc-gui 控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目增强型活动面板误判为 cc-gui 已有控件。
- 本轮差距诊断（超越 cc-gui）：本项目当前活动卡片在右上角用紧凑数字 pill 表示 pending/error 工具数量，视觉用户能结合颜色和 hover `title` 理解，但辅助技术可能只获得孤立数字。用户价值：键盘焦点和读屏用户能听到 `Pending tools: 3` / `Failed tools: 1`，不用从颜色、位置或相邻标题反推数字含义。
- 本轮规划：只改 current activity count pill 的 `aria-label`、一个 SSR 回归、前端组件规范和 Trellis 记录；不改可见文本、tooltip、状态计数、活动列表、Store/Rust/Tauri response。优先级中等：该信息只在存在 pending/error 工具时出现，但实现成本极低，且与近期 StatusPanel a11y 主题一致。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `pendingToolCountLabel` / `errorToolCountLabel`，并分别用于 pending/error 数字 pill 的 `aria-label`，可见数字和既有 `title` 保持不变。`src/components/chat/StatusPanel.test.tsx` 的 current activity count 用例新增 pending/error `aria-label` 回归断言。`.trellis/spec/frontend/component-guidelines.md` 已记录 numeric-only current-activity count pill 必须暴露 label + count。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 在实现前已失败 1 项，失败点为 pending/error 数字 pill 只有 `title`，缺少 `aria-label="Pending tools: 3"` / `aria-label="Failed tools: 1"`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。全量前端通过：`npm test`（55 files / 503 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未返回编译结果。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在。`git diff --check` / `git diff --cached --check` exit 0；前者仅打印 Windows LF/CRLF 转换提示，后者无输出。
- 未验证项：未启动真实 Tauri 桌面触发 pending/error count pill；未手动确认 hover tooltip、Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 pending/error 工具活动后点测 current activity count pill、任务/子代理滚动区域、recent edits、MCP details、daemon reconnect 的 tooltip、焦点顺序和读屏名称；2) 继续审计 `StatusPanel` 中非按钮状态 pill，例如 MCP live result / server enabled pill / session load status pill 是否需要 `aria-label` 或更明确的 region 语义；3) 对 ToolBlock + StatusPanel 做一次运行态可访问性扫描，核对所有紧凑按钮、自定义 row、数字 pill 和状态 pill 的 `title/aria-label/disabled/onKeyDown`；4) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-20 status panel diagnostics toggle labels

- 目标：补齐 `StatusPanel` 中 MCP 详情与 History load 详情折叠按钮的 action-specific `title` / `aria-label`，让辅助技术和 tooltip 用户能直接听到/看到“展开或收起哪个状态详情”，而不是只依赖可见文本与 `aria-expanded` 推断。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend specs、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：核对本项目 `StatusPanel` MCP / session-load toggles 与 cc-gui `StatusPanel/FileChangesList` 的状态面板控件语义；确认本轮只补状态详情 toggle 的 accessible name，不改状态数据、MCP live check、session load metrics、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，断言 collapsed / expanded MCP toggle 与 History load toggle 均有 action-specific `aria-label`；旧实现应因缺少 `aria-label` 失败。
- 功能点 4：实现最小修复。验证方式：在 `src/components/chat/StatusPanel.tsx` 中用 `translateWithFallback()` 生成 expand/collapse MCP details 与 history load details 标签，并同时写入对应按钮的 `title` / `aria-label`。
- 功能点 5：同步 i18n 与 Trellis 规范。验证方式：补 `src/locales/en.json` / `src/locales/zh.json` 对应 key；更新 `.trellis/spec/frontend/component-guidelines.md`，记录状态面板内可折叠诊断控件需要 action-specific 标签。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat 状态面板测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-20 status panel diagnostics toggle labels

- 上轮进展与阻塞：上一轮完成 `StatusPanel` 最近改动树 open-file 按钮的 keydown 冒泡边界，代码中确实存在 `.status-edit-tree-open` 的 `onKeyDown(event.stopPropagation())`，并有 `StatusPanelOpenFilePropagation.test.tsx` 回归覆盖。已知阻塞仍是真实 Tauri 桌面键盘 smoke、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 运行态并排截图，以及 IDE build 可能超时。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `FileChangesList` 的文件变更操作按钮至少提供 `title`，状态面板用户能从 hover/控件语义了解操作意图；本项目右侧 `StatusPanel` 的 MCP 与 History load 折叠按钮此前只有可见文本和 `aria-expanded`，缺少直接说明“展开/收起哪个详情”的 action-specific 名称。用户价值：键盘、tooltip 和辅助技术用户可以在聚焦控件时直接理解操作，不必从当前展开状态和旁边摘要反推。
- 本轮差距诊断（超越 cc-gui）：cc-gui 状态面板 tab 仍有 click-driven `div` 模式；本项目继续把状态诊断入口收敛为原生 button，并为 MCP / History load 这种诊断 toggle 补充明确 `title` / `aria-label`。用户价值：右侧状态面板的信息密度更高时，用户仍能快速判断当前按钮会展开配置细节、收起诊断细节，减少误点和重复展开。
- 本轮规划：只补 `StatusPanel` MCP toggle 与 session-load toggle 的 action-specific `title` / `aria-label`、对应中英文 i18n key、SSR 回归测试和 Trellis 规范；不改状态数据、MCP live check、session load metrics、Store/Rust/Tauri response。优先级中等：用户价值集中在高频诊断入口和可访问性，出现频率高，实现成本低，回归面可由现有 StatusPanel 测试覆盖。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 新增 4 个 RED/GREEN 断言，覆盖 collapsed / expanded MCP toggle 与 History load toggle 的 action-specific `aria-label`；`src/components/chat/StatusPanel.tsx` 用 `translateWithFallback()` 生成 `Expand/Collapse MCP details` 与 `Expand/Collapse history load details`，并同步写入按钮 `title` / `aria-label`；`src/locales/en.json`、`src/locales/zh.json` 增加对应中英文 key；`.trellis/spec/frontend/component-guidelines.md` 记录状态面板诊断 toggle 必须有明确 action label。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 4 项，失败点均为 MCP / History load toggle 缺少对应 `aria-label`。GREEN 定向通过：同命令通过（1 file / 33 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/utils/chatStatusSummary.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolPresentation.test.ts`（9 files / 113 tests）。全量前端通过：`npm test`（55 files / 500 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。`dist` 已清理，`out` 不存在。`git diff --check` / `git diff --cached --check` 退出码 0；前者仅打印既有 LF/CRLF 提示，后者无输出。
- 未验证项：未启动真实 Tauri 桌面 hover/focus MCP 与 History load toggle；未用屏幕阅读器确认新增 action label 实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 对 `StatusPanel` 里其他原生折叠/诊断按钮做同类 action-label 审计，例如 recent edit tree expand/collapse、diff view toggle、reopen diff pane、daemon reconnect、MCP live check 是否全部有完整 `title` / `aria-label`；2) 做一次 Tauri desktop keyboard smoke，覆盖右侧状态面板所有可聚焦控件的 Tab 顺序、Enter/Space 行为和 tooltip 文案；3) 把状态面板可访问性规则整理为一组专门的 SSR 测试清单，减少后续新增诊断卡片时遗漏 `aria-label` / fallback 文案。

## 本轮 PLAN 2026-06-20 status panel edit open file keydown boundary

- 目标：补齐 `StatusPanel` 最近改动树文件行内部 open-file 按钮的 `keydown` 冒泡边界，避免用户在打开文件按钮上按 Enter/Space 时，同时触发父级文件行的 inspect diff 选择逻辑。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend specs、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：核对本项目 `StatusPanel` 最近改动树、现有 StatusPanel 测试、上一轮 tool group open-file propagation 测试与 cc-gui `StatusPanel`；确认本轮只处理最近改动树 open-file 按钮 keydown 冒泡，不改可见 UI、diff 选择逻辑、文件打开参数、状态聚合、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：新增 `src/components/chat/StatusPanelOpenFilePropagation.test.tsx`，直接渲染 StatusPanel React element，断言 `.status-edit-tree-open` 的 `onKeyDown` 会调用 `stopPropagation()`；旧实现应因缺少 `onKeyDown` 失败。
- 功能点 4：实现最小修复。验证方式：在 `src/components/chat/StatusPanel.tsx` 的最近改动树 open-file 按钮上补 `onKeyDown={(event) => event.stopPropagation()}`，保留现有 `onClick`、`handleOpenEditedFile(...)` 参数、diff row 选择和 hover preview 行为。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，明确 status-panel 最近改动树中嵌套在键盘可选文件行内的 open-file 控件，也必须同时阻止 click 与 keydown 冒泡。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/tool-block 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-20 status panel edit open file keydown boundary

- 上轮进展与阻塞：上一轮完成 `SearchToolGroupBlock` 展开详情文件行的 keydown 冒泡边界，代码中确实存在 `.search-result-file-row` 的 `onKeyDown(event.stopPropagation())`，并有 `ToolGroupOpenFilePropagation.test.tsx` 回归覆盖。已知阻塞仍是真实 Tauri 桌面键盘 smoke、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 运行态并排截图，以及 IDE build 可能超时。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 以 tabs/popover 方式呈现 todo/files/subagents，并通过 JetBrains diff bridge 处理文件变更；未看到与本项目完全同构的“右侧最近改动树文件行可键盘选择 + 内嵌 open-file 按钮”实现。用户价值：基于真实代码校准对标边界，不把本项目增强型 status tree 误报成 cc-gui 原有能力。
- 本轮差距诊断（超越 cc-gui）：本项目 `StatusPanel` 最近改动树的文件行本身是 `role="button"` 且有 `onKeyDown(handleEditedFileKeyDown)`，内部 open-file 按钮只阻止 click 冒泡。用户在该按钮上按 Enter/Space 打开文件时，事件可能继续冒泡到父级文件行并触发 inspect diff 选择。用户价值：键盘用户在右侧“最近改动”里打开源文件时，不会同时切换/重开中央 diff 检查上下文，减少误操作和焦点跳动。
- 本轮规划：只补 `StatusPanel` 最近改动树 open-file 按钮的 `onKeyDown(stopPropagation)`、新增独立回归测试、同步 Trellis 规范；不改可见 UI、diff 选择逻辑、hover preview、`openFile()` 参数、状态聚合、Store/Rust/Tauri response。优先级中等偏高：右侧最近改动是高频审阅路径，实现成本极低，风险集中且可用纯 React element 测试固定。
- 本轮完成：`src/components/chat/StatusPanelOpenFilePropagation.test.tsx` 新增回归测试，用可控 React hooks mock 直接渲染 StatusPanel element，并断言 `.status-edit-tree-open` 的 `onKeyDown` 调用 `stopPropagation()`；`src/components/chat/StatusPanel.tsx` 在最近改动树 open-file 按钮上补 `onKeyDown={(event) => event.stopPropagation()}`；`.trellis/spec/frontend/component-guidelines.md` 记录 status-panel recent edit tree 的嵌套 open-file 控件必须同时阻止 click 与 keydown 冒泡。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanelOpenFilePropagation.test.tsx` 先失败 1 项，失败点为 recent edit open-file button 未调用 `stopPropagation()`。GREEN 定向通过：同命令通过（1 file / 1 test）。相邻验证通过：`npm test -- src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/utils/chatStatusSummary.test.ts src/utils/toolPresentation.test.ts`（6 files / 84 tests）。全量前端通过：`npm test`（55 files / 500 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。`dist` 已清理，`out` 不存在。`git diff --check` / `git diff --cached --check` 退出码 0；前者仅打印既有 LF/CRLF 提示，后者无输出。
- 未验证项：未启动真实 Tauri 桌面触发右侧最近改动树 open-file 按钮；未手动确认 Tab 到该按钮后 Enter/Space 只打开文件、不选择父级 diff row；未用屏幕阅读器确认嵌套按钮与父 row 的实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做一次 Tauri desktop keyboard smoke，覆盖右侧最近改动树、tool group header、expanded search result file row、single tool open-file/copy 按钮的 Tab/Enter/Space 行为；2) 把 “嵌套 open-file 控件必须同时 stop click/keyDown” 抽成共享小 helper 或 lintable 测试清单，降低后续新增 composite row 时遗漏的风险；3) 对 StatusPanel 最近改动树补真实 DOM 交互级测试环境时，增加“open-file keydown 不触发 onSelectedEditChange / onOpenDiffPanel”的端到端断言；4) 开始拆分当前大脏工作区的可审查批次，把工具块传播边界、状态面板历史树、消息流视觉等改动分组收束。

## 本轮 PLAN 2026-06-20 search expanded result file keydown boundary

- 目标：补齐 `SearchToolGroupBlock` 展开详情中搜索结果文件行按钮的 `keydown` 冒泡边界，避免用户在文件行按钮上按 Enter/Space 时，同时触发父级 group item row 的展开/折叠逻辑。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend component/quality specs、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：核对本项目 `SearchToolGroupBlock`、既有 open-file propagation 测试与 cc-gui 搜索分组实现；确认本轮只处理展开详情文件行按钮的 keydown 冒泡，不改可见 UI、文件打开参数、搜索摘要、分组状态、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx`，让 Search group 的第一行处于展开态，断言 `.search-result-file-row` 的 `onKeyDown` 会调用 `stopPropagation()`；旧实现应因缺少 `onKeyDown` 失败。
- 功能点 4：实现最小修复。验证方式：在 `src/components/toolBlocks/SearchToolGroupBlock.tsx` 的展开详情文件行按钮上补 `onKeyDown={(event) => event.stopPropagation()}`，保留现有 `onClick`、`openFile(...)` 参数和 target label。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，明确可展开 tool group row 内的展开详情文件行也属于嵌套 open-file 控件，必须阻止 click 与 keydown 冒泡。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool-block 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-20 search expanded result file keydown boundary

- 上轮进展与阻塞：上一轮完成通用 `isToolBlockToggleActivationKey()`，代码中确实存在 Bash/Read/Edit/Generic/Agent/Task 单块与 Bash/Read/Edit/Search 分组块统一复用该 helper。已知阻塞仍是真实 Tauri 桌面键盘 smoke、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 运行态并排截图，以及 IDE build 容易超时。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读搜索分组实现主要是 click-driven 列表，未看到与本项目完全同构的“group item row 可键盘展开 + 展开详情文件行按钮”模式。因此本轮没有直接 cc-gui 控件缺口可补齐。用户价值：继续用真实对标代码校准范围，避免把本项目增强能力误报成 cc-gui parity 缺失。
- 本轮差距诊断（超越 cc-gui）：本项目 Search group 展开后会列出可点击文件行，但这些文件行处于可键盘展开的 group item 子树内。此前 header 首个文件按钮已阻止 keydown 冒泡，展开详情里的 `.search-result-file-row` 只阻止 click 冒泡。用户价值：键盘用户在文件行按钮上按 Enter/Space 打开文件时，不会额外触发父级 row 展开/折叠，减少误操作和焦点上下文跳动。
- 本轮规划：只补 `SearchToolGroupBlock` 展开详情文件行按钮的 `onKeyDown(stopPropagation)`、对应 open-file propagation 回归测试和 Trellis 规范；不改可见 UI、`openFile()` 参数、搜索摘要、group 展开状态、状态聚合、Store/Rust/Tauri response。优先级中等偏高：覆盖高频搜索结果文件打开路径，实现成本低，回归风险集中且可用现有纯 React element 测试验证。
- 本轮完成：`src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx` 新增 Search group 展开态测试，通过可控 `useState` mock 渲染第一条 expanded row，并断言 `.search-result-file-row` 的 `onKeyDown` 调用 `stopPropagation()`；`src/components/toolBlocks/SearchToolGroupBlock.tsx` 在展开详情搜索结果文件按钮上补 `onKeyDown={(event) => event.stopPropagation()}`；`.trellis/spec/frontend/component-guidelines.md` 记录 expanded group details 内的 open-file controls 也必须阻止 click 与 keydown 冒泡。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx` 先失败 1 项，失败点为 expanded Search result file row 未调用 `stopPropagation()`。GREEN 定向通过：同命令通过（1 file / 3 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx src/components/toolBlocks/SubagentProcessOpenFilePropagation.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/chat/MessageItem.test.tsx src/components/chat/MessageList.test.tsx src/utils/toolGrouping.test.ts src/utils/toolPresentation.test.ts`（17 files / 109 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（54 files / 499 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。`dist` 已清理，`out` 不存在。
- 未验证项：未启动真实 Tauri 桌面触发 Search group 展开详情文件行；未手动确认 Tab 到展开详情文件行后 Enter/Space 只打开文件、不切换父 row；未用屏幕阅读器确认文件行 label 与父 row `aria-expanded` 的实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Generic/Bash/Read/Edit/Search/Agent/Task 单块与分组，点测 header、group item row、展开详情文件行、bulk action disabled、Open File、Copy Input/Output、状态摘要和 diff hover；2) 对 ToolBlock 可访问性做运行态扫描，核对所有可展开入口和操作按钮的 `role/button/tabindex/aria-expanded/title/aria-label/onKeyDown/disabled` 覆盖；3) 如果后续引入 DOM 交互测试环境，补 Search 展开详情文件行 Enter/Space 打开文件且父 row 不切换的真实交互级测试；4) 开始收束同主题提交边界，把工具块可访问性修复与历史加载/消息合并等其他脏改拆成可审查批次。

## 本轮 PLAN 2026-06-20 tool block toggle activation helper

- 目标：把 Chat tool-block 自定义 `role="button"` 折叠头部的键盘激活判断，从分组块专用 helper 推广为通用 helper，覆盖 Bash/Read/Edit/Generic/Agent/Task 单块与分组块，避免 Enter/Space 合同继续分散复制。
- 功能点 1：恢复上下文与对标路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend component/code-reuse specs 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断键盘合同。验证方式：检查 `BashToolBlock`、`ReadToolBlock`、`EditToolBlock`、`GenericToolBlock`、`AgentGroupBlock`、`TaskExecutionBlock` 以及已有 group blocks，确认除 `Generic` 需保留 `hasExpandableContent` guard 外，激活键均为 Enter/Space。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/utils/toolGrouping.test.ts`，先引用 `isToolBlockToggleActivationKey()` 并运行 `npm test -- src/utils/toolGrouping.test.ts`，旧实现应因缺少导出失败。
- 功能点 4：实现最小共享 helper 并迁移调用点。验证方式：在 `src/utils/toolGrouping.ts` 新增通用 helper，保留 `isToolGroupToggleActivationKey` 兼容别名；单块与分组块统一使用通用 helper，`GenericToolBlock` 继续先判断 `hasExpandableContent`。
- 功能点 5：同步 Trellis 前端规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，把“分组 toggle helper”规则升级为“tool-block toggle helper”规则。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相关 toolBlocks 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out` 构建产物。

## 迭代记录 2026-06-20 tool block toggle activation helper

- 上轮进展与阻塞：上一轮已把 group block 的 Enter/Space 判断抽成 `isToolGroupToggleActivationKey()` 并通过前端/后端全量验证；本轮复查确认代码中仍有 Bash/Read/Edit/Generic/Agent/Task 单块保留内联 `event.key !== 'Enter' && event.key !== ' '` 判断。`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原指定 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 已提供一致的工具块折叠键盘体验；本项目虽然行为上已支持 Enter/Space，但单块和分组块各自维护判断，未来新增 tool block 时容易遗漏 Space 或破坏 `preventDefault()`。用户价值：键盘用户和辅助技术用户获得一致的可预测操作路径。
- 本轮差距诊断（超越 cc-gui）：把键盘激活合同沉到共享 helper，并用测试固定 API，比只在组件里复制行为更可维护。用户价值：后续优化单块、分组块、Agent/Task 头部时，交互不会因局部改动漂移。
- 本轮规划：选择“通用 tool-block toggle activation helper”作为本轮唯一实现项。优先级理由：用户价值中等偏高（可访问性与一致性）、出现频率高（每个可折叠工具块都会触发）、实现成本低（单 helper + 调用点替换）、回归风险可通过现有 tool-block 测试覆盖。
- 本轮完成项：`src/utils/toolGrouping.ts` 新增 `isToolBlockToggleActivationKey()`，保留 `isToolGroupToggleActivationKey` 兼容别名；`src/utils/toolGrouping.test.ts` 新增通用 helper RED/GREEN 测试；`src/components/toolBlocks/BashToolBlock.tsx`、`ReadToolBlock.tsx`、`EditToolBlock.tsx`、`GenericToolBlock.tsx`、`AgentGroupBlock.tsx`、`TaskExecutionBlock.tsx` 改用通用 helper；`BashToolGroupBlock.tsx`、`ReadToolGroupBlock.tsx`、`EditToolGroupBlock.tsx`、`SearchToolGroupBlock.tsx` 从旧 group helper 名称迁移到通用 helper；`.trellis/spec/frontend/component-guidelines.md` 将 helper 复用规范升级到所有 tool-block custom toggle。
- 本轮验证：RED：`npm test -- src/utils/toolGrouping.test.ts` 先失败 1 项，失败点为 `TypeError: isToolBlockToggleActivationKey is not a function`。GREEN：同命令通过（1 file / 10 tests）。定向 tool-block 验证通过：`npm test -- src/utils/toolGrouping.test.ts src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx`（11 files / 61 tests）。全量验证通过：`npm test`（54 files / 498 tests，仅 Browserslist 过期提示）、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）、`git diff --check`、`git diff --cached --check`。`dist` 已清理，`out` 不存在。
- 未验证项：未启动真实 Tauri 桌面做键盘 Tab/Enter/Space 手工 smoke；本轮为内部交互 helper 收口，视觉表现无改动。
- 下一步候选：1) 为单块和分组块补充显式键盘交互测试，覆盖 Enter/Space 展开、Escape/Arrow 不触发，以及 Generic 无可展开内容时不暴露/不响应 toggle；2) 继续审计 tool-block 内部嵌套按钮的 `onKeyDown(event.stopPropagation())` 是否覆盖所有 open/copy 入口；3) 检查 cc-gui 可读代码中是否有额外的工具块 a11y 模式可借鉴，并只吸收能降低误操作或提升扫描效率的部分。

# 当前任务计划

## Chat UI cc-gui parity 持续迭代

- [ ] 历史会话结构化块渲染：加载 Claude/Codex 历史会话时保留 `thinking`、`tool_use`、`tool_result` 等 content blocks，避免退化成一段长文本。
  - 验证方式：新增历史消息映射单元测试；`npm test`；`npm run build`；如改 Rust 会话解析则执行 `cargo check --manifest-path src-tauri/Cargo.toml`。
  - 当前状态：已实现并验证；Claude 历史保留原始 raw，Codex 历史转换 reasoning/function_call/function_call_output 为统一 raw blocks，前端 `loadSession()` 映射回 `ChatMessage.raw`。已补齐历史图片后端链路：Claude `image/input_image` 也算结构化历史内容，图片-only 消息不再因文本为空被跳过；Codex `message.content[]` 中的 `input_image/image` 会透传到统一 raw blocks。本轮继续补齐 Codex 历史 patch 链路：`custom_tool_call/apply_patch` 与 `custom_tool_call_output` 按 `call_id` 转成统一 `tool_use/tool_result`，`function_call exec_command/shell_command` 内嵌的 `*** Begin Patch` 也会归一化为 `apply_patch`，右侧“最近改动”即可复用现有 `collectEditToolItems()` 统计文件和增删行；同时新增 store 级回归测试，验证 `loadSession()` 后 normalized Codex patch 会直接进入 `buildChatStatusSummary()`。历史系统上下文过滤继续收口，`# AGENTS.md instructions` 与 `<heartbeat>` 这类用户角色重放块不会进入 transcript。验证通过：`cargo test --manifest-path src-tauri/Cargo.toml session_manager::providers::codex -- --nocapture`、`npm test -- src/utils/chatStatusSummary.test.ts src/utils/toolPresentation.test.ts`、`npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`。
- [ ] 工具块 UI 与 Open File 打磨：按命令类型提供不同摘要展示，压缩工具块布局；修复 `Open File` 对相对路径、JetBrains/VS Code 不同环境的打开失败。
  - 验证方式：`cargo test --manifest-path src-tauri/Cargo.toml editor_commands`；`cargo check --manifest-path src-tauri/Cargo.toml`；`npm run build`；浏览器/桌面手动点击 Read/Edit 的 Open File。
  - 当前状态：已实现并验证；前端所有 Read/Edit/Generic/Search 文件入口传 `currentCwd`，按钮/文件链接阻止折叠冒泡；后端支持相对路径、`/c/...`、`/mnt/c/...`、`file://...` 和 `file.ts:line[:column]` 归一化，VS Code/Cursor/JetBrains Toolbox/Program Files 发现和系统默认打开兜底；命令摘要区分 Build/Test/Git/Search/Read/List/Patch/Run，Search 单个工具也走专用块并把搜索结果文件行渲染为可点击列表。最新补充：`GenericToolBlock` 现在会把 tool result 的首行摘要直接显示在 header，压缩空白、多行自动省略，进一步贴近 cc-gui 的“操作行 + 轻结果”扫描体验；`EditToolBlock` 已补 `compact` 模式，分组展开项不再重复渲染完整头部卡片，而是直接呈现详情内容；`Read/Search/Agent/Task` 的组头又统一压成更轻的主摘要 + 二级摘要 + 低对比状态，`Agent/Task` 头部改由共享 `summarizeAgentToolHeader()` 消费；`EditDiffPreview` 本轮又补了一层 hover 顶栏摘要，直接显示“新增/删除行数”总览，减少只靠 `+/-` 数字理解 diff 的成本；本轮继续解码 MCP/codegraph text-block wrapper 与字面量 `\n` 输出，避免 codegraph 代码块在 Generic 工具结果里挤成一坨，并补 `extractResultText()` 对 wrapper object、array block、未知结构回退的单测。验证通过：`npm test -- src/utils/toolPresentation.test.ts`、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`；真实 Open File 点击需在 Tauri 桌面运行时继续点测。
  - 本轮新增：状态面板改成树状文件列表，150+ 文件进入独立滚动区，文件夹支持折叠/展开，diff 预览新增 unified / split 两种模式按钮；hover 预览改为不透明固定浮层，避免在滚动列表里被裁掉。验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/utils/chatStatusSummary.test.ts src/utils/toolPresentation.test.ts`、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`git diff --check`。
  - 本轮修复：同一个文件被多次 `apply_patch` / `MultiEdit` 修改时，批量编辑文件列表按 normalized path 合并成一条文件记录，增删行与 hover diff 合并统计；`StatusPanel` 复用同一聚合 helper，避免右侧摘要和 transcript 列表计数漂移。验证通过：`npm test -- src/components/chat/ContentBlockRenderer.test.tsx src/utils/toolPresentation.test.ts src/utils/chatStatusSummary.test.ts src/components/chat/StatusPanel.test.tsx`、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`git diff --check`。
  - 本轮修复：中间 `文件差异` 面板新增显式收起按钮，收起后完全不渲染 diff 面板与 resize handle，不再占用中间页面空间；右侧点击文件会自动重新展开完整 diff；右侧最近改动 hover diff 改为状态栏专用窄预览，限制宽度和 split 行布局，避免浮层跨到聊天/完整 diff 内容区造成遮挡。验证通过：`npm test -- src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/StatusPanel.test.tsx`、`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx`、`npm test`、`npm run build`、`git diff --check`。
- [ ] 消息流视觉打磨：对齐 cc-gui 的 transcript 结构，用户消息保留紧凑右侧气泡，assistant 消息改为无大卡片的连续内容流；thinking/edit/run/search 等工具事件在 assistant 流内以轻量操作行呈现，避免一次 AI 响应被多个大框割裂。
  - 验证方式：`npm run build`；`npm test -- src/stores/useChatStore.test.ts src/utils/toolPresentation.test.ts`；浏览器检查 Chat 页面无横向滚动，长用户 Markdown 不出现巨大标题，assistant 回复不再每段一个大气泡。
  - 当前状态：已实现并持续收口；`MessageItem` 已分离 user/assistant shell，assistant 透明流式展示，user bubble 限制宽度并压低 Markdown 标题层级；assistant 上下文内 thinking/tool blocks 弱化为低对比左边界操作行。最新补充：`Edit` 分组展开项改为 compact 详情，`Read/Search` 首屏摘要已回到主扫描行，assistant transcript 内的 `tool-command-chip`、`tool-state-pill` 与操作按钮又做了一轮降权，`Agent/Task` 两类块也改为共享摘要逻辑并继续压低 runtime/历史占位的视觉重量，`MessageMeta` 在 assistant 收尾处改为 compact inline 摘要，`Read/Search/Agent/Task` 的组头进一步统一成更轻的操作行结构；本轮把 `MessageList` 的历史折叠计算抽到共享逻辑，顶部入口从强交互按钮压成轻提示，并继续沿用触顶自动加载旧消息的连续滚动方式；同时搜索框 placeholder 也改成真实可用文案，不再显示“预留入口”。本轮继续补齐 `ContentBlockRenderer.compact`，让 assistant 主消息与子代理历史共用紧凑渲染路径；`ThinkingBlock` 增加 compact 变体，assistant 流内的 think/tool 节点统一压低圆角、内边距和块间距，进一步减少“一次响应一个大框”的割裂感；本轮继续把 `compact` 透传到 Bash/Read/Edit/Generic 单工具块以及 Bash/Read/Edit/Search 分组块、Agent/Task 块，避免子代理历史或紧凑流里重新出现默认大头部卡片；针对截图反馈，compact 单工具块改为默认折叠，只显示操作行摘要，点击后才展开参数/结果，避免历史会话默认铺开大段输出。验证通过：`npm test -- src/components/chat/ThinkingBlock.test.tsx src/components/chat/MessageMeta.test.tsx src/components/toolBlocks/SubagentHistoryPanel.test.tsx src/components/toolBlocks/subagentHistoryUtils.test.ts`、`npm test -- src/components/chat/ContentBlockRenderer.test.tsx src/utils/toolPresentation.test.ts src/components/chat/StatusPanel.test.tsx`、`npm test -- src/components/chat/ContentBlockRenderer.test.tsx src/components/chat/MessageAnchorRail.test.tsx`、`npm test -- src/components/chat/ContentBlockRenderer.test.tsx src/utils/chatNavigation.test.ts src/components/chat/MessageAnchorRail.test.tsx`、`npm run build`；浏览器与桌面态 smoke 仍需继续点测。
- [ ] Composer 紧凑布局与图片附件：输入框默认一行，不显示空文件上下文占位；图片上传/粘贴/拖拽必须作为真实附件 payload 发送给 AI，不能退化为 `@filename` 文本。
  - 验证方式：`npm test -- src/stores/useChatStore.test.ts`；`npm run build`；Node bridge 直接验证 Codex base64 图片可落盘为 `local_image`；浏览器/桌面手动检查输入区高度和图片 chip。
  - 当前状态：已实现；`ChatComposer` 读取 PNG/JPEG/WebP/GIF 为 base64 并保留 Tauri 本地 path，支持纯图片发送；Claude 走 `sendWithAttachments`，Codex path 走 `local_image`，Codex base64 由 bridge 写入临时文件后转 `local_image`。输入区已改为中间列内居中的紧凑面板，默认一行，桌面与 900px 窗口浏览器几何检查无横向溢出。本轮继续补齐当前发送消息的 `raw.image` 结构化块，live 消息和历史消息共用缩略图渲染路径，不再只显示 `[Image: name]` / `[图片: name]` 文本；历史图片渲染新增 `file:///C:/...` 归一化，先转本地路径再交给 Tauri asset URL。最新补充：用户上传图在用户气泡内改为 cc-gui 风格的小缩略图（桌面约 200px 宽、移动端按视口收缩），点击仍打开 lightbox；历史 raw blocks 中图片前后的疑似 base64 payload 残片会从可渲染文本和合并消息文本中过滤，不再在气泡里显示 `SUVORK5CYII=` / `Jggg==` 这类残留。`/` 补全不再只依赖前端 5 条硬编码命令，新增 Rust `chat_list_slash_commands` registry，按 Claude/Codex provider 返回 cc-gui parity 内置命令，并扫描当前项目向上的 `.claude/commands/**/*.md`；前端 `useCompletions` 优先调用后端 registry，失败时才回退内置兜底，已覆盖 `/context`、`/plan`、`/resume`、`/batch`、`/claude-api`、`/debug`、`/loop`、`/simplify`、`/update-config`、`/diff` 等候选。验证通过：`npm test -- src/stores/useChatStore.test.ts`、`npm test -- src/stores/useChatStore.test.ts src/components/chat/ContentBlockRenderer.test.tsx`、`npm test -- src/components/chat/composer/useCompletions.test.ts src/stores/useChatStore.test.ts src/components/chat/ContentBlockRenderer.test.tsx`、`npm test -- src/components/chat/ContentBlockRenderer.test.tsx src/utils/chatMessageFlow.test.ts`、`npm test`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml chat::slash_commands -- --nocapture`、`cargo test --manifest-path src-tauri/Cargo.toml`、`cargo check --manifest-path src-tauri/Cargo.toml`、`git diff --check`、Node bridge 直接验证。
- [ ] 会话管理第一版：在 Chat 页面接入项目/会话侧栏，支持选择已有会话加载历史并继续发送，支持新建会话保留项目 cwd。
  - 验证方式：`npm run build`；手动选择项目、选择 session、发送新消息检查 `sessionId/cwd/provider`。
  - 当前状态：已实现并持续收口；新增活跃请求切换会话前 abort 保护、会话列表搜索和刷新、项目列表/会话列表分栏布局优化。最新补充：会话切换时会先显示 `pendingSessionKey` 的加载态，连续点击历史会话时用 token 防止旧请求覆盖新选择。本轮继续优化点击历史会话的列表性能：按项目路径缓存 `list_sessions` 结果，非强制刷新直接复用；刷新中保留已有会话列表，不再整体替换为大 spinner；`loadSession()` 对当前已激活 session 重复点击直接短路，并按 `provider/sourcePath/sessionId/lastActiveAt` 缓存已映射历史消息，历史会话来回切换时减少后端读取和大数组映射，`lastActiveAt` 变化会重新读取；本轮补充项目路径缓存别名，会同时按 `list_sessions(projectPath)` 和 session 自带 `projectDir` 建 key，配合 ChatPage/侧栏对 active/pending 会话的点击短路，减少点击历史会话后由 `currentCwd` 变化带来的重复会话列表加载；侧栏还新增同项目 in-flight 请求去重，已有 `list_sessions(projectPath)` 未完成时不会再发第二次扫描。后端 Codex 项目会话扫描也改为先读取 `session_meta.payload.cwd` 过滤项目，只有匹配当前项目的 jsonl 才继续提取标题，避免为无关项目读取大型 Codex 历史。本轮新增修复：用户手动点击项目后，侧栏不会再被当前会话的 `currentCwd` 自动拉回；切换到未缓存项目时会立即清空旧会话列表并显示加载态，避免新项目下继续看到旧项目会话。验证通过：`npm test -- src/stores/useChatStore.test.ts src/components/chat/chatSessionSidebarUtils.test.ts`、`npm test -- src/components/chat/chatSessionSidebarUtils.test.ts src/stores/useChatStore.test.ts src/utils/desktopNotification.test.ts`、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`。真实桌面 session 选择/继续发送仍需继续做点测。
- [ ] 长会话导航与运行摘要：继续对标 cc-gui 的消息锚点与状态面板，让长会话下的定位、回看和运行态读取更高效。
  - 验证方式：`npm test -- src/components/chat/MessageAnchorRail.test.tsx`；`npm run build`；浏览器/桌面态检查左侧锚点栏与右侧状态面板在长会话下的信息密度和可读性。
  - 本轮补充：历史会话里的 Bash/Read/Edit/Search 分组在 `compact` 模式下默认只显示一行操作摘要，点击组头后才显示子项列表和“展开全部/折叠全部”；Codex/长历史锚点在列表尚未回传折叠状态时先按默认可见窗口生成，避免为未挂载的历史消息创建不可跳转锚点。
  - 当前状态：进行中；当前项目原本只有左侧回顶/到底按钮和右侧 provider/messageCount 静态摘要。上一轮已把 `MessageAnchorRail` 补成“锚点数量 + 当前摘要 + 回顶/到底”复合控件；本轮继续推进为真实消息锚点导航：`MessageList` 会把当前可见 user message 节点注册到 `ChatPage`，`MessageAnchorRail` 改为沿纵向轨道分布的可点击点阵锚点，并用 `IntersectionObserver` + 滚动回退逻辑跟踪当前 active anchor，右侧 `StatusPanel` 也改为显示当前视口对应的用户消息摘要，不再固定显示“最后一条用户消息”。同时把搜索/渲染/锚点摘要的共享逻辑抽到 `src/utils/chatNavigation.ts`，避免 `ChatPage` 与 `MessageList` 重复维护消息过滤和文案截断。本轮补齐 system prompt 过滤与图片锚点摘要：`role=system` 不再进入 transcript/锚点/跨模型 handoff，图片 raw block 会生成 `图片 screen.png` 这类可读锚点和搜索文本；本轮进一步扩大协议上下文识别，覆盖 Codex/Claude system 开场、Tools/Skills/Plugins/Heartbeats 等历史上下文块，且 handoff 构造时也会过滤这类内容。最新补充：锚点轨道新增密集列表降采样，超长会话只渲染代表点并保留首尾/active 锚点，点位仍按原始消息序号定位，避免大量点重叠后看起来空白或不可见。本轮继续增强锚点 hover：`chatNavigation` 会输出 `text/image/mixed/empty` 预览类型，`ChatPage` 传入用户消息序号、总数与时间，`MessageAnchorRail` 以更高对比度的点阵、类型胶囊和四行预览浮层展示，图片-only 与图文消息不再表现成一批难以区分的空白点；右侧最近编辑 hover 改为向左展开，中间 diff hover 改为居中展开并使用不透明深色背景，避免侧栏截断和透明难读。当前轮继续修正状态面板编辑统计：`recentEdits` 列表仍限制为最近 4 个文件以保持侧栏紧凑，但 `touchedFileCount`、`totalAdditions`、`totalDeletions` 改为按全量聚合 edit set 计算，并在面板标题显示“文件数 · +增量 / -删量”，避免长历史会话只按可见 4 项低估变更规模；如果实际变更文件超过可见列表，会显示“还有 N 个文件未显示”的低对比入口，并可展开查看完整 `allEdits` 列表再收起。本轮又把密集锚点抽样改为优先选择 `text/image/mixed` 非空预览，避免 empty 锚点占满可见轨道预算；锚点 hover 改为不透明浮层，中间与右侧 diff hover 也提高 z-index 和对比度。针对 Codex 锚点异常，新增 `isMessageAnchorCandidate()` 并让 `ChatPage` 与 `MessageList` 共享，只有真实可见的用户输入/图片消息注册锚点，`[tool_result]`、空白和协议上下文用户行不会进入锚点轨道。验证通过：`npm test -- src/utils/chatNavigation.test.ts src/components/chat/MessageAnchorRail.test.tsx`、`npm test -- src/utils/chatStatusSummary.test.ts src/components/chat/StatusPanel.test.tsx src/utils/toolPresentation.test.ts`、`npm test -- src/components/chat/ContentBlockRenderer.test.tsx src/components/chat/MessageAnchorRail.test.tsx`、`npm test -- src/components/chat/ContentBlockRenderer.test.tsx src/utils/chatNavigation.test.ts src/components/chat/MessageAnchorRail.test.tsx`、`npm test`（18 files / 122 tests）、`npm run build`、`git diff --check`（仅 CRLF 提示）、`cargo check --manifest-path src-tauri/Cargo.toml`（仅既有 `ChatMessageEvent` dead code warning）。剩余差距：Tauri 桌面态真实点测、锚点与右侧运行摘要更细的联动仍待继续打磨。
- [ ] Claude 子代理历史接入：Agent/Task 工具块按 cc-gui 思路懒加载 `subagents/agent-*.jsonl` 历史，展示结构化 thinking / tool_use / tool_result，而不是长期停留在占位文案。
  - 验证方式：Rust 单测覆盖按 `agentId` 与 `.meta.json description` 两条查找路径；`cargo test --manifest-path src-tauri/Cargo.toml session_manager::providers::claude`；前端测试覆盖 agent/task 元信息驱动的调用参数；`npm test -- src/utils/toolPresentation.test.ts src/stores/useChatStore.test.ts` 或新增相关测试；`npm run build`；桌面态手动展开历史会话中的 Agent/Task 工具块验证真实轨迹。
  - 当前状态：已实现前端接入并持续收口；`SubagentHistoryPanel` 现在会在 Claude 历史会话与当前活跃会话中懒加载真实 `agent-*.jsonl` 轨迹，当前活跃会话在缺少 `activeSession.sourcePath` 时会回退到 `currentCwd` 作为查找根路径，不再因为只拿到 `sessionId` 而长期显示占位文案。面板顶部新增轻量过程摘要，会从 structured blocks 中提炼“思考 / 读取文件 / 其他工具 / 结果”概览，下方仍保留原始 transcript 级 thinking/tool_use 渲染。最新补充：过程卡片已接入 tool result runtime 元信息，显示耗时、工具调用数、读取文件数与 token 统计；当结果文本以 JSON runtime 前缀开头时，首屏结果摘要会跳过该前缀直接显示真实输出首行，并支持“查看完整输出”折叠。进一步收口：子代理过程摘要已拆成可单测的纯渲染组件 `SubagentProcessSummary`，读取文件项现在同时保留 `displayPath`、真实 `openPath` 和读取行号，展示层渲染为可点击的轻量文件行并复用 `openFile(path, start, end, currentCwd)` 直接定位到对应范围；“其他工具”区域本轮又从统一 `Wrench` 胶囊收口为类型化操作行，复用现有 `summarizeCommand()` / 搜索分类语义区分 `Search / Run / Patch / List / Web / Agent` 等类别，且对 `Edit / Write` 这类非 Read 的文件型工具额外保留真实 target，允许直接从过程摘要打开对应文件，不再只显示一串静态路径文本。继续补齐后，`Search / Grep` 这类搜索工具现在也会从 `tool_result` 中提取首个命中文件和行号，过程摘要优先展示为可点击文件目标，并把查询词与 `x matches in y files` 统计分层展示，进一步贴近 cc-gui 的扫描式操作流；`Glob / List` 这类文件枚举工具则已经和真正的搜索命中语义拆开，摘要统一改为更自然的 `N files`。此外 `Bash / Run / Command` 类结果现在会复用主工具块的 bash 结果摘要逻辑，优先显示 `stdout` 的真实首行，不再直接露出 JSON blob；`Fetch / Web` 类结果也开始优先取 `title / name / url / content / summary` 之类结构化字段，避免继续把 JSON 原文露出来；剩余 generic JSON 结果也补了统一回退，会优先取 `message / title / name / summary / description / result / output / stdout / content` 等常见字段，进一步减少子代理过程行里直接出现原始对象。本轮又额外压掉了明显重复的 JSON 次级 detail，避免工具行在已经有可读摘要时再次挂出原始对象文本。验证通过：`npm test -- src/components/toolBlocks/subagentHistoryUtils.test.ts src/components/toolBlocks/SubagentHistoryPanel.test.tsx`、`npm run build`；Rust 侧历史查找链路与桌面态展开历史 Agent/Task 工具块的真实点测仍待继续完成。
- [ ] 文案与可访问性：新增用户可见文案同步 `zh.json` / `en.json`，按钮提供 title/aria 语义。
  - 验证方式：`npm run build`；检查浅色/深色基础布局不遮挡输入区。
  - 当前状态：中英文 key 已补齐；普通工具权限弹窗新增 `chat.permission.*` 文案，按钮提供 `title` / `aria-label`；`npm run build` 通过；浏览器默认主题未发现遮挡；深色主题和真实桌面视觉待验证。
- [ ] 权限审批假死修复：对齐 cc-gui 的普通工具权限文件协议，`request-<sessionId>-<requestId>.json` 必须弹出确认框并写回 `response-<sessionId>-<requestId>.json`，避免 MCP/Chrome DevTools、Bash、Edit 等工具一直 pending。
  - 验证方式：store 单测覆盖 `permission://tool` → `pendingToolPermission` → `permission_respond_tool`；Rust 单测覆盖 `write_tool_permission_response()` 生成 `{allow}` 响应和缺省 inputs 归一；`npm test`；`npm run build`；`cargo test --manifest-path src-tauri/Cargo.toml`；`cargo check --manifest-path src-tauri/Cargo.toml`；`git diff --check`。
  - 当前状态：已实现普通工具权限闭环；Rust `PermissionWatcher` 现在监听 `request-default-*.json`，emit `permission://tool`，前端 `ToolPermissionDialog` 展示工具名、cwd、关键参数和完整 JSON，用户允许/拒绝后调用 `permission_respond_tool` 写 `response-default-*.json`，payload 严格为 `{ "allow": true|false }`。已记录 backend/frontend spec，桌面态真实 MCP 权限弹窗仍需在 Tauri app 中点测。
- [ ] Trellis 质量门：执行 `trellis-check`，必要时更新 spec，再提交当前阶段。
  - 验证方式：`npm run build` 通过；如仅改前端则记录无需 `cargo check`。
  - 本轮验证：`npm test -- src/components/chat/ContentBlockRenderer.test.tsx src/utils/chatNavigation.test.ts src/components/chat/MessageAnchorRail.test.tsx` 通过（3 files / 19 tests）；`npm test` 通过（18 files / 126 tests）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过（仅既有 `ChatMessageEvent` dead_code warning）；`git diff --check` 无空白错误，仅 LF/CRLF 提示。最新补充：`chat://done` 成功/失败、发送失败和显式 abort 都会触发 Windows WebView 系统通知，通知去重按 requestId 控制；验证通过 `npm test -- src/utils/desktopNotification.test.ts src/stores/useChatStore.test.ts`。
  - 当前状态：已执行 Trellis check 要求的差异检查、单测、前端构建、Rust 单测、`cargo check` 和 `git diff --check`；上一轮验证：`cargo test --manifest-path src-tauri/Cargo.toml session_manager::providers::codex -- --nocapture` 通过（4 tests），`cargo check --manifest-path src-tauri/Cargo.toml` 通过（仅既有 warning），`cargo test --manifest-path src-tauri/Cargo.toml` 通过（80 tests）。当前轮新增验证：`npm test -- src/utils/chatStatusSummary.test.ts src/components/chat/StatusPanel.test.tsx src/utils/toolPresentation.test.ts` 通过（3 files / 38 tests），`npm test -- src/components/chat/ContentBlockRenderer.test.tsx src/components/chat/MessageAnchorRail.test.tsx` 通过（2 files / 8 tests），`npm test -- src/components/chat/composer/useCompletions.test.ts` 通过（6 tests），`npm test -- src/stores/useChatStore.test.ts` 通过（15 tests），`cargo test --manifest-path src-tauri/Cargo.toml write_tool_permission_response_writes_bridge_allow_payload tool_permission_request_allows_missing_inputs` 通过（2 tests），`npm test` 通过（19 files / 140 tests），`npm run build` 通过，`cargo test --manifest-path src-tauri/Cargo.toml` 通过（84 tests），`cargo check --manifest-path src-tauri/Cargo.toml` 通过（仅既有 warning：`ChatMessageEvent` dead code），`git diff --check` 无 whitespace error（仅 CRLF 提示）。已更新 frontend/backend spec 与 TODO；待提交当前阶段。

## 迭代记录 2026-06-18 12:16 heartbeat

- 上轮实际进展：代码中确实存在 diff 面板完全收起、不占页面空间；系统通知入口已接在 `chat://done`、发送失败和 abort；会话侧栏已加手动项目选择锁，切项目不会继续显示旧项目会话。阻塞：系统通知尚未在真实 Tauri 桌面态观察 Windows 右下角弹出；当前仍依赖 WebView Notification API，若系统权限或 WebView 行为不稳定，后续再切 Tauri 原生通知插件。
- 本轮诊断（补齐 cc-gui）：cc-gui changelog 明确有“task completion toast notification”且展示会话标题/最新回答预览；本项目上一轮只显示“任务已完成/停止/失败”，缺少结果预览，用户离开窗口后难以判断是哪次任务完成。
- 本轮诊断（超越 cc-gui）：用户要求的是电脑右下角通知，不是应用内 toast；本项目保留系统通知路线，并补充最终回答/停止时部分输出预览，解决多任务并行或长任务等待时“弹了但不知道完成了什么”的痛点。
- 本轮规划：优先实现系统通知预览，价值高、实现成本低、风险小；暂不引入 `tauri-plugin-notification`，避免同时改 Rust capabilities 和前端 API，先把现有通知内容做完整并保持可回退。
- 本轮完成：`src/utils/desktopNotification.ts` 清洗、折叠空白并截断通知 detail；`src/stores/useChatStore.ts` 从最新 assistant 的最后 text block 或流式文本提取预览，成功和中断通知都会带预览，失败通知继续带错误原因；更新 `desktopNotification` 与 `useChatStore` 单测。
- 本轮验证：`npm test -- src/utils/desktopNotification.test.ts src/stores/useChatStore.test.ts` 通过（2 files / 21 tests）；`npm test` 通过（20 files / 150 tests）；`npm run build` 初次发现 `Array.prototype.at()` 与当前 TS target 不兼容，已改为索引写法后通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过（仅既有 `ChatMessageEvent` dead_code warning）；`git diff --check` 无 whitespace error（仅 CRLF 提示）。
- 下一步候选：1) Tauri 桌面态真实点测系统通知权限与弹出稳定性，必要时接原生通知插件；2) 继续对标 cc-gui 的 completion toast 设置项，为通知增加开关/静默模式；3) 对历史会话锚点和状态面板做真实长会话桌面点测，定位仍然“点不准/看不清”的剩余场景。

## 迭代记录 2026-06-18 继续推进

- 上轮实际进展：系统通知已支持成功/失败/中断三类停止输出通知，并带最终回答或 partial 输出预览；`npm test`、`npm run build`、`cargo check` 和 `git diff --check` 已通过。阻塞仍是未在真实 Tauri 桌面态观察 Windows 右下角弹窗。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 completion toast 有设置入口和通知内容，但当前项目更关键的问题是 WebView Notification 权限可能在任务结束事件里才请求，容易因为不在用户手势内被系统或 WebView 忽略。
- 本轮诊断（超越 cc-gui）：把通知权限请求提前到用户点击发送/停止的动作路径，能减少“任务结束了但系统通知没弹”的概率，同时不引入应用内 toast，不违背用户要求的右下角系统通知。
- 本轮规划：先做前端通知权限预热，不引入 `tauri-plugin-notification`；理由是改动小、可单测、风险低。若真实桌面点测仍不稳定，再升级为 Tauri 原生通知。
- 本轮完成：`src/utils/desktopNotification.ts` 新增 `prepareChatTurnStoppedNotificationPermission()`，只请求权限不创建通知；`src/stores/useChatStore.ts` 在 `send()` 和显式 `abort()` 用户动作路径调用预热；`src/utils/desktopNotification.test.ts` 覆盖预热不弹通知；`src/stores/useChatStore.test.ts` 覆盖发送与停止路径会调用预热。
- 本轮验证：`npm test -- src/utils/desktopNotification.test.ts src/stores/useChatStore.test.ts` 通过（2 files / 22 tests）；`npm test` 通过（20 files / 151 tests）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过（仅既有 `ChatMessageEvent` dead_code warning）；`git diff --check` 无 whitespace error（仅 CRLF 提示）。
- 下一步候选：1) 真实 Tauri 桌面运行后发送一条短消息并点击停止，观察权限提示和 Windows 右下角通知；2) 若预热仍不能稳定弹出，接 Tauri 原生通知插件并补 capabilities；3) 回到长会话锚点/状态面板点测，继续修复可见但跳转不准的场景。

## 迭代记录 2026-06-18 native notification

- 上轮实际进展：WebView `Notification` 已在发送/停止的用户动作路径预热权限，并在 `chat://done`、发送失败和显式 abort 时触发；单测、前端构建和 Rust 检查均通过。阻塞：真实 Tauri 桌面态仍未观察到 Windows 右下角通知，且 WebView 通知权限在桌面 WebView 中存在被静默忽略的风险。
- 本轮诊断（补齐 cc-gui）：cc-gui 有任务完成 toast/通知类能力；当前项目虽然已有通知内容，但主路径依赖浏览器通知 API，和用户要求的“电脑右下角系统通知”不够一致。
- 本轮诊断（超越 cc-gui）：改为 Tauri 原生通知主路径后，停止输出提醒不再依赖 WebView permission prompt；成功/失败/中断通知仍保留最终回答、partial 输出或错误原因预览，解决用户离开窗口等待长任务时“任务停了但不知道发生什么”的痛点。
- 本轮规划：优先引入 Rust 侧 `tauri-plugin-notification` 并新增薄命令 `chat_show_system_notification`，前端 `desktopNotification.ts` 只做运行时分流和文案清洗；不使用前端 `@tauri-apps/plugin-notification`，因为其发送实现仍是 `new window.Notification(...)`，无法解决 WebView 依赖问题。
- 本轮完成：`src-tauri/Cargo.toml` / `Cargo.lock` 增加 `tauri-plugin-notification`；`src-tauri/src/lib.rs` 初始化 notification 插件并注册 `chat_show_system_notification`；`src-tauri/src/commands/chat_commands.rs` 新增系统通知命令；`src-tauri/capabilities/default.json` 与 `src-tauri/tauri.conf.json` 同步 `notification:default`；`src/utils/desktopNotification.ts` 改为 Tauri runtime 下先调用原生命令，失败或非 Tauri 环境再回退 WebView；`src/utils/desktopNotification.test.ts` 增加 native 主路径、Tauri 下不请求 WebView 权限、native 失败兜底的回归测试；`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/backend/cross-layer-protocol.md` 已同步新约定。
- 本轮验证：`npm test -- src/utils/desktopNotification.test.ts` 通过（8 tests）；`npm test -- src/stores/useChatStore.test.ts` 通过（17 tests）；`npm test` 通过（20 files / 154 tests）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过（仅既有 `ChatMessageEvent` dead_code warning）；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（85 tests，doc tests 仅既有 ignored）；真实 Tauri 桌面右下角弹窗仍需运行 `npm run tauri dev` 后人工观察。
- 下一步候选：1) 桌面态点测发送完成、失败和停止三类通知是否稳定弹出；2) 回到用户反馈的 Codex 锚点不准/长会话空锚点，做真实长历史滚动与跳转验证；3) 继续优化 diff 三栏和状态面板在 150+ 文件时的滚动、收缩和 hover 预览细节。

## 迭代记录 2026-06-18 12:46 heartbeat

- 上轮实际进展：代码中确实存在 Tauri 原生系统通知主路径，`src-tauri/src/lib.rs` 已初始化 `tauri_plugin_notification`，`chat_show_system_notification` 已注册，前端 `desktopNotification.ts` 会先调用原生命令再回退 WebView；验证记录中的 `npm test`、`npm run build`、`cargo check`、`cargo test` 已完成。阻塞：仍未真实运行 Tauri 桌面观察 Windows 右下角弹窗。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 `MessageList.tsx` 在 first message id 变化时重置 `revealedCount`，避免历史分页展开状态跨 session 继承；当前项目 `MessageList.tsx` 只按 `totalEarlierMessages` clamp 旧状态，如果用户在上一个历史会话触顶展开过全部消息，再打开另一个历史会话，新会话可能默认全部展开，符合用户“历史会话全部默认展开”的反馈。
- 本轮诊断（超越 cc-gui）：当前项目已经做了触顶连续加载，比 cc-gui 点击“show earlier”更省操作；本轮保留连续滚动加载，同时把 reveal 状态绑定到 transcript key，解决“切会话继承展开状态”的痛点，并避免新会话首帧产生不可跳转锚点。
- 本轮规划：优先修复 `MessageList` reveal state 继承问题；价值高（直接对应用户反馈）、出现频率高（频繁切历史会话）、实现成本低（纯前端状态 helper + 单测），暂不扩展到真实桌面点测和 diff 布局大改。
- 本轮完成：`src/utils/chatUiBehavior.ts` 新增 `TranscriptRevealState`、`getEffectiveRevealedCount()`、`getClampedRevealState()`、`getNextRevealState()`；`src/components/chat/MessageList.tsx` 改为按第一条可渲染消息 id 作为 transcript key 派生 revealed count，切换历史时同步回到默认最近 15 条窗口；`src/utils/chatUiBehavior.test.ts` 覆盖跨 transcript 必须归零、同 transcript 分页继续保留；`.trellis/spec/frontend/component-guidelines.md` 记录 reveal state 不得跨会话继承。
- 本轮验证：`npm test -- src/utils/chatUiBehavior.test.ts src/utils/chatNavigation.test.ts src/components/chat/MessageAnchorRail.test.tsx` 通过（3 files / 21 tests）；`npm test -- src/components/chat/ContentBlockRenderer.test.tsx src/components/chat/StatusPanel.test.tsx` 通过（2 files / 12 tests）；`npm test` 通过（20 files / 156 tests）；`npm run build` 通过。未运行 `cargo check`，本轮仅改前端/文档。
- 下一步候选：1) 真实桌面点测历史会话切换后默认只显示最近窗口、触顶继续分页加载、锚点只指向已挂载用户消息；2) 继续排查 Codex 锚点“点不准”的真实数据样本，重点看 normalized Codex history 是否仍混入内部用户行；3) 继续做 Tauri 桌面态系统通知人工点测。

## 迭代记录 2026-06-18 12:56 heartbeat

- 上轮实际进展：Trellis 记录中声称完成的 reveal state 隔离在代码中确实存在，`MessageList` 已按首条可渲染消息 id 作为 transcript key，切换历史会话会回到默认最近 15 条窗口；Tauri 原生系统通知代码也确实存在。阻塞仍是：未运行真实 Tauri 桌面点测系统通知；未从 UI 人工确认长 Codex 历史锚点跳转体验。
- 本轮诊断（补齐 cc-gui）：对照 cc-gui 后确认锚点应只来自可见 user 消息；本项目已有共享 `isMessageAnchorCandidate()`，但只读抽样本机 Codex 历史时发现最新 ccg-switch 会话 97 条 user message 中有 51 条协议/运行上下文，且当前规则漏掉 `<turn_aborted>`，会把 Codex 内部中断标记渲染为用户气泡和锚点。
- 本轮诊断（超越 cc-gui）：不是把所有 XML-looking 文本都隐藏，而是只把明确的 Codex 控制标记纳入协议上下文过滤；这样可以清掉空锚点/假用户消息，同时保留 `<image name=...>`、`<TASK>` 这类可能承载真实用户内容的历史文本，降低误杀风险。
- 本轮规划：优先补共享协议上下文过滤和标题提取过滤，价值高（直接影响历史显示、搜索、锚点、会话标题）、实现成本低（前端 shared helper + 后端私有 title helper）、风险可通过单测覆盖；暂不扩大到图片文本占位解析和真实桌面点测。
- 本轮完成：`src/utils/chatMessageFlow.ts` 将 `<turn_aborted>`、`<user_action>`、`<subagent_notification>`、`<agents-instructions>`、`<skill>` 纳入协议上下文前缀；`src/utils/chatMessageFlow.test.ts` 与 `src/utils/chatNavigation.test.ts` 覆盖这些 Codex 控制标记不渲染且不成为锚点；`src-tauri/src/session_manager/providers/codex.rs` 的 `is_system_instruction()` 同步识别这些控制标记和 Codex/Claude/API system prompt，避免它们优先成为 Codex 会话标题；`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/backend/cross-layer-protocol.md` 已记录该跨层约定。
- 本轮验证：`npm test -- src/utils/chatMessageFlow.test.ts src/utils/chatNavigation.test.ts` 通过（2 files / 23 tests）；`cargo test --manifest-path src-tauri/Cargo.toml session_manager::providers::codex -- --nocapture` 通过（5 tests）；`npm test` 通过（20 files / 158 tests）；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（86 tests，doc tests 2 ignored）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过（仅既有 `ChatMessageEvent` dead_code warning）；`git diff --check` 无 whitespace error（仅 LF/CRLF 提示）。
- 下一步候选：1) 运行 `npm run tauri dev` 做真实桌面点测：历史会话默认窗口、Codex 锚点跳转、系统通知弹出；2) 继续处理历史图片中的文本占位 `<image name=...>`，确认是否能从 Codex 原始记录恢复可点击缩略图；3) 继续优化 diff 三栏在长 diff/150+ 文件场景下的空间分配与滚动体验。

## 迭代记录 2026-06-18 13:09 heartbeat

- 上轮实际进展：12:56 记录中提到的 Codex 控制标记过滤在代码中确实存在，`chatMessageFlow`、`chatNavigation` 与 Codex provider 的标题提取都已避开 `<turn_aborted>` 等协议标记；阻塞仍是未做真实 Tauri 桌面点测（系统通知、历史默认窗口、锚点跳转、Open File）。
- 本轮诊断（补齐 cc-gui）：只读抽样全局 Codex 历史发现 `<image name=...>` 样本都出现在同一条 `content[]` 中，结构是 `input_text` 图片 wrapper、真实 `input_image`、`</image>`、再接真实用户说明。若直接渲染这些 wrapper，本项目会在历史 transcript、搜索文本和锚点里显示无意义 XML 占位；cc-gui 的历史图片展示不会把这类 wrapper 当成正文。
- 本轮诊断（超越 cc-gui）：不能把所有 XML-looking 文本都隐藏，因为用户 prompt 里可能包含任务 XML 或示例文本。本轮采用更窄规则：只有同条 raw 消息存在真实 `image/input_image` block 时，才过滤纯 `<image ...>` / `</image>` wrapper，既清掉污染又降低误杀真实 prompt 的风险。
- 本轮规划：优先补 Codex 历史图片占位清洗，价值高（直接解决“历史图片丢失/占位文本污染/锚点异常”）、出现频率高（图片反馈多次出现）、实现成本中低（已有共享 image helper 和 raw block 渲染链路）；暂不扩大到真实桌面点测和 diff 布局继续重构。
- 本轮完成：`src/utils/chatImageBlocks.ts` 新增 `isImagePlaceholderText()`；`src/utils/chatMessageFlow.ts` 在 raw 中存在图片块时过滤纯图片 wrapper 和相邻 base64 残片；`src/utils/chatNavigation.ts` 让搜索/锚点文本也排除图片 wrapper；`src-tauri/src/session_manager/providers/codex.rs` 在 Codex provider 统一 raw blocks 时丢弃同条图片消息里的纯占位 text，保留 `input_image` 和真实用户说明；新增/更新 `chatMessageFlow`、`chatNavigation` 与 Codex provider 回归测试；`.trellis/spec/frontend/component-guidelines.md` 和 `.trellis/spec/backend/cross-layer-protocol.md` 已记录该窄口径跨层规则。
- 本轮验证：`npm test -- src/utils/chatMessageFlow.test.ts src/utils/chatNavigation.test.ts src/components/chat/ContentBlockRenderer.test.tsx` 通过（3 files / 34 tests）；`cargo test --manifest-path src-tauri/Cargo.toml session_manager::providers::codex -- --nocapture` 通过（6 tests）；`npm test` 通过（20 files / 161 tests）；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（87 tests，doc tests 2 ignored）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过（仅既有 `ChatMessageEvent` dead_code warning）；`git diff --check` 无 whitespace error（仅 LF/CRLF 提示）。
- 下一步候选：1) 真实 Tauri 桌面点测历史图片缩略图与 lightbox，确认 `<image name=...>` 不再出现在 transcript/搜索/锚点；2) 继续点测 Codex 长历史锚点跳转是否仍有错位，若有用真实 session jsonl 反推注册/折叠状态；3) 继续优化 diff 三栏在长 diff/150+ 文件时的可视范围、区域 resize 和收起按钮；4) 桌面态验证系统通知与 Open File 点击链路。

## 迭代记录 2026-06-18 13:21 heartbeat

- 上轮实际进展：Codex 历史图片 wrapper 清洗在代码中确实存在，前端 raw 渲染/合并内容/搜索锚点与 Rust Codex provider 都有对应测试；`npm test`、`npm run build`、`cargo test`、`cargo check` 已通过。阻塞仍是未做真实 Tauri 桌面点测，特别是图片 lightbox、系统通知、Open File、长历史锚点跳转。
- 本轮诊断（补齐 cc-gui）：当前项目已经有三栏 diff review，但 `ChatPage` 只在 diff 展开时暴露 conversation-diff 与 diff-status resize handle；一旦用户点击收起 diff，布局退化成“对话 + 固定右侧状态栏”，右侧宽度不能调，和用户要求的“对话框、diff 区域、最右侧区域都可以调节大小”仍有差距。
- 本轮诊断（超越 cc-gui）：diff 收起后不占页面空间是正确方向，但两栏状态也要保持可调；否则用户为了回看长 transcript 还要被右侧状态栏占固定宽度。新增 conversation-status resize handle 可以在无 diff 或 diff 已收起时把空间还给对话区，解决“宝贵页面空间被固定侧栏占住”的痛点。
- 本轮规划：优先补 diff 收起后的右侧调宽能力，价值高（直接改善长 diff/长会话空间分配）、出现频率高（用户会频繁收起 diff 回到对话）、成本低（复用既有 resizer 交互）。同时把 pane width clamp 迁到 `chatUiBehavior` 并补测试，避免页面内继续堆局部尺寸算法。
- 本轮完成：`src/utils/chatUiBehavior.ts` 新增 pane width 常量、`clampPaneResizeDelta()` 与 `getPaneWidthsAfterResize()`；`src/pages/ChatPage.tsx` 复用共享 helper，新增 `conversation-status` resize 边界，并在 diff 不显示时渲染右侧调宽手柄；`src/utils/chatUiBehavior.test.ts` 覆盖左右两侧宽度 clamp；`src/locales/zh.json` / `src/locales/en.json` 增加 `resizeConversationStatus` title/aria 文案；`.trellis/spec/frontend/component-guidelines.md` 已记录该布局契约。
- 本轮验证：`npm test -- src/utils/chatUiBehavior.test.ts src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/StatusPanel.test.tsx` 通过（3 files / 16 tests）；`npm test` 通过（20 files / 162 tests）；`npm run build` 通过；`git diff --check` 无 whitespace error（仅 LF/CRLF 提示）。本轮仅改前端布局、i18n、测试与文档，未运行 Rust check。
- 下一步候选：1) 真实桌面点测三种拖拽边界：conversation-diff、diff-status、conversation-status；2) 继续验证长 diff panel 在 split/unified 模式下是否仍横向挤压，必要时给 diff body 增加更明确的横向滚动/列宽策略；3) 桌面态验证历史图片 lightbox、系统通知、Open File、Codex 锚点跳转。

## 迭代记录 2026-06-18 diff reopen fix

- 上轮实际进展：diff 收起后已完全不渲染中间 diff 面板，并补了 conversation-status resize；代码里确实存在 `diffPaneCollapsed` 状态和右侧宽度调节手柄。阻塞：用户反馈差异对比关闭后没有按钮再次打开，说明恢复入口过于隐式。
- 本轮诊断（补齐 cc-gui）：`ChatPage` 的 `handleSelectedEditChange()` 会在点击右侧文件行时重新展开 diff，但 UI 没有明确告诉用户“这里能重新打开”；如果当前文件已经选中，用户更容易认为 diff 面板被永久关闭。
- 本轮诊断（超越 cc-gui）：收起行为应该可逆且可发现。显式 reopen 按钮能保留“收起后不占空间”的优点，同时避免用户为了恢复 diff 只能猜测点击文件行。
- 本轮规划：在右侧最近改动工具栏增加一个仅在 `isDiffPaneCollapsed && selectedEditKey` 时显示的图标按钮，点击后调用 `onOpenDiffPanel()`；不新增占位面板，不改变文件行点击重新展开的现有逻辑。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `isDiffPaneCollapsed` / `onOpenDiffPanel` props，并在 edit toolbar 渲染 `status-diff-pane-reopen` 按钮；`src/pages/ChatPage.tsx` 传入 `diffPaneCollapsed` 和 `setDiffPaneCollapsed(false)`；`src/locales/zh.json` / `src/locales/en.json` 增加 `expandDiffPanel` 文案；`src/components/chat/StatusPanel.test.tsx` 增加显式恢复按钮回归测试；`.trellis/spec/frontend/component-guidelines.md` 已记录收起后必须有显式恢复入口。
- 本轮验证：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatUiBehavior.test.ts` 通过（3 files / 17 tests）；`npm test` 通过（20 files / 163 tests）；`npm run build` 通过；`git diff --check` 无 whitespace error（仅 LF/CRLF 提示）。本轮仅改前端，不需要 Rust check。
- 下一步候选：1) 桌面态点测：收起 diff 后右侧最近改动工具栏出现展开按钮，点击能恢复同一个 selected edit；2) 继续验证 split/unified 长 diff 横向滚动和列宽；3) 继续验证 Open File、系统通知、Codex 锚点。

## 迭代记录 2026-06-18 diff line wrap

- 上轮实际进展：diff 收起后显式恢复按钮已在代码中存在，`StatusPanel` 会在 `isDiffPaneCollapsed && selectedEditKey` 时展示 `status-diff-pane-reopen`，`ChatPage` 会传入 `setDiffPaneCollapsed(false)`；对应测试和构建已通过。阻塞仍是未做真实 Tauri 桌面点测。
- 本轮诊断（补齐 cc-gui）：cc-gui 主要用状态面板文件变更列表打开 IDE diff；当前项目已有更强的中间三栏 diff pane，但 `EditDiffPreview` panel 样式使用 `max-content` 列和长行不换行，窄三栏下用户容易只能看到局部代码，需要频繁横向滚动。
- 本轮诊断（超越 cc-gui）：中间 diff pane 应默认适配当前面板宽度，让用户在对话区、diff 区和右侧状态区同时存在时也能读完整变更；同时保留不换行模式，满足格式敏感代码或超长行精确审查。
- 本轮规划：优先给中间完整 diff 增加 wrap/no-wrap 展示切换，默认 wrap；价值高（直接对应“diff 看不全”和“三栏空间不足”）、频率高（每次查看长 diff 都会遇到）、成本低（纯展示层 CSS 与小状态），暂不扩大到后端 diff 数据或桌面权限链路。
- 本轮完成：`src/components/toolBlocks/EditDiffPreview.tsx` 新增 panel-only `wrapLines` 支持并默认开启；`src/components/chat/ChatDiffReviewPane.tsx` 增加可发现的图标切换按钮，默认自动换行，切换后进入横向滚动不换行；`src/styles/toolBlocks.css` 为 `edit-diff-panel-wrap` / `edit-diff-panel-nowrap` 分别定义三栏适配和横向滚动策略；`src/locales/zh.json` / `src/locales/en.json` 增加换行模式文案；`EditDiffPreview` 与 `ChatDiffReviewPane` 测试覆盖默认换行和切换入口；`.trellis/spec/frontend/component-guidelines.md` 已记录该 diff pane 约定。
- 本轮验证：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx` 通过（2 files / 7 tests）；`npm run build` 通过；`npm test` 通过（20 files / 166 tests）；`git diff --check` 无 whitespace error（仅 LF/CRLF 提示）。本轮仅改前端展示、i18n、测试与文档，未运行 Rust check。
- 下一步候选：1) 真实桌面点测三栏 diff：默认换行是否解决长 diff 看不全，切换不换行是否保留横向滚动；2) 继续点测 diff 收起/展开按钮和三种 resize handle；3) 继续验证 Open File、系统通知、历史图片 lightbox、Codex 锚点跳转。

## 迭代记录 2026-06-18 diff wrap persistence

- 上轮实际进展：代码中确实存在 diff 默认换行、wrap/no-wrap 图标切换和收起后显式恢复按钮；`EditDiffPreview` 的 panel 变体默认 `wrapLines ?? true`，`StatusPanel` 在 `isDiffPaneCollapsed && selectedEditKey` 时显示 reopen 按钮。阻塞仍是未做真实 Tauri 桌面点测。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 diff 入口更多依赖 IDE 打开；当前项目新增了中间 diff pane 后，用户会频繁在三栏、两栏、收起/展开之间切换。如果换行偏好是 pane 内部 state，收起再展开会丢失用户刚选的审查模式。
- 本轮诊断（超越 cc-gui）：把换行偏好提升到 `ChatPage` 能让三栏审查行为更稳定，用户在看长 diff 时不需要每次重新切换 wrap/no-wrap，解决“收起来再打开又回默认”的重复操作痛点。
- 本轮规划：本轮不扩大改动面，只确认并固化 `ChatPage` 持有 `diffWrapLines`、`ChatDiffReviewPane` 受控渲染的现有实现，再补全全量验证和 Trellis 规范记录。
- 本轮完成：`src/pages/ChatPage.tsx` 已确认持有 `diffWrapLines` 并通过 `onWrapLinesChange={setDiffWrapLines}` 控制中间 diff pane；`src/components/chat/ChatDiffReviewPane.tsx` 已确认 `wrapLines` 为必传受控 prop；`.trellis/spec/frontend/component-guidelines.md` 新增“wrap/no-wrap preference belongs to ChatPage state”的组件契约。
- 本轮验证：`npm test` 通过（20 files / 167 tests）；`npm run build` 通过；`git diff --check` 无 whitespace error（仅 LF/CRLF 提示）。
- 下一步候选：1) 真实桌面点测三栏 diff：wrap/no-wrap 切换后收起再展开是否保持偏好；2) 点测 diff 收起/展开按钮和三种 resize handle；3) 回到仍未闭环的 Open File、系统通知、历史图片 lightbox、Codex 锚点跳转。

## 迭代记录 2026-06-18 open file bridge normalization

- 上轮实际进展：diff wrap 状态已经由 `ChatPage` 持有并完成前端全量验证；阻塞仍是未在真实 Tauri 桌面点测 Open File、系统通知、图片 lightbox 和 Codex 锚点。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 `webview/src/utils/bridge.ts` 会在 `openFile()` 中解码 percent-encoded 路径、剥离 `file://` wrapper、解析 `:line` / `:line-endLine` 后再发给宿主；当前项目 `src/utils/bridge.ts` 直接把原始字符串传给 Tauri，遇到 `my%20file.ts`、`file:///C:/...`、Markdown/Search 里的 `path:line` 更容易打开失败。
- 本轮诊断（超越 cc-gui）：本项目后端已经支持 `cwd`、VS Code/Cursor/JetBrains 多编辑器发现和 Windows slash-drive / WSL 路径归一化；把前端导航字符串先清洗成后端命令参数，可以让右侧状态树、中间 diff、Read/Search 工具块和 Markdown 文件链接共用同一稳定 Open File 链路。
- 本轮规划：优先补 `src/utils/bridge.ts` 的路径清洗和行号解析，并加桥接层单测；不重写后端编辑器发现策略，避免扩大风险。
- 本轮完成：`src/utils/bridge.ts` 新增 `normalizeOpenFileTarget()`、`splitEditorLineSuffix()` 等局部 helper，`openFile()` 现在会 decode `%xx`、剥离 `file://`、解析 `:line` / `:line:column` / `:line-endLine`，显式 line 参数优先级高于路径后缀，并保留 `cwd` 传给 Tauri；`src/utils/bridge.test.ts` 新增回归测试；`.trellis/spec/backend/cross-layer-protocol.md` 更新 Editor Open File 命令契约。
- 本轮验证：`npm test -- src/utils/bridge.test.ts src/utils/toolPresentation.test.ts` 通过（2 files / 37 tests）；`cargo test --manifest-path src-tauri/Cargo.toml editor_commands -- --nocapture` 通过（5 tests）；`npm test` 通过（21 files / 172 tests）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过（仅既有 `ChatMessageEvent` dead_code warning）；`git diff --check` 无 whitespace error（仅 LF/CRLF 提示）。
- 下一步候选：1) 真实 Tauri 桌面点测状态面板、Read/Edit/Search、Markdown 文件链接的 Open File；2) 若仍失败，收集失败路径并补后端 fuzzy filename / project-root display path 兜底；3) 继续点测系统通知、历史图片 lightbox、Codex 长历史锚点。

## 迭代记录 2026-06-18 open file backend fuzzy fallback

- 上轮实际进展：`StatusPanel` 中已确认存在 `status-diff-pane-reopen` 显式恢复按钮，`ChatPage` 会传入 `diffPaneCollapsed` 和 `setDiffPaneCollapsed(false)`；前端 `openFile()` 也已完成 percent decode、`file://` 剥离和 line suffix 解析。阻塞仍是未做真实 Tauri 桌面点测，且后端仍缺 cc-gui 的短路径 fuzzy 兜底。
- 本轮诊断（补齐 cc-gui）：对照 `jetbrains-cc-gui/src/main/java/com/github/claudecodegui/handler/file/OpenFileHandler.java`，cc-gui 在直接路径和 project/session cwd 解析失败后，会通过文件名和路径后缀搜索项目文件；当前项目 `src-tauri/src/commands/editor_commands.rs` 只返回 `cwd + relative`，当工具块/历史只给 `linkify.ts` 或 `utils/linkify.ts` 时仍可能交给编辑器一个不存在的路径。
- 本轮诊断（超越 cc-gui）：Tauri 端没有 IDE 索引，所以不能无界递归扫整个项目；本轮采用有上限的项目目录搜索，并跳过 `.git`、`node_modules`、`target`、`dist` 等生成/依赖目录，优先路径后缀精确匹配，再退回文件名和常见源码目录偏好，避免 Open File 因短路径变慢或误开依赖文件。
- 本轮规划：优先补后端 `resolve_file_path()` fuzzy fallback，并用 Rust 单测锁住短文件名、路径后缀优先级和跳过依赖目录三类边界；暂不扩大到真实桌面启动和点击测试。
- 本轮完成：`src-tauri/src/commands/editor_commands.rs` 新增 bounded fuzzy search helper，`resolve_file_path()` 在 `cwd + relative` 不存在时会按文件名/路径后缀从 `cwd` 下查找最佳文件；同时新增 `MAX_FUZZY_SEARCH_ENTRIES` 与跳过目录规则，避免大型项目卡顿；`.trellis/spec/backend/cross-layer-protocol.md` 已补充 Editor Open File 的后端 fuzzy fallback 跨层契约。
- 本轮验证：`cargo test --manifest-path src-tauri/Cargo.toml editor_commands -- --nocapture` 通过（8 tests）；`cargo check --manifest-path src-tauri/Cargo.toml` 通过（仅既有 `ChatMessageEvent` dead_code warning）；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（90 tests，doc tests 2 ignored）；`npm run build` 通过；`git diff --check` 无 whitespace error（仅 LF/CRLF 提示）。未运行真实 Tauri 桌面点测。
- 下一步候选：1) 运行 `npm run tauri dev` 桌面点测状态树、Read/Edit/Search、Markdown 文件链接 Open File 是否能打开短路径和路径后缀；2) 点测 diff 收起/展开、wrap/no-wrap 和三种 resize handle；3) 继续验证 Windows 右下角系统通知、历史图片 lightbox、Codex 长历史锚点跳转。

## 迭代记录 2026-06-18 session refresh visibility

- 上轮实际进展：后端 Open File fuzzy fallback 已落地并通过 Rust 定向/全量测试；`StatusPanel` 的 diff 重新打开按钮、会话列表缓存、手动项目选择锁、普通工具权限弹窗和原生系统通知主路径在代码中均真实存在。阻塞仍是未做真实 Tauri 桌面点测。
- 本轮诊断（补齐 cc-gui）：会话侧栏已经具备项目/会话分栏与缓存，但强制刷新会话时保留旧列表且只有刷新按钮旋转。用户之前反馈“切项目/加载会话列表慢、列表不变”，在这种状态下容易误判为没有触发刷新。
- 本轮诊断（超越 cc-gui）：刷新时不清空旧列表是正确的，能避免空白闪烁；但需要一个低对比、明确的状态行告诉用户“旧列表仍可看，新数据正在加载”，解决可感知反馈不足的问题。
- 本轮规划：优先给 `ChatSessionSidebar` 增加刷新状态行，价值中高（会话入口高频、直接回应列表不变的误解）、成本低（纯前端展示 + i18n + 单测），不扩大到真实桌面点测或后端扫描策略。
- 本轮完成：`src/components/chat/chatSessionSidebarUtils.ts` 新增 `shouldShowSessionRefreshStatus()`；`src/components/chat/ChatSessionSidebar.tsx` 在刷新已有会话列表时保留旧列表并显示 `role="status"` 的紧凑刷新行；`src/locales/zh.json` / `src/locales/en.json` 新增 `chat.sessionPanel.refreshingSessions`；`src/components/chat/chatSessionSidebarUtils.test.ts` 补充刷新状态判断测试；`.trellis/spec/frontend/component-guidelines.md` 记录刷新时保留列表并显示轻量状态行的组件约定。
- 本轮验证：`npm test -- src/components/chat/chatSessionSidebarUtils.test.ts` 通过（12 tests）；`npm run build` 通过；`npm test` 通过（21 files / 173 tests）；`git diff --check` 无 whitespace error（仅 LF/CRLF 提示）。本轮仅改前端展示/文案/文档，未运行 Rust check。
- 下一步候选：1) 真实 Tauri 桌面点测会话侧栏：切项目、强制刷新、点击历史会话加载与继续发送；2) 真实点测 Open File、系统通知、历史图片 lightbox、Codex 长历史锚点；3) 若桌面点测发现列表扫描仍慢，再回到 Rust session provider 做增量读取/取消扫描优化。

## 迭代记录 2026-06-18 diff reopen discoverability

- 上轮实际进展：会话侧栏刷新状态行已在代码中存在并通过定向测试、全量前端测试和构建；`StatusPanel` 也确实已有 `status-diff-pane-reopen` 按钮。阻塞：用户继续反馈“差异对比关闭，没有按钮再次打开”，说明上一轮按钮虽然存在，但过于隐式且显示条件依赖 `selectedEditKey`，真实使用中仍可能不可见或不够可发现；真实 Tauri 桌面点测仍未完成。
- 本轮诊断（补齐 cc-gui）：对照当前 `StatusPanel.tsx` 代码，diff 恢复入口位于 edit toolbar 左侧的小图标，并且条件是 `isDiffPaneCollapsed && selectedEditKey && onOpenDiffPanel`。如果用户没有明确选中文件、或者只看“最近改动”标题行右侧，就会认为收起后没有恢复入口。
- 本轮诊断（超越 cc-gui）：中间 diff pane 收起后应保持“不占页面空间”，但恢复入口必须固定、右侧、可发现。把入口放到“最近改动”标题行最右侧，并放宽为“diff 已收起 + 当前加载窗口有任意 edit 记录”，可以避免把恢复行为藏在文件行点击或选中状态里。
- 本轮规划：优先修复 diff 恢复入口可发现性，价值高（直接回应用户刚反馈的断点）、频率高（查看 diff 后频繁收起/恢复）、成本低（纯展示层 + 回归测试）；暂不扩大到桌面启动和会话扫描后端优化。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `canReopenDiffPane`，恢复按钮移到“最近改动”标题行右侧，显示条件从依赖 `selectedEditKey` 改为依赖 `allEdits.length > 0`；`src/styles/toolBlocks.css` 为标题行恢复按钮增加更明确的浅/深色主题强调；`src/components/chat/StatusPanel.test.tsx` 把回归测试改为“不传 selectedEditKey 也必须显示恢复入口”；`.trellis/spec/frontend/component-guidelines.md` 更新组件契约，明确恢复入口不应依赖 selected edit。
- 本轮验证：`npm test -- src/components/chat/StatusPanel.test.tsx` 通过（5 tests）；`npm test -- src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatUiBehavior.test.ts` 通过（2 files / 14 tests）；`npm test` 通过（21 files / 173 tests）；`npm run build` 通过；`git diff --check` 无 whitespace error（仅 LF/CRLF 提示）。本轮仅改前端展示、测试与 Trellis 规范，未运行 Rust check。
- 下一步候选：1) 真实 Tauri 桌面点测：收起 diff 后“最近改动”标题行右侧是否一直能看到恢复按钮，点击是否恢复同一条/第一条 edit；2) 继续点测 Open File、系统通知、历史图片 lightbox、Codex 长历史锚点；3) 若会话切项目或历史加载仍慢，回到 Rust session provider 做增量读取/取消扫描优化。

## 迭代记录 2026-06-18 handoff summary filtering

- 上轮实际进展：diff 恢复入口可发现性已经在代码中落地，`StatusPanel` 的恢复按钮条件确实不再依赖 `selectedEditKey`；对应定向测试、全量前端测试、构建和 `git diff --check` 已通过。阻塞：真实 Tauri 桌面点测仍未完成；另外上一轮 handoff-summary 过滤虽然代码和测试已完成，但尚未写入 Trellis 迭代记录。
- 本轮诊断（补齐 cc-gui）：历史会话加载会把运行时 handoff summary 当作用户消息带入 transcript、搜索和锚点，这类内容与 cc-gui 的可见聊天历史语义不一致，属于运行上下文而非用户提示词。
- 本轮诊断（超越 cc-gui）：跨模型续聊需要隐藏上下文转交信息，但不能让这些文字再次进入 provider-switch handoff prompt，否则会污染下一轮模型上下文并制造伪用户输入。统一走 shared protocol-context predicate 可以同时保护 transcript、anchors、search 和 provider handoff。
- 本轮规划：优先扩展共享协议上下文判断，覆盖 `Another language model started to solve this problem...` 与 `## Handoff Summary`，并补前端回归测试；不新增组件分支，避免每个渲染入口自行过滤。
- 本轮完成：`src/utils/chatMessageFlow.ts` 的 `PROTOCOL_CONTEXT_PREFIXES` 增加 handoff summary 前缀；`src/utils/chatMessageFlow.test.ts` 覆盖 handoff summary 不渲染；`src/utils/chatNavigation.test.ts` 覆盖 handoff summary 不进入 renderable messages 与 anchors；`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/backend/cross-layer-protocol.md` 已记录该协议上下文规则。
- 本轮验证：`npm test -- src/utils/chatMessageFlow.test.ts src/utils/chatNavigation.test.ts` 通过（2 files / 28 tests）；`npm test -- src/components/chat/ContentBlockRenderer.test.tsx src/components/chat/MessageAnchorRail.test.tsx` 通过（2 files / 12 tests）；`npm test` 通过（21 files / 175 tests）；`npm run build` 通过；`git diff --check` 无 whitespace error（仅 LF/CRLF 提示）。本轮是前端共享过滤与文档更新，未运行 Rust check。
- 下一步候选：1) 真实 Tauri 桌面点测：历史会话中 AGENTS、heartbeat、handoff summary、system prompt 不显示且不生成锚点；2) 继续点测 diff 收起/恢复、Open File、系统通知、历史图片 lightbox；3) 若长历史锚点仍错位，继续用真实 Codex/Claude session jsonl 反推折叠窗口与 DOM 注册状态。

## 迭代记录 2026-06-18 large edit tree default collapse

- 上轮实际进展：handoff summary 过滤已在代码中确实存在，本轮已补齐 Trellis 记录；diff 恢复按钮和三栏 diff 的前端验证仍保持通过。阻塞：真实 Tauri 桌面点测仍未完成，尤其是 Open File、系统通知、图片 lightbox 和长历史锚点。
- 本轮诊断（补齐 cc-gui）：状态面板已经有树状文件列表、文件夹展开/收起按钮和独立滚动区，但 `collapsedFolders` 初始为空，导致大量改动文件默认全展开；用户之前明确反馈 150 个文件时列表难滚动，希望是树状结构并能收缩展开。
- 本轮诊断（超越 cc-gui）：大文件数默认折叠到文件夹行，小文件数保持展开，能兼顾扫描效率和直接可见性；同时保留用户手动展开/收起后的选择，避免流式更新时反复重置视图。
- 本轮规划：只改 `StatusPanel` 的展示默认值和状态保持逻辑，价值高（大量修改场景高频且痛点明显）、成本低（纯前端展示）、风险低（不碰后端和 diff 数据聚合）。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `AUTO_COLLAPSE_EDIT_TREE_FILE_THRESHOLD`、编辑集 key 和默认折叠文件夹集合；超过阈值时初始收起文件夹，小列表保持展开，用户手动调整后在后续 transcript 更新中保留该选择；`src/components/chat/StatusPanel.test.tsx` 新增 30 文件默认折叠回归测试，并确认既有小列表树仍展开；`.trellis/spec/frontend/component-guidelines.md` 记录大文件树默认折叠契约。
- 本轮验证：`npm test -- src/components/chat/StatusPanel.test.tsx` 通过（1 file / 6 tests）；`npm test -- src/utils/chatMessageFlow.test.ts src/utils/chatNavigation.test.ts` 通过（2 files / 28 tests）；`npm test` 通过（21 files / 176 tests）；`npm run build` 通过；`git diff --check` 无 whitespace error（仅 LF/CRLF 提示）。本轮仅改前端展示、测试与 Trellis spec，未运行 Rust check。
- 下一步候选：1) 真实 Tauri 桌面点测：150+ 文件状态树默认是否折叠、展开/收起按钮是否顺手、diff 恢复按钮是否可见；2) 继续点测 Open File、系统通知、历史图片 lightbox、Codex 长历史锚点；3) 若会话切项目或历史加载仍慢，进入 Rust session provider 的增量扫描/取消扫描优化。

## 迭代记录 2026-06-18 session cache project boundary

- 上轮实际进展：大文件树默认折叠已经在代码中落地，`StatusPanel` 有阈值默认折叠、用户手动状态保留和回归测试；`npm test`、`npm run build`、`git diff --check` 均已通过。阻塞仍是未做真实 Tauri 桌面点测。
- 本轮诊断（补齐 cc-gui）：用户曾反馈“点击 ccg-switch 项目有会话，再切换到别的项目，会话列表不变”。当前会话侧栏已经有项目列表、缓存、in-flight 去重和刷新状态，但 `rememberProjectSessions()` 会把一次 `list_sessions(projectPath)` 返回的所有 `session.projectDir` 都写成缓存别名；如果后端扫描结果混入其它项目 session，前端就可能把旧项目结果缓存到新项目 key。
- 本轮诊断（超越 cc-gui）：缓存应提升速度，但不能牺牲项目边界。前端需要把“明确属于其它 projectDir 的 session”挡在展示和缓存之外，避免一次脏扫描污染后续项目切换，让用户误判为点击项目没有生效。
- 本轮规划：优先收紧 `chatSessionSidebarUtils` 的项目过滤与缓存别名策略。价值高（直接对应会话列表不变反馈）、成本低（纯 helper + 单测）、风险低（保留 `projectDir=null` 的 legacy session，避免误删无法归属但已由后端 scoped 的历史）。
- 本轮完成：`src/components/chat/chatSessionSidebarUtils.ts` 新增 `filterProjectChatSessions()` 与项目归属判断；`rememberProjectSessions()` 只保留 provider 支持且 `projectDir` 为空或等于当前项目的 session，并且不再把明确不同项目的 `projectDir` 写入 cache key；`src/components/chat/chatSessionSidebarUtils.test.ts` 新增跨项目过滤和缓存污染回归测试；`.trellis/spec/frontend/component-guidelines.md` 记录会话缓存不得跨项目污染的组件契约。
- 本轮验证：`npm test -- src/components/chat/chatSessionSidebarUtils.test.ts` 通过（1 file / 14 tests）；`npm test -- src/stores/useChatStore.test.ts src/utils/chatNavigation.test.ts` 通过（2 files / 29 tests）；`npm test` 通过（21 files / 178 tests）；`npm run build` 通过；`git diff --check` 无 whitespace error（仅 LF/CRLF 提示）。本轮仅改前端 helper、测试与 Trellis spec，未运行 Rust check。
- 下一步候选：1) 真实 Tauri 桌面点测：切换 ccg-switch 与其它项目时，旧会话列表是否立即清空/刷新且不被缓存污染；2) 若桌面点测仍慢，再进入 Rust `list_sessions(projectPath)` 做增量扫描和取消扫描优化；3) 继续点测 Open File、系统通知、历史图片 lightbox、Codex 长历史锚点。

## 迭代记录 2026-06-18 codex session metadata cache

- 上轮实际进展：前端会话缓存项目边界已经在代码中落地，`rememberProjectSessions()` 会过滤明确属于其他项目的 session，相关前端测试/构建通过。阻塞仍是未做真实 Tauri 桌面点测；用户此前反馈的“点击项目/加载会话慢”在后端仍可能由 Codex 会话目录重复扫描放大。
- 本轮诊断（补齐 cc-gui）：对照 cc-gui 的 `SessionLoadService`，历史加载请求显式携带 `sessionId + projectPath`，项目语境是核心约束。当前项目后端 `list_sessions(projectPath)` 已先读 `session_meta.payload.cwd` 过滤无关 JSONL，但每次项目切换仍会递归收集并重新打开解析 `.codex/sessions` 下所有历史文件。
- 本轮诊断（超越 cc-gui）：前端缓存解决的是同项目重复点击；后端 metadata cache 解决的是跨项目来回切换。用 JSONL 路径 + 文件长度 + modified timestamp 做失效键，可以在不改变 Tauri 命令协议的前提下，复用已解析的 `cwd/title/timestamps`，让会话列表刷新更接近“只确认文件是否变更”，降低假死感。
- 本轮规划：只改 Codex provider 的会话列表扫描，不碰历史消息加载、前端侧栏和现有返回结构。优先级高：用户价值高（会话入口高频）、出现频率高（项目切换/刷新）、实现成本中低（provider 内部缓存 + Rust 单测）。
- 本轮完成：`src-tauri/src/session_manager/providers/codex.rs` 新增进程内 `CODEX_SESSION_META_CACHE`，缓存 `session_id/project_dir/title/created_at/last_active_at/source_path/title_scanned`；项目不匹配时只缓存 `cwd` 和时间戳，不继续读取标题；之后切到匹配项目时会补读标题并更新缓存；文件长度或 modified timestamp 变化会自动失效；`.trellis/spec/backend/cross-layer-protocol.md` 同步记录 Codex session metadata cache 契约。
- 本轮验证：`cargo fmt --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml session_manager::providers::codex -- --nocapture` 通过（8 tests）；`cargo check --manifest-path src-tauri/Cargo.toml` 通过（仅既有 `ChatMessageEvent` dead_code warning）；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（92 tests，doc tests 2 ignored）；`npm test` 通过（21 files / 178 tests）；`npm run build` 通过；`git diff --check` 无 whitespace error（仅 LF/CRLF 提示）。未运行真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测会话侧栏项目切换耗时与列表是否清空/刷新；2) 继续点测 diff 恢复按钮、Open File、系统通知、历史图片 lightbox、Codex 长历史锚点；3) 若项目切换仍慢，继续把 Codex 文件枚举本身做目录级缓存或最近日期目录优先策略。

## 迭代记录 2026-06-18 codex session file list cache

- 上轮实际进展：`src-tauri/src/session_manager/providers/codex.rs` 中已确认存在进程内 `CODEX_SESSION_META_CACHE`，可以按 JSONL 路径 + 文件长度 + modified timestamp 复用 `cwd/title/timestamps`；`StatusPanel` 的 diff 恢复入口也已在代码中确认存在，条件是 `diffPaneCollapsed && allEdits.length > 0`，不再依赖 `selectedEditKey`。阻塞仍是未运行真实 Tauri 桌面点测，无法直接量化项目切换列表耗时和按钮可见性。
- 本轮诊断（补齐 cc-gui）：cc-gui 的会话加载始终带 `sessionId + projectPath`，项目切换的响应应稳定且明确。当前项目虽然已避免重复解析 Codex JSONL 内容，但每次 `list_sessions(projectPath)` 仍会重新递归枚举 `~/.codex/sessions` 下所有日期目录和 JSONL 文件，大历史目录下仍可能表现为切项目慢或刷新假死。
- 本轮诊断（超越 cc-gui）：只缓存 per-file metadata 还不够，目录枚举本身也应缓存；但不能用纯 project filter 缓存，否则新增/删除会话可能不可见。本轮采用目录树 stamp 作为文件列表缓存失效依据：项目间切换复用 JSONL 路径列表，新增/删除会话通过日期目录 modified timestamp 触发重新枚举，文件内容变化仍交给 per-file metadata cache。
- 本轮规划：优先补 Codex provider 内部文件列表缓存，价值高（直接针对会话入口慢）、频率高（项目切换/刷新/项目 provider map 都会触发）、实现成本中（Rust provider 内部改动 + 单测），不改变 Tauri 命令参数和前端状态结构。
- 本轮完成：`src-tauri/src/session_manager/providers/codex.rs` 新增 `CODEX_SESSION_FILE_LIST_CACHE`、`CodexSessionDirectoryStamp`、`CachedCodexSessionFileList` 和 `collect_jsonl_files_cached()`；`scan_codex_sessions_inner()` 与 `scan_codex_project_dirs()` 统一复用缓存路径列表；新增两个 Rust 单测覆盖“目录 stamp 相同则复用缓存”和“目录 stamp 变化则重新枚举”；为避免全量测试并发污染全局缓存，新增测试级 mutex 隔离文件列表缓存测试；`.trellis/spec/backend/cross-layer-protocol.md` 记录目录级缓存的失效边界。
- 本轮验证：`cargo fmt --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml session_manager::providers::codex -- --nocapture` 通过（10 tests）；`cargo check --manifest-path src-tauri/Cargo.toml` 通过（仅既有 `ChatMessageEvent` dead_code warning）；`npm test` 通过（21 files / 178 tests）；`npm run build` 通过；首次并行 `cargo test --manifest-path src-tauri/Cargo.toml` 暴露新增测试共享全局缓存的隔离问题，修复后复跑通过（94 tests，doc tests 2 ignored）；`git diff --check` 无 whitespace error（仅 LF/CRLF 提示）。未运行真实 Tauri 桌面点测。
- 下一步候选：1) 运行真实 `npm run tauri dev` 点测会话侧栏：切换 ccg-switch 与其它项目时列表是否立即清空/刷新、第二次切换是否明显变快；2) 点测 diff 收起后右侧恢复按钮是否始终可见且能恢复同一/第一条 edit；3) 继续点测 Open File、系统通知、历史图片 lightbox、Codex 长历史锚点；4) 若会话列表仍慢，再考虑最近日期目录优先或给 `list_sessions` 增加显式 force/refresh 参数。

## 迭代记录 2026-06-18 diff reopen global affordance 待确认

- 上轮实际进展：Codex 会话文件列表缓存已在代码中存在，`scan_codex_sessions_inner()` 与 `scan_codex_project_dirs()` 复用目录 stamp 缓存；上一轮验证记录显示 Rust 定向/全量测试、前端测试、构建均通过。阻塞仍是 `npm run tauri dev` 被已有 1420 Vite 进程占用，真实桌面点测未完成。
- 本轮诊断（补齐 cc-gui）：`StatusPanel.tsx` 当前确实有 `canReopenDiffPane = isDiffPaneCollapsed && allEdits.length > 0 && onOpenDiffPanel`，恢复按钮渲染在右侧状态栏“最近改动”标题行。但 `StatusPanel` 外层是 `hidden xl:block`，且中间 `ChatDiffReviewPane` 收起后完全卸载；在非 xl 宽度、右侧栏不可见或用户视线不在右侧栏时，会表现为“差异对比关闭后没有按钮再次打开”。
- 本轮诊断（超越 cc-gui）：收起 diff pane 不应继续占用中间空间，但恢复入口应独立于状态栏可见性。页面级浮动恢复按钮可以固定在 Chat review 区域最右侧，只在 `diffPaneCollapsed && selectedEdit` 时出现，点击后恢复中间 diff pane，同时保留右侧状态栏已有入口。
- 本轮规划：按 `AGENTS.md` bug 修复确认机制，先等待用户确认再改代码。拟改范围：`src/pages/ChatPage.tsx` 增加页面级恢复按钮；`src/styles/toolBlocks.css` 或现有全局样式增加按钮样式；`src/locales/zh.json` / `src/locales/en.json` 复用或补充 i18n 文案；`src/components/chat/ChatDiffReviewPane.test.tsx` 或 `src/utils/chatUiBehavior.test.ts` 以现有测试能力补回归；`.trellis/spec/frontend/component-guidelines.md` 记录恢复入口不依赖右侧栏。
- 本轮验证：本轮仅完成诊断与方案记录，未改业务代码，未运行构建/测试。
- 下一步候选：用户确认后先实现 diff 页面级恢复入口并跑 `npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatUiBehavior.test.ts`、`npm test`、`npm run build`、`git diff --check`；随后继续真实桌面点测 diff 收起/恢复、会话切换、Open File、系统通知、历史图片 lightbox 与 Codex 锚点。

## 迭代记录 2026-06-18 diff reopen global affordance

- 上轮实际进展：上一条记录已确认 diff 恢复入口的真实缺口：`StatusPanel` 的恢复按钮存在，但右侧状态栏在 `xl` 以下隐藏，且用户视线不一定落在右侧栏，所以收起中间 diff pane 后仍可能表现为没有重新打开按钮。阻塞仍是没有完成 Tauri 桌面态点测。
- 本轮诊断（补齐 cc-gui）：当前项目已经有中间 diff pane、右侧 edit tree、split/unified toggle 和收起按钮；缺的是不依赖状态栏可见性的恢复入口。cc-gui 这类 IDE 风格面板交互通常保留显式 affordance，本项目原先把 affordance 放在可能隐藏的区域，和用户反馈一致。
- 本轮诊断（超越 cc-gui）：恢复入口应满足两个约束：一是收起 diff pane 后中间区域完全释放，不保留占位列；二是任何宽度下都能发现恢复按钮。本轮采用 Chat review layout 右侧浮动按钮，只在 `diffPaneCollapsed && selectedEdit` 时显示，按钮浮在布局上方而不参与三栏 flex 宽度分配。
- 本轮规划：优先修复 diff 恢复入口可发现性，价值高（直接回应最新用户反馈）、频率高（diff 查看/收起是高频操作）、实现成本低（纯前端 helper + UI + CSS + 回归测试），不扩大到真实 Tauri 点测或后端逻辑。
- 本轮完成：`src/utils/chatUiBehavior.ts` 新增 `shouldShowDiffPaneReopenControl()` 并由 `src/pages/ChatPage.tsx` 复用；`ChatPage` 在 diff 已收起且仍有 selected edit 时渲染 `chat-diff-pane-reopen-floating` 右侧浮动按钮，点击恢复中间 diff pane；`src/styles/toolBlocks.css` 为 Chat review layout 增加相对定位和浅/深色浮动按钮样式；`src/utils/chatUiBehavior.test.ts` 先红后绿覆盖恢复入口显示条件；`.trellis/spec/frontend/component-guidelines.md` 记录恢复入口不能只依赖右侧状态栏。
- 本轮验证：先运行 `npm test -- src/utils/chatUiBehavior.test.ts` 得到预期 RED（`shouldShowDiffPaneReopenControl is not a function`）；实现后复跑通过（10 tests）；`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatUiBehavior.test.ts` 通过（3 files / 21 tests）；`npm test` 通过（21 files / 179 tests）；`npm run build` 通过；`git diff --check` 无 whitespace error，仅既有 LF/CRLF 提示。未运行 Rust check，因为本轮只改前端展示/helper/spec；未完成真实 Tauri 桌面点测。
- 下一步候选：1) 复用现有 1420 Vite 服务或调整 Tauri dev 启动方式，真实点测 diff 收起后右侧浮动恢复按钮是否可见且能恢复；2) 继续点测会话侧栏项目切换、历史加载耗时和列表缓存边界；3) 继续点测 Open File、Windows 系统通知、历史图片 lightbox、Codex 长历史锚点跳转；4) 若真实点测确认恢复按钮位置遮挡状态栏内容，再把浮动按钮改为贴边半露出的 vertical tab。

## 迭代记录 2026-06-18 session list project owner guard

- 上轮实际进展：diff 收起后的页面级恢复入口已在代码中存在，并通过定向测试、全量前端测试、构建和 `git diff --check`；阻塞仍是尚未完成真实 Tauri 桌面点测。两份项目路径本轮已确认可读：`C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui`，两边 `.codegraph/` 也存在。
- 本轮诊断（补齐 cc-gui）：cc-gui 的会话加载服务明确携带 `sessionId + projectPath`，项目语境是会话历史的边界。当前项目虽然已有项目缓存、跨项目过滤和 in-flight 去重，但 UI 只有一个 `sessions` 数组；如果项目切换、缓存更新或请求竞态出现状态漂移，旧项目列表仍可能短暂展示在新选中项目下，让用户继续感觉“切项目后列表没变”。
- 本轮诊断（超越 cc-gui）：会话列表不只要缓存快，还要让“当前列表属于哪个项目”成为状态契约。将列表归属项目独立记录，可以在新项目扫描未完成时立即隐藏旧项目行，同时强制刷新失败时保留当前项目旧列表，降低空白闪烁和误判。
- 本轮规划：优先做前端小范围状态守卫，价值高（直接对应项目切换列表不变反馈）、频率高（会话侧栏高频入口）、成本低（纯 helper + 组件状态 + 单测），暂不扩大到 Rust provider 新扫描策略。
- 本轮完成：`src/components/chat/chatSessionSidebarUtils.ts` 新增 `getVisibleProjectSessions()`，只有列表归属项目与当前选中项目规范化一致时才返回可见列表，并继续过滤 unsupported provider 和其它项目 session；`src/components/chat/ChatSessionSidebar.tsx` 新增 `sessionsProjectPath`，缓存命中、清空、成功和失败分支都会标记列表归属，渲染和搜索统一使用 `visibleSessions`，强制刷新失败不清空当前项目已有列表；`src/components/chat/chatSessionSidebarUtils.test.ts` 新增旧项目列表隐藏、当前项目列表过滤两条回归测试；`.trellis/spec/frontend/component-guidelines.md` 补充会话列表必须跟踪列表归属项目的组件契约。
- 本轮验证：`npm test -- src/components/chat/chatSessionSidebarUtils.test.ts` 通过（16 tests）；`npm test -- src/components/chat/chatSessionSidebarUtils.test.ts src/stores/useChatStore.test.ts src/utils/chatNavigation.test.ts` 通过（3 files / 45 tests）；`npm run build` 通过；`npm test` 通过（21 files / 181 tests）；`git diff --check` 无 whitespace error，仅既有 LF/CRLF 提示。本轮仅改前端组件/helper/spec/TODO，未运行 Rust check；真实 Tauri 桌面点测仍未完成。
- 下一步候选：1) 真实 Tauri 桌面点测会话侧栏：在 `ccg-switch` 与其它项目间切换，确认旧项目行立即隐藏、缓存命中正常、刷新失败不误清空当前项目列表；2) 复测 diff 收起后的页面级恢复按钮、Open File、Windows 右下角系统通知、历史图片 lightbox、Codex 长历史锚点；3) 若真实点测仍感觉切项目慢，再进入 Rust `list_sessions(projectPath)` 做最近日期目录优先、显式刷新/取消扫描或增量返回。

## 迭代记录 2026-06-18 turn stopped notification dedupe

- 上轮实际进展：会话列表项目归属守卫已经在代码中落地，`ChatSessionSidebar` 用 `sessionsProjectPath` 防止旧项目列表展示到新项目下，相关测试、构建和 `git diff --check` 已通过。阻塞仍是没有完成真实 Tauri 桌面点测；本轮确认 1420 前端服务可访问（`HTTP 200`），但未重启/接管 Tauri 桌面进程。
- 本轮诊断（补齐 cc-gui）：用户明确要求任务完成、中断等停止输出场景要走 Windows 右下角通知。当前代码中 `chat://done` success/error、`abort()` 和 `chat_send` 失败路径已经调用 `notifyStoppedRequestOnce()`，但 store 层缺少“同一 request 多个终止事件只通知一次”的回归测试，未来改动容易造成重复 OS 通知。
- 本轮诊断（超越 cc-gui）：桌面通知要可靠但不能打扰。daemon 可能重复发 terminal done，用户 abort 后也可能再收到迟到 done；以 request id 去重能保证用户收到停止状态，但不会因为重复事件被连续弹窗打断。
- 本轮规划：不改运行时代码，优先补 store 级幂等回归测试并同步状态管理规范。价值中高（直接覆盖用户要求的系统通知行为）、成本低（单测 + spec）、风险低（不触碰 Tauri 通知命令实现）。
- 本轮完成：`src/stores/useChatStore.test.ts` 新增 `deduplicates stopped notifications for the same request id`，模拟同一 `chat://done` 连续到达两次，只允许 `notifyChatTurnStopped()` 调用一次；`.trellis/spec/frontend/state-management.md` 新增 Chat Turn-Stopped Notifications 场景，记录 success/error/aborted/send-error 覆盖范围、request id 去重和测试要求。
- 本轮验证：`npm test -- src/stores/useChatStore.test.ts` 通过（18 tests）；`npm test -- src/stores/useChatStore.test.ts src/utils/desktopNotification.test.ts` 通过（2 files / 26 tests）；`npm run build` 通过；`npm test` 通过（21 files / 182 tests）；`git diff --check` 无 whitespace error，仅既有 LF/CRLF 提示。本轮仅改前端测试、Trellis spec/TODO，未运行 Rust check；真实 Windows 通知弹窗仍需在 Tauri 桌面态点测。
- 下一步候选：1) 真实 Tauri 桌面点测 Windows 右下角通知：完成、失败、abort 是否各弹一次且重复 done 不重复弹；2) 点测会话侧栏项目切换和历史加载速度；3) 继续点测 diff 全局恢复按钮、Open File、历史图片 lightbox、Codex 长历史锚点；4) 若真实点测仍不能覆盖，可考虑给 Tauri `chat_show_system_notification` 增加后端命令级 smoke test。

## 迭代记录 2026-06-19 notification backend guard 待确认

- 上轮实际进展：会话停止通知的前端 store 层去重测试已在代码中存在，`notifyStoppedRequestOnce()` 能避免同一 request id 重复触发系统通知；`desktopNotification.ts` 已优先调用 Tauri 原生命令，失败后回退 Web Notification。阻塞仍是未做真实 Tauri 桌面点测，无法直接观察 Windows 右下角弹窗。
- 本轮诊断（补齐 cc-gui）：本轮已确认 `src-tauri/src/lib.rs` 初始化了 `tauri_plugin_notification`，`chat_show_system_notification` 已注册到 `generate_handler!`，`src-tauri/capabilities/default.json` 和 `src-tauri/tauri.conf.json` 均包含 `notification:default`，`src-tauri/Cargo.toml` 已包含 `tauri-plugin-notification = "2.3.3"`。因此当前主链路不是“未接插件/权限”，而是 Rust 命令 payload 边界缺少单测守护。
- 本轮诊断（超越 cc-gui）：系统通知是长任务完成后的跨窗口提醒，应该稳定但不打扰。把标题/正文 trim、空标题拒绝等边界抽成纯 helper 并补 Rust 单测，可以在不触发真实 OS 通知的情况下守住命令入参契约，降低后续改动造成通知悄悄失效的风险。
- 本轮规划：根据 `AGENTS.md` 的 Bug 修复确认机制，本轮先不改业务代码。拟在用户确认后，仅修改 `src-tauri/src/commands/chat_commands.rs`：新增私有 `normalize_system_notification_payload(title, body) -> Result<(&str, &str), String>` 或等价 owned helper，`chat_show_system_notification` 复用它，并添加 `#[cfg(test)]` 单测覆盖 title/body trim、空 title 报错、空 body 允许发送。若实现需要 owned 字符串，会选择最小生命周期风险方案。
- 本轮验证：本轮只做只读核实与方案记录，未运行构建/测试，未改业务代码。
- 下一步候选：1) 用户确认后实现通知 payload helper + Rust 单测，运行 `cargo test --manifest-path src-tauri/Cargo.toml commands::chat_commands -- --nocapture`、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm test -- src/stores/useChatStore.test.ts src/utils/desktopNotification.test.ts`、`npm run build`、`git diff --check`；2) 真实 Tauri 桌面点测 Windows 通知；3) 继续点测会话侧栏项目切换、diff 全局恢复按钮、Open File、历史图片 lightbox、Codex 长历史锚点。

## 迭代记录 2026-06-19 notification backend guard 等待确认

- 上轮实际进展：`TODO_LIST.md` 已记录通知后端 payload guard 的具体方案；本轮复查确认两份项目路径与 `.codegraph/` 仍可读，工作区仍有大量既有未提交改动。
- 本轮阻塞：根据 `AGENTS.md`，Bug 修复/脚本执行/高风险文件修改必须先出方案并等待用户确认。当前尚未收到对 `src-tauri/src/commands/chat_commands.rs` 通知 payload helper + Rust 单测方案的明确确认，因此本轮不改业务代码、不运行验证命令。
- 下一步候选：用户确认后实施上一条方案并执行 Rust/前端验证；若用户希望跳过该 guard，则转入真实 Tauri 桌面点测会话切换、diff 恢复按钮、Open File、系统通知、历史图片 lightbox 与 Codex 锚点。

## 迭代记录 2026-06-19 chat interaction bug audit 待确认

- 上轮实际进展：通知后端 payload guard 仍处于待确认状态，尚未改业务代码；用户最新要求转向“仔细排查聊天交互方面的 bug”。本轮按 Trellis 恢复记录，并用 CodeGraph 复查 `ccg-switch` 与 `jetbrains-cc-gui` 关键聊天链路。两份代码路径均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：`src-tauri/src/chat/manager.rs` 已给 `chat://stream` / `chat://done` 带 `requestId`，但 `src/stores/useChatStore.ts` 的 stream/done/message 监听仍按全局“最后一条 streaming assistant”处理，没有校验 active request。`chat://message` 事件甚至没有携带 `requestId`。这会导致 abort、切会话、新建会话、切 provider 后的迟到事件污染当前会话或错误结束新请求。cc-gui 后端会把发送时的 provider/session/model 固定在 `SessionState` / `runtimeSessionEpoch` 一类会话状态中，本项目前端全局 store 更需要显式 request ownership guard。
- 本轮诊断（补齐 cc-gui）：`src/components/chat/composer/ButtonArea.tsx` 在 `isLoading` 时仍允许切 provider / mode / model / reasoning；`src/stores/useChatStore.ts` 的 `setProvider()` 会清空 `sessionId/activeSession` 并设置 handoff。流式输出中切换这些选项会放大上面的 request 归属错乱。
- 本轮诊断（超越 cc-gui）：`src/components/chat/composer/useCompletions.ts` 在补全菜单 loading 且 `items.length === 0` 时不消费 Enter/Tab，`ChatComposer` 会继续把半成品 `/`、`@path`、`#agent` 直接发送。现有 `useCompletions.test.ts` 只覆盖纯函数，缺少键盘交互回归。
- 本轮诊断（超越 cc-gui）：`src/components/chat/composer/ChatComposer.tsx` 会在 `await send(...)` 前立即 `setAttachments([])`；而 store 的 `send()` catch 会吞掉 `chat_send` 错误，只写 error 状态，不把失败反馈给 composer。后端 invoke 失败时图片附件会丢失，用户需要重新上传。
- 本轮诊断（补齐 cc-gui）：`src/components/chat/AskUserQuestionDialog.tsx` 的 `handleCancel()` 同时调用 `onAnswer({})` 和 `onCancel()`，而 `src/pages/ChatPage.tsx` 又把 `onCancel` 绑定为 `answerAskUserQuestion(...,{})`，取消/点击遮罩会对同一 request 响应两次，可能造成权限响应竞态或后端日志噪声。
- 本轮规划：根据 `AGENTS.md` bug 修复确认机制，本轮只记录排查结果与方案，不直接改业务代码。建议第一批优先修 P0/P1：1) request ownership guard + `chat://message` requestId 补齐；2) active request 期间冻结 provider/model/mode/reasoning 的 UI 和 store action；3) AskUserQuestion 取消只响应一次。第二批再修补全 loading Enter/Tab 与发送失败附件恢复，因为它们影响面较小但体验价值明确。
- 本轮验证：本轮只读核实与 Trellis 记录更新，未运行构建/测试，未改业务代码。用户确认后先写失败测试，再实现，最后执行 `npm test -- src/stores/useChatStore.test.ts src/components/chat/composer/useCompletions.test.ts`、`npm test`、`npm run build`、如改 Rust 事件 payload 则执行 `cargo test --manifest-path src-tauri/Cargo.toml chat -- --nocapture`、`cargo check --manifest-path src-tauri/Cargo.toml`、`git diff --check`。
- 下一步候选：用户确认后先进入第一批修复；若需要更稳，可拆成 P0 request ownership 单独一轮，权限/输入区交互作为后一轮。

## 迭代记录 2026-06-19 chat request ownership guard

- 上轮实际进展：用户已确认 `chat interaction bug audit` 中的第一批修复方案。本轮复查确认两份代码路径可读：`C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui`；两边 `.codegraph/` 均存在。记录里声称的风险也在代码中确实存在：`chat://stream` / `chat://done` 之前虽然有 requestId，但前端监听缺少归属过滤；`chat://message` 之前缺少 requestId；composer selector 在 loading 中仍可点击；AskUserQuestion cancel 路径存在重复响应风险。
- 本轮诊断（补齐 cc-gui）：cc-gui 的会话/运行状态会把一次发送的 provider、session、model 与 runtime 请求绑定；本项目全局 Zustand store 必须显式按 `requestId` 拦截迟到 `stream/message/done`，否则 abort、切会话或新建会话后的旧事件会污染当前 transcript。进一步排查还发现一个更细竞态：旧 request 已 done/abort 后，如果用户马上发新请求，而新 `chat_send` 还没返回 requestId，旧 request 的迟到事件会借新的 streaming assistant 绑定进去。
- 本轮诊断（超越 cc-gui）：active turn 期间不仅要禁用 UI 下拉，也要让 store action 自身 no-op，防止测试、快捷键或未来入口绕过 UI；AskUserQuestion 取消/遮罩/重复点击应是幂等响应，避免后端权限文件被写两次导致日志噪声或等待状态异常。
- 本轮规划：第一批只修 P0/P1 交互正确性：1) request ownership guard + `chat://message` 携带 requestId；2) active request 期间冻结 provider/model/permission mode/reasoning；3) AskUserQuestion 取消只响应一次。暂不处理第二批 `/` 补全 loading Enter/Tab 和发送失败附件恢复，避免本轮范围过大。
- 本轮完成：`src-tauri/src/models/chat.rs` 扩展 Rust `ChatMessageEvent { requestId, json }`；`src-tauri/src/chat/manager.rs` 用该 struct emit `chat://message` 并保留原始 stream 行；`src/types/chat.ts` 新增前端 `ChatMessageEvent`；`src/stores/useChatStore.ts` 新增 request event 归属过滤、pending request 绑定、retired request id 集合、active turn setter no-op 和 AskUserQuestion 乐观清空/失败恢复；`src/components/chat/AskUserQuestionDialog.tsx` 的 cancel 路径改为单入口；`src/components/chat/composer/SelectorDropdown.tsx` 支持 disabled 并自动关闭菜单；`src/components/chat/composer/ButtonArea.tsx` 在 `isLoading` 时禁用 provider/mode/model/reasoning selector；`src/stores/useChatStore.test.ts` 与 `src/components/chat/composer/ButtonArea.test.tsx` 补齐回归测试；`.trellis/spec/frontend/state-management.md` 和 `.trellis/spec/backend/cross-layer-protocol.md` 同步记录请求归属与事件 payload 契约。
- 本轮验证：先新增回归测试并确认 RED：`npm test -- src/stores/useChatStore.test.ts` 失败于“retired request 绑定下一轮 pending turn”和“并发 AskUserQuestion 重复 invoke”；实现后 `npm test -- src/stores/useChatStore.test.ts` 通过（23 tests）；`npm test -- src/stores/useChatStore.test.ts src/components/chat/composer/ButtonArea.test.tsx` 通过（2 files / 24 tests）；`npm test` 通过（22 files / 188 tests）；首次 `npm run build` 暴露新增测试用了项目 TS lib 不支持的 `Array.prototype.at()`，已改成兼容索引写法；复跑 `npm run build` 通过；`cargo fmt --manifest-path src-tauri/Cargo.toml` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过且已无 `ChatMessageEvent` dead_code warning；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（94 tests，2 doctests ignored）。尚未运行真实 Tauri 桌面点测。
- 下一步候选：1) 第二批聊天交互修复：补全菜单 loading 时拦截 Enter/Tab，避免发送半成品 `/`、`@path`、`#agent`；2) 发送失败时恢复 composer 图片附件，避免 `chat_send` invoke 失败后用户必须重新上传；3) 真实 Tauri 桌面点测 request ownership、权限弹窗取消、会话切换、diff 恢复按钮、Open File、系统通知、历史图片 lightbox 与 Codex 长历史锚点。

## 迭代记录 2026-06-19 composer completion and failed send recovery

- 上轮实际进展：request ownership guard 已在代码中落地并验证，`chat://message` 已携带 requestId，active turn selector 与 store setter 已冻结，AskUserQuestion 响应已幂等；`npm test`、`npm run build`、`cargo check`、`cargo test` 均通过。阻塞仍是没有真实 Tauri 桌面点测。
- 本轮诊断（补齐 cc-gui）：对照 `jetbrains-cc-gui/webview/src/components/ChatInputBox/hooks/useCompletionDropdown.ts`，补全菜单只要打开就会消费 Enter/Tab；当前项目 `useCompletions.handleKeyDown()` 只有 `items.length > 0` 才消费 Enter/Tab，异步 loading 且空列表时会让 `ChatComposer` 把半成品 `/`、`@path`、`#agent` 直接发送。
- 本轮诊断（超越 cc-gui）：当前项目支持图片附件和 `chat_send` 前 optimistic 清空附件，但 store 原先吞掉失败且不返回结果，composer 无法知道需要恢复。对用户来说，发送失败后重新上传图片是高摩擦；失败发生在后端 request id 产生前时，应该保留可重试输入上下文。
- 本轮规划：只处理两个输入区交互点，优先级高（高频键盘路径 + 发送失败恢复）、成本低（纯前端 store/component/helper + tests）、风险低（不改后端命令协议）。暂不扩大到真实桌面点测或更多 slash command 数据源。
- 本轮完成：`src/components/chat/composer/useCompletions.ts` 新增 `shouldConsumeCompletionKey()`，补全菜单打开时 Enter/Tab 一律消费，避免 loading 空项误发送；`src/stores/useChatStore.ts` 将 `send()` 返回值改为 `Promise<boolean>`，成功拿到 requestId 返回 true，空输入或 `chat_send` 失败返回 false，同时保留原有 transcript/error/通知处理；`src/components/chat/composer/ChatComposer.tsx` 新增 `restoreFailedSendAttachments()`，发送失败时把刚清空的附件合并回当前附件列表，避免重复，并在当前 store draft 仍为空时恢复原 prompt；`src/components/chat/composer/useCompletions.test.ts`、`src/components/chat/composer/ChatComposer.test.ts`、`src/stores/useChatStore.test.ts` 补齐回归测试；`.trellis/spec/frontend/state-management.md` 与 `.trellis/spec/frontend/component-guidelines.md` 同步记录新契约。
- 本轮验证：先新增测试并确认 RED：`npm test -- src/components/chat/composer/useCompletions.test.ts src/components/chat/composer/ChatComposer.test.ts src/stores/useChatStore.test.ts` 失败于缺少 `shouldConsumeCompletionKey`、缺少 `restoreFailedSendAttachments`、`send()` 失败返回 `undefined`；实现后同命令通过（3 files / 34 tests）；`npm test` 通过（23 files / 193 tests）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`git diff --check` 无 whitespace error（仅 LF/CRLF 提示）。本轮未运行 Rust 全量测试，因为只改前端逻辑与文档，Rust 侧未新增改动。
- 下一步候选：1) 真实 Tauri 桌面点测：补全 loading 时按 Enter/Tab 是否不发送半成品、`chat_send` 失败时附件和 prompt 是否恢复；2) 系统通知后端 payload guard 之前已记录方案但未实施，当前用户已授权无需确认，可在下一轮补 Rust helper + 单测；3) 继续真实点测会话切换、diff 恢复按钮、Open File、历史图片 lightbox 与 Codex 长历史锚点。

## 迭代记录 2026-06-19 notification backend payload guard

- 上轮实际进展：composer 补全菜单 Enter/Tab 拦截与发送失败附件恢复已经在代码中落地并验证；通知后端 guard 此前只记录过方案，用户后续已明确授权无需确认。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：系统通知链路已接通，`src/utils/desktopNotification.ts` 会优先 invoke `chat_show_system_notification`，`src-tauri/src/lib.rs` 已初始化 `tauri_plugin_notification` 并注册命令，`src-tauri/capabilities/default.json` 包含 `notification:default`。缺口不是插件/权限，而是 `src-tauri/src/commands/chat_commands.rs` 里 payload trim/空标题校验写在命令体内，缺少可单测的命令边界契约。
- 本轮诊断（超越 cc-gui）：系统通知是长任务完成、失败、中断后的跨窗口反馈。把 title/body 归一化抽成纯 helper 后，可以不触发真实 OS 通知也能锁住“trim、空标题拒绝、空 body 允许”的边界，避免后续改动让 Windows 通知静默失效。
- 本轮规划：本轮只处理通知命令 payload guard，价值中高（停止通知是后台长任务高频反馈）、频率中高（success/error/abort 都走该链路）、成本低（Rust helper + 单测）、风险低（不改前端文案和 Tauri 插件配置）。
- 本轮完成：`src-tauri/src/commands/chat_commands.rs` 新增私有 `SystemNotificationPayload` 与 `normalize_system_notification_payload()`，`chat_show_system_notification` 复用 helper 后再调用 `tauri-plugin-notification`；新增 3 个 Rust 单测覆盖 title/body trim、空 title 报错、空 body 允许；`.trellis/spec/backend/cross-layer-protocol.md` 将 Chat Turn-Stopped System Notification 升级为 7 段式可执行契约。
- 本轮验证：先新增 Rust 单测并确认 RED，`cargo test --manifest-path src-tauri/Cargo.toml commands::chat_commands -- --nocapture` 失败于 helper 未定义；实现后同命令通过（3 tests）。后续验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`；`npm test -- src/stores/useChatStore.test.ts src/utils/desktopNotification.test.ts`（2 files / 32 tests）；`npm test`（23 files / 193 tests）；`npm run build`；`cargo test --manifest-path src-tauri/Cargo.toml`（97 tests，2 doctests ignored）；`cargo fmt --manifest-path src-tauri/Cargo.toml`；`git diff --check` 无 whitespace error，仅 LF/CRLF 提示。未做真实 Windows 右下角通知点测。
- 下一步候选：1) 真实 Tauri 桌面点测系统通知：完成、失败、abort 是否各弹一次，native 失败时 WebView fallback 是否可接受；2) 继续真实点测会话侧栏项目切换、diff 恢复按钮、Open File、历史图片 lightbox、Codex 长历史锚点；3) 下一轮继续排查聊天交互 bug，优先看历史加载/切会话与 active request 的真实桌面竞态，以及附件恢复在真实 `chat_send` 失败时的表现。

## 迭代记录 2026-06-19 pending send invalidation guard

- 上轮实际进展：系统通知后端 payload guard 已落地，Rust/前端全量验证通过；阻塞仍是真实 Tauri 桌面点测未完成。本轮按记录继续排查聊天交互竞态，并复查 `useChatStore.ts` 与现有 store 测试。
- 本轮诊断（补齐 cc-gui）：`useChatStore` 已按 requestId 过滤 stream/message/done，但仍存在 requestId 生成前的空窗：`send()` 先创建 streaming assistant，再等待 `chat_send` 返回 requestId；如果用户在这段时间点击清空/新建/加载历史，原先 `abortActiveRequestIfNeeded()` 只看 `activeRequestId`，不会 abort 这个 pending turn。旧 `chat_send` 返回后会把旧 requestId 写回当前 store，并可能绑定到下一轮 pending assistant。
- 本轮诊断（超越 cc-gui）：用户主动清空或切会话意味着旧 turn 不应恢复输入、不应污染新 turn，也不应因为旧 requestId 迟到锁住 provider/model 选择。需要一个 UI turn token 覆盖“requestId 未知但已有 streaming assistant”的阶段，而不是只依赖后端 requestId。
- 本轮规划：只修 store 层 pending send invalidation。优先级高（清空/切会话/重新发送是高频恢复路径，污染当前会话属于严重交互正确性问题）、成本低（单个 Zustand store + 单测 + spec）、风险低（不改 Tauri 命令协议和组件结构）。
- 本轮完成：`src/stores/useChatStore.ts` 新增 `latestChatTurnToken`，`send()` 创建 turn 时递增 token；`clear()` / `startNewSession()` / `loadSession()` 通过 `abortActiveRequestIfNeeded()` 在存在 streaming assistant 但尚无 requestId 时也调用 `chat_abort` 并废弃 pending turn；`abort()` 同样废弃 pending turn；旧 `chat_send` 成功或失败在 token 失效后不再改写 active state / error / notification，成功返回的旧 requestId 会 retire，避免迟到 stream/message/done 绑定到下一轮 pending turn。`src/stores/useChatStore.test.ts` 新增回归测试覆盖清空后第一轮 requestId 迟到、第二轮仍 pending 时旧 stream 被忽略且新 stream 正常接管；`.trellis/spec/frontend/state-management.md` 更新 Chat Send Outcome 与 Request Ownership 契约。
- 本轮验证：先新增测试并确认 RED：`npm test -- src/stores/useChatStore.test.ts` 失败于 `clear()` 没有调用 `chat_abort`；实现后同命令通过（25 tests）。后续验证通过：`npm test -- src/stores/useChatStore.test.ts src/components/chat/composer/ChatComposer.test.ts src/utils/desktopNotification.test.ts`（3 files / 35 tests）；`npm test`（23 files / 194 tests）；`npm run build`；`git diff --check` 无 whitespace error，仅 LF/CRLF 提示。本轮仅改前端 store/test/spec/TODO，未运行 Rust check；未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测 pending send：发送后立刻清空/切会话/再发，确认旧输出不回流、新 turn 正常；2) 继续点测系统通知、diff 恢复按钮、Open File、历史图片 lightbox、Codex 长历史锚点；3) 下一轮继续排查聊天交互 bug，重点看 `loadSession()` 缓存命中、pendingSessionKey 和用户快速连点历史会话是否还有可感知错位。

## 迭代记录 2026-06-19 pending history load invalidation

- 上轮实际进展：pending send invalidation guard 已在 `useChatStore` 中落地并通过定向、全量前端测试与构建；本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 代码和 `.codegraph/` 均可读。阻塞仍是真实 Tauri 桌面点测未完成。
- 本轮诊断（补齐 cc-gui）：cc-gui 的会话加载请求携带 `sessionId + projectPath`，用户切历史会话时有明确的请求归属。当前项目 `loadSession()` 已用 `latestSessionLoadToken` 防止旧历史加载覆盖新历史选择，但 `send()` 没有废弃正在等待的历史加载；用户点击历史会话后若马上发送新消息，迟到的 `get_unified_session_messages` 结果仍可能把刚发送的 transcript 覆盖成旧历史。
- 本轮诊断（超越 cc-gui）：用户主动发送新 prompt 是比 pending history load 更强的意图，输入区应立即重新获得 transcript ownership。此时需要清掉 `pendingSessionKey`，否则侧边栏仍显示旧会话 loading，且迟到历史回写会造成“我刚发的消息消失”的高损伤体验。
- 本轮规划：只修 `useChatStore.send()` 与 store 回归测试。优先级高：用户价值高（避免消息丢失/错会话覆盖）、出现频率中高（历史加载慢时常见）、实现成本低（已有 session load token 机制，只需让 send 参与失效）、风险低（不改 Tauri 命令协议和组件结构）。
- 本轮完成：`src/stores/useChatStore.test.ts` 新增回归测试 `ignores a pending history load after the user sends a new message`；`src/stores/useChatStore.ts` 在真实发送开始时递增 `latestSessionLoadToken`，并在 optimistic user/assistant 状态更新中清空 `pendingSessionKey`，确保迟到历史加载被 token 拦截且不会覆盖新发送；`.trellis/spec/frontend/state-management.md` 更新 Chat Send Outcome 与 Request Ownership 契约，明确发送新 prompt 必须废弃 pending history load。
- 本轮验证：先新增测试并确认 RED：`npm test -- src/stores/useChatStore.test.ts` 失败于 `pendingSessionKey` 仍为旧历史会话 key；实现后同命令通过（26 tests）。后续验证通过：`npm test -- src/stores/useChatStore.test.ts src/components/chat/chatSessionSidebarUtils.test.ts src/components/chat/composer/ChatComposer.test.ts`（3 files / 44 tests）；`npm test`（23 files / 195 tests）；`npm run build`；`cargo check --manifest-path src-tauri/Cargo.toml`；`git diff --check` 无 whitespace error，仅 LF/CRLF 提示。未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测：点击历史会话后立刻发送，确认 pending 状态消失、旧历史不覆盖新消息；2) 继续真实点测 pending send 清空/切会话/再发、系统通知、diff 恢复按钮、Open File、历史图片 lightbox 与 Codex 长历史锚点；3) 继续排查聊天交互 bug，下一轮优先看 `answerToolPermission()` 是否需要与 AskUserQuestion 一样做并发幂等/失败恢复，避免权限按钮双击或失败重试造成状态错位。

## 迭代记录 2026-06-19 tool permission response idempotence

- 上轮实际进展：pending history load invalidation 已在 `useChatStore.send()` 中落地并通过定向、全量前端测试、构建、`cargo check` 与 `git diff --check`；本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 代码和 `.codegraph/` 均可读。阻塞仍是真实 Tauri 桌面点测未完成。
- 本轮诊断（补齐 cc-gui）：对照 `jetbrains-cc-gui/webview/src/components/PermissionDialog.tsx`，普通工具权限弹窗使用 `markSubmitted()` 阻止重复提交；当前项目 `ToolPermissionDialog` 有遮罩拒绝、右上关闭、底部拒绝和允许按钮等多个入口，但 `answerToolPermission()` 原先在 `permission_respond_tool` 成功后才清 `pendingToolPermission`，双击/快速点击会重复 invoke 并可能写两次权限响应。
- 本轮诊断（超越 cc-gui）：普通工具权限和 AskUserQuestion 一样都是后端等待响应文件的阻塞点。store 层应作为最终幂等边界：先按 requestId 校验当前 pending，再乐观清空；失败时恢复同一个 pending 请求，且不能覆盖后续新请求。这能避免双击制造日志噪声，也能在写响应失败时保留用户可重试入口。
- 本轮规划：只修 `answerToolPermission()` 和 store 回归测试。优先级高：用户价值高（权限弹窗阻塞工具执行，重复/失败会卡住聊天）、出现频率高（Bash/Edit/Write/MCP 都可能触发）、实现成本低（复用 AskUserQuestion 同模式）、风险低（不改 Tauri 命令协议和 UI 结构）。
- 本轮完成：`src/stores/useChatStore.test.ts` 新增 3 条回归测试，覆盖顺序重复工具权限响应、并发重复响应、invoke 失败恢复原 pending；`src/stores/useChatStore.ts` 将 `answerToolPermission()` 改为先校验 `pendingToolPermission.requestId`、立即清 pending、失败时只在没有新请求时恢复原 pending；`.trellis/spec/frontend/state-management.md` 更新 Permission Event 和 Chat Request Ownership 契约，明确 ToolPermission 与 AskUserQuestion 一样必须 store 层幂等。
- 本轮验证：先新增测试并确认 RED：`npm test -- src/stores/useChatStore.test.ts` 失败于顺序重复和并发重复都调用了两次 `permission_respond_tool`；实现后同命令通过（29 tests）。后续验证通过：`npm test -- src/stores/useChatStore.test.ts src/utils/desktopNotification.test.ts`（2 files / 37 tests）；`npm test`（23 files / 198 tests）；`npm run build`；`cargo check --manifest-path src-tauri/Cargo.toml`。未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测普通工具权限：双击允许/拒绝、点击遮罩后快速点按钮，确认只响应一次，失败时可重试；2) 继续排查 `approvePlan()` 是否也需要同样幂等/失败恢复，避免 plan approval 多入口重复响应；3) 继续真实点测 pending send、历史加载后立刻发送、系统通知、diff 恢复按钮、Open File、历史图片 lightbox 与 Codex 长历史锚点。

## 迭代记录 2026-06-19 plan approval response idempotence

- 上轮实际进展：普通工具权限 `answerToolPermission()` 已经按 requestId 做 store 层幂等，顺序重复、并发重复和失败恢复均有回归测试；pending send / pending history load / AskUserQuestion 等交互竞态也已在记录和代码中落地。阻塞仍是真实 Tauri 桌面点测未完成。
- 本轮诊断（补齐 cc-gui）：对照 `jetbrains-cc-gui/webview/src/components/PlanApprovalDialog.tsx`，cc-gui 的计划审批弹窗通过 `markSubmitted()` 在组件层阻止重复提交；当前项目 `src/components/chat/PlanApprovalDialog.tsx` 有遮罩取消、右上关闭、Deny、Approve、Approve & Auto 多个入口，但 `src/stores/useChatStore.ts` 的 `approvePlan()` 原先成功后才清 `pendingPlanApproval`，重复点击或并发点击会两次调用 `permission_respond_plan_approval`。
- 本轮诊断（超越 cc-gui）：计划审批和工具权限一样都是 daemon 等待响应文件的阻塞点。仅靠组件层禁用按钮不够，store 层必须成为最终边界：先校验 pending requestId，再乐观清空，失败时恢复同一个 pending 且不覆盖新请求。这样 overlay、关闭、按钮和未来快捷键都能复用同一幂等语义。
- 本轮规划：只修 `approvePlan()` 和 store 回归测试。优先级高：用户价值高（计划审批阻塞后续执行，重复响应会写两次响应文件）、出现频率中高（计划模式/审批流常见）、实现成本低（复用 ToolPermission 模式）、风险低（不改 Tauri 命令协议和 UI 结构）。
- 本轮完成：`src/stores/useChatStore.test.ts` 新增 3 条回归测试，覆盖顺序重复计划审批响应、并发重复响应、invoke 失败恢复原 pending；`src/stores/useChatStore.ts` 将 `approvePlan()` 改为先校验 `pendingPlanApproval.requestId`、立即清 pending、失败时只在没有新请求时恢复原 pending；`.trellis/spec/frontend/state-management.md` 更新 Permission Event 和 Chat Request Ownership 契约，明确 PlanApproval 与 AskUserQuestion / ToolPermission 一样必须 store 层幂等。
- 本轮验证：先新增测试并确认 RED：`npm test -- src/stores/useChatStore.test.ts` 失败于顺序重复和并发重复都调用了两次 `permission_respond_plan_approval`；实现后同命令通过（32 tests）。后续验证通过：`npm test -- src/stores/useChatStore.test.ts src/utils/desktopNotification.test.ts`（2 files / 40 tests）；`npm test`（23 files / 201 tests）；`npm run build`；`cargo check --manifest-path src-tauri/Cargo.toml`；`cargo test --manifest-path src-tauri/Cargo.toml`（97 tests，2 doctests ignored）；`git diff --check` 无 whitespace error，仅既有 LF/CRLF 提示。未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测普通工具权限和计划审批：双击允许/拒绝/Approve & Auto、遮罩后快速点按钮，确认只响应一次，失败时可重试；2) 真实点测 pending send 清空/切会话/再发、历史加载后立刻发送，确认旧输出和旧历史不回流；3) 继续排查聊天交互 bug，优先看 PlanApprovalDialog 是否需要组件层提交中状态/键盘快捷键、弹窗超时/关闭路径和真实 Windows 通知；4) 继续点测 diff 恢复按钮、Open File、历史图片 lightbox 与 Codex 长历史锚点。

## 迭代记录 2026-06-19 plan approval keyboard shortcuts

- 上轮实际进展：`approvePlan()` 已具备 store 层 requestId 幂等、提交前清 pending 和失败恢复；普通工具权限、AskUserQuestion、pending send、pending history load 等关键聊天竞态也已有回归测试。阻塞仍是真实 Tauri 桌面点测未完成。
- 本轮诊断（补齐 cc-gui）：对照 `jetbrains-cc-gui/webview/src/components/PlanApprovalDialog.tsx`，cc-gui 计划审批弹窗支持 `Enter` 批准、`Escape` 拒绝；当前项目 `src/components/chat/PlanApprovalDialog.tsx` 只能鼠标点遮罩/关闭/Deny/Approve/Approve & Auto，键盘审批流缺失。高频审批时用户需要离开键盘去点按钮，效率低，也容易在长计划弹窗中误操作。
- 本轮诊断（超越 cc-gui）：快捷键不应只做全局抢占。当前弹窗内有折叠按钮和多个操作按钮，`Enter` 如果焦点在按钮或可编辑控件上，应保留原生行为，避免用户想展开/按钮确认时被全局批准计划。`Escape` 可在按钮焦点下快速拒绝，但不抢输入/可编辑控件。
- 本轮规划：只改 `PlanApprovalDialog` 交互层和纯 helper 测试。优先级中高：用户价值高（审批流高频且键盘用户明显受益）、出现频率中（计划模式触发）、实现成本低（一个组件 + 一个测试文件）、风险低（store 幂等已经兜底，不改命令协议）。
- 本轮完成：`src/components/chat/PlanApprovalDialog.tsx` 新增 `resolvePlanApprovalShortcutAction()` 纯 helper，并通过 `useEffect` 监听 `window.keydown`；`Enter` 在非控件焦点下触发默认批准，`Escape` 在非编辑目标下触发拒绝；`src/components/chat/PlanApprovalDialog.test.ts` 新增 3 条测试覆盖快捷键映射、Enter 不抢按钮/textarea/contenteditable、Escape 在按钮焦点可用但不抢 input；`.trellis/spec/frontend/component-guidelines.md` 记录计划审批快捷键契约。新增测试文件已执行 `git add -- src/components/chat/PlanApprovalDialog.test.ts`。
- 本轮验证：先新增测试并确认 RED：`npm test -- src/components/chat/PlanApprovalDialog.test.ts` 失败于 `resolvePlanApprovalShortcutAction is not a function`；实现后同命令通过（3 tests）。后续验证通过：`npm test -- src/components/chat/PlanApprovalDialog.test.ts src/stores/useChatStore.test.ts`（2 files / 35 tests）；`npm test`（24 files / 204 tests）；`npm run build`；`cargo check --manifest-path src-tauri/Cargo.toml`；`cargo test --manifest-path src-tauri/Cargo.toml`（97 tests，2 doctests ignored）。未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测计划审批：Enter/Escape、按钮焦点 Enter、Approve & Auto、遮罩/关闭重复操作是否符合预期；2) 给普通工具权限弹窗补同类键盘路径（Enter 允许、Escape 拒绝）并确保不抢控件行为；3) 继续真实点测 pending send 清空/切会话/再发、历史加载后立刻发送、系统通知、diff 恢复按钮、Open File、历史图片 lightbox 与 Codex 长历史锚点。

## 迭代记录 2026-06-19 tool permission keyboard shortcuts

- 上轮实际进展：计划审批弹窗已支持 `Enter` 默认批准、`Escape` 拒绝，并且快捷键决策有纯 helper 回归测试；store 层 `answerToolPermission()` 和 `approvePlan()` 均已具备 requestId 幂等和失败恢复。阻塞仍是真实 Tauri 桌面点测未完成。
- 本轮诊断（补齐 cc-gui）：对照 `jetbrains-cc-gui/webview/src/components/PermissionDialog.tsx`，cc-gui 普通工具权限弹窗支持键盘快速选择；当前项目 `src/components/chat/ToolPermissionDialog.tsx` 只能鼠标点击遮罩、关闭、拒绝或允许一次。普通工具权限覆盖 Bash/Edit/Write/MCP 等高频工具，缺少键盘路径会让用户在连续审批中频繁离开键盘。
- 本轮诊断（超越 cc-gui）：本项目当前只有“允许一次/拒绝”两个动作，没有 cc-gui 的 approve-always 模式。本轮只补 `Enter` 允许一次、`Escape` 拒绝，避免引入本项目没有的权限模式；同时不抢按钮/输入/可编辑控件的 `Enter`，减少误允许风险。最终重复响应边界仍由 store 的 `answerToolPermission()` 负责。
- 本轮规划：只改 `ToolPermissionDialog` 交互层和纯 helper 测试。优先级中高：用户价值高（工具权限是高频阻塞点）、出现频率高（大量工具调用会触发）、实现成本低（一个组件 + 一个测试文件）、风险低（不改后端命令协议和权限模型）。
- 本轮完成：`src/components/chat/ToolPermissionDialog.tsx` 新增 `resolveToolPermissionShortcutAction()` 纯 helper，并通过 `useEffect` 监听 `window.keydown`；`Enter` 在非控件焦点下触发允许一次，`Escape` 在非编辑目标下触发拒绝；`src/components/chat/ToolPermissionDialog.test.ts` 新增 3 条测试覆盖快捷键映射、Enter 不抢按钮/textarea/contenteditable、Escape 在按钮焦点可用但不抢 input；`.trellis/spec/frontend/component-guidelines.md` 记录普通工具权限快捷键契约。新增测试文件已执行 `git add -- src/components/chat/ToolPermissionDialog.test.ts`。
- 本轮验证：先新增测试并确认 RED：`npm test -- src/components/chat/ToolPermissionDialog.test.ts` 失败于 `resolveToolPermissionShortcutAction is not a function`；实现后同命令通过（3 tests）。后续验证通过：`npm test -- src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/stores/useChatStore.test.ts`（3 files / 38 tests）；`npm test`（25 files / 207 tests）；`npm run build`；`cargo check --manifest-path src-tauri/Cargo.toml`；`cargo test --manifest-path src-tauri/Cargo.toml`（97 tests，2 doctests ignored）。未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测普通工具权限：Enter/Escape、按钮焦点 Enter、遮罩/关闭重复操作是否符合预期；2) 继续真实点测计划审批快捷键、pending send 清空/切会话/再发、历史加载后立刻发送；3) 继续排查聊天交互 bug，优先看 AskUserQuestionDialog 是否也需要明确键盘路径和提交中视觉状态；4) 继续点测系统通知、diff 恢复按钮、Open File、历史图片 lightbox 与 Codex 长历史锚点。

## 本轮 PLAN 2026-06-19 ask-user-question dialog interaction guard

- 目标：排查并修复 `AskUserQuestionDialog` 相比 cc-gui 和本项目 plan/tool permission 弹窗仍缺失的聊天交互边界。
- 功能点 1：确认根因与对标行为。验证方式：读取本项目 `AskUserQuestionDialog` / `useChatStore` 与 cc-gui `AskUserQuestionDialog`，明确是否应支持全局 `Enter`。
- 功能点 2：补回归测试。验证方式：新增 `src/components/chat/AskUserQuestionDialog.test.ts`，先运行定向测试确认 RED。
- 功能点 3：实现最小交互修复。验证方式：定向测试通过，并复跑 plan/tool permission 相关测试，确认共享快捷键判断不回归。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 与本文件最终记录。
- 功能点 5：质量验证。验证方式：按 `package.json` 确认脚本后运行定向测试、`npm test`、`npm run build`、Rust check/test（因工作区含 Rust 改动）和 `git diff --check`。

## 迭代记录 2026-06-19 ask-user-question dialog interaction guard

- 上轮实际进展：普通工具权限弹窗已支持 `Enter` 允许一次、`Escape` 拒绝，并通过相邻测试、全量前端测试、构建和 Rust 验证；PlanApproval / ToolPermission 的 store 层响应也已有 requestId 幂等。阻塞仍是真实 Tauri 桌面点测未完成。
- 本轮诊断（补齐 cc-gui）：对照 `jetbrains-cc-gui/webview/src/components/AskUserQuestionDialog.tsx`，cc-gui 的 AskUserQuestion 支持 `Escape` 取消，并通过 `markSubmitted()` 防重复提交；当前项目 `src/components/chat/AskUserQuestionDialog.tsx` 只有鼠标 Cancel/Submit，缺少键盘取消和组件层提交中视觉状态。
- 本轮诊断（超越 cc-gui）：本项目同时已有 PlanApproval / ToolPermission 两套相似快捷键 target 判断，再给 AskUserQuestion 复制第三份会造成规则漂移。本轮抽出 `src/utils/dialogShortcuts.ts`，让三个阻塞弹窗共享 editable target、Enter-owned control 和一次性提交 guard；AskUserQuestion 不做全局 `Enter` 提交，避免抢占 radio/checkbox 与未来自定义输入的原生键盘语义。
- 本轮规划：优先修 AskUserQuestion 的 `Escape` 取消、提交中禁用和重复入口 guard。优先级中高：用户价值高（阻塞式提问弹窗会中断聊天流，重复提交/取消容易写两次响应）、出现频率中（AskUserQuestion 比工具权限少但一旦出现就是强阻塞）、实现成本低（一个组件 + 共享 helper + 纯测试）、风险低（不改 Tauri 命令协议和 store 响应语义）。
- 本轮完成：`src/components/chat/AskUserQuestionDialog.tsx` 新增 `resolveAskUserQuestionShortcutAction()`、`Escape` 快捷取消、`submitted` 视觉状态、禁用答案控件和 `markDialogSubmitted()` 一次性响应 guard；`src/utils/dialogShortcuts.ts` 新增共享 shortcut target 与提交 guard；`src/components/chat/PlanApprovalDialog.tsx` / `src/components/chat/ToolPermissionDialog.tsx` 改为复用共享 target helper；`src/components/chat/AskUserQuestionDialog.test.ts` 和 `src/utils/dialogShortcuts.test.ts` 覆盖新契约；`.trellis/spec/frontend/component-guidelines.md` 记录 AskUserQuestion 与共享 helper 规则。新增文件已执行 `git add`。
- 本轮验证：先新增测试并确认 RED：`npm test -- src/components/chat/AskUserQuestionDialog.test.ts src/utils/dialogShortcuts.test.ts` 失败于缺少 `resolveAskUserQuestionShortcutAction` 和 `dialogShortcuts`；实现后同命令通过（2 files / 5 tests）。后续验证通过：`npm test -- src/components/chat/AskUserQuestionDialog.test.ts src/utils/dialogShortcuts.test.ts src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/stores/useChatStore.test.ts`（5 files / 43 tests）；`npm test`（27 files / 212 tests）；`npm run build`；`cargo check --manifest-path src-tauri/Cargo.toml`；`cargo test --manifest-path src-tauri/Cargo.toml`（97 tests，2 doctests ignored）；`git diff --check` 无 whitespace error，仅工作区既有 LF/CRLF 提示。未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测 AskUserQuestion：`Escape`、Submit 后禁用/加载状态、失败恢复后能否重试；2) 真实点测普通工具权限和计划审批快捷键：按钮焦点 Enter、遮罩/关闭重复操作；3) 继续排查聊天交互 bug，优先看历史加载/切会话与 active request 的真实桌面竞态，以及附件恢复在真实 `chat_send` 失败时的表现；4) 继续点测系统通知、diff 恢复按钮、Open File、历史图片 lightbox 与 Codex 长历史锚点。

## 本轮 PLAN 2026-06-19 active permission dialog gate

- 目标：修复 `ChatPage` 同时挂载 AskUserQuestion / PlanApproval / ToolPermission 时，多个 portal 和全局快捷键监听器可能同时响应一次键盘操作的问题。
- 功能点 1：确认根因与对标行为。验证方式：CodeGraph 读取本项目 `ChatPage` / 三个 dialog / store 事件监听，并对照 cc-gui `AppDialogs`。
- 功能点 2：补回归测试。验证方式：在 `src/utils/chatUiBehavior.test.ts` 新增 active permission dialog 推导测试，先运行定向测试确认 RED。
- 功能点 3：实现页面层唯一 active dialog。验证方式：`ChatPage` 只根据 helper 渲染一个阻塞权限弹窗，定向测试和相邻弹窗测试通过。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 与本文件最终记录。
- 功能点 5：质量验证。验证方式：运行定向测试、`npm test`、`npm run build`、Rust check/test（因工作区已有 Rust 改动）和 `git diff --check`。

## 迭代记录 2026-06-19 active permission dialog gate

- 上轮实际进展：AskUserQuestion 已支持 `Escape` 快捷取消、提交中禁用和共享 shortcut helper；`npm test`、`npm run build`、`cargo check`、`cargo test`、`git diff --check` 均已通过。阻塞仍是真实 Tauri 桌面点测未完成。
- 本轮诊断（补齐 cc-gui）：本项目 `src/pages/ChatPage.tsx` 会同时条件渲染 `AskUserQuestionDialog`、`PlanApprovalDialog`、`ToolPermissionDialog` 三个 portal；三者都拥有 `window.keydown` 监听。若 daemon 事件短时间叠加，用户一次 `Escape` / `Enter` 可能被多个监听器处理，造成多个阻塞请求同时响应。cc-gui 通过统一 `AppDialogs` 管理顶层弹窗状态，本项目需要页面层唯一 active dialog gate。
- 本轮诊断（超越 cc-gui）：三个 pending request 都有 timestamp，页面层可以按“最新 pending 请求优先”挂载唯一弹窗；时间戳缺失/相同则按当前视觉层级 `tool-permission > plan-approval > ask-user-question` 兜底。未激活的 pending request 保留在 store，当前弹窗响应后再显示下一个，避免同时丢失请求或同时响应。
- 本轮规划：只改 `ChatPage` 和 `chatUiBehavior` 纯 helper。优先级高：用户价值高（避免一次键盘操作误响应多个 daemon-blocking 请求）、出现频率低到中（正常情况下较少叠加，但一旦出现后果严重）、实现成本低（纯前端 helper + 页面渲染 gate + 单测）、风险低（不改 store/后端响应协议）。
- 本轮完成：`src/utils/chatUiBehavior.ts` 新增 `getActivePermissionDialog()` 和 `ActivePermissionDialog` 类型；`src/pages/ChatPage.tsx` 基于 helper 只挂载一个 active permission dialog；`src/utils/chatUiBehavior.test.ts` 新增多 pending 时选择唯一 active dialog 的回归测试；`.trellis/spec/frontend/component-guidelines.md` 记录页面层唯一权限弹窗契约。
- 本轮验证：先新增测试并确认 RED：`npm test -- src/utils/chatUiBehavior.test.ts` 失败于 `getActivePermissionDialog is not a function`；实现后同命令通过（1 file / 11 tests）。后续验证通过：`npm test -- src/components/chat/AskUserQuestionDialog.test.ts src/utils/dialogShortcuts.test.ts src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/utils/chatUiBehavior.test.ts`（5 files / 22 tests）；`npm test`（27 files / 213 tests）；`npm run build`；`cargo check --manifest-path src-tauri/Cargo.toml`；`cargo test --manifest-path src-tauri/Cargo.toml`（97 tests，2 doctests ignored）；`git diff --check` 无 whitespace error，仅工作区既有 LF/CRLF 提示。未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测三类权限弹窗叠加场景：构造/模拟多个 pending 时只显示最新一个，响应后显示下一个；2) 继续真实点测 AskUserQuestion 的失败恢复、普通工具权限和计划审批快捷键；3) 继续排查聊天交互 bug，优先看历史加载/切会话与 active request 的真实桌面竞态，以及真实 `chat_send` 失败时附件恢复；4) 继续点测系统通知、diff 恢复按钮、Open File、历史图片 lightbox 与 Codex 长历史锚点。

## 本轮 PLAN 2026-06-19 active permission dialog timestamp presence guard

- 目标：修复唯一 active 权限弹窗选择逻辑中“pending 请求存在但 timestamp 为空字符串时被隐藏”的边界缺陷。
- 功能点 1：确认根因与对标行为。验证方式：读取 `ChatPage` / `chatUiBehavior` 当前实现，并对照 cc-gui 顶层 dialog 入口，确认候选存在性不应依赖 timestamp 真值。
- 功能点 2：补回归测试。验证方式：在 `src/utils/chatUiBehavior.test.ts` 增加空 timestamp 仍显示 pending dialog 的用例，先运行定向测试确认 RED。
- 功能点 3：实现最小修复。验证方式：`getActivePermissionDialog()` 改为显式 `hasXxx` 判断候选存在，timestamp 只用于排序，`ChatPage` 传入 pending 对象存在性。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 与本文件最终记录。
- 功能点 5：质量验证。验证方式：运行定向测试、相邻弹窗测试、`npm test`、`npm run build`、Rust check/test（工作区已有 Rust 改动）和 `git diff --check`。

## 迭代记录 2026-06-19 active permission dialog timestamp presence guard

- 上轮实际进展：`ChatPage` 已经只挂载一个 active permission dialog，PlanApproval / ToolPermission / AskUserQuestion 的快捷键与 store 幂等边界均已落地；阻塞仍是真实 Tauri 桌面点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：cc-gui 通过统一 `AppDialogs` 管理顶层权限类弹窗；本项目上一轮已经补了唯一 active dialog gate，但 `getActivePermissionDialog()` 用 timestamp 真值判断候选是否存在。如果 daemon/migration/malformed payload 带来了空 timestamp，真实 pending 请求会被页面层隐藏，用户看不到必须响应的阻塞弹窗。
- 本轮诊断（超越 cc-gui）：权限弹窗的“是否存在”和“谁最新”是两个不同契约。存在性应来自 pending request 对象，timestamp 只用于排序；空/缺失/非法 timestamp 只应降低排序优先级，再用 `tool-permission > plan-approval > ask-user-question` 兜底，避免后台请求被静默卡住。
- 本轮规划：只修 active permission helper 与 `ChatPage` 调用点。优先级中高：用户价值高（避免阻塞权限请求不可见）、出现频率低到中（异常 payload/兼容迁移场景，但后果严重）、实现成本低（纯前端 helper + 单测 + spec）、风险低（不改 store/后端协议）。
- 本轮完成：`src/utils/chatUiBehavior.ts` 为 `getActivePermissionDialog()` 增加 `hasAskUserQuestion` / `hasPlanApproval` / `hasToolPermission` 显式存在性输入，并保留旧 timestamp-only 调用的兼容行为；`src/pages/ChatPage.tsx` 改为按 pending 对象存在性传参；`src/utils/chatUiBehavior.test.ts` 增加空 timestamp 仍显示 pending dialog 的回归测试；`.trellis/spec/frontend/component-guidelines.md` 记录候选存在性不能依赖 timestamp 真值。
- 本轮验证：先新增测试并确认 RED：`npm test -- src/utils/chatUiBehavior.test.ts` 失败于期望 `ask-user-question` 但收到 `null`；实现后同命令通过（12 tests）。后续验证通过：`npm test -- src/components/chat/AskUserQuestionDialog.test.ts src/utils/dialogShortcuts.test.ts src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/utils/chatUiBehavior.test.ts`（5 files / 23 tests）；`npm test`（27 files / 214 tests）；`npm run build`；`cargo check --manifest-path src-tauri/Cargo.toml`；`cargo test --manifest-path src-tauri/Cargo.toml`（97 tests，2 doctests ignored）；`git diff --check` 无 whitespace error，仅工作区既有 LF/CRLF 提示。未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测三类权限弹窗叠加与空/非法 timestamp 模拟，确认只显示一个且响应后继续显示队列下一项；2) 继续排查历史加载/切会话与 active request 的真实竞态，重点看 `loadSession()` 后立刻发送和 pending request 回流；3) 真实点测 AskUserQuestion 失败恢复、ToolPermission/PlanApproval 快捷键、真实 `chat_send` 失败附件恢复；4) 继续点测系统通知、diff 恢复按钮、Open File、历史图片 lightbox 与 Codex 长历史锚点。

## 本轮 PLAN 2026-06-19 session sidebar cached switch ownership guard

- 目标：修复 `ChatSessionSidebar` 在“项目 A 请求未返回 -> 切到有缓存的项目 B -> A 迟到返回”时，旧项目响应仍可能覆盖当前项目会话列表归属的问题。
- 功能点 1：确认根因与对标行为。验证方式：CodeGraph 读取本项目 `ChatSessionSidebar` / `chatSessionSidebarUtils`，并对照 cc-gui 历史会话入口确认 projectPath 是会话历史边界。
- 功能点 2：补回归测试。验证方式：在 `src/components/chat/chatSessionSidebarUtils.test.ts` 增加会话列表响应归属判断用例，先运行定向测试确认 RED。
- 功能点 3：实现最小修复。验证方式：`loadSessions()` 缓存命中也必须使旧 in-flight 请求失效；异步响应同时校验 request seq 和当前选中项目 key。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 与本文件最终记录。
- 功能点 5：质量验证。验证方式：运行定向测试、相邻聊天状态测试、`npm test`、`npm run build`、Rust check/test（工作区已有 Rust 改动）和 `git diff --check`。

## 迭代记录 2026-06-19 session sidebar cached switch ownership guard

- 上轮实际进展：权限类弹窗唯一 active gate 与空 timestamp 存在性守卫已在代码中落地，PlanApproval / ToolPermission / AskUserQuestion 的快捷键与 store 幂等边界均已验证；阻塞仍是真实 Tauri 桌面点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的历史会话入口以 projectPath 作为会话历史边界；本项目 `ChatSessionSidebar` 也已有项目缓存和 `sessionsProjectPath`，但 `loadSessions()` 在缓存命中时直接返回，没有递增 `sessionRequestSeqRef`。如果项目 A 的扫描仍在 flight，用户切到已有缓存的项目 B，A 的迟到响应仍满足旧序号并覆盖 `sessionsProjectPath`，导致 B 的列表被 `getVisibleProjectSessions()` 隐藏成空。
- 本轮诊断（超越 cc-gui）：会话列表不只要隐藏旧项目 rows，还要把“当前响应是否仍属于最新选中项目”做成可测试契约。异步响应必须同时满足 request seq 最新和 normalized selected project key 匹配；缓存命中也代表新的列表归属决策，必须退休旧请求。
- 本轮规划：只修会话侧栏项目切换竞态。优先级高：用户价值高（会话入口一旦显示空列表或旧列表会直接阻断历史恢复）、出现频率中高（项目切换和缓存命中是高频操作）、实现成本低（纯前端 helper + 组件 ref + 单测）、风险低（不改 Tauri 命令协议和后端扫描逻辑）。
- 本轮完成：`src/components/chat/chatSessionSidebarUtils.ts` 新增 `shouldAcceptSessionListResponse()`，统一判断 session list 响应归属；`src/components/chat/ChatSessionSidebar.tsx` 新增 `selectedProjectKeyRef`，缓存命中时递增 `sessionRequestSeqRef`、清空逻辑 in-flight 状态并关闭加载态，异步成功/失败分支同时校验 request seq 和当前选中项目 key；`src/components/chat/chatSessionSidebarUtils.test.ts` 新增响应归属回归测试；`.trellis/spec/frontend/component-guidelines.md` 记录会话侧栏缓存命中与 async response ownership 规则。
- 本轮验证：先新增测试并确认 RED：`npm test -- src/components/chat/chatSessionSidebarUtils.test.ts` 失败于 `shouldAcceptSessionListResponse is not a function`；实现后同命令通过（17 tests）。后续验证通过：`npm test -- src/components/chat/chatSessionSidebarUtils.test.ts src/stores/useChatStore.test.ts src/utils/chatNavigation.test.ts`（3 files / 61 tests）；`npm test`（27 files / 215 tests）；`cargo check --manifest-path src-tauri/Cargo.toml`；`npm run build`；`cargo test --manifest-path src-tauri/Cargo.toml`（97 tests，2 doctests ignored）；`git diff --check` 无 whitespace error，仅 LF/CRLF 转换提示。未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测会话侧栏：项目 A 扫描中切到已有缓存项目 B，确认 B 的列表不被迟到 A 响应清空；2) 继续排查聊天交互 bug，优先看 `loadSession()` 后立刻发送、pending request 回流和真实 `chat_send` 失败附件恢复；3) 真实点测三类权限弹窗队列、系统通知、diff 恢复按钮、Open File、历史图片 lightbox 与 Codex 长历史锚点。

## 本轮 PLAN 2026-06-19 active session reselection while history pending

- 目标：修复历史会话 B 正在加载时，用户点击当前 active 会话 A 想留在 A，却被 `ChatPage` 粗粒度 active-session 判断吞掉点击，导致 pending B 继续完成并切走的问题。
- 功能点 1：确认根因与对标行为。验证方式：CodeGraph 读取 `ChatPage.handleSessionSelect`、`ChatSessionSidebar.shouldIgnoreSessionClick`、`useChatStore.loadSession`，确认页面层和侧栏层判断不一致。
- 功能点 2：补回归测试。验证方式：在 `src/utils/chatUiBehavior.test.ts` 新增 session selection helper 用例，先运行定向测试确认 RED。
- 功能点 3：实现最小修复。验证方式：抽出页面与侧栏共用的 session selection ignore helper，`ChatPage` 和 `chatSessionSidebarUtils` 统一调用。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 与本文件最终记录。
- 功能点 5：质量验证。验证方式：运行定向测试、相邻 sidebar/store/navigation 测试、`npm test`、`npm run build`、Rust check/test（工作区已有 Rust 改动）和 `git diff --check`。

## 迭代记录 2026-06-19 active session reselection while history pending

- 上轮实际进展：会话侧栏缓存命中时的旧请求退休和 async response ownership guard 已落地，并通过定向、全量前端、构建、Rust check/test、`git diff --check` 验证；阻塞仍是真实 Tauri 桌面点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的会话切换由统一状态入口处理，当前项目则有两层判断：`ChatSessionSidebar.shouldIgnoreSessionClick()` 在有另一个 pending session 时允许点击 active session，但 `ChatPage.handleSessionSelect()` 仍粗暴拦截所有 active session 点击。结果是用户在历史会话 B 加载中点击当前会话 A 试图留在 A，侧栏放行，页面层却吞掉点击，pending B 最终仍可能完成并切走。
- 本轮诊断（超越 cc-gui）：历史列表的“重复点击忽略”必须区分两种意图：没有 pending 时点击 active session 是无效重复操作；有另一个 pending session 时点击 active session 是明确的取消/覆盖 pending 选择意图。这个判断必须在页面层和侧栏层共用，不能各写一份。
- 本轮规划：只修 session selection ignore 规则。优先级中高：用户价值高（避免用户无法取消一次误点的历史加载）、出现频率中（历史加载慢或快速点击时出现）、实现成本低（纯 helper + 两个调用点 + 单测）、风险低（不改 Tauri 命令协议和 store load token）。
- 本轮完成：`src/utils/chatUiBehavior.ts` 新增 `shouldIgnoreChatSessionSelection()`；`src/pages/ChatPage.tsx` 的 `handleSessionSelect()` 改为复用该 helper；`src/components/chat/chatSessionSidebarUtils.ts` 的 `shouldIgnoreSessionClick()` 也复用同一 helper；`src/utils/chatUiBehavior.test.ts` 增加 active session 在另一个 pending session 存在时不应被忽略的回归测试；`.trellis/spec/frontend/component-guidelines.md` 记录该交互契约。
- 本轮验证：先新增测试并确认 RED：`npm test -- src/utils/chatUiBehavior.test.ts` 失败于 `shouldIgnoreChatSessionSelection is not a function`；实现后 `npm test -- src/utils/chatUiBehavior.test.ts` 通过（13 tests），`npm test -- src/components/chat/chatSessionSidebarUtils.test.ts` 通过（17 tests）。后续验证通过：`npm test -- src/utils/chatUiBehavior.test.ts src/components/chat/chatSessionSidebarUtils.test.ts src/stores/useChatStore.test.ts src/utils/chatNavigation.test.ts`（4 files / 74 tests）；`npm test`（27 files / 216 tests）；`cargo check --manifest-path src-tauri/Cargo.toml`；`npm run build`；`cargo test --manifest-path src-tauri/Cargo.toml`（97 tests，2 doctests ignored）；`git diff --check` 无 whitespace error，仅 LF/CRLF 转换提示。未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测历史会话快速切换：B 加载中点击 active A，确认 pending B 被覆盖且不会最终切走；2) 继续排查聊天交互 bug，优先看 abort 失败/daemon 离线时的切会话状态呈现，以及 pending permission dialog 失败恢复的实际 WebView 表现；3) 继续点测系统通知、diff 恢复按钮、Open File、历史图片 lightbox 与 Codex 长历史锚点。

## 本轮 PLAN 2026-06-19 session transition abort failure visibility

- 目标：修复会话新建/清空/加载历史时 `chat_abort` 失败被后续 `error: null` 覆盖，用户看不到 daemon 未正常中止的交互反馈问题。
- 功能点 1：确认根因与现有契约。验证方式：CodeGraph 读取 `useChatStore.abortActiveRequestIfNeeded()`、`startNewSession()`、`clear()`、`loadSession()` 与现有 store 测试。
- 功能点 2：补回归测试。验证方式：在 `src/stores/useChatStore.test.ts` 增加 abort 失败后 transition 仍完成但错误保留的用例，先运行定向测试确认 RED。
- 功能点 3：实现最小修复。验证方式：让 session transition 的最终状态保留 abort 失败错误，同时不改变“尽量继续切换/清空”的恢复策略。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/frontend/state-management.md` 与本文件最终记录。
- 功能点 5：质量验证。验证方式：运行定向 store 测试、相关聊天交互测试、`npm test`、`npm run build`、Rust check/test（工作区已有 Rust 改动）和 `git diff --check`。

## 迭代记录 2026-06-19 session transition abort failure visibility

- 上轮实际进展：active 会话重选规则已落地，页面层和侧栏层共享 `shouldIgnoreChatSessionSelection()`，解决“历史会话 B 加载中点击 active A 被页面层吞掉”的问题；全量前端、构建、Rust check/test、`git diff --check` 均已通过。阻塞仍是真实 Tauri 桌面点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的会话切换由统一状态边界处理；本项目已有 `abortActiveRequestIfNeeded()` 防止旧请求事件污染新 transcript，但它在 `chat_abort` 失败时只短暂 `set({ error })`，随后 `startNewSession()`、`clear()`、`loadSession()` 的最终状态又写回 `error: null`。结果是 daemon 未正常接受中止的事实被静默隐藏，用户无法判断旧请求是否真的被干净打断。
- 本轮诊断（超越 cc-gui）：为了从 stuck daemon 中恢复，会话新建/清空/加载历史可以继续执行；但交互反馈不能丢。最终 store 状态应同时表达“用户请求的 transition 已完成”和“上一轮 abort 失败，需要留意 daemon 状态”。这比直接阻塞切换更符合恢复场景，也比吞错更可诊断。
- 本轮规划：只修 store 的 session transition abort 错误保留。优先级中高：用户价值高（避免中止失败被隐藏，减少会话状态错判）、出现频率低到中（daemon 离线/卡死时出现，但后果严重）、实现成本低（helper 返回错误 + 三条状态落点）、风险低（不改后端协议，不改变成功路径）。
- 本轮完成：`src/stores/useChatStore.ts` 让 `abortActiveRequestIfNeeded()` 返回 `String(error) | null`，`loadSession()`、`startNewSession()`、`clear()` 在最终状态中保留 abort 错误；`src/stores/useChatStore.test.ts` 新增三条回归测试覆盖新建、清空、加载历史时 abort 失败仍完成 transition 且错误可见；`.trellis/spec/frontend/state-management.md` 记录该 session transition abort 边界契约。
- 本轮验证：先新增测试并确认 RED：`npm test -- src/stores/useChatStore.test.ts` 失败 3 项，均为期望 abort 错误但实际 `error: null`。实现后同命令通过（1 file / 35 tests）。后续验证通过：`npm test -- src/stores/useChatStore.test.ts src/utils/chatUiBehavior.test.ts src/components/chat/chatSessionSidebarUtils.test.ts src/utils/chatNavigation.test.ts`（4 files / 77 tests）；`npm test`（27 files / 219 tests）；`cargo check --manifest-path src-tauri/Cargo.toml`；`npm run build`；`cargo test --manifest-path src-tauri/Cargo.toml`（97 tests，2 doctests ignored）；`git diff --check` 无 whitespace error，仅 LF/CRLF 转换提示。未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测 abort 失败/daemon 离线场景：新建、清空、加载历史后错误是否可见且旧流不会写入新 transcript；2) 继续排查 pending permission dialog 失败恢复的实际 WebView 表现，尤其组件 submitted 状态与 store restore 的生命周期；3) 真实点测历史会话快速切换、系统通知、diff 恢复按钮、Open File、历史图片 lightbox 与 Codex 长历史锚点。
## 本轮 PLAN 2026-06-19 permission dialogs submitted guard

- 目标：修复 `PlanApprovalDialog` 与 `ToolPermissionDialog` 在首次响应后仍允许按钮/遮罩/键盘重复触发的交互缺陷，让组件层与 store 级 requestId 幂等边界形成双保险。
- 功能点 1：确认根因与对标行为。验证方式：CodeGraph 读取本项目三类权限弹窗与 store 响应动作，并对照 cc-gui `PermissionDialog` / `PlanApprovalDialog` 的 `markSubmitted()` 单入口 guard。
- 功能点 2：补回归测试。验证方式：在 `PlanApprovalDialog.test.ts` 与 `ToolPermissionDialog.test.ts` 增加 submitted helper 用例，先运行定向测试确认 RED。
- 功能点 3：实现最小修复。验证方式：Plan/Tool 弹窗复用 `markDialogSubmitted()`，首次响应后设置 `submitted`，禁用响应按钮和关闭入口，键盘/遮罩重复触发不再调用回调。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 与本文件最终记录。
- 功能点 5：质量验证。验证方式：运行定向弹窗测试、相邻 dialog/store 测试、`npm test`、`npm run build`、Rust check/test（工作区已有 Rust 改动）和 `git diff --check`。

## 迭代记录 2026-06-19 permission dialogs submitted guard

- 上轮实际进展：session transition abort 失败可见性已在 store 层落地并通过全量前端、构建、Rust check/test 与 `git diff --check` 验证；阻塞仍是真实 Tauri 桌面点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 `PermissionDialog` 与 `PlanApprovalDialog` 都通过 `markSubmitted()` 把按钮、键盘和超时响应收敛成第一次提交单入口。本项目 AskUserQuestion 已有组件级 submitted guard，但 `PlanApprovalDialog` 与 `ToolPermissionDialog` 只依赖 store 级 requestId 幂等。store 能阻止重复写响应文件，但用户双击、按住 Enter 或连续点击遮罩时，UI 在 pending 被清除/恢复前仍表现为可重复提交。
- 本轮诊断（超越 cc-gui）：权限类弹窗应有两层边界：组件层负责即时交互反馈和禁用重复入口，store 层负责最终 requestId 幂等与失败恢复。这样即使 invoke 失败、store 恢复同一个 pending 请求，组件也会随重新挂载/新 requestId 重置 submitted 状态，用户可以明确重试。
- 本轮规划：只修 PlanApproval / ToolPermission 组件级 submitted guard。优先级中高：用户价值高（阻塞弹窗的重复提交会直接影响聊天继续执行）、出现频率中（键盘/双击高频）、实现成本低（复用已有 `markDialogSubmitted()` + 单测）、风险低（不改后端协议和 store 响应语义）。
- 本轮完成：`src/components/chat/PlanApprovalDialog.tsx` 新增 `submitPlanApprovalDecision()`、submitted ref/state、requestId 切换重置、`aria-busy`、按钮禁用和 spinner，Approve / Approve Auto / Deny / Cancel / 遮罩 / Enter / Escape 都先走单入口 guard；`src/components/chat/ToolPermissionDialog.tsx` 新增 `submitToolPermissionDecision()`、submitted ref/state、requestId 切换重置、`aria-busy`、按钮禁用和 allow spinner，Allow / Deny / 遮罩 / Enter / Escape 都先走单入口 guard；`src/components/chat/PlanApprovalDialog.test.ts` 与 `src/components/chat/ToolPermissionDialog.test.ts` 增加重复提交回归测试；`.trellis/spec/frontend/component-guidelines.md` 记录 Plan/Tool 弹窗组件级 submitted 契约。
- 本轮验证：先新增测试并确认 RED：`npm test -- src/components/chat/PlanApprovalDialog.test.ts src/components/chat/ToolPermissionDialog.test.ts` 失败于 `submitPlanApprovalDecision is not a function` / `submitToolPermissionDecision is not a function`。实现后同命令通过（2 files / 8 tests）。后续验证通过：`npm test -- src/components/chat/AskUserQuestionDialog.test.ts src/utils/dialogShortcuts.test.ts src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/stores/useChatStore.test.ts src/utils/chatUiBehavior.test.ts`（6 files / 61 tests）；`npm test`（27 files / 221 tests）；`cargo check --manifest-path src-tauri/Cargo.toml`；`npm run build`；`cargo test --manifest-path src-tauri/Cargo.toml`（97 tests，2 doctests ignored）；`git diff --check` 无 whitespace error，仅 LF/CRLF 转换提示。未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测三类权限弹窗：双击、按住 Enter/Escape、遮罩点击、invoke 失败后恢复同一 pending 请求是否可重试；2) 继续排查 pending permission dialog 失败恢复的真实 WebView 生命周期，尤其 store 恢复同一个 requestId 时是否一定重新挂载；3) 真实点测 abort 失败/daemon 离线、新建/清空/加载历史、历史会话快速切换、系统通知、diff 恢复按钮、Open File、历史图片 lightbox 与 Codex 长历史锚点。
## 本轮 PLAN 2026-06-19 permission failure restored request identity

- 目标：修复权限/计划/提问弹窗在响应 invoke 失败后恢复同一 pending request 时，组件 submitted 状态可能不复位导致用户无法重试的边界问题。
- 功能点 1：确认根因与对标行为。验证方式：CodeGraph 读取 `useChatStore` 三类响应失败恢复、`ChatPage` 弹窗挂载点和三个 dialog 的 submitted reset 依赖，对照 cc-gui `markSubmitted()` 请求 key 重置模式。
- 功能点 2：补回归测试。验证方式：在 `useChatStore.test.ts` 增加三类 pending 恢复后对象引用必须更新的测试，先运行定向测试确认 RED。
- 功能点 3：实现最小修复。验证方式：失败恢复 pending 时 clone 原 request，并让 AskUserQuestion / ToolPermission / PlanApproval 的 submitted reset effect 依赖 request 对象变化。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/frontend/state-management.md`、`.trellis/spec/frontend/component-guidelines.md` 与本文件最终记录。
- 功能点 5：质量验证。验证方式：运行定向 store/dialog 测试、`npm test`、`npm run build`、Rust check/test（工作区已有 Rust 改动）和 `git diff --check`。

## 迭代记录 2026-06-19 permission failure restored request identity

- 上轮实际进展：PlanApproval / ToolPermission 弹窗已补组件级 submitted guard，阻止首次响应后按钮/遮罩/键盘重复触发；全量前端测试、构建、Rust check/test 和 `git diff --check` 均通过。阻塞仍是真实 Tauri 桌面点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 dialog submitted 状态会按请求 key 重置；本项目上轮虽有组件级 submitted guard，但 `useChatStore` 在三类权限响应失败时把原 pending 对象原样恢复，三个 dialog 又只按 `request.requestId` 重置 submitted。如果 React 批处理掉中间 `pending=null` 的卸载过程，失败恢复同一个 requestId 可能保留 `submitted=true`，用户看到恢复的弹窗但控件仍禁用，无法重试。
- 本轮诊断（超越 cc-gui）：失败恢复应同时表达“还是同一个业务请求”和“这是新一轮可交互提交周期”。store 保留 requestId/内容不变，但用新的顶层 request 对象引用作为 UI reset 信号；组件则监听 request 对象变化，而不是只监听 requestId。
- 本轮规划：只修失败恢复 request identity 与 dialog submitted reset。优先级中高：用户价值高（响应写入失败后必须可重试，否则聊天被阻塞）、出现频率低到中（失败少见但影响严重）、实现成本低（store clone + effect dependency + 单测）、风险低（不改后端协议和 payload 语义）。
- 本轮完成：`src/stores/useChatStore.ts` 新增 `clonePermissionRequest()`，`answerAskUserQuestion()` / `answerToolPermission()` / `approvePlan()` 在 invoke 失败恢复 pending 时返回等价的新对象引用；`src/components/chat/AskUserQuestionDialog.tsx`、`src/components/chat/ToolPermissionDialog.tsx`、`src/components/chat/PlanApprovalDialog.tsx` 的 submitted reset effect 改为依赖 `request` 对象变化；`src/stores/useChatStore.test.ts` 新增/调整三类失败恢复测试，要求恢复对象 `toEqual` 原 pending 但 `not.toBe` 原 pending；`.trellis/spec/frontend/state-management.md` 与 `.trellis/spec/frontend/component-guidelines.md` 记录该契约。
- 本轮验证：先新增/调整测试并确认 RED：`npm test -- src/stores/useChatStore.test.ts` 失败 3 项，均为恢复对象仍与原 pending `Object.is` 相等。实现后同命令通过（1 file / 36 tests）。后续验证通过：`npm test -- src/components/chat/AskUserQuestionDialog.test.ts src/utils/dialogShortcuts.test.ts src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/stores/useChatStore.test.ts src/utils/chatUiBehavior.test.ts`（6 files / 62 tests）；`npm test`（27 files / 222 tests）；`cargo check --manifest-path src-tauri/Cargo.toml`；`npm run build`；`cargo test --manifest-path src-tauri/Cargo.toml`（97 tests，2 doctests ignored）；`git diff --check` 无 whitespace error，仅 LF/CRLF 转换提示。未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测三类权限响应失败恢复：模拟/制造 invoke 失败后确认同一 requestId 的弹窗重新可点；2) 继续排查真实 WebView 生命周期下权限队列响应后下一个 pending 是否稳定浮现；3) 继续点测 abort 失败/daemon 离线、新建/清空/加载历史、历史会话快速切换、系统通知、diff 恢复按钮、Open File、历史图片 lightbox 与 Codex 长历史锚点。

## 本轮 PLAN 2026-06-19 permission request queue

- 目标：修复同类权限请求在用户响应前连续到达时被单个 pending 槽位覆盖的问题，确保 watcher 已删除的旧 request 文件仍有前端可响应入口。
- 功能点 1：确认根因与对标行为。验证方式：CodeGraph 读取 `PermissionWatcher.poll_once()`、`useChatStore.init()` 的 permission listener、三类响应 action，并对照 cc-gui 的阻塞 permission 入口。
- 功能点 2：补回归测试。验证方式：在 `src/stores/useChatStore.test.ts` 模拟两个同类 permission event 连续到达，先运行定向测试确认 RED：响应第一个后第二个应浮现并最终响应。
- 功能点 3：实现最小修复。验证方式：在 store 内为 AskUserQuestion / ToolPermission / PlanApproval 分别维护队列，保持现有 `pendingXxx` 对外 API 表示队首请求；响应成功弹出队首，失败恢复队首且不丢后续请求。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/frontend/state-management.md` 与本文件最终记录。
- 功能点 5：质量验证。验证方式：运行定向 store 测试、dialog/chatUiBehavior 相邻测试、`npm test`、`npm run build`、Rust check/test（工作区已有 Rust 改动）和 `git diff --check`。

## 迭代记录 2026-06-19 permission request queue

- 上轮实际进展：三类权限弹窗 submitted guard 与失败恢复 request identity 已落地并通过全量前端、构建、Rust check/test 与 `git diff --check`；阻塞仍是真实 Tauri 桌面点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：`src-tauri/src/chat/permission_watcher.rs` 的 `poll_once()` 会在一次轮询里遍历多个 permission request 文件，emit 后立即删除 request 文件；但 `src/stores/useChatStore.ts` 原先每类只保留一个 `pendingXxx`。同类请求连续到达时，后一个会覆盖前一个，前一个 request 文件已经被删除且没有响应入口，daemon 只能等超时。
- 本轮诊断（超越 cc-gui）：同类权限请求不只要“只显示一个 active dialog”，还必须有 FIFO 队列和 in-flight requestId 去重。响应写入过程中，如果同一 requestId 的重复事件到达，应丢弃；如果其他同类请求到达，应排在后面，避免键盘/遮罩/后端 watcher 时序把请求顺序打乱。
- 本轮规划：只修 store 的三类 permission request queue。优先级高：用户价值高（权限请求丢失会直接卡住聊天）、出现频率中（并行工具或快速连续 permission 文件时出现）、实现成本中低（store 队列 + 单测）、风险低（不改后端文件 IPC 命名，不改 dialog props）。
- 本轮完成：`src/stores/useChatStore.ts` 新增三类 pending 队列与 in-flight requestId 字段，`permission://ask-user-question` / `permission://tool` / `permission://plan-approval` listener 改为 FIFO 入队；三类响应 action 成功后提升下一条 queued request，失败时恢复当前请求的新对象引用并保留队列。`src/stores/useChatStore.test.ts` 新增三类同类请求排队回归测试，并补充“响应写入中重复 tool permission event 不会重新入队”的边界测试。`.trellis/spec/frontend/state-management.md` 记录 permission queue 契约。
- 本轮验证：RED：`npm test -- src/stores/useChatStore.test.ts` 先失败 3 项，证明同类请求被后到事件覆盖；随后新增 in-flight 重复事件测试再失败 1 项，证明正在响应的同 requestId 会被重新入队。GREEN：`npm test -- src/stores/useChatStore.test.ts` 通过（40 tests）；`npm test -- src/stores/useChatStore.test.ts src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/utils/chatUiBehavior.test.ts` 通过（5 files / 63 tests）；`npm test` 通过（27 files / 226 tests）；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`npm run build` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（97 tests，2 doctests ignored）；`git diff --check` 退出 0，仅 LF/CRLF 提示。未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测并行/连续 permission 文件：连续两个工具权限、两个 AskUserQuestion、两个 PlanApproval，确认队首响应后下一条稳定浮现；2) 修复/固化 permission response 的 sessionId 协议债，目前 manager 和响应命令都写死 `default`，未来一旦启用随机 `CLAUDE_SESSION_ID` 会造成 response 文件名不匹配；3) 继续点测 abort 失败/daemon 离线、新建/清空/加载历史、历史会话快速切换、系统通知、diff 恢复按钮、Open File、历史图片 lightbox 与 Codex 长历史锚点。

## 本轮 PLAN 2026-06-19 permission response session id

- 目标：修复权限响应链路丢失 `sessionId` 的协议缺口，避免 daemon 使用非 `default` `CLAUDE_SESSION_ID` 时前端响应写到错误文件名，导致聊天卡在权限等待。
- 功能点 1：确认跨层根因。验证方式：CodeGraph 读取 `PermissionWatcher`、三类 `write_*_response`、`permission_respond_*` 命令和 `useChatStore` 三类响应 action，并对照 cc-gui 的 permission 请求/响应文件 IPC 行为。
- 功能点 2：补前端回归测试。验证方式：在 `src/stores/useChatStore.test.ts` 覆盖 AskUserQuestion / ToolPermission / PlanApproval 响应 invoke 必须携带 `sessionId`，并覆盖旧事件缺失 `sessionId` 时回退 `default`，先运行定向测试确认 RED。
- 功能点 3：补后端回归测试。验证方式：在 Rust permission watcher / command 附近覆盖三类请求反序列化包含 `sessionId`，以及响应 writer 能写入自定义 session 文件名；先运行定向 Rust 测试确认 RED。
- 功能点 4：实现最小修复。验证方式：请求模型增加 `sessionId`，watcher 从文件名或自身 session 注入缺失字段；前端类型与 store 响应 action 传回 `sessionId ?? 'default'`；Rust Tauri 命令按传入 session 写响应文件。
- 功能点 5：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/backend/cross-layer-protocol.md`、`.trellis/spec/frontend/state-management.md` 与本文件最终记录。
- 功能点 6：质量验证。验证方式：运行定向前端/Rust 测试、相关权限 dialog/store 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check`。

## 迭代记录 2026-06-19 permission response session id

- 上轮实际进展：permission request queue 已在 store 层落地，三类权限请求可 FIFO 入队、响应成功后提升下一条，响应写入中的重复 requestId 会被丢弃；全量前端测试、构建、Rust check/test 与 `git diff --check` 均已通过。阻塞仍是真实 Tauri 桌面点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：本项目 permission request 文件名包含 session 维度：`request-<sessionId>-<requestId>.json`、`ask-user-question-<sessionId>-<requestId>.json`、`plan-approval-<sessionId>-<requestId>.json`；但 `src-tauri/src/commands/chat_commands.rs` 三类响应命令原先按硬编码 `default` 写 response 文件，`src/stores/useChatStore.ts` 的响应 action 也没有传回 `sessionId`。当前能工作只是因为 watcher/daemon 默认都使用 `default`，一旦 `CLAUDE_SESSION_ID` 变成非默认值，前端会写到错误文件名，daemon 会一直等待正确 session 的响应。
- 本轮诊断（超越 cc-gui）：sessionId 不应只在后端文件名里隐式存在，应该成为前端 pending request 与响应命令之间的显式协议字段。watcher 需要兼容旧 JSON：缺失或空白 `sessionId` 时用文件名/自身 session 补齐；前端响应时始终回传规范化后的 session；后端命令再按该 session 写 response 文件。这样既保留旧事件兼容，也为未来多 session / 随机 session id 打基础。
- 本轮规划：只修 permission response `sessionId` 透传链路。优先级高：用户价值高（权限等待卡死会直接阻断聊天）、出现频率中（当前默认 session 下不暴露，但一旦启用非默认 session 就是必现）、实现成本中低（类型字段 + store payload + Rust 命令参数/normalize helper + 测试）、风险可控（保持 `default` fallback，不改 UI 结构和 response payload 语义）。
- 本轮完成：`src/types/permission.ts` 为 `AskUserQuestionRequest`、`ToolPermissionRequest`、`PlanApprovalRequest` 增加可选 `sessionId`；`src/stores/useChatStore.ts` 新增 `DEFAULT_PERMISSION_SESSION_ID` 与 `permissionSessionId()`，三类响应 action 都把 pending request 的 `sessionId` 传给 Tauri invoke，旧事件回退 `default`；`src-tauri/src/chat/permission_watcher.rs` 为三类 request 增加 `session_id` 字段、默认值、normalize/fill helper，并在 emit 前把缺失 session 补齐；`src-tauri/src/chat/mod.rs` re-export `permission_response_session_id()`；`src-tauri/src/chat/manager.rs` 让 `PermissionWatcher` 使用 daemon 的 `SESSION_ID`；`src-tauri/src/commands/chat_commands.rs` 三类 `permission_respond_*` 命令接受可选 `session_id` 并按规范化 session 写响应文件；`src/stores/useChatStore.test.ts` 和 `permission_watcher.rs` 测试覆盖自定义 session、旧事件 fallback、三类 request 反序列化与 normalize；`.trellis/spec/backend/cross-layer-protocol.md` 与 `.trellis/spec/frontend/state-management.md` 同步记录 permission session 协议。
- 本轮验证：RED 阶段已确认：`npm test -- src/stores/useChatStore.test.ts` 失败于三类 invoke payload 缺少 `sessionId`；`cargo test --manifest-path src-tauri/Cargo.toml permission_watcher -- --nocapture` 失败于 request 结构未保留 `sessionId`。GREEN 与回归验证通过：`npm test -- src/stores/useChatStore.test.ts`（40 tests）；`cargo test --manifest-path src-tauri/Cargo.toml permission_watcher -- --nocapture`（7 tests）；`npm test -- src/stores/useChatStore.test.ts src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/utils/chatUiBehavior.test.ts`（5 files / 63 tests）；`npm test`（27 files / 226 tests）；`cargo check --manifest-path src-tauri/Cargo.toml`；`npm run build`；`cargo test --manifest-path src-tauri/Cargo.toml`（102 tests，2 doctests ignored）；`git diff --check` 退出 0，仅 LF/CRLF 转换提示。接手收尾后再次复跑 `npm test -- src/stores/useChatStore.test.ts`（40 tests）、`cargo test --manifest-path src-tauri/Cargo.toml permission_watcher -- --nocapture`（7 tests）和 `git diff --check`，均通过。未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测非默认 `sessionId`：制造/观察 `request-custom-req.json`，确认前端事件携带 `sessionId`，响应文件写成 `response-custom-req.json`；2) 真实 Tauri 桌面点测连续权限队列：连续两个 ToolPermission、两个 AskUserQuestion、两个 PlanApproval，确认 FIFO 队首响应后下一条稳定浮现；3) 继续排查聊天交互 bug，优先看 daemon offline / abort failure 的真实 WebView 表现、pending permission 失败恢复生命周期、历史会话快速切换、系统通知、diff 恢复按钮、Open File、历史图片 lightbox 与 Codex 长历史锚点。

## 本轮 PLAN 2026-06-19 composer pre-request send guard

- 目标：修复 `ChatComposer` 在 `chat_send` 返回 `requestId` 前的提交空窗，避免用户双击发送按钮或快速按 Enter 造成同一草稿被发送两次。
- 功能点 1：确认根因与对标行为。验证方式：CodeGraph 读取本项目 `ChatComposer` / `ButtonArea` / `useChatStore.send()` 与 cc-gui `ChatInputBox` / `useSubmitHandler`，确认当前项目没有消息队列，因此需要本地 in-flight guard。
- 功能点 2：补回归测试。验证方式：在 `ChatComposer.test.ts` 增加提交阻塞 helper 用例，在 `ButtonArea.test.tsx` 增加 pre-request submitting 状态渲染用例，先运行定向测试确认 RED。
- 功能点 3：实现最小修复。验证方式：`ChatComposer` 增加 ref + state 的本地 `isSending`，发送开始立即阻塞后续提交，`finally` 释放；`ButtonArea` 区分 `isSubmitting` 与 `isLoading`，pre-request 阶段显示禁用发送按钮而不是 Stop。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 与本文件最终记录。
- 功能点 5：质量验证。验证方式：运行定向 composer/button 测试、相邻 store 测试、`npm test`、`npm run build`、Rust check/test（工作区已有 Rust 改动）和 `git diff --check`。

## 迭代记录 2026-06-19 composer pre-request send guard

- 上轮实际进展：permission response `sessionId` 已显式贯穿三类权限 pending request、前端 invoke、Rust Tauri 命令与 response 文件名；前端/Rust 权限测试、全量前端测试、构建、Rust check/test 与 `git diff --check` 均已通过。阻塞仍是真实 Tauri 桌面点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 `ChatInputBox` 发送链路由 `useSubmitHandler()` 清输入后交给父层，且有 message queue 语义兜底；当前项目 `src/components/chat/composer/ChatComposer.tsx` 没有消息队列，`isStreaming` 只来自 `activeRequestId !== null`。但 `useChatStore.send()` 要等 `chat_send` 返回 requestId 才设置 `activeRequestId`，这之前双击发送按钮或快速按两次 Enter 会穿过同一个 render 的旧 closure，重复调用 `send()`，同一草稿可能被追加并发送两次。
- 本轮诊断（超越 cc-gui）：pre-requestId 阶段和 streaming 阶段不是同一种状态。前者不能显示 Stop，因为 `chat_abort` 还没有可定位的 requestId；正确交互是立即禁用发送、增强和 selector，显示一个轻量 sending affordance，等 requestId 返回后再进入真正 streaming/Stop 状态。
- 本轮规划：只修 composer 的 pre-request send guard。优先级高：用户价值高（重复发送会产生重复消息、重复工具执行和重复费用/副作用）、出现频率中高（双击按钮、键盘连击很常见）、实现成本低（本地 ref/state + ButtonArea 状态区分 + 单测）、风险低（不改 store/后端协议，不引入消息队列）。
- 本轮完成：`src/components/chat/composer/ChatComposer.tsx` 新增 `shouldBlockChatComposerSubmit()` 纯 helper、`sendInFlightRef` 与 `isSending`，在发送开始前立即阻塞后续提交，并在 `finally` 中释放；发送失败恢复附件/草稿逻辑保留。`src/components/chat/composer/ButtonArea.tsx` 新增 `isSubmitting` prop，selector/enhance 在 pre-requestId 阶段禁用，发送按钮显示 disabled loader，只有 `isLoading` 才显示 Stop。`src/components/chat/composer/ChatComposer.test.ts` 增加提交阻塞 helper 测试；`src/components/chat/composer/ButtonArea.test.tsx` 增加 submitting 状态渲染测试；`.trellis/spec/frontend/component-guidelines.md` 记录 composer pre-request submit guard 契约。
- 本轮验证：先新增测试并确认 RED：`npm test -- src/components/chat/composer/ChatComposer.test.ts src/components/chat/composer/ButtonArea.test.tsx` 失败于 `shouldBlockChatComposerSubmit is not a function`，且 ButtonArea 在 `isSubmitting` 下仍渲染可点击发送按钮。实现后同命令通过（2 files / 7 tests）；相邻验证 `npm test -- src/components/chat/composer/ChatComposer.test.ts src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/useCompletions.test.ts src/stores/useChatStore.test.ts` 通过（4 files / 55 tests）；全量 `npm test` 通过（27 files / 230 tests，仅 Browserslist/caniuse-lite 过期提示）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（102 tests，2 doctests ignored）；`git diff --check` 退出 0，仅 LF/CRLF 转换提示。未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测 composer：双击 Send、快速按 Enter、SDK 缺失、`chat_send` 失败附件恢复，确认只发一次且 UI 状态符合 pre-request/streaming 区分；2) 继续真实 Tauri 点测非默认 permission `sessionId` 和连续权限队列；3) 继续排查聊天交互 bug，优先看 prompt enhance 的并发/失败恢复、历史会话快速切换、系统通知、diff 恢复按钮、Open File、历史图片 lightbox 与 Codex 长历史锚点。

## 本轮 PLAN 2026-06-19 prompt enhancer duplicate guard

- 目标：修复 `ChatComposer` Prompt Enhance 在 React `enhancing` 状态刷新前的重复点击空窗，避免同一提示词并发触发多次 `chat_enhance_prompt`。
- 功能点 1：确认根因与对标行为。验证方式：CodeGraph 读取本项目 `ChatComposer.handleEnhance` / `ButtonArea` / `PromptEnhancerDialog`，并对照 cc-gui `usePromptEnhancer`，确认本项目没有即时 in-flight guard。
- 功能点 2：补回归测试。验证方式：在 `ChatComposer.test.ts` 增加 prompt enhance 阻塞 helper 用例，先运行定向测试确认 RED。
- 功能点 3：实现最小修复。验证方式：`ChatComposer` 增加 prompt enhancer ref guard，handler 入口立即阻止重复 enhance；保持失败时关闭弹窗、保留原文的现有策略。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 与本文件最终记录。
- 功能点 5：质量验证。验证方式：运行定向 composer/button 测试、相邻 store/completion 测试、`npm test`、`npm run build`、Rust check/test（工作区已有 Rust 改动）和 `git diff --check`。

## 迭代记录 2026-06-19 prompt enhancer duplicate guard

- 上轮实际进展：composer pre-request send guard 已在 `ChatComposer` / `ButtonArea` 落地，发送开始到 `requestId` 返回前会禁用发送、增强和 selector，避免同一草稿重复发送；全量前端测试、构建、Rust check/test 与 `git diff --check` 均已通过。阻塞仍是真实 Tauri 桌面点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：对照 `jetbrains-cc-gui/webview/src/components/ChatInputBox/hooks/usePromptEnhancer.ts`，cc-gui 的增强入口会打开对比弹窗并置 `isEnhancing`，但没有 ref 级即时防重。本项目 `src/components/chat/composer/ChatComposer.tsx` 的 `handleEnhance()` 也只检查 `enhancing` state，双击 Sparkles 或快速触发两次 handler 时，第二次可能在 React commit 前穿过旧 closure，向 `chat_enhance_prompt` 并发发送同一提示词。
- 本轮诊断（超越 cc-gui）：Prompt Enhance 是高成本、可能返回乱序结果的异步改写动作，应该像发送一样区分“已触发但 UI 尚未重渲染”的 pre-render 空窗。用 ref 作为立即边界、state 负责视觉反馈，可以避免重复请求、重复弹窗内容回写和用户误以为增强结果不稳定。
- 本轮规划：只修 `ChatComposer` 的 Prompt Enhance 防重。优先级中高：用户价值高（避免重复请求和 stale enhanced result 覆盖）、出现频率中（增强按钮天然容易双击）、实现成本低（纯 helper + ref）、风险低（不改后端命令协议，不改变失败时保留原文/关闭弹窗策略）。
- 本轮完成：`src/components/chat/composer/ChatComposer.tsx` 新增 `shouldBlockPromptEnhance()` 纯 helper 和 `enhanceInFlightRef`，在 `chat_enhance_prompt` invoke 前同步置位，`finally` 释放；`handleEnhance()` 继续在失败时清空增强文本、关闭弹窗并保留原草稿。`src/components/chat/composer/ChatComposer.test.ts` 已有 3 条 helper 回归测试覆盖 in-flight、空文本和 rendered loading 状态。`.trellis/spec/frontend/component-guidelines.md` 记录 Prompt Enhance ref-level guard 与失败策略。
- 本轮验证：接手前 RED 已确认：`npm test -- src/components/chat/composer/ChatComposer.test.ts` 失败于 `shouldBlockPromptEnhance is not a function`。实现后验证通过：`npm test -- src/components/chat/composer/ChatComposer.test.ts`（1 file / 8 tests）；`npm test -- src/components/chat/composer/ChatComposer.test.ts src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/useCompletions.test.ts src/stores/useChatStore.test.ts`（4 files / 58 tests）；`npm test`（27 files / 233 tests，仅 Browserslist/caniuse-lite 过期提示）；`npm run build`；`cargo check --manifest-path src-tauri/Cargo.toml`；`cargo test --manifest-path src-tauri/Cargo.toml`（102 tests，2 doctests ignored）；`git diff --check` 退出 0，仅 LF/CRLF 转换提示。未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测 composer：双击 Prompt Enhance、增强失败、Enhance 进行中再发送/切 selector，确认只触发一次且失败保留原文；2) 真实 Tauri 点测双击 Send、快速 Enter、`chat_send` 失败附件恢复，补齐上一轮未验证项；3) 继续排查聊天交互 bug，优先看 daemon offline / abort failure 的真实 WebView 表现、历史会话快速切换、系统通知、diff 恢复按钮、Open File、历史图片 lightbox 与 Codex 长历史锚点。

## 本轮 PLAN 2026-06-19 prompt enhancer dialog enter shortcut

- 目标：补齐 `PromptEnhancerDialog` 相比 cc-gui 缺失的“增强结果就绪后按 Enter 采用增强版”键盘路径，同时避免焦点在按钮/输入控件上时发生全局快捷键与原生控件行为双触发。
- 功能点 1：确认根因与对标行为。验证方式：CodeGraph 读取本项目 `PromptEnhancerDialog` / `ChatComposer` 与 cc-gui `PromptEnhancerDialog`，确认本项目只处理 `Escape`。
- 功能点 2：补回归测试。验证方式：新增 `src/components/chat/composer/PromptEnhancerDialog.test.ts`，先运行定向测试确认 RED。
- 功能点 3：实现最小修复。验证方式：新增纯 helper 解析 `Escape` / `Enter`，复用 `dialogShortcuts` 避免抢按钮/编辑控件；弹窗 keydown 根据 helper 调用 `onClose` 或 `onUseEnhanced`。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 与本文件最终记录。
- 功能点 5：质量验证。验证方式：运行定向 enhancer/composer 测试、相邻 dialog shortcut 测试、`npm test`、`npm run build`、Rust check/test（工作区已有 Rust 改动）和 `git diff --check`。

## 迭代记录 2026-06-19 prompt enhancer dialog enter shortcut

- 上轮实际进展：Prompt Enhance 重复触发 guard 已在 `ChatComposer` 落地，`chat_enhance_prompt` invoke 前会用 `enhanceInFlightRef` 即时防重，失败时关闭弹窗并保留原草稿；全量前端测试、构建、Rust check/test、IDE 文件构建和 `git diff --check` 均已通过。阻塞仍是真实 Tauri 桌面点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：对照 `jetbrains-cc-gui/webview/src/components/ChatInputBox/PromptEnhancerDialog.tsx`，cc-gui 在增强结果就绪后支持 `Enter` 直接采用增强版、`Escape` 关闭；本项目 `src/components/chat/composer/PromptEnhancerDialog.tsx` 只监听 `Escape`，键盘用户必须 Tab 到按钮或改用鼠标，和输入区“键盘优先”的交互不一致。
- 本轮诊断（超越 cc-gui）：直接照抄全局 `Enter` 会产生新风险：焦点在 footer button 上时，浏览器原生 Enter click 和 window keydown 可能同时调用回调。本轮用纯 helper 复用 `dialogShortcuts` 的 editable / button target 判断，让全局 Enter 只在焦点不属于控件时采用增强版；Escape 保持可关闭但不抢编辑控件。
- 本轮规划：只修 Prompt Enhancer 对比弹窗快捷键。优先级中高：用户价值高（增强结果确认是高频键盘路径）、出现频率中（每次使用 enhancer 都会遇到）、实现成本低（纯 helper + effect）、风险低（不改 `chat_enhance_prompt` 协议，不改 ChatComposer 状态流）。
- 本轮完成：`src/components/chat/composer/PromptEnhancerDialog.tsx` 新增 `resolvePromptEnhancerShortcutAction()`，支持 `Enter` 在非 loading 且存在增强结果时采用增强版，`Escape` 关闭，并避开 button / editable target；弹窗 keydown effect 改为根据该 helper 调用 `onUseEnhanced` / `onClose`。新增 `src/components/chat/composer/PromptEnhancerDialog.test.ts` 覆盖 Enter ready/loading/empty、Escape、按钮和 contenteditable target。`.trellis/spec/frontend/component-guidelines.md` 记录 enhancer 对比弹窗快捷键契约。新增测试文件已执行 `git add -- src/components/chat/composer/PromptEnhancerDialog.test.ts`。
- 本轮验证：先新增测试并确认 RED：`npm test -- src/components/chat/composer/PromptEnhancerDialog.test.ts` 失败于 `resolvePromptEnhancerShortcutAction is not a function`。实现后验证通过：`npm test -- src/components/chat/composer/PromptEnhancerDialog.test.ts`（1 file / 3 tests）；`npm test -- src/components/chat/composer/PromptEnhancerDialog.test.ts src/components/chat/composer/ChatComposer.test.ts src/components/chat/composer/ButtonArea.test.tsx src/utils/dialogShortcuts.test.ts src/components/chat/PlanApprovalDialog.test.ts src/components/chat/ToolPermissionDialog.test.ts`（6 files / 24 tests）；`npm test`（28 files / 236 tests，仅 Browserslist/caniuse-lite 过期提示）；`npm run build`；`cargo check --manifest-path src-tauri/Cargo.toml`；`cargo test --manifest-path src-tauri/Cargo.toml`（102 tests，2 doctests ignored）；IDE `build_project` 针对 `PromptEnhancerDialog.tsx` 通过；`git diff --check` 退出 0，仅 LF/CRLF 转换提示。未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测 Prompt Enhancer：增强完成后 Enter 是否采用增强版、按钮焦点 Enter 是否只触发一次、Escape 是否关闭、loading 中 Enter 是否无效；2) 继续真实点测 composer 双击 Send / 双击 Enhance / `chat_send` 失败附件恢复；3) 继续排查聊天交互 bug，优先看 daemon offline / abort failure 的真实 WebView 表现、历史会话快速切换、系统通知、diff 恢复按钮、Open File、历史图片 lightbox 与 Codex 长历史锚点。

## 本轮 PLAN 2026-06-19 open file percent path guard

- 目标：修复 Chat 工具块 / 搜索结果里的 Open File 在合法文件名包含普通 `%` 字符时被前端桥接层误判为 malformed encoded path、导致点击无效的问题。
- 功能点 1：确认根因与对标行为。验证方式：CodeGraph 读取本项目 `src/utils/bridge.ts` / `src/utils/bridge.test.ts`，并对照 `jetbrains-cc-gui/webview/src/utils/bridge.ts` 与 `linkify.ts` 的 `normalizeFileNavigationTarget()`。
- 功能点 2：补回归测试。验证方式：在 `src/utils/bridge.test.ts` 增加普通 `%` 文件名不应阻断 invoke、有效 percent-encoding 仍应解码的用例，先运行定向测试确认 RED。
- 功能点 3：实现最小修复。验证方式：`openFile()` 的归一化改为尽力解码，遇到非法 percent escape 时保留原始合法路径，不再直接返回 null；仍继续拒绝控制字符。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 与本文件最终记录。
- 功能点 5：质量验证。验证方式：运行 `npm test -- src/utils/bridge.test.ts src/utils/toolPresentation.test.ts`、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、必要 Rust editor command 测试和 `git diff --check`。

## 迭代记录 2026-06-19 open file percent path guard

- 上轮实际进展：Prompt Enhancer 对比弹窗已补齐 `Enter` 采用增强版与 `Escape` 关闭，并避开 button/editable target；定向测试、相邻对话框测试、全量前端测试、构建、Rust check/test、IDE 构建与 `git diff --check` 均已通过。阻塞仍是真实 Tauri 桌面点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：对照 `jetbrains-cc-gui/webview/src/utils/bridge.ts` 与 `linkify.ts`，cc-gui 的 `openFile()` 会通过 `normalizeFileNavigationTarget()` 尽力规范化 href，不会因为普通文件名里的 `%` 直接丢掉 Open File。当前项目 `src/utils/bridge.ts` 对整个 path 直接 `decodeURIComponent()`，合法文件名如 `src/reports/100% coverage.md` 会触发 `URI malformed`，`openFile()` 返回 `false`，工具块/搜索结果点击没有任何 Tauri 调用。
- 本轮诊断（超越 cc-gui）：Chat 工具结果里的路径来源混杂：有 URL 编码的 file URI，也有 shell/rg 原样输出的本地路径。桥接层应区分“普通文件系统路径里的字面量 `%`”与“真正损坏的 UTF-8 percent-encoding”，否则用户会误以为工具块文件入口坏了，而且这类 bug 很难从 UI 上看出原因。
- 本轮规划：只修 `openFile()` 的前端路径解码边界。优先级中高：用户价值高（Open File 是工具块核心回看路径）、出现频率中（报告、覆盖率、URL 命名文件常见 `%`）、实现成本低（桥接层 helper + 单测）、风险低（不改后端命令协议，控制字符拦截保持不变）。
- 本轮完成：`src/utils/bridge.test.ts` 新增合法字面量 `%` 文件名回归测试；`src/utils/bridge.ts` 新增 `decodeOpenFileTarget()`，将非 percent-escape 的 `%` 转义后再解码，最多三轮处理重复编码；遇到真正无法解码的损坏 percent payload 仍返回 `null`，并继续在 invoke 前拒绝控制字符。`.trellis/spec/frontend/component-guidelines.md` 记录 Open File 路径归一化契约。
- 本轮验证：先新增测试并确认 RED：`npm test -- src/utils/bridge.test.ts` 失败于 `expected false to be true`，证明当前实现确实阻断 `src/reports/100% coverage.md`。实现后验证通过：`npm test -- src/utils/bridge.test.ts`（6 tests）；`npm test -- src/utils/bridge.test.ts src/utils/toolPresentation.test.ts`（2 files / 38 tests）；`npm test`（28 files / 237 tests，仅 Browserslist/caniuse-lite 过期提示）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（102 tests，2 doctests ignored）；IDE `build_project` 针对 `src/utils/bridge.ts` 和 `src/utils/bridge.test.ts` 通过；`git diff --check` 退出 0，仅 LF/CRLF 转换提示。未做真实 Tauri 桌面点击 Open File 点测。
- 下一步候选：1) 真实 Tauri 桌面点测 Open File：普通 `%` 文件名、`file:///C:/...%20...`、`path:line`、相对路径 + `currentCwd`、搜索结果行号；2) 继续排查 daemon offline / abort failure 的 WebView 恢复路径，尤其是错误提示、按钮状态和附件/草稿恢复是否一致；3) 继续真实点测历史图片 lightbox、diff 恢复按钮、长历史 Codex 锚点与会话快速切换。

## 本轮 PLAN 2026-06-19 load session failure after abort closes streaming turn

- 目标：修复加载历史会话时先成功中止当前流式回复、随后历史读取失败，旧 assistant 消息仍保持 `streaming: true` 导致 UI 永久显示生成中且按钮状态不一致的问题。
- 功能点 1：确认根因与对标行为。验证方式：CodeGraph 读取 `useChatStore.loadSession()` / `abortActiveRequestIfNeeded()` / `ChatComposer` / `ChatPage` streaming 状态来源，并对照 cc-gui 的会话切换边界。
- 功能点 2：补回归测试。验证方式：在 `src/stores/useChatStore.test.ts` 模拟 active turn + `chat_abort` 成功 + `get_unified_session_messages` 失败，先运行定向测试确认 RED。
- 功能点 3：实现最小修复。验证方式：当历史加载失败且本次切换已经中止过活跃 turn 时，保留当前 transcript，但把旧 streaming assistant 收口为 stopped/error 状态，避免 WebView 无限 loading。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/frontend/state-management.md` 与本文件最终记录。
- 功能点 5：质量验证。验证方式：运行定向 store 测试、相邻聊天交互测试、`npm test`、`npm run build`、Rust check/test（工作区已有 Rust 改动）和 `git diff --check`。

## 迭代记录 2026-06-19 load session failure after abort closes streaming turn

- 上轮实际进展：Prompt Enhancer 对比弹窗、Open File `%` 路径、permission 队列/失败恢复、composer pre-request 防重等此前迭代均已记录并通过自动化验证；阻塞仍是真实 Tauri 桌面点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的会话切换会在统一状态边界内结束旧 turn；当前项目 `loadSession()` 虽会先 `chat_abort`，但当 `get_unified_session_messages` 失败时只设置 `error` 和清 `pendingSessionKey`，会保留原 transcript。因为旧 assistant 仍是 `streaming: true`、`activeRequestId` 已被清空，WebView 可能同时呈现“还在生成”和“可继续操作”的矛盾状态。
- 本轮诊断（超越 cc-gui）：历史读取失败时保留旧 transcript 是合理恢复策略，但必须把已被主动中止的旧 assistant 收口为 stopped/error。这样用户既不会丢上下文，也不会误判 daemon 仍在为旧消息生成。
- 本轮规划：只修 `loadSession` 的“abort 后历史读取失败”分支。优先级中高：用户价值高（避免永久 loading 与按钮状态错乱）、出现频率低到中（历史文件损坏/移动/权限错误时触发）、实现成本低（store helper + 单测）、风险低（不改后端协议，不改成功加载路径）。
- 本轮完成：`src/stores/useChatStore.ts` 新增 `stopStreamingAssistantMessages()`，在 `loadSession()` 记录本次是否中止过活跃 turn；若后续历史读取失败，则保留旧 transcript，但把旧 streaming assistant 标记为 `streaming: false`、`error: 已停止输出` 并写入 `durationMs`。`src/stores/useChatStore.test.ts` 新增回归测试覆盖 active turn + abort 成功 + history load 失败。`.trellis/spec/frontend/state-management.md` 增补该状态边界契约。
- 本轮验证：先新增测试并确认 RED：`npm test -- src/stores/useChatStore.test.ts` 失败 1 项，实际旧 assistant 仍为 `streaming: true`。实现后验证通过：`npm test -- src/stores/useChatStore.test.ts`（1 file / 41 tests）；`npm test -- src/stores/useChatStore.test.ts src/components/chat/composer/ChatComposer.test.ts src/components/chat/composer/ButtonArea.test.tsx src/utils/chatUiBehavior.test.ts src/components/chat/chatSessionSidebarUtils.test.ts src/utils/chatNavigation.test.ts`（6 files / 93 tests）；`npm test`（28 files / 238 tests，仅 Browserslist/caniuse-lite 过期提示）；`npm run build`；`cargo check --manifest-path src-tauri/Cargo.toml`；`cargo test --manifest-path src-tauri/Cargo.toml`（102 tests，2 doctests ignored）；IDE `build_project` 针对 `src/stores/useChatStore.ts` 和 `src/stores/useChatStore.test.ts` 通过；`git diff --check` 退出 0，仅 LF/CRLF 转换提示。未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测加载历史失败场景：删除/移动历史文件后在流式回复中切换，确认旧消息停止、错误可见且可继续发送；2) 继续排查 daemon offline / abort failure 的真实 WebView 表现，尤其 `chat_abort` 失败时旧事件是否被退休且不会污染当前 transcript；3) 继续真实点测 Open File、历史图片 lightbox、diff 恢复按钮、长历史 Codex 锚点与会话快速切换。

## 本轮 PLAN 2026-06-19 daemon stdout close offline recovery

- 目标：修复 daemon 子进程 stdout 关闭后后端仍可能认为 daemon running、前端顶部状态仍显示 ready、下一次发送可能写到旧 stdin 的恢复问题。
- 功能点 1：确认根因与对标行为。验证方式：CodeGraph 读取 `DaemonClient.start()` reader loop、`ChatManager.send()` / `warm_up()`、前端 `chat://daemon` 状态消费，并对照 cc-gui `SessionState` busy/loading/error 边界。
- 功能点 2：补回归测试。验证方式：在 `src-tauri/src/chat/daemon_client.rs` 增加 stdout close 后必须标记 stopped、drain pending、emit shutdown 的 Rust 单测，先运行定向测试确认 RED。
- 功能点 3：实现最小修复。验证方式：reader loop 结束时统一收口 daemon 状态；`ChatManager` 获取已存在但 stopped 的 client 时重启并重新拉起 heartbeat。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/backend/cross-layer-protocol.md` 或相关 frontend state spec，并追加本文件最终记录。
- 功能点 5：质量验证。验证方式：运行定向 Rust 测试、Rust 全量测试、前端相关测试、`npm test`、`npm run build`、`cargo check` 和 `git diff --check`。

## 迭代记录 2026-06-19 daemon stdout close offline recovery

- 上轮实际进展：历史加载失败后 streaming assistant 收口已在 `useChatStore` 落地，定向 store 测试、相关前端测试、全量前端测试、构建、Rust check/test、IDE 构建与 `git diff --check` 均已通过；阻塞仍是真实 Tauri 桌面点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：对照 `jetbrains-cc-gui/src/main/java/com/github/claudecodegui/provider/common/DaemonBridge.java`，cc-gui 的 reader thread 在 stdout 结束后进入 `handleDaemonDeath()`，会用 `compareAndSet(true,false)` 关闭 running、失败 pending request、通知 lifecycle listener，并按限制重启。当前项目 `src-tauri/src/chat/daemon_client.rs` 旧 reader loop 只 drain pending，不会把 `running` 置 false，也不会 emit `chat://daemon shutdown`；`src-tauri/src/chat/manager.rs` 旧 `send()`/`warm_up()` 直接复用 cached client，下一次发送可能写到 stale stdin。
- 本轮诊断（超越 cc-gui）：本项目先不做后台无限自启，而是把异常 stdout close 明确变成 offline 状态，并在下一次用户触发的 `send()` / `warm_up()` 前重启。这样避免 daemon 崩溃后 UI 继续显示 ready，同时减少无用户操作时反复重启的噪声。
- 本轮规划：只修 daemon stdout close 恢复边界。优先级高：用户价值高（避免 ready 假象和下一次发送失败）、出现频率中（daemon crash、SDK 加载异常、stdout pipe 关闭都会触发）、实现成本低到中（Rust helper + manager restart guard）、风险可控（不改前端事件形状，不改 daemon JS 协议）。
- 本轮完成：`src-tauri/src/chat/daemon_client.rs` 将 `running` 改为 `Arc<AtomicBool>` 供 stdout reader 共享；新增 `handle_stdout_closed()`，stdout close 时清理 stdin/child、drain pending 为 `Done { success: false, error: "daemon exited" }`，并只在 `running` 从 true 切到 false 时 emit `shutdown`，避免主动 stop/restart 后重复 shutdown。新增两条 Rust 单测覆盖异常退出和 already stopped 不重复事件。`src-tauri/src/chat/manager.rs` 已通过 `running_client()` 让 `send()` / `warm_up()` 在 cached client 已停止时 restart 并重启 heartbeat；`abort()` 保持不主动拉起 daemon。`.trellis/spec/backend/cross-layer-protocol.md` 新增 `Chat Daemon Stdout-Close Recovery` 7 段契约。
- 本轮验证：接手前 RED 已确认：`cargo test --manifest-path src-tauri/Cargo.toml chat::daemon_client::tests::stdout_close_marks_stopped_drains_pending_and_emits_shutdown` 失败于缺少 `handle_stdout_closed`。本轮补充的重复 shutdown 边界实现后验证通过：`cargo test --manifest-path src-tauri/Cargo.toml chat::daemon_client::tests::stdout_close`（2 tests）；`npm test -- src/stores/useChatStore.test.ts`（41 tests）；`npm test`（28 files / 238 tests，仅 Browserslist/caniuse-lite 过期提示）；`cargo test --manifest-path src-tauri/Cargo.toml`（104 tests，2 doctests ignored）；`npm run build`；`cargo check --manifest-path src-tauri/Cargo.toml`；IDE `build_project` 针对 `src-tauri/src/chat/daemon_client.rs` 与 `src-tauri/src/chat/manager.rs` 通过。未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测 daemon offline 恢复：手动 kill daemon 后确认顶部状态变 shutdown、当前请求失败可见、下一次发送/预热可恢复；2) 为 `ChatManager::running_client()` 增加可注入 fake client 的 manager-level 回归，验证 stopped cached client 的 restart/heartbeat 行为；3) 继续真实点测历史加载失败、Open File、历史图片 lightbox、diff 恢复按钮、长历史 Codex 锚点与会话快速切换。

## 本轮 PLAN 2026-06-19 daemon offline manual reconnect

- 目标：把后端已能发出的 `chat://daemon shutdown` 状态接到前端可见、可恢复的 Chat UI 入口，避免用户只看到“启动中”却不知道如何恢复。
- 功能点 1：确认本项目与 cc-gui 对标行为。验证方式：CodeGraph 读取本项目 `useChatStore` / `ChatPage` / `StatusPanel`，并对照 cc-gui `DaemonBridge.handleDaemonDeath()` / `ClaudeSession.restart()`。
- 功能点 2：补回归测试。验证方式：在 `src/stores/useChatStore.test.ts` 覆盖 `reconnectDaemon()` 成功/失败状态；在 `src/components/chat/StatusPanel.test.tsx` 覆盖离线状态文本与恢复按钮。
- 功能点 3：实现最小恢复入口。验证方式：`useChatStore` 新增 `daemonReconnecting` 与 `reconnectDaemon()`，顶部与右侧状态面板在 daemon offline/shutdown/error 时显示低噪声恢复按钮；成功等待后端 `ready` 事件，失败保留错误。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/frontend/state-management.md` 与 `.trellis/spec/frontend/component-guidelines.md`，最后追加本轮记录。
- 功能点 5：质量验证。验证方式：运行定向 store/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check`；如 IDE 可用再执行相关文件构建。

## 迭代记录 2026-06-19 daemon offline manual reconnect

- 上轮实际进展：daemon stdout close 后端恢复已落地，异常 stdout close 会标记 stopped、drain pending、emit `chat://daemon shutdown`，`ChatManager.send()` / `warm_up()` 会在 cached client stopped 时重启；定向 Rust、前端 store、全量前端、Rust 全量、构建、IDE 构建与 `git diff --check` 均已通过。阻塞仍是真实 Tauri 桌面 kill daemon 点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：对照 `jetbrains-cc-gui/src/main/java/com/github/claudecodegui/provider/common/DaemonBridge.java`，cc-gui 在 reader 结束后进入 `handleDaemonDeath()`，会关闭 running、失败 pending request、通知 lifecycle listener 并尝试 restart；本项目后端已能发 `shutdown`，但 `src/pages/ChatPage.tsx` 和 `src/components/chat/StatusPanel.tsx` 只把 non-ready 显示成启动中，用户没有显式恢复入口。
- 本轮诊断（超越 cc-gui）：本项目不做静默无限自启，而是在 offline/error 时给出低噪声手动重连入口。这样用户能分辨“正常启动中”和“daemon 已离线”，也能在发送前主动恢复，避免下一次发送才暴露错误。
- 本轮规划：只补 daemon offline 前端恢复路径。优先级高：用户价值高（daemon crash 后可见可恢复）、出现频率中（stdout close / Node/SDK 异常会触发）、实现成本低（store action + 两个展示入口）、风险低（不改后端事件形状，不改 send 协议）。
- 本轮完成：`src/stores/useChatStore.ts` 新增 `daemonReconnecting` 和 `reconnectDaemon()`，调用 `chat_start_daemon`，成功后等待后端 `ready` 事件确认 ready，失败时保留 `error` 与 `daemonStatus='error'`；`chat://daemon ready/shutdown` 会清理 reconnecting。`src/utils/chatDaemonStatus.ts` 新增共享状态派生 helper，顶部和右侧状态面板统一识别 ready/starting/offline/error。`src/pages/ChatPage.tsx` 在 offline/error 时显示重连按钮；`src/components/chat/StatusPanel.tsx` 显示守护进程状态与 icon-only 重连按钮；`src/styles/toolBlocks.css` 补状态面板按钮样式；`src/locales/en.json` / `src/locales/zh.json` 补 i18n 文案。新增 `src/utils/chatDaemonStatus.test.ts`，并扩展 `src/stores/useChatStore.test.ts`、`src/components/chat/StatusPanel.test.tsx`。`.trellis/spec/frontend/state-management.md` 新增 7 段 daemon 手动恢复契约，`.trellis/spec/frontend/component-guidelines.md` 记录展示约束。新增文件已执行 `git add -- src/utils/chatDaemonStatus.ts src/utils/chatDaemonStatus.test.ts`。
- 本轮验证：先确认 RED：`npm test -- src/stores/useChatStore.test.ts` 失败于 `reconnectDaemon is not a function`；`npm test -- src/components/chat/StatusPanel.test.tsx` 失败于缺少 `status-daemon-reconnect`。实现后验证通过：`npm test -- src/utils/chatDaemonStatus.test.ts src/stores/useChatStore.test.ts src/components/chat/StatusPanel.test.tsx`（3 files / 54 tests）；`npm test`（29 files / 245 tests，仅 Browserslist/caniuse-lite 过期提示）；`npm run build`；`cargo check --manifest-path src-tauri/Cargo.toml`；`cargo test --manifest-path src-tauri/Cargo.toml`（104 tests，2 doctests ignored）；IDE `build_project` 针对 `useChatStore.ts`、`StatusPanel.tsx`、`ChatPage.tsx`、`chatDaemonStatus.ts` 通过；`git diff --check` 退出 0，仅 LF/CRLF 转换提示。未做真实 Tauri 桌面 kill daemon 点测。
- 下一步候选：1) 真实 Tauri 桌面点测 daemon offline 恢复：手动 kill daemon 后确认顶部/右侧显示离线，点击重连后 ready 事件恢复，重连失败时错误可见；2) 为 `ChatManager::running_client()` 增加 manager-level fake client 回归，验证 stopped cached client restart + heartbeat 行为；3) 继续真实点测历史加载失败、Open File、历史图片 lightbox、diff 恢复按钮、长历史 Codex 锚点与会话快速切换。

## 本轮 PLAN 2026-06-19 daemon init warmup failure recoverable

- 目标：修复 Chat 页面首次 `init()` 预热 daemon 失败后只设置 `error`、未进入 `daemonStatus='error'` 的状态缺口，避免 UI 长期显示“启动中”且缺少重连入口。
- 功能点 1：确认根因与对标行为。验证方式：CodeGraph 读取本项目 `useChatStore.init()` / `reconnectDaemon()`，并对照 cc-gui `DaemonBridge.handleDaemonDeath()` 的 daemon 死亡状态边界。
- 功能点 2：补回归测试。验证方式：在 `src/stores/useChatStore.test.ts` 模拟 `chat_start_daemon` 在 `init()` 阶段 reject，先运行定向测试确认 RED。
- 功能点 3：实现最小修复。验证方式：`init()` warm-up 失败时设置 `daemonReady: false`、`daemonStatus: 'error'`、`daemonReconnecting: false` 并保留 `String(error)`。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/frontend/state-management.md` 的 daemon 手动恢复契约，并在本文件追加最终记录。
- 功能点 5：质量验证。验证方式：运行定向 store/daemon 状态测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE 构建与 `git diff --check`。

## 迭代记录 2026-06-19 daemon init warmup failure recoverable

- 上轮实际进展：daemon stdout close 后端恢复与前端手动重连入口已落地，`chat://daemon shutdown` 可被顶部/右侧状态面板识别为 offline/error 并暴露重连按钮；定向测试、全量前端测试、构建、Rust check/test、IDE 构建与 `git diff --check` 均已通过。阻塞仍是真实 Tauri 桌面 kill daemon / 重连点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：对照 `jetbrains-cc-gui/src/main/java/com/github/claudecodegui/provider/common/DaemonBridge.java`，cc-gui 在 reader 结束或 daemon 死亡时会关闭 running、失败 pending request、通知 lifecycle listener 并尝试 restart。当前项目上一轮已补 shutdown 后的恢复入口，但 `src/stores/useChatStore.ts` 的首次 `init()` warm-up 失败只设置 `error`，没有把 `daemonStatus` 置为 `error`，因此 UI 仍可能按 starting 展示。
- 本轮诊断（超越 cc-gui）：本项目采用低噪声手动恢复而非静默无限自启，前提是所有 daemon 启动失败入口都进入同一个 recoverable state。首启失败也应该立即给用户“失败 + 可重连”的状态，而不是等用户下一次发送时才暴露。
- 本轮规划：只修 `init()` warm-up 失败分支。优先级高：用户价值高（首屏 daemon 启动失败是阻断性体验）、出现频率中（Node/SDK/路径问题常在首启暴露）、实现成本极低（store catch 分支 + 单测）、风险低（不改成功路径、不改 Tauri 命令协议）。
- 本轮完成：`src/stores/useChatStore.test.ts` 新增 `init()` warm-up 失败回归测试；`src/stores/useChatStore.ts` 在 `chat_start_daemon` 预热失败时设置 `daemonReady: false`、`daemonStatus: 'error'`、`daemonReconnecting: false` 和 `error: String(e)`；`.trellis/spec/frontend/state-management.md` 补充首启 warm-up 失败的 daemon recoverable error 契约。
- 本轮验证：先确认 RED：`npm test -- src/stores/useChatStore.test.ts` 失败于 `daemonStatus` 实际为 `null`、期望 `error`。实现后验证通过：`npm test -- src/stores/useChatStore.test.ts`（44 tests）；`npm test -- src/utils/chatDaemonStatus.test.ts src/stores/useChatStore.test.ts src/components/chat/StatusPanel.test.tsx`（3 files / 55 tests）；`npm test`（29 files / 246 tests，仅 Browserslist/caniuse-lite 过期提示）；`npm run build`；`cargo check --manifest-path src-tauri/Cargo.toml`；`cargo test --manifest-path src-tauri/Cargo.toml`（104 tests，2 doctests ignored）；IDE `build_project` 针对 `src/stores/useChatStore.ts` 与 `src/stores/useChatStore.test.ts` 通过；`git diff --check` 与 `git diff --cached --check` 退出 0，仅 LF/CRLF 转换提示。未做真实 Tauri 桌面首启失败/kill daemon 点测。
- 下一步候选：1) 真实 Tauri 桌面点测 daemon recoverable path：首启失败、手动 kill daemon、点击重连、重连失败时顶部/右侧状态是否一致；2) 为 `ChatManager::running_client()` 增加 manager-level fake client 回归，验证 stopped cached client restart + heartbeat 行为；3) 继续真实点测历史加载失败、Open File、历史图片 lightbox、diff 恢复按钮、长历史 Codex 锚点与会话快速切换。

## 本轮 PLAN 2026-06-19 daemon failure diagnostic visibility

- 目标：把 store 已保存的 daemon 启动/重连失败原因以低噪声方式展示在 Chat 状态面板与顶部状态 tooltip 中，避免用户只看到泛化“错误/离线”而不知道恢复动作失败的具体原因。
- 功能点 1：确认差距与对标行为。验证方式：CodeGraph 读取 `chatDaemonStatus`、`StatusPanel`、`ChatPage`，并对照 cc-gui `DaemonBridge.handleDaemonDeath()` lifecycle 行为。
- 功能点 2：补回归测试。验证方式：在 `src/utils/chatDaemonStatus.test.ts` 覆盖诊断文本优先级、空白压缩、长度截断与 ready/starting 隐藏；在 `src/components/chat/StatusPanel.test.tsx` 覆盖 daemon error 诊断与重连按钮同屏出现，先运行定向测试确认 RED。
- 功能点 3：实现最小展示。验证方式：新增共享 `getChatDaemonDiagnosticText()`，`StatusPanel` 接收并渲染 compact 诊断，`ChatPage` 将 store `error` 传入面板并把顶部状态 `title` 设为诊断/状态文本。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md`，最后追加本轮记录。
- 功能点 5：质量验证。验证方式：运行定向 helper/status/store 测试、`npm test`、`npm run build`、Rust check/test、IDE 构建与 `git diff --check`。

## 迭代记录 2026-06-19 daemon failure diagnostic visibility

- 上轮实际进展：daemon stdout close 后端恢复、前端手动重连入口、首启 warm-up 失败进入 recoverable error 已落地并通过自动化验证；阻塞仍是真实 Tauri 桌面首启失败 / kill daemon / 重连点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：对照 `jetbrains-cc-gui/src/main/java/com/github/claudecodegui/provider/common/DaemonBridge.java`，cc-gui daemon 死亡会进入 lifecycle listener，并在日志/请求错误里保留原因。本项目已有 offline/error 与重连按钮，但 `src/components/chat/StatusPanel.tsx` 只显示泛化 `chat.daemon.error` / `chat.daemon.offline`，具体 `error` 只留在 store/page alert，右侧恢复入口附近看不到原因。
- 本轮诊断（超越 cc-gui）：本项目采用用户可控的手动恢复路径，恢复按钮旁边应显示一行清洗后的失败原因；顶部状态保持克制，只在 tooltip 中复用同一诊断，避免主工具栏变吵。
- 本轮规划：只补 daemon 失败诊断展示。优先级中高：用户价值高（Node/SDK/path/权限失败时能直接知道原因）、出现频率中（首启/重连失败常见）、实现成本低（纯 helper + 两处透传）、风险低（不改 daemon 协议，不新增 i18n 文案）。
- 本轮完成：`src/utils/chatDaemonStatus.ts` 新增 `getChatDaemonDiagnosticText()`，在 ready/starting/reconnecting 时隐藏诊断，offline/error 时优先使用 store `error`，再使用非泛化 `daemonStatus`，并压缩空白、140 字符截断。`src/components/chat/StatusPanel.tsx` 新增 `daemonError` prop，失败时在 daemon 行下方渲染 `status-daemon-diagnostic`，同时保留重连按钮。`src/pages/ChatPage.tsx` 传入 store `error`，并把顶部 daemon 状态容器 `title` 设置为同一诊断/状态文本。`src/utils/chatDaemonStatus.test.ts` 和 `src/components/chat/StatusPanel.test.tsx` 增加回归测试。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 同步记录诊断展示契约。
- 本轮验证：先确认 RED：`npm test -- src/utils/chatDaemonStatus.test.ts src/components/chat/StatusPanel.test.tsx` 失败于 `getChatDaemonDiagnosticText is not a function` 与缺少 `status-daemon-diagnostic`。实现后验证通过：`npm test -- src/utils/chatDaemonStatus.test.ts src/components/chat/StatusPanel.test.tsx`（2 files / 16 tests）；`npm test -- src/utils/chatDaemonStatus.test.ts src/components/chat/StatusPanel.test.tsx src/stores/useChatStore.test.ts`（3 files / 60 tests）；`npm test`（29 files / 251 tests，仅 Browserslist/caniuse-lite 过期提示）；`npm run build`；`cargo check --manifest-path src-tauri/Cargo.toml`；`cargo test --manifest-path src-tauri/Cargo.toml`（104 tests，2 doctests ignored）；IDE `build_project` 针对本轮 5 个 TS/TSX 文件通过；`git diff --check` 与 `git diff --cached --check` 退出 0，未发现空白错误，仅 `git diff --check` 输出既有 LF/CRLF 转换提示。未做真实 Tauri 桌面点测。
- 下一步候选：1) 真实 Tauri 桌面点测 daemon recoverable path：首启失败、手动 kill daemon、点击重连失败/成功时，顶部 tooltip、右侧诊断、page alert 是否一致；2) 为 `ChatManager::running_client()` 增加 manager-level fake client 回归，验证 stopped cached client restart + heartbeat 行为；3) 继续真实点测历史加载失败、Open File、历史图片 lightbox、diff 恢复按钮、长历史 Codex 锚点与会话快速切换。

## 本轮 PLAN 2026-06-19 daemon manager stopped-client regression

- 目标：补齐 `ChatManager::running_client()` 的 manager-level 回归保护，锁住 cached daemon stopped 后 `send()` / `warm_up()` 必须先 restart、并重新拉起 heartbeat 的行为。
- 功能点 1：确认差距与对标行为。验证方式：CodeGraph 读取本项目 `ChatManager` / `DaemonClient` 以及 cc-gui `DaemonBridge.handleDaemonDeath()`；区分当前代码已实现与测试未覆盖。
- 功能点 2：补 RED 测试。验证方式：在 `src-tauri/src/chat/manager.rs` 增加 fake daemon client 测试，先运行定向 Rust 测试确认缺少测试 seam 或行为失败。
- 功能点 3：实现最小测试 seam。验证方式：生产路径仍创建真实 `DaemonClient`；测试路径通过内部 trait/fake 覆盖 stopped cached client restart、running cached client 不 restart、restart 失败不 spawn heartbeat。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/backend/cross-layer-protocol.md` 的测试契约，并在本文件追加最终记录。
- 功能点 5：质量验证。验证方式：运行定向 Rust 测试、`cargo test --manifest-path src-tauri/Cargo.toml`、`cargo check --manifest-path src-tauri/Cargo.toml`、前端全量测试/构建，以及 `git diff --check`。

## 迭代记录 2026-06-19 daemon manager stopped-client regression

- 上轮实际进展：daemon stdout close 后端恢复、前端手动重连入口、首启 warm-up 失败可恢复、失败诊断可见性均已落地并通过自动化验证；阻塞仍是真实 Tauri 桌面首启失败 / kill daemon / 重连点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：对照 `jetbrains-cc-gui/src/main/java/com/github/claudecodegui/provider/common/DaemonBridge.java`，cc-gui 的 `handleDaemonDeath()` 会关闭 running、失败 pending request、通知 lifecycle listener 并在限制内尝试 restart。当前项目已有 `ChatManager::running_client()` 让 `send()` / `warm_up()` 在 cached client stopped 时 restart，但 CodeGraph 显示该 manager 决策边界没有测试覆盖。
- 本轮诊断（超越 cc-gui）：本项目继续保持“用户触发前恢复”的低噪声策略，不做后台无限自启；本轮通过 fake daemon client 锁住 manager 判断，而不是启动真实 Node daemon，减少测试 flake 与环境依赖。
- 本轮规划：只补 manager-level 回归保护。优先级中高：用户价值高（避免 daemon crash 后下一次发送写 stale stdin）、出现频率中（daemon stdout close / restart / SDK 异常路径都会触发）、实现成本低到中（内部 trait seam + 单文件单测）、风险可控（不改 Tauri 命令协议、不改前端状态）。
- 本轮完成：`src-tauri/src/chat/manager.rs` 新增内部 `ManagerDaemonClient` trait 与 future type alias，让生产 `DaemonClient` 和测试 fake client 走同一条 manager 决策路径；`ChatManager` 仍在生产路径懒创建真实 daemon client，`send()` / `warm_up()` 继续通过 `running_client()`，`abort()` 仍不主动 restart。新增三条 Rust 单测覆盖 stopped cached client restart + heartbeat、running cached client 复用、restart 失败不 spawn heartbeat。`.trellis/spec/backend/cross-layer-protocol.md` 在 `Chat Daemon Stdout-Close Recovery` 的 Tests Required 中补充 fake-client manager regression 契约。
- 本轮验证：先确认 RED：`cargo test --manifest-path src-tauri/Cargo.toml chat::manager::tests::running_client_restarts_stopped_cached_client_and_spawns_heartbeat` 失败于缺少 `ManagerDaemonClient` / `ClientFuture` / `ClientResultFuture` / `ensure_running_client_with_heartbeat`。实现后验证通过：`cargo test --manifest-path src-tauri/Cargo.toml chat::manager::tests::running_client`（3 tests）；`cargo test --manifest-path src-tauri/Cargo.toml`（107 tests，2 doctests ignored）；`npm test`（29 files / 251 tests，仅 Browserslist/caniuse-lite 过期提示）；`npm run build`；`cargo check --manifest-path src-tauri/Cargo.toml`；IDE `build_project` 针对 `src-tauri/src/chat/manager.rs` 通过；`git diff --check` 与 `git diff --cached --check` 退出 0，前者仅输出既有 LF/CRLF 转换提示。未做真实 Tauri 桌面 kill daemon / 重连点测。
- 下一步候选：1) 真实 Tauri 桌面点测 daemon recoverable path：首启失败、手动 kill daemon、点击重连失败/成功时，顶部 tooltip、右侧诊断、page alert 是否一致；2) 继续真实点测历史加载失败、Open File、历史图片 lightbox、diff 恢复按钮、长历史 Codex 锚点与会话快速切换；3) 若桌面点测发现 daemon 重启耗时较长，再评估顶部状态增加短期“重启中/等待 ready”细分反馈。

## 本轮 PLAN 2026-06-19 daemon reconnect waits for ready

- 目标：修复 `reconnectDaemon()` 在 `chat_start_daemon` resolve 后过早清除 `daemonReconnecting` 的状态空窗，确保手动重连一直保持 pending，直到后端 `ready` / `shutdown` 事件或前端等待超时收口。
- 功能点 1：确认差距与对标行为。验证方式：CodeGraph 读取本项目 `useChatStore.reconnectDaemon()` / daemon lifecycle listener，并对照 cc-gui `DaemonBridge.start()` ready latch / timeout 行为。
- 功能点 2：补 RED 回归测试。验证方式：在 `src/stores/useChatStore.test.ts` 覆盖 `chat_start_daemon` resolve 后仍保持 reconnecting、`ready` 事件清除 pending、`shutdown` 事件清除 pending、等待 ready 超时进入 recoverable error。
- 功能点 3：实现最小等待 ready 状态机。验证方式：`reconnectDaemon()` success 不再清 pending；新增模块级 ready timeout helper；`ready` / `shutdown` / reconnect failure 清理 timeout；timeout 仅在仍处于 reconnecting + starting + not ready 时写入 error。
- 功能点 4：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/state-management.md` 的 daemon manual recovery 契约、验证矩阵和 Tests Required。
- 功能点 5：质量验证。验证方式：运行定向 store/daemon 状态测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE 构建与 `git diff --check` / `git diff --cached --check`。

## 迭代记录 2026-06-19 daemon reconnect waits for ready

- 上轮实际进展：`ChatManager::running_client()` manager-level fake client 回归已落地，覆盖 stopped cached client restart + heartbeat、running client 复用、restart failure 不启动 heartbeat；Rust 全量、前端全量、构建、IDE 构建与空白检查均已通过。阻塞仍是真实 Tauri 桌面 kill daemon / 重连点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：对照 `jetbrains-cc-gui/src/main/java/com/github/claudecodegui/provider/common/DaemonBridge.java`，cc-gui `start()` 会等待 ready latch，并在进程提前退出或 ready timeout 时保持失败语义；当前项目 `src/stores/useChatStore.ts` 的 `reconnectDaemon()` 在 `chat_start_daemon` resolve 后立即清除 `daemonReconnecting`，导致“启动命令成功但 ready 事件没到”的空窗没有 pending 反馈和超时收口。
- 本轮诊断（超越 cc-gui）：本项目继续保留用户可控的手动重连策略，但把“命令已发起”和“daemon 已 ready”拆成两个状态。手动重连期间 UI 一直保持 starting/reconnecting，直到 backend `ready` / `shutdown` 或前端 ready timeout 给出明确结果，避免用户误以为恢复已经完成。
- 本轮规划：只修前端 store 的重连等待 ready 边界。优先级高：用户价值高（daemon 恢复失败不会卡在无反馈 starting）、出现频率中（Node/SDK/daemon 启动慢或 ready 事件丢失时出现）、实现成本低（store timer + 单测 + i18n key）、风险低（不改 Tauri 命令和后端协议）。
- 本轮完成：`src/stores/useChatStore.ts` 新增 ready timeout helper，`reconnectDaemon()` 在 `chat_start_daemon` success 后保持 `daemonReconnecting: true` 并等待 `ready` / `shutdown` / timeout；`ready` 和 `shutdown` 事件都会清理 timeout；timeout 只在仍处于 reconnecting + starting + not ready 时转入 recoverable error。`src/stores/useChatStore.test.ts` 增加三条回归测试覆盖 success-before-ready、shutdown-before-ready、ready timeout。`src/utils/chatDaemonStatus.ts` 新增 timeout 诊断 key；`src/pages/ChatPage.tsx` 与 `src/components/chat/StatusPanel.tsx` 在展示边界翻译该 key，普通后端错误仍原样显示。`src/locales/en.json` / `src/locales/zh.json` 增加 timeout 文案。`.trellis/spec/frontend/state-management.md` 和 `.trellis/spec/frontend/component-guidelines.md` 同步更新契约。
- 本轮验证：RED：`npm test -- src/stores/useChatStore.test.ts` 先失败 3 项，均证明旧实现会在 `chat_start_daemon` resolve 后提前把 `daemonReconnecting` 置为 false。GREEN：`npm test -- src/stores/useChatStore.test.ts` 通过（46 tests）；`npm test -- src/stores/useChatStore.test.ts src/utils/chatDaemonStatus.test.ts src/components/chat/StatusPanel.test.tsx` 通过（3 files / 62 tests）；`npm test` 通过（29 files / 253 tests，仅 Browserslist/caniuse-lite 过期提示）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（107 tests，2 doctests ignored）；IDE `build_project` 针对 `useChatStore.ts`、`useChatStore.test.ts`、`chatDaemonStatus.ts`、`StatusPanel.tsx`、`ChatPage.tsx` 通过；`git diff --check` 与 `git diff --cached --check` 退出 0，前者仅输出既有 LF/CRLF 转换提示。未做真实 Tauri 桌面 daemon ready timeout / kill daemon / 重连点测。
- 下一步候选：1) 真实 Tauri 桌面点测 daemon recoverable path：首启失败、手动 kill daemon、点击重连成功、点击重连后 ready 长时间不到时顶部 tooltip / 右侧诊断 / page alert 是否一致；2) 继续真实点测历史加载失败、Open File、历史图片 lightbox、diff 恢复按钮、长历史 Codex 锚点与会话快速切换；3) 若真实点测发现 daemon ready 时间常超过 15s，再结合日志调整 ready timeout 或把“等待 ready”状态细分成更明确的长等待提示。

## 本轮 PLAN 2026-06-19 daemon init waits for ready

- 目标：修复 Chat 页面首次 `init()` warm-up 在 `chat_start_daemon` resolve 后未收到 `ready` 事件时长期停留在启动中的状态缺口，确保首屏 daemon 预热也有 bounded ready wait。
- 功能点 1：确认差距与对标行为。验证方式：CodeGraph 读取本项目 `useChatStore.init()` / daemon listener / ready timeout helper，并对照 cc-gui `DaemonBridge.start()` ready latch / timeout。
- 功能点 2：补 RED 回归测试。验证方式：在 `src/stores/useChatStore.test.ts` 模拟 `init()` warm-up resolve 但没有 `chat://daemon ready`，用 fake timer 断言进入 recoverable error。
- 功能点 3：实现最小修复。验证方式：复用 ready timeout helper，让 `init()` warm-up success 后在未 ready 时启动 timeout；如果 ready 已经先到或 shutdown 到来，则不再误触发 timeout。
- 功能点 4：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/state-management.md` 的 daemon manual recovery 契约和 Tests Required。
- 功能点 5：质量验证。验证方式：运行定向 store/daemon 状态测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE 构建与 `git diff --check` / `git diff --cached --check`。

## 迭代记录 2026-06-19 daemon init waits for ready

- 上轮实际进展：手动 `reconnectDaemon()` 已改为等待 backend `ready` / `shutdown` / ready-timeout，而不是在 `chat_start_daemon` resolve 后提前结束 pending；前端全量、构建、Rust check/test、IDE 构建和空白检查均已通过。阻塞仍是真实 Tauri 桌面 daemon ready timeout / kill daemon / 重连点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：对照 `jetbrains-cc-gui/src/main/java/com/github/claudecodegui/provider/common/DaemonBridge.java`，cc-gui `start()` 会阻塞等待 ready 或 timeout；当前项目上一轮只补了手动重连路径，`src/stores/useChatStore.ts` 的首次 `init()` warm-up 在 `chat_start_daemon` resolve 且没有 ready 事件时仍可能保持 `daemonStatus: null/starting`，没有 timeout 收口。
- 本轮诊断（超越 cc-gui）：首屏 warm-up 与手动重连应共享同一条 bounded ready wait。用户首次进入 Chat 时，如果 daemon 没有真正 ready，应在短时间后给出可恢复错误与诊断，而不是无限展示启动中，让用户误以为只是正常预热。
- 本轮规划：只修 `init()` warm-up success-without-ready。优先级高：用户价值高（首屏阻断状态可见且可恢复）、出现频率中（Node/SDK/daemon 启动慢或 ready 事件丢失时出现）、实现成本低（复用上一轮 helper + 单测）、风险低（不改后端协议，不新增 UI 文案）。
- 本轮完成：`src/stores/useChatStore.test.ts` 新增 `init()` warm-up success 后 ready 缺失的 fake-timer 回归测试；`src/stores/useChatStore.ts` 在 `init()` 开始时显式进入 `daemonStatus: 'starting'`，并在 warm-up success 后如果仍未 ready 就调度同一个 ready timeout；ready/shutdown 事件继续清理 timeout。ready timeout helper 的触发条件从“必须正在手动 reconnect”放宽为“已调度、未 ready、仍为 starting”，因此首屏 warm-up 和手动重连共用收口逻辑。`.trellis/spec/frontend/state-management.md` 同步记录 init warm-up bounded ready wait 契约和测试要求。
- 本轮验证：RED：`npm test -- src/stores/useChatStore.test.ts` 先失败 1 项，证明 `init()` success 后 `daemonStatus` 仍为 `null` 且没有 timeout 进入 recoverable error。GREEN：`npm test -- src/stores/useChatStore.test.ts` 通过（47 tests）；`npm test -- src/stores/useChatStore.test.ts src/utils/chatDaemonStatus.test.ts src/components/chat/StatusPanel.test.tsx` 通过（3 files / 63 tests）；`npm test` 通过（29 files / 254 tests，仅 Browserslist/caniuse-lite 过期提示）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（107 tests，2 doctests ignored）；IDE `build_project` 针对 `useChatStore.ts`、`useChatStore.test.ts` 通过；`git diff --check` 与 `git diff --cached --check` 退出 0，前者仅输出既有 LF/CRLF 转换提示。未做真实 Tauri 桌面首屏 ready timeout / kill daemon / 重连点测。
- 下一步候选：1) 真实 Tauri 桌面点测 daemon recoverable path：首屏 ready 缺失/启动失败、手动 kill daemon、点击重连成功/失败，确认顶部 tooltip、右侧诊断、page alert 与按钮状态一致；2) 继续真实点测历史加载失败、Open File、历史图片 lightbox、diff 恢复按钮、长历史 Codex 锚点与会话快速切换；3) 如果真实点测发现首屏 ready 经常超过 15s，再结合 daemon 日志把 timeout 调整为后端可观测的启动 SLA 或增加更明确的长等待提示。

## 本轮 PLAN 2026-06-19 image lightbox visible context

- 目标：让 Chat 图片/历史图片全屏预览在大图之外保留可见文件名/标签上下文，避免用户检查多张截图或附件时丢失来源。
- 功能点 1：确认差距与对标行为。验证方式：CodeGraph 读取本项目 `ContentBlockRenderer` 图片 lightbox 与 cc-gui `ContentBlockRenderer` image preview，确认本项目已有 portal lightbox、但没有可见 caption。
- 功能点 2：补 RED 测试。验证方式：新增/补充 `ContentBlockRenderer` 测试，点击图片后断言 dialog 内出现可见 caption；先运行定向测试确认失败。
- 功能点 3：实现最小展示层增强。验证方式：在 portal lightbox 底部渲染当前图片 label，沿用已有 `ImageRenderData.label`，不新增后端字段或新依赖。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 的图片预览契约，并在本文件追加最终记录。
- 功能点 5：质量验证。验证方式：运行定向图片渲染测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE 构建与 `git diff --check`。

## 迭代记录 2026-06-19 image lightbox visible context

- 上轮实际进展：首屏 `init()` daemon warm-up 已复用 bounded ready wait，`chat_start_daemon` resolve 后若没有 `ready` 事件会在 timeout 后进入可恢复错误；前端全量、构建、Rust check/test、IDE 构建和空白检查均已通过。阻塞仍是真实 Tauri 桌面 daemon 首屏失败 / kill daemon / 重连点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：对照 `jetbrains-cc-gui/webview/src/components/MessageItem/ContentBlockRenderer.tsx`，cc-gui 的图片块可点击进入全屏预览。本项目 `src/components/chat/ContentBlockRenderer.tsx` 已经有 React portal lightbox、Escape 关闭和缩略图入口，因此“图片可放大”能力已补齐。
- 本轮诊断（超越 cc-gui）：本项目的 lightbox 只显示大图，文件名/标签只存在于 `alt` / `aria-label` / 缩略图隐藏 caption 中。用户连续检查多张历史截图或附件时，进入全屏后会丢失来源上下文，尤其是文件名相近或带 `%`、截图编号的场景。
- 本轮规划：只做图片 lightbox 可见上下文。优先级中等：用户价值高（历史图片回看和多图比对不会丢来源）、出现频率中（截图/附件越来越常见）、实现成本低（同文件小组件 + 单测）、风险低（不改图片解析、不改后端、不新增 i18n 文案）。
- 本轮完成：`src/components/chat/ContentBlockRenderer.tsx` 导出 `ImageLightbox` 小组件，portal 改为复用该组件；lightbox 底部新增 `chat-image-lightbox-caption`，展示当前 `ImageRenderData.label`，并保留 `title` 以便长文件名查看完整值；关闭按钮补充 `stopPropagation()`，避免按钮点击同时冒泡到遮罩。`src/components/chat/ContentBlockRenderer.test.tsx` 新增 lightbox caption 回归测试。`.trellis/spec/frontend/component-guidelines.md` 记录图片全屏预览必须保留可见文件名/标签上下文。
- 本轮验证：RED：`npm test -- src/components/chat/ContentBlockRenderer.test.tsx` 先失败 1 项，原因是 `ImageLightbox` 尚未导出，证明新增断言未被旧实现满足。GREEN：`npm test -- src/components/chat/ContentBlockRenderer.test.tsx` 通过（9 tests）；`npm test -- src/components/chat/ContentBlockRenderer.test.tsx src/components/chat/MessageMeta.test.tsx src/utils/chatNavigation.test.ts src/utils/chatImageBlocks.test.ts` 通过（3 files / 24 tests；其中 `chatImageBlocks.test.ts` 当前未匹配到测试文件）；`npm test` 通过（29 files / 255 tests，仅 Browserslist/caniuse-lite 过期提示）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（107 tests，2 doctests ignored）；IDE `build_project` 针对 `ContentBlockRenderer.tsx` / `ContentBlockRenderer.test.tsx` 通过；`git diff --check` 与 `git diff --cached --check` 退出 0，仅输出既有 LF/CRLF 转换提示。未做真实 Tauri 桌面点击图片 lightbox 点测。
- 下一步候选：1) 真实 Tauri 桌面点测图片 lightbox：历史图片、本轮上传图片、长文件名、Escape/遮罩/关闭按钮关闭路径，确认 caption 不遮挡主体；2) 真实 Tauri 桌面点测 daemon recoverable path：首屏 ready 缺失/启动失败、手动 kill daemon、重连成功/失败；3) 继续补齐高价值 Chat UI：diff 恢复按钮的真实文件回滚路径、Open File 真实点击、长历史 Codex 锚点与快速会话切换。

## 本轮 PLAN 2026-06-19 diff pane copy path action

- 目标：给中心 `ChatDiffReviewPane` 增加 Copy Path 快捷操作，让用户在审阅文件 diff 时能直接复制当前文件路径，而不必回到工具块详情或手选路径文本。
- 功能点 1：确认差距与对标行为。验证方式：CodeGraph 读取 `ChatDiffReviewPane`、`ReadToolBlock`、`EditToolBlock` 与 cc-gui 文件操作/工具块，确认本项目 diff 面板已有 Open File 但缺少 Copy Path。
- 功能点 2：补 RED 测试。验证方式：在 `ChatDiffReviewPane.test.tsx` 断言选中 edit 时渲染 `chat-diff-review-copy` 和既有 `tools.copyPath` 文案/标题，先运行定向测试确认失败。
- 功能点 3：实现最小展示层增强。验证方式：`ChatDiffReviewPane` 复用 `copyToClipboard(edit.openPath || edit.displayPath)`，按钮复制后短暂显示 `tools.copied`；空状态禁用该按钮。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 的 diff 面板局部操作契约，并在本文件追加最终记录。
- 功能点 5：质量验证。验证方式：运行定向 diff 面板测试、相邻 tool block/bridge 测试、`npm test`、`npm run build`、Rust check/test（工作区已有 Rust 改动）和 `git diff --check`。

## 迭代记录 2026-06-19 diff pane copy path action

- 上轮实际进展：图片 lightbox 已在 `src/components/chat/ContentBlockRenderer.tsx` 补充可见 caption，并通过定向测试、前端全量测试、构建、Rust check/test、IDE 构建和空白检查；阻塞仍是真实 Tauri 桌面图片点击路径未点测。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：本项目中心 `src/components/chat/ChatDiffReviewPane.tsx` 已有 Open File、视图模式切换、换行切换和折叠入口，但缺少 Copy Path；同项目 `src/components/toolBlocks/ReadToolBlock.tsx` / `src/components/toolBlocks/EditToolBlock.tsx` 已有复制路径能力，因此用户在中心 diff 审阅流里需要回到工具块详情或手选路径文本，操作不一致。
- 本轮诊断（超越 cc-gui）：把文件级低成本操作直接放在中心 diff 面板，能减少审阅时的上下文切换。用户在比对变更、记录问题或把路径贴给外部工具时，可以在当前视觉焦点内完成复制，不必离开 diff。
- 本轮规划：只补 Copy Path，不做 diff restore/revert。优先级中高：用户价值高（代码审阅中复制路径频繁）、出现频率中高（每次查看 diff 都可能用到）、实现成本低（复用 `copyToClipboard` 和既有 i18n key）、风险低（不改后端、不改 diff 数据结构）。restore/revert 涉及跨层写文件、确认与回滚安全，保留为后续独立设计。
- 本轮完成：`src/components/chat/ChatDiffReviewPane.tsx` 新增 Copy Path icon button、复制成功态和卸载清理 timer，复制目标使用 `edit.openPath || edit.displayPath`；`src/components/chat/ChatDiffReviewPane.test.tsx` 新增 SSR 回归，确保选中 diff 文件时渲染复制路径入口；`src/styles/toolBlocks.css` 将复制按钮纳入中心 diff 面板操作样式，并补充 hover/focus/disabled 与 copied 成功态；`.trellis/spec/frontend/component-guidelines.md` 记录中心 diff 面板应同时暴露 Open File 和 Copy Path，且继续复用桥接 helper 与 i18n key。
- 本轮验证：RED：`npm test -- src/components/chat/ChatDiffReviewPane.test.tsx` 先失败于缺少 `chat-diff-review-copy`。GREEN：`npm test -- src/components/chat/ChatDiffReviewPane.test.tsx` 通过（6 tests）；`npm test -- src/components/chat/ChatDiffReviewPane.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/utils/bridge.test.ts` 通过（3 files / 15 tests）；IDE `build_project` 针对 `src/components/chat/ChatDiffReviewPane.tsx`、`src/components/chat/ChatDiffReviewPane.test.tsx`、`src/styles/toolBlocks.css` 通过；`npm test` 通过（29 files / 256 tests，仅 Browserslist/caniuse-lite 过期提示）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（107 tests，2 doctests ignored）；`git diff --check` 与 `git diff --cached --check` 退出 0，仅输出既有 LF/CRLF 转换提示。未做真实 Tauri 桌面 WebView 点击复制点测。
- 下一步候选：1) 真实 Tauri 桌面点测中心 diff Copy Path：点击后剪贴板内容是否为预期路径、成功态是否显示并自动恢复；2) 真实 Tauri 桌面点测图片 lightbox：历史图片、本轮上传图片、长文件名、Escape/遮罩/关闭按钮关闭路径；3) 真实 Tauri 桌面点测 daemon recoverable path：首屏 ready 缺失/启动失败、手动 kill daemon、重连成功/失败；4) 若继续做 diff restore/revert，需要先设计确认跨层写文件、旧内容提取、确认弹窗和失败回滚策略。

## 本轮 PLAN 2026-06-19 chat mcp availability status

- 目标：在 Chat 右侧状态面板展示当前 provider 的 MCP 配置可用性摘要，让用户发送前能看到 MCP 基础状态，而不是必须切到 MCP 页面检查。
- 功能点 1：确认当前进度与差距。验证方式：CodeGraph 读取 `StatusPanel`、`ChatPage`、`useMcpStoreV2`、`McpPage` 与后端 `check_mcp_status`，区分“配置启用可见性”和“真实连通性检测”。
- 功能点 2：补 RED 测试。验证方式：在 `StatusPanel.test.tsx` 断言传入 MCP 摘要时渲染 `status-mcp-summary`、当前 provider 可用数量和 loading/error 诊断；先运行定向测试确认旧 UI 不满足。
- 功能点 3：实现最小展示层增强。验证方式：新增共享 MCP 摘要 helper，`ChatPage` 复用 `useMcpStoreV2` 加载服务器列表并按当前 provider 派生 enabled/total/error/loading，`StatusPanel` 渲染 compact 摘要。
- 功能点 4：同步 i18n 与 Trellis 规范。验证方式：新增 `en.json` / `zh.json` 文案，更新 `.trellis/spec/frontend/component-guidelines.md` 和 `.trellis/spec/frontend/state-management.md`。
- 功能点 5：质量验证。验证方式：运行 MCP 摘要 helper / StatusPanel / ChatPage 邻近测试、`npm test`、`npm run build`、Rust check/test（工作区已有 Rust 改动）和 `git diff --check`。

## 迭代记录 2026-06-19 chat mcp availability status

- 上轮实际进展：中心 diff 面板 Copy Path 已落地并通过定向测试、前端全量测试、构建、Rust check/test、IDE 构建和空白检查；阻塞仍是真实 Tauri 桌面点击复制、图片 lightbox、daemon recoverable path 点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 当前任务进度判断：`06-16-toolblocks` 的工具块基础能力已基本完成，后续真实差距集中在 Chat UI 的“发送前可判断性”和“运行时基础信息可见性”。已有状态面板能显示 provider、消息数、锚点、daemon、pending/failed tools、当前工具和编辑摘要；缺口是 MCP 配置可用性、工具/agent/MCP 实时连通性、模型/权限模式/工作区等基础上下文还不够集中可见。
- 本轮诊断（补齐 cc-gui）：cc-gui 的工具/daemon 状态更强调运行过程可见；本项目已有 MCP 管理页和 `useMcpStoreV2`，也有后端 `check_mcp_status`，但 Chat 页没有展示当前 provider 对应的 MCP 基础可用数。用户发送前无法判断当前 provider 到底启用了几个 MCP server。
- 本轮诊断（超越 cc-gui）：本轮先展示“配置可用性”，不自动触发真实连通性检测。原因是后端 `check_mcp_status` 对 stdio MCP 会实际启动命令，放在 Chat 首屏自动轮询会有噪声和风险。先把当前 provider 的 enabled/total、加载状态和加载错误放到右侧状态面板，能解决最高频判断问题，同时为下一轮手动连通性检测留出清晰入口。
- 本轮规划：只做 Chat 状态面板 MCP 配置摘要。优先级高：用户价值高（发送前知道 MCP 是否对当前 provider 可用）、出现频率高（每次切 provider / 进入 Chat 都相关）、实现成本低（复用已有 store 和配置开关）、风险低（不改后端、不启动 MCP 进程、不改协议）。
- 本轮完成：`src/utils/chatMcpStatus.ts` 新增 `buildChatMcpAvailabilitySummary()` 和 provider enablement 映射，按 `enabledClaude` / `enabledCodex` / `enabledGemini` 计算当前 provider 的 MCP 可用数，并清洗加载错误；`src/utils/chatMcpStatus.test.ts` 覆盖 provider 映射、摘要计算、错误压缩截断。`src/pages/ChatPage.tsx` 接入 `useMcpStoreV2`，进入 Chat 时加载 MCP server 列表并把摘要传入状态面板。`src/components/chat/StatusPanel.tsx` 新增 `status-mcp-summary` compact 行，展示 `enabled / total`、loading spinner 和错误诊断；`src/components/chat/StatusPanel.test.tsx` 新增 MCP 可用性与 loading/error 断言。`src/locales/en.json` / `src/locales/zh.json` 新增 MCP 状态面板文案。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 记录“Chat 中展示的是 MCP 配置可用性，不是实时连通性”的契约。新增文件已执行 `git add -- src/utils/chatMcpStatus.ts src/utils/chatMcpStatus.test.ts`。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，证明旧 UI 缺少 `status-mcp-summary`。GREEN：`npm test -- src/utils/chatMcpStatus.test.ts src/components/chat/StatusPanel.test.tsx` 通过（2 files / 13 tests）；`npm test -- src/utils/chatMcpStatus.test.ts src/components/chat/StatusPanel.test.tsx src/utils/chatDaemonStatus.test.ts` 通过（3 files / 21 tests）；`npm test` 通过（30 files / 261 tests，仅 Browserslist/caniuse-lite 过期提示）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（107 tests，2 doctests ignored）；IDE `build_project` 针对 `chatMcpStatus.ts`、`chatMcpStatus.test.ts`、`StatusPanel.tsx`、`StatusPanel.test.tsx`、`ChatPage.tsx` 通过；`git diff --check` 与 `git diff --cached --check` 退出 0，仅 `git diff --check` 输出既有 LF/CRLF 转换提示。未做真实 Tauri 桌面 Chat 状态面板视觉点测，也未触发真实 MCP 连通性检测。
- 下一步候选：1) Chat 右侧状态面板继续补基础上下文：当前模型、权限模式、工作区路径、SDK installed 状态，用同一 compact surface 展示；2) 做 MCP 手动连通性检测入口：只在用户点击时调用 `check_mcp_status`，展示 online/offline/timeout/error 和延迟，避免自动启动 stdio MCP；3) 把 agent/subagent 可用状态纳入状态面板：可用 subagent 数、当前 agent-like tool 的模型/耗时/token 摘要；4) 真实 Tauri 桌面点测本轮 UI：切换 claude/codex 后 MCP enabled/total 是否变化，MCP 加载失败时诊断是否可见。

## 本轮 PLAN 2026-06-19 large history first-paint optimization

- 目标：优化点击历史大会话后的首屏加载速度，减少 React 首屏对全量历史消息的同步派生和工具结果扫描。
- 功能点 1：完成根因诊断与 cc-gui 对标。验证方式：CodeGraph 读取 `useChatStore.loadSession()`、`ChatPage`、`MessageList`、`chatNavigation`、`findToolResult` 与 cc-gui `MessageList`，确认慢点来自全量历史读取后仍全量派生。
- 功能点 2：补 RED 测试。验证方式：在 `src/utils/chatNavigation.test.ts` 增加最近可渲染窗口测试，断言非搜索首屏只扫描尾部窗口且保留原始索引。
- 功能点 3：实现最小前端优化。验证方式：新增共享窗口 helper；`ChatPage` 非搜索时只用最近窗口计算锚点/status；`MessageList` 非搜索时只从最近窗口渲染并用窗口内 tool result map，搜索时保留全量匹配。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md`，最后追加本轮完成记录。
- 功能点 5：质量验证。验证方式：运行定向 `chatNavigation` / `chatUiBehavior` / `chatStatusSummary` / store 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE 构建与空白检查。

## 迭代记录 2026-06-19 large history first-paint optimization

- 上轮实际进展：Chat 状态面板已展示当前 provider 的 MCP 配置可用性摘要，并通过前端全量测试、构建、Rust check/test、IDE 构建和空白检查；阻塞仍是真实 Tauri 桌面视觉/交互点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：cc-gui `webview/src/components/MessageList.tsx` 在普通浏览时只渲染最近 15 条并逐页 reveal。当前项目虽然已有折叠窗口，但 `src/pages/ChatPage.tsx` 仍对全量 `messages` 做 `getRenderableMessages()`、搜索过滤、锚点、`buildChatStatusSummary()`，`src/components/chat/MessageList.tsx` 也先全量 renderable/filter 后再 slice；每个 `MessageItem` 还把全量 `messages` 交给工具结果查找。
- 本轮诊断（超越 cc-gui）：本项目的右侧状态、锚点和中心 diff 面板比 cc-gui 信息密度更高，所以更需要把“首屏可见窗口”和“显式全量搜索”区分开。普通浏览应先围绕可见尾部稳定显示，只有用户搜索或继续 reveal earlier 时才逐步扩大派生范围。
- 本轮规划：先做前端首屏窗口化，不改后端 Tauri 命令。优先级高：用户价值高（点击大会话更快看到最近上下文）、出现频率高（历史会话越长越明显）、实现成本低到中（纯前端 helper + 三处接入）、风险可控（搜索仍保留全量；store 仍保留完整历史；不改 provider 文件解析）。
- 本轮完成：`src/utils/chatNavigation.ts` 新增 `getRecentRenderableMessages()`，返回最近可渲染窗口、隐藏可渲染数量、总可渲染数量并保留 `originalIndex`；`src/utils/chatNavigation.test.ts` 增加回归测试。`src/components/chat/MessageList.tsx` 非搜索时按最近窗口 + revealed count 渲染，不再先构造全量 renderable/filter；同时为当前可见尾部预建 `toolResultById` map，`src/components/chat/MessageItem.tsx` 改为接收窗口级 `findToolResult`。`src/pages/ChatPage.tsx` 非搜索时用最近窗口驱动锚点和消息计数，并用从首个可见原始下标开始的 raw slice 构建状态摘要，避免全量历史 edit/tool 汇总拖慢首屏；搜索时仍扫描完整历史。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 记录长历史首屏窗口化契约。
- 本轮验证：RED：`npm test -- src/utils/chatNavigation.test.ts` 先失败于 `getRecentRenderableMessages is not a function`。GREEN：`npm test -- src/utils/chatNavigation.test.ts src/utils/chatUiBehavior.test.ts src/utils/chatStatusSummary.test.ts` 通过（31 tests）；`npm test -- src/stores/useChatStore.test.ts` 通过（47 tests）；`npm test` 通过（30 files / 262 tests，仅 Browserslist/caniuse-lite 过期提示）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（107 tests，2 doctests ignored）；IDE `build_project` 针对本轮 5 个 TS/TSX 文件通过；`git diff --check` 与 `git diff --cached --check` 退出 0，前者仅输出既有 LF/CRLF 转换提示。未做真实 Tauri 桌面大会话点击性能点测，也未改后端全量读取/序列化。
- 下一步候选：1) 增加后端尾部窗口命令，例如 `get_unified_session_messages_window(providerId, sourcePath, tailCount)`，先让 Claude/Codex JSONL provider 只解析尾部有效消息并返回 `total/hasMore`，从源头减少 Tauri 序列化和前端映射；2) 为大会话点击加入轻量性能埋点或测试夹具，记录 backend invoke、history map、first render 三段耗时；3) 真实 Tauri 桌面点测 1k/5k/10k 历史会话：点击到首屏可见、滚动顶部 reveal earlier、搜索全量、状态面板 edit/diff 是否随 reveal 扩大；4) 继续补 Chat 状态面板基础上下文：当前模型、权限模式、工作区路径、SDK installed 状态。

## 本轮 PLAN 2026-06-19 chat model selector icons and dynamic models

- 目标：把 Chat 输入区模型切换改成 cc-gui 同类的模型图标 + 短标签呈现，并把模型列表从组件硬编码改为动态配置层加载，保留内置模型兜底。
- 功能点 1：确认对标和现状。验证方式：CodeGraph 读取本项目 `ButtonArea` / `ChatComposer` / `useChatStore` / `constants`，对照 cc-gui `ModelSelect` / `ButtonArea` / `ProviderModelIcon`，区分真实动态合并与未经核实的在线拉取。
- 功能点 2：补 RED 测试。验证方式：新增 `chatModels` 工具测试覆盖 Provider 默认模型、本地自定义模型、去重、当前模型保留；扩展 `ButtonArea` / `ChatComposer` 测试覆盖模型图标与动态模型透传。
- 功能点 3：实现最小改动。验证方式：新增共享模型列表 helper；`ChatComposer` 复用 `useProviderStore.loadAllProviders()` 动态读取 Provider 模型配置；`ButtonArea` 只消费传入模型，不再直接读取硬编码数组；模型 selector 触发按钮和列表项使用模型家族图标。
- 功能点 4：同步 Trellis 规范和迭代记录。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` / `.trellis/spec/frontend/state-management.md`，并在本文件追加最终记录。
- 功能点 5：质量验证。验证方式：运行新增/相邻前端测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE 构建与空白检查；不做自动在线模型拉取。

## 迭代记录 2026-06-19 chat model selector icons and dynamic models

- 上轮实际进展：Chat 状态面板 MCP 配置摘要和大会话首屏窗口化均已落地并通过自动化验证；阻塞仍是真实 Tauri 桌面点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 均可读，两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：cc-gui `webview/src/components/ChatInputBox/selectors/ModelSelect.tsx` 的模型 selector 是“provider/model 图标 + 短标签”，并在 `ButtonArea.tsx` 中把内置模型与 localStorage 自定义模型、Claude 模型映射动态合并。当前项目 `src/components/chat/composer/ButtonArea.tsx` 旧实现直接调用 `modelsForProvider()`，模型列表来自 `constants.ts` 静态数组，触发按钮和列表项都用通用 `Terminal` 图标；`src/stores/useChatStore.ts` 默认模型也直接取静态数组第一项。
- 本轮诊断（超越 cc-gui）：本项目已有 Provider 配置和 `fetch_models` 表单能力，但 Chat 首屏不应自动调用远端 `/v1/models`：该命令会携带 API Key 访问 provider URL，且现有实现不一定适配所有 Anthropic-compatible 源。更稳妥的第一步是把 Chat 模型列表做成动态配置层：读取当前 provider 的已保存模型字段、本地自定义模型，再叠加内置兜底；后续再设计用户点击触发的刷新入口。
- 本轮规划：只做模型 selector 图标化 + 动态配置列表，不改后端命令、不做自动远端拉取。优先级高：用户价值高（发送前能看到实际 provider 配置模型，模型切换更像 cc-gui）、出现频率高（每次发消息都会看到/可能切换模型）、实现成本低到中（纯前端 helper + store 复用 + 测试）、风险低（内置模型兜底保留，失败不阻断发送）。
- 本轮完成：`src/utils/chatModels.ts` 新增模型列表合并层，按“active Provider 默认模型字段 -> localStorage `ccg-chat-custom-models:<provider>` -> 内置 fallback”合并并去重，`ensureChatModelInList()` 确保历史/本地保存的当前模型不会在 selector 上显示成错误的 fallback 标签。`src/components/chat/composer/ModelIcon.tsx` 新增模型家族图标，区分 Claude Opus/Sonnet/Haiku/Fable/custom 与 Codex/GPT/custom。`src/components/chat/composer/ButtonArea.tsx` 改为接收 `models` / loading / error，模型按钮和选项使用 `ModelIcon`，只在未传入列表时才回退 `modelsForProvider()`。`src/components/chat/composer/ChatComposer.tsx` 接入 `useProviderStore.loadAllProviders()`，从 Provider 配置派生模型列表并监听本地自定义模型 storage 变更。`src/stores/useChatStore.ts` 默认模型兜底改用共享 helper，不再直接读 `CLAUDE_MODELS` / `CODEX_MODELS`。`src/locales/en.json` / `src/locales/zh.json` 增加模型配置加载/失败文案。新增/扩展 `chatModels`、`ButtonArea`、`ChatComposer` 测试。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 已同步该模型列表契约。新增文件已执行 `git add -- src/utils/chatModels.ts src/utils/chatModels.test.ts src/components/chat/composer/ModelIcon.tsx src/components/chat/composer/ChatComposer.render.test.tsx`。
- 本轮验证：RED：`npm test -- src/utils/chatModels.test.ts src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx` 先失败于缺少 `chatModels` 模块、模型按钮仍显示 `Opus 4.8` 和通用 Terminal。GREEN：同命令通过（3 files / 9 tests）；`npm test -- src/utils/chatModels.test.ts src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ChatComposer.test.ts src/stores/useChatStore.test.ts` 通过（5 files / 64 tests）；`npm run build` 通过；`npm test` 通过（32 files / 269 tests，仅 Browserslist/caniuse-lite 过期提示）；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（107 tests，2 doctests ignored）；IDE `build_project` 针对本轮 TS/TSX 文件通过；`git diff --check` 与 `git diff --cached --check` 退出 0，前者仅输出既有 LF/CRLF 转换提示。未做真实 Tauri 桌面视觉点测，也未做自动远端模型拉取。
- 下一步候选：1) 给模型 selector 增加用户触发的“刷新模型”入口，点击后调用当前 active Provider 的 `fetch_models`，结果写入 `ccg-chat-custom-models:<provider>` 并显示错误/耗时；2) 在 Chat 右侧状态面板补当前模型、权限模式、工作区路径、SDK installed 状态，和本轮模型来源保持一致；3) 给 Provider 表单的模型获取结果增加持久化选项，避免用户每次打开表单重新获取；4) 真实 Tauri 桌面点测模型 selector：active Provider 默认模型是否出现在首位、切换 Claude/Codex 后图标和列表是否正确、本地自定义模型变更是否刷新。

## 本轮 PLAN 2026-06-19 chat mcp expandable details

- 目标：修复 Chat 右侧状态面板 MCP 摘要点击后无法展开查看的问题，让用户能直接看到当前 provider 下每个 MCP server 的配置启用状态。
- 功能点 1：确认根因与对标上下文。验证方式：CodeGraph 读取 `StatusPanel` / `chatMcpStatus` / `ChatPage`，确认当前只有 compact summary，没有 server 明细和展开 affordance；复查 cc-gui 可读路径。
- 功能点 2：补 RED 测试。验证方式：扩展 `chatMcpStatus.test.ts` 和 `StatusPanel.test.tsx`，断言 summary 派生 server detail，且状态面板渲染可展开 details、server 名称和启用/未启用标签。
- 功能点 3：实现最小交互增强。验证方式：`buildChatMcpAvailabilitySummary()` 返回 server 明细；`StatusPanel` 用原生 `<details>/<summary>` 展开列表，不自动触发 `check_mcp_status`。
- 功能点 4：同步 i18n 与 Trellis 规范。验证方式：新增中英文文案，并更新 `.trellis/spec/frontend/component-guidelines.md` / `.trellis/spec/frontend/state-management.md` 的 MCP 配置可用性契约。
- 功能点 5：质量验证。验证方式：运行定向 MCP / StatusPanel 测试、全量前端测试、前端构建、Rust check/test、IDE 构建与空白检查。

## 迭代记录 2026-06-19 chat mcp expandable details

- 上轮实际进展：大会话首屏窗口化、模型 selector 图标化与动态配置列表、Chat MCP 配置摘要均已落地并通过自动化验证；阻塞仍是真实 Tauri 桌面视觉/交互点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读，用户给定的 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且两个仓库都有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：`src/components/chat/StatusPanel.tsx` 的 MCP 区域只有 `status-mcp-summary` 普通容器，`src/utils/chatMcpStatus.ts` 的 `ChatMcpAvailabilitySummary` 也只有 enabled/total/loading/error；所以点击 MCP 无法展开不是事件失效，而是没有展开交互和 server detail 数据。
- 本轮诊断（超越 cc-gui）：展开详情只展示配置可用性，不自动调用 `check_mcp_status`。这样用户能发送前检查当前 provider 到底启用了哪些 MCP server，同时避免展开面板时启动 stdio MCP 命令或产生误导性的 online/offline 标签。
- 本轮规划：只修 MCP 摘要的可展开详情。优先级高：用户价值高（直接回应“点击 mcp 无法展开查看”且补齐基础信息）、出现频率高（进入 Chat / 切 provider / 发送前都会看状态）、实现成本低（纯前端 helper + 原生 disclosure + 测试）、风险低（不改后端、不启动 MCP 进程）。
- 本轮完成：`src/utils/chatMcpStatus.ts` 新增 `ChatMcpAvailabilityServerSummary`，`buildChatMcpAvailabilitySummary()` 返回每个 server 的 id/name/enabled/transport，并继续按 provider 映射 enabled count。`src/components/chat/StatusPanel.tsx` 把 MCP 摘要改为原生 `<details>/<summary>`，保留 compact enabled/total 行，展开后展示配置服务器列表、transport 和启用/未启用标签，loading/error 时默认展开诊断。`src/styles/toolBlocks.css` 增加 MCP chevron 旋转与 summary marker 处理。`src/locales/en.json` / `src/locales/zh.json` 增加 MCP details 文案。`src/utils/chatMcpStatus.test.ts` 与 `src/components/chat/StatusPanel.test.tsx` 增加展开详情回归测试。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 同步记录“配置明细可展开、不是实时连通性”的契约。
- 本轮验证：RED：`npm test -- src/utils/chatMcpStatus.test.ts src/components/chat/StatusPanel.test.tsx` 先失败 3 项，分别证明旧 helper 缺少 `servers` 明细、旧面板缺少 `status-mcp-details`。GREEN：同命令通过（2 files / 13 tests）；`npm test -- src/utils/chatMcpStatus.test.ts src/components/chat/StatusPanel.test.tsx src/utils/chatDaemonStatus.test.ts` 通过（3 files / 21 tests）；`npm test` 通过（32 files / 269 tests，仅 Browserslist/caniuse-lite 过期提示）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（107 tests，2 doctests ignored）；IDE `build_project` 针对本轮 7 个文件通过；`git diff --check` 与 `git diff --cached --check` 退出 0，前者仅输出既有 LF/CRLF 转换提示。未做真实 Tauri 桌面点击展开点测，也未做真实 MCP 连通性检测。
- 下一步候选：1) 真实 Tauri 桌面点测 MCP details：切 Claude/Codex 后 enabled/disabled 是否随 provider 切换、loading/error 默认展开是否合理；2) 增加用户触发的 MCP 手动连通性检测入口，点击后调用 `check_mcp_status` 并展示 online/offline/timeout/error/耗时，明确区别于配置可用性；3) Chat 状态面板继续补基础上下文：当前模型、权限模式、工作区路径、SDK installed 状态；4) 给模型 selector 增加用户触发的“刷新模型”入口，调用当前 active Provider 的 `fetch_models` 并写入本地自定义模型缓存。

## 本轮 PLAN 2026-06-19 chat composer task-subagent-edit tabs

- 目标：补齐输入框上方的「任务 / 子代理 / 编辑」三栏状态入口，对齐 cc-gui `StatusPanel` 在 composer 上方展示 todo/subagent/files tabs 的基础体验，并复用本项目已有工具时间线和编辑摘要。
- 功能点 1：确认差距与对标上下文。验证方式：CodeGraph 读取本项目 `ChatPage` / `ChatComposer` / `StatusPanel` / `buildChatStatusSummary()`，并对照 `C:/guodevelop/demo/jetbrains-cc-gui/webview/src/components/StatusPanel/StatusPanel.tsx`。
- 功能点 2：补 RED 测试。验证方式：扩展 `src/utils/chatStatusSummary.test.ts`，新增 `src/components/chat/ChatInputStatusTabs.test.tsx`，先运行定向测试确认旧代码缺少 `toolTimeline` / `agentTools` 与输入区状态 tabs。
- 功能点 3：实现最小 UI 与数据层。验证方式：`buildChatStatusSummary()` 输出工具时间线和 agent/task 子集；新增 `ChatInputStatusTabs` 三栏入口，点击/默认展开可看任务、子代理、编辑明细；`ChatPage` 将其插入 `ChatComposer` 上方并把编辑点击接入现有 diff 选择。
- 功能点 4：同步 i18n 与 Trellis 规范。验证方式：新增中英文文案，并在 `.trellis/spec/frontend/component-guidelines.md` / `.trellis/spec/frontend/state-management.md` 记录 composer 状态入口和 summary 数据契约。
- 功能点 5：质量验证。验证方式：运行定向组件/summary 测试、全量前端测试、前端构建、Rust check/test、IDE 构建与空白检查。

## 迭代记录 2026-06-19 chat composer task-subagent-edit tabs

- 上轮实际进展：大会话首屏窗口化、模型 selector 图标化 + 动态配置列表、Chat MCP 配置摘要与可展开 server 明细均已落地并通过自动化验证；阻塞仍是真实 Tauri 桌面视觉/交互点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：对照 `C:/guodevelop/demo/jetbrains-cc-gui/webview/src/components/ChatScreen.tsx` 与 `webview/src/components/StatusPanel/StatusPanel.tsx`，cc-gui 在 `ChatInputBox` 上方渲染 `StatusPanel`，三栏为 todo/subagent/files。当前项目 `src/pages/ChatPage.tsx` 只有 `MessageList -> ScrollControl -> ChatComposer`，右侧 `src/components/chat/StatusPanel.tsx` 虽有活动/编辑/MCP 信息，但没有用户截图所指的输入框上方「任务 / 子代理 / 编辑」入口。
- 本轮诊断（超越 cc-gui）：本项目暂不复制 cc-gui 的 undo/discard/keep all 高风险操作，而是先把高频、低风险的运行态信息固定在 composer 上方。任务来自当前加载窗口内的工具时间线，子代理来自 agent/task 工具子集，编辑复用现有 diff selection；这样用户发送前能快速确认当前动作、子代理和文件改动，同时避免新增后端协议或破坏性操作。
- 本轮规划：聚焦 composer 上方三栏状态入口。优先级高：用户价值高（直接回应输入框上方入口缺失，发送前可见任务/子代理/编辑基础信息）、出现频率高（每轮对话都会看到 composer）、实现成本低到中（复用已有 `buildChatStatusSummary()` 和 diff handler）、风险低（纯前端展示，不改 daemon/Tauri 命令）。
- 本轮完成：`src/utils/chatStatusSummary.ts` 扩展 `ChatStatusSummary`，新增 `toolTimeline` 与 `agentTools`，并让 agent/task 摘要保留 subagent type / nickname / model 等可见元数据；`src/components/chat/ChatInputStatusTabs.tsx` 新增输入框上方三栏入口，支持任务、子代理、编辑三类 inline 展开详情，编辑项点击复用现有 diff 选择；`src/pages/ChatPage.tsx` 在 `ScrollControl` 和 `ChatComposer` 之间插入该入口，并传入 `statusSummary`、streaming 状态和 `handleSelectedEditChange`；`src/locales/en.json` / `src/locales/zh.json` 增加入口条文案；`src/utils/chatStatusSummary.test.ts` 与新增 `src/components/chat/ChatInputStatusTabs.test.tsx` 覆盖工具时间线、子代理列表、三栏入口和编辑选择态；`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 记录 composer status strip 的 UI/状态契约。新增文件已执行 `git add -- src/components/chat/ChatInputStatusTabs.tsx src/components/chat/ChatInputStatusTabs.test.tsx`。
- 本轮验证：RED：`npm test -- src/utils/chatStatusSummary.test.ts src/components/chat/ChatInputStatusTabs.test.tsx` 先失败于缺少 `ChatInputStatusTabs` 模块和 `summary.toolTimeline` 为 `undefined`。GREEN：同命令通过（2 files / 9 tests）；相邻验证 `npm test -- src/utils/chatStatusSummary.test.ts src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx` 通过（4 files / 20 tests）；`npm run build` 通过；`npm test` 通过（33 files / 273 tests，仅 Browserslist/caniuse-lite 过期提示）；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（107 tests，2 doctests ignored）；IDE `build_project` 针对本轮 TS/TSX/i18n 文件通过；`git diff --check` 与 `git diff --cached --check` 退出 0，其中 `git diff --check` 仅输出既有 LF/CRLF 转换提示。未做真实 Tauri 桌面视觉点测，也未用浏览器手动点击 tabs。
- 下一步候选：1) 真实 Tauri 桌面点测 composer 状态条：空会话、普通工具流、Task/Agent 调用、编辑文件点击打开 diff pane、长历史窗口化后统计范围是否符合预期；2) 若用户希望更接近 cc-gui，再设计输入区状态条的 popover 定位和外部点击关闭，不把破坏性 undo/discard/keep all 放进首轮；3) Chat 右侧状态面板继续补基础上下文：当前模型、权限模式、工作区路径、SDK installed 状态；4) 给模型 selector 增加用户触发的“刷新模型”入口，调用当前 active Provider 的 `fetch_models` 并写入本地自定义模型缓存。

## 本轮 PLAN 2026-06-19 chat status strip git branch and conditional buttons

- 目标：让 Chat 输入区状态条支持 Git 分支可见性，并把状态条按钮改成触发式显示隐藏：当前 cwd 是 Git 仓库才显示分支；任务、子代理、编辑只有存在对应数据时才显示；完全无状态时不占位。
- 功能点 1：确认数据来源和对标边界。验证方式：CodeGraph 读取 `ChatInputStatusTabs`、`ChatPage`、`StatusPanel`、`chat_commands`，确认当前无 Git 分支命令且状态条固定显示三栏 0。
- 功能点 2：补 RED 测试。验证方式：扩展 `ChatInputStatusTabs.test.tsx` 覆盖 Git 分支显示、空状态隐藏、单类状态动态布局；扩展 Rust `chat_commands` 测试覆盖非 Git 目录返回 none、`.git/HEAD` 分支解析。
- 功能点 3：实现只读 Git 工作区摘要。验证方式：新增 `chat_workspace_status(cwd)` Tauri 命令，从 cwd 向上找 `.git` 并解析 HEAD；前端新增 helper，`ChatPage` 随 `currentCwd` 加载并传入状态条。
- 功能点 4：实现状态条触发式显示。验证方式：`ChatInputStatusTabs` 根据 Git/任务/子代理/编辑 presence 构造可见项；无可见项返回 null；不再显示 0 占位按钮。
- 功能点 5：同步 i18n 与 Trellis 规范。验证方式：新增 Git 分支文案，更新 `.trellis/spec/frontend/component-guidelines.md` / `.trellis/spec/frontend/state-management.md`，最后追加本轮完成记录。
- 功能点 6：质量验证。验证方式：运行定向前端/Rust 测试、前端全量测试、前端构建、Rust check/test、IDE 构建与空白检查。

## 本轮 PLAN 2026-06-19 large history transcript hydration

- 目标：修复比较大的历史会话加载到聊天区域后仍卡顿的问题，保证普通浏览路径只渲染最近窗口，后台完整历史只用于缓存与诊断指标，不再自动把几千条消息回灌到 `messages`。
- 功能点 1：锁定根因。验证方式：复查 `useChatStore.loadSession()`、`ChatPage`、`MessageList`，确认 full history 完成后仍替换 `messages`，cache hit 也会直接塞入完整数组。
- 功能点 2：补 RED 测试。验证方式：更新 `src/stores/useChatStore.test.ts`，断言 full load 完成后 `messages` 仍为首屏窗口长度，`lastSessionLoadMetrics` 完整，重复点击缓存大会话也只恢复窗口。
- 功能点 3：实现最小 store 修复。验证方式：`loadSession()` 首屏窗口继续写入 `messages`；后台完整历史只 `rememberSessionHistory()` 和更新 metrics；cache hit 从完整缓存派生最近窗口写入显示状态。
- 功能点 4：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/state-management.md` 和必要的组件规范，记录普通浏览不得自动 full-hydrate 聊天区。
- 功能点 5：质量验证。验证方式：运行定向 store/navigation 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE 构建与空白检查；未做真实 Tauri 桌面体感点测需明确标注。

## 迭代记录 2026-06-19 large history transcript hydration

- 上轮实际进展：历史大会话已有首屏窗口加载、缓存复用和 `lastSessionLoadMetrics` 诊断面板；输入区任务/子代理/编辑曾通过 idle 完整摘要补齐；Markdown 挤压问题已通过后端正文保留换行和前端相邻 text block 合并修复。用户最新反馈“比较大的历史会话加载到聊天区域依然卡顿”属实：代码复查确认 `useChatStore.loadSession()` 在首屏窗口后仍会把后台完整历史替换进 `messages`，cache hit 也会直接把完整缓存数组写入聊天区。阻塞仍是真实 Tauri 桌面体感点测未完成。
- 本轮诊断（补齐 cc-gui）：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。本项目已有 cc-gui 风格的 recent-window 首屏策略，但没有守住“普通浏览只消费窗口”的后半段契约；后台 full 完成后触发 `ChatPage` / `MessageList` / 状态摘要重新处理 5000+ 条消息，造成可见卡顿。
- 本轮诊断（超越 cc-gui）：用户点击大会话时最重要的是首屏可交互和滚动稳定，而不是立即把全量历史塞进 React 树。完整历史应先作为缓存与诊断数据存在；搜索、展开全历史、跨全量历史恢复输入区活动入口应走显式分页/索引，不应由后台 full load 隐式触发 UI 大更新。
- 本轮规划：只改 `useChatStore` 的历史加载状态契约与测试，不改后端读取命令、不改 ChatPage 布局、不引入虚拟列表。优先级高：用户价值高（直接降低大会话点击后的主线程压力）、出现频率高（历史会话切换常见）、实现成本低（已有窗口/缓存/metrics 结构）、风险可控（完整历史仍缓存，metrics 仍记录 full 阶段）。
- 本轮完成：`src/stores/useChatStore.test.ts` 将大会话回归契约改为 RED：full resolve 后 `messages` 仍为 120 条首屏窗口，cache hit 重选大会话也只显示 120 条窗口，同时保留 `fullMessageCount/totalMessageCount` 指标。`src/stores/useChatStore.ts` 新增 `getSessionHistoryDisplayWindow()`；cache hit 从完整缓存派生最近窗口写入 `messages`；后台 `get_unified_session_messages` 完成后只 `rememberSessionHistory()` 并更新 `lastSessionLoadMetrics`，不再调用 `getLoadedSessionState(...mappedHistory...)` 替换聊天区。`.trellis/spec/frontend/state-management.md` 与 `.trellis/spec/frontend/component-guidelines.md` 已同步“完整历史缓存/指标和可见 transcript 分离”的契约。
- 本轮验证：RED：`npm test -- src/stores/useChatStore.test.ts` 先失败 3 项，失败点均为旧实现把 `messages` 扩成 5000。GREEN：同命令通过（52 tests）；相邻验证 `npm test -- src/stores/useChatStore.test.ts src/utils/chatNavigation.test.ts src/utils/chatStatusSummary.test.ts src/components/chat/MessageList.test.tsx` 通过（3 files / 72 tests；其中不存在的 `MessageList.test.tsx` 未匹配到测试文件）。全量验证通过：`npm test`（35 files / 303 tests，仅 Browserslist/caniuse-lite 过期提示）、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests，2 doctests ignored）、IDE `build_project`、`git diff --check`、`git diff --cached --check`。`git diff --check` 仅输出既有 LF/CRLF 提示。`npm run build` 生成的未跟踪 `out/` 已校验在工作区内并删除。未做真实 Tauri 桌面点击大会话体感点测。
- 下一步候选：1) 真实 Tauri 桌面点测 1k/5k/10k 历史会话：首次打开、cache hit 重复点击、A/B 大会话切换、full metrics 完成后滚动是否仍卡；2) 若仍卡，下一轮把后台 full fetch 改为显式搜索/展开触发，避免 IPC 反序列化和 full map 抢主线程；3) 给完整历史的任务/子代理/编辑入口做轻量索引或 worker 化，避免用 `messages` full hydration 恢复输入区状态；4) 设计“加载更多历史/搜索全历史”的明确入口和分页协议，替代隐式自动全量回灌。

## 迭代记录 2026-06-19 chat status strip git branch and conditional buttons

- 上轮实际进展：输入框上方「任务 / 子代理 / 编辑」状态条已落地并通过自动化验证，但用户指出的问题属实：旧实现固定显示三栏 0，占位噪声较大；Git 分支、非 Git 隐藏、以及状态按钮按数据触发显示均未实现。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 在输入区附近更强调当前 workspace/task/file 状态可判断性；当前项目 `src/components/chat/ChatInputStatusTabs.tsx` 只有任务、子代理、编辑三栏，且没有仓库上下文。`src-tauri/src/commands/chat_commands.rs` 也没有只读 workspace Git 状态命令，导致前端无可靠数据源展示当前分支。
- 本轮诊断（超越 cc-gui）：触发式状态条比固定 0 占位更适合本项目的长历史和密集状态面板。用户在空会话或非 Git 目录时不应看到无意义按钮；一旦有 Git、工具、子代理或编辑活动，状态条才出现，降低 composer 区域噪声。
- 本轮规划：只做 Git 分支只读检测 + 状态条触发式显示，不做分支切换、Git dirty 状态或 live Git 命令。优先级高：用户价值高（发送前立即知道当前项目分支和活动状态）、出现频率高（每次 Chat composer 都可见）、实现成本低到中（新增只读命令 + 组件条件渲染）、风险低（不执行外部 `git`，不改发送链路）。
- 本轮完成：`src-tauri/src/commands/chat_commands.rs` 新增 `ChatWorkspaceStatus`、`chat_workspace_status(cwd)` 和纯文件读取的 Git 解析逻辑，支持普通 `.git/HEAD` 与 worktree/submodule `gitdir:` 文件，并把非 Git / 不存在路径规范化为空状态；`src-tauri/src/lib.rs` 注册该命令。`src/utils/chatWorkspaceStatus.ts` 新增前端归一化与失败兜底；`src/pages/ChatPage.tsx` 随 `currentCwd` 加载 workspace status 并传入状态条。`src/components/chat/ChatInputStatusTabs.tsx` 新增 GitBranch chip，任务/子代理/编辑改为有数据才渲染，全部无内容时返回 null，不再显示 0 占位。`src/locales/en.json` / `src/locales/zh.json` 新增 Git 状态条文案；`.trellis/spec/frontend/component-guidelines.md`、`.trellis/spec/frontend/state-management.md`、`.trellis/spec/backend/cross-layer-protocol.md` 同步记录触发式状态条与 `chat_workspace_status` 跨层契约。新增文件已执行 `git add -- src/utils/chatWorkspaceStatus.ts src/utils/chatWorkspaceStatus.test.ts`。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败于缺少 `workspaceStatus` prop / Git chip / 空状态隐藏；`cargo test --manifest-path src-tauri/Cargo.toml commands::chat_commands::tests::resolves_workspace_status` 先失败于缺少 `resolve_chat_workspace_status`。GREEN：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatWorkspaceStatus.test.ts` 通过（2 files / 9 tests）；`cargo test --manifest-path src-tauri/Cargo.toml commands::chat_commands::tests::resolves_workspace_status` 通过（3 tests，含 worktree `gitdir:`）；`npm test` 通过（34 files / 279 tests，仅 Browserslist/caniuse-lite 过期提示）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（110 tests，2 doctests ignored）；IDE `build_project` 针对本轮前端/Rust/i18n 文件通过；`git diff --check` 与 `git diff --cached --check` 退出 0，前者仅输出既有 LF/CRLF 转换提示。未做真实 Tauri 桌面视觉点测。
- 下一步候选：1) 真实 Tauri 桌面点测 composer 状态条：Git 仓库显示分支、非 Git 目录完全隐藏、工具/子代理/编辑活动出现后可展开；2) 在右侧状态面板补完整基础上下文：当前模型、权限模式、工作区路径、SDK installed 状态，与 composer 状态条分工清晰；3) 给 Git chip 增加可选扩展：显示 repo root tooltip 已有，后续可增加 dirty/clean 只读状态，但必须评估扫描成本；4) 给模型 selector 增加用户触发的“刷新模型”入口，调用当前 active Provider 的 `fetch_models` 并写入本地自定义模型缓存。

## 本轮 PLAN 2026-06-19 chat markdown newline preservation

- 目标：修复历史聊天消息里个别 assistant/user Markdown 被压成一坨的问题，保留正文中的段落、列表、标题和代码块换行，让 `MarkdownBlock` 能按真实 Markdown 渲染。
- 功能点 1：定位根因。验证方式：CodeGraph 读取 `MessageItem`、`ContentBlockRenderer`、`MarkdownBlock`、`chatMessageFlow`、Claude/Codex session providers，确认换行是否在前端渲染前已经被压平。
- 功能点 2：补 RED 测试。验证方式：扩展 Rust provider tests，覆盖 Codex `output_text` 与 Claude JSONL text content 中的 `\n\n- item` Markdown，断言 `content` 和 raw text block 保留换行。
- 功能点 3：实现正文专用清理。验证方式：新增保留 Markdown 换行的正文清理函数；仅切换消息正文路径，保留标题/摘要使用单行 `sanitize_session_text()`。
- 功能点 4：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` / `.trellis/spec/frontend/state-management.md` 或 backend contract，记录历史正文清理不得折叠 Markdown 换行。
- 功能点 5：质量验证。验证方式：运行定向 Claude/Codex provider tests、前端相邻渲染测试、`npm test`、`npm run build`、`cargo check`、`cargo test`、IDE 构建与空白检查；未做真实 Tauri 桌面截图点测需明确标注。

## 迭代记录 2026-06-19 chat markdown newline preservation

- 上轮实际进展：Chat UI 已持续补齐 provider 图标、动态模型、输入区任务/子代理/编辑状态、MCP/daemon/workspace 状态、历史会话缓存和大历史首屏加载等多轮能力；这些在工作区代码和测试中已有对应实现。阻塞仍是用户最新截图反馈的个别历史 assistant final answer 被压成一坨，真实 Tauri 桌面截图点测未完成。
- 本轮诊断（补齐 cc-gui）：`MarkdownBlock` 能正常解析保留换行的 Markdown；问题发生在进入渲染前。Rust 历史 provider 共用 `sanitize_session_text()`，该函数把正文按行 `join(" ")` 并 `split_whitespace()`，会把标题、空行、列表压成一行；同时前端 `getRenderableContentBlocks()` 只过滤不合并，相邻 `text` blocks 会被 `ContentBlockRenderer` 拆成多个独立 `MarkdownBlock`。
- 本轮诊断（超越 cc-gui）：只靠 CSS 间距无法修复语义。正确方案是后端保留 transcript body 的 Markdown 换行，前端把 provider 拆分的相邻 text fragments 重新视为一个 Markdown 文档；同时不跨 image/thinking/tool_use 边界合并，避免破坏真实 transcript 结构。
- 本轮规划：优先修复“历史正文 Markdown 换行保真 + 相邻 text block 合并”。优先级高：用户价值高（直接解决长 final answer 难读）、出现频率中高（历史会话/长回复常见）、成本中低（provider 清理函数 + 共享前端 helper + 回归测试）；暂不扩大到其它状态栏/MCP/模型切换需求。
- 本轮完成：`src-tauri/src/session_manager/providers/utils.rs` 新增 `sanitize_session_markdown_text()`，保留 Markdown 换行并过滤内部噪声；`claude.rs` / `codex.rs` 的正文、reasoning 文本路径切到正文专用清理，标题/摘要仍保留单行清理；`src/utils/chatMessageFlow.ts` 新增 `mergeAdjacentTextContentBlocks()`，`getRenderableContentBlocks()` 和 `getTextFromRaw()` 共用；`src/components/chat/ContentBlockRenderer.tsx` 在工具分组前合并相邻 text blocks；对应 Rust/前端测试新增 Markdown 换行与相邻 text block 回归；`.trellis/spec/backend/cross-layer-protocol.md` 与 `.trellis/spec/frontend/component-guidelines.md` 已同步契约。
- 本轮验证：先补 RED，`cargo test --manifest-path src-tauri/Cargo.toml preserves_markdown_line_breaks` 与 `npm test -- src/utils/chatMessageFlow.test.ts src/components/chat/ContentBlockRenderer.test.tsx` 均按预期失败；实现后上述定向测试通过。全量验证通过：`npm test`（35 files / 303 tests）、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests）、IDE `build_project`、`git diff --check`。`git diff --check` 仅输出既有 LF/CRLF 提示，无 whitespace error。未做真实 Tauri 桌面截图点测。
- 下一步候选：1) 用真实 Tauri 桌面加载用户截图对应的长历史会话，确认 final answer 段落/列表/标题不再挤压；2) 继续点测和收口用户反馈的 MCP 展开、输入框上方任务/子代理/编辑触发区、Git 分支触发式显示、Claude Code/Codex 图标一致性；3) 若仍有个别消息挤压，抓取对应 raw `message.content[]`，判断是否还有 provider 特有 wrapper 或工具结果文本需要结构化处理。

## 本轮 PLAN 2026-06-19 chat provider switch brand icons

- 目标：把 Chat 输入区 Claude Code / Codex provider 切换按钮从通用 Terminal 图标改为 provider 专属图标，并对齐 cc-gui `ProviderSelect` 的 compact provider 图标行为。
- 功能点 1：确认对标实现。验证方式：读取 `C:/guodevelop/demo/jetbrains-cc-gui/webview/src/components/ChatInputBox/selectors/ProviderSelect.tsx` 与 `ProviderModelIcon.tsx`，确认 cc-gui 触发按钮和下拉选项都使用 provider/model vendor 图标。
- 功能点 2：补 RED 测试。验证方式：扩展 `src/components/chat/composer/ButtonArea.test.tsx`，断言 Claude/Codex provider 切换控件渲染 `data-chat-provider-icon="claude|codex"`，且不再依赖统一 Terminal。
- 功能点 3：实现最小展示层改动。验证方式：`ButtonArea` 增加 provider 专属图标渲染，provider 触发按钮和 option 共用同一 helper；不新增依赖、不改 provider 切换状态逻辑。
- 功能点 4：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 provider selector 不得回退为通用 Terminal 图标。
- 功能点 5：质量验证。验证方式：运行定向 `ButtonArea` 测试、相邻 composer 测试、`npm test`、`npm run build`、Rust check/test、IDE 构建与空白检查；未做真实 Tauri 桌面点测需明确标注。

## 迭代记录 2026-06-19 chat provider switch brand icons

- 上轮实际进展：记录中声称模型 selector 图标化/手动动态刷新、MCP 右侧 details 与输入区 MCP tab、输入区任务/子代理/编辑/Git/MCP 触发式状态条、右侧运行上下文、历史大会话加载诊断都已存在；代码复查确认这些能力确实分布在 `ChatComposer`、`ButtonArea`、`ChatInputStatusTabs`、`StatusPanel`、`useChatStore` 和相关 utils 中。用户最新指出的 provider 切换图标问题属实：`src/components/chat/composer/ButtonArea.tsx` 的 provider options 和 trigger 在本轮前仍统一使用 `Terminal`。已知阻塞仍是真实 Tauri 桌面视觉/点击点测不足。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 可读，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 `webview/src/components/ChatInputBox/selectors/ProviderSelect.tsx` 在 compact 模式下只渲染 `ProviderModelIcon`，下拉选项同样渲染 provider 专属图标；`webview/src/components/shared/ProviderModelIcon.tsx` 将 Claude 映射到 Claude 图标，将 Codex 映射到 OpenAI 图标。本项目 provider selector 此前没有同等区分，Claude Code 与 Codex 都显示通用终端图标，用户切换时无法形成稳定 provider 视觉锚点。
- 本轮诊断（超越 cc-gui）：本项目没有引入 `@lobehub/icons`，因此本轮不新增依赖，而是在 composer 内保留轻量 inline provider 图标。这样能获得 cc-gui 的“provider 专属、compact toolbar”体验，同时不扩大依赖面、不影响动态模型刷新或 provider store。
- 本轮规划：只做 provider 切换控件展示层修正，不改 provider 状态、发送链路、模型列表动态加载或 MCP 状态。优先级高：用户价值高（切 Claude/Codex 时一眼识别上下文）、出现频率高（每次发送前都在输入区可见）、实现成本低（纯前端展示 + SSR 测试）、风险低（状态仍由 `useChatStore` 和 props 驱动）。
- 本轮完成：`src/components/chat/composer/ButtonArea.test.tsx` 新增 RED/GREEN 测试，断言 Claude/Codex provider 按钮分别渲染 `data-chat-provider-icon="claude|codex"` 且不再是 provider 按钮内的 `lucide-terminal`。`src/components/chat/composer/SelectorDropdown.tsx` 增加 `compact` 模式，紧凑按钮只显示图标并保留 disabled 关闭逻辑。`src/components/chat/composer/ButtonArea.tsx` 新增 `ProviderBrandIcon`，Claude Code 使用 Claude 风格橙色标识，Codex 使用 OpenAI/Codex 风格线性标识；provider trigger 改为 icon-only compact，dropdown options 复用同一 provider 图标并保留 label。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 已同步 provider selector 图标与状态边界契约。
- 本轮验证：RED：`npm test -- src/components/chat/composer/ButtonArea.test.tsx` 先失败 1 项，失败点为缺少 `data-chat-provider-icon="claude"` 且旧按钮仍输出 `lucide-terminal`。GREEN：同命令通过（1 file / 6 tests）；相邻验证 `npm test -- src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ChatComposer.test.ts` 通过（3 files / 16 tests）；`npm test -- src/components/chat/composer/ButtonArea.test.tsx src/utils/chatModels.test.ts` 通过（2 files / 14 tests）；`npm test` 通过（35 files / 301 tests，仅 Browserslist/caniuse-lite 过期提示）；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`npm run build` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（112 tests，2 doctests ignored）；IDE `build_project` 通过；`git diff --check` 与 `git diff --cached --check` 退出 0，只有既有 LF/CRLF 转换提示。`npm run build` 产生的 `out/` 已确认位于工作区内并删除。未做真实 Tauri 桌面视觉点测，因此图标在 WebView 里的最终视觉大小/色彩仍需 smoke 确认。
- 下一步候选：1) 真实 Tauri 桌面点测 provider selector：Claude/Codex 切换、compact 图标点击范围、下拉选项图标+label、禁用态、暗色主题；2) 如果需要更接近 cc-gui，可评估是否引入统一 provider/model 图标组件或受控图标资源，但要先评估依赖体积与许可；3) 真实桌面点测模型刷新、MCP details/live check、输入区任务/子代理/编辑/Git/MCP 展开项；4) 若用户继续反馈大会话慢，用 `lastSessionLoadMetrics` 分段数据定位下一轮是后端 tail reader、前端 map，还是 idle 完整摘要补齐。

## 本轮 PLAN 2026-06-19 chat status panel runtime context

- 目标：在 Chat 右侧状态面板补齐基础上下文：当前模型、权限模式、推理强度、工作区路径、SDK 安装状态，让用户发送前能一次性确认运行上下文。
- 功能点 1：确认上下文来源。验证方式：CodeGraph 读取 `StatusPanel`、`ChatPage`、`useChatStore`、`ChatComposer`、`useSdkStore`，确认这些字段已在前端状态中存在，不新增后端命令。
- 功能点 2：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx`，断言传入模型/权限/推理/工作区/SDK 后渲染 `status-runtime-context`、模型 id、权限文案、工作区末尾路径和 SDK installed/missing 状态。
- 功能点 3：实现最小展示层增强。验证方式：`StatusPanel` 增加可选 props 并在顶部 summary 下方渲染 compact runtime context；`ChatPage` 从 `useChatStore` / `useSdkStore` 传入现有字段。
- 功能点 4：同步 i18n 与 Trellis 规范。验证方式：新增中英文状态面板文案，更新 `.trellis/spec/frontend/component-guidelines.md` / `.trellis/spec/frontend/state-management.md`。
- 功能点 5：质量验证。验证方式：运行定向 `StatusPanel` 测试、相邻 Chat composer/store 测试、`npm test`、`npm run build`、Rust check/test、IDE 构建与空白检查。

## 迭代记录 2026-06-19 chat status panel runtime context

- 上轮实际进展：输入框上方状态条已支持 Git 分支、非 Git 隐藏、任务/子代理/编辑触发式显示，并通过前端全量测试、构建、Rust check/test、IDE 构建和空白检查；阻塞仍是真实 Tauri 桌面视觉点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：右侧 `StatusPanel` 旧实现已有 provider、daemon、MCP、工具计数、当前活动和编辑摘要，但缺少用户发送前最常核对的运行上下文：当前模型、权限模式、推理强度、工作区路径和 SDK 安装状态。对应数据在 `useChatStore` 与 `useSdkStore` 已存在，缺口是展示层没有集中呈现。
- 本轮诊断（超越 cc-gui）：把模型/模式/推理/工作区/SDK 放在右侧 compact context 中，可减少用户反复展开 composer 下拉、依赖弹窗或顶部状态来确认环境。此处只展示已有状态，不触发模型刷新、SDK 安装、MCP 连通性检测或 daemon 重启，避免状态面板变成隐式副作用入口。
- 本轮规划：只做 `StatusPanel` 运行上下文展示和 `ChatPage` 接线。优先级高：用户价值高（发送前避免用错模型/工作区/权限模式）、出现频率高（每轮 Chat 都相关）、实现成本低（复用已有 store 状态和 i18n）、风险低（纯前端展示，不改后端协议和发送链路）。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 新增 RED/GREEN 测试，覆盖 `status-runtime-context`、模型、权限模式、推理强度、工作区和 SDK installed 文案。`src/components/chat/StatusPanel.tsx` 增加可选 `model` / `permissionMode` / `reasoningEffort` / `sdkStatus` props，在顶部 summary 下方渲染运行上下文卡片，并复用 composer constants 的模式/推理 i18n 映射。`src/pages/ChatPage.tsx` 从 `useChatStore` 取现有模型、权限模式、推理强度，并把当前 provider 对应的 `currentSdk` 传给右侧面板。`src/locales/en.json` / `src/locales/zh.json` 新增 runtime context、workspace、SDK 状态面板文案。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 已同步“运行上下文只消费已有状态，不新增 store/副作用”的契约。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败于缺少 `status-runtime-context`。GREEN：同命令通过（1 file / 11 tests）；相邻验证 `npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/stores/useChatStore.test.ts` 通过（4 files / 64 tests）；`npm run build` 通过；`npm test` 通过（34 files / 280 tests，仅 Browserslist/caniuse-lite 过期提示）；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（110 tests，2 doctests ignored）；IDE `build_project` 针对本轮前端/i18n 文件通过；`git diff --check` 与 `git diff --cached --check` 退出 0，前者仅输出既有 LF/CRLF 转换提示。未做真实 Tauri 桌面视觉点测。
- 下一步候选：1) 真实 Tauri 桌面点测右侧状态面板：Claude/Codex 切换后模型、权限、推理、工作区、SDK 和 MCP 展开详情是否同步刷新；2) 增加用户触发的 MCP 手动连通性检测入口，点击后调用 `check_mcp_status` 并展示 online/offline/timeout/error/耗时，明确区别于配置可用性；3) 给模型 selector 增加用户触发的“刷新模型”入口，调用当前 active Provider 的 `fetch_models` 并写入本地自定义模型缓存；4) 给 Git chip 增加可选 dirty/clean 只读状态，但必须评估扫描成本，避免影响大会话首屏。

## 本轮 PLAN 2026-06-19 chat mcp manual connectivity check

- 目标：在 Chat 右侧 MCP 展开详情中增加用户触发的真实连通性检测入口，复用现有 `check_mcp_status(server_ids)` 命令，展示当前 provider 已启用 MCP server 的 online/offline/timeout/error/unknown 和延迟。
- 功能点 1：确认命令与状态边界。验证方式：CodeGraph 读取 `check_mcp_status`、`mcp_status_service`、`chatMcpStatus`、`StatusPanel`、`ChatPage`，确认检测会启动 stdio MCP，只能由用户点击触发。
- 功能点 2：补 RED 测试。验证方式：新增 `chatMcpConnectivity` 工具测试覆盖结果归一化、错误截断和 serverId 映射；扩展 `StatusPanel.test.tsx` 覆盖手动检测按钮、checking 状态、online/latency 结果。
- 功能点 3：实现最小交互增强。验证方式：`ChatPage` 维护 page-local MCP 检测状态，只检测当前 provider 已启用的 server id；`StatusPanel` 展示检测按钮和每个 server 的最近结果，不自动检测。
- 功能点 4：同步 i18n 与 Trellis 规范。验证方式：新增中英文文案，更新 `.trellis/spec/frontend/component-guidelines.md` / `.trellis/spec/frontend/state-management.md`。
- 功能点 5：质量验证。验证方式：运行定向 MCP/StatusPanel 测试、全量前端测试、前端构建、Rust check/test、IDE 构建与空白检查。

## 迭代记录 2026-06-19 chat mcp manual connectivity check

- 上轮实际进展：右侧 `StatusPanel` 已补齐运行上下文，能展示当前模型、权限模式、推理强度、工作区路径和 SDK 安装状态，并通过定向/全量前端测试、构建、Rust check/test、IDE 构建和空白检查；阻塞仍是真实 Tauri 桌面视觉/交互点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：Chat 右侧 MCP 区域此前只有配置可用性（enabled/total、server 列表、transport、enabled/disabled），没有真实连通性结果。后端已有 `check_mcp_status(server_ids)` 和 `mcp_status_service`，但前端没有调用入口，用户无法在 Chat 内确认 MCP server 是否能实际启动或连接。
- 本轮诊断（超越 cc-gui）：真实检测不能自动触发，因为 stdio MCP 检测会启动命令并立即 kill；HTTP/SSE 检测也会发请求。把检测做成 MCP details 内的显式按钮，并且只检测当前 provider 已启用的 server，可让用户在发送前获得可信 live 状态，同时避免进入 Chat 或展开详情就产生副作用。
- 本轮规划：只做 MCP 手动 live 检测入口，不改后端命令、不做自动轮询。优先级高：用户价值高（发送前能区分“已启用配置”和“真的可用”）、出现频率中高（MCP 配置变化或切 provider 后会用）、实现成本低到中（复用既有 Tauri 命令 + page-local state）、风险可控（显式点击、stale response 防护）。
- 本轮完成：`src/utils/chatMcpConnectivity.ts` 新增前端 MCP live status 类型、`checkChatMcpConnectivity(serverIds)` 命令封装、结果按 serverId 索引和错误压缩工具；`src/utils/chatMcpConnectivity.test.ts` 覆盖结果映射和错误截断。`src/pages/ChatPage.tsx` 新增 page-local `mcpConnectivity` 状态、enabled server target 派生、手动检测 handler 和 stale-response 防护，provider/target 变化时清空旧结果。`src/components/chat/StatusPanel.tsx` 在 MCP details 内新增 `status-mcp-check` 按钮、checking 状态、错误诊断，以及每个 enabled server 的 live status/latency pill；disabled server 不显示 live 结果，避免误导。`src/components/chat/StatusPanel.test.tsx` 增加手动检测按钮和 online/latency 展示回归。`src/locales/en.json` / `src/locales/zh.json` 新增 live 检测文案。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 同步记录“手动检测、只测当前 provider 已启用 server、忽略过期响应”的契约。新增文件已执行 `git add -- src/utils/chatMcpConnectivity.ts src/utils/chatMcpConnectivity.test.ts`。
- 本轮验证：RED：`npm test -- src/utils/chatMcpConnectivity.test.ts src/components/chat/StatusPanel.test.tsx` 先失败于缺少 `chatMcpConnectivity` 模块和 `status-mcp-check`。GREEN：同命令通过（2 files / 14 tests）；相邻验证 `npm test -- src/utils/chatMcpConnectivity.test.ts src/utils/chatMcpStatus.test.ts src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx` 通过（4 files / 22 tests）；`npm test` 通过（35 files / 283 tests，仅 Browserslist/caniuse-lite 过期提示）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（110 tests，2 doctests ignored）；IDE `build_project` 针对本轮前端/i18n 文件通过；`git diff --check` 与 `git diff --cached --check` 退出 0，前者仅输出既有 LF/CRLF 转换提示。未做真实 Tauri 桌面点击 `check_mcp_status` 点测，因此真实 MCP 命令启动/HTTP 请求结果仍未人工确认。
- 下一步候选：1) 真实 Tauri 桌面点测 MCP live 检测：无 enabled server、stdio 成功/命令不存在、HTTP/SSE online/offline/timeout、切 provider 时 stale result 是否清空；2) 给模型 selector 增加用户触发的“刷新模型”入口，调用当前 active Provider 的 `fetch_models` 并写入本地自定义模型缓存；3) 给 Git chip 增加可选 dirty/clean 只读状态，但必须评估扫描成本，避免影响大会话首屏；4) 做真实桌面全链路点测清单：MCP details、运行上下文、输入区状态条、历史大会话首屏、图片 lightbox、diff Copy Path。

## 本轮 PLAN 2026-06-19 chat model selector manual refresh

- 目标：给 Chat 模型选择器增加用户触发的动态模型刷新入口，复用现有 `fetch_models(url, apiKey)` 命令，将当前 active provider 返回的模型列表写入 `ccg-chat-custom-models:<provider>` 缓存，并刷新 selector 选项。
- 功能点 1：确认现状与数据来源。验证方式：CodeGraph 读取 `ChatComposer`、`ButtonArea`、`SelectorDropdown`、`chatModels`、Provider store 和 `fetch_models` 命令，确认当前列表来自 Provider 配置 + localStorage + fallback，但缺少 Chat 内刷新入口。
- 功能点 2：补 RED 测试。验证方式：扩展 `chatModels.test.ts` 覆盖远端模型缓存序列化/事件 key；扩展 `ChatComposer.render.test.tsx` 或 `ButtonArea` 测试覆盖刷新按钮、loading、错误文案。
- 功能点 3：实现最小交互。验证方式：`ChatComposer` 从当前 active provider 读取 `apiBaseUrl/baseUrl/url` 与 `apiKey/key`，点击刷新时调用 `fetch_models`，成功后写入本地缓存并触发现有 `localStorageChange`，失败在模型菜单 footer 显示短错误；不自动拉取、不输出密钥。
- 功能点 4：同步 i18n 与 Trellis 规范。验证方式：新增中英文文案，更新 `.trellis/spec/frontend/component-guidelines.md` / `.trellis/spec/frontend/state-management.md` 的模型选择器契约。
- 功能点 5：质量验证。验证方式：运行定向模型/Composer 测试、前端全量测试、前端构建、Rust check/test、IDE 构建与空白检查。

## 迭代记录 2026-06-19 chat model selector manual refresh

- 上轮实际进展：右侧 MCP 展开详情已增加用户触发的 live 连通性检测，复用 `check_mcp_status(server_ids)`，只检测当前 provider 已启用 servers，并通过前端/Rust/IDE/空白检查；阻塞仍是真实 Tauri 桌面 MCP 点击点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：模型选择器此前已经有模型图标、Provider 配置模型和 localStorage 缓存合并，但 Chat 内没有用户触发的动态刷新入口；用户只能依赖静态 fallback 或先去 Provider/Token 页面获取模型，和“在输入区快速确认/切换模型”的使用场景不匹配。
- 本轮诊断（超越 cc-gui）：模型发现会带着 Provider API key 访问远端 `/v1/models`，因此不能在打开 Chat、切 provider 或展开下拉时自动调用。本轮把它做成模型 selector 旁的显式图标按钮，只有当前 provider 同时有 `url` 和 `apiKey` 才显示；成功结果写入本地缓存，失败保留现有模型列表。
- 本轮规划：只做手动刷新当前 provider 模型，不做自动轮询、不改后端命令、不把模型列表提升到全局 store。优先级高：用户价值高（解决模型列表写死/滞后）、出现频率高（每次切模型都相关）、实现成本低到中（复用现有 `fetch_models` 和 `chatModels`）、风险可控（显式点击、错误不清空现有列表、不输出密钥）。
- 本轮完成：`src/utils/chatModels.ts` 新增 `getChatModelRefreshSource()`、`normalizeFetchedChatModelIds()`、`storeFetchedChatModels()`，负责从当前 Provider 解析刷新源、去重远端模型 id、写入 `ccg-chat-custom-models:<provider>` 并 dispatch `localStorageChange`。`src/components/chat/composer/ButtonArea.tsx` 在模型 selector 后增加 icon-only 刷新按钮，支持刷新中 spinner 和错误 title，模型下拉 footer 继续展示 loading/error。`src/components/chat/composer/ChatComposer.tsx` 从 active Provider 取 `url`/`apiKey`，点击后调用 `fetch_models`，成功写缓存并刷新模型选项，空结果/保存失败/请求失败均保留已有模型。`src/locales/en.json` / `src/locales/zh.json` 新增刷新文案。`src/utils/chatModels.test.ts`、`src/components/chat/composer/ButtonArea.test.tsx`、`src/components/chat/composer/ChatComposer.render.test.tsx` 增加回归测试。`.trellis/spec/frontend/component-guidelines.md`、`.trellis/spec/frontend/state-management.md`、`.trellis/spec/backend/cross-layer-protocol.md` 同步记录模型刷新契约。
- 本轮验证：RED：`npm test -- src/utils/chatModels.test.ts src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx` 先失败 6 项，分别证明缺少 refresh source/cache helper、刷新按钮、刷新状态和 ChatComposer 接线。GREEN：同命令通过（3 files / 15 tests）；相邻验证 `npm test -- src/utils/chatModels.test.ts src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ChatComposer.test.ts src/components/chat/composer/useCompletions.test.ts` 通过（5 files / 31 tests）；`npm test` 通过（35 files / 289 tests，仅 Browserslist/caniuse-lite 过期提示）；`npm run build` 首次失败于测试漏传 `onModelChange`，补齐后通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（110 tests，2 doctests ignored）；IDE `build_project` 通过；`git diff --check` 与 `git diff --cached --check` 退出 0，前者仅输出既有 LF/CRLF 转换提示。未做真实 Tauri 桌面点击模型刷新点测，也未对真实 Provider API 做网络验证。
- 下一步候选：1) 真实 Tauri 桌面点测模型刷新：active provider 有/无 URL+API key、成功返回模型、空列表、401/网络失败、刷新后 selector 是否立即出现新模型；2) 真实 Tauri 桌面点测 MCP live 检测：无 enabled server、stdio 成功/命令不存在、HTTP/SSE online/offline/timeout、provider 切换 stale result；3) 针对历史大会话加载慢继续做性能诊断，优先检查 session list/meta 缓存、消息窗口化首屏、点击会话后的重复解析；4) Git chip 后续可增加 dirty/clean 只读状态，但必须先评估扫描成本，避免拖慢大会话首屏。

## 本轮 PLAN 2026-06-19 chat large history cache reuse

- 目标：优化点击历史大会话后的二次加载速度，优先消除 `useChatStore.loadSession()` 命中历史缓存时仍对完整 `messages` 数组做 O(n) 浅拷贝的问题。
- 功能点 1：确认慢点与对标边界。验证方式：CodeGraph 读取 `useChatStore.loadSession()`、`sessionHistoryCache`、`MessageList` 窗口化路径，并读取 cc-gui 历史加载/索引路径，区分代码中确实存在的问题与记录中声称完成的能力。
- 功能点 2：补 RED 测试。验证方式：扩展 `src/stores/useChatStore.test.ts`，构造 5000 条历史消息，断言第二次加载同一 session 复用缓存数组引用且不再调用 `get_unified_session_messages`。
- 功能点 3：实现最小性能改动。验证方式：`sessionHistoryCache` 保存/返回已映射历史数组引用，不再在缓存读写时全量 clone；保留 LRU 刷新和 `lastActiveAt` cache key 失效规则。
- 功能点 4：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/state-management.md` 与 `.trellis/spec/frontend/component-guidelines.md`，记录大会话缓存复用与不可原地 mutation 的约束。
- 功能点 5：质量验证。验证方式：运行定向 store 测试、相邻 Chat navigation/status 测试、`npm test`、`npm run build`、Rust check/test、IDE 构建与空白检查。

## 迭代记录 2026-06-19 chat large history cache reuse

- 上轮实际进展：模型选择器已改为图标化并支持用户手动动态刷新模型，MCP 展开详情已有手动 live 检测，输入框上方状态条已支持任务/子代理/编辑触发式显示和 Git 分支只读显示，右侧状态面板已补运行上下文。上述能力在代码中可见并已通过自动化验证；阻塞仍是真实 Tauri 桌面点击点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 历史侧有后端索引缓存与前端虚拟列表，避免大会话重复做重活。本项目虽已有最近窗口渲染和 session list 缓存，但 `src/stores/useChatStore.ts` 的 `sessionHistoryCache` 在写入和命中读取时都会 `messages.map({...message})` 全量浅拷贝；几千条历史会话重复点击或切回来时，不再读后端但仍有 O(n) CPU 和内存分配。
- 本轮诊断（超越 cc-gui）：本项目可以保留完整 transcript 的功能语义，同时让普通浏览路径复用已映射消息数组引用。用户痛点是点击大会话后列表和 transcript 迟迟不稳定；去掉缓存命中的重复数组克隆能直接降低二次进入同一历史会话的等待和 GC 抖动。
- 本轮规划：只优化 store 历史缓存读写，不改后端 `get_unified_session_messages`、不引入分页协议、不调整窗口化渲染。优先级高：用户价值高（直接命中“点击历史大会话慢”）、出现频率高（历史切换常用）、成本低（两处缓存 helper）、风险可控（保留 `lastActiveAt` cache key 和 LRU，新增回归测试约束 immutable usage）。
- 本轮完成：`src/stores/useChatStore.test.ts` 新增 5000 条历史消息回归测试，断言第二次加载同一 session 不再调用后端且复用第一次映射出的 `messages` 数组引用。`src/stores/useChatStore.ts` 删除缓存读写阶段的全量浅拷贝，`rememberSessionHistory()` 保存已映射数组引用，`getCachedSessionHistory()` 命中后刷新 LRU 并直接返回该引用。`.trellis/spec/frontend/state-management.md` 与 `.trellis/spec/frontend/component-guidelines.md` 已同步大会话缓存复用契约：状态更新继续不可变替换，不允许组件或 store action 原地 mutation 历史消息。
- 本轮验证：RED：`npm test -- src/stores/useChatStore.test.ts` 先失败于新增用例 `reuses cached large history without cloning the message array`，证明当前缓存命中仍返回新数组引用。GREEN：同命令通过（1 file / 48 tests）；相邻验证 `npm test -- src/stores/useChatStore.test.ts src/utils/chatNavigation.test.ts src/utils/chatStatusSummary.test.ts` 通过（3 files / 67 tests）；`npm test` 通过（35 files / 290 tests，仅 Browserslist/caniuse-lite 过期提示）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（110 tests，2 doctests ignored）。未做真实 Tauri 桌面点击大会话性能点测，后端首读超大历史仍可能受文件解析成本影响。
- 下一步候选：1) 真实 Tauri 桌面点测历史大会话：首次打开、重复点击同一 session、切 A/B 大会话、进入搜索全历史模式的体感耗时；2) 若首次打开仍慢，继续做后端分页或轻量首屏加载：先加载最近 N 条 + 后台补全全文，但需要设计跨层协议；3) 给 session 详情加载加性能埋点或开发态日志，分辨后端 JSONL 解析、前端 map、React commit、状态摘要计算各自耗时；4) 继续补真实桌面点测清单：模型刷新、MCP live 检测、Git 状态条、右侧运行上下文。

## 本轮 PLAN 2026-06-19 chat large history first-paint window

- 目标：优化历史大会话首次打开速度。`loadSession()` 未命中缓存时先加载最近消息窗口用于首屏显示，再后台补齐完整历史并写入缓存，避免首屏等待完整 JSONL 读取结果通过 IPC 和前端全量映射。
- 功能点 1：确认对标与根因边界。验证方式：CodeGraph 读取本项目 `loadSession()` / `MessageList` / provider 历史解析与 cc-gui `MessageList` / HistoryReader，区分已具备的渲染窗口化与仍缺的数据窗口化。
- 功能点 2：补 RED 测试。验证方式：扩展 `src/stores/useChatStore.test.ts`，模拟 5000 条完整历史和 120 条窗口历史，断言 window promise resolve 后 store 已显示窗口消息且 pending 清空，full promise resolve 后替换为完整历史并缓存；同时覆盖用户在 full 未返回前发送新消息时旧 full 结果不会覆盖当前 transcript。
- 功能点 3：新增最小跨层命令。验证方式：Rust 新增 `get_unified_session_message_window(providerId, sourcePath, tailLimit)` 和 `session_manager::load_message_window()`，返回 `{messages,startIndex,totalCount,complete}`；Claude/Codex 解析复用现有行级语义，只保留尾部窗口，避免 IPC 传完整历史。
- 功能点 4：前端接入首屏窗口。验证方式：`useChatStore.loadSession()` 先请求窗口并按 `startIndex` 生成稳定 history id，窗口不完整时继续请求 full；full 到达后写入 `sessionHistoryCache` 并替换；缓存命中仍直接复用完整数组。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/backend/cross-layer-protocol.md` 与 `.trellis/spec/frontend/state-management.md` 的大会话首屏窗口契约。
- 功能点 6：质量验证。验证方式：运行定向 store/Rust session tests、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE 构建与空白检查。

## 迭代记录 2026-06-19 chat large history first-paint window

- 上轮实际进展：历史大会话二次进入已优化为复用 `sessionHistoryCache` 中的完整 `ChatMessage[]` 引用，不再在缓存命中时做 O(n) 浅拷贝；模型 selector 图标化 + 手动动态刷新、MCP 展开详情 + 手动 live 检测、输入框上方任务/子代理/编辑/Git 触发式状态条、右侧运行上下文均已在代码中存在并通过自动化验证。已知阻塞仍是真实 Tauri 桌面点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 `webview/src/components/MessageList.tsx` 有最近 15 条 + 每次 reveal 30 条的窗口渲染，后端历史 reader/index service 也减少 session list/meta 成本。本项目 `src/components/chat/MessageList.tsx` 已有渲染窗口化，但 `src/stores/useChatStore.ts` 的 `loadSession()` 未命中缓存时仍先等待完整 `get_unified_session_messages` 返回并完成前端全量 map，导致首次点击大会话时首屏被 JSONL 全量解析、IPC 传输和前端映射共同拖慢。
- 本轮诊断（超越 cc-gui）：首屏先显示最近 120 条统一历史消息，后台再补齐完整 transcript，可在保留完整历史、搜索和状态摘要能力的同时降低用户感知等待。用户痛点是“点击历史大会话后列表迟迟不出现”；窗口首屏让最近上下文先可读，full load 只负责补齐旧内容和缓存。
- 本轮规划：只做历史消息数据层首屏窗口，不引入通用分页 UI、不改变搜索全历史入口、不重写 session list。优先级高：用户价值高（直击首次打开慢）、出现频率高（历史切换常用）、成本中等（新增一个 Tauri 命令 + provider tail parser + store 接线）、风险可控（token 防迟到覆盖、完整历史仍后台加载）。
- 本轮完成：`src/types/session.ts` 新增 `UnifiedSessionMessageWindow`。`src-tauri/src/session_manager/mod.rs` 新增 `UnifiedSessionMessageWindow`、`MessageWindowBuilder`、`load_message_window()` 和 tailLimit 1..500 归一化，Gemini 保留 full-load 后裁剪兜底。`src-tauri/src/session_manager/providers/claude.rs` / `codex.rs` 抽出行级 parser 并新增 tail window loader；Codex fallback tool id 改为基于原始解析序号，保证 window 与 full 一致。`src-tauri/src/commands/session_commands.rs` 新增 `get_unified_session_message_window(providerId, sourcePath, tailLimit)`，`src-tauri/src/lib.rs` 注册命令。`src/stores/useChatStore.ts` 的 `loadSession()` 未命中缓存时先请求 120 条窗口并用 `startIndex` 映射稳定 history id，`complete=false` 时后台请求 full、写入完整缓存并替换；窗口阶段和 full 阶段都使用 session-load token 防止迟到响应覆盖用户新 prompt。`src/stores/useChatStore.test.ts` 新增大会话首屏窗口和迟到 full 防护回归。`.trellis/spec/frontend/state-management.md`、`.trellis/spec/frontend/component-guidelines.md`、`.trellis/spec/backend/cross-layer-protocol.md` 已同步首屏窗口跨层契约。
- 本轮验证：RED：`npm test -- src/stores/useChatStore.test.ts` 先失败于新增用例未观察到 `get_unified_session_message_window` 调用和窗口首屏状态。GREEN：同命令通过（1 file / 50 tests）；相邻验证 `npm test -- src/stores/useChatStore.test.ts src/utils/chatNavigation.test.ts src/utils/chatStatusSummary.test.ts` 通过（3 files / 69 tests）；Rust 定向 `cargo test --manifest-path src-tauri/Cargo.toml session_manager::providers::claude -- --nocapture` 通过（5 tests），`cargo test --manifest-path src-tauri/Cargo.toml session_manager::providers::codex -- --nocapture` 通过（11 tests）；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`npm test` 通过（35 files / 292 tests，仅 Browserslist/caniuse-lite 过期提示）；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（112 tests，2 doctests ignored）；`npm run build` 通过；IDE `build_project` 通过；`git diff --check` 与 `git diff --cached --check` 退出 0，前者仅输出既有 LF/CRLF 转换提示。未做真实 Tauri 桌面点击大会话性能点测，因此首次打开的体感耗时改善仍需桌面态确认。
- 下一步候选：1) 真实 Tauri 桌面点测历史大会话：首次打开、full 补齐前后滚动/reveal、重复点击缓存命中、A/B 大会话切换、搜索全历史模式；2) 给历史加载加开发态性能分段埋点，拆分后端 JSONL 解析、IPC 序列化、前端 map、React commit、状态摘要计算耗时；3) 继续检查 MCP 展开/模型刷新/输入区状态条/Git chip 的真实桌面交互，覆盖用户反馈的“点击 MCP 无法展开”“动态模型列表”“状态栏触发显示隐藏”；4) 若首屏窗口仍不够快，再设计真正的后端分页/按需 reveal 协议，但需单独评估搜索、锚点、tool_result 查找和状态摘要的完整性。

## 本轮 PLAN 2026-06-19 chat mcp details controlled expansion

- 目标：修复 Chat 右侧 MCP 区域“点击无法展开查看”的可靠性问题，把当前依赖原生 `<details>` 的展开交互改为显式受控按钮，确保用户点击 MCP 行能看到 server 名称、transport/config type、enabled/disabled 和手动 live 检测入口。
- 功能点 1：确认根因与对标方式。验证方式：CodeGraph 读取本项目 `StatusPanel` / `StatusPanel.test.tsx` / `ChatPage` MCP 链路，并读取 cc-gui `ServerCard` 的受控展开模式，区分静态渲染存在与真实点击可靠性。
- 功能点 2：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，断言普通 MCP summary 初始渲染为显式 `button`、带 `aria-expanded="false"`，且不把 server 详情直接铺在初始 DOM；loading/error/checking/live result 状态应自动展开以保留诊断。
- 功能点 3：实现最小修复。验证方式：`StatusPanel` 用 `useState` 管理 MCP details 展开状态，用 `button.status-mcp-toggle` 替代 `<details>/<summary>`，在 loading/error/checking/已有 live result 时自动展开；检测按钮阻止冒泡，避免点击 live check 时误折叠。
- 功能点 4：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 的 MCP 展开契约，明确 Chat MCP details 必须是显式受控交互。
- 功能点 5：质量验证。验证方式：运行定向 `StatusPanel` / MCP 工具测试、前端全量测试、前端构建、Rust check、IDE 构建与空白检查；如能启动前端，再做浏览器/桌面点击 smoke。

## 迭代记录 2026-06-19 chat mcp details controlled expansion

- 上轮实际进展：历史大会话首屏窗口、模型 selector 图标化与手动动态刷新、MCP 手动 live 检测、输入框上方任务/子代理/编辑/Git 触发式状态条、右侧运行上下文都已在代码中存在并通过自动化验证；阻塞仍是真实 Tauri 桌面点测不足。用户继续反馈“点击 MCP 无法展开查看”，本轮复查确认这不是能力完全缺失，而是 `StatusPanel` MCP 详情依赖原生 `<details>`，测试仅覆盖静态 HTML，未覆盖显式点击入口和受控展开状态。`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 MCP server card 使用受控 `isExpanded` 和整行 header click，展开内容由 React 条件渲染；本项目虽然在 DOM 中有 MCP server 明细，但普通状态下是原生 `<details>` 子树，且没有稳定的 `button` / `aria-expanded` 可测入口。固定右侧面板与原生 disclosure 的组合会让“代码里有详情”不等于“真实点击可靠展开”。
- 本轮诊断（超越 cc-gui）：本项目保留普通状态下的轻量 collapsed summary，只在用户点击后渲染 server 明细；loading、error、checking 或已有 live result 会自动展开，避免诊断信息被折叠隐藏。这样既减少默认右侧噪音，又让异常和连通性结果立即可见。
- 本轮规划：只修复 MCP details 展开可靠性，不扩大到小屏 composer 状态条、不改 `check_mcp_status` 后端、不增加自动检测。优先级高：用户价值高（直接解决用户点不开）、出现频率中高（MCP 配置和发送前检查常用）、实现成本低（单组件受控状态 + 样式选择器 + 测试）、风险低（不改变数据来源和后端命令）。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 新增/调整 MCP 展开回归：普通 MCP summary 必须渲染 `status-mcp-toggle`、`aria-expanded="false"`，初始 DOM 不铺开 server 明细；loading/error/checking/live result 自动 `aria-expanded="true"` 并渲染 details。`src/components/chat/StatusPanel.tsx` 用 `mcpExpanded` 本地状态替代原生 `<details>`，summary 改为整行 `button.status-mcp-toggle`，详情区条件渲染；loading/error/checking/hasResults 自动展开，live check 按钮阻止冒泡并复用现有 handler。`src/styles/toolBlocks.css` 将 chevron 旋转从 `[open]` 选择器改为 `.status-mcp-summary-expanded`。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 已记录 Chat MCP details 必须使用显式受控按钮与本地 UI state。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 3 项，失败点为缺少 `status-mcp-toggle`，证明旧实现没有显式受控展开入口。GREEN：同命令通过（1 file / 12 tests）；相邻验证 `npm test -- src/components/chat/StatusPanel.test.tsx src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/components/chat/ChatInputStatusTabs.test.tsx` 通过（4 files / 22 tests）；`npm test` 通过（35 files / 292 tests，仅 Browserslist/caniuse-lite 过期提示）；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`npm run build` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（112 tests，2 doctests ignored）；IDE `build_project` 通过；`git diff --check` 与 `git diff --cached --check` 退出 0，前者仅有既有 LF/CRLF 提示。`npm run build` 产生的未跟踪 `out/` 已确认位于工作区内并删除。未做真实 Tauri 桌面点击 MCP 展开点测，原因是本轮未启动桌面运行态；真实 WebView 下的点击仍需下一步 smoke 确认。
- 下一步候选：1) 真实 Tauri 桌面点测 MCP details：右侧可见窗口点击展开/收起、点击 live check 不折叠、provider 切换后 stale result 清空、loading/error 自动展开；2) 检查小屏或 `xl` 以下 Chat UI 是否需要把 MCP summary 入口放入 composer 状态条，否则右侧状态面板隐藏时用户仍无法查看 MCP；3) 给历史大会话首屏窗口加开发态性能分段埋点，拆分后端 JSONL、IPC、前端 map、React commit 和状态摘要耗时；4) 继续真实桌面点测模型刷新、Git chip、输入区任务/子代理/编辑展开项。

## 本轮 PLAN 2026-06-19 chat composer mcp status strip entry

- 目标：在 Chat 输入区状态条补 MCP 触发式入口，让 `xl` 以下右侧状态面板隐藏时，用户仍能查看 MCP 配置摘要和 server 明细。
- 功能点 1：确认差距与数据来源。验证方式：CodeGraph 读取 `ChatInputStatusTabs` / `ChatPage` / `chatMcpStatus` / `StatusPanel`，确认当前 MCP 只传给右侧面板，输入区状态条没有 MCP tab。
- 功能点 2：补 RED 测试。验证方式：扩展 `src/components/chat/ChatInputStatusTabs.test.tsx`，断言有 MCP server 时显示 MCP chip；展开后显示 server 名称、transport、enabled/disabled、enabled/total；无 server 且无 loading/error 时保持触发式隐藏。
- 功能点 3：实现最小前端入口。验证方式：`ChatInputStatusTabs` 增加 `mcpStatus` 可选 prop 和 `mcp` tab，本地 state 管理展开；只展示配置可用性，不触发 live check；`ChatPage` 传入现有 `mcpStatus`。
- 功能点 4：同步 i18n 与 Trellis 规范。验证方式：新增中英文输入区 MCP 文案，并更新 `.trellis/spec/frontend/component-guidelines.md` / `.trellis/spec/frontend/state-management.md`，记录小屏 composer 状态条承载 MCP 配置入口。
- 功能点 5：质量验证。验证方式：运行定向 `ChatInputStatusTabs` / `StatusPanel` / MCP utils 测试、前端全量测试、前端构建、Rust check/test、IDE 构建与空白检查；如未做真实桌面点测，明确标注。

## 迭代记录 2026-06-19 chat composer mcp status strip entry

- 上轮实际进展：历史大会话首屏窗口、缓存复用、模型 selector 图标化和手动动态刷新、MCP 右侧 details 受控展开与手动 live 检测、输入区任务/子代理/编辑/Git 触发式状态条、右侧运行上下文都已在代码中存在并通过自动化验证；阻塞仍是真实 Tauri 桌面视觉/点击点测不足。记录中提到的小屏 MCP 入口在本轮开始前确实不存在：`ChatPage` 只把 `mcpStatus` 传给右侧 `StatusPanel`，`ChatInputStatusTabs` 只支持 `tasks/subagents/edits` 三类可展开 tab，Git 是静态 chip。
- 本轮诊断（补齐 cc-gui）：cc-gui 在 composer 附近提供任务/文件/状态可见性；本项目右侧面板在 `xl` 以下隐藏后，用户无法从输入区查看 MCP server 配置明细。已有 `buildChatMcpAvailabilitySummary()` 能提供 server id/name/enabled/transport，缺口是输入区没有消费这份数据。
- 本轮诊断（超越 cc-gui）：输入区 MCP 入口保持触发式：有配置、loading 或 error 才出现；展开只展示配置可用性和 enabled/disabled，不运行 `check_mcp_status`，避免小屏查看详情时隐式启动 stdio MCP 进程或网络检测。
- 本轮规划：只做 `ChatInputStatusTabs` MCP tab 与 `ChatPage` 接线，不改后端、不新增 store、不把 live check 挪到输入区。优先级高：用户价值高（回应“小屏/右侧隐藏时 MCP 看不到、点击不可查看”）、出现频率中高（发送前常看 MCP 可用状态）、实现成本低（复用现有 summary）、风险低（纯前端展示）。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.test.tsx` 新增 MCP 状态条回归测试，覆盖有 server 时显示 `chat-input-status-tab-mcp`、默认展开时展示 server name/transport/enabled/disabled、空 idle MCP 配置不渲染占位。`src/components/chat/ChatInputStatusTabs.tsx` 新增 `mcpStatus` prop、`mcp` tab、MCP server 明细面板和紧凑 `enabled / total` 计数；展开状态仍由组件本地 `openTab` 管理。`src/pages/ChatPage.tsx` 把已存在的 `mcpStatus` 传给输入区状态条。`src/locales/en.json` / `src/locales/zh.json` 增加 MCP server 溢出文案。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 已记录 composer MCP 入口契约。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 2 项，失败点为旧组件返回空字符串，缺少 `chat-input-status-tab-mcp` 与 `chat-input-status-mcp-server`。GREEN：同命令通过（1 file / 8 tests）；相邻验证 `npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts` 通过（4 files / 25 tests）；`npm test` 通过（35 files / 295 tests，仅 Browserslist/caniuse-lite 过期提示）；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`npm run build` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（112 tests，2 doctests ignored）；IDE `build_project` 通过；`git diff --check` 与 `git diff --cached --check` 退出 0，其中 `git diff --check` 仅输出既有 LF/CRLF 转换提示。未做真实 Tauri 桌面小屏点击点测，也未做真实 MCP server 配置切换视觉点测。
- 下一步候选：1) 真实 Tauri 桌面点测小屏/窄宽度：右侧状态面板隐藏时 MCP chip 出现、点击展开/收起、loading/error/空配置边界；2) 真实 Tauri 桌面点测右侧 MCP live 检测与输入区只读 MCP 详情的分工，确认用户不会把配置可用性误解为在线状态；3) 给历史大会话首屏窗口加开发态性能分段埋点，拆分后端 JSONL、IPC、前端 map、React commit 和状态摘要耗时；4) 继续真实桌面点测模型刷新、Git chip、输入区任务/子代理/编辑展开项。

## 本轮 PLAN 2026-06-19 chat session load performance snapshot

- 目标：补齐历史大会话加载速度的可见诊断信息。记录最近一次历史 session 加载的 cache/window/full/map 阶段耗时，并在右侧状态面板展示，帮助判断“点击大会话慢”卡在缓存未命中、首屏窗口读取、前端映射还是后台 full 补齐。
- 功能点 1：确认差距与边界。验证方式：CodeGraph 读取 `useChatStore.loadSession()`、`StatusPanel`、`ChatPage` 和对标 cc-gui `MessageList` / `StatusPanel`，确认本轮只做性能诊断展示，不改变加载协议、不自动触发重新加载。
- 功能点 2：补 RED 测试。验证方式：扩展 `src/stores/useChatStore.test.ts` 覆盖 window/full/cache 指标；扩展 `src/components/chat/StatusPanel.test.tsx` 断言指标区渲染阶段耗时和 cache hit。
- 功能点 3：实现最小数据层。验证方式：`useChatStore` 新增最近一次 `sessionLoadMetrics`，在 cache hit、window resolve/map、full resolve/map、失败/完成路径按 token 保护更新；`startNewSession` / `clear` / `send` 清理陈旧诊断。
- 功能点 4：实现最小展示层。验证方式：`ChatPage` 将指标传给 `StatusPanel`，状态面板以只读 compact 模块展示 cache、window、full、message count 和 completed/error 状态。
- 功能点 5：同步 i18n 与 Trellis 规范。验证方式：新增中英文文案，并更新 `.trellis/spec/frontend/state-management.md` / `.trellis/spec/frontend/component-guidelines.md` 的历史加载诊断契约。
- 功能点 6：质量验证。验证方式：运行定向 store/status 测试、相邻测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、必要时 `cargo test --manifest-path src-tauri/Cargo.toml`、IDE 构建与空白检查；如未做真实 Tauri 桌面体感点测，最终明确标注。

## 迭代记录 2026-06-19 chat session load performance snapshot

- 上轮实际进展：历史大会话首屏窗口、缓存复用、模型 selector 图标化和手动动态刷新、MCP 右侧 details 受控展开与手动 live 检测、输入区任务/子代理/编辑/Git/MCP 触发式状态条、右侧运行上下文都已在代码中存在并通过自动化验证。已知阻塞仍是真实 Tauri 桌面视觉/点击点测不足。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：本项目已有 `get_unified_session_message_window` 首屏窗口和 `sessionHistoryCache` 缓存复用，但用户反馈“点击历史大会话加载到列表特别慢”仍缺可见证据。当前 UI 无法区分慢在 cache miss、窗口读取、前端 map、后台 full 读取还是 full map；因此每次优化后仍只能靠体感判断。
- 本轮诊断（超越 cc-gui）：把最近一次历史加载分段耗时放进右侧状态面板，能让用户和开发者直接看到“首屏窗口多少条 / 总计多少条 / window load / map / full load / full map / 总耗时”。这不是单纯美化，而是把大会话慢的问题变成可定位、可回归的诊断面板。
- 本轮规划：只做只读性能快照，不改后端协议、不加日志、不自动重新加载历史。优先级高：用户价值高（直击大会话慢且帮助后续定位）、出现频率中高（历史会话切换常用）、实现成本低（纯前端 store + 状态面板）、风险低（指标受 session-load token 保护，不改变 transcript 数据）。
- 本轮完成：`src/types/session.ts` 新增 `ChatSessionLoadMetrics`。`src/stores/useChatStore.ts` 新增 `lastSessionLoadMetrics`，在 `loadSession()` 的 initial/cache/window/full/error 路径记录 cacheHit、status、message counts、window/full read/map ms、elapsed ms，并在 send/startNewSession/clear/provider switch 清理陈旧指标；所有异步写入继续受 `latestSessionLoadToken` 保护。`src/pages/ChatPage.tsx` 将指标传给 `StatusPanel`。`src/components/chat/StatusPanel.tsx` 增加 `status-session-load-metrics` 只读模块，展示 cache hit 或 window/full 分段耗时与错误。`src/locales/en.json` / `src/locales/zh.json` 新增历史加载诊断文案。`src/stores/useChatStore.test.ts` 覆盖 cache hit 指标和 window/full 分段耗时，`src/components/chat/StatusPanel.test.tsx` 覆盖状态面板指标渲染。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 已记录该只读诊断契约。
- 本轮验证：RED：`npm test -- src/stores/useChatStore.test.ts src/components/chat/StatusPanel.test.tsx` 先失败 4 项，失败点为 `lastSessionLoadMetrics` 为 `null` 且状态面板缺少 `status-session-load-metrics`。GREEN：同命令通过（2 files / 66 tests）；相邻验证 `npm test -- src/stores/useChatStore.test.ts src/utils/chatNavigation.test.ts src/utils/chatStatusSummary.test.ts src/components/chat/StatusPanel.test.tsx` 通过（4 files / 85 tests）；`npm test` 通过（35 files / 299 tests，仅 Browserslist/caniuse-lite 过期提示）；`npm run build` 通过；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（112 tests，2 doctests ignored）；IDE `build_project` 通过；`git diff --check` 与 `git diff --cached --check` 退出 0，其中 `git diff --check` 仅输出既有 LF/CRLF 转换提示。`npm run build` 生成的未跟踪 `out/` 已确认位于工作区内并删除。未做真实 Tauri 桌面点击大会话体感点测。
- 下一步候选：1) 真实 Tauri 桌面点测 1k/5k/10k 历史会话：首次打开、cache hit 重复点击、A/B 大会话切换、full 补齐后滚动和搜索全历史；2) 若指标显示 window load 仍慢，继续做 provider 级 tail reader 优化或真正分页/reveal 协议；3) 若指标显示 full load/map 慢但首屏可用，给状态面板增加“full background still loading”的更明确提示；4) 继续真实桌面点测模型刷新、MCP live 检测、Git chip、输入区任务/子代理/编辑/MCP 展开项。

## 本轮 PLAN 2026-06-19 chat input status full-history trigger restore

- 目标：修复长历史/折叠窗口下输入区任务、子代理、编辑状态入口“代码存在但用户看不到”的问题，同时保留大会话首屏窗口化性能优化。
- 功能点 1：确认差距与边界。验证方式：复查 `ChatPage` 当前只用可见尾部 `statusMessages` 构建 `statusSummary`，因此早期工具/编辑记录会让输入区触发按钮消失；对照 cc-gui 输入区与状态可见性，确认本轮只修输入区状态条，不重写工具块或后端历史协议。
- 功能点 2：补 RED 测试。验证方式：扩展 `src/utils/chatStatusSummary.test.ts`，断言输入区状态摘要能从“首屏可见摘要 + 空闲补全的全量摘要”合并出任务、子代理和编辑入口数据。
- 功能点 3：实现最小前端修复。验证方式：新增共享合并 helper；`ChatPage` 保持右侧状态面板使用首屏可见摘要，另在浏览器 idle 阶段构建完整摘要并仅传给 `ChatInputStatusTabs`，避免首屏同步扫描全量历史。
- 功能点 4：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md`，记录输入区状态条的“首屏摘要 + idle 完整摘要”契约。
- 功能点 5：质量验证。验证方式：运行定向 status summary / input tabs / ChatPage 相邻测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、必要时 `cargo test --manifest-path src-tauri/Cargo.toml`、IDE 构建与空白检查；未做真实 Tauri 桌面点测需明确标注。

## 迭代记录 2026-06-19 chat input status full-history trigger restore

- 上轮实际进展：记录中声称历史大会话首屏窗口、缓存复用、模型 selector 图标化/动态刷新、MCP 右侧 details 与输入区 MCP tab、输入区任务/子代理/编辑/Git 触发式状态条、右侧运行上下文都已存在。代码复查确认这些能力确实在 `ChatPage`、`ChatInputStatusTabs`、`StatusPanel`、`chatMcpStatus`、`chatWorkspaceStatus` 等文件中存在；但输入区任务/子代理/编辑的触发条件只依赖 `ChatPage` 为首屏性能裁剪后的可见尾部 `statusSummary`，因此长历史里早期工具/编辑记录会被折叠窗口隐藏，用户看到的效果就是“功能依旧没有”。已知阻塞仍是真实 Tauri 桌面视觉/点击点测不足。本轮复查确认 `C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 可读，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在。
- 本轮诊断（补齐 cc-gui）：cc-gui 的输入区附近不把历史活动入口和首屏渲染窗口强绑定。本项目在大会话优化后，`StatusPanel` 和输入区状态条共享同一个可见尾部摘要，导致早期 `Task` / `Agent` / `Edit` 无法触发输入区按钮。Git 分支显示链路代码存在：后端 `chat_workspace_status` 能从 `.git/HEAD` 和 `gitdir:` 文件读取分支，前端只有在 `currentCwd` 已解析到 Git 项目且返回分支时显示；初始空会话没有 cwd 时按触发式规则隐藏。
- 本轮诊断（超越 cc-gui）：输入区状态条既要能从完整历史恢复“任务 / 子代理 / 编辑”入口，又不能把全量 `buildChatStatusSummary(messages)` 放回首屏同步路径。否则会抵消大会话首屏优化。本轮采用“可见尾部摘要立即渲染 + idle 完整摘要补齐输入区触发”的折中：右侧状态面板保持轻量首屏摘要，输入区在浏览器空闲后获得全量历史中的任务/子代理/编辑入口。
- 本轮规划：只修输入区状态条的长历史触发缺口，不改后端历史协议、不改 MCP live 检测、不改变 Git chip 的“有 Git 才显示”规则。优先级高：用户价值高（直接回应任务/子代理/编辑入口缺失）、出现频率中高（长历史/折叠窗口常见）、实现成本低（纯前端 helper + page-local idle state）、风险可控（不阻塞首屏、不改变右侧状态面板数据）。
- 本轮完成：`src/utils/chatStatusSummary.ts` 新增 `mergeChatInputStatusSummary()`，把首屏可见摘要与空闲阶段完整摘要合并给输入区状态条使用，并保留可见窗口中的 pending active tool 优先级。`src/pages/ChatPage.tsx` 新增 page-local `completeStatusSummaryState`，在非搜索、非空 transcript 下通过 `requestIdleCallback`（无支持时 fallback `setTimeout`）构建完整 `buildChatStatusSummary(messages)`，只将合并结果传给 `ChatInputStatusTabs`；`StatusPanel` 仍使用轻量 `statusSummary`。`src/utils/chatStatusSummary.test.ts` 新增回归测试，覆盖可见窗口没有工具但完整历史有 Task/Edit 时，输入区摘要仍能恢复任务、子代理和编辑入口数据。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 已同步该契约。
- 本轮验证：RED：`npm test -- src/utils/chatStatusSummary.test.ts` 先失败 1 项，失败点为 `mergeChatInputStatusSummary is not a function`。GREEN：同命令通过（1 file / 7 tests）；`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 通过（1 file / 8 tests）；相邻验证 `npm test -- src/utils/chatStatusSummary.test.ts src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatUiBehavior.test.ts src/utils/chatWorkspaceStatus.test.ts` 通过（4 files / 32 tests）；`npm run build` 通过；`npm test` 通过（35 files / 300 tests，仅 Browserslist/caniuse-lite 过期提示）；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（112 tests，2 doctests ignored）；IDE `build_project` 通过；`git diff --check` 与 `git diff --cached --check` 退出 0，其中 `git diff --check` 仅输出既有 LF/CRLF 转换提示。`npm run build` 生成的未跟踪 `out/` 已确认位于工作区内并删除。未做真实 Tauri 桌面长历史点击、Git chip、MCP 展开或输入区状态条视觉点测。
- 下一步候选：1) 真实 Tauri 桌面点测：打开含早期 Task/Agent/Edit 的大会话，确认首屏后输入区状态条在 idle 补齐后出现任务/子代理/编辑，点击可展开；2) 点测 Git chip：选中带 `projectDir` 的历史会话或新会话 cwd 后显示当前分支，非 Git cwd 不显示；3) 点测 MCP：右侧 details 展开/收起、live check 不折叠，小屏输入区 MCP 只读 tab 可展开；4) 如果用户仍反馈大会话慢，用 `lastSessionLoadMetrics` 的分段数据决定是继续优化 provider tail reader，还是把完整摘要构建移到 worker/增量索引。

## 本轮 PLAN 2026-06-19 chat claude provider glyph parity

- 目标：修正 Chat 输入区 provider 切换按钮的 Claude 图标，替换上一轮手写近似 placeholder，按 cc-gui 的 `@lobehub/icons@5.8.0` Claude glyph 形态渲染。
- 功能点 1：确认真实对标与根因。验证方式：读取 cc-gui `ProviderModelIcon.tsx` / `ProviderSelect.tsx`，确认 Claude 使用 `ClaudeColor` / `ClaudeMono`；读取本项目 `ButtonArea.tsx`，确认当前旧 path 不是 cc-gui glyph。
- 功能点 2：补 RED 测试。验证方式：扩展 `src/components/chat/composer/ButtonArea.test.tsx`，断言 Claude provider button 使用 cc-gui Claude glyph 标记与 path，并且不包含旧 placeholder path。
- 功能点 3：实现最小展示层修复。验证方式：只替换 `ProviderBrandIcon` 的 Claude SVG path/fill，保留 `data-chat-provider-icon`、Codex 图标、provider 状态和模型刷新逻辑不变。
- 功能点 4：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 Claude provider selector 不得使用 generic/starburst placeholder。
- 功能点 5：质量验证。验证方式：运行定向 `ButtonArea` 测试、相邻 composer 测试、全量前端测试、前端构建、Rust check、IDE 构建与空白检查；未做真实 Tauri 桌面视觉点测需明确标注。

## 迭代记录 2026-06-19 chat claude provider glyph parity

- 上轮实际进展：记录中声称 provider selector 已从通用 Terminal 改成 Claude Code / Codex 专属图标；代码复查确认 `ButtonArea.tsx` 确实已有 `ProviderBrandIcon`、compact provider trigger 和 option 图标，但 Claude 分支使用的是上一轮手写近似 path，不是 cc-gui 实际使用的 `@lobehub/icons` Claude glyph。`C:/guodevelop/ccg-switch` 与 `C:/guodevelop/demo/jetbrains-cc-gui` 可读，用户原始 `C:/guodevelop/jetbrains-cc-gui` 仍不存在。本轮无新阻塞；真实 Tauri 桌面视觉点测仍未完成。
- 本轮诊断（补齐 cc-gui）：cc-gui `ProviderModelIcon.tsx` 将 `claude` 映射到 `@lobehub/icons/es/Claude/components/Color` / `Mono`，`ProviderSelect.tsx` 在 compact trigger 与 dropdown option 中复用该图标。本项目虽然已区分 Claude/Codex，但 Claude 图标 path 是泛化 starburst placeholder，用户反馈“claude 图标不对”属实。
- 本轮诊断（超越 cc-gui）：本项目不新增 `@lobehub/icons` 依赖，而是读取 cc-gui lockfile 对应的 `@lobehub/icons@5.8.0` tarball 源码，仅内联 Claude glyph path。这样能达到视觉对齐，同时避免扩大前端 bundle 与依赖维护面。
- 本轮规划：只修 provider selector 的 Claude 图标形态，不改 provider 状态、模型动态刷新、发送链路、MCP 或历史加载。优先级高：用户价值高（provider 视觉锚点错误会影响每次发送前确认）、出现频率高（输入区常驻）、成本低（单组件 path 替换 + 测试）、风险低（纯展示）。
- 本轮完成：`src/components/chat/composer/ButtonArea.test.tsx` 扩展 provider icon 回归，断言 Claude provider button 必须包含 `data-chat-provider-icon-glyph="claude-lobehub"` 和 cc-gui/LobeHub Claude path 开头，并不得包含旧 `M11.1...` placeholder path。`src/components/chat/composer/ButtonArea.tsx` 将 Claude SVG 替换为 `@lobehub/icons@5.8.0` Claude glyph path，colored 状态使用 `#D97757`，保留原有 `data-chat-provider-icon="claude"` 与 Codex 图标不变。`.trellis/spec/frontend/component-guidelines.md` 补充 provider selector 规范：Claude icon 必须匹配 cc-gui/LobeHub glyph，不得用 generic starburst/placeholder。
- 本轮验证：RED：`npm test -- src/components/chat/composer/ButtonArea.test.tsx` 先失败 1 项，失败点为缺少 `data-chat-provider-icon-glyph="claude-lobehub"` 且仍输出旧 `M11.1...` path。GREEN：同命令通过（1 file / 6 tests）。相邻验证 `npm test -- src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ChatComposer.test.ts src/utils/chatModels.test.ts` 通过（4 files / 24 tests）；`npm test` 通过（35 files / 303 tests，仅 Browserslist/caniuse-lite 过期提示）；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`npm run build` 通过；IDE `build_project` 通过；`git diff --check` 退出 0 但输出既有 LF/CRLF 提示，`git diff --cached --check` 退出 0。未做真实 Tauri 桌面视觉点测。
- 下一步候选：1) 真实 Tauri 桌面点测 provider selector：Claude/Codex trigger 图标、dropdown 图标+label、暗色主题、disabled/loading 状态；2) 若仍希望完全复用 cc-gui 图标体系，再单独评估引入本地 provider/model 图标组件或轻量图标资源文件，但需先评估依赖体积和许可；3) 继续真实桌面点测 MCP details/live check、小屏 MCP tab、模型刷新、Git chip、任务/子代理/编辑触发式状态条；4) 若用户继续反馈大会话慢，用 `lastSessionLoadMetrics` 分段数据决定后续优化点。

## 本轮 PLAN 2026-06-19 chat input layout density and status dedupe

- 目标：收口 Chat 输入区的信息架构和基础排版密度，优先解决用户反馈的任务列表重复、模型/Provider 图标不对齐、输入区工具栏拥挤等高频低成本问题。
- 功能点 1：确认真实差距。验证方式：基于 `ChatInputStatusTabs`、`SelectorDropdown`、`ModelIcon`、`ButtonArea` 和 cc-gui selector 代码，区分任务/子代理重复与纯展示错位。
- 功能点 2：补 RED 测试。验证方式：扩展 `ChatInputStatusTabs.test.tsx`，断言 tasks panel 不再重复展示 agent/subagent；扩展 `ButtonArea.test.tsx`，断言 selector trigger/option/checkmark 和 model icon 使用稳定 icon box 对齐契约。
- 功能点 3：实现最小交互与展示修复。验证方式：`ChatInputStatusTabs` 将非 agent 工具作为任务流，agent 专属进入子代理；`SelectorDropdown` 统一 trigger/option/check icon box；`ModelIcon` 固定外层尺寸和 line-height；输入区 toolbar 分成可换行选择器组与固定动作组。
- 功能点 4：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` / `.trellis/spec/frontend/state-management.md`，记录 status strip 任务/子代理分工与 selector icon box 契约。
- 功能点 5：质量验证。验证方式：运行定向 ChatInputStatusTabs/ButtonArea 测试、相邻 composer/status 测试、全量前端测试、前端构建、Rust check/test、IDE 构建与空白检查；真实 Tauri 桌面点测若未执行需明确标注。

## 迭代记录 2026-06-19 chat input layout density and status dedupe

- 上轮实际进展：记录中声称 Claude provider 图标已按 cc-gui/LobeHub glyph 修正；代码复查确认 `ButtonArea.tsx` 已有 `data-chat-provider-icon-glyph="claude-lobehub"` 和对应 path，`ButtonArea.test.tsx` 有回归覆盖。记录中也显示模型动态刷新、MCP details/live check、Git/任务/子代理/编辑/MCP 输入区状态条、运行上下文和历史加载性能指标已在代码中存在；本轮不回滚这些既有改动。已知阻塞仍是真实 Tauri 桌面视觉/点击点测未完成。
- 本轮诊断（补齐 cc-gui）：cc-gui selector 使用固定 compact 图标入口和图标+label 的 option 行。本项目 `SelectorDropdown` 此前把 trigger 图标直接插入按钮、option 图标用 `mt-0.5` 裸包裹、checkmark 单独偏移；`ModelIcon` 也只输出裸 lucide 图标，模型/Provider 图标在 compact toolbar 中容易出现基线和外框不一致。`ChatInputStatusTabs` 的 tasks 直接使用完整 `toolTimeline`，而 subagents 又从同一 timeline/`agentTools` 提取 agent，导致同一 agent run 同时出现在任务和子代理列表。
- 本轮诊断（超越 cc-gui）：任务和子代理应是两个不同队列：任务表示 Bash/Edit/Read/Search 等非 agent 工具流，子代理表示 agent/subagent 执行流；否则用户会把同一条 agent run 误解为两个待处理项。输入区 toolbar 也需要把 selectors 与主动作区分组，保证窄宽度下模型名/刷新/推理控制先换行，而增强/发送/停止不被挤出。
- 本轮规划：只做展示层和派生层收口，不改后端、store、发送链路、模型刷新协议或 MCP live check。优先级高：用户价值高（减少重复入口、修复图标错位、降低输入区拥挤）、出现频率高（每次 Chat 输入都可见）、实现成本低（3 个组件和 2 个测试文件）、风险低（纯前端派生与 class/布局调整）。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.tsx` 新增 `taskTools = toolTimeline.filter(type !== 'agent')`，tasks tab/panel/progress/spinner 只统计非 agent 工具，subagents tab 继续使用 `agentTools`。`src/components/chat/ChatInputStatusTabs.test.tsx` 增加回归，断言 tasks panel 不再包含 `frontend-reviewer` / agent model detail。`src/components/chat/composer/SelectorDropdown.tsx` 为 trigger、option、checkmark 增加稳定 icon box class。`src/components/chat/composer/ModelIcon.tsx` 增加 `chat-model-icon-box`、固定 width/height 和 `leading-none`。`src/components/chat/composer/ButtonArea.tsx` 将底部 toolbar 改为左侧 flexible selector group + 右侧固定动作组。`src/components/chat/composer/ButtonArea.test.tsx` 覆盖 trigger icon box 与 model icon box。`.trellis/spec/frontend/component-guidelines.md` 和 `.trellis/spec/frontend/state-management.md` 已同步任务/子代理分工、selector icon box 与 composer toolbar 分组契约。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/composer/ButtonArea.test.tsx` 先失败 3 项，失败点分别为 tasks panel 仍包含 `Review composer status strip`、缺少 `chat-model-icon-box`、provider trigger 缺少 `selector-dropdown-trigger-icon`。GREEN：同命令通过（2 files / 14 tests）。相邻验证 `npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ChatComposer.test.ts src/utils/chatStatusSummary.test.ts src/utils/chatModels.test.ts` 通过（6 files / 39 tests）。全量验证通过：`npm test`（35 files / 303 tests，只有 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests + 2 doctests ignored）、IDE `build_project`。`git diff --check` 和 `git diff --cached --check` 均 exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。`npm run build` / IDE 构建生成的未跟踪 `out/` 已确认在工作区内并删除。未做真实 Tauri 桌面视觉/点击点测。
- 下一步候选：1) 真实 Tauri 桌面点测输入区：窄宽度 toolbar 换行、Provider/Model/Mode/Reasoning 下拉图标对齐、发送/停止按钮是否保持可触达；2) 点测长历史会话下任务/子代理/编辑状态条：idle 补齐后是否出现、tasks 是否不再重复 agent、subagents 是否可展开；3) 继续排查页面元素重复：右侧 `StatusPanel` 与输入区 strip 的信息分工是否需要进一步压缩文案或只保留图标+count；4) 若大会话仍体感卡顿，基于 `lastSessionLoadMetrics` 决定是否把完整摘要构建移到 worker/增量索引，或继续优化后端 tail reader。

## 本轮 PLAN 2026-06-19 chat status strip compact quick entries

- 目标：进一步降低输入区状态条的信息重复感，把 tasks/subagents/edits/MCP 从“迷你详情按钮”收口为“快捷入口 chip”：图标 + 短 label + count pill + 完整 aria-label，详情仍由展开面板和右侧 `StatusPanel` 承担。
- 功能点 1：确认差距。验证方式：CodeGraph 读取 `ChatInputStatusTabs` 与 `StatusPanel`，确认输入区状态条和右侧面板当前都有任务/子代理/编辑/MCP 信息，状态条应只保留快速摘要。
- 功能点 2：补 RED 测试。验证方式：扩展 `ChatInputStatusTabs.test.tsx`，断言 tab 按钮渲染 `chat-input-status-count-pill`、`aria-label="label stat"`，并且 label 具备小屏隐藏/大屏显示 class。
- 功能点 3：实现最小展示层优化。验证方式：`renderTabButton()` 改为 compact quick-entry 结构，stat 进入 pill，label 用 `hidden sm:inline`，按钮加 `title` / `aria-label`；不改变 openTab、panel、summary 派生或点击逻辑。
- 功能点 4：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 composer strip 是快速入口，完整诊断归 `StatusPanel`。
- 功能点 5：质量验证。验证方式：运行定向 `ChatInputStatusTabs` 测试、相邻 status/composer 测试、全量前端测试、前端构建、Rust check/test、IDE 构建和空白检查；真实 Tauri 桌面点测若未执行需明确标注。

## 迭代记录 2026-06-19 chat status strip compact quick entries

- 上轮实际进展：上一轮已修任务/子代理重复、selector/model 图标对齐和 composer toolbar 左右分组；代码复查确认 `ChatInputStatusTabs` 已按非 agent tools / `agentTools` 拆分，`SelectorDropdown` / `ModelIcon` 已有固定 icon box，`ButtonArea` 已有 flexible selector group。已知阻塞仍是真实 Tauri 桌面视觉/点击点测未完成；本轮继续在纯前端展示层收口，不回滚既有改动。
- 本轮诊断（补齐 cc-gui）：cc-gui 输入区附近更像 compact quick control，完整诊断不塞进 collapsed composer 区。本项目状态条按钮仍显示 `icon + label + stat`，label/stat 在小宽度下占用输入区横向空间；同时右侧 `StatusPanel` 已经展示 runtime、MCP、工具、编辑、历史加载等完整详情，collapsed strip 不应重复承担详情阅读职责。
- 本轮诊断（超越 cc-gui）：把状态条 collapsed tabs 改成 quick-entry chip 可以降低页面元素重复感：小屏优先保留 icon 和 count pill，完整 label 通过 `aria-label` / tooltip 保留，可访问性不丢；用户要看细节时再展开 inline panel 或看右侧 `StatusPanel`。
- 本轮规划：只改 `ChatInputStatusTabs.renderTabButton()` 的 collapsed button 结构，不改 open state、panel 内容、summary 派生、MCP live check、Git chip 或右侧状态面板。优先级高：用户价值中高（直接缓解输入区拥挤和重复感）、出现频率高（composer 常驻）、成本低（单组件+测试）、风险低（纯 markup/class/aria）。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.test.tsx` 新增回归断言，要求 status tab 渲染 `chat-input-status-count-pill`、`chat-input-status-tab-label hidden sm:inline`、完整 `aria-label` 与 `title`。`src/components/chat/ChatInputStatusTabs.tsx` 将 tab 按钮改为 compact quick-entry：按钮增加完整 `aria-label/title`，视觉 label 在小屏隐藏、`sm` 以上显示，stat 进入 rounded count pill，spinner 保持在 pending 时显示。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 已记录 composer strip 是快速入口，完整诊断留给展开面板和右侧 `StatusPanel`。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 1 项，失败点为缺少 `chat-input-status-count-pill`。GREEN：同命令通过（1 file / 8 tests）。相邻验证 `npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/utils/chatStatusSummary.test.ts src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ButtonArea.test.tsx` 通过（5 files / 37 tests）。全量验证通过：`npm test`（35 files / 303 tests，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests + 2 doctests ignored）、IDE `build_project`。`git diff --check` 与 `git diff --cached --check` exit 0，前者仅输出既有 LF/CRLF 转换提示。构建生成的未跟踪 `out/` 已确认位于工作区内并删除。未做真实 Tauri 桌面视觉/点击点测。
- 下一步候选：1) 真实 Tauri 桌面点测输入区状态条：小宽度下 label 隐藏、count pill 可读、tooltip/aria 保留语义、展开 panel 位置不遮挡输入框；2) 继续梳理右侧 `StatusPanel` 信息密度，尤其 runtime context、MCP live、session load metrics 是否需要默认折叠或分组；3) 若用户继续反馈大会话卡顿，用 `lastSessionLoadMetrics` 选择下一轮性能点，避免继续盲改 UI；4) 对 cc-gui 真实运行画面做一次并排截图审计，补齐仍未验证的 provider/model selector、MCP details、Git chip、状态条和长历史消息排版。

## 本轮 PLAN 2026-06-19 chat status panel diagnostic density

- 目标：继续降低 Chat 右侧 `StatusPanel` 常态信息密度，把历史加载性能指标从默认展开详情收口为“诊断入口”；加载中或错误态仍自动展开，避免隐藏关键问题。
- 功能点 1：恢复上下文与差距确认。验证方式：读取 `TODO_LIST.md`、Trellis 当前任务与规范，确认 `C:/guodevelop/ccg-switch` 可读，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际对标路径 `C:/guodevelop/demo/jetbrains-cc-gui` 可读。
- 功能点 2：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，断言完成态 `sessionLoadMetrics` 默认渲染 `status-session-load-toggle` 且 `aria-expanded=false`，详细 window/full/map 耗时不进入默认 DOM；loading/error 自动展开。
- 功能点 3：实现最小展示层改动。验证方式：`StatusPanel` 新增本地 disclosure state，完成态默认折叠、加载/错误态自动展开，指标详情条件渲染；不改变 `useChatStore`、后端历史加载协议、MCP live check 或 composer 状态条。
- 功能点 4：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 和 `.trellis/spec/frontend/state-management.md`，记录历史加载诊断折叠契约和本地 UI state 边界。
- 功能点 5：质量验证。验证方式：运行定向 `StatusPanel` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` 与生成物清理。

## 迭代记录 2026-06-19 chat status panel diagnostic density

- 上轮实际进展：上一轮已把输入区 tasks/subagents/edits/MCP 状态条收口为 quick-entry chip，并修复任务/子代理重复、模型/Provider 图标对齐和 composer toolbar 分组；代码复查确认这些能力在 `ChatInputStatusTabs`、`SelectorDropdown`、`ModelIcon`、`ButtonArea` 中存在。已知阻塞仍是真实 Tauri 桌面视觉/点击点测未完成。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 Chat 辅助信息更偏 compact 控制面，完整诊断不会在常态右栏持续占多行。本项目 `StatusPanel` 的 `sessionLoadMetrics` 虽然对大会话性能排查有价值，但完成态仍默认展开 window/full/map/elapsed 多行，和 runtime/MCP/工具/编辑详情一起造成右侧面板扫描负担。
- 本轮诊断（超越 cc-gui）：保留性能诊断入口，但完成后默认折叠，可以让普通聊天状态先展示 provider、daemon、MCP、工具和编辑等高频信息；当历史加载中或失败时自动展开，确保排障信息不被隐藏。用户痛点是“页面元素功能重复、布局排版不合理”；本轮减少的是右侧面板常驻诊断噪声。
- 本轮规划：只改 `StatusPanel` 的展示层 disclosure 行为，不改历史加载协议、store ownership、MCP live check、输入区状态条或后端命令。优先级中高：用户价值中高（降低常态噪声）、出现频率高（右侧面板常驻）、实现成本低（单组件 + 测试）、风险低（本地 UI state）。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 新增/调整历史加载指标回归，覆盖完成态默认折叠和 loading 自动展开。`src/components/chat/StatusPanel.tsx` 新增 `SessionLoadDisclosureState`，按 `providerId/sessionKey/startedAt` 重置本地展开状态；完成态默认折叠，只在 summary 行显示来源、消息数和总耗时，展开后才显示 window/full/map/error 详情；loading/error 自动展开。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 已同步诊断折叠和 state 所有权约束。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为缺少 `status-session-load-toggle` 且完成态仍默认输出 `40ms`/`50ms` 详情。GREEN：同命令通过（1 file / 15 tests）。相邻验证 `npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatStatusSummary.test.ts src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ButtonArea.test.tsx` 通过（5 files / 38 tests）。全量验证通过：`npm test`（35 files / 304 tests，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests + 2 doctests ignored）、IDE `build_project`。`git diff --check` 和 `git diff --cached --check` exit 0，其中 `git diff --check` 仅输出既有 LF/CRLF 转换提示。`npm run build` / IDE build 生成的未跟踪 `out/` 已确认在工作区内并删除。未做真实 Tauri 桌面视觉/点击点测。
- 下一步候选：1) 真实 Tauri 桌面点测右侧状态面板：历史加载诊断折叠/展开、loading/error 自动展开、MCP details/live check、runtime context 在窄右栏下是否可扫读；2) 继续压缩 `StatusPanel` runtime context，可考虑把 model/mode/reasoning/workspace/SDK 合成两行 compact grid，但需先截图审计避免过度隐藏；3) 对 cc-gui 真实运行画面做并排截图审计，覆盖 provider/model selector、MCP details、Git chip、输入区状态条和历史消息排版；4) 若用户继续反馈大会话卡顿，用 `lastSessionLoadMetrics` 的分段数据决定是否优化后端 tail reader、IPC payload 或完整摘要构建。

## 本轮 PLAN 2026-06-19 chat runtime context compact grid

- 目标：继续收口 Chat 右侧 `StatusPanel` 的常态信息密度，把 model / permission mode / reasoning / workspace / SDK 从纵向键值列表改为 compact tile grid，保留完整 tooltip，减少右栏滚动压力。
- 功能点 1：恢复上下文与确认边界。验证方式：读取 `TODO_LIST.md`、Trellis 当前任务制品、前端规范与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，断言 runtime context 渲染 `status-runtime-context-grid`、5 个 `status-runtime-context-item` 和 `status-runtime-context-value`。
- 功能点 3：实现最小展示层改动。验证方式：`StatusPanel` 将 runtime context 的 5 个 display-only 行改为两列 compact tile，值保持 truncate + title；不改 `ChatPage`、store、后端命令、MCP live check 或历史加载。
- 功能点 4：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 和 `.trellis/spec/frontend/state-management.md`，记录 runtime context 应是右侧状态面板的 compact-grid 展示，不引入 Zustand 展开/密度状态。
- 功能点 5：质量验证。验证方式：运行定向 `StatusPanel` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check`，并清理构建生成物。

## 迭代记录 2026-06-19 chat runtime context compact grid

- 上轮实际进展：上一轮已将历史加载性能指标从默认展开详情改为完成态折叠、loading/error 自动展开；代码复查确认 `StatusPanel` 中已有 `status-session-load-toggle` 与 `status-session-load-details` 条件渲染，且 Trellis 规范已记录诊断折叠契约。已知阻塞仍是真实 Tauri 桌面视觉/点击点测未完成。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 Chat 状态辅助面板更偏扫描型 compact 控制面。本项目 `StatusPanel` runtime context 虽然补齐了模型、权限模式、推理强度、工作区和 SDK 状态，但仍是 5 个纵向 key/value 行；在右栏同时展示 MCP、工具、编辑树、历史诊断时，这块会占用过多高度。
- 本轮诊断（超越 cc-gui）：把 runtime context 改成两列 compact tile 后，用户仍能在发送前确认关键运行环境，同时通过 `title` 查看完整模型 id 和 Windows 工作区路径。用户痛点是“页面元素重复、布局排版不合理、模型图标/状态不对齐”；本轮减少的是右侧状态栏的基础信息铺排成本。
- 本轮规划：只改 `StatusPanel` runtime context 的 markup/class，不新增图标、不新增文案、不改变数据来源。优先级中高：用户价值中高（提高右栏可扫读性）、出现频率高（常驻面板）、实现成本低（单组件+测试）、风险低（纯展示层）。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 增加 compact runtime context 回归，要求 `status-runtime-context-grid`、5 个 `status-runtime-context-item` 和 `status-runtime-context-value` 存在。`src/components/chat/StatusPanel.tsx` 将 model/mode/reasoning/workspace/SDK 的纵向列表改为两列 compact tile，保留 `title` 和状态颜色。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 已同步 runtime context compact-grid 与本地展示边界。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为缺少 `status-runtime-context-grid`。GREEN：同命令通过（1 file / 15 tests）。相邻验证 `npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatStatusSummary.test.ts src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ButtonArea.test.tsx` 通过（5 files / 38 tests）。全量验证通过：`npm test`（35 files / 304 tests，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests + 2 doctests ignored）、IDE `build_project`。`git diff --check` 和 `git diff --cached --check` exit 0，其中 `git diff --check` 仅输出既有 LF/CRLF 转换提示。`npm run build` / IDE build 生成的未跟踪 `out/` 已确认位于工作区内并删除。未做真实 Tauri 桌面视觉/点击点测。
- 下一步候选：1) 真实 Tauri 桌面点测右侧状态面板：runtime compact grid 在窄右栏下是否可读、title 是否能检查完整模型/路径、历史诊断折叠是否正常；2) 做一次 cc-gui 与本项目真实运行画面并排截图审计，重点看 provider/model selector、MCP details、Git chip、输入区状态条、右栏密度和历史消息排版；3) 若仍觉得右侧面板拥挤，下一轮再评估将 `currentActivity` 与 tool counts 合并为一个 compact activity card；4) 若大会话继续卡顿，用 `lastSessionLoadMetrics` 分段数据定位是后端 tail reader、IPC payload、React commit 还是完整摘要构建。

## 本轮 PLAN 2026-06-19 chat status panel activity counts consolidation

- 目标：继续降低 Chat 右侧 `StatusPanel` 的重复信息，把 standalone “进行中工具 / 失败工具”两行合并到 `currentActivity` 卡片标题右侧，只在有非零计数时显示 compact pills。
- 功能点 1：恢复上下文与确认边界。验证方式：读取 `TODO_LIST.md`、Trellis 规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `StatusPanel.tsx` 与 `StatusPanel.test.tsx`，确认 `pendingTools` / `errorTools` 独立渲染在顶部 grid，同时 `currentActivity` 另起卡片。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，断言工具计数进入 `status-activity-tool-counts`，并且顶部 grid 不再直接渲染 `Pending tools` / `Failed tools` 标签行。
- 功能点 4：实现最小展示层改动。验证方式：`StatusPanel` 从顶部 grid 删除两行 tool count，把 pending/error 派生为 current activity header pills；不改 `statusSummary` 来源、store、MCP、历史加载或输入区状态条。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md`，记录工具计数属于 current activity card 派生摘要，不新增 Zustand 状态。
- 功能点 6：质量验证。验证方式：运行定向 `StatusPanel` 测试、相邻 Chat UI 测试、`npm test`、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 IDE 生成的 `out/`。

## 迭代记录 2026-06-19 chat status panel activity counts consolidation

- 上轮实际进展：上一轮已把 `StatusPanel` runtime context 从纵向列表改为 compact tile grid；代码复查确认 `src/components/chat/StatusPanel.tsx` 已有 `status-runtime-context-grid`，测试 `src/components/chat/StatusPanel.test.tsx` 覆盖 5 个 `status-runtime-context-item`。已知阻塞仍是真实 Tauri 桌面视觉/点击点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的状态辅助区更偏 activity-centered，工具运行/失败状态贴近当前活动本身。本项目顶部 status grid 仍有独立 “进行中工具 / 失败工具” 两格，而下方 `currentActivity` 又显示 active tool，用户需要在两块区域之间拼读同一类信息。
- 本轮诊断（超越 cc-gui）：把 pending/failed 计数并入 `currentActivity` header 后，右侧面板的基础 grid 只保留 provider、消息数、锚点、daemon、MCP、历史诊断等基础/环境信息；工具动态状态贴在当前活动标题旁。用户痛点是“页面元素功能重复、布局排版不合理”，本轮直接减少两行常驻重复信息。
- 本轮规划：只做 `StatusPanel` 展示层收口，不改 `ChatStatusSummary` 派生、不改 `ChatPage`、store、MCP live check、历史加载和 composer strip。优先级中高：用户价值中高（减少右栏重复扫描）、出现频率高（右栏常驻）、实现成本低（单组件 + 测试 + 规范）、风险低（纯派生展示）。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 新增回归 `consolidates tool counts into the current activity card header`，覆盖 activity header count pills 与顶部 grid 标签移除。`src/components/chat/StatusPanel.tsx` 新增 `pendingToolCount` / `errorToolCount` / `hasActivityToolCounts` 派生值，删除顶部 standalone `pendingTools` / `errorTools` 行，并在 `currentActivity` header 右侧仅对非零计数渲染 `status-activity-tool-count` pills，保留中英文 title。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 已同步：pending/failed tool counts 属于 current activity card 派生摘要，不再进入顶部 status grid，也不需要新的 Zustand 状态。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为缺少 `status-activity-tool-counts` 且 HTML 中仍有顶部 “进行中工具 / 失败工具”。GREEN：同命令通过（1 file / 16 tests）。相邻验证 `npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatStatusSummary.test.ts src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ButtonArea.test.tsx` 通过（5 files / 39 tests）。全量验证通过：`npm test`（35 files / 305 tests，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests + 2 doctests ignored）、IDE `build_project`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。IDE 构建生成的未跟踪 `out/production/...` 已确认位于工作区内并删除。未做真实 Tauri 桌面视觉/点击点测。
- 下一步候选：1) 真实 Tauri 桌面点测右侧 `StatusPanel`：activity count pills 在 pending/error/idle 下是否可扫读，MCP details/live check、runtime grid、历史诊断折叠是否仍正常；2) 做一次 cc-gui 与本项目真实运行画面并排截图审计，重点看右栏密度、输入区状态条、provider/model selector、Git chip、MCP details 和长历史消息排版；3) 如果右侧仍拥挤，下一轮优先评估把 recent edits 卡片的“0 文件”空态隐藏或合并为轻量 footer，避免空态也占整块卡片；4) 如果大会话继续卡顿，用 `lastSessionLoadMetrics` 分段数据定位是后端 tail reader、IPC payload、React commit 还是完整摘要构建。

## 本轮 PLAN 2026-06-19 chat status panel edit empty-state trigger

- 目标：继续降低 Chat 右侧 `StatusPanel` 常态噪音，隐藏无编辑时的整块“最近改动 / 0 文件 / 暂时没有可展示的文件改动”空态卡片；有编辑或可重开中央 diff 面板时仍显示。
- 功能点 1：恢复上下文。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务 `prd.md` / `design.md` / `implement.md`、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 对标诊断。验证方式：读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 与 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx`，确认 cc-gui 文件变更 tab 只在有变更时显示统计和 popover 详情，本项目则无条件渲染整块空态卡片。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，断言无编辑时不输出 “Recent edits / 最近改动” 和 “No file edits to show yet / 暂时没有可展示的文件改动”。
- 功能点 4：实现最小展示层改动。验证方式：`StatusPanel` 新增 `shouldShowRecentEditsCard`，仅在 touched file count、`allEdits` 或 diff-pane reopen action 存在时渲染最近改动卡；不改 `statusSummary` 派生、编辑树逻辑、中央 diff pane 或输入区 edits tab。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md`，记录 recent-edits card 是触发式展示，不新增 Zustand 状态。
- 功能点 6：质量验证。验证方式：运行定向 `StatusPanel` 测试、相邻 Chat UI 测试、`npm test`、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 IDE 生成的 `out/`。

## 迭代记录 2026-06-19 chat status panel edit empty-state trigger

- 上轮实际进展：上一轮已把 pending/failed tool counts 从顶部 grid 合并到 `currentActivity` header；代码复查确认 `src/components/chat/StatusPanel.tsx` 已有 `status-activity-tool-counts`，`src/components/chat/StatusPanel.test.tsx` 已覆盖顶部标签移除。已知阻塞仍是真实 Tauri 桌面视觉/点击点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 `webview/src/components/StatusPanel/StatusPanel.tsx` 使用 tab header + popover，文件变更 tab 常驻但只有 `hasFileChanges` 时显示统计，详情不是默认铺开。本项目右侧 `StatusPanel` 在无编辑时仍无条件渲染整块 “最近改动” 卡片和 “+0 / -0” 空态，和已压缩的 runtime/activity/diagnostic 面板方向不一致。
- 本轮诊断（超越 cc-gui）：本项目右栏已经有输入区 edits quick-entry 和中央 diff pane 作为真实编辑入口，右侧最近改动卡应只在有可检查内容或可重开 diff 面板时出现。用户痛点是“页面元素功能重复、布局排版不合理”；隐藏零编辑空卡可减少常态滚动和视觉噪音，而不影响真实编辑审查。
- 本轮规划：只改 `StatusPanel` 最近改动卡的触发条件，不改编辑树、diff preview、diff pane reopen handler、`ChatStatusSummary` 或输入区 edits tab。优先级中高：用户价值中（减少右栏空态噪音）、出现频率高（无编辑是常态）、实现成本低（单组件 + 测试 + 规范）、风险低（有编辑和 diff pane 折叠场景保留）。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 新增回归 `omits the recent edits card when there are no edits to inspect`，覆盖无编辑时不输出最近改动空态。`src/components/chat/StatusPanel.tsx` 新增 `shouldShowRecentEditsCard = touchedFileCount > 0 || allEdits.length > 0 || canReopenDiffPane`，用它包裹最近改动卡；同时去掉空态卡内部的 `+0 / -0` fallback 和 `noRecentEdits` 文案路径，因为无编辑时整卡不再渲染。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 已同步 recent-edits card 的触发式展示和 state 边界。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为 HTML 中仍输出 “最近改动” 和 “暂时没有可展示的文件改动”。GREEN：同命令通过（1 file / 17 tests）。相邻验证 `npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatStatusSummary.test.ts src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ButtonArea.test.tsx` 通过（5 files / 40 tests）。全量验证通过：`npm test`（35 files / 306 tests，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests + 2 doctests ignored）、IDE `build_project`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。IDE 构建生成的未跟踪 `out/production/...` 已确认位于工作区内并删除。未做真实 Tauri 桌面视觉/点击点测。
- 下一步候选：1) 真实 Tauri 桌面点测右侧 `StatusPanel`：无编辑时最近改动卡消失，有编辑时编辑树/hover diff/reopen diff pane 仍可用；2) 做一次 cc-gui 与本项目真实运行画面并排截图审计，重点看右栏密度、输入区状态条、provider/model selector、Git chip、MCP details 和长历史消息排版；3) 如果右侧仍拥挤，下一轮优先评估 idle `currentActivity` 卡是否可以和基础状态合并为更轻量状态行；4) 如果大会话继续卡顿，用 `lastSessionLoadMetrics` 分段数据定位是后端 tail reader、IPC payload、React commit 还是完整摘要构建。

## 本轮 PLAN 2026-06-19 chat status panel idle activity trigger

- 目标：继续降低 Chat 右侧 `StatusPanel` 常态噪音，纯空闲态不再渲染整块 “Current activity / 当前活动” 卡片；有 active tool、回复流式生成或 pending/failed tool 计数时仍显示。
- 功能点 1：恢复上下文。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `StatusPanel.tsx` / `StatusPanel.test.tsx`，确认 `currentActivity` 卡在旧实现中无条件渲染，纯空闲时仍输出 idle 文案。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，断言完全 idle 时不输出 “Current activity / 当前活动” 和 “Idle / 当前空闲”，同时流式回复时仍显示 activity 卡。
- 功能点 4：实现最小展示层改动。验证方式：`StatusPanel` 新增 `shouldShowCurrentActivityCard = activeTool || isStreaming || hasActivityToolCounts`，只包裹 current activity 卡；不改 `statusSummary`、store、MCP、历史加载或 composer strip。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md`，记录 activity 卡触发式展示和 state 边界。
- 功能点 6：质量验证。验证方式：运行定向 `StatusPanel` 测试、相邻 Chat UI 测试、`npm test`、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 IDE 生成的 `out/`。

## 迭代记录 2026-06-19 chat status panel idle activity trigger

- 上轮实际进展：上一轮已隐藏无编辑时的 `recentEdits` 空态卡；代码复查确认 `src/components/chat/StatusPanel.tsx` 已有 `shouldShowRecentEditsCard`，`src/components/chat/StatusPanel.test.tsx` 已覆盖无编辑时不输出最近改动空态。已知阻塞仍是真实 Tauri 桌面视觉/点击点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的状态辅助信息偏 activity-centered，只有真正有操作/运行状态时才需要用户扫读。本项目虽然已把 pending/failed 工具计数并入 `currentActivity` header，但 `currentActivity` 卡仍在完全 idle 时无条件渲染，并显示一整块 “当前空闲”，与前几轮 runtime、history metrics、recent edits 的触发式收口方向不一致。
- 本轮诊断（超越 cc-gui）：空闲状态本身已经可由没有 spinner、没有 tool count、没有 active tool 来表达，不需要占用右栏卡片高度；当回复 streaming、工具正在执行/失败或存在 active tool 时再显示 activity 卡，能把用户注意力集中到可行动状态。用户痛点是“页面元素重复、布局排版不合理”；本轮减少常态右栏噪音，同时保留动态状态的可见性。
- 本轮规划：只改 `StatusPanel` 展示层触发条件，不新增 i18n、不改 `ChatStatusSummary` 派生、不改后端命令、不改输入区状态条。优先级中高：用户价值中（减少常态卡片堆叠）、出现频率高（idle 是 Chat 常态）、实现成本低（单组件 + 测试 + 规范）、风险低（active/streaming/count 场景保留）。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 新增 idle/streaming 回归：纯 idle 不输出 `Current activity / 当前活动` 与 idle 文案，streaming 仍显示 activity 卡和回复生成状态。`src/components/chat/StatusPanel.tsx` 新增 `shouldShowCurrentActivityCard` 派生条件，并用它包裹 current activity 卡；active tool 展示、streaming 展示、pending/error count pills 逻辑保持原样。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 已同步：current-activity card 是触发式展示，完全 idle 不渲染大卡，也不引入 Zustand 状态。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为旧实现仍输出 “当前活动 / 当前空闲”。GREEN：同命令通过（1 file / 19 tests）。相邻验证 `npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatStatusSummary.test.ts src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ButtonArea.test.tsx` 通过（5 files / 42 tests）。全量验证通过：`npm test`（35 files / 308 tests，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests + 2 doctests ignored）、IDE `build_project`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。IDE 构建生成的未跟踪 `out/production/...` 已确认位于工作区内并删除。未做真实 Tauri 桌面视觉/点击点测。
- 下一步候选：1) 真实 Tauri 桌面点测右侧 `StatusPanel`：idle 时 activity/recent-edits 空卡消失，streaming/active tool/pending-error counts 时 activity 卡仍可见；2) 做一次 cc-gui 与本项目真实运行画面并排截图审计，重点看右栏密度、输入区状态条、provider/model selector、Git chip、MCP details 和长历史消息排版；3) 下一轮优先检查输入区布局是否仍存在重复入口或按钮对齐问题，尤其 provider/model icon、任务/子代理/编辑/MCP/Git chip 的窄宽度排布；4) 若大会话继续卡顿，用 `lastSessionLoadMetrics` 分段数据定位是后端 tail reader、IPC payload、React commit 还是完整摘要构建。

## 本轮 PLAN 2026-06-19 chat input git chip compact density

- 目标：继续优化 Chat 输入区状态条密度，把 Git 分支 chip 收口成与任务/子代理/编辑/MCP 一致的 compact quick-entry 形态：小屏隐藏“分支”视觉标签，分支名截断，并保留完整可访问上下文。
- 功能点 1：恢复上下文。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 对标诊断。验证方式：读取本项目 `ChatInputStatusTabs.tsx` / 测试与 cc-gui `StatusPanel` tab header，确认 cc-gui 状态入口是紧凑 header/popover，本项目 Git chip 仍常驻显示 label 且 title 没有分支名。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ChatInputStatusTabs.test.tsx`，断言 Git chip 渲染 `chat-input-status-git-label hidden sm:inline`、`chat-input-status-git-value`，并在 `aria-label` / `title` 中保留完整 branch/root 上下文。
- 功能点 4：实现最小展示层改动。验证方式：`ChatInputStatusTabs` 只调整 Git chip class、title、aria-label 和分支值宽度；不改 `chat_workspace_status` 后端命令、不改 `chatWorkspaceStatus.ts`、不改 Git 触发显示条件。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md`，记录 Git chip 密度规则属于展示层，不进入 Zustand。
- 功能点 6：质量验证。验证方式：运行定向 `ChatInputStatusTabs` 测试、相邻 Chat UI 测试、`npm test`、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 IDE 生成的 `out/`。

## 迭代记录 2026-06-19 chat input git chip compact density

- 上轮实际进展：上一轮已把 `StatusPanel` idle current-activity 空态卡改为触发式展示；代码复查确认 `src/components/chat/StatusPanel.tsx` 已有 `shouldShowCurrentActivityCard`，测试已覆盖 idle 不输出 activity 卡、streaming 保留 activity 卡。已知阻塞仍是真实 Tauri 桌面视觉/点击点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标仓库也有 `.codegraph/`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 `webview/src/components/StatusPanel/StatusPanel.tsx` 用紧凑 tab header 承载 tasks/subagents/files，详情进 popover；本项目输入区状态条已经有任务/子代理/编辑/MCP compact tab，但 Git chip 仍在小屏常驻显示“分支”标签，且 `title` 只显示 gitRoot，不包含实际 branch。多个状态入口同时出现时，Git chip 比其他入口更容易抢横向空间。
- 本轮诊断（超越 cc-gui）：Git 分支是发送前上下文提示，不是可展开详情入口。它应该像 quick-entry chip 一样低噪：图标 + 截断分支名即可，完整 branch/root 放在 `aria-label` 和 `title`。用户痛点是“输入区布局排版不合理、状态按钮触发显示隐藏、Git 分支显示”；本轮让 Git 显示更可靠且不挤占任务/MCP 入口空间。
- 本轮规划：只做 `ChatInputStatusTabs` Git chip 展示层收口，不改后端 Git 检测、不改 `ChatPage` cwd 触发、不改任务/子代理/编辑/MCP 展开逻辑。优先级中高：用户价值中（减少窄宽状态条拥挤并提升可访问信息）、出现频率高（Git 项目中常驻）、实现成本低（单组件 + 测试 + 规范）、风险低（不改变数据来源）。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.test.tsx` 新增回归 `keeps the git branch chip compact while preserving accessible branch context`，覆盖小屏 label 隐藏、分支值 class、完整 branch aria/title。`src/components/chat/ChatInputStatusTabs.tsx` 新增 `gitBranchTitle`，Git chip 增加 `aria-label`，视觉 label 改为 `hidden sm:inline`，分支值增加 `chat-input-status-git-value` 与 `max-w` 截断；Git 仍仅在 `workspaceStatus.isGitRepository && gitBranch` 时显示。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 已同步 Git chip compact density 和展示层 state 边界。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 1 项，失败点为旧 Git chip 缺少 `chat-input-status-git-label hidden sm:inline`、`chat-input-status-git-value` 和 branch aria/title。GREEN：同命令通过（1 file / 9 tests）。相邻验证 `npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/utils/chatStatusSummary.test.ts src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ButtonArea.test.tsx` 通过（5 files / 43 tests）。全量验证通过：`npm test`（35 files / 309 tests，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests + 2 doctests ignored）、IDE `build_project`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。IDE 构建生成的未跟踪 `out/production/...` 已确认位于工作区内并删除。未做真实 Tauri 桌面窄宽视觉点测。
- 下一步候选：1) 真实 Tauri 桌面点测输入区状态条：Git + tasks + subagents + edits + MCP 同时出现时是否换行合理、分支 title 是否可检查完整上下文、编辑/MCP 展开面板是否不遮挡 composer；2) 继续检查 bottom toolbar provider/model/mode/reasoning 图标对齐，尤其 Claude/Codex provider 图标和 model-family glyph 在不同模型名长度下是否漂移；3) 做 cc-gui 与本项目真实运行画面并排截图审计，覆盖输入区、右栏、长历史消息排版和大会话加载指标；4) 若大会话继续卡顿，用 `lastSessionLoadMetrics` 分段数据决定优化后端 tail reader、IPC payload、React commit 还是完整摘要构建。

## 本轮 PLAN 2026-06-19 chat composer selector icon alignment

- 目标：收口 Chat 输入区 bottom toolbar 的 provider / mode / model / reasoning 选择器图标对齐，所有触发器图标、菜单选项图标和选中 checkmark 都使用同一套固定 icon box，减少 Claude/Codex provider glyph、model-family glyph 与 lucide glyph 混排时的视觉漂移。
- 功能点 1：恢复上下文。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务 `prd.md` / `design.md` / `implement.md`、前端规范和 `package.json`；确认本轮只改前端展示层。
- 功能点 2：CodeGraph 对标诊断。验证方式：读取本项目 `ButtonArea.tsx` / `SelectorDropdown.tsx` / `ModelIcon.tsx` / `ButtonArea.test.tsx` 与 cc-gui `ChatInputBox` provider/model selector 实现，确认当前统一落点是 `SelectorDropdown`。
- 功能点 3：补 RED 测试。验证方式：新增或扩展 composer selector 测试，断言 trigger / option / selected checkmark 都渲染 `selector-dropdown-icon-box` 及对应变体 class。
- 功能点 4：实现最小展示层改动。验证方式：`SelectorDropdown` 统一固定 icon box class 和尺寸，去掉 option/checkmark 的一处一处 margin 微调；不改 provider/model/mode/reasoning 状态、模型来源、刷新逻辑或后端命令。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md`，记录 selector icon box 是展示层契约，不引入新的 Zustand state。
- 功能点 6：质量验证。验证方式：运行定向 composer 测试、相邻 Chat UI 测试、`npm test`、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 IDE 生成的 `out/`。

## 迭代记录 2026-06-19 chat composer selector icon alignment

- 上轮实际进展：上一轮已把输入区 Git chip 收口成 compact quick-entry；代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已具备 `chat-input-status-git-label hidden sm:inline`、`chat-input-status-git-value`、完整 `aria-label` / `title`。已知阻塞仍是真实 Tauri 桌面窄宽视觉/点击点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 `webview/src/components/ChatInputBox/selectors/ProviderSelect.tsx` 和 `ModelSelect.tsx` 在 selector trigger / option 中复用 provider/model 图标，整体是紧凑 icon + label 的 toolbar 模式。本项目已经有 `ProviderBrandIcon` / `ModelIcon`，但 `SelectorDropdown` 的 trigger、option 和 checkmark 没有统一语义 wrapper；checkmark 仍是裸 `Check` 图标，option/check 还依赖局部 `mt-0.5` 微调，容易在 Claude/Codex provider glyph、model-family glyph 和 lucide glyph 混排时出现垂直漂移。
- 本轮诊断（超越 cc-gui）：把图标对齐收口到 `SelectorDropdown` 一个公共边界，比在 `ButtonArea` 为 provider/model/mode/reasoning 分别调尺寸更可维护。用户痛点是“模型图标不对齐、输入区布局排版不合理、功能入口重复”；统一 icon box 后，后续新增模型族图标或 provider 图标不会重新打破 toolbar 对齐。
- 本轮规划：只改 `SelectorDropdown` 展示层和测试，不改变 `ButtonArea` props、不改变 provider/model/mode/reasoning 状态、不触发模型动态刷新、不改后端命令。优先级中高：用户价值中（提升输入区精度和扫读稳定性）、出现频率高（每次输入都可见）、实现成本低（单组件 + 单测试 + 规范）、风险低（只调整 wrapper class 和 checkmark 包裹）。
- 本轮完成：新增 `src/components/chat/composer/SelectorDropdown.test.tsx`，通过 SSR 强制打开菜单并断言 trigger / option / selected checkmark 都使用 `selector-dropdown-icon-box` 及 `--trigger` / `--option` / `--check` 变体。`src/components/chat/composer/SelectorDropdown.tsx` 新增共享 icon-box class 常量，trigger、option 和 checkmark 均使用固定 `h-4 w-4` flex box，checkmark 改为 wrapper 内渲染 `Check`，并移除 option/checkmark 的局部 `mt-0.5` 对齐补丁。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 已同步：selector icon box 是展示层契约，固定 class 为 `selector-dropdown-icon-box`，不进入 Zustand。
- 本轮验证：RED：`npm test -- src/components/chat/composer/SelectorDropdown.test.tsx` 先失败 1 项，失败点为旧实现缺少 `selector-dropdown-icon-box` 统一 wrapper 且 checkmark 是裸图标。GREEN：同命令通过（1 file / 1 test）。局部 composer 验证 `npm test -- src/components/chat/composer/SelectorDropdown.test.tsx src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx` 通过（3 files / 9 tests）。相邻 Chat UI 验证 `npm test -- src/components/chat/composer/SelectorDropdown.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/utils/chatStatusSummary.test.ts src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ButtonArea.test.tsx` 通过（6 files / 44 tests）。全量验证通过：`npm test`（36 files / 310 tests，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests + 2 doctests ignored）、IDE `build_project`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。IDE 构建生成的未跟踪 `out/` 已确认位于工作区内并删除。新增测试文件已按仓库规则 `git add`。未做真实 Tauri 桌面视觉/点击点测。
- 下一步候选：1) 真实 Tauri 桌面点测 bottom toolbar：Claude/Codex provider icon、model icon、mode icon、reasoning icon 在不同模型名长度和窄宽换行下是否保持基线稳定；2) 继续做输入区布局排版审计，评估 provider/mode/model/reasoning/refresh/enhance/send 在常见宽度下是否有重复入口或主按钮被挤压；3) 做 cc-gui 与本项目真实运行画面并排截图审计，覆盖输入区、右栏、长历史消息排版和大会话加载指标；4) 若大会话继续卡顿，用 `lastSessionLoadMetrics` 分段数据决定优化后端 tail reader、IPC payload、React commit 还是完整摘要构建。

## 本轮 PLAN 2026-06-19 chat composer toolbar action group hardening

- 目标：收口 Chat 输入区 bottom toolbar 的右侧主操作区，把 enhance/send/stop 统一为固定尺寸 icon action group，避免 provider/mode/model/reasoning 选择器换行时挤压发送/停止按钮，减少 DaisyUI `btn-xs` 自适应尺寸导致的视觉跳动。
- 功能点 1：恢复上下文。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认本轮只改前端展示层。
- 功能点 2：CodeGraph 对标诊断。验证方式：读取本项目 `ButtonArea.tsx` / `ButtonArea.test.tsx` 与 cc-gui `webview/src/components/ChatInputBox/ButtonArea.tsx`，确认 cc-gui 右侧是独立 `button-area-right`，send/stop 复用固定语义按钮。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/composer/ButtonArea.test.tsx`，断言 toolbar、selector group、action group 有稳定语义 class，enhance/send/stop 使用固定 `h-7 w-7 shrink-0` icon button，并有 `aria-label`。
- 功能点 4：实现最小展示层改动。验证方式：`ButtonArea` 只调整 wrapper class、按钮 class 与 aria-label；不改 provider/model/mode/reasoning 状态、不改动态模型刷新、不改发送/停止/增强回调。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md`，记录 composer toolbar 左侧可换行、右侧固定 action group 的展示层契约。
- 功能点 6：质量验证。验证方式：运行定向 `ButtonArea` 测试、相邻 Chat UI 测试、`npm test`、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理构建生成物。

## 迭代记录 2026-06-19 chat composer toolbar action group hardening

- 上轮实际进展：上一轮已把 `SelectorDropdown` trigger/option/checkmark 图标统一进固定 icon box；代码复查确认 `src/components/chat/composer/SelectorDropdown.tsx` 已有 `selector-dropdown-icon-box` 及 `--trigger` / `--option` / `--check`，新增 `SelectorDropdown.test.tsx` 已覆盖。已知阻塞仍是真实 Tauri 桌面视觉/点击点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 `webview/src/components/ChatInputBox/ButtonArea.tsx` 使用独立 `button-area-left` / `button-area-right`，send/stop 复用固定语义按钮。本项目虽然已有右侧 `shrink-0` 容器，但 send/stop 仍使用 DaisyUI `btn btn-xs` 自适应尺寸，和 enhance 的固定 icon button 不一致；当模型名较长或状态条换行时，右侧主操作的视觉宽度和对齐更容易跳动。
- 本轮诊断（超越 cc-gui）：把 toolbar 外层、selector group、action group 暴露成稳定 class，并把 enhance/send/stop 固定为 28px icon buttons，可以让后续窄屏、长模型名、动态模型刷新状态都有可测试的布局边界。用户痛点是“输入区布局排版不合理、模型图标不对齐、页面元素重复”；本轮处理的是主操作按钮不应被选择器挤压和尺寸漂移。
- 本轮规划：只改 `ButtonArea` 展示层和测试，不改 provider/model/mode/reasoning 数据来源、不改动态模型刷新、不改发送/停止/增强回调、不新增依赖。优先级中高：用户价值中（输入区主操作更稳定）、出现频率高（每次聊天都可见）、实现成本低（单组件 + 测试 + 规范）、风险低（纯 class/aria 调整）。
- 本轮完成：`src/components/chat/composer/ButtonArea.test.tsx` 新增回归 `keeps composer primary actions in a fixed right-side action group`，覆盖 `chat-composer-toolbar`、`chat-composer-toolbar-selectors`、`chat-composer-toolbar-actions`，以及 enhance/send/stop 的固定 `h-7 w-7 shrink-0` 和 `aria-label`。`src/components/chat/composer/ButtonArea.tsx` 给 toolbar 三段加稳定 class，并将 send/stop 从 DaisyUI 自适应 `btn btn-xs` 改为固定方形 icon-only primary action；enhance 保持原行为但补齐语义 class、`shrink-0` 和 `aria-label`。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 已同步 toolbar action group 展示层契约。
- 本轮验证：RED：`npm test -- src/components/chat/composer/ButtonArea.test.tsx` 先失败 1 项，失败点为旧实现缺少 `chat-composer-toolbar` 等稳定 class。GREEN：同命令通过（1 file / 7 tests）。相邻验证 `npm test -- src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/SelectorDropdown.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/utils/chatStatusSummary.test.ts src/components/chat/composer/ChatComposer.render.test.tsx` 通过（6 files / 45 tests）。全量验证通过：`npm test`（36 files / 311 tests，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests + 2 doctests ignored）、IDE `build_project`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。IDE build 生成的未跟踪 `out/` 已确认位于工作区内并删除。未做真实 Tauri 桌面窄宽视觉/点击点测。
- 下一步候选：1) 真实 Tauri 桌面点测 bottom toolbar：长模型名、动态刷新 loading/error、reasoning 可见/不可见、Git/tasks/MCP 状态条同时出现时，右侧主操作是否始终固定在可点击位置；2) 做 cc-gui 与本项目真实运行画面并排截图审计，覆盖输入区、右栏、长历史消息排版和大会话加载指标；3) 下一轮优先从用户反馈继续排查长历史加载到聊天区域卡顿，用 `lastSessionLoadMetrics` 分段数据定位后端 tail reader、IPC payload、React commit 或完整摘要构建；4) 继续审计功能重复入口，尤其右侧 `StatusPanel` 与输入区状态条在 tasks/subagents/edits/MCP 上的信息层级是否还可进一步合并。

## 本轮 PLAN 2026-06-19 chat large history background map yielding

- 目标：继续优化点击大型历史会话后的卡顿。首屏窗口已经只加载 120 条、聊天区只渲染 15 条，但后台完整历史返回后仍一次性 `mapHistoryMessages(session, history)`，会把几千条映射压在一个 JS 任务里；本轮把完整历史映射改为分块让出事件循环，避免首屏显示后 UI 再次冻结。
- 功能点 1：恢复上下文。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认本轮修改 store 历史加载链路，不改 UI 样式和后端命令。
- 功能点 2：根因诊断。验证方式：用 CodeGraph 读取 `useChatStore.loadSession()`、`mapHistoryMessages()`、`MessageList` 和 `ChatPage` 历史窗口逻辑，确认首屏窗口没有写全量，但后台完整映射仍同步执行。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/stores/useChatStore.test.ts`，模拟大历史窗口首屏后完整历史返回，断言 `loadSession()` 不会在同一个微任务内完成完整映射和 metrics complete。
- 功能点 4：实现最小性能改动。验证方式：新增分块完整历史映射 helper，只用于后台 `get_unified_session_messages` 结果；首屏 `get_unified_session_message_window` 仍同步映射以保证最快可见。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/state-management.md` 与 `.trellis/spec/frontend/component-guidelines.md`，记录后台完整历史映射必须分块让出主线程。
- 功能点 6：质量验证。验证方式：运行定向 `useChatStore` 测试、相邻 Chat UI/历史测试、`npm test`、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理构建生成物。

## 迭代记录 2026-06-19 chat large history background map yielding

- 上轮实际进展：上一轮已把 Chat composer bottom toolbar 的右侧主操作区固定为 `chat-composer-toolbar-actions`，send/stop/enhance 为固定 `h-7 w-7 shrink-0` icon button；代码复查确认 `src/components/chat/composer/ButtonArea.tsx` 和 `ButtonArea.test.tsx` 已具备该契约。已知阻塞仍是真实 Tauri 桌面视觉/点击点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的历史浏览体验不应在点击历史后长时间阻塞主聊天区。本项目已实现 `get_unified_session_message_window` 首屏窗口和缓存命中 recent window，测试也覆盖“缓存大历史不全量注入 messages”。但 `useChatStore.loadSession()` 在后台完整历史返回后仍同步执行 `mapHistoryMessages(session, history)`，这一步不写回聊天区却会在前端主线程一次性映射几千条历史，用户会感受到“聊天区域已经出现但仍卡顿”。
- 本轮诊断（超越 cc-gui）：完整历史缓存/诊断是后台能力，不应抢占用户对首屏窗口的滚动、搜索输入、继续发送等交互。把完整映射改为分块让出事件循环，可以保留后续缓存命中和 metrics，同时让已经渲染的 15 条可见消息保持响应。根因定位：不是首屏窗口缺失，而是后台 full-map 的单任务阻塞。
- 本轮规划：只改 store 内部完整历史映射边界，不改后端命令、不改首屏窗口大小、不改 `MessageList` 渲染窗口、不改 UI 样式。优先级高：用户价值高（直接对应大会话卡顿反馈）、出现频率中高（每次打开大历史）、实现成本低中（单 store helper + 测试 + 规范）、风险可控（首屏路径不变，后台完成后只更新 metrics/cache）。
- 本轮完成：`src/stores/useChatStore.test.ts` 新增回归 `yields between full history mapping chunks after the first-paint window`，模拟 1500 条历史，断言完整历史返回后 `loadSession()` 不会在同一微任务内完成 full metrics，必须等 chunk timers 推进后才完成。`src/stores/useChatStore.ts` 新增 `SESSION_HISTORY_FULL_MAP_CHUNK_SIZE`、`deferSessionHistoryMapChunk()`、`mapHistoryMessagesInChunks()`，仅用于后台 `get_unified_session_messages` 的 full-map；首屏 window 仍用同步 `mapHistoryMessages()`。因为分块引入额外 await，full-map 完成后、写 metrics/cache 前增加一次 `loadToken` 校验，避免用户发送新消息或切换会话后旧 full-map 再写入 stale metrics/cache。`.trellis/spec/frontend/state-management.md` 与 `.trellis/spec/frontend/component-guidelines.md` 已同步后台完整历史映射分块契约。
- 本轮验证：RED：`npm test -- src/stores/useChatStore.test.ts` 先失败 1 项，失败点为完整历史返回后 metrics 已同步变成 `complete/fullMessageCount=1500`。GREEN：同命令通过（1 file / 53 tests）。相邻历史验证 `npm test -- src/stores/useChatStore.test.ts src/utils/chatNavigation.test.ts src/utils/chatUiBehavior.test.ts src/utils/chatStatusSummary.test.ts` 通过（4 files / 86 tests）。全量验证通过：`npm test`（36 files / 312 tests，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests + 2 doctests ignored）、IDE `build_project`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。IDE build 生成的未跟踪 `out/` 已确认位于工作区内并删除。未做真实 Tauri 大历史点击性能点测；因此实际桌面体感改善仍标注为未验证。
- 下一步候选：1) 做真实 Tauri 桌面大历史点击点测，观察 `lastSessionLoadMetrics.windowLoadMs/windowMapMs/fullLoadMs/fullMapMs/elapsedMs`，确认卡顿是否从 full-map 转移到 IPC payload 或后端 full reader；2) 如果仍卡，下一轮优先优化后台完整历史读取策略，例如延迟 full load 到 idle、更低优先级触发、或仅在显式搜索/展开早期历史时拉完整历史；3) 继续做 cc-gui 与本项目真实运行画面并排截图审计，覆盖输入区、右栏、长历史消息排版和大会话加载指标；4) 继续审计功能重复入口，尤其右侧 `StatusPanel` 与输入区状态条在 tasks/subagents/edits/MCP 上的信息层级是否还可进一步合并。

## 本轮 PLAN 2026-06-19 chat large history deferred full load

- 目标：继续优化大型历史会话点击后的体感卡顿。上一轮已经把后台 full-map 分块让出事件循环，但 `loadSession()` 在首屏 window 写入后仍立即发起 `get_unified_session_messages`，大文件读取与 IPC payload 仍可能在首屏刚出现时抢占交互；本轮把 full history request 本身延迟到首屏提交后的短定时边界。
- 功能点 1：恢复上下文。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：根因诊断。验证方式：用 CodeGraph 读取 `useChatStore.loadSession()`、`mapHistoryMessagesInChunks()` 和历史加载测试，确认 full request 在首屏 `set(...)` 后同一异步链路立即启动。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/stores/useChatStore.test.ts`，断言首屏 window 已写入 `messages` 后，未推进 defer timer 前 `get_unified_session_messages` 不能被调用。
- 功能点 4：实现最小性能改动。验证方式：新增 `SESSION_HISTORY_FULL_LOAD_DEFER_MS` 和 `deferSessionHistoryFullLoad()`，只在 partial window 后、full request 前等待，并在等待后再次检查 session-load token；不改首屏 window size、不改后端命令、不改 `MessageList` 渲染窗口。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/state-management.md` 与 `.trellis/spec/frontend/component-guidelines.md`，记录 full request defer、token re-check 和 full-map yielding 的组合契约。
- 功能点 6：质量验证。验证方式：运行定向 `useChatStore` 测试、相邻历史/UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 IDE 生成的 `out/`。

## 迭代记录 2026-06-19 chat large history deferred full load

- 上轮实际进展：上一轮记录声称后台完整历史映射已分块让出事件循环；代码复查确认 `src/stores/useChatStore.ts` 已存在 `SESSION_HISTORY_FULL_MAP_CHUNK_SIZE`、`deferSessionHistoryMapChunk()`、`mapHistoryMessagesInChunks()`，并且 full-map 完成后有 token 再校验。记录中仍标注真实 Tauri 大历史点击体感未验证，本轮没有做桌面点测。
- 本轮诊断（补齐 cc-gui）：cc-gui 对标目标是历史会话点击后尽快可读、可滚动。本项目虽然已经首屏只拉 120 条并只渲染可见尾部，但 CodeGraph 显示 `loadSession()` 在首屏 window `set(...)` 之后马上执行 `get_unified_session_messages`；对几千条历史来说，后端读取、IPC 传输和随后映射会在首屏刚出现时继续竞争资源。
- 本轮诊断（超越 cc-gui）：后台完整历史是缓存/诊断能力，不应和用户刚看到的首屏交互抢优先级。延迟 full request 后，用户能先滚动、继续输入或发送；如果用户在这段窗口内切换会话或发送新消息，token 再校验会直接取消旧 full request，避免无意义的大 payload。
- 本轮规划：只改 store 内部历史加载时序与测试，不改后端命令、不改 UI 样式、不改历史窗口大小、不改 full-map 分块逻辑。优先级高：用户价值高（直接对应“大历史加载到聊天区域依然卡顿”）、出现频率中高（每次打开大历史）、成本低（单 helper + 测试调整）、风险可控（首屏路径和最终 cache/metrics 语义保留）。
- 本轮完成：`src/stores/useChatStore.test.ts` 新增回归 `defers the background full history request after first-paint window`，RED 时失败点为当前实现已经调用 2 次 invoke；同步调整 4 个既有大历史时序测试，让它们显式等待 defer 边界，或在发送新消息后断言 stale full request 不再启动。`src/stores/useChatStore.ts` 新增 `SESSION_HISTORY_FULL_LOAD_DEFER_MS = 50` 与 `deferSessionHistoryFullLoad()`，在 partial window 后、full request 前等待，并在等待后检查 `loadToken`。`.trellis/spec/frontend/state-management.md` 和 `.trellis/spec/frontend/component-guidelines.md` 已同步该契约。
- 本轮验证：RED：`npm test -- src/stores/useChatStore.test.ts` 先失败 1 项，失败点为 `get_unified_session_messages` 在首屏后已被立即调用。GREEN：同命令通过（1 file / 54 tests）。相邻历史验证 `npm test -- src/stores/useChatStore.test.ts src/utils/chatNavigation.test.ts src/utils/chatUiBehavior.test.ts src/utils/chatStatusSummary.test.ts` 通过（4 files / 87 tests）。全量验证通过：`npm test`（36 files / 313 tests，仅 Browserslist/caniuse-lite 过期提示）、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests + 2 doctests ignored）、IDE `build_project`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。IDE build 生成的 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并删除。
- 未验证项：未启动真实 Tauri 桌面做大历史点击点测，因此实际体感改善仍需下一轮用 `lastSessionLoadMetrics` 和桌面交互观察确认。
- 下一步候选：1) 真实 Tauri 桌面大历史点测：点击大历史后记录 window/full/map 分段指标，并观察首屏后 50ms 内滚动、输入、发送是否可响应；2) 若仍卡，优先定位 fullLoadMs 是后端 reader 还是 IPC payload，再决定是否把 full load 改为显式搜索/展开早期历史时才触发；3) 做 cc-gui 与本项目真实运行画面并排截图审计，覆盖输入区、右栏、长历史消息排版和大会话加载指标；4) 继续审计功能重复入口，尤其右侧 `StatusPanel` 与输入区状态条在 tasks/subagents/edits/MCP 上的信息层级是否还可进一步合并。

## 本轮 PLAN 2026-06-19 chat large history idle full load

- 目标：继续降低大型历史会话首屏后的交互抢占风险。上一轮把 full history request 延迟到短 timer 后，但在 Chromium/Tauri 环境里更合适的边界是优先等待 `requestIdleCallback`，只在不支持 idle API 时才走 timer fallback。
- 功能点 1：恢复上下文。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范、package 命令和路径可读性；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：根因诊断。验证方式：用 CodeGraph 读取 `useChatStore.loadSession()`、`MessageList` 和 `ChatPage` 搜索路径，确认当前 full request 仍是自动后台缓存/诊断任务，搜索尚未显式接入完整历史。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/stores/useChatStore.test.ts`，模拟 `globalThis.requestIdleCallback` 可用，断言首屏后推进普通 timers 不会启动 full request，只有触发 idle callback 后才调用 `get_unified_session_messages`。
- 功能点 4：实现最小性能改动。验证方式：`deferSessionHistoryFullLoad()` 优先使用 `requestIdleCallback({timeout})`，fallback 保持现有 timer；不改后端命令、不改首屏 window、不改 full-map 分块、不改 UI。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/state-management.md` 与 `.trellis/spec/frontend/component-guidelines.md`，记录 full request 应优先走 idle boundary。
- 功能点 6：质量验证。验证方式：运行定向 `useChatStore` 测试、相邻历史/UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理构建生成物。

## 迭代记录 2026-06-19 chat large history idle full load

- 上轮实际进展：上一轮已把 full history request 从首屏同一异步链路延后到短 timer，并在 timer 后再次检查 session-load token；代码复查确认 `src/stores/useChatStore.ts` 已有 `SESSION_HISTORY_FULL_LOAD_DEFER_MS` 和 `deferSessionHistoryFullLoad()`，`src/stores/useChatStore.test.ts` 已覆盖 timer fallback。仍未做真实 Tauri 大历史点击体感点测。
- 本轮诊断（补齐 cc-gui）：cc-gui 对标目标是历史会话点击后首屏尽快可读、交互不被后台历史处理抢占。本项目上一轮 timer defer 已避免同一微任务立即发 full request，但在 Tauri/Chromium 里还能更进一步：优先等浏览器 idle callback，让 full request 只在主线程空闲或 bounded timeout 后启动。
- 本轮诊断（超越 cc-gui）：full history 目前只是缓存/诊断后台任务，搜索尚未显式接入完整历史；因此它不应该和用户首屏后的滚动、输入、发送争抢调度。`requestIdleCallback` 能把这个后台任务放到更合适的浏览器调度点，fallback timer 保证非浏览器测试/旧环境仍可完成。
- 本轮规划：只改 `deferSessionHistoryFullLoad()` 调度边界与测试，不改后端命令、不改首屏窗口、不改 full-map 分块、不改 UI 或搜索行为。优先级中高：用户价值高（继续降低大历史体感卡顿）、出现频率中（大历史点击）、成本低（单 helper + 单测试）、风险低（fallback 保持现有 timer 行为）。
- 本轮完成：`src/stores/useChatStore.test.ts` 新增回归 `waits for browser idle before the background full history request when available`，RED 时失败点为 `requestIdleCallback` 未被调用。`src/stores/useChatStore.ts` 为 `deferSessionHistoryFullLoad()` 增加 `requestIdleCallback(() => resolve(), {timeout: SESSION_HISTORY_FULL_LOAD_DEFER_MS})` 优先路径，环境不支持时仍走 `setTimeout`。`.trellis/spec/frontend/state-management.md` 与 `.trellis/spec/frontend/component-guidelines.md` 已同步 idle boundary 契约。
- 本轮验证：RED：`npm test -- src/stores/useChatStore.test.ts` 先失败 1 项，失败点为 `requestIdleCallback` 调用次数为 0。GREEN：同命令通过（1 file / 55 tests）。相邻历史验证 `npm test -- src/stores/useChatStore.test.ts src/utils/chatNavigation.test.ts src/utils/chatUiBehavior.test.ts src/utils/chatStatusSummary.test.ts` 通过（4 files / 88 tests）。全量验证通过：`npm test`（36 files / 314 tests，仅 Browserslist/caniuse-lite 过期提示）、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests + 2 doctests ignored）、IDE `build_project`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。IDE build 生成的 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并删除。
- 未验证项：未做真实 Tauri 桌面大历史点击点测；因此实际桌面体感改善仍需用 `lastSessionLoadMetrics` 和手动滚动/输入观察确认。
- 下一步候选：1) 真实 Tauri 桌面大历史点测，观察首屏后 idle full-load 是否仍造成可感知卡顿；2) 如果仍卡，下一轮优先做“搜索/展开早期历史显式触发 full load”，避免默认后台拉完整 payload；3) 做 cc-gui 与本项目真实运行画面并排截图审计，覆盖输入区、右栏、长历史消息排版和大会话加载指标；4) 继续审计功能重复入口，尤其右侧 `StatusPanel` 与输入区状态条在 tasks/subagents/edits/MCP 上的信息层级是否还可进一步合并。

## 本轮 PLAN 2026-06-19 chat large history windowed default

- 目标：继续排查并降低大型历史会话加载到聊天区域后的卡顿。上一轮已把默认后台 full request 推迟到 idle，但用户仍反馈卡顿；本轮把普通历史点击收口为“首屏窗口即完成”，不再自动拉完整历史 payload，完整历史留给后续显式搜索/展开意图。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis 当前任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：根因诊断。验证方式：用 CodeGraph 读取 `useChatStore.loadSession()`、`ChatSessionLoadMetrics`、`MessageList`、`ChatPage` 搜索路径和 `StatusPanel` 指标展示，确认首屏窗口已隔离，但 full request 仍默认启动且搜索尚未消费完整历史。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/stores/useChatStore.test.ts`，断言 incomplete 大历史在 timers/idle 后仍不调用 `get_unified_session_messages`，`loadSession()` 结束为 `windowed`；扩展 `src/components/chat/StatusPanel.test.tsx`，断言 `windowed` 展示为“窗口就绪”且默认折叠。
- 功能点 4：实现最小性能改动。验证方式：`useChatStore.loadSession()` 在 partial window 后写入 `windowed` metrics 并返回；删除普通路径的后台 full request / full-map 分块调度；`StatusPanel` 增加 `windowed` 标签；不改后端命令、不改首屏窗口大小、不改 `MessageList` 渲染窗口。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/state-management.md` 与 `.trellis/spec/frontend/component-guidelines.md`，记录普通浏览不自动完整读取、partial window 是 `windowed` 完成态，完整历史必须由显式意图触发。
- 功能点 6：质量验证。验证方式：运行定向 store / StatusPanel 测试、相邻历史/UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 IDE 生成的 `out/`。

## 迭代记录 2026-06-19 chat large history windowed default

- 上轮实际进展：上一轮记录声称 full history request 已优先等 `requestIdleCallback` 后再启动；代码复查确认 `src/stores/useChatStore.ts` 的 `loadSession()` 在 partial window 后仍会继续进入 `get_unified_session_messages`，只是推迟到 idle/timer，搜索路径仍只消费当前 `messages` 窗口。也就是说，后台 full payload 对普通浏览没有直接用户价值，却仍可能造成大会话点击后的体感卡顿。已知阻塞仍是真实 Tauri 桌面大历史点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 对标目标是历史会话点击后最近内容优先可读、可滚动。本项目已实现 tail window 和可见窗口渲染，但普通历史点击仍默认拉完整历史，导致后端读文件、IPC 传输和前端映射在用户刚进入会话时继续发生。根因不是首屏窗口本身，而是“后台完整读取”仍绑定在普通浏览动作上。
- 本轮诊断（超越 cc-gui）：把 partial 大历史明确标记为 `windowed` 完成态后，用户可以立即浏览最近窗口，右栏显示“窗口就绪”而不是一直“加载中”；完整历史只应在未来搜索全历史或显式展开早期历史时触发。这个取舍解决的痛点是大会话点击后 UI 仍被无感后台任务抢占，而不是单纯延后后台任务。
- 本轮规划：只改 store 的普通历史加载边界和右栏指标展示，不改后端命令、不改 tail window 大小、不改 `MessageList` 渲染窗口、不新增依赖。优先级高：用户价值高（直接对应大会话卡顿反馈）、出现频率中高（所有大型历史点击）、成本中低（store + 指标展示 + 测试）、风险可控（首屏窗口和完整窗口缓存语义保留）。
- 本轮完成：`src/types/session.ts` 为 `ChatSessionLoadMetrics.status` 新增 `windowed`。`src/stores/useChatStore.ts` 删除普通路径的 idle/timer full request 和 full-map 分块逻辑；partial window 映射后直接写入 `windowed` metrics、`completedAt`、`elapsedMs` 并结束本次 `loadSession()`；complete window 仍写入缓存。`src/components/chat/StatusPanel.tsx` 将 `windowed` 显示为完成类状态但标签为“窗口就绪”。`src/locales/en.json` / `src/locales/zh.json` 增加 `sessionLoadWindowed`。`src/stores/useChatStore.test.ts` 改为验证 incomplete 大历史不调用 `get_unified_session_messages`、partial window 不进完整缓存、窗口指标无 full-stage 噪声。`src/components/chat/StatusPanel.test.tsx` 增加 `windowed` 指标默认折叠展示回归。`.trellis/spec/frontend/state-management.md` 与 `.trellis/spec/frontend/component-guidelines.md` 已同步普通浏览不自动完整读取、显式 full-history 意图边界和 `windowed` 指标契约。
- 本轮验证：RED：`npm test -- src/stores/useChatStore.test.ts` 先失败 1 项，失败点为 `loadSession()` 未结束且等待自动 full request；`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为 `windowed` 被展示为“完成”而非“窗口就绪”。GREEN：两条定向测试分别通过（store：1 file / 53 tests；StatusPanel：1 file / 20 tests）。相邻验证通过：`npm test -- src/stores/useChatStore.test.ts src/utils/chatNavigation.test.ts src/utils/chatUiBehavior.test.ts src/utils/chatStatusSummary.test.ts`（4 files / 86 tests）；`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ButtonArea.test.tsx`（4 files / 38 tests）。全量验证通过：`npm test`（36 files / 313 tests，仅 Browserslist/caniuse-lite 过期提示）、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests + 2 doctests ignored）、IDE `build_project`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。IDE build 生成的 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并删除。
- 未验证项：未启动真实 Tauri 桌面做大历史点击体感点测；因此“实际桌面卡顿是否完全消失”仍需下一轮用真实历史会话操作确认。
- 下一步候选：1) 真实 Tauri 桌面大历史点测：点击大历史后确认最近窗口出现、右栏显示“窗口就绪”、无后台 full-load 指标、滚动/输入/发送可响应；2) 设计并实现显式 full-history intent，例如搜索框输入后再按需拉完整历史索引，避免普通浏览付出完整 payload 成本；3) 继续做 cc-gui 与本项目真实运行画面并排截图审计，覆盖输入区布局、任务/子代理/编辑状态层级、provider/model selector 图标对齐和右栏密度；4) 继续审计重复入口，重点看右侧 `StatusPanel` 与输入区状态条在 tasks/subagents/edits/MCP 上是否还存在信息重复。

## 本轮 PLAN 2026-06-19 chat input status popover panel

- 目标：继续优化 Chat 输入区布局排版。当前 `ChatInputStatusTabs` 的 active details panel 渲染在普通文档流里，展开 tasks/subagents/edits/MCP 时会挤占 composer 周边高度；本轮将详情面板改为贴近 cc-gui 状态面板交互的浮层 popover，不参与布局推挤。
- 功能点 1：恢复上下文与差距确认。验证方式：读取 `TODO_LIST.md`、Trellis 当前任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取本项目 `ChatInputStatusTabs` / `ButtonArea` / `SelectorDropdown` / `ModelIcon` / `MessageList`，以及 cc-gui `StatusPanel` / `ButtonArea` / `ProviderModelIcon`，确认本项目状态 strip 已 compact，但 active panel 仍是文档流详情块；cc-gui 状态详情为 popover。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ChatInputStatusTabs.test.tsx`，断言打开 tab 时外层容器提供 `relative` 锚点，详情面板带 `chat-input-status-popover-panel absolute bottom-full` 与高 z-index，且仍渲染当前 tab 内容。
- 功能点 4：实现最小展示层改动。验证方式：只调整 `ChatInputStatusTabs` active panel 的 class/布局锚点；不改 openTab 状态、不改 tasks/subagents/edits/MCP 数据派生、不改右侧 `StatusPanel`、不改历史加载。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 composer strip 的详情层应是浮层 quick detail，不参与 composer 文档流高度。
- 功能点 6：质量验证。验证方式：运行定向 `ChatInputStatusTabs` 测试、相邻 status/composer 测试、`npm test`、`npm run build`、Rust check/test、IDE build 和 diff 空白检查；真实 Tauri 桌面点测若未执行需明确标注。

## 迭代记录 2026-06-19 chat input status popover panel

- 上轮实际进展：上一轮已把大会话普通历史点击收口为 `windowed` 完成态，不再自动拉完整历史 payload；代码复查确认 `src/stores/useChatStore.ts` partial window 后会写入 `windowed` metrics 并返回，`StatusPanel` 会显示“窗口就绪”。已知阻塞仍是真实 Tauri 桌面大会话体感点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：CodeGraph 对标显示 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 的 tasks/subagents/files 详情是 tab header 下的 popover 内容；本项目 `src/components/chat/ChatInputStatusTabs.tsx` 虽已将 collapsed entries 收口为 quick-entry chip，但 active panel 仍以 `mt-1` 普通块渲染，展开时会参与输入区周边布局高度。
- 本轮诊断（超越 cc-gui）：本项目保留右侧 `StatusPanel` 的完整诊断和输入区 strip 的小屏 fallback，比 cc-gui 信息层级更完整；将 composer strip 详情改为 anchored popover 后，用户打开 tasks/subagents/edits/MCP 只是在输入区上方临时查看，不会推挤 composer、消息列表或底部操作按钮，直接缓解“输入区域布局排版不合理”和“页面元素挤”的痛点。
- 本轮规划：只改 `ChatInputStatusTabs` 的布局 class 和测试，不改 openTab、状态 summary 派生、MCP 数据、右侧状态面板、历史加载或 provider/model 图标。优先级中高：用户价值中高（减少展开详情导致的布局跳动）、出现频率高（composer 常驻）、实现成本低（单组件 + 测试）、风险低（纯展示层）。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.test.tsx` 新增回归 `renders the open status details as an anchored popover so composer layout is not pushed`，覆盖 strip 容器 `relative` 锚点、详情面板 `chat-input-status-popover-panel absolute bottom-full z-[30]` 和内容保留。`src/components/chat/ChatInputStatusTabs.tsx` 将状态 strip 内容容器改为相对定位，并将 active panel 改为 `bottom-full` 绝对定位浮层，增加最大高度与内部滚动，避免详情过长时挤压输入区。`.trellis/spec/frontend/component-guidelines.md` 已同步 composer strip quick-detail popover 契约。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 1 项，失败点为缺少 `relative` 锚点和 `chat-input-status-popover-panel`。GREEN：同命令通过（1 file / 10 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ButtonArea.test.tsx`（4 files / 39 tests）；`npm test -- src/components/chat/composer/SelectorDropdown.test.tsx src/components/chat/composer/ButtonArea.test.tsx`（2 files / 8 tests）。全量验证通过：`npm test`（36 files / 314 tests，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests + 2 doctests ignored）、IDE `build_project`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。IDE build 生成的 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并删除。
- 未验证项：未启动真实 Tauri 桌面做状态 strip 展开/收起视觉点测；因此浮层在真实窗口尺寸、窗口边缘和不同主题下的最终视觉表现仍需下一轮截图确认。
- 下一步候选：1) 真实 Tauri 桌面点测输入区状态 popover：打开 tasks/subagents/edits/MCP 时 composer 不跳动、浮层不遮挡关键按钮、长列表可滚动；2) 真实 Tauri 桌面大会话点测：确认 `windowed` 入口体感、右栏“窗口就绪”、滚动/输入/发送响应；3) 设计显式 full-history intent：搜索或展开早期历史时再拉完整历史，避免普通浏览付出全量 payload 成本；4) 继续做 cc-gui 与本项目运行画面并排截图审计，重点覆盖 provider/model selector 图标对齐、任务/子代理/编辑信息层级和输入区布局。

## 本轮 PLAN 2026-06-19 chat input status popover dismiss

- 目标：补齐输入区状态浮层的关闭交互。上一轮已把 `ChatInputStatusTabs` 详情改为 anchored popover，但当前只能再次点击同一 tab 关闭；本轮增加点击浮层外部和按 `Escape` 关闭，避免临时详情层持续遮挡输入区上方内容。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis 当前任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 对标诊断。验证方式：读取本项目 `ChatInputStatusTabs.tsx` / 测试与 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx`，确认 cc-gui popover 有 outside-click close，本项目状态浮层缺少外部点击/Escape 关闭。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ChatInputStatusTabs.test.tsx`，先断言本地 helper 对 inside/outside click 与 Escape/非 Escape key 的关闭判断；初始 RED 为 helper 尚未导出。
- 功能点 4：实现最小交互改动。验证方式：`ChatInputStatusTabs` 增加 root ref 和 `useEffect`，仅在 `activeOpenTab` 存在时监听 `mousedown` / `keydown`，用纯 helper 判断关闭；不改 tab 数据派生、不改浮层布局、不改右侧 `StatusPanel`。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 composer strip quick-detail popover 应支持 outside click 和 Escape dismissal。
- 功能点 6：质量验证。验证方式：运行定向 `ChatInputStatusTabs` 测试、相邻 status/composer 测试、全量 `npm test`、`npm run build`、Rust check/test、IDE build 和 diff 空白检查；真实 Tauri 桌面点测若未执行需明确标注。

## 迭代记录 2026-06-19 chat input status popover dismiss

- 上轮实际进展：上一轮记录声称输入区状态详情已从文档流块改为 anchored popover；代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 的详情面板已具备 `chat-input-status-popover-panel absolute bottom-full z-[30]`，测试也覆盖了不推挤 composer 布局。仍未做真实 Tauri 桌面状态浮层视觉/点击点测。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：CodeGraph 对标显示 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 的浮层打开时会监听外部点击并关闭；本项目 `ChatInputStatusTabs` 上一轮虽已改为浮层，但只能再次点击同一 tab 关闭。用户打开 tasks/subagents/edits/MCP 详情后，如果继续操作输入区或消息区，浮层会持续遮挡输入区上方内容。
- 本轮诊断（超越 cc-gui）：本项目输入区状态条承担小屏 fallback 和快速查看职责，比右侧 `StatusPanel` 更靠近用户的输入动作；因此 quick-detail popover 必须具备轻量关闭路径。outside click 解决“看完后继续点别处仍被遮挡”的痛点，`Escape` 解决键盘流用户不想离开输入区也能收起浮层的痛点。
- 本轮规划：只改 `ChatInputStatusTabs` 浮层关闭交互和测试，不改 tasks/subagents/edits/MCP 数据派生、不改浮层定位、不改右侧 `StatusPanel`、不改大会话加载策略。优先级中高：用户价值中高（减少遮挡和误停留）、出现频率高（composer 常驻）、实现成本低（单组件 + helper 测试）、风险低（监听仅在 open tab 存在时启用）。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.test.tsx` 新增回归 `treats outside pointer events and Escape as status popover dismiss actions`，覆盖 inside/outside pointer 与 Escape/非 Escape key 的关闭判断。`src/components/chat/ChatInputStatusTabs.tsx` 新增 `shouldDismissInputStatusPopoverForPointer()`、`shouldDismissInputStatusPopoverForKey()`、`popoverRootRef` 和 open 状态下的 `mousedown` / `keydown` 监听；外部点击或 Escape 时调用 `setOpenTab(null)`，组件卸载或关闭后自动移除监听。`.trellis/spec/frontend/component-guidelines.md` 已同步 composer strip quick-detail popover 应支持 outside click 和 Escape dismissal。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 1 项，失败点为 `shouldDismissInputStatusPopoverForPointer is not a function`。GREEN：同命令通过（1 file / 11 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/SelectorDropdown.test.tsx`（5 files / 41 tests）；`npm test -- src/utils/chatStatusSummary.test.ts src/utils/chatUiBehavior.test.ts src/utils/chatNavigation.test.ts`（3 files / 33 tests）。全量验证通过：`npm test`（36 files / 315 tests，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests + 2 doctests ignored）、`npm run build`、IDE `build_project`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。IDE build 生成的 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并删除。
- 未验证项：未启动真实 Tauri 桌面做状态浮层 outside click / Escape 点测；因此真实窗口尺寸、焦点状态和主题下的最终交互仍需下一轮手动确认。
- 下一步候选：1) 真实 Tauri 桌面点测输入区状态 popover：打开 tasks/subagents/edits/MCP 后点击消息区、输入框、右栏并按 Escape，确认浮层关闭且 composer 不跳动；2) 真实 Tauri 桌面大会话点测：确认 `windowed` 最近窗口出现、右栏“窗口就绪”、滚动/输入/发送响应；3) 设计显式 full-history intent：搜索或展开早期历史时再拉完整历史，避免普通浏览付出全量 payload 成本；4) 做 cc-gui 与本项目运行画面并排截图审计，重点覆盖 provider/model selector 图标对齐、任务/子代理/编辑信息层级和输入区布局；5) 继续审计重复入口，重点看右侧 `StatusPanel` 与输入区状态条在 tasks/subagents/edits/MCP 上是否还存在信息重复。

## 本轮 PLAN 2026-06-19 chat input status desktop fallback density

- 目标：减少宽屏桌面下输入区状态条与右侧 `StatusPanel` 的功能重复。当前 `StatusPanel` 在 `xl` 断点可见时已经承载 tasks/subagents/edits/MCP 详情，但 `ChatInputStatusTabs` 仍在 composer 上方渲染同类 quick tabs；本轮将这些 expandable status tabs 收口为小屏 fallback，宽屏仅保留 Git 分支上下文。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 对标诊断。验证方式：读取本项目 `ChatInputStatusTabs.tsx` / 测试 / `ChatPage.tsx` 与 cc-gui `StatusPanel`，确认右侧状态面板是桌面主详情区，输入区 strip 应承担小屏 fallback 和 Git 近输入上下文。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ChatInputStatusTabs.test.tsx`，断言 `collapseStatusTabsOnDesktop` 开启时 tasks/subagents/edits/MCP tab 带 `xl:hidden`，Git chip 不带 `xl:hidden`，打开 tab 的 popover 也带 `xl:hidden`。
- 功能点 4：实现最小展示层改动。验证方式：`ChatInputStatusTabs` 新增展示层 prop 并在 tab/panel class 上响应；`ChatPage` 在右侧 `StatusPanel` 存在的桌面布局中传入该 prop；不改 status summary 派生、不改 Zustand、不改右侧 `StatusPanel` 数据。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 和 `.trellis/spec/frontend/state-management.md`，记录 composer status tabs 是小屏 fallback，桌面右栏可见时不应重复同类 expandable tabs。
- 功能点 6：质量验证。验证方式：运行定向 `ChatInputStatusTabs` 测试、相邻 status/composer 测试、全量 `npm test`、`npm run build`、Rust check/test、IDE build 和 diff 空白检查；真实 Tauri 桌面点测若未执行需明确标注。

## 迭代记录 2026-06-19 chat input status desktop fallback density

- 上轮实际进展：上一轮已补齐输入区状态 popover 的 outside click / `Escape` 关闭；代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已有 `popoverRootRef`、open 状态下的 `mousedown` / `keydown` 监听和纯 helper 测试。仍未做真实 Tauri 桌面 popover 点击点测。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：CodeGraph 显示本项目 `src/pages/ChatPage.tsx` 的右侧 `StatusPanel` 在 `xl` 断点通过 `hidden xl:block` 常驻，已承载 tasks/subagents/edits/MCP 详情；`ChatInputStatusTabs` 同时在 composer 上方渲染同类 expandable tabs。cc-gui 的状态详情更集中在状态面板/浮层里，本项目宽屏下出现了用户指出的“页面元素功能重复”。
- 本轮诊断（超越 cc-gui）：输入区状态条在本项目仍有价值，但应该聚焦小屏 fallback 和发送前上下文。把 tasks/subagents/edits/MCP tabs 设为 `xl:hidden` 后，桌面用户主要看右栏完整诊断，窄屏用户仍能在 composer 附近展开 quick detail；Git 分支 chip 保留在桌面，因为它不是重复详情面板，而是发送前的轻量工作区上下文。
- 本轮规划：只做展示层密度收口，不改 `buildChatStatusSummary()`、`mergeChatInputStatusSummary()`、Zustand、MCP/任务/编辑数据来源，也不改右侧 `StatusPanel`。优先级中高：用户价值中高（直接减少重复入口和输入区拥挤）、出现频率高（Chat 常驻）、成本低（单组件 prop + 页面传参 + 测试/规范）、风险低（CSS 响应式隐藏，数据仍可用）。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.test.tsx` 新增回归 `keeps expandable status tabs as a small-screen fallback when the desktop status panel is visible`，RED 时失败点为旧实现缺少 `xl:hidden`。`src/components/chat/ChatInputStatusTabs.tsx` 新增 `collapseStatusTabsOnDesktop` 展示 prop；开启时 tasks/subagents/edits/MCP tab 和 popover 带 `xl:hidden`，无 Git 且只有 status tabs 时整个 strip 在桌面隐藏，避免空边框占位；Git chip 不带 `xl:hidden`。`src/pages/ChatPage.tsx` 在渲染 `ChatInputStatusTabs` 时传入 `collapseStatusTabsOnDesktop`，与同布局下 `StatusPanel` 的 `hidden xl:block` 对齐。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 已同步该展示层契约。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 1 项，失败点为旧实现没有桌面 fallback class。GREEN：同命令通过（1 file / 12 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/SelectorDropdown.test.tsx`（5 files / 42 tests）；`npm test -- src/utils/chatStatusSummary.test.ts src/utils/chatUiBehavior.test.ts src/utils/chatNavigation.test.ts`（3 files / 33 tests）。全量验证通过：`npm test`（36 files / 316 tests，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests + 2 doctests ignored）、IDE `build_project`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。IDE build 生成的 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并删除。
- 未验证项：未启动真实 Tauri 桌面做宽屏/窄屏视觉点测；因此 `xl` 断点下右栏与 composer 状态条的真实视觉密度仍需下一轮截图确认。
- 下一步候选：1) 真实 Tauri 桌面截图/点测：宽屏确认 tasks/subagents/edits/MCP 只在右栏出现、Git chip 保留，窄屏确认 quick tabs 仍可展开；2) 做 cc-gui 与本项目运行画面并排截图审计，覆盖输入区布局、右栏密度、provider/model selector 图标对齐和大会话加载指标；3) 继续设计显式 full-history intent：搜索或展开早期历史时再拉完整历史，避免普通浏览付出全量 payload 成本；4) 继续审计输入区功能入口，尤其 provider/model/mode/reasoning/refresh/enhance/send 在窄宽下是否还有可合并或可降噪的重复信息。

## 本轮 PLAN 2026-06-19 chat runtime layout smoke desktop mobile

- 目标：补齐上一轮缺失的真实运行态布局验证。基于 Vite Web 运行环境检查 Chat 页面在宽屏 `xl` 和窄屏下的状态面板、输入区状态 strip、composer toolbar 和状态浮层关闭交互，确认上一轮“桌面右栏主诊断、小屏 composer fallback”的展示层契约是否真的生效。
- 功能点 1：恢复上下文。验证方式：读取 `TODO_LIST.md`、Trellis 当前任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 仍不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 复核组件边界。验证方式：读取 `ChatInputStatusTabs` / `ChatPage` / `StatusPanel` 相关源码，确认本轮只验证上一轮展示层契约，不扩大到 store、后端命令或历史加载策略。
- 功能点 3：启动本地运行态并检查 Web 限制。验证方式：运行 `npm run dev -- --host 127.0.0.1 --port 5173`，打开 `http://127.0.0.1:5173/#/chat`，记录是否出现 Tauri bridge 限制；若出现，明确标注 Vite Web 不等同于真实 Tauri 桌面验证。
- 功能点 4：宽屏/窄屏布局烟测。验证方式：在 `1440x900` 检查 `chat-status-pane-shell hidden xl:block` 可见、`chat-input-status-tabs ... xl:hidden` 隐藏、composer action buttons 固定 28px；在 `900x800` 检查右栏隐藏、输入区 status strip 可见、输入框和主操作按钮未被挤压。
- 功能点 5：小屏 fallback 浮层交互烟测。验证方式：点击 `MCP 0 / 0` 入口，检查 `chat-input-status-popover-panel absolute bottom-full` 出现在 strip 上方、不推挤 textarea；点击 textarea 和按 `Escape` 后浮层关闭。
- 功能点 6：质量验证与清理。验证方式：运行相邻 Chat UI 测试、`npm run build`、`git diff --check`、`git diff --cached --check`；恢复浏览器视口，停止本轮 Vite 服务。

## 迭代记录 2026-06-19 chat runtime layout smoke desktop mobile

- 上轮实际进展：上一轮记录声称 `ChatInputStatusTabs` 已通过 `collapseStatusTabsOnDesktop` 把 tasks/subagents/edits/MCP tabs 收口为小屏 fallback；代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 的 root strip、tab 和 popover 均具备 `xl:hidden` 条件，`src/pages/ChatPage.tsx` 已传入该 prop，`src/components/chat/ChatInputStatusTabs.test.tsx` 已覆盖桌面 fallback class。已知阻塞是未做真实运行态宽/窄屏点测。
- 本轮诊断（补齐 cc-gui）：cc-gui 在桌面下集中展示状态详情，本项目上一轮已按同类信息层级把桌面诊断交给右栏 `StatusPanel`，但此前只有单元测试与构建验证，缺少运行态 CSS 断点证据。用户指出“页面元素功能重复、输入区布局排版不合理”，因此需要确认 `xl` 断点下重复入口确实消失，而不是只在测试字符串层面存在。
- 本轮诊断（超越 cc-gui）：本项目比 cc-gui 多了小屏 composer fallback 和右侧完整诊断的双层信息架构；运行态验证的价值是确认这两层在不同宽度下互斥承接，避免桌面重复、窄屏丢信息，并确认浮层不推挤输入区，解决用户在真实窗口中感受到的“挤在一起”和“元素重复”。
- 本轮规划：本轮只做验证和记录，不改业务代码。优先级高：用户价值高（补上上一轮最大未验证项）、出现频率高（Chat 首屏常驻）、实现成本低（运行态检查 + 相邻测试）、风险低（不改源码）。
- 本轮完成：启动 Vite Web 并打开 Chat 页面；`1440x900` 下实测 `chat-status-pane-shell hidden xl:block` display 为 `block`、右栏宽 320px，`chat-input-status-tabs ... xl:hidden` display 为 `none`，MCP 状态 tab display 为 `none`，composer enhance/send 按钮均为 `28x28` 且 selector icon box 为 `16x16`。`900x800` 下实测右栏和拖拽条 display 为 `none`，输入区 status strip display 为 `block`，MCP fallback tab display 为 `flex`，textarea 与 toolbar/actions 仍有稳定尺寸。小屏点击 `MCP 0 / 0` 后，`chat-input-status-popover-panel absolute bottom-full ... xl:hidden` 可见，浮层位于 strip 上方且 textarea 位置未变化；点击 textarea 后浮层关闭，重新打开后在输入框按 `Escape` 也关闭。浏览器视口已 reset，端口 `5173` 的 Vite node 进程已停止。
- 本轮验证：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/SelectorDropdown.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx` 通过（5 files / 42 tests）。`npm run build` 通过（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。`git diff --check` exit 0，仅输出既有 LF/CRLF 转换提示；`git diff --cached --check` exit 0。Vite Web 运行受限项：页面可渲染 Chat 主体，但 MCP 区域出现 `TypeError: Cannot read properties of undefined (reading 'invoke')`，原因是 Web 环境没有 Tauri bridge；因此本轮只能证明 DOM/CSS 布局合同，不能等同于真实 Tauri 桌面端完整功能验证。
- 未验证项：未启动 `npm run tauri dev` 做真实桌面窗口截图/点击验证；未用真实历史大会话复测 `windowed` 加载体感；未做 cc-gui 与本项目并排截图审计。
- 下一步候选：1) 启动真实 Tauri 桌面做宽/窄窗口截图点测，重点验证 Tauri bridge 下 MCP 错误不出现、右栏/输入区层级与 Web 烟测一致；2) 用真实大历史会话点测 `windowed` 首屏、右栏“窗口就绪”、滚动和输入响应，定位剩余卡顿来自后端 tail reader、IPC payload、React commit 还是摘要构建；3) 做 cc-gui 与本项目并排截图审计，继续检查任务列表、会话列表、provider/model/mode/reasoning/refresh/enhance/send 的重复入口和对齐；4) 设计显式 full-history intent：搜索或跳到更早历史时才拉完整历史，避免普通浏览触发全量 payload。

## 本轮 PLAN 2026-06-19 chat windowed history summary gate

- 目标：继续处理用户反馈的“大历史会话加载到聊天区域仍卡顿”。上一轮已验证布局，本轮聚焦前端普通浏览路径：当后端返回 `windowed` 大会话时，`ChatPage` 不再在 idle 回调中对整个已加载窗口再次构建完整状态摘要，只保留可见尾部摘要，减少加载后额外 raw/tool 扫描。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `useChatStore.loadSession()`、`MessageList`、`ChatPage`、`chatStatusSummary` 与 cc-gui `MessageList`，确认本项目已 window-first 和最近消息渲染，但 `ChatPage` 仍会对 `messages` 窗口构建 idle 完整摘要。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/utils/chatUiBehavior.test.ts`，断言新的纯判断函数在 `sessionLoadStatus: 'windowed'` 且非搜索、消息非空时返回 false；非 windowed 完成态仍返回 true，搜索/空消息返回 false。
- 功能点 4：实现最小行为改动。验证方式：在 `src/utils/chatUiBehavior.ts` 增加 `shouldBuildCompleteChatStatusSummary()`，`ChatPage` 的 idle 完整摘要 effect 使用该 gate；不改 `loadSession()`、不改后端 `get_unified_session_message_window`、不改 `MessageList` 渲染窗口。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/state-management.md` 和 `.trellis/spec/frontend/component-guidelines.md`，记录 `windowed` 大会话普通浏览不应触发全窗口状态摘要，未来完整摘要只能来自显式 full-history intent。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `chatUiBehavior` 测试、相邻 Chat 状态/导航/store 测试、`npm run build`、必要 Rust check、diff 空白检查；真实 Tauri 大会话体感若未执行需明确标注。

## 迭代记录 2026-06-19 chat windowed history summary gate

- 上轮实际进展：上一轮已用 Vite Web 对宽屏/窄屏 Chat 布局做运行态 smoke，确认桌面 `xl` 下右侧 `StatusPanel` 可见、输入区 tasks/subagents/edits/MCP quick tabs 隐藏，窄屏下输入区 fallback tabs 与 popover 可用；同时确认 Vite Web 没有 Tauri bridge，MCP 区域会出现 `invoke` 相关错误，不能等同真实 Tauri 桌面验证。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：本项目已通过 `useChatStore.loadSession()` 的 `windowed` 首屏策略和 `MessageList` 的最近消息窗口降低首屏压力，但 `ChatPage` 仍在 idle callback 中对整个当前 `messages` 窗口执行 `buildChatStatusSummary(messages)`，即使该窗口来自大历史 `windowed` 普通浏览。对大历史来说，这会在加载后继续扫描 120 条左右 raw/tool payload，形成用户感知的“首屏出来后仍卡一下”。cc-gui 的目标体验是最近内容先可读、状态信息按需展开，本项目这里还存在普通浏览路径上的额外摘要工作。
- 本轮诊断（超越 cc-gui）：本项目右栏和输入区状态条提供比 cc-gui 更丰富的 tasks/subagents/edits/MCP 摘要，但这些摘要不能以牺牲大会话进入体验为代价。`windowed` 普通浏览时只保留可见尾部摘要，完整摘要留给未来显式搜索/展开历史意图，可以解决“我只是想看最近回复，却被隐藏的历史统计拖慢”的痛点。
- 本轮规划：只给完整状态摘要构建增加纯函数 gate，不改 `loadSession()`、不改后端窗口读取、不改 `MessageList` 可见窗口、不改搜索行为。优先级高：用户价值高（直接对应大会话卡顿反馈）、出现频率中高（所有大型历史点击）、成本低（一个 helper + 一个 effect guard + 规范同步）、风险低（`complete` / `null` 状态仍保留原完整摘要行为，搜索仍不构建该 idle 摘要）。
- 本轮完成：`src/utils/chatUiBehavior.ts` 新增 `shouldBuildCompleteChatStatusSummary()`，规则为有消息、非搜索且 `sessionLoadStatus !== 'windowed'` 才允许构建完整摘要。`src/pages/ChatPage.tsx` 在 idle 完整摘要 effect 入口调用该 gate；`windowed` 时立即清空 `completeStatusSummaryState` 并返回，不再排队 `requestIdleCallback` / `setTimeout` 扫描完整当前窗口。`src/utils/chatUiBehavior.test.ts` 新增回归 `skips complete status summary work for windowed large-history browsing`，覆盖 `windowed` 跳过、`complete` / `null` 保留、搜索/空消息跳过。`.trellis/spec/frontend/state-management.md` 和 `.trellis/spec/frontend/component-guidelines.md` 已同步约定：自动加载的 `windowed` history 普通浏览不应触发全窗口 complete status summary，未来完整历史摘要只能来自显式 full-history intent。
- 本轮验证：RED：`npm test -- src/utils/chatUiBehavior.test.ts` 先失败，失败点为 `shouldBuildCompleteChatStatusSummary is not a function`。GREEN：同命令通过（1 file / 14 tests）。相邻链路验证通过：`npm test -- src/utils/chatUiBehavior.test.ts src/utils/chatStatusSummary.test.ts src/utils/chatNavigation.test.ts src/stores/useChatStore.test.ts`（4 files / 87 tests）。构建验证通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。全量前端验证通过：`npm test`（36 files / 317 tests，仅 Browserslist/caniuse-lite 过期提示）。空白检查通过：`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。
- 未验证项：本轮未跑 `cargo check --manifest-path src-tauri/Cargo.toml` / Rust 测试，因为改动仅限前端页面、前端纯 helper、前端测试和 Trellis 规范；未启动真实 Tauri 桌面，也未用真实大历史会话做体感点测，因此实际桌面卡顿改善仍需下一轮确认。
- 下一步候选：1) 真实 Tauri 桌面大历史点测：点击大历史后确认最近窗口出现、右栏显示“窗口就绪”、加载后不再因 complete status summary 出现额外停顿，并观察滚动/输入/发送响应；2) 若仍卡，继续拆分定位后端 tail reader、IPC payload、`mapHistoryMessages`、React commit、`getRecentRenderableMessages` / anchor/status 派生的耗时；3) 设计显式 full-history intent：搜索全历史或展开更早历史时再按需读取/索引完整历史，普通浏览保持 windowed；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看任务列表、会话列表、输入区按钮、provider/model 图标对齐和右栏密度；5) 继续审计重复入口，尤其右侧 `StatusPanel` 与输入区状态条、任务列表与会话列表之间是否还存在同类信息重复。

## 本轮 PLAN 2026-06-19 chat explicit full-history search intent

- 目标：补齐上一轮留下的显式 full-history intent。普通历史点击已经保持 `windowed`，但搜索仍只过滤当前窗口；本轮让用户输入搜索时才显式读取完整历史作为搜索源，普通浏览继续不自动 full-load。
- 功能点 1：恢复上下文与规范。验证方式：读取 `TODO_LIST.md`、Trellis 当前任务制品、`.trellis/spec/frontend/state-management.md`、`.trellis/spec/frontend/component-guidelines.md`、质量规范和共享思考指南；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `useChatStore.loadSession()`、`sessionHistoryCache`、`ChatPage` 搜索路径和 `MessageList`，确认 `MessageList` 搜索目前只消费传入的 `messages`，而 `windowed` 普通浏览不会拥有完整历史。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/stores/useChatStore.test.ts`，断言只有显式 action 会调用 `get_unified_session_messages`，返回完整历史但主 `messages` 保持最近窗口；扩展 `src/utils/chatUiBehavior.test.ts`，断言搜索触发条件仅在 active windowed session 且未加载/未加载中时成立。
- 功能点 4：实现最小行为改动。验证方式：`useChatStore` 新增 `loadActiveSessionFullHistory()`，复用完整历史缓存、token/active-session ownership 和分块映射；`ChatPage` 在搜索 windowed 会话时触发该 action，并把返回完整历史作为临时 `MessageList` / anchor / status 搜索源，不改普通浏览主 `messages`。
- 功能点 5：同步 Trellis 规范。验证方式：更新 state/component 规范，把“未来 full-history intent”改为当前搜索实现，并记录完整历史只作为搜索源和缓存，不得回灌普通 transcript。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻历史/导航/状态测试、全量 `npm test`、`npm run build`、IDE build、`git diff --check` / `git diff --cached --check`，清理 IDE 生成的 `out/`。

## 迭代记录 2026-06-19 chat explicit full-history search intent

- 上轮实际进展：上一轮把 `windowed` 大历史普通浏览下的 idle 完整状态摘要扫描关掉；代码复查确认 `ChatPage` 已通过 `shouldBuildCompleteChatStatusSummary()` 在 `windowed` 状态跳过 `buildChatStatusSummary(messages)`。已知阻塞仍是真实 Tauri 桌面大历史体感未验证。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：`loadSession()` 已按规范不再自动调用 `get_unified_session_messages()`，但 `ChatPage` 搜索仍只对当前 `messages` 过滤；`MessageList` 在搜索态会扫描它收到的 `messages`，因此大历史 `windowed` 会话只能搜最近窗口，无法覆盖早期历史。cc-gui 对标里的历史搜索应能作为明确用户意图进入更大范围，而不是让普通浏览承担全量成本。
- 本轮诊断（超越 cc-gui）：搜索是明确、高意图、高价值动作，适合承担完整历史读取成本；普通打开历史则只需要最近窗口快速可读。把 full-history 绑定到搜索输入解决两个痛点：普通浏览不卡，用户真正搜索时又不会误以为“没有结果”只是因为旧历史没加载。
- 本轮规划：本轮只实现搜索触发的 full-history intent，不做 reveal-all、不做新 UI 文案、不改后端命令、不改首屏窗口大小。优先级高：用户价值高（搜索历史正确性 + 大会话性能边界）、出现频率中（大历史搜索）、实现成本中低（store action + 页面接入 + 测试）、风险可控（主 `messages` 不全量回灌，普通 loadSession 测试继续保证不自动 full-load）。
- 本轮完成：`src/stores/useChatStore.ts` 新增 `loadActiveSessionFullHistory()`，只在显式调用时执行 `get_unified_session_messages()`；命中 `sessionHistoryCache` 时直接返回完整历史并更新 metrics，未命中时读取完整历史、分块映射、用 token 和 active session key 防止 stale 写入，缓存完整历史，但不替换当前可见 `messages`。`src/pages/ChatPage.tsx` 新增 page-local `fullHistorySearchState`；搜索 `windowed` 会话时触发 store action，完整历史返回后作为 `searchSourceMessages` 传给 `MessageList`，并用于搜索态 anchor/status 派生；搜索清空后释放 page-local full-history search state。`src/utils/chatUiBehavior.ts` 新增 `shouldRequestFullHistoryForSearch()`，集中判断搜索触发条件。测试文件 `src/stores/useChatStore.test.ts` 和 `src/utils/chatUiBehavior.test.ts` 覆盖显式 full-history 调用、缓存复用、主 `messages` 保持 windowed、触发条件去重。`.trellis/spec/frontend/state-management.md` 与 `.trellis/spec/frontend/component-guidelines.md` 已同步当前契约。
- 本轮验证：RED：`npm test -- src/stores/useChatStore.test.ts` 先失败，失败点为 `loadActiveSessionFullHistory is not a function`；`npm test -- src/utils/chatUiBehavior.test.ts` 先失败，失败点为 `shouldRequestFullHistoryForSearch is not a function`。GREEN：`npm test -- src/stores/useChatStore.test.ts` 通过（1 file / 54 tests），`npm test -- src/utils/chatUiBehavior.test.ts` 通过（1 file / 15 tests）。相邻链路验证通过：`npm test -- src/stores/useChatStore.test.ts src/utils/chatUiBehavior.test.ts src/utils/chatNavigation.test.ts src/utils/chatStatusSummary.test.ts`（4 files / 89 tests）。构建验证通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。全量前端测试通过：`npm test`（36 files / 319 tests，仅 Browserslist/caniuse-lite 过期提示）。IDE `build_project` 通过，`problems: []`；IDE build 生成的 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并删除。`git diff --check` 与 `git diff --cached --check` exit 0，仅输出既有 LF/CRLF 转换提示。
- 未验证项：未启动真实 Tauri 桌面做大历史搜索点测；未跑 Rust check/test，因为本轮没有改 Rust 后端命令或模型。实际桌面里完整历史搜索的读取耗时、IPC 体感、搜索结果跳转仍需下一轮验证。
- 下一步候选：1) 真实 Tauri 桌面大历史搜索点测：先确认普通打开仍是 `windowed`，输入早期历史关键字后触发完整读取并出现早期命中；2) 若搜索 full-load 仍卡，下一轮把 full-history search source 改成分页/索引式搜索，而不是一次性返回完整数组；3) 做 cc-gui 与本项目真实运行画面并排截图审计，覆盖任务列表、会话列表、输入区按钮、provider/model 图标对齐、右栏密度和搜索反馈；4) 给搜索加载态补更明确的 UI 反馈，例如搜索框附近的轻量“正在搜索完整历史”状态，但需要避免增加输入区噪音；5) 继续审计重复入口，尤其右侧 `StatusPanel` 与输入区状态条、任务列表与会话列表的信息层级。

## 本轮 PLAN 2026-06-19 chat full-history search feedback

- 目标：补齐上一轮显式 full-history search intent 的可见反馈。当前 `windowed` 大历史搜索会在后台读取完整历史，但消息区顶部仍只显示当前窗口的匹配数/无结果，用户会误以为“搜索没生效”或“没有更早结果”。本轮只做轻量展示层反馈，不改普通浏览 windowed 策略、不改 store 完整历史缓存、不新增后端命令。
- 功能点 1：恢复上下文与规范。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、`.trellis/spec/frontend/component-guidelines.md`、`.trellis/spec/frontend/state-management.md`、`package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断搜索边界。验证方式：读取 `ChatPage`、`MessageList`、`chatUiBehavior` 和现有搜索/i18n 入口，确认 `MessageList` 只做展示，完整历史读取仍由 `ChatPage` 和 `useChatStore.loadActiveSessionFullHistory()` 拥有。
- 功能点 3：补 RED 测试。验证方式：新增/扩展 `MessageList` SSR 测试，断言搜索状态为 `loading` 时顶部提示完整历史正在搜索，`error` 时显示错误提示和重试按钮，`complete` 时回到匹配结果文本。
- 功能点 4：实现最小展示改动。验证方式：`MessageList` 增加只读 `fullHistorySearchStatus` / `onRetryFullHistorySearch` props，`ChatPage` 从 page-local `fullHistorySearchState` 派生并传入；新增 en/zh i18n 文案；失败态重试只清理当前 page-local 状态，让现有 effect 重新走显式 full-history intent。
- 功能点 5：质量验证与记录。验证方式：运行 RED/GREEN 定向测试、相邻 Chat UI 测试、`npm run build`、`npm test`、`git diff --check`、`git diff --cached --check`；若未启动 Tauri 桌面或未真实大历史点测，明确标注。

## 迭代记录 2026-06-19 chat full-history search feedback

- 上轮实际进展：上一轮实现了显式 full-history search intent：普通历史点击保持 `windowed` 最近窗口，只有用户输入搜索时才调用 `loadActiveSessionFullHistory()` 并把完整历史作为临时搜索源；代码复查确认主 `messages` 没有被完整历史回灌。阻塞仍是未启动真实 Tauri 桌面做大历史搜索体感点测。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：`ChatPage` 已能在 `windowed` 搜索时后台拉取完整历史，但 `MessageList` 顶部仍只显示当前搜索结果数或“没有找到匹配消息”。在完整历史尚未返回时，这个提示会把“当前窗口暂无结果”误表达成最终搜索结果，容易让用户认为搜索没有覆盖更早历史。
- 本轮诊断（超越 cc-gui）：本项目为了大会话性能把普通浏览和完整历史搜索分开，这是正确的，但需要把这个异步边界告诉用户。轻量显示“正在搜索完整历史”和失败重试，可以解决两个痛点：用户知道旧历史仍在搜索，不会误判无结果；失败时仍可继续搜当前窗口并显式重试，而不是悄悄退化。
- 本轮规划：只做展示层和 page-local retry，不改 `useChatStore.loadSession()`、不改 `loadActiveSessionFullHistory()`、不改后端命令、不把重试状态放进 Zustand。优先级中高：用户价值高（补齐搜索正确性的可感知反馈）、出现频率中（大历史搜索）、成本低（展示 props + i18n + SSR 测试）、风险低（普通浏览与完整历史缓存策略不变）。
- 本轮完成：新增 `src/components/chat/MessageList.test.tsx`，覆盖 full-history 搜索 loading/error/complete 三种提示。`src/components/chat/MessageList.tsx` 增加只读 `fullHistorySearchStatus` 和 `onRetryFullHistorySearch` props；搜索条保留当前匹配数，同时在 loading 时显示 spinner + “正在搜索完整历史”，在 error 时显示警告 + 重试按钮。`src/pages/ChatPage.tsx` 从当前 `activeSessionKey`、`lastSessionLoadMetrics.status` 和 page-local `fullHistorySearchState` 派生 `fullHistorySearchStatus`；失败重试通过清空 page-local snapshot 并递增 retry counter 触发现有显式搜索 effect 重新执行。`src/locales/en.json` 与 `src/locales/zh.json` 新增三条搜索状态文案。`.trellis/spec/frontend/component-guidelines.md` 和 `.trellis/spec/frontend/state-management.md` 已同步记录该展示与状态边界。
- 本轮验证：RED：`npm test -- src/components/chat/MessageList.test.tsx` 先失败，失败点为 loading/error 文案不存在；测试夹具先补齐 `react-i18next` / `dompurify` SSR mock 后得到业务级 RED。GREEN：同命令通过（1 file / 3 tests）。相邻链路验证通过：`npm test -- src/components/chat/MessageList.test.tsx src/utils/chatUiBehavior.test.ts src/utils/chatNavigation.test.ts src/stores/useChatStore.test.ts`（4 files / 85 tests）。构建验证通过：`npm run build`（`tsc && vite build`）。全量前端测试通过：`npm test`（37 files / 322 tests）。IDE `build_project` 通过，`problems: []`；IDE 生成的 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并删除。`git diff --check` 与 `git diff --cached --check` exit 0，仅输出既有 LF/CRLF 转换提示。新增测试文件 `src/components/chat/MessageList.test.tsx` 已 `git add`。
- 未验证项：本轮未启动真实 Tauri 桌面，也未用真实大历史会话点测搜索加载体感、早期命中跳转或失败重试；未跑 Rust check/test，因为本轮没有改 Rust 后端命令或模型。
- 下一步候选：1) 启动真实 Tauri 桌面做大历史搜索点测：普通打开保持 `windowed`，输入早期关键字时显示完整历史搜索状态，完整历史返回后早期命中可见；2) 若完整历史搜索仍造成明显卡顿，下一轮改为后端分页/索引式搜索，而不是一次性返回完整数组；3) 做 cc-gui 与本项目并排截图审计，继续检查任务列表、会话列表、输入区按钮、provider/model 图标对齐、右栏密度和搜索反馈；4) 继续审计重复入口，尤其右侧 `StatusPanel`、输入区状态条、搜索条和会话侧栏之间的信息层级。

## 本轮 PLAN 2026-06-19 chat search status scope and spacing

- 目标：继续排查用户反馈的“大历史加载/搜索仍卡”和“个别消息挤成一坨”。本轮只做前端小范围优化：完整历史搜索后，状态摘要只从命中附近的有限上下文构建，避免搜索后再同步扫描整份历史；assistant 内容块改用稳定的块间距类，让 text / thinking / tool 行在紧凑模式下仍可扫描。
- 功能点 1：恢复上下文与规范。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、frontend component/state/quality/type 规范和 shared guides；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：查看 `MessageList` / `MessageItem` / `ContentBlockRenderer` / `ChatPage` / `useChatStore.loadSession()` / 后端 `load_*_message_window`，区分前端渲染窗口、搜索完整历史、状态摘要和后端文件扫描各自成本。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/utils/chatNavigation.test.ts`，断言搜索状态摘要只保留命中消息后的有限上下文，避免纳入未命中的远端历史；扩展 `src/components/chat/ContentBlockRenderer.test.tsx`，断言 compact 渲染暴露稳定 spacing 类。
- 功能点 4：实现最小行为改动。验证方式：在 `chatNavigation` 增加搜索状态摘要上下文 helper，`ChatPage` 搜索态使用该 helper 作为 `buildChatStatusSummary()` 输入；`ContentBlockRenderer` 根节点改为稳定类名并在 `App.css` 定义 compact/default gap；微调 assistant 行间距，不改变工具块组件 API。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 和 `.trellis/spec/frontend/state-management.md`，记录完整历史搜索不能把整份历史再次交给状态摘要同步扫描，compact transcript 需要稳定块间距。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 Chat 导航/状态/渲染测试、`npm run build`、`npm test`、`git diff --check` / `git diff --cached --check`；若未启动真实 Tauri 桌面或未跑 Rust，明确标注。

## 迭代记录 2026-06-19 chat search status scope and spacing

- 上轮实际进展：上一轮记录声称已实现 full-history 搜索反馈；代码复查确认 `ChatPage` 只在 `windowed` 会话搜索时触发 `loadActiveSessionFullHistory()`，完整历史作为 page-local `searchSourceMessages` 传给 `MessageList`，没有回灌主 store `messages`。阻塞仍是未启动真实 Tauri 桌面做大历史点测。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：普通浏览已经 windowed，但完整历史搜索返回后，搜索态 `statusSummary` 仍可能把 `searchSourceMessages` 整份完整历史交给 `buildChatStatusSummary()`，相当于用户搜索后又做一次全历史状态摘要扫描；同时 assistant 历史消息里 `ContentBlockRenderer`、`MessageItem` 和 `.assistant-message-content` 的紧凑间距叠加过小，连续 text / thinking / tool 内容容易读成一坨。cc-gui 的基础体验是长历史可快速定位且工具/思考/正文层次清晰，本项目这两点仍有可感知差距。
- 本轮诊断（超越 cc-gui）：状态摘要不需要在搜索后覆盖未命中的远端历史，只需要命中附近足够上下文来恢复工具状态；这样可以保留“搜索完整历史”的能力，同时避免把搜索成本再放大一轮。紧凑模式也不应等于压缩到不可扫描，稳定块间距能解决用户在长历史里快速分辨正文、思考块和工具块的痛点。
- 本轮规划：只改搜索态状态摘要输入和 assistant 内容块间距，不改后端历史读取、不改 `loadSession()` / `loadActiveSessionFullHistory()`、不新增依赖、不重构工具块 API。优先级高：用户价值高（直接对应“大历史卡顿”和“消息挤成一坨”）、出现频率高（长历史搜索与 assistant 多块输出）、实现成本低（一个纯 helper + 样式契约 + 两个回归测试）、风险低（普通浏览路径和完整历史搜索源保持原行为）。
- 本轮完成：`src/utils/chatNavigation.ts` 新增 `getSearchStatusContextMessages()`，默认最多 80 条、每个命中向后取 16 条上下文，用于搜索态状态摘要限界；`src/pages/ChatPage.tsx` 在搜索态把 `statusMessages` 改为 `getSearchStatusContextMessages(searchSourceMessages, filteredMessages)`，普通浏览仍按可见窗口起点切片。`src/components/chat/ContentBlockRenderer.tsx` 将根节点从 Tailwind `space-y-*` 改为稳定契约类 `chat-content-blocks chat-content-blocks-compact/default`；`src/App.css` 定义 compact/default gap；`src/components/chat/MessageItem.tsx` 移除 assistant content 内层 `space-y-1.5` 并把 assistant 行 padding 调整为 `py-3`；`src/styles/toolBlocks.css` 将 `.assistant-message-content > :not(:first-child)` 间距从 4px 调到 8px。`src/utils/chatNavigation.test.ts` 和 `src/components/chat/ContentBlockRenderer.test.tsx` 分别补充搜索上下文限界与稳定 spacing 类回归。`.trellis/spec/frontend/component-guidelines.md` 与 `.trellis/spec/frontend/state-management.md` 已同步搜索摘要限界和 compact transcript 间距契约。
- 本轮验证：RED：`npm test -- src/components/chat/ContentBlockRenderer.test.tsx` 先失败，失败点为 `chat-content-blocks` 类不存在；`npm test -- src/utils/chatNavigation.test.ts` 先失败，失败点为 `getSearchStatusContextMessages is not a function`。GREEN：两条定向测试通过（ContentBlockRenderer：1 file / 11 tests；chatNavigation：1 file / 14 tests）。相邻链路验证通过：`npm test -- src/components/chat/ContentBlockRenderer.test.tsx src/utils/chatNavigation.test.ts src/utils/chatStatusSummary.test.ts src/utils/chatUiBehavior.test.ts src/stores/useChatStore.test.ts`（5 files / 101 tests）。构建验证通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。全量前端测试通过：`npm test`（37 files / 324 tests，仅 Browserslist/caniuse-lite 过期提示）。IDE `build_project` 通过，`problems: []`；IDE 生成的 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并删除。`git diff --check` 与 `git diff --cached --check` exit 0，仅输出既有 LF/CRLF 转换提示。
- 未验证项：本轮未启动真实 Tauri 桌面，也未用真实大历史会话点测搜索后卡顿、消息间距视觉效果、早期命中跳转或输入响应。未跑 `cargo check --manifest-path src-tauri/Cargo.toml` / Rust 测试，因为本轮没有改 Rust 后端命令、模型或 session provider。
- 下一步候选：1) 启动真实 Tauri 桌面做大历史点测：普通打开确认 `windowed` 最近窗口即就绪，搜索早期关键字后确认完整历史搜索状态、命中显示和状态摘要不卡顿；2) 若仍卡，优先优化后端 `load_claude_message_window()` / `load_codex_message_window()` 的从头读全文件策略，考虑 tail reader 或分页/索引式搜索，而不是一次性 parse/IPC 完整 JSONL；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点覆盖任务列表、会话列表、输入区布局、provider/model/Claude 图标对齐、右栏密度和搜索反馈；4) 继续审计重复入口，尤其右侧 `StatusPanel`、输入区状态条、搜索条和会话侧栏之间的信息层级，避免 tasks/subagents/edits/MCP 在宽屏重复出现。

## 本轮 PLAN 2026-06-19 chat model brand icon alignment

- 目标：回应用户点名的 “Claude 图标不对 / 模型图标不对齐”。本轮只处理 composer provider/model selector 的品牌图标与固定图标盒，不改模型列表来源、不改远程刷新、不改 provider 状态。
- 功能点 1：恢复上下文与规范。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端 component/state/quality 规范和 shared code-reuse guide；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：对标诊断。验证方式：CodeGraph 读取本项目 `ButtonArea` / `ModelIcon` / `SelectorDropdown` / tests；读取 cc-gui `modelIconMapping.ts` 和 `ChatInputBox` toolbar 相关代码，确认 cc-gui 以 vendor 图标为模型图标，本项目模型图标仍用 Gem/Sparkles/Feather/Terminal 等能力隐喻图标。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/composer/ButtonArea.test.tsx`，断言 Claude 模型 selector 使用 Claude LobeHub glyph，Codex/OpenAI 模型 selector 使用统一 Codex/OpenAI glyph，不出现旧 Lucide terminal/sparkles 类；若需要，新增/扩展 `ModelIcon` 纯渲染测试。
- 功能点 4：实现最小展示改动。验证方式：把 `ButtonArea` 内已有 provider SVG 提取到 `ModelIcon.tsx` 共享，`ProviderBrandIcon` 与 `ModelIcon` 共用同一套品牌 glyph；保留现有 `data-chat-model-icon` 语义 kind，新增 glyph data attribute 用于稳定测试；保持 `selector-dropdown-icon-box` 固定尺寸契约。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 provider/model selector 应共用品牌 glyph，不能用装饰性能力图标代替 Claude/OpenAI vendor 图标。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 composer 测试、相邻 selector/composer 测试、全量 `npm test`、`npm run build`、`git diff --check` / `git diff --cached --check`；真实 Tauri 桌面视觉点测若未执行需明确标注。

## 迭代记录 2026-06-19 chat model brand icon alignment

- 上轮实际进展：上一轮已完成搜索态状态摘要限界和 assistant 内容块间距优化；代码复查确认 `src/utils/chatNavigation.ts` 已用 `getSearchStatusContextMessages()` 限制搜索态状态摘要输入，`ContentBlockRenderer` / `MessageItem` / `toolBlocks.css` 已有稳定块间距改动。已知阻塞仍是真实 Tauri 桌面大历史体感和视觉点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui `webview/src/utils/modelIconMapping.ts` 以 vendor/model 映射识别 Claude/Anthropic、OpenAI/GPT/Codex、Qwen、DeepSeek 等图标；本项目 `src/components/chat/composer/ModelIcon.tsx` 仍把 Claude Sonnet 渲染为 `Sparkles`、Opus 渲染为 `Gem`、Codex 渲染为 `Terminal`。这解释了用户反馈的 “Claude 图标不对” 和模型 selector 图标不齐：同一 provider 在 provider selector 与 model selector 使用了两套视觉语言。
- 本轮诊断（超越 cc-gui）：provider selector 和 model selector 都在输入区常驻，用户需要一眼确认当前执行引擎，而不是解读能力隐喻。统一品牌 glyph 后，Claude/Anthropic 模型始终显示 Claude LobeHub glyph，Codex/OpenAI/GPT 模型始终显示 Codex/OpenAI glyph；`data-chat-model-icon` 仍保留 `claude-sonnet` / `codex-codex` 等模型族语义，便于后续做更细模型状态或测试，不牺牲可扩展性。
- 本轮规划：只改 composer 的品牌图标呈现和稳定测试，不改模型列表来源、不改远程刷新、不改 provider 状态、不新增依赖。优先级高：用户价值高（直接修正用户点名问题并提升输入区识别效率）、出现频率高（composer 常驻）、实现成本低（共享 SVG + 测试）、风险低（纯展示层，selector 数据流不变）。
- 本轮完成：`src/components/chat/composer/ButtonArea.test.tsx` 扩展回归，断言 Claude 模型按钮包含 `data-chat-model-icon="claude-sonnet"`、`data-chat-model-icon-glyph="claude-lobehub"` 和 Claude LobeHub path，且不再包含 `lucide-sparkles` / `lucide-gem`；新增 Codex 回归，断言 Codex 模型按钮包含 `data-chat-model-icon="codex-codex"`、`data-chat-model-icon-glyph="codex-openai"`，且不再包含 `lucide-terminal`。`src/components/chat/composer/ModelIcon.tsx` 把模型图标从 Lucide 能力隐喻改为品牌 glyph 渲染，导出 `ProviderBrandIcon` 供 provider selector 与 model selector 复用，保留模型族 kind 并新增 glyph data attribute。`src/components/chat/composer/ButtonArea.tsx` 删除本地重复的 provider SVG / `ProviderBrandIcon` 实现，统一从 `ModelIcon.tsx` 引入共享实现。`.trellis/spec/frontend/component-guidelines.md` 已同步 provider/model selector 共用品牌 glyph 的契约。
- 本轮验证：RED：`npm test -- src/components/chat/composer/ButtonArea.test.tsx` 在实现前失败，失败点为 Claude 仍是 `lucide-sparkles`、Codex 仍是 `lucide-terminal`，缺少对应 `data-chat-model-icon-glyph`。GREEN：同命令通过（1 file / 8 tests）。相邻 composer 验证通过：`npm test -- src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/SelectorDropdown.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx`（3 files / 11 tests）。全量前端测试通过：`npm test`（37 files / 325 tests，仅 Browserslist/caniuse-lite 过期提示）。构建验证通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。Rust 快速编译通过：`cargo check --manifest-path src-tauri/Cargo.toml`。IDE `build_project` 通过，`problems: []`；IDE 生成的 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并删除。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。
- 未验证项：未启动真实 Tauri 桌面做 provider/model selector 视觉点测；未用真实窗口截图确认 Claude/Codex 图标在不同缩放、主题和窄屏换行下的最终像素对齐。未跑 Rust 全量测试，因为本轮没有改 Rust 后端命令或 session provider。
- 下一步候选：1) 启动真实 Tauri 桌面做输入区视觉点测：覆盖 Claude/Codex provider 切换、动态模型列表、自定义模型、深浅色主题和窄屏换行，确认品牌图标居中且不会压缩 label；2) 继续做 cc-gui 与本项目并排截图审计，优先看任务列表、会话列表、输入区布局、右栏密度和重复入口；3) 处理用户提到的任务列表体验：梳理任务/子代理/编辑状态在右侧 `StatusPanel`、输入区 status strip 和消息流工具块之间的职责边界，减少重复显示；4) 如果大历史仍卡，进入后端 tail reader / 分页搜索方向，避免前端继续围绕完整数组做补丁式优化。

## 本轮 PLAN 2026-06-19 chat status panel recent task list

- 目标：回应用户提到的“任务列表”体验问题。桌面 `xl` 下输入区 tasks/subagents/edits/MCP quick tabs 已作为小屏 fallback 隐藏，右侧 `StatusPanel` 是主诊断区；但当前活动卡只显示 active tool 和 pending/error 计数，缺少最近任务清单。本轮在右侧活动卡内增加最近非 agent 工具任务列表，让桌面用户能直接扫描最近任务而不是只看到数量。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis 当前任务制品、前端 component/state/quality 规范和 shared code-reuse guide；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `StatusPanel.tsx`、`ChatInputStatusTabs.tsx`、`chatStatusSummary.ts` 和相关测试，确认输入区桌面 fallback 已存在，右栏当前活动缺少 recent task list。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，构造 `toolTimeline` 中多个非 agent 工具和一个 agent，断言右栏渲染 `status-activity-task-list`，显示最近非 agent 工具，排除 agent 工具，并展示 hidden count；旧实现应因缺少任务列表 class 失败。
- 功能点 4：实现最小展示改动。验证方式：`StatusPanel` 从 `statusSummary.toolTimeline` 派生最近非 agent 工具列表，复用现有 chip/state 样式渲染 compact rows；不改 `buildChatStatusSummary()`、不改 `ChatInputStatusTabs`、不改 Zustand、不中断现有 active tool 展示。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录桌面右侧 `StatusPanel` 的 current activity 应展示最近任务清单，输入区 tasks tab 只是小屏 fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `StatusPanel` 测试、相邻 status/composer 测试、全量 `npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`；真实 Tauri 桌面点测若未执行需明确标注。

## 迭代记录 2026-06-19 chat status panel recent task list

- 上轮实际进展：上一轮完成 provider/model selector 品牌图标对齐；代码复查确认 `src/components/chat/composer/ModelIcon.tsx` 已导出共享 `ProviderBrandIcon`，`ModelIcon` 使用 Claude LobeHub 与 Codex/OpenAI glyph，并在 `ButtonArea` 中复用。已知阻塞仍是真实 Tauri 桌面 provider/model 视觉点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：本项目已经把输入区 tasks/subagents/edits/MCP quick tabs 在桌面 `xl` 下收口为小屏 fallback；右侧 `StatusPanel` 是桌面主诊断区。但 `StatusPanel` 的 `currentActivity` 卡片只展示 active tool 或 pending/error 计数，没有列出最近任务历史。结果是桌面用户能看到“有几个工具”，却无法直接扫描最近执行了哪些任务；这比 cc-gui 集中展示任务/状态详情的路径少了一层可读性。
- 本轮诊断（超越 cc-gui）：任务列表不应重新挤回输入区。更合适的层级是：右侧 `StatusPanel` 在桌面展示 compact recent task list，输入区只在小屏提供 quick fallback。这样解决用户“任务列表”和“页面元素功能重复”两个痛点：桌面不重复入口，同时任务清单可扫；小屏仍保留近输入区的状态入口。
- 本轮规划：只改 `StatusPanel` 当前活动卡的展示层和测试，不改 `buildChatStatusSummary()`、不改 `ChatInputStatusTabs`、不改 Zustand、不中断 active tool 主展示。优先级中高：用户价值中高（任务列表直接可扫）、出现频率高（右侧状态面板常驻）、实现成本低（单组件 + SSR 测试）、风险低（只消费已有 `statusSummary.toolTimeline`）。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 新增回归 `renders a recent desktop task list in the current activity card without mixing in subagents`，构造多个非 agent 工具和一个 agent，断言右栏渲染 `status-activity-task-list` / `status-activity-task-row`，显示最近任务，排除 agent，隐藏最早任务并展示 hidden count。`src/components/chat/StatusPanel.tsx` 新增 `MAX_ACTIVITY_TASKS`、`activityTasks` / `recentActivityTasks` 派生、`renderActivityTaskRow()` 和 compact task list；active tool 仍作为主行展示，recent list 排除当前 active tool 和 agent tools，超过 5 条时复用已有 `inputStatusMoreTools` 文案显示更早任务数量。`.trellis/spec/frontend/component-guidelines.md` 已同步：桌面 `currentActivity` 是主任务列表 surface，输入区 tasks tab 是小屏 fallback。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为缺少 `status-activity-task-list`。GREEN：同命令通过（1 file / 21 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/SelectorDropdown.test.tsx`（5 files / 44 tests）。全量前端测试通过：`npm test`（37 files / 326 tests，仅 Browserslist/caniuse-lite 过期提示）。构建验证通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。Rust 快速编译通过：`cargo check --manifest-path src-tauri/Cargo.toml`。IDE `build_project` 通过，`problems: []`；IDE 生成的 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并删除。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。
- 未验证项：未启动真实 Tauri 桌面做右侧任务列表视觉点测；未与 cc-gui 做并排截图确认任务列表密度、滚动高度和右栏信息层级；未跑 Rust 全量测试，因为本轮没有改 Rust 后端命令或 session provider。
- 下一步候选：1) 真实 Tauri 桌面截图/点测：宽屏确认右栏 current activity 显示最近任务列表、输入区 tasks tab 隐藏，小屏确认输入区 fallback 仍可展开；2) 继续审计右栏任务列表与消息流工具块的边界：右栏只做最近任务扫描和跳转入口，消息流保留完整工具详情；3) 做 cc-gui 与本项目并排截图审计，覆盖任务列表、会话列表、输入区布局、provider/model 图标对齐和右栏密度；4) 如果大历史仍卡，进入后端 tail reader / 分页搜索方向，避免前端继续围绕完整数组做补丁式优化；5) 继续优化会话列表密度和空状态，尤其大项目多会话时的分组、搜索和当前会话定位。

## 本轮 PLAN 2026-06-19 chat status panel task jump anchors

- 目标：把上一轮右侧 `StatusPanel` 的最近任务清单从“只能扫描”补齐为“可定位”的桌面任务入口，点击任务行后滚动到消息流中的对应工具块。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `StatusPanel.tsx`、`ContentBlockRenderer.tsx`、`ChatPage.tsx` 与 `chatStatusSummary.ts`，确认 `toolTimeline` 有稳定 `toolId`，但消息流工具块缺少 DOM anchor，右侧任务行也没有 click/keyboard 定位入口。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx`，断言任务行在提供回调时渲染为可操作按钮并携带 `data-target-tool-id`；扩展 `ContentBlockRenderer.test.tsx`，断言单工具和分组工具都暴露稳定 `data-chat-tool-id` / `data-chat-tool-ids` 锚点。
- 功能点 4：实现最小交互改动。验证方式：`ContentBlockRenderer` 给工具块包装层加可聚焦锚点；`StatusPanel` 任务行调用 `onSelectTool`；`ChatPage` 在滚动容器内按 toolId 找锚点并 `scrollIntoView`。不改工具块数据结构、不改后端、不改状态摘要生成。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录桌面右侧任务清单应作为定位入口，消息流工具块必须暴露稳定 tool anchor。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 status/message 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`；真实 Tauri 桌面点测若未执行需明确标注。

## 迭代记录 2026-06-19 chat status panel task jump anchors

- 上轮实际进展：上一轮已把桌面右侧 `StatusPanel` 的 `currentActivity` 补为最近非 agent 工具任务清单；代码复查确认 `StatusPanel.tsx` 已有 `status-activity-task-list`、最近 5 条裁剪和 hidden count，且输入区 tasks/subagents/edits/MCP tabs 在桌面作为小屏 fallback。已知阻塞仍是真实 Tauri 桌面视觉点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：右侧任务列表已经可扫描，但仍只是静态列表；用户看到最近执行了哪些工具后，不能一键回到消息流中的工具块上下文。cc-gui 的状态/任务区域核心价值不是只展示计数，而是帮助用户定位过程细节。本项目 `toolTimeline` 已有稳定 `toolId`，但 `ContentBlockRenderer` 输出的单工具和分组工具缺少 DOM anchor。
- 本轮诊断（超越 cc-gui）：右栏不应该复制完整工具详情，否则会和消息流工具块重复；更好的交互是右栏作为任务索引，点击后滚动到消息流中已有的工具块。这样解决“任务列表可扫但不可用”的痛点，同时保留消息流作为唯一完整详情来源。
- 本轮规划：只加 DOM anchor、右栏任务行按钮和 `ChatPage` 局部滚动 handler；不改 `ChatStatusToolSummary` 数据结构、不改 `buildChatStatusSummary()`、不改工具块详情、不改后端。优先级中高：用户价值中高（任务行可直接定位）、出现频率高（桌面右栏常驻）、成本低（组件边界内小改动 + SSR 回归）、风险低（不影响数据流）。
- 本轮完成：`src/components/chat/ContentBlockRenderer.tsx` 新增 `getToolAnchorProps()`，单工具包装层暴露 `data-chat-tool-id`，分组工具包装层暴露 `data-chat-tool-ids`，并设置可聚焦 `chat-tool-anchor`；`src/components/chat/StatusPanel.tsx` 为最近任务行新增按钮语义、`data-target-tool-id`、aria-label 和 `onSelectTool` 回调；`src/pages/ChatPage.tsx` 新增 `findToolAnchorElement()` / `handleSelectStatusTool()`，在当前对话滚动容器内按 toolId 定位并 `scrollIntoView`；`src/locales/en.json` / `src/locales/zh.json` 新增任务定位文案；`src/components/chat/ContentBlockRenderer.test.tsx` 与 `src/components/chat/StatusPanel.test.tsx` 增加锚点和任务跳转回归；`.trellis/spec/frontend/component-guidelines.md` 已同步桌面任务行定位与工具 anchor 契约。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ContentBlockRenderer.test.tsx` 先失败 3 项，失败点分别为缺少 `<button>` / `data-target-tool-id`、缺少 `chat-tool-anchor` / `data-chat-tool-id`、缺少 `data-chat-tool-ids`。GREEN：同命令通过（2 files / 35 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ContentBlockRenderer.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx`（4 files / 50 tests）。全量前端验证通过：`npm test`（37 files / 329 tests，仅 Browserslist/caniuse-lite 过期提示）。构建验证通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。Rust 快速编译通过：`cargo check --manifest-path src-tauri/Cargo.toml`。IDE `build_project` 通过，`problems: []`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。IDE 生成的 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并删除。
- 未验证项：未启动真实 Tauri 桌面做右侧任务行点击后的视觉滚动点测；未与 cc-gui 做并排截图确认任务列表密度、滚动位置和焦点环表现；未跑 `cargo test --manifest-path src-tauri/Cargo.toml`，因为本轮没有改 Rust 后端命令或 session provider。
- 下一步候选：1) 启动真实 Tauri 桌面做宽屏/窄屏点测：宽屏点击右栏最近任务行应跳到对应工具块，小屏输入区 fallback 仍可展开；2) 如果定位体验不够明显，给刚跳转到的 `chat-tool-anchor` 增加短暂高亮，但要先确认不会造成视觉噪音；3) 做 cc-gui 与本项目并排截图审计，覆盖任务列表、会话列表、输入区布局、provider/model 图标和右栏密度；4) 继续检查任务列表与消息流工具块边界，右栏只做索引和诊断，完整参数/结果继续留在消息流；5) 若用户继续反馈大历史卡顿，转向后端 tail reader / 分页搜索，而不是继续在前端完整数组上补丁。

## 本轮 PLAN 2026-06-19 chat status panel jump highlight

- 目标：增强上一轮右侧任务行跳转后的视觉确认。当前点击 `StatusPanel` 最近任务行会滚动并聚焦到 transcript 工具锚点，但用户在长历史里可能仍不确定目标是哪一个；本轮给被跳转工具块增加短暂、低噪音的高亮反馈。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `ChatPage.findToolAnchorElement()`、`handleSelectStatusTool()`、`ContentBlockRenderer` 锚点和 `StatusPanel` 任务行，确认现有实现只有 `scrollIntoView + focus`，没有可见确认态。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/utils/chatUiBehavior.test.ts`，断言 highlight helper 会先移除旧计时器、添加 highlight class，并在 timeout 后清理 class 与 data marker；旧实现应因 helper 不存在失败。
- 功能点 4：实现最小交互改动。验证方式：在 `src/utils/chatUiBehavior.ts` 增加 `highlightTranscriptToolAnchor()`，`ChatPage` 持有清理 ref 并在任务跳转后调用；`src/App.css` 增加 `chat-tool-anchor-jump-highlight` 的短暂 ring/背景动画。不要改变工具块内部组件、状态摘要或后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录从右侧任务列表跳转后应有短暂目标高亮，但不能持久占用视觉状态。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `chatUiBehavior` 测试、相邻 status/message 测试、全量 `npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、IDE build 和 diff 空白检查；真实 Tauri 桌面点测若未执行需明确标注。

## 迭代记录 2026-06-19 chat status panel jump highlight

- 上轮实际进展：上一轮已把右侧 `StatusPanel` 最近任务行接成 transcript 跳转入口；代码复查确认 `src/components/chat/StatusPanel.tsx` 的任务行有 `onSelectTool` / `data-target-tool-id`，`src/components/chat/ContentBlockRenderer.tsx` 的单工具和分组工具已暴露 `data-chat-tool-id` / `data-chat-tool-ids`，`src/pages/ChatPage.tsx` 已在当前滚动容器内查找 tool anchor 并 `scrollIntoView`。已知阻塞仍是真实 Tauri 桌面点击点测未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：右栏任务行虽然能滚动到工具块，但长历史里滚动完成后缺少目标确认态；用户可能看到页面位置变化，却不知道具体落在哪个工具块。cc-gui 的任务/状态区域强调过程定位，本项目需要补上“跳转目标可感知”的细节。
- 本轮诊断（超越 cc-gui）：右栏仍不复制完整工具详情，只作为任务索引；短暂高亮把注意力引回消息流中的唯一详情来源。这样解决“能跳但不确定跳到哪里”的痛点，同时避免持久选中态污染 transcript 阅读。
- 本轮规划：只加 `chatUiBehavior` helper、`ChatPage` 调用和 CSS 动画，不改工具块内部组件、不改状态摘要、不改后端、不新增依赖。优先级中等偏高：用户价值中高（跳转反馈明确）、出现频率中高（每次从右栏任务列表定位）、成本低（helper + CSS + 测试）、风险低（短时 class，自动清理）。
- 本轮完成：`src/utils/chatUiBehavior.test.ts` 新增回归 `marks a transcript tool anchor briefly after jumping to it`，覆盖旧高亮清理、添加 class/data marker、cleanup 清理 class 和 timeout。`src/utils/chatUiBehavior.ts` 新增 `TOOL_ANCHOR_JUMP_HIGHLIGHT_CLASS`、`TOOL_ANCHOR_JUMP_HIGHLIGHT_DURATION_MS` 和 `highlightTranscriptToolAnchor()`；`src/pages/ChatPage.tsx` 新增 `toolAnchorHighlightCleanupRef`，跳转后调用 helper，并在页面卸载时清理。`src/App.css` 新增 `chatToolAnchorJumpHighlight` 动画和 reduced-motion fallback。`.trellis/spec/frontend/component-guidelines.md` 已同步右栏跳转后短暂目标高亮、不得变成持久选中态或重复详情的契约。
- 本轮验证：RED：`npm test -- src/utils/chatUiBehavior.test.ts` 先失败 1 项，失败点为 `highlightTranscriptToolAnchor is not a function`。GREEN：同命令通过（1 file / 16 tests）。相邻验证通过：`npm test -- src/utils/chatUiBehavior.test.ts src/components/chat/StatusPanel.test.tsx src/components/chat/ContentBlockRenderer.test.tsx src/components/chat/MessageList.test.tsx`（4 files / 54 tests）。全量前端验证通过：`npm test`（37 files / 330 tests，仅 Browserslist/caniuse-lite 过期提示）。`npm run build` 初次失败于测试文件 TypeScript 窄化：`timeoutHandler` 在 `tsc` 下被视为 `null/never`；按 `superpowers:systematic-debugging` 读取错误、确认根因为回调内赋值不参与控制流窄化后，仅调整测试断言为显式 `unknown` 函数断言。修复后 `npm test -- src/utils/chatUiBehavior.test.ts` 通过，`npm run build` 通过（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。Rust 快速编译通过：`cargo check --manifest-path src-tauri/Cargo.toml`。IDE `build_project` 通过，`problems: []`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。IDE 生成的 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并删除。
- 未验证项：未启动真实 Tauri 桌面做右侧任务行点击后的高亮视觉点测；未与 cc-gui 做并排截图确认高亮强度、滚动位置和焦点环表现；未跑 `cargo test --manifest-path src-tauri/Cargo.toml`，因为本轮没有改 Rust 后端命令或 session provider。
- 下一步候选：1) 启动真实 Tauri 桌面做右栏任务跳转点测：确认滚动、焦点、高亮动画在深浅色主题和长历史中都清晰但不过度；2) 若高亮视觉过强或过弱，微调 `chatToolAnchorJumpHighlight` 的 opacity/ring，而不是增加持久选中状态；3) 做 cc-gui 与本项目并排截图审计，覆盖任务列表、会话列表、输入区布局、provider/model 图标和右栏密度；4) 继续检查任务列表与消息流工具块边界，右栏只做索引和诊断，完整参数/结果继续留在消息流；5) 若用户继续反馈大历史卡顿，转向后端 tail reader / 分页搜索，而不是继续在前端完整数组上补丁。

## 本轮 PLAN 2026-06-19 chat status panel subagent activity

- 目标：补齐桌面右侧 `StatusPanel` 对子代理 / Task 活动的可扫可定位展示。上一轮 recent task list 为避免混杂过滤了 `agent` 工具，但 cc-gui 的状态面板有独立 subagent 入口；本轮在不复制完整子代理详情的前提下，把 `agentTools` 渲染成紧凑活动列表，并复用现有 transcript jump/highlight 机制。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `StatusPanel.tsx`、`ChatInputStatusTabs.tsx`、`chatStatusSummary.ts` 与 cc-gui `StatusPanel`，确认本项目已有 `agentTools` 数据，输入区小屏 fallback 已展示 subagents，但桌面右栏未消费该列表。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，构造多个 `agentTools`，断言右栏渲染 `status-activity-subagent-list`、最近子代理行、hidden count，并且提供 `onSelectTool` 时每行携带 `data-target-tool-id`；旧实现应因缺少子代理列表失败。
- 功能点 4：实现最小展示/交互改动。验证方式：`StatusPanel` 从 `statusSummary.agentTools` 派生最近子代理列表，复用 `renderActivityTaskRow()` 输出可跳转行；普通工具列表仍排除 agent，active tool 主行不重复出现在子列表；不改 `buildChatStatusSummary()`、不改 `ChatInputStatusTabs`、不改后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录桌面 `StatusPanel` 需要把 subagent/Task 作为独立活动索引展示，输入区 subagents tab 仍是小屏 fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `StatusPanel` 测试、相邻 status/message 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`；真实 Tauri 桌面点测若未执行需明确标注。

## 迭代记录 2026-06-19 chat status panel subagent activity

- 上轮实际进展：上一轮已给右侧 `StatusPanel` 最近任务跳转补上短暂 transcript 高亮；代码复查确认 `src/utils/chatUiBehavior.ts` 有 `highlightTranscriptToolAnchor()`，`src/pages/ChatPage.tsx` 在 `handleSelectStatusTool()` 中调用该 helper，`src/App.css` 有 `chat-tool-anchor-jump-highlight` 动画。已知阻塞仍是真实 Tauri 桌面点击点测和 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 `webview/src/components/StatusPanel/StatusPanel.tsx` 有独立 subagent tab；本项目 `src/utils/chatStatusSummary.ts` 已经产出 `agentTools`，`src/components/chat/ChatInputStatusTabs.tsx` 也在小屏 fallback 中展示 subagents，但桌面右侧 `StatusPanel` 只展示非 agent recent task list，导致子代理活动在宽屏主诊断区不可扫。
- 本轮诊断（超越 cc-gui）：子代理不应混入普通工具任务，也不应把完整子代理历史复制到右栏。更合适的层级是：右栏 `currentActivity` 内增加独立 compact subagent list，行本身仍是 transcript jump target；消息流保留完整 Agent/Task 块和后续历史承载。这样解决“桌面看不到子代理进展”和“右栏信息重复堆叠”两个痛点。
- 本轮规划：只改 `StatusPanel` 展示层与 SSR 测试，不改 `buildChatStatusSummary()`、不改 `ChatInputStatusTabs`、不改后端、不新增 i18n key，复用已有 `inputStatusSubagents` / `inputStatusMoreSubagents` / `scrollToToolTask` 文案。优先级中高：用户价值中高（子代理活动常是长任务关键线索）、出现频率中（启用 subagent/Task 时必看）、实现成本低（已有数据 + 既有跳转行）、风险低（单组件内分层）。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `MAX_ACTIVITY_SUBAGENTS`、从 `statusSummary.agentTools` 派生 `activitySubagents` / `recentActivitySubagents`，并在 `currentActivity` 卡片中渲染 `status-activity-subagent-list`；子代理行复用 `renderActivityTaskRow()`，因此保留 `data-target-tool-id`、aria-label、disabled/onSelectTool 行为和 transcript jump/highlight 机制；active tool 会从子代理列表中排除，避免当前运行子代理在主行和列表中重复；仅有子代理历史时不再显示 idle 文案。`src/components/chat/StatusPanel.test.tsx` 新增子代理列表与子代理-only idle 边界回归，并同步旧任务列表测试为“任务列表与子代理列表分离”。`.trellis/spec/frontend/component-guidelines.md` 已同步桌面右栏 subagent 独立索引契约。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为缺少 `status-activity-subagent-list`。GREEN：同命令通过（1 file / 23 tests），随后补充子代理-only idle 边界后相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/ContentBlockRenderer.test.tsx src/components/chat/MessageList.test.tsx src/utils/chatStatusSummary.test.ts`（5 files / 59 tests）。全量验证通过：`npm test`（37 files / 332 tests，仅 Browserslist/caniuse-lite 过期提示）、`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests + 2 doctests ignored）、IDE `build_project`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。IDE 生成的 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并删除。
- 未验证项：未启动真实 Tauri 桌面做右栏 subagent 行点击后的滚动/高亮视觉点测；未与 cc-gui 做并排截图确认右栏 subagent 密度、标题层级和长文本截断效果。
- 下一步候选：1) 真实 Tauri 桌面点测：宽屏确认普通 tasks 与 subagents 分区、点击子代理行跳到消息流 Agent/Task 块并短暂高亮；窄屏确认输入区 subagents fallback 仍正常；2) 做 cc-gui 与本项目并排截图审计，覆盖右栏 tabs/list 密度、会话列表、输入区布局、provider/model 图标和长历史消息排版；3) 如果右栏 currentActivity 因 tasks + subagents 同时很多而过高，下一轮考虑给 currentActivity 内部列表加局部高度上限，而不是隐藏子代理；4) 若用户继续反馈大历史卡顿，进入后端 tail reader / 分页搜索方向，避免继续在前端完整数组上补丁。

## 本轮 PLAN 2026-06-19 chat status panel subagent jump label

- 目标：修正桌面右侧 `StatusPanel` 子代理活动行的跳转可访问语义。上一轮子代理列表复用了普通任务行渲染，功能可跳转，但 aria-label 仍是“Jump to tool task / 定位工具任务”；本轮改为子代理专用“Jump to subagent activity / 定位子代理活动”。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `StatusPanel.tsx`、`StatusPanel.test.tsx` 和 i18n 资源，确认 `renderActivityTaskRow()` 对 task/subagent 共用 `scrollToToolTask`。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，断言子代理行 aria-label 使用专用子代理跳转文案，普通 task 行仍使用原工具任务文案。
- 功能点 4：实现最小可访问性改动。验证方式：新增 `chat.layout.scrollToSubagentActivity` 英/中文文案；`StatusPanel` 根据 `tool.type === 'agent'` 选择 aria-label key；不改视觉布局、不改跳转行为、不改状态摘要。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 task/subagent 跳转行必须有各自准确的 accessible label。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `StatusPanel` 测试、相邻 status/message/i18n 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `out/`。

## 迭代记录 2026-06-19 chat status panel subagent jump label

- 上轮实际进展：上一轮已在桌面右侧 `StatusPanel` 的 `currentActivity` 卡片中增加独立 subagent / Task compact list，并复用 transcript jump/highlight 机制；代码复查确认 `src/components/chat/StatusPanel.tsx` 已从 `statusSummary.agentTools` 派生 `recentActivitySubagents`，并渲染 `status-activity-subagent-list`。已知阻塞仍是未启动真实 Tauri 桌面做子代理行点击后的滚动/高亮点测，以及未与 cc-gui 做并排截图审计。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：桌面状态面板已能展示子代理活动，但子代理行复用普通任务行后，辅助技术读到的仍是“Jump to tool task / 定位工具任务”。这会把 subagent / Task 活动误报成普通工具任务，削弱右栏作为过程索引的语义清晰度。
- 本轮诊断（超越 cc-gui）：本项目把右栏设计成“索引 + 跳转”，而不是复制完整工具详情；因此每一种索引行都应该用准确语义告诉用户跳到哪里。子代理专用 aria-label 解决屏幕阅读器、自动化测试和键盘用户在任务/子代理混排场景下无法区分目标类型的痛点。
- 本轮规划：只做可访问语义修正，不改视觉布局、不改数据派生、不改跳转和高亮逻辑、不新增依赖。优先级中等偏高：用户价值中等（主要提升可访问性和语义准确性）、出现频率中（有 subagent/Task 活动时触发）、成本低（单 helper + i18n + 测试）、风险低（普通 task 保持原文案）。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 已有 RED 回归，断言子代理行使用 `Jump to subagent activity / 定位子代理活动`，并且不再使用 `Jump to tool task / 定位工具任务`。`src/components/chat/StatusPanel.tsx` 新增 `getActivityJumpLabelKey()`，按 `tool.type === 'agent'` 选择 `chat.layout.scrollToSubagentActivity`，普通任务继续使用 `chat.layout.scrollToToolTask`。`src/locales/en.json` / `src/locales/zh.json` 新增对应英中文文案。`.trellis/spec/frontend/component-guidelines.md` 已同步 task/subagent jump rows 必须使用准确 accessible labels 的契约。
- 本轮验证：RED：接手时测试断言已写入，上一执行段已运行 `npm test -- src/components/chat/StatusPanel.test.tsx` 并因子代理行仍为“定位工具任务：write regression coverage”失败；本轮复查确认该 RED 断言仍在代码中。GREEN：`npm test -- src/components/chat/StatusPanel.test.tsx` 通过（1 file / 24 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/ContentBlockRenderer.test.tsx src/components/chat/MessageList.test.tsx src/utils/chatStatusSummary.test.ts`（5 files / 59 tests）。全量验证通过：`npm test`（37 files / 332 tests，仅 Browserslist/caniuse-lite 过期提示）、`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）、IDE `build_project`（`problems: []`）。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。构建/IDE 生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理。
- 未验证项：未启动真实 Tauri 桌面做右栏子代理行点击后的滚动/高亮视觉点测；未与 cc-gui 做并排截图确认右栏 subagent 密度、标题层级、截断和可访问名称。
- 下一步候选：1) 启动真实 Tauri 桌面做宽屏/窄屏点测：宽屏点击右栏普通 task 与 subagent 行都应跳到对应消息流工具块并短暂高亮，窄屏输入区 subagents fallback 仍正常；2) 做 cc-gui 与本项目并排截图审计，覆盖右栏任务/子代理分区、会话列表、输入区布局、provider/model 图标和长历史消息排版；3) 如果右栏 `currentActivity` 因 tasks + subagents 同时很多而过高，下一轮给该卡片内部列表加局部高度上限，而不是隐藏子代理；4) 若用户继续反馈大历史卡顿，转向后端 tail reader / 分页搜索方向，避免继续在前端完整数组上补丁。

## 本轮 PLAN 2026-06-19 chat status panel activity scroll region

- 目标：给桌面右侧 `StatusPanel` 的 `currentActivity` 中 tasks + subagents 列表增加局部高度上限和滚动区域。上一轮已补齐子代理活动和准确 aria-label，但当普通任务与子代理同时较多时，活动卡片会继续纵向增长，可能把 recent edits、MCP、runtime context 挤出右栏首屏。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 和 cc-gui `StatusPanel.tsx` / `StatusPanel.less` / `SubagentList.tsx`，确认 cc-gui 对 todo/subagent/file lists 有 `max-height` + `overflow-y:auto`，本项目当前 activity lists 没有局部滚动边界。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，断言同时存在 task/subagent 列表时渲染 `status-activity-scroll-region`，并带 `max-h-64` / `overflow-y-auto`。
- 功能点 4：实现最小展示层改动。验证方式：`StatusPanel` 只在有 tasks/subagents 列表时包一层 scroll region；active tool / streaming / idle 主行保留在滚动区外，任务和子代理仍独立分区，跳转和 aria-label 不变。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录桌面 currentActivity 内部列表需要局部高度上限，避免右栏其它诊断被任务历史挤掉。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `StatusPanel` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist/` / `out/`。

## 迭代记录 2026-06-19 chat status panel activity scroll region

- 上轮实际进展：上一轮修正了右侧 `StatusPanel` 子代理活动行的可访问语义；代码复查确认 `src/components/chat/StatusPanel.tsx` 已通过 `getActivityJumpLabelKey()` 区分普通工具任务和子代理活动，`src/locales/en.json` / `src/locales/zh.json` 已有 `scrollToSubagentActivity` 文案，`src/components/chat/StatusPanel.test.tsx` 已覆盖子代理行不再读作普通工具任务。已知阻塞仍是真实 Tauri 桌面点击/高亮点测和 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui `webview/src/components/StatusPanel/StatusPanel.less` 对 todo、subagent、file changes 列表均设置了约 200px 的 `max-height` 和 `overflow-y:auto`；本项目 `StatusPanel` 的 `currentActivity` 在上一轮后会同时展示 recent tasks 与 subagents，但两个列表直接堆在卡片内，没有内部滚动边界。工具密集会话中，这张活动卡片会把 recent edits、MCP 和 runtime context 推出右栏首屏。
- 本轮诊断（超越 cc-gui）：本项目没有照搬 cc-gui 的 tab/popover，而是把最近任务和子代理直接放进桌面右栏，降低点击成本；为了让这个“更快扫”的设计成立，列表区域必须自己吸收溢出，而不是让整个右栏跟着增长。这样解决“右栏任务历史很有用但抢占其它诊断”的痛点。
- 本轮规划：只做展示层密度控制，不改 `MAX_ACTIVITY_TASKS` / `MAX_ACTIVITY_SUBAGENTS`、不改 `ChatStatusSummary`、不改跳转/高亮/aria-label、不改后端。优先级中高：用户价值中高（保护右栏其它诊断可见）、出现频率中（工具密集会话常见）、实现成本低（单组件 Tailwind class + SSR 断言）、风险低（只影响列表容器）。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 在现有“tasks 与 subagents 分区”回归中新增断言，要求渲染 `status-activity-scroll-region` 且带 `max-h-64` / `overflow-y-auto`。RED 失败点符合预期：旧实现缺少 `status-activity-scroll-region`。`src/components/chat/StatusPanel.tsx` 在有 task 或 subagent 列表时包裹 `status-activity-scroll-region mt-2 max-h-64 min-h-0 space-y-2 overflow-y-auto overscroll-contain pr-1`；active tool / streaming / idle 主行保留在滚动区外，普通 tasks 与 subagents 仍为独立分区。`.trellis/spec/frontend/component-guidelines.md` 已同步桌面 `currentActivity` 活动列表应使用内部滚动区域和局部高度上限的契约。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为缺少 `status-activity-scroll-region`。GREEN：同命令通过（1 file / 24 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/ContentBlockRenderer.test.tsx src/components/chat/MessageList.test.tsx src/utils/chatStatusSummary.test.ts`（5 files / 59 tests）。全量验证通过：`npm test`（37 files / 332 tests，仅 Browserslist/caniuse-lite 过期提示）、`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）、IDE `build_project`（`problems: []`）。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。构建/IDE 生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理。
- 未验证项：未启动真实 Tauri 桌面做右栏 activity scroll region 的滚轮/触控板滚动点测；未与 cc-gui 并排截图确认 16rem 上限在真实 320px 右栏中的密度是否需要微调。
- 下一步候选：1) 启动真实 Tauri 桌面做右栏 activity scroll region 点测：构造 tasks + subagents 同时较多的会话，确认活动列表内部滚动、active tool 主行不被卷走、recent edits/MCP 仍可见；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看右栏密度、会话列表、输入区按钮、provider/model 图标对齐和搜索反馈；3) 如果 `max-h-64` 在真实桌面中过高或过低，只微调局部上限，不改变 tasks/subagents 的数据截断策略；4) 若用户继续反馈大历史卡顿，继续转向后端 tail reader / 分页搜索，不再在前端完整数组上补丁。

## 本轮 PLAN 2026-06-19 chat status panel activity scroll accessibility

- 目标：补齐上一轮右侧 `StatusPanel` activity 内部滚动区的键盘可达性。上一轮已加 `max-h-64` / `overflow-y-auto`，但滚动容器本身没有语义和焦点入口；键盘用户只能 Tab 到内部按钮，不能可靠地把焦点放到滚动容器上使用 PageUp/PageDown/方向键浏览长任务历史。
- 功能点 1：恢复上下文与规范。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端 component/quality/type 规范、shared guides 和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `StatusPanel.tsx` 与 `StatusPanel.test.tsx`，确认当前 `status-activity-scroll-region` 只有样式类，没有 `role`、`tabIndex`、`aria-label` 或焦点样式。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，断言 activity scroll region 渲染为 `role="region"`、`tabindex="0"`，并带 i18n 后的 `Activity history / 活动历史` accessible label；旧实现应因缺少这些属性失败。
- 功能点 4：实现最小可访问性改动。验证方式：`StatusPanel` 只给滚动容器新增 `role="region"`、`tabIndex={0}`、`aria-label={t('chat.layout.activityHistoryRegion')}` 和低噪音 focus ring；新增英中文 i18n key；不改 active tool 主行、tasks/subagents 分区、跳转、hidden count 或数据上限。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录桌面 `currentActivity` 内部滚动区必须是可聚焦、有命名的 region。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `StatusPanel` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、必要时 `cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist/` / `out/`。

## 迭代记录 2026-06-19 chat status panel activity scroll accessibility

- 上轮实际进展：上一轮已给右侧 `StatusPanel` 的 `currentActivity` tasks/subagents 列表加上内部滚动区；代码复查确认 `src/components/chat/StatusPanel.tsx` 已有 `status-activity-scroll-region mt-2 max-h-64 min-h-0 space-y-2 overflow-y-auto overscroll-contain pr-1`，并且 active tool / streaming / idle 主行在滚动区外。已知阻塞仍是真实 Tauri 桌面滚轮/触控板点测和 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的状态面板列表有内部滚动边界；本项目上一轮已补齐视觉滚动区，但该容器没有 `role`、`aria-label` 或 `tabIndex`。在任务/子代理很多时，键盘用户无法直接聚焦滚动容器并使用 PageUp/PageDown/方向键浏览活动历史，只能进入内部按钮序列。
- 本轮诊断（超越 cc-gui）：本项目把右栏设计成可跳转的活动索引，列表滚动区本身也应成为一个命名导航区域。给滚动区增加“活动历史”语义和焦点入口，可以解决“有滚动但键盘无法可靠进入滚动上下文”的痛点，同时不增加视觉噪音。
- 本轮规划：只做可访问性与焦点状态，不改 `MAX_ACTIVITY_TASKS` / `MAX_ACTIVITY_SUBAGENTS`、不改 `ChatStatusSummary`、不改跳转/高亮/hidden count、不改后端。优先级中等偏高：用户价值中等（键盘与辅助技术可用性）、出现频率中（任务密集会话触发）、实现成本低（单容器属性 + i18n + SSR 断言）、风险低（不改变数据或布局结构）。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 扩展 activity list 回归，断言 `status-activity-scroll-region` 同时带 `role="region"`、`tabindex="0"` 和 `Activity history / 活动历史` accessible label；RED 失败点符合预期：旧实现缺少 `role="region"`。`src/components/chat/StatusPanel.tsx` 给滚动容器新增 `role="region"`、`tabIndex={0}`、`aria-label={t('chat.layout.activityHistoryRegion')}` 和 `focus:ring`。`src/locales/en.json` / `src/locales/zh.json` 新增 `chat.layout.activityHistoryRegion`。`.trellis/spec/frontend/component-guidelines.md` 已同步：桌面 `currentActivity` 内部滚动区必须是可聚焦、有命名的 region。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为缺少 `role="region"`。GREEN：同命令通过（1 file / 24 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/ContentBlockRenderer.test.tsx src/components/chat/MessageList.test.tsx src/utils/chatStatusSummary.test.ts`（5 files / 59 tests，仅 Browserslist/caniuse-lite 过期提示）。全量验证通过：`npm test`（37 files / 332 tests，仅 Browserslist/caniuse-lite 过期提示）、`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）、IDE `build_project`（`problems: []`）。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。构建/IDE 生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理。
- 未验证项：未启动真实 Tauri 桌面做右栏 activity scroll region 的键盘滚动、滚轮/触控板滚动和焦点 ring 视觉点测；未与 cc-gui 并排截图确认 `max-h-64` 与 focus ring 在真实 320px 右栏中的视觉密度。
- 下一步候选：1) 启动真实 Tauri 桌面做右栏 activity scroll region 点测：构造 tasks + subagents 同时较多的会话，确认 Tab 可聚焦“活动历史”区域、PageUp/PageDown/方向键能滚动内部列表、active tool 主行不被卷走、recent edits/MCP 仍可见；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看右栏密度、会话列表、输入区按钮、provider/model 图标对齐和搜索反馈；3) 如果 `max-h-64` 或 focus ring 在真实桌面中过强/过弱，只微调局部上限或 ring 强度，不改变 tasks/subagents 数据截断策略；4) 若用户继续反馈大历史卡顿，继续转向后端 tail reader / 分页搜索，不再在前端完整数组上补丁。

## 本轮 PLAN 2026-06-19 chat input status tool jump targets

- 目标：补齐输入区 `ChatInputStatusTabs` 小屏 fallback 中 tasks/subagents 行的 transcript 跳转能力。右侧 `StatusPanel` 已支持点击普通 task / subagent 行定位到消息流工具块并高亮；但小屏隐藏右栏时，输入区状态条目前只展示静态行，用户无法从 tasks/subagents 面板快速回到对应工具块。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端 component/state/quality/type 规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ChatInputStatusTabs.tsx`、`ChatPage.tsx`、`StatusPanel.tsx` 与 cc-gui `StatusPanel` / `TodoList` / `SubagentList`，确认本项目右栏已有 `onSelectTool` 跳转，输入区 fallback 缺少同等入口。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ChatInputStatusTabs.test.tsx`，断言 tasks/subagents 面板中的工具行渲染为带 `data-target-tool-id`、准确 `aria-label` 的按钮；未传 `onSelectTool` 时保留 disabled 只读语义。
- 功能点 4：实现最小交互改动。验证方式：`ChatInputStatusTabs` 新增 `onSelectTool?: (tool) => void` prop，普通 task 行使用 `chat.layout.scrollToToolTask`，subagent 行使用 `chat.layout.scrollToSubagentActivity`；点击时委托给 `ChatPage.handleSelectStatusTool`，不改状态摘要、不改右栏、不改工具块锚点机制。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 composer status strip 作为 small-screen fallback 时，tasks/subagents 行应复用 transcript jump target 语义。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `ChatInputStatusTabs` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、必要时 `cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist/` / `out/`。

## 迭代记录 2026-06-19 chat input status tool jump targets

- 上轮实际进展：上一轮记录声称已给右侧 `StatusPanel` activity 内部滚动区补齐 `role="region"`、`tabIndex={0}`、`aria-label` 和 focus ring；代码复查确认 `src/components/chat/StatusPanel.tsx`、`StatusPanel.test.tsx`、`src/locales/en.json` / `zh.json` 与 `.trellis/spec/frontend/component-guidelines.md` 均存在该实现。已知阻塞仍是未启动真实 Tauri 桌面做滚动区键盘/滚轮/焦点视觉点测，以及未与 cc-gui 做并排截图。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 将 todo、subagent、file changes 作为状态面板入口，并通过 `TodoList.tsx` / `SubagentList.tsx` 展示可操作活动列表。本项目右侧 `StatusPanel` 已超前做成 transcript 索引和跳转目标，但输入区 `ChatInputStatusTabs` 作为小屏 fallback 仍把 task/subagent 行渲染为静态 `div`；窄屏隐藏右栏时，用户无法从 tasks/subagents 面板定位到对应工具块。
- 本轮诊断（超越 cc-gui）：本项目的价值不只是展示状态，而是把状态行变成“索引 + 跳转 + 高亮”的导航入口。把同等能力补到输入区 fallback，解决小屏用户只能看到任务摘要、不能回到消息流上下文的痛点，同时保持桌面 `xl` 下不重复展示详细面板。
- 本轮规划：只做输入区状态条 tasks/subagents 行的交互语义，不改 `ChatStatusSummary`、不改右侧 `StatusPanel`、不改 `MessageList` 锚点机制、不新增 i18n 文案或依赖。优先级中高：用户价值高（窄屏任务定位可用）、出现频率中（工具/子代理会话常见）、实现成本低（一个 prop + 行元素改 button + 现有 handler 接线）、风险低（复用已有跳转逻辑）。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.test.tsx` 新增 3 个回归，覆盖普通 task 行 `data-target-tool-id`、子代理专用 `aria-label`、无 handler 时 disabled 只读语义。`src/components/chat/ChatInputStatusTabs.tsx` 新增 `onSelectTool?: (tool: ChatStatusToolSummary) => void`，把 `renderToolRow()` 从静态 `div` 改为按钮，增加 `data-target-tool-id`、准确 task/subagent aria-label、hover/focus 状态和 disabled 只读语义，点击时委托 `onSelectTool`。`src/pages/ChatPage.tsx` 将已有 `handleSelectStatusTool` 传给 `ChatInputStatusTabs`，复用现有 `findToolAnchorElement()`、`scrollIntoView()`、focus 和短暂高亮机制。`.trellis/spec/frontend/component-guidelines.md` 已同步 composer status strip 小屏 fallback 的 transcript jump target 契约。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 3 项，失败点为缺少 `data-target-tool-id="tool-build"` / `data-target-tool-id="tool-agent"`，符合预期。GREEN：同命令通过（1 file / 15 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/ContentBlockRenderer.test.tsx src/utils/chatStatusSummary.test.ts src/utils/chatNavigation.test.ts`（6 files / 76 tests，仅 Browserslist/caniuse-lite 过期提示）。全量验证通过：`npm test`（37 files / 335 tests，仅 Browserslist/caniuse-lite 过期提示）、`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）、IDE `build_project`（`problems: []`）。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。构建/IDE 生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理。
- 未验证项：未启动真实 Tauri 桌面做窄屏输入区 tasks/subagents 面板点击后的滚动、focus、高亮视觉点测；未与 cc-gui 做真实运行并排截图确认小屏 fallback 与右栏密度的实际体验差异。
- 下一步候选：1) 启动真实 Tauri 桌面做窄屏/宽屏点测：窄屏打开输入区 tasks/subagents 面板，点击普通 task 与 subagent 行都应跳到对应工具块并短暂高亮；宽屏确认右栏仍是主状态索引，输入区 detail tabs `xl:hidden` 不重复占位；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看右栏密度、输入区 fallback、会话列表、provider/model 图标和搜索反馈；3) 若窄屏 popover 点击后仍遮挡目标位置，下一轮只在 selection 后关闭 popover或微调滚动 block 策略，不改锚点数据结构；4) 若用户继续反馈大历史卡顿，优先转向后端 tail reader / 分页搜索，不再继续在前端完整数组上补丁。

## 本轮 PLAN 2026-06-19 chat input status popover dismiss on jump

- 目标：补齐上一轮输入区 tasks/subagents 跳转后的浮层关闭策略。上一轮已让小屏 fallback 的 task/subagent 行可跳到 transcript 工具块，但 `ChatInputStatusTabs` 的 popover 是绝对定位在 composer 上方；点击跳转后如果浮层保持打开，窄屏用户可能看不到刚跳转并高亮的目标上下文。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `ChatInputStatusTabs.tsx` 与测试，确认当前 `renderToolRow` 只调用 `onSelectTool?.(tool)`，不会改变 `openTab`。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ChatInputStatusTabs.test.tsx`，用纯 helper 断言可跳转工具行选择后应关闭当前 tab；无 selection handler 时保持当前 tab，避免 disabled 行产生状态变化。
- 功能点 4：实现最小交互改动。验证方式：`ChatInputStatusTabs` 新增/使用 helper，在工具行点击时先计算下一 open tab；有 `onSelectTool` 时 `setOpenTab(null)` 并委托 page-level jump handler，不改跳转、高亮、aria-label 或数据摘要。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 small-screen fallback 的 transcript jump 行在触发定位后应关闭临时 popover，避免遮挡目标。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `ChatInputStatusTabs` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-19 chat input status popover dismiss on jump

- 上轮实际进展：上一轮已让输入区 `ChatInputStatusTabs` 的 tasks/subagents 行具备 transcript jump target 语义；代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已有 `onSelectTool` prop、`data-target-tool-id`、task/subagent 专用 aria-label，并且 `src/pages/ChatPage.tsx` 已把 `handleSelectStatusTool` 传入输入区状态条。已知阻塞仍是真实 Tauri 桌面窄屏点击/滚动/高亮视觉点测，以及 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的状态入口属于临时 popover / 面板式状态查看，本项目上一轮把小屏 fallback 的 task/subagent 行升级为 transcript 跳转入口后，`ChatInputStatusTabs` 仍只执行 `onSelectTool?.(tool)`，不会关闭 `openTab`。由于该 popover 绝对定位在 composer 上方，窄屏跳转后它可能继续遮挡刚滚到中间并高亮的 transcript 工具块。
- 本轮诊断（超越 cc-gui）：本项目的状态行承担“定位到消息流上下文”的职责；定位成功后临时详情面板应让位给目标内容。点击 task/subagent 行后关闭 popover，可以解决小屏用户“点了跳转但浮层挡住目标”的痛点，同时保留 edits/MCP 面板的现有查看行为。
- 本轮规划：只调整输入区 task/subagent selection 后的 open tab 状态，不改右栏 `StatusPanel`、不改 `handleSelectStatusTool`、不改消息锚点/高亮、不改 i18n 或后端。优先级中等偏高：用户价值中高（直接改善上一轮新增交互的完成感）、出现频率中（小屏工具/子代理定位时触发）、实现成本低（纯 helper + 一行状态更新）、风险低（disabled/no-handler 路径保持当前 tab）。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.test.tsx` 新增 `dismisses the transient status popover after a transcript tool jump` 回归，覆盖有 selection handler 时 `tasks` / `subagents` 变为 `null`，无 handler 时保留当前 tab。`src/components/chat/ChatInputStatusTabs.tsx` 新增 `getInputStatusTabAfterToolSelection()` 并在工具行点击时调用 `setOpenTab(...)` 后继续委托 `onSelectTool`，实现跳转后关闭临时 popover。`.trellis/spec/frontend/component-guidelines.md` 已同步 small-screen fallback 的 task/subagent jump 行触发定位后应关闭 popover 的契约。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 1 项，失败点为 `getInputStatusTabAfterToolSelection is not a function`。GREEN：同命令通过（1 file / 16 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/ContentBlockRenderer.test.tsx src/utils/chatStatusSummary.test.ts src/utils/chatNavigation.test.ts`（6 files / 77 tests，仅 Browserslist/caniuse-lite 过期提示）。全量验证通过：`npm test`（37 files / 336 tests，仅 Browserslist/caniuse-lite 过期提示）、`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）、IDE `build_project`（`problems: []`）。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。构建/IDE 生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理。
- 未验证项：未启动真实 Tauri 桌面做窄屏输入区 task/subagent 行点击后 popover 关闭、目标滚动、focus 和高亮的视觉点测；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 启动真实 Tauri 桌面做窄屏/宽屏点测，确认小屏点击 tasks/subagents 行后 popover 关闭且目标工具块高亮可见，宽屏右栏仍为主状态索引；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看右栏密度、输入区 fallback、会话列表、provider/model 图标和搜索反馈；3) 如果真实点测发现关闭 popover 后滚动目标仍被 composer 遮挡，下一轮只微调 `scrollIntoView` block 策略或额外 scroll offset，不改锚点数据结构；4) 若用户继续反馈大历史卡顿，优先转向后端 tail reader / 分页搜索，不再继续在前端完整数组上补丁。

## 本轮 PLAN 2026-06-19 chat input status popover accessibility

- 目标：补齐输入区 `ChatInputStatusTabs` 小屏 quick-detail popover 的可访问性闭环。上一轮已让 task/subagent 行可跳转并在跳转后关闭 popover，但打开的详情面板本身仍只是一个可滚动 `div`，缺少可聚焦和命名区域语义；键盘用户无法直接进入这个滚动上下文浏览任务、子代理、编辑或 MCP 详情。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端 component/state/quality/type 规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ChatInputStatusTabs.tsx`、`ChatInputStatusTabs.test.tsx` 和 cc-gui `StatusPanel` / `TodoList` / `SubagentList`，确认本项目已超越 cc-gui 做 transcript jump，但 quick-detail popover 还缺少命名可聚焦滚动区。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ChatInputStatusTabs.test.tsx`，断言打开的 `chat-input-status-popover-panel` 渲染为 `role="region"`、`tabIndex={0}`，并使用 i18n key `chat.layout.inputStatusDetailsRegion` 作为 accessible label；旧实现应因缺少这些属性失败。
- 功能点 4：实现最小展示/交互改动。验证方式：`ChatInputStatusTabs` 只给 popover panel 增加 `role`、`tabIndex`、`aria-label` 和低噪音 focus ring；新增英中文 i18n key；不改 task/subagent 跳转、popover 关闭策略、状态摘要或后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 composer status strip 的 quick-detail popover 必须是命名且可聚焦的滚动 region。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `ChatInputStatusTabs` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、必要时 `cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-19 chat input status popover accessibility

- 上轮实际进展：上一轮记录声称已让小屏输入区 `ChatInputStatusTabs` 的 task/subagent 行在 transcript jump 后关闭 transient popover；代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已存在 `getInputStatusTabAfterToolSelection()`，点击工具行会先更新 `openTab`，并继续委托 `onSelectTool`；`src/pages/ChatPage.tsx` 已把 `handleSelectStatusTool` 接入输入区状态条。已知阻塞仍是未启动真实 Tauri 桌面做窄屏点击、滚动、focus 和高亮视觉点测，以及未与 cc-gui 做真实运行并排截图。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 把 todo/subagent/file changes 放在临时 popover 中，基础体验是可快速查看状态详情；本项目的小屏 composer status strip 已做到比 cc-gui 更进一步的 transcript jump，但打开的 `chat-input-status-popover-panel` 仍只是一个滚动 `div`，没有 `role`、命名或 `tabIndex`。键盘用户打开任务/子代理/编辑/MCP 详情后，无法直接聚焦这个滚动上下文。
- 本轮诊断（超越 cc-gui）：本项目把 composer status strip 设计成右栏 `StatusPanel` 的小屏 fallback。右栏活动历史滚动区上一轮已补齐 named region；输入区 quick-detail popover 也应该有同样的可访问性边界。给临时详情面板增加命名可聚焦 region 可以解决“详情已展开但键盘无法可靠进入滚动区域”的痛点，同时不改变布局、跳转、状态摘要或后端行为。
- 本轮规划：只做 popover panel 的可访问性属性和 i18n 文案，不改 task/subagent 行按钮、不改 `onSelectTool`、不改 Escape/outside-click 关闭、不改 `ChatPage` 和任何 store。优先级中等偏高：用户价值中等（键盘/辅助技术可用性）、出现频率中（小屏或窄窗口使用状态详情时触发）、实现成本低（一个容器属性 + 两条 locale + SSR 断言）、风险低（纯展示语义）。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.test.tsx` 扩展 anchored popover 回归，断言打开的 quick-detail panel 带 `role="region"`、`tabIndex={0}` 和 `chat.layout.inputStatusDetailsRegion` accessible label。`src/components/chat/ChatInputStatusTabs.tsx` 给 `chat-input-status-popover-panel` 增加 `role="region"`、`tabIndex={0}`、`aria-label={t('chat.layout.inputStatusDetailsRegion')}` 和低噪音 focus ring。`src/locales/en.json` / `src/locales/zh.json` 新增 `Status details` / `状态详情`。`.trellis/spec/frontend/component-guidelines.md` 已同步：composer status strip 的 quick-detail popover 滚动容器必须是可聚焦、有命名的 region。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 1 项，失败点为旧 HTML 缺少 `role="region"`，符合预期。GREEN：同命令通过（1 file / 16 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/ContentBlockRenderer.test.tsx src/utils/chatStatusSummary.test.ts src/utils/chatNavigation.test.ts`（6 files / 77 tests，仅 Browserslist/caniuse-lite 过期提示）。全量验证通过：`npm test`（37 files / 336 tests，仅 Browserslist/caniuse-lite 过期提示）、`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）、IDE `build_project`（`problems: []`）。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。构建/IDE 生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查均不存在。
- 未验证项：本轮未启动真实 Tauri 桌面做窄屏 quick-detail popover 的 Tab 聚焦、方向键/PageUp/PageDown 滚动、focus ring 视觉强度、task/subagent 跳转后高亮可见性点测；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 启动真实 Tauri 桌面做窄屏/宽屏点测，确认输入区 quick-detail popover 可聚焦可滚动、task/subagent 行跳转后 popover 关闭且目标高亮可见，宽屏右栏仍为主状态索引；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看右栏密度、输入区 fallback、会话列表、provider/model 图标和搜索反馈；3) 如果真实点测发现关闭 popover 后滚动目标仍被 composer 遮挡，下一轮只微调 `scrollIntoView` block 策略或额外 scroll offset，不改锚点数据结构；4) 若用户继续反馈大历史卡顿，优先转向后端 tail reader / 分页搜索，不再继续在前端完整数组上补丁。

## 本轮 PLAN 2026-06-19 chat input status edit selection popover behavior

- 目标：补齐输入区 `ChatInputStatusTabs` edits 面板的选择语义。当前 task/subagent 行已经在无 handler 时 disabled，并在触发 transcript jump 后关闭临时 popover；edit 行仍然在无 `onSelectedEditChange` 时可聚焦/可点击但实际 no-op，且选择文件后不关闭浮层，容易遮挡随后打开的页面级 diff/context。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ChatInputStatusTabs.tsx` / 测试与 cc-gui `StatusPanel` / `FileChangesList` 状态入口，确认本项目 edits 行复用 page-level selection handler，但缺少 disabled/no-handler 与选择后关闭 popover 的契约。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ChatInputStatusTabs.test.tsx`，断言 edit selection helper 在可选择时返回 `null`、无 handler 时保留当前 tab；断言有 handler 的 edit 行不是 disabled，无 handler 的 edit 行是 disabled。
- 功能点 4：实现最小交互改动。验证方式：`ChatInputStatusTabs` 新增 edit selection helper 和本地 handler；edit 行在无 handler 时 disabled，只读但保持现有展示；有 handler 时点击先关闭 transient popover，再委托 `onSelectedEditChange`。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 composer status strip 的 edit 行无 handler 时必须只读，选择后应关闭临时 popover，避免遮挡页面级 diff/context。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `ChatInputStatusTabs` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、必要时 `cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-19 chat input status edit selection popover behavior

- 上轮实际进展：上一轮记录声称已给输入区 `ChatInputStatusTabs` 的 quick-detail popover 补齐 `role="region"`、`tabIndex={0}`、命名 `aria-label` 和 focus ring；代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx`、`ChatInputStatusTabs.test.tsx`、`src/locales/en.json` / `zh.json` 与 `.trellis/spec/frontend/component-guidelines.md` 均存在该实现。已知阻塞仍是未启动真实 Tauri 桌面做窄屏 popover 键盘滚动 / task-subagent 跳转高亮点测，以及未与 cc-gui 做真实运行并排截图。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 通过状态 tab 打开 todo/subagent/file changes popover，并在 `FileChangesList` 内提供文件变更操作入口。本项目已经把输入区 status strip 的 edits 入口接到 page-level diff selection handler，但 `renderEditsPanel()` 中 edit 行始终是可点击 `<button>`：没有 `onSelectedEditChange` 时仍可聚焦/可点但只是 no-op；有 handler 时选择后也不关闭 transient popover，和 task/subagent 行“动作完成后让位给目标内容”的策略不一致。
- 本轮诊断（超越 cc-gui）：本项目的 composer status strip 是右栏状态面板的小屏 fallback，edit 行应是“选择页面级 diff/context”的入口，而不是一个独立 diff surface。无 handler 时禁用行可以避免键盘用户进入无效操作；选择成功后关闭 popover 可以解决“选中了文件但浮层继续遮挡 diff/context”的痛点。
- 本轮规划：只改 `ChatInputStatusTabs` 的 edit selection helper、edit 行 disabled 语义和选择后关闭 popover；不改 `ChatPage`、不改 diff pane、不改状态摘要数据结构、不新增 i18n 或依赖。优先级中等偏高：用户价值中等（减少小屏无效点击和遮挡）、出现频率中（发生在有编辑历史时）、实现成本低（一个 helper + 行属性）、风险低（只影响 handler 存在与否的交互边界）。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.test.tsx` 新增 edit selection helper 回归，断言可选择 edit 后 open tab 变为 `null`，无 handler 时保留 `edits`；扩展 edits 面板 SSR 断言，有 `onSelectedEditChange` 时 edit 行不含 `disabled=""`，无 handler 时 edit 行含 `disabled=""`。`src/components/chat/ChatInputStatusTabs.tsx` 新增 `getInputStatusTabAfterEditSelection()` 和 `handleSelectEditRow()`，edit 行增加 focus ring、disabled 只读样式、`disabled={!onSelectedEditChange}`，点击时先按 helper 关闭/保留 popover，再委托 `onSelectedEditChange`。`.trellis/spec/frontend/component-guidelines.md` 已同步 composer status strip edit 行的 no-handler 只读与选择后关闭 popover 契约。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 2 项，失败点为 `getInputStatusTabAfterEditSelection is not a function` 和无 handler edit 行缺少 `disabled=""`，符合预期。GREEN：同命令通过（1 file / 18 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/ContentBlockRenderer.test.tsx src/utils/chatStatusSummary.test.ts src/utils/chatNavigation.test.ts`（6 files / 79 tests，仅 Browserslist/caniuse-lite 过期提示）。全量前端验证通过：`npm test`（37 files / 338 tests，仅 Browserslist/caniuse-lite 过期提示）、`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。构建/IDE 生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查均不存在。
- 未验证项：未启动真实 Tauri 桌面做 edits 面板选择后 popover 关闭、页面级 diff/context 是否立即可见、focus ring 视觉强度和窄屏滚动遮挡点测；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 启动真实 Tauri 桌面做窄屏/宽屏点测，覆盖 quick-detail popover 可聚焦滚动、task/subagent 跳转关闭与高亮、edit 选择关闭并露出 diff/context；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看右栏密度、输入区 fallback、会话列表、provider/model 图标和搜索反馈；3) 如果真实点测发现关闭 popover 后目标仍被 composer 遮挡，下一轮只微调 `scrollIntoView` block 策略或额外 scroll offset，不改锚点数据结构；4) 若用户继续反馈大历史卡顿，优先转向后端 tail reader / 分页搜索，不再继续在前端完整数组上补丁。

## 本轮 PLAN 2026-06-19 chat status edit hover preview contrast

- 目标：修复聊天右侧 `StatusPanel` 文件悬浮 diff 预览透明感过强、内容看不清的问题。当前 status 专用预览外层已设置 `opacity: 1`，但内部 diff 行仍大量使用半透明底色，叠在状态面板和消息背景上会降低可读性。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、`package.json`、前端组件规范和相关代码；确认本项目位于 `C:/guodevelop/ccg-switch` 且 `.codegraph/` 可用，cc-gui 原始路径不可用时沿用已记录的 `C:/guodevelop/demo/jetbrains-cc-gui` 结论。
- 功能点 2：定位问题边界。验证方式：通过 CodeGraph 确认 `StatusPanel.tsx` 使用 `EditDiffPreview surface="status"` 渲染文件 hover 预览；通过 CSS 复查确认默认 hover preview 与 status 专用 preview 共用基础 diff 行样式。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/toolBlocks/EditDiffPreview.test.tsx`，断言 status hover preview 额外输出 `edit-diff-hover-preview-solid` 类，且仍保留 `edit-diff-hover-preview-status` / `is-visible`；旧实现应失败。
- 功能点 4：实现最小展示层修复。验证方式：`EditDiffPreview` 仅在 `surface="status"` 的 hover preview 上增加 solid 类；`src/styles/toolBlocks.css` 只增强 status solid preview 的实底背景、边框、阴影、header/body 和 added/removed/context/split 行底色，不影响默认 transcript hover preview。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，明确 status-panel diff hover preview 内部行也必须使用实底高对比色，避免半透明叠底造成文字不可读。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `EditDiffPreview` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-19 chat status edit hover preview contrast

- 上轮实际进展：上一轮记录声称已修正输入区 `ChatInputStatusTabs` edits 面板的选择语义；本轮代码复查确认相关实现仍存在，当前用户反馈转向聊天右侧文件 hover diff 预览“透明度太高、看不清楚”。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 仍未在本轮重新验证到，沿用既有记录：实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。已知阻塞仍是真实 Tauri 桌面 hover 视觉点测和 cc-gui 并排截图未完成。
- 本轮诊断（补齐 cc-gui）：cc-gui 状态面板的 file changes 入口强调临时查看时的可读性；本项目 `src/components/chat/StatusPanel.tsx` 通过 `EditDiffPreview surface="status"` 渲染文件悬浮 diff 预览，`src/styles/toolBlocks.css` 的外层 preview 虽然已有 `opacity: 1`，但内部 added/removed/context/split diff 行沿用通用 hover preview 的半透明或透明背景。悬浮框叠在右侧状态面板和聊天背景上时，代码行对比度不足，符合用户反馈。
- 本轮诊断（超越 cc-gui）：本项目右侧 edit tree 是一个高频扫描入口，hover preview 应该像小型代码审阅浮层，而不是半透明提示层。把 status 专用预览改成实底高对比，并保持默认 transcript hover preview 不变，可以解决“鼠标悬浮能打开但看不清 diff 内容”的痛点，同时避免全局视觉风格被一次反馈过度修改。
- 本轮规划：只处理 `surface="status"` 的 hover preview，不改 `StatusPanel` 数据流、不改 diff pane、不改默认 tool block hover preview、不新增依赖。优先级高：用户价值高（直接影响文件变更查看）、出现频率高（每次 hover 文件都触发）、实现成本低（类名 + CSS 覆盖 + SSR 回归）、风险低（status 专用选择器隔离）。
- 本轮完成：`src/components/toolBlocks/EditDiffPreview.test.tsx` 新增 status hover preview 回归，要求输出 `edit-diff-hover-preview-solid`。`src/components/toolBlocks/EditDiffPreview.tsx` 仅在 `surface="status"` 的 hover preview 上增加 `edit-diff-hover-preview-solid` 类。`src/styles/toolBlocks.css` 强化 `.status-edit-diff-trigger .edit-diff-hover-preview-status` 的实底背景、边框和阴影，并用不透明深色背景覆盖 status solid preview 的 header/body、context、added、removed、split 行和行号/marker 色值。`.trellis/spec/frontend/component-guidelines.md` 同步规范：status-panel diff hover preview 的内部 diff 行也必须使用不透明高对比背景。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx` 先失败 1 项，失败点为旧 HTML 缺少 `edit-diff-hover-preview-solid`。GREEN：同命令通过（1 file / 4 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx`（3 files / 34 tests）。全量验证通过：`npm test`（37 files / 339 tests，仅 Browserslist/caniuse-lite 过期提示）、`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）、IDE `build_project`（`problems: []`）。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查结果均不存在。
- 未验证项：未启动真实 Tauri 桌面做鼠标 hover 视觉点测；未与 cc-gui 做真实运行并排截图。本轮改动基于真实代码和 CSS 规则验证，实际透明感改善仍建议在桌面端用一条包含 added/removed/context 行的文件变更记录点测。
- 下一步候选：1) 启动真实 Tauri 桌面构造含多行 added/removed/context 的 edit history，点测右侧 `StatusPanel` 文件 hover preview 的浅色/深色主题可读性、边界定位和是否遮挡主聊天内容；2) 若真实点测发现 300px status hover preview 横向仍不够读代码，下一轮只调宽度/定位，不再回退到半透明风格；3) 做 cc-gui 与本项目真实运行并排截图审计，重点看 file changes hover、右栏密度、输入区 fallback、provider/model 图标和搜索反馈；4) 如果用户继续反馈文件变更查看成本高，优先把 hover preview 与中央 diff pane 的选择状态联动优化，而不是继续堆更多悬浮层。

## 本轮 PLAN 2026-06-19 chat status edit hover preview readable width

- 目标：延续上一轮 status 文件 hover diff 可读性修复，解决实底后仍可能因为 300px 窄浮层导致代码行、路径和变更摘要被过度截断的问题。cc-gui 的 file changes 是 popover + diff 按钮，本项目已经超越为右栏文件树 hover 小 diff；该超越功能需要具备足够代码阅读宽度。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `StatusPanel.tsx`、`EditDiffPreview` 与 `toolBlocks.css`，确认 status hover preview 当前宽度为 `min(300px, calc(100vw - 48px))`；读取 cc-gui `StatusPanel` / `FileChangesList` / `.less`，确认其提供 file changes popover 和显式 diff 操作，但无本项目这种 hover diff 预览。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/toolBlocks/EditDiffPreview.test.tsx`，锁定 status hover preview 必须输出 `edit-diff-hover-preview-readable` 类；旧实现应因缺少该类失败。
- 功能点 4：实现最小展示层修复。验证方式：只调整 `.status-edit-diff-trigger .edit-diff-hover-preview-status` 的宽度上限和 summary 可用宽度，不改默认 transcript hover preview、不改数据流、不改浮层触发逻辑。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，明确 status-panel diff hover preview 需要足够宽度给代码行，而不只是实底高对比。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `EditDiffPreview` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、必要时 `cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-19 chat status edit hover preview readable width

- 上轮实际进展：上一轮已修复右侧 `StatusPanel` 文件 hover diff 预览透明感过强的问题；代码复查确认 `src/components/toolBlocks/EditDiffPreview.tsx` 已给 `surface="status"` 输出 `edit-diff-hover-preview-solid`，`src/styles/toolBlocks.css` 已把 status 专用 hover preview 外层与内部 diff 行改为实底高对比。已知阻塞仍是真实 Tauri 桌面 hover 视觉点测和 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` / `FileChangesList.tsx` 提供 file changes popover 和显式 diff 按钮，`StatusPanel.less` 的 file changes list 有 200px 高度滚动区域；它没有本项目这种右栏文件树 hover diff 小预览。本项目已在交互上超出 cc-gui，但当前 status hover preview 仍被 `.edit-diff-hover-preview-status` 限制为 `min(300px, calc(100vw - 48px))`，实底后仍可能因为宽度过窄导致代码行、路径和 summary 被过度截断。
- 本轮诊断（超越 cc-gui）：右栏文件树 hover diff 是为了让用户不打开中央 diff pane 也能快速判断变更风险。它承载的是代码审阅，不是普通 tooltip，因此需要可读宽度。把 status hover preview 提升到 readable-width variant，可以解决“内容不透明了但代码还是太挤”的痛点，同时保留默认 transcript hover preview 的宽度和行为。
- 本轮规划：只给 `surface="status"` hover preview 增加 `edit-diff-hover-preview-readable` 类，并用 CSS 专用选择器把宽度提升到 420px、summary 可用宽度提升到 130px；不改浮层触发、定位算法、diff pane、默认 hover preview 或后端。优先级中高：用户价值中高（提升 hover 读代码效率）、出现频率高（文件 hover 常用）、实现成本低（类名 + CSS）、风险低（status 专用选择器隔离）。
- 本轮完成：`src/components/toolBlocks/EditDiffPreview.test.tsx` 扩展 status hover preview 回归，断言同时输出 `edit-diff-hover-preview-status`、`edit-diff-hover-preview-solid`、`edit-diff-hover-preview-readable` 和 `is-visible`。`src/components/toolBlocks/EditDiffPreview.tsx` 给 `surface="status"` 增加 `edit-diff-hover-preview-readable` 类。`src/styles/toolBlocks.css` 新增 `.status-edit-diff-trigger .edit-diff-hover-preview-readable`，将宽度和最大宽度提升到 `min(420px, calc(100vw - 48px))`，并把 hover summary 最大宽度从 86px 扩到 130px。`.trellis/spec/frontend/component-guidelines.md` 已同步：status-panel hover preview 需要比普通 tooltip 更宽的 readable-width variant。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx` 先失败 1 项，失败点为旧 HTML 缺少 `edit-diff-hover-preview-readable`。GREEN：同命令通过（1 file / 4 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx`（3 files / 34 tests）。全量验证通过：`npm test`（37 files / 339 tests，仅 Browserslist/caniuse-lite 过期提示）、`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）、IDE `build_project`（`problems: []`）。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查均不存在。
- 未验证项：未启动真实 Tauri 桌面做 hover 视觉点测；未在真实 320px 右栏 / 不同窗口宽度下截图确认 420px 是否是最佳值。当前验证覆盖组件契约、CSS diff 复查、前端全量测试、构建、Rust 和 IDE 编译。
- 下一步候选：1) 启动真实 Tauri 桌面做右侧 edit tree hover 点测，确认 420px 预览在 1280/1440/1920 宽度下不会越界且代码可读；2) 如果真实点测发现 420px 仍不够读长代码，下一轮考虑在 hover body 内允许横向滚动或把 unified preview 默认改为 wrap，而不是继续无上限扩宽；3) 做 cc-gui 与本项目真实运行并排截图审计，重点看 file changes 操作密度、右栏 edit tree、输入区 fallback、provider/model 图标和搜索反馈；4) 如果用户继续反馈文件变更查看成本高，优先优化 hover preview 与中央 diff pane 选中状态联动。

## 本轮 PLAN 2026-06-19 chat status edit hover preview long line wrapping

- 目标：延续用户反馈“聊天界面鼠标悬浮文件查看的悬浮框透明度太高，看不清楚内容”的修复链路，在已完成实底高对比和 readable-width 后，解决 status 文件 hover diff 中长代码行被 ellipsis 截断的问题。该 hover preview 是不可交互浮层，用户无法横向滚动，因此长行应在 status 专用预览里换行。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / 样式诊断。验证方式：读取 `EditDiffPreview.tsx`、`EditDiffPreview.test.tsx` 与 `toolBlocks.css`，确认 status hover preview 已有 `solid` / `readable` 类，但 `.edit-diff-hover-content` 仍默认 `overflow:hidden; text-overflow:ellipsis; white-space:pre`。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/toolBlocks/EditDiffPreview.test.tsx`，断言 status hover preview 必须输出 `edit-diff-hover-preview-wrap` 类；旧实现应因缺少该类失败。
- 功能点 4：实现最小展示层修复。验证方式：`EditDiffPreview` 仅在 `surface="status"` 的 hover preview 上增加 `edit-diff-hover-preview-wrap`；`src/styles/toolBlocks.css` 只在 status wrap preview 下把 diff content 改为 `pre-wrap` / `overflow-wrap:anywhere`，并让行内容顶端对齐；不改默认 transcript hover preview。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，明确 status-panel diff hover preview 的长代码行应换行而不是截断，因为该浮层不可滚动。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `EditDiffPreview` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-19 chat status edit hover preview long line wrapping

- 上轮实际进展：上一轮已把右侧 `StatusPanel` 文件 hover diff 预览从 300px 提升到 readable-width variant；代码复查确认 `src/components/toolBlocks/EditDiffPreview.tsx` 已给 `surface="status"` 输出 `edit-diff-hover-preview-solid` 与 `edit-diff-hover-preview-readable`，`src/styles/toolBlocks.css` 已把 status 专用 hover preview 改为实底高对比并提升到 420px。已知阻塞仍是真实 Tauri 桌面 hover 视觉点测和 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui `StatusPanel` / `FileChangesList` 提供 file changes popover 和显式 diff 操作，本项目用 hover diff 小预览在交互上已超出 cc-gui。但当前 status hover preview 虽然已经实底且更宽，内部 `.edit-diff-hover-content` 仍继承通用 hover 的 `overflow:hidden; text-overflow:ellipsis; white-space:pre`，长代码行会被截断。
- 本轮诊断（超越 cc-gui）：右栏 hover diff 的价值是“不打开中央 diff pane 也能快速判断变更风险”。因为该浮层是 `pointer-events: none` 的临时预览，用户无法横向滚动恢复被截断的内容；status 专用长行换行能解决“浮层清楚了但关键代码仍被省略”的痛点，同时不改变默认 transcript hover 的紧凑行为。
- 本轮规划：只给 `surface="status"` hover preview 增加 `edit-diff-hover-preview-wrap` 类，并用 status 专用 CSS 覆盖内容行换行；不改 `StatusPanel` 数据流、不改 hover 触发/定位、不改默认 transcript hover preview、不改中央 diff pane、不新增依赖。优先级中高：用户价值中高（直接提升 hover 读代码完整度）、出现频率高（文件 hover 高频触发）、实现成本低（类名 + CSS + SSR 断言）、风险低（status 专用选择器隔离）。
- 本轮完成：`src/components/toolBlocks/EditDiffPreview.test.tsx` 扩展 status hover preview 回归，断言同时输出 `edit-diff-hover-preview-status`、`edit-diff-hover-preview-solid`、`edit-diff-hover-preview-readable`、`edit-diff-hover-preview-wrap` 和 `is-visible`。`src/components/toolBlocks/EditDiffPreview.tsx` 仅在 `surface="status"` 的 hover preview 上增加 `edit-diff-hover-preview-wrap` 类。`src/styles/toolBlocks.css` 新增 `.status-edit-diff-trigger .edit-diff-hover-preview-wrap` 覆盖：content 行 `overflow: visible`、`text-overflow: clip`、`white-space: pre-wrap`、`overflow-wrap: anywhere`，并把 unified/split 行内容顶端对齐。`.trellis/spec/frontend/component-guidelines.md` 已同步不可交互 status hover preview 长行应换行而非 ellipsize 的契约。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx` 先失败 1 项，失败点为旧 HTML 缺少 `edit-diff-hover-preview-wrap`，符合预期。GREEN：同命令通过（1 file / 4 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx`（3 files / 34 tests）。全量前端验证通过：`npm test`（37 files / 339 tests，仅 Browserslist/caniuse-lite 过期提示）、`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查均不存在。
- 未验证项：未启动真实 Tauri 桌面做右侧文件 hover 视觉点测；未在真实 320px 右栏、不同窗口宽度、浅色/深色主题下截图确认换行后高度是否仍合适；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 启动真实 Tauri 桌面构造含长代码行、added/removed/context 的 edit history，点测 status hover preview 是否清晰、换行是否导致遮挡、浅色/深色主题是否都可读；2) 如果真实点测发现换行后 260px body 高度不足，下一轮只在 status hover preview 内微调 max-height 或增加垂直渐隐/更多行提示，不改默认 hover；3) 做 cc-gui 与本项目真实运行并排截图审计，重点看 file changes 操作密度、右栏 edit tree、输入区 fallback、provider/model 图标和搜索反馈；4) 若用户继续反馈文件变更查看成本高，优先优化 hover preview 与中央 diff pane 选中状态联动，而不是继续堆更多悬浮层。

## 本轮 PLAN 2026-06-19 chat status edit hover preview readable height

- 目标：延续 status 文件 hover diff 可读性修复链路。在已完成实底、高对比、readable-width 和长行换行后，解决换行内容更容易被 260px body 高度裁掉的问题。该 hover preview 是不可交互的快速审阅浮层，应给代码内容更多垂直阅读空间，但仍用视口上限避免遮挡整页。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `EditDiffPreview`、`StatusPanel` 与 `toolBlocks.css`；读取 cc-gui `StatusPanel` / `FileChangesList` / `.less`，确认 cc-gui 是 file changes popover + 显式 diff 操作，本项目 status hover diff 是超越项；本项目当前 status hover body 仍固定 `max-height: 260px`。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/toolBlocks/EditDiffPreview.test.tsx`，断言 status hover preview 必须输出 `edit-diff-hover-preview-tall` 类；旧实现应因缺少该类失败。
- 功能点 4：实现最小展示层修复。验证方式：`EditDiffPreview` 仅在 `surface="status"` 的 hover preview 上增加 `edit-diff-hover-preview-tall`；`src/styles/toolBlocks.css` 只在 status tall preview 下把 body 高度提升为视口感知上限，例如 `min(420px, calc(100vh - 96px))`；不改默认 transcript hover preview、不改触发/定位、不改数据流。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，明确 status-panel hover preview 在换行后需要 viewport-aware readable-height variant，避免内容过早被裁掉。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `EditDiffPreview` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat status edit hover preview readable height

- 上轮实际进展：上一轮已给右侧 `StatusPanel` 文件 hover diff 预览增加长行换行能力；代码复查确认 `src/components/toolBlocks/EditDiffPreview.tsx` 已给 `surface="status"` 输出 `edit-diff-hover-preview-wrap`，`src/styles/toolBlocks.css` 已在 status wrap preview 下把 diff content 改为 `pre-wrap` / `overflow-wrap:anywhere`。已知阻塞仍是真实 Tauri 桌面 hover 视觉点测和 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` / `FileChangesList.tsx` 提供 file changes popover 和显式 diff 操作，`StatusPanel.less` 对 file changes list 使用约 200px 高度滚动区域。本项目右栏 hover diff 是超越项，但在换行后仍保留 `.status-edit-diff-trigger .edit-diff-hover-preview-status .edit-diff-hover-body { max-height: 260px; overflow: hidden; }`，更容易在长行和多行 diff 下裁掉有用上下文。
- 本轮诊断（超越 cc-gui）：本项目的 hover diff 不是普通 tooltip，而是快速审阅小面板。换行解决了横向截断，但如果高度仍过低，用户会看到更完整的一两行，却丢失后续上下文和 “more lines” 线索。使用 viewport-aware readable-height variant 可以解决“内容不截断横向了，但纵向过早被裁掉”的痛点，同时避免无上限浮层遮住整页。
- 本轮规划：只给 `surface="status"` hover preview 增加 `edit-diff-hover-preview-tall` 类，并用 status 专用 CSS 把 body 高度提升为 `min(420px, calc(100vh - 96px))`；不改默认 transcript hover preview、不改 fixed 定位、不改触发逻辑、不改 `StatusPanel` 数据流、不新增依赖。优先级中高：用户价值中高（提升 hover diff 上下文完整度）、出现频率高（文件 hover 高频触发）、实现成本低（类名 + CSS + SSR 断言）、风险低（status 专用选择器隔离）。
- 本轮完成：`src/components/toolBlocks/EditDiffPreview.test.tsx` 扩展 status hover preview 回归，断言同时输出 `edit-diff-hover-preview-status`、`edit-diff-hover-preview-solid`、`edit-diff-hover-preview-readable`、`edit-diff-hover-preview-wrap`、`edit-diff-hover-preview-tall` 和 `is-visible`。`src/components/toolBlocks/EditDiffPreview.tsx` 仅在 `surface="status"` 的 hover preview 上增加 `edit-diff-hover-preview-tall` 类。`src/styles/toolBlocks.css` 新增 `.status-edit-diff-trigger .edit-diff-hover-preview-tall .edit-diff-hover-body { max-height: min(420px, calc(100vh - 96px)); }`。`.trellis/spec/frontend/component-guidelines.md` 已同步：status hover preview 在换行后需要 viewport-aware readable-height variant。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx` 先失败 1 项，失败点为旧 HTML 缺少 `edit-diff-hover-preview-tall`，符合预期。GREEN：同命令通过（1 file / 4 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx`（3 files / 34 tests）。全量前端验证通过：`npm test`（37 files / 339 tests，仅 Browserslist/caniuse-lite 过期提示）、`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查均不存在。
- 未验证项：未启动真实 Tauri 桌面做右侧文件 hover 视觉点测；未在真实 320px 右栏、不同窗口高度、浅色/深色主题下截图确认 420px / `100vh - 96px` 是否是最佳高度；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 启动真实 Tauri 桌面构造含长代码行、added/removed/context 的 edit history，点测 status hover preview 在不同窗口高度下是否清晰、不越界、不会遮挡关键聊天内容；2) 如果真实点测发现 `overflow: hidden` 仍会隐藏重要结尾，下一轮只给 status hover preview 增加非交互式底部渐隐与 “more lines” 可见性优化，不把 hover 改成可滚动交互浮层；3) 做 cc-gui 与本项目真实运行并排截图审计，重点看 file changes 操作密度、右栏 edit tree、输入区 fallback、provider/model 图标和搜索反馈；4) 若用户继续反馈文件变更查看成本高，优先优化 hover preview 与中央 diff pane 选中状态联动。

## 本轮 PLAN 2026-06-20 chat status edit hover preview hidden line clue

- 目标：延续 status 文件 hover diff 可读性修复链路。在已完成实底、高对比、readable-width、长行换行和 readable-height 后，解决 body 仍可能因 `overflow: hidden` 裁掉底部 “more lines” 提示的问题。该 hover preview 是非交互式浮层，用户不能滚动恢复被裁掉的提示，因此隐藏行数线索必须在 header 中可见。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `EditDiffPreview`、`toolBlocks.css` 与 cc-gui `FileChangesList` / `StatusPanel.less`，确认 cc-gui 是 file changes list + 显式 diff 操作，本项目 status hover diff 是超越项；本项目当前 hidden-line 文案只出现在 body 底部。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/toolBlocks/EditDiffPreview.test.tsx`，构造超过默认 16 行的 status hover preview，断言 header 中存在 `edit-diff-hover-hidden-summary` 且包含 `tools.moreDiffLines` 文案；旧实现应因缺少该 class 失败。
- 功能点 4：实现最小展示层修复。验证方式：`EditDiffPreview` 仅在 `surface="status"` 且 `hiddenLineCount > 0` 时，在 header stats 区额外渲染同一隐藏行数提示；保留 body 底部原提示用于通用 hover / panel 语义，不改变 lineLimit、定位、触发和数据流。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，明确 status-panel hover preview 的 hidden-line/truncation clue 必须在 header 可见，因为 body/footer 可能被高度裁剪。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `EditDiffPreview` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat status edit hover preview hidden line clue

- 上轮实际进展：上一轮已把右侧 `StatusPanel` 文件 hover diff 预览 body 高度提升为 viewport-aware readable-height；代码复查确认 `src/components/toolBlocks/EditDiffPreview.tsx` 已给 `surface="status"` 输出 `edit-diff-hover-preview-tall`，`src/styles/toolBlocks.css` 已有 `.status-edit-diff-trigger .edit-diff-hover-preview-tall .edit-diff-hover-body { max-height: min(420px, calc(100vh - 96px)); }`。已知阻塞仍是真实 Tauri 桌面 hover 视觉点测和 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui `FileChangesList` 提供 file changes popover、200px 内部滚动列表和显式 diff 按钮；它没有本项目这种右栏文件树 hover diff 快速预览。本项目已超出 cc-gui，但当前 `EditDiffPreview` 只在 body 底部渲染 `edit-diff-hover-more`，而 status hover body 仍然 `overflow: hidden`，在长行换行和高度上限下，用户可能看不到“还有更多行”的提示。
- 本轮诊断（超越 cc-gui）：右栏 hover diff 的价值是不用打开中央 diff pane 也能快速判断变更风险。因为该浮层是非交互式临时预览，不能指望用户滚动到底部发现隐藏内容。把隐藏行数提示同步放到 header stats 区，可以解决“预览看起来像完整 diff，但其实被 lineLimit/高度裁掉”的痛点，并且不增加新的交互复杂度。
- 本轮规划：只在 `surface="status"` 且 `hiddenLineCount > 0` 时向 header stats 区增加 `edit-diff-hover-hidden-summary`；保留 body 底部原提示，避免影响普通 hover / panel 语义。不改 lineLimit、定位、触发、`StatusPanel` 数据流、中央 diff pane 或后端。优先级中高：用户价值中高（避免误判 diff 完整性）、出现频率高（文件 hover 高频）、实现成本低（渲染条件 + CSS + SSR 回归）、风险低（status 专用样式隔离）。
- 本轮完成：`src/components/toolBlocks/EditDiffPreview.test.tsx` 新增超过 16 行的 status hover preview 回归，先 RED 确认旧实现缺少 `edit-diff-hover-hidden-summary`，后 GREEN 断言 header 提示和 `3 more lines` 可见。`src/components/toolBlocks/EditDiffPreview.tsx` 抽出 `moreLinesLabel`，给 `tools.moreDiffLines` 增加测试/SSR fallback，并在 status header stats 区渲染隐藏行数提示，同时复用该文案给 body 底部提示。`src/styles/toolBlocks.css` 新增 `.status-edit-diff-trigger .edit-diff-hover-hidden-summary`，用紧凑 amber pill 在深色实底 preview 上强调“还有更多行”。`.trellis/spec/frontend/component-guidelines.md` 已同步：status hover preview 的 hidden-line/truncation clue 必须在 header 可见，因为 body/footer 可能被裁剪。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx` 先失败 1 项，失败点为旧 HTML 缺少 `edit-diff-hover-hidden-summary`，符合预期。GREEN：同命令通过（1 file / 5 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx`（3 files / 35 tests）。全量前端验证通过：`npm test`（37 files / 340 tests，仅 Browserslist/caniuse-lite 过期提示）、`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出既有 LF/CRLF 转换提示。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查均不存在。
- 未验证项：未启动真实 Tauri 桌面做右侧文件 hover 视觉点测；未在真实 320px 右栏、不同窗口高度、浅色/深色主题下截图确认 header amber pill 是否刚好醒目且不挤压路径/统计；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 启动真实 Tauri 桌面构造含长代码行、added/removed/context 且超过 16 行的 edit history，点测 status hover preview 的 header “more lines” 是否持续可见、是否被路径/统计挤压、不同窗口高度下是否不越界；2) 如果真实点测发现 header stats 过挤，下一轮只微调隐藏行数 pill 的文案密度或把 summary 与 counts 横向/纵向排布重新平衡，不改数据流；3) 做 cc-gui 与本项目真实运行并排截图审计，重点看 file changes 操作密度、右栏 edit tree、输入区 fallback、provider/model 图标和搜索反馈；4) 若用户继续反馈文件变更查看成本高，优先优化 hover preview 与中央 diff pane 选中状态联动。

## 本轮 PLAN 2026-06-20 chat status edit hover preview line budget

- 目标：延续 status 文件 hover diff 可读性链路。在已完成 readable-height 与 header hidden-line clue 后，解决 status hover 仍沿用普通 tooltip 默认 16 行预览的问题。现在 status body 可容纳更多内容，但 `lineLimit` 默认仍过早截断，短行 diff 会浪费可读高度。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `EditDiffPreview` 和测试，确认当前 `effectiveLineLimit` 对所有 hover 统一使用 16 行，未区分 status readable-height surface。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/toolBlocks/EditDiffPreview.test.tsx`，断言 status hover 默认渲染第 24 行、隐藏第 25 行，并显示 `1 more lines`；同时断言普通 hover 仍不渲染第 17 行且显示 `9 more lines`，防止全局放宽。
- 功能点 4：实现最小展示层修复。验证方式：`EditDiffPreview` 只在 `variant="hover"` 且 `surface="status"` 时使用 24 行默认上限；显式 `lineLimit` 和 panel 模式保持现有行为。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 status hover preview 的 line budget 应匹配 readable-height，普通 tooltip 仍保持紧凑。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `EditDiffPreview` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat status edit hover preview line budget

- 上轮实际进展：上一轮已把右侧 `StatusPanel` 文件 hover diff 预览的隐藏行数提示同步到 header；代码复查确认 `src/components/toolBlocks/EditDiffPreview.tsx` 已在 `surface="status"` 且 `hiddenLineCount > 0` 时渲染 `edit-diff-hover-hidden-summary`，`src/styles/toolBlocks.css` 已有对应 amber pill 样式。已知阻塞仍是真实 Tauri 桌面 hover 视觉点测和 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui `StatusPanel` / `FileChangesList` 提供 file changes popover、滚动列表和显式 diff 操作；它没有本项目右栏文件树 hover diff 快速预览。本项目已超出 cc-gui，但 status hover preview 在已有 `edit-diff-hover-preview-tall` 后仍沿用普通 hover 的 16 行默认预算，短行 diff 会过早截断，浪费已经提升的可读高度。
- 本轮诊断（超越 cc-gui）：右栏 hover diff 的价值是快速判断变更风险。状态面板预览已经采用实底、更宽、换行、更高和 header hidden-line clue，因此默认行数也应与这个快速审阅面板匹配；否则用户看到的是一个更大的窗口但内容仍按小 tooltip 被截断。把 status 默认预算提升到 24 行，可以解决“预览空间变大但上下文仍不足”的痛点，同时普通 transcript hover 保持紧凑。
- 本轮规划：只调整 `EditDiffPreview` 的默认 hover line budget：`surface="status"` 默认为 24 行，普通 hover 默认为 16 行；显式 `lineLimit` 和 `variant="panel"` 保持既有语义。不改 `StatusPanel` 数据流、不改浮层定位/触发、不改中央 diff pane、不新增依赖。优先级中高：用户价值中高（提升短行 diff 上下文完整度）、出现频率高（文件 hover 高频）、实现成本低（默认值 + SSR 回归）、风险低（测试同时锁定默认 hover 不被放宽）。
- 本轮完成：`src/components/toolBlocks/EditDiffPreview.test.tsx` 新增 `uses a larger default line budget for status hover previews only` 回归，构造 25 行 diff，断言 status hover 渲染第 24 行、不渲染第 25 行并显示 `1 more lines`；同时断言普通 hover 仍只到第 16 行并显示 `9 more lines`。既有 hidden-line clue 回归改为 27 行，匹配 status 24 行预算后仍能覆盖隐藏提示。`src/components/toolBlocks/EditDiffPreview.tsx` 新增 `defaultHoverLineLimit = surface === 'status' ? 24 : 16`，并让 `effectiveLineLimit` 继续尊重显式 `lineLimit` 与 panel 不限行语义。`.trellis/spec/frontend/component-guidelines.md` 已同步：status hover preview 的默认 line budget 应匹配 readable-height，普通 tooltip 保持紧凑。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx` 先失败，失败点为 status hover 仍只渲染到第 16 行并显示 `9 more lines`，符合预期。GREEN：同命令通过（1 file / 6 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx`（3 files / 36 tests）。全量前端验证通过：`npm test`（37 files / 341 tests，仅 Browserslist/caniuse-lite 过期提示）、`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored；期间等待 package/build lock 后通过）。IDE `build_project` 通过，`problems: []`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出 Windows LF/CRLF 转换提示。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查结果均为不存在。
- 未验证项：未启动真实 Tauri 桌面做右侧文件 hover 视觉点测；未在真实 320px 右栏、20-30 行短 diff、长行 diff、不同窗口高度和浅色/深色主题下截图确认 24 行预算是否刚好；未与 cc-gui 做真实运行并排截图。本轮 cc-gui 结论基于已读取的 `C:/guodevelop/demo/jetbrains-cc-gui` 源码，不是原始 `C:/guodevelop/jetbrains-cc-gui` 路径。
- 下一步候选：1) 启动真实 Tauri 桌面构造 20-30 行 diff、长代码行、added/removed/context 混合 edit history，点测 status hover preview 是否清晰、不越界、header stats 不拥挤；2) 如果真实点测发现 24 行仍过多或过少，只微调 status 专用预算或 header stats 密度，不改普通 hover / panel 语义；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、右栏 edit tree、hover preview 可读性、输入区 fallback、provider/model 图标和搜索反馈；4) 若用户继续反馈文件变更查看成本高，优先优化 hover preview 与中央 diff pane 选中状态联动。

## 本轮 PLAN 2026-06-20 chat status edit full diff action semantics

- 目标：补齐右侧 `StatusPanel` edit tree 文件行与中央 diff pane 的动作语义。代码确认文件行已通过 `onSelectedEditChange` 选中并展开中央 diff pane，也已有 selected 样式，但行本身只有 `role="button"` 和文件路径 `title`，没有明确告诉键盘/辅助技术用户“点击会查看完整 diff”。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `StatusPanel.tsx`、`ChatPage.tsx`、`ChatDiffReviewPane.tsx` 与 cc-gui `FileChangesList.tsx`，确认本项目已有 central diff selection 主干，cc-gui 有显式 diff 按钮，本项目缺少 edit row 的明确 action label。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，断言 edit tree 文件行输出 `aria-label="Inspect full diff: <file>"` / `查看完整差异：<file>`；旧实现应因缺少该 label 失败。
- 功能点 4：实现最小交互语义改动。验证方式：`StatusPanel` 为文件行新增 i18n 后的 `aria-label` 和同语义 `title`，不改选中数据流、不改 hover preview、不改中央 diff pane、不改样式或后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 status edit tree 文件行是完整 diff selection target，必须用 accessible label 明确该动作。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `StatusPanel` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat status edit full diff action semantics

- 上轮实际进展：上一轮已把右侧 `StatusPanel` 文件 hover diff 预览默认行数提升到 status 专用 24 行；代码复查确认 `src/components/toolBlocks/EditDiffPreview.tsx` 已有 `defaultHoverLineLimit = surface === 'status' ? 24 : 16`，普通 hover 仍保持 16 行，panel 仍不默认限行。已知阻塞仍是真实 Tauri 桌面 hover/点击视觉点测和 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui `FileChangesList.tsx` 把文件名打开文件、diff 按钮展示差异拆成明确操作入口。本项目右侧 edit tree 已通过 `StatusPanel.handleSelectEditedFile()` -> `ChatPage.handleSelectedEditChange()` 选中中央 diff pane，另有独立 open-file 图标按钮，但文件行本身只有 `role="button"`、`title={edit.displayPath}` 和 selected 样式，没有明确的“查看完整差异”动作语义。
- 本轮诊断（超越 cc-gui）：本项目的右栏文件树同时承担 hover 快速预览和中央 diff 选择入口。用户在键盘/辅助技术下需要知道文件行不是单纯文件路径，也不是打开文件，而是会把中央 diff pane 切到该文件。给文件行加明确的 i18n action label 和 title，可以解决“能点但不知道点了会打开哪里”的痛点，同时保留 open-file 图标按钮的独立语义。
- 本轮规划：只补 `StatusPanel` edit tree 文件行的 `aria-label` / `title` 和英中文 i18n 文案；不改 `onSelectedEditChange` 数据流、不改 hover preview、不改中央 diff pane、不改样式、不改后端。优先级中等：用户价值中等（提升可发现性和辅助技术可用性）、出现频率高（每个 edit 文件行都会遇到）、实现成本低（属性 + i18n + SSR 回归）、风险低（不改变行为路径）。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 扩展 selected edit row 回归，先 RED 确认旧 HTML 缺少 `aria-label="Inspect full diff: ..."` / `查看完整差异：...`，后 GREEN 锁定文件行动作语义。`src/components/chat/StatusPanel.tsx` 为 edit tree 文件行新增 `inspectDiffLabel = t('chat.layout.inspectFullDiff', {file: edit.displayPath})`，并同时用于 `aria-label` 与 `title`。`src/locales/en.json` / `src/locales/zh.json` 新增 `chat.layout.inspectFullDiff`。`.trellis/spec/frontend/component-guidelines.md` 已同步：选择中央 diff pane 的 status edit tree 文件行必须用 accessible label 明确“查看完整 diff”动作。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为旧 HTML 缺少 `aria-label="Inspect full diff: src/components/chat/StatusPanel.tsx"`，符合预期。GREEN：同命令通过（1 file / 24 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx`（4 files / 54 tests）。全量前端验证通过：`npm test`（37 files / 341 tests，仅 Browserslist/caniuse-lite 过期提示）、`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出 Windows LF/CRLF 转换提示。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查结果均为不存在。
- 未验证项：未启动真实 Tauri 桌面点测右侧文件行 hover title、键盘 Enter/Space 选中、中央 diff pane 展开后的焦点/可见性；未与 cc-gui 做真实运行并排截图。本轮 cc-gui 结论基于已读取的 `C:/guodevelop/demo/jetbrains-cc-gui` 源码，不是原始 `C:/guodevelop/jetbrains-cc-gui` 路径。
- 下一步候选：1) 启动真实 Tauri 桌面做右侧 edit tree 点测：鼠标 hover 文件行应显示“查看完整差异”title，Enter/Space 应选择中央 diff pane，open-file 图标仍只打开文件；2) 如果真实点测发现选择中央 diff 后焦点仍留在右栏且用户看不到 diff pane，可考虑在选择后把焦点转移到中央 diff pane header，但需要谨慎避免打断键盘浏览；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、右栏 edit tree、hover preview 可读性、输入区 fallback、provider/model 图标和搜索反馈；4) 若文件变更查看成本仍高，下一轮优先验证 hover preview 与中央 diff pane 的选中/焦点反馈，而不是继续扩展 hover 内容。

## 本轮 PLAN 2026-06-20 chat diff pane focus handoff

- 目标：补齐上一轮“查看完整差异”动作后的焦点闭环。右侧 edit tree 文件行已经有明确 action label，也会展开中央 diff pane，但键盘用户按 Enter/Space 后焦点仍停在右栏文件行；如果没有焦点交接，用户无法立即知道中央 diff pane 已更新，也不能直接操作 diff pane 的 wrap/copy/open/collapse 控件。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `ChatDiffReviewPane.tsx`、`ChatPage.tsx` 和测试，确认中央 diff pane 当前是 `section aria-label` 但不可程序聚焦，`handleSelectedEditChange()` 只设置 selected key 和展开状态。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ChatDiffReviewPane.test.tsx`，断言 diff pane section 输出 `data-chat-diff-review-pane="true"` 与 `tabIndex={-1}`；旧实现应因缺少这些属性失败。
- 功能点 4：实现最小焦点交接。验证方式：`ChatDiffReviewPane` 支持 forwarded ref 并把 section 设为 programmatic focus target；`ChatPage.handleSelectedEditChange()` 在桌面 `xl` 断点下、下一帧聚焦该 pane，避免小屏输入区 fallback 选中 edit 时聚焦隐藏 pane。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录从 status edit tree 选择文件后，桌面端应把焦点交给中央 diff pane；小屏不应聚焦隐藏 pane。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `ChatDiffReviewPane` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat diff pane focus handoff

- 上轮实际进展：上一轮已给右侧 `StatusPanel` edit tree 文件行补齐“查看完整差异”动作语义；代码复查确认 `src/components/chat/StatusPanel.tsx` 已把 `chat.layout.inspectFullDiff` 同时用于文件行 `aria-label` 和 `title`，`src/locales/en.json` / `zh.json` 已有对应文案。已知阻塞仍是真实 Tauri 桌面点测右侧文件行 hover title、Enter/Space 选择后中央 diff pane 焦点/可见性，以及 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui `FileChangesList.tsx` 用显式 diff 操作入口降低动作歧义；本项目右侧文件行已具备更丰富的 hover preview + central diff pane 选择，但 `ChatDiffReviewPane` 根节点只有 `section aria-label`，不可程序聚焦，`ChatPage.handleSelectedEditChange()` 也只更新 selected key 和展开状态。键盘用户触发“查看完整差异”后焦点仍留在右栏，不能自然落到新打开/更新的 diff 审阅区域。
- 本轮诊断（超越 cc-gui）：本项目中央 diff pane 有 wrap、copy path、open file、collapse 等审阅控制。桌面端选择文件后主动把焦点交给 pane，可以解决“按了 Enter/Space 但不知道主审阅区是否更新、还要重新 Tab 很久才能操作 diff 控件”的痛点；小屏 composer fallback 下不聚焦隐藏 pane，避免焦点丢到不可见区域。
- 本轮规划：只补焦点 handoff 链路：`ChatDiffReviewPane` 根节点成为 named programmatic focus target，`ChatPage` 在桌面 `xl` breakpoint 下一帧聚焦它；不改选中数据流、不改 hover preview、不改 diff 内容、不改后端、不新增依赖。优先级中高：用户价值中高（键盘/辅助技术闭环）、出现频率中高（每次从右栏选择文件审阅 diff）、实现成本低（ref + DOM 属性 + guarded focus）、风险低（仅桌面可见 pane 聚焦，小屏显式跳过）。
- 本轮完成：`src/components/chat/ChatDiffReviewPane.test.tsx` 保留并跑通 RED/GREEN 回归，锁定 pane 根节点必须输出 `data-chat-diff-review-pane="true"` 和 `tabIndex={-1}`。`src/components/chat/ChatDiffReviewPane.tsx` 改为 `forwardRef<HTMLElement, ChatDiffReviewPaneProps>`，根 `<section>` 增加稳定 data selector 与程序焦点能力，默认导出保持不变。`src/pages/ChatPage.tsx` 新增 `diffReviewPaneRef`，`handleSelectedEditChange()` 在设置 selected key、展开 pane 后，若 `window.matchMedia('(min-width: 1280px)')` 命中则下一帧 `focus({preventScroll: true})`，并保护 `matchMedia` 不存在的边界。`.trellis/spec/frontend/component-guidelines.md` 已同步桌面焦点交接、小屏不聚焦隐藏 pane、central diff pane root 作为 programmatic focus target 的规范。
- 本轮验证：RED：`npm test -- src/components/chat/ChatDiffReviewPane.test.tsx` 先失败 1 项，失败点为旧 HTML 缺少 `data-chat-diff-review-pane="true"`，符合预期。GREEN：同命令通过（1 file / 6 tests）。相邻验证通过：`npm test -- src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx`（4 files / 54 tests）。前端构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。全量前端验证通过：`npm test`（37 files / 341 tests，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出 Windows LF/CRLF 转换提示。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查结果均为不存在。
- 未验证项：未启动真实 Tauri 桌面做右侧 edit tree 文件行 Enter/Space 后焦点是否落到中央 diff pane 的实机点测；未验证屏幕阅读器宣布 pane 名称与后续控件顺序；未与 cc-gui 做真实运行并排截图。本轮交互判断基于真实源码、SSR 回归、构建和单元测试，实际焦点体验仍建议用桌面端点测确认。
- 下一步候选：1) 启动真实 Tauri 桌面，构造含文件变更的会话，点测右侧文件行鼠标 hover title、Enter/Space 选择、中央 diff pane 焦点 ring/控件 Tab 顺序、小屏 composer fallback 不聚焦隐藏 pane；2) 如果实机发现焦点转移后视觉提示不够明显，下一轮只增强 `.chat-diff-review-pane:focus-visible`，不改数据流；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、右栏 edit tree、hover preview 可读性、central diff pane focus handoff、输入区 fallback、provider/model 图标和搜索反馈；4) 若文件变更查看成本仍高，优先把 hover preview 与中央 diff pane 的选中状态反馈做更明确，而不是继续扩展浮层内容。

## 本轮 PLAN 2026-06-20 chat diff pane focus visible feedback

- 目标：延续上一轮 `chat diff pane focus handoff`。中央 diff pane 已能被程序聚焦，但样式复查确认 `.chat-diff-review-pane` 本体没有 `:focus-visible` 反馈；键盘用户可能已经被转移到 pane，却看不出当前焦点落点。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / 样式诊断。验证方式：读取 `ChatDiffReviewPane.tsx`、`ChatPage.tsx` 和 `toolBlocks.css`，确认 pane 已有 `tabIndex={-1}` / stable data selector / ref handoff，但缺少 `.chat-diff-review-pane:focus-visible` 样式。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ChatDiffReviewPane.test.tsx`，读取 `src/styles/toolBlocks.css` 并断言包含 `.chat-diff-review-pane:focus-visible`、`outline` 与 `box-shadow`；旧样式应因缺少该 selector 失败。
- 功能点 4：实现最小展示层修复。验证方式：只在 `src/styles/toolBlocks.css` 增加 diff pane focus-visible 样式，使用主色 outline + 内外阴影，让 programmatic focus handoff 有可见边界；不改焦点触发、不改 pane 结构、不改右栏或后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 programmatic focus target 也必须有可见 focus feedback，尤其是从右栏跳到中央 diff pane 的场景。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `ChatDiffReviewPane` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat diff pane focus visible feedback

- 上轮实际进展：上一轮已补齐右侧 edit tree 文件行到中央 diff pane 的焦点交接；代码复查确认 `src/components/chat/ChatDiffReviewPane.tsx` 根节点已有 `data-chat-diff-review-pane="true"`、`tabIndex={-1}` 和 forwarded ref，`src/pages/ChatPage.tsx` 已在桌面 `xl` 断点下一帧聚焦该 pane。已知阻塞仍是真实 Tauri 桌面点测焦点落点、focus ring 视觉强度和 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 用明确 diff 操作入口减少 file changes 动作歧义；本项目已经进一步实现右栏选择后打开/更新中央 diff pane，但 `toolBlocks.css` 中 `.chat-diff-review-pane` 只有布局与背景样式，没有本体 `:focus-visible` 反馈。键盘用户被程序聚焦到 pane 后，视觉上可能仍像停留在右栏或没有任何变化。
- 本轮诊断（超越 cc-gui）：本项目中央 diff pane 承载完整审阅控件，焦点交接只有在“看得见落点”时才完整。给 pane 本体增加可见 outline/shadow，可以解决“焦点已经转移但用户看不见在哪里”的痛点，同时保持 pane 不进入正常 Tab 顺序，只服务于 status edit tree 的 handoff。
- 本轮规划：只补中央 diff pane 本体的 focus-visible 视觉反馈：给 pane 增加语义 class `chat-diff-review-pane-focus-target`，CSS 对基础 pane 和 focus-target class 增加主色 outline + 内外阴影；不改焦点触发、不改 pane 布局、不改右栏、不改 hover preview、不改后端。优先级中等偏高：用户价值中高（键盘可见性闭环）、出现频率中（从右栏选择 diff 文件时）、实现成本低（class + CSS）、风险低（仅 focus-visible 状态生效）。
- 本轮完成：`src/components/chat/ChatDiffReviewPane.test.tsx` 扩展首个 SSR 回归，断言 pane 根节点同时输出 `chat-diff-review-pane-focus-target`、`data-chat-diff-review-pane="true"` 和 `tabIndex={-1}`。`src/components/chat/ChatDiffReviewPane.tsx` 为 root section 增加 `chat-diff-review-pane-focus-target` class，保持 ref、selector 和 props 不变。`src/styles/toolBlocks.css` 增加 `.chat-diff-review-pane:focus-visible, .chat-diff-review-pane-focus-target:focus-visible` 规则，使用 `outline`、`outline-offset` 和 `box-shadow` 提供不改变布局的可见焦点边界。`.trellis/spec/frontend/component-guidelines.md` 已同步：programmatic focus target 也必须有可见 focus feedback，尤其是从右栏交接到中央 diff pane 的场景。
- 本轮验证：RED：`npm test -- src/components/chat/ChatDiffReviewPane.test.tsx` 先失败，失败点为 CSS 中缺少 `.chat-diff-review-pane:focus-visible`；随后发现 `node:fs` / CSS raw 读取测试不适配当前项目 TS/Vitest 配置并改为 SSR class 契约。GREEN：`npm test -- src/components/chat/ChatDiffReviewPane.test.tsx` 通过（1 file / 6 tests）。相邻验证通过：`npm test -- src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx`（4 files / 54 tests）。前端构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。全量前端验证通过：`npm test`（37 files / 341 tests，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出 Windows LF/CRLF 转换提示。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查结果均为不存在。
- 未验证项：未启动真实 Tauri 桌面点测 focus ring 在浅色/深色主题和不同宽度下的视觉强度；未验证屏幕阅读器在焦点进入 pane 时的宣布顺序；未与 cc-gui 做真实运行并排截图。本轮视觉判断基于真实 CSS 规则、SSR 回归和构建测试，实际颜色/阴影观感仍需桌面截图确认。
- 下一步候选：1) 启动真实 Tauri 桌面点测右侧 edit tree 文件行 Enter/Space 后中央 pane focus ring 是否足够明显、不会被 header/边框吞掉、Tab 顺序是否直接进入 wrap/copy/open/collapse 控件；2) 如果实机发现 outline 太强或太弱，下一轮只微调 `outline` alpha / `box-shadow` 强度，不改 handoff 数据流；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、右栏 edit tree、hover preview 可读性、central diff pane focus feedback、输入区 fallback、provider/model 图标和搜索反馈；4) 若文件变更查看成本仍高，优先把 hover preview 与中央 diff pane 的 selected 状态反馈做得更连续。

## 本轮 PLAN 2026-06-20 chat diff pane selected file accessible name

- 目标：延续中央 diff pane 焦点闭环。当前 pane 已能被程序聚焦且有可见 focus ring，但 `aria-label` 仍是通用“文件差异”；屏幕阅读器用户在焦点交接后听不到当前选中的文件，必须继续探索 header 内容才知道落到哪个 diff。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / i18n 诊断。验证方式：读取 `ChatDiffReviewPane.tsx`、`ChatDiffReviewPane.test.tsx` 和英中文 locale，确认 pane root 当前只使用 `chat.layout.diffPanel`，而右栏文件行已有 `inspectFullDiff: {{file}}` 语义。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ChatDiffReviewPane.test.tsx`，断言有选中 edit 时 root `aria-label` 包含当前文件路径；旧实现应只输出通用“文件差异”并失败。
- 功能点 4：实现最小可访问性修复。验证方式：新增 `chat.layout.diffPanelForFile` 英中文文案；`ChatDiffReviewPane` 在 `edit` 存在时用该文案作为 root `aria-label`，无 edit 时保持通用 label。不改视觉布局、不改焦点触发、不改 diff 数据流。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 central diff pane 的 programmatic focus label 必须包含当前文件，避免焦点交接后只宣布通用区域名。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `ChatDiffReviewPane` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat diff pane selected file accessible name

- 上轮实际进展：上一轮已补齐中央 diff pane 的焦点交接和可见 focus ring；代码复查确认 `src/components/chat/ChatDiffReviewPane.tsx` 根节点已有 `data-chat-diff-review-pane="true"`、`tabIndex={-1}`、`chat-diff-review-pane-focus-target` 和 forwarded ref，`src/pages/ChatPage.tsx` 已在桌面 `xl` 断点下一帧聚焦该 pane，`src/styles/toolBlocks.css` 已有 `.chat-diff-review-pane:focus-visible` 反馈。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器宣布顺序和 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui `FileChangesList.tsx` 通过显式 file changes / diff 操作让用户知道当前操作对象；本项目已经有更完整的中央 diff pane，但在焦点交接后如果 root 仍只宣布通用“文件差异”，辅助技术用户必须继续探索 header 才知道当前选中的文件。该问题不影响视觉用户直接看 header，但会削弱上一轮焦点 handoff 的可访问性闭环。
- 本轮诊断（超越 cc-gui）：本项目中央 diff pane 承载完整审阅控件和程序焦点落点。把选中文件路径写入 pane 的 accessible name，可以解决“焦点已经进入 diff pane，但不知道是哪一个文件 diff”的痛点，让右栏文件行、中央 pane 和屏幕阅读器宣布内容形成连续语义。
- 本轮规划：只补中央 diff pane root `aria-label` 的文件语义：有 `edit` 时使用 `chat.layout.diffPanelForFile`，无 `edit` 时继续使用通用 `chat.layout.diffPanel`；同步英中文 i18n 和组件规范。不改视觉布局、不改焦点触发、不改 diff 数据流、不改 hover preview、不改后端。优先级中高：用户价值中高（辅助技术和键盘用户的上下文确认）、出现频率中高（每次从右栏选择文件审阅 diff）、实现成本低（i18n + 一处 label + SSR 断言）、风险低（无行为数据流变化）。
- 本轮完成：`src/components/chat/ChatDiffReviewPane.test.tsx` 扩展首个 SSR 回归，断言选中 `src/components/chat/ChatDiffReviewPane.tsx` 时 root 输出 `aria-label="File diff: src/components/chat/ChatDiffReviewPane.tsx"` / `文件差异：src/components/chat/ChatDiffReviewPane.tsx`，同时继续锁定 full diff、focus target、data selector 和 `tabIndex`。`src/components/chat/ChatDiffReviewPane.tsx` 新增 `diffPanelLabel`，在有 `edit` 时调用 `t('chat.layout.diffPanelForFile', {file: edit.displayPath})`，否则保持 `t('chat.layout.diffPanel')`。`src/locales/en.json` / `src/locales/zh.json` 新增 `chat.layout.diffPanelForFile`。`.trellis/spec/frontend/component-guidelines.md` 已同步：central diff pane 的 programmatic focus label 必须包含当前文件路径，避免只宣布通用区域名。
- 本轮验证：RED：`npm test -- src/components/chat/ChatDiffReviewPane.test.tsx` 先失败 1 项，失败点为旧 HTML 只输出通用 `aria-label="文件差异"`，符合预期。GREEN：同命令通过（1 file / 6 tests）。相邻验证通过：`npm test -- src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx`（4 files / 54 tests）。前端构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。全量前端验证通过：`npm test`（37 files / 341 tests，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。补充收尾验证：`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查结果均为不存在。
- 未验证项：未启动真实 Tauri 桌面点测右侧 edit tree 文件行 Enter/Space 后焦点落点、focus ring 视觉强度、Tab 顺序和屏幕阅读器宣布文案；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、SSR 回归、构建和单元/后端测试。
- 下一步候选：1) 启动真实 Tauri 桌面，构造含文件变更的会话，点测右侧文件行 hover title、Enter/Space 选择、中央 diff pane 焦点 ring、屏幕阅读器/Accessibility tree 中的 `File diff: <path>` 宣布、wrap/copy/open/collapse 控件 Tab 顺序；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、右栏 edit tree、hover preview 可读性、central diff pane focus feedback、输入区 fallback、provider/model 图标和搜索反馈；3) 如果实机发现焦点语义正确但视觉连续性仍弱，下一轮优先在中央 pane header 增加更明确的 selected-file 状态反馈，而不是继续增加 hover 内容；4) 如果 focus ring 在浅色/深色主题下过强或过弱，只微调 `outline` alpha / `box-shadow` 强度，不改 handoff 数据流。

## 本轮 PLAN 2026-06-20 diff pane reopen file-specific label

- 目标：延续中央 diff pane 的可访问性闭环。当前 pane 本体已能宣布选中文件，但当用户收起 pane 后，右栏 header 恢复按钮和页面级浮动恢复按钮仍只说“展开文件差异面板”，没有说明会恢复哪个文件的 diff。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范、`package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `ChatPage.tsx`、`StatusPanel.tsx`、`StatusPanel.test.tsx`、`chatUiBehavior.ts`，确认全局浮动恢复按钮和右栏 header 恢复按钮都使用通用 `chat.layout.expandDiffPanel`。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，断言 diff pane collapsed 且存在选中文件时，header 恢复按钮输出 `Expand file diff panel: <file>` / `展开文件差异面板：<file>`；旧实现应因只输出通用 label 失败。
- 功能点 4：实现最小可访问性修复。验证方式：新增 `chat.layout.expandDiffPanelForFile` 英中文文案；`StatusPanel` 在能确定恢复目标文件时用文件级 title/aria-label，否则保留通用 label；`ChatPage` 的页面级浮动恢复按钮对 `selectedEdit` 使用同一文件级 label。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 diff pane 恢复入口在目标文件可知时必须包含文件路径。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `StatusPanel` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、必要时 `cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 diff pane reopen file-specific label

- 上轮实际进展：上一轮已让中央 diff pane 在选中文件时用 `File diff: <path>` / `文件差异：<path>` 作为 root accessible name；代码复查确认 `ChatDiffReviewPane`、英中文 locale 与规范均已落地。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器宣布顺序、focus ring 主题观感和 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 file changes 操作入口强调“对哪个文件做 diff 操作”。本项目中央 pane 收起后，右栏 header 恢复按钮与页面级浮动恢复按钮都只使用通用 `chat.layout.expandDiffPanel`，在多文件编辑场景下不能告诉用户会恢复哪个文件的完整 diff。
- 本轮诊断（超越 cc-gui）：本项目支持收起中央 diff pane 以释放 transcript 空间，也提供右栏和页面级恢复入口。恢复入口如果能带上目标文件路径，可以解决“面板收起后不知道点开会回到哪个 diff”的痛点，尤其适合键盘/屏幕阅读器用户和多文件长会话。
- 本轮规划：只给 diff pane 恢复入口补文件级 title/aria-label：新增 `chat.layout.expandDiffPanelForFile`；`StatusPanel` 能从 `selectedEditKey` 或 `allEdits[0]` 确定恢复目标时使用文件级 label，否则保留通用 label；`ChatPage` 页面级浮动恢复按钮使用 `selectedEdit` 的同一文件级 label。不改按钮视觉、不改点击行为、不改 pane 展开/收起状态机、不改后端。优先级中等偏高：用户价值中高（恢复目标明确）、出现频率中（收起/恢复 diff pane）、实现成本低、风险低。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 扩展 collapsed diff pane 恢复按钮回归，先 RED 确认旧 HTML 只有 `aria-label="展开文件差异面板"`，后 GREEN 锁定 `aria-label="Expand file diff panel: src/components/chat/StatusPanel.tsx"` / `展开文件差异面板：src/components/chat/StatusPanel.tsx`。`src/components/chat/StatusPanel.tsx` 新增 `reopenDiffPaneEdit` 与 `reopenDiffPaneLabel`，右栏 header 恢复按钮复用该 label。`src/pages/ChatPage.tsx` 新增 `diffPaneReopenLabel`，页面级 `.chat-diff-pane-reopen-floating` 按钮在有 `selectedEdit` 时也使用文件级 label。`src/locales/en.json` / `src/locales/zh.json` 新增 `chat.layout.expandDiffPanelForFile`。`.trellis/spec/frontend/component-guidelines.md` 已同步：diff pane restore controls 在目标 edit 可知时必须包含文件路径。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为旧按钮缺少文件级 `aria-label`，符合预期。GREEN：同命令通过（1 file / 24 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx`（4 files / 54 tests）。全量前端验证通过：`npm test`（37 files / 341 tests，仅 Browserslist/caniuse-lite 过期提示）、`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出 Windows LF/CRLF 转换提示。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查结果均为不存在。
- 未验证项：未启动真实 Tauri 桌面点测右栏 header 恢复按钮和页面级浮动恢复按钮的 tooltip / Accessibility tree；未验证屏幕阅读器是否按预期宣布 `Expand file diff panel: <path>`；未与 cc-gui 做真实运行并排截图。本轮页面级浮动按钮缺少专门组件测试，已通过源码复查、构建和全量测试覆盖类型/i18n 集成。
- 下一步候选：1) 启动真实 Tauri 桌面，构造多文件 edit 会话，点测收起 diff pane 后右栏 header 恢复按钮与页面级浮动恢复按钮的 tooltip、焦点顺序、Accessibility tree 文件级 label；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、右栏 edit tree、hover preview 可读性、central diff pane restore/focus feedback、输入区 fallback、provider/model 图标和搜索反馈；3) 如果实机发现恢复按钮的目标文件仍不够明显，下一轮可考虑在浮动恢复按钮旁用非卡片化、低噪声的 `title`/视觉辅助状态显示当前文件短名，但不要扩大为新面板；4) 若 hover preview 仍被用户反馈可读性不足，优先做真实截图校准，而不是继续盲调 CSS。

## 本轮 PLAN 2026-06-20 diff pane restore focus handoff

- 目标：补齐 diff pane 收起后的恢复焦点闭环。当前右栏 header 恢复按钮和页面级浮动恢复按钮会重新展开中央 diff pane，但只调用 `setDiffPaneCollapsed(false)`；键盘用户激活按钮后，焦点不会交给新出现的 diff pane，甚至可能停在消失的浮动按钮位置。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / 复用诊断。验证方式：读取 `ChatPage.tsx`、`StatusPanel.tsx`、`chatUiBehavior.ts` 与测试，确认文件行选择已有桌面端 `requestAnimationFrame` 聚焦逻辑，但恢复按钮没有复用，且重复实现会带来行为漂移。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/utils/chatUiBehavior.test.ts`，先断言 `queueDiffPaneFocusAfterOpen` 在桌面端排队聚焦、非桌面不聚焦、target 下一帧可为空；旧实现应因 helper 不存在失败。
- 功能点 4：实现最小交互修复。验证方式：在 `src/utils/chatUiBehavior.ts` 新增 `queueDiffPaneFocusAfterOpen()`，以 `matchMedia('(min-width: 1280px)')` 保护桌面可见 pane，并在下一帧调用 `getTarget()?.focus({preventScroll:true})`；`ChatPage` 的文件选择、右栏恢复、浮动恢复全部复用该 helper。不改 `StatusPanel` props，不改视觉布局，不改后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 diff pane restore controls 也应复用桌面 focus handoff。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `chatUiBehavior` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 diff pane restore focus handoff

- 上轮实际进展：上一轮已让 diff pane 恢复入口带上文件级 accessible label；代码复查确认 `StatusPanel` 右栏 header 恢复按钮和 `ChatPage` 页面级浮动恢复按钮均能在目标 edit 可知时使用 `Expand file diff panel: <path>` / `展开文件差异面板：<path>`。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器宣布顺序、focus ring 主题观感和 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 file changes 操作入口强调“进入具体文件 diff 后继续审阅”。本项目右栏文件行选择已有桌面端焦点 handoff，但 diff pane 被收起后，右栏 header 恢复按钮和页面级浮动恢复按钮只负责重新展开，不负责把焦点交给刚恢复的中央 pane。键盘用户激活恢复按钮后，焦点可能停在右栏按钮，或停在页面级浮动按钮消失前的位置。
- 本轮诊断（超越 cc-gui）：本项目中央 diff pane 不只是展示区，还提供 wrap、copy path、open file、collapse 等审阅控件。恢复 pane 后自动把桌面焦点交给 pane，可以解决“面板已经恢复但还要重新 Tab 很久才能继续操作 diff”的痛点，并让恢复按钮、pane label、focus ring 三者形成连续闭环；小屏继续跳过，避免聚焦不可见 pane。
- 本轮规划：抽出共享 `queueDiffPaneFocusAfterOpen()`，让文件行选择、右栏恢复按钮、页面级浮动恢复按钮共用同一桌面断点和下一帧聚焦策略；不改 `StatusPanel` props、不改视觉布局、不改 diff 数据流、不改后端。优先级中高：用户价值中高（键盘和辅助技术用户恢复后可直接继续审阅）、出现频率中（收起/恢复 diff pane）、实现成本低（纯前端 helper + 调用点复用）、风险低（仅桌面可见 pane 聚焦，非桌面直接返回 false）。
- 本轮完成：`src/utils/chatUiBehavior.test.ts` 新增 RED/GREEN 回归，覆盖桌面 `matchMedia('(min-width: 1280px)')` 命中时排队 `requestAnimationFrame` 并在下一帧 `focus({preventScroll:true})`、非桌面不聚焦、下一帧 target 为空也安全。`src/utils/chatUiBehavior.ts` 新增 `queueDiffPaneFocusAfterOpen()`，集中处理 `matchMedia` / `requestAnimationFrame` 缺失和桌面断点保护。`src/pages/ChatPage.tsx` 让 `handleSelectedEditChange()`、右栏 `onOpenDiffPanel`、页面级 `.chat-diff-pane-reopen-floating` 点击都复用该 helper，避免恢复入口和文件选择入口行为漂移。`.trellis/spec/frontend/component-guidelines.md` 已同步：diff pane restore controls 也应复用桌面 programmatic focus handoff。
- 本轮验证：RED：`npm test -- src/utils/chatUiBehavior.test.ts` 先失败，失败点为 `queueDiffPaneFocusAfterOpen is not a function`，符合预期。GREEN：同命令通过（1 file / 17 tests）。相邻验证通过：`npm test -- src/utils/chatUiBehavior.test.ts src/components/chat/StatusPanel.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx`（5 files / 71 tests）。全量前端验证通过：`npm test`（37 files / 342 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。补充收尾验证：`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查结果均为不存在。
- 未验证项：未启动真实 Tauri 桌面点测右栏 header 恢复按钮和页面级浮动恢复按钮激活后焦点是否确实落到中央 pane；未验证屏幕阅读器在 restore focus handoff 后是否按预期宣布 `File diff: <path>` / `文件差异：<path>`；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、单元测试、构建、后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造多文件 edit 会话，点测右侧文件行 Enter/Space、右栏 header 恢复按钮、页面级浮动恢复按钮三条路径的焦点落点、focus ring、Tab 顺序和 Accessibility tree label；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、右栏 edit tree、hover preview 可读性、central diff pane restore/focus feedback、输入区 fallback、provider/model 图标和搜索反馈；3) 如果实机发现恢复焦点过于主动，下一轮考虑只对键盘激活恢复按钮执行 handoff，但需先增加事件来源测试；4) 如果实机确认焦点正确但视觉连续性仍弱，优先优化中央 pane header 的 selected-file 状态反馈，而不是继续扩大浮层内容。

## 本轮 PLAN 2026-06-20 status edit selected row current label

- 目标：补齐右侧最近改动树的当前文件语义。当前选中文件行已有 `status-edit-tree-file-selected` 和 `aria-current="true"`，但行本身的 accessible label 仍和普通文件行一样是 `Inspect full diff: <file>`；辅助技术用户只能从额外状态猜测这就是中央 diff pane 当前文件。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和共享指南；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `StatusPanel.tsx`、`StatusPanel.test.tsx`、`ChatPage.tsx` 与 cc-gui file changes 相关代码；确认右栏文件行已有选中 class/`aria-current`，本轮不重复改视觉结构，只补当前行 label 语义。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx` 中 selected edit row 回归，断言选中行输出 `Current full diff: <file>` / `当前完整差异：<file>`；旧实现应因仍输出 `Inspect full diff` 失败。
- 功能点 4：实现最小可访问性修复。验证方式：新增 `chat.layout.inspectCurrentFullDiff` 英中文文案；`StatusPanel` 在 `isSelected` 时用当前文件 label，否则保留普通 `inspectFullDiff`。不改 DOM 结构、不改点击/键盘处理、不改 central diff pane、不开新依赖。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 status edit tree selected row 的 accessible label 应明确当前 diff，而不是只依赖 `aria-current`。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `StatusPanel` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、必要时 `cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 status edit selected row current label

- 上轮实际进展：上一轮已完成 diff pane restore focus handoff；代码复查确认 `queueDiffPaneFocusAfterOpen()` 已集中实现桌面断点下一帧聚焦，`ChatPage` 的文件行选择、右栏恢复按钮和页面级浮动恢复按钮都复用该 helper。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器宣布顺序和 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 file changes 操作入口让用户持续知道当前审阅对象；本项目右侧 edit tree 已有选中 class 与 `aria-current="true"`，但选中行的 `aria-label` 仍和普通行一样是 `Inspect full diff: <file>`。辅助技术用户需要依赖屏幕阅读器是否宣布 `aria-current` 才能理解这就是中央 pane 当前文件，语义闭环不够稳。
- 本轮诊断（超越 cc-gui）：本项目中央 diff pane、右侧 edit tree 和恢复入口已经形成多入口审阅流。把选中行的可访问名称改为 `Current full diff: <file>` / `当前完整差异：<file>`，可以解决“我在右栏文件树里重新扫文件时，不知道哪一行已经映射到中央完整 diff”的痛点，不增加视觉噪声，也不改变鼠标/键盘行为。
- 本轮规划：只补 selected row label：新增 `chat.layout.inspectCurrentFullDiff` 英中文文案；`StatusPanel` 在 `isSelected` 时使用该文案，未选中行继续使用 `inspectFullDiff`。不改 DOM 结构、不改 `role="button"` 与内部 Open File 按钮结构、不改点击/键盘处理、不改 central diff pane、不改后端。优先级中等偏高：用户价值中（辅助技术和长文件树定位更明确）、出现频率中（多文件 diff 审阅时反复扫右栏）、实现成本低（i18n + 一处条件 label + SSR 断言）、风险低（无数据流和布局变化）。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 更新 selected edit row 回归，先 RED 确认旧 HTML 仍输出 `查看完整差异：src/components/chat/StatusPanel.tsx`，后 GREEN 锁定 `Current full diff: ...` / `当前完整差异：...`。`src/components/chat/StatusPanel.tsx` 将文件行 `inspectDiffLabel` 改为按 `isSelected` 选择 `inspectCurrentFullDiff` 或 `inspectFullDiff`。`src/locales/en.json` / `src/locales/zh.json` 新增 `chat.layout.inspectCurrentFullDiff`。`.trellis/spec/frontend/component-guidelines.md` 已同步：status edit tree 的 selected row 应明确宣布当前 full diff，不能只依赖 `aria-current`。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为选中行仍输出普通 `Inspect full diff` / `查看完整差异` label，符合预期。GREEN：同命令通过（1 file / 24 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 71 tests）。全量前端验证通过：`npm test`（37 files / 342 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查结果均为不存在。
- 未验证项：未启动真实 Tauri 桌面点测右栏 selected edit row 在 Accessibility tree 中是否按预期宣布当前 diff；未验证不同屏幕阅读器是否同时宣布 `aria-current` 和新 label；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造多文件 edit 会话，点测右侧 selected row、右栏 header 恢复按钮、页面级浮动恢复按钮、中央 diff pane root 的 Accessibility tree label 与焦点顺序；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、右栏 edit tree、hover preview 可读性、central diff pane restore/focus feedback、输入区 fallback、provider/model 图标和搜索反馈；3) 如果实机发现 selected row label 已清楚但视觉连续性仍弱，下一轮优先在中央 pane header 增强 selected-file 状态反馈；4) 如果屏幕阅读器对 `role="button"` + 内部 Open File 按钮组合表现不稳定，再单独设计非嵌套交互结构，避免在当前小轮次里做 DOM 重构。

## 本轮 PLAN 2026-06-20 diff pane file action target labels

- 目标：补齐中央 diff pane 内文件操作按钮的目标语义。当前 pane root 已能宣布 `File diff: <file>`，但焦点进入 pane 后继续 Tab 到 `Open file` / `Copy path` icon 按钮时，按钮本身仍是通用 label；用户需要记住当前文件上下文，长路径或多文件审阅时容易误操作。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务制品、前端规范和共享指南；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `ChatDiffReviewPane.tsx`、`ChatDiffReviewPane.test.tsx` 和 cc-gui `FileChangesList.tsx`；确认 cc-gui 拆分文件名打开与 diff 动作，本项目中央 pane 已有更完整按钮组，但 Open/Copy 仍缺目标文件 label。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ChatDiffReviewPane.test.tsx`，断言选中文件时 Open/Copy 按钮输出 `Open file: <file>` / `Copy path: <file>`；旧实现应因只输出通用 `Open file` / `Copy path` 失败。
- 功能点 4：实现最小可访问性修复。验证方式：新增 `tools.openFileForPath`、`tools.copyPathForPath`、`tools.copiedPathForPath` 英中文文案；`ChatDiffReviewPane` 有 edit 时使用文件级 title/aria-label，无 edit 时保留通用 label。不改按钮视觉、不改点击逻辑、不改 open/copy helper、不改后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 central diff pane 的 file actions 在目标 edit 可知时应包含文件路径。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `ChatDiffReviewPane` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、必要时 `cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 diff pane file action target labels

- 上轮实际进展：上一轮已补齐右侧 selected edit row 的当前 diff 语义；代码复查确认 `src/components/chat/StatusPanel.tsx` 已在 `isSelected` 时使用 `chat.layout.inspectCurrentFullDiff`，`ChatPage` 与 `chatUiBehavior` 也已复用桌面端 diff pane focus handoff。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器宣布顺序和 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 file changes 交互会把“进入哪个文件”作为显式操作对象；本项目中央 diff pane 已有 `File diff: <path>` 根级 accessible name，但 pane 内的 Open/Copy icon 按钮仍只宣布 `Open file` / `Copy path`。键盘或辅助技术用户 Tab 到按钮时，必须记住当前 pane 上下文，长路径/多文件审阅时容易误触。
- 本轮诊断（超越 cc-gui）：本项目中央 diff pane 同时承担完整 diff 审阅、打开文件、复制路径、复制完成反馈。把按钮自身改为 `Open file: <path>`、`Copy path: <path>`，并让 copied 状态保持 `Copied path: <path>`，可以解决“复制成功后不知道刚复制的是哪个文件路径”的痛点，不增加视觉噪声，也不改变鼠标点击路径。
- 本轮规划：只补中央 diff pane file action 的目标语义：`ChatDiffReviewPane` 按 `edit` 派生 open/copy/copied 文件级 label；英中文 locale 新增 `tools.openFileForPath`、`tools.copyPathForPath`、`tools.copiedPathForPath`；同步前端组件规范。不改按钮布局、不改 `openFile` / `copyToClipboard`、不改 diff 数据流、不改后端。优先级中等偏高：用户价值中（减少多文件审阅误操作）、出现频率中（每次进入 pane 后常用 open/copy）、实现成本低（i18n + label 派生）、风险低（空状态保留通用 label）。
- 本轮完成：`src/components/chat/ChatDiffReviewPane.test.tsx` 新增并跑通 RED/GREEN 回归，锁定选中 diff 文件时 Open/Copy 按钮必须输出文件级 `aria-label`。`src/components/chat/ChatDiffReviewPane.tsx` 新增 `openFileLabel`、`copyPathLabel`、`copiedPathLabel`、`activeCopyLabel`，有 `edit` 时用于按钮 `title` 与 `aria-label`，无 `edit` 时回退现有通用文案。`src/locales/en.json` / `src/locales/zh.json` 新增三组工具文案。`.trellis/spec/frontend/component-guidelines.md` 已同步：central diff pane 的 file-action buttons 在目标 edit 可知时必须带目标路径，copied 状态不能退回泛化 `Copied`。
- 本轮验证：RED：`npm test -- src/components/chat/ChatDiffReviewPane.test.tsx` 先失败 1 项，失败点为旧 HTML 仍输出 `aria-label="打开文件"` / `aria-label="复制路径"`，符合预期。GREEN：同命令通过（1 file / 7 tests）。相邻验证通过：`npm test -- src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 72 tests）。全量前端验证通过：`npm test`（37 files / 343 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。
- 未验证项：未启动真实 Tauri 桌面点测中央 diff pane Open/Copy 按钮 tooltip 与 Accessibility tree；未验证复制后 `Copied path: <path>` 在真实浏览器焦点按钮上的宣布效果；未与 cc-gui 做真实运行并排截图。本轮判断基于真实源码、CodeGraph 复查、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造多文件 edit 会话，点测中央 diff pane 的 Open/Copy/Copied tooltip、Accessibility tree label、焦点顺序与复制后 2 秒状态恢复；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、右栏 edit tree、hover preview 可读性、central diff pane action labels、输入区 fallback、provider/model 图标和搜索反馈；3) 如果实机发现按钮 file path 太长导致 tooltip 可读性差，下一轮可保留 accessible label 完整路径，同时视觉 title 改为短路径 + header 兜底，但需先有截图/点测证据；4) 若用户继续反馈 hover 文件预览可读性不足，优先启动真实截图校准浮层背景/边框/阴影，而不是继续盲调透明度。

## 本轮 PLAN 2026-06-20 status hover preview viewport positioning

- 目标：延续文件 hover 预览可读性优化，修正右侧 `StatusPanel` 文件 hover diff 浮层在窗口底部可能越界的问题。当前 CSS 已允许 status hover body 到 420px，但 `StatusPanel.showFloatingPreview()` 仍用 `window.innerHeight - 420` 估算总高度，漏算 header、边框和边距；文件行靠近窗口底部时，浮层可能被底部裁掉，用户仍然看不清完整内容。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务上下文、前端规范和相关源码；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / CSS 诊断。验证方式：读取 `StatusPanel.tsx` 与 `toolBlocks.css`，确认定位逻辑的 `420` 与 CSS 中 status tall preview body `max-height: min(420px, calc(100vh - 96px))` 不一致。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，断言底部文件行的浮层 top 会按完整浮层估算高度夹住，小窗口会退回 12px 安全 top；旧实现应因仍用 420px 而失败。
- 功能点 4：实现最小定位修复。验证方式：在 `StatusPanel.tsx` 抽出 `getStatusEditPreviewTop()`，集中处理 row top、viewport height、预估浮层总高和安全边距；`showFloatingPreview()` 复用该 helper。不改 hover preview 内容、不改 CSS、不改中央 diff pane、不改后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 status hover preview 的浮层定位必须按完整预览高度和 viewport padding 计算，不能只用 body max-height。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `StatusPanel` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 status hover preview viewport positioning

- 上轮实际进展：上一轮已补齐中央 diff pane 文件动作按钮的目标语义；代码复查确认 `src/components/chat/ChatDiffReviewPane.tsx` 已用 `openFileLabel`、`copyPathLabel`、`copiedPathLabel`、`activeCopyLabel` 为 Open/Copy/Copied 状态提供文件级 `title` 与 `aria-label`，英中文 locale 已新增对应 `tools.*ForPath` 文案。已知阻塞仍是真实 Tauri 桌面点测 hover/tooltip/Accessibility tree 和 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 file changes popover 是稳定容器，不依赖文件行附近的半截浮层；本项目为了超越 cc-gui 做了 status hover diff 快速预览，但 `StatusPanel.showFloatingPreview()` 仍用 `window.innerHeight - 420` 夹住 top。CSS 中 status tall preview 的 body 可到 420px，外层还包含 header、边框和 padding，因此靠近窗口底部的文件行可能把浮层开到视口外，削弱之前“实底更清晰”的改动效果。
- 本轮诊断（超越 cc-gui）：本项目的 hover diff 是快速审阅入口，清晰度不只取决于不透明背景，也取决于浮层完整可见。把定位计算抽成可测 helper 并按完整浮层估算高度夹住，可以解决“底部文件 hover 后浮层下半截被裁掉，仍然看不清”的痛点，同时不牺牲已提升的 24 行预算和 420px 可读高度。
- 本轮规划：只修正 status edit hover preview 的 top 计算：新增 `getStatusEditPreviewTop()`，用 480px 估算完整浮层高度并保留 12px 安全边距；`showFloatingPreview()` 复用 helper。不改 hover 内容、不改 CSS、不改 central diff pane、不改行数预算、不改后端。优先级中高：用户价值中高（直接影响底部文件 hover 可读性）、出现频率中（长 edit tree 或窗口较矮时）、实现成本低（纯函数 + 调用点）、风险低（只改变浮层 top）。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 新增 RED/GREEN 回归，覆盖底部文件行、矮窗口和顶部文件行三种 top 计算边界。`src/components/chat/StatusPanel.tsx` 新增 `STATUS_EDIT_PREVIEW_TOP_PADDING`、`STATUS_EDIT_PREVIEW_ROW_OFFSET`、`STATUS_EDIT_PREVIEW_ESTIMATED_HEIGHT` 与导出 helper `getStatusEditPreviewTop()`，`showFloatingPreview()` 改为使用该 helper。`.trellis/spec/frontend/component-guidelines.md` 已同步：status hover preview 浮层定位必须按完整预览外壳计算，不能只用 body max-height。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为 `getStatusEditPreviewTop is not a function`，符合预期。GREEN：同命令通过（1 file / 25 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 73 tests）。全量前端验证通过：`npm test`（37 files / 344 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。
- 未验证项：未启动真实 Tauri 桌面点测文件行靠近窗口底部时 hover preview 是否完全可见；未在真实 360/720/1080 高度、浅色/深色主题下截图确认 480px 估算是否刚好；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CSS 规则复查、单元测试、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造长 edit tree，把文件行滚到视口底部，点测 status hover preview 是否完整可见、不遮挡关键控制、header stats 不拥挤；2) 若实机发现 480px 估算仍偏大或偏小，只调 `STATUS_EDIT_PREVIEW_ESTIMATED_HEIGHT`，不回退实底/换行/24 行预算；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、右栏 edit tree、hover preview 可读性和定位、central diff pane action labels、输入区 fallback；4) 如果文件 hover 体验已稳定，下一轮优先处理真实 Accessibility tree 点测，而不是继续增加 hover 内容。

## 本轮 PLAN 2026-06-20 status hover hidden-line clue placement

- 目标：延续右侧文件 hover diff 可读性优化，修正 status hover 预览中隐藏行数提示的放置策略。当前 `EditDiffPreview` 在 status surface 下已经把 hidden-line clue 放到 header，但仍会在 body 末尾渲染 `edit-diff-hover-more`；而 status hover body 是 `overflow: hidden` 的裁剪区域，尾部提示经常不可见，造成重复且不可靠的信息。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务上下文、前端规范和相关源码；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / CSS 诊断。验证方式：读取 `EditDiffPreview.tsx`、`EditDiffPreview.test.tsx` 和 `toolBlocks.css`，确认 status header 已有 `edit-diff-hover-hidden-summary`，body 末尾仍有 `edit-diff-hover-more`，且 hover body 不可滚动。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/toolBlocks/EditDiffPreview.test.tsx`，断言 status hover 有 header hidden clue 但没有 body footer `edit-diff-hover-more`；普通 hover 仍保留 footer clue。旧实现应因 status HTML 仍包含 footer class 而失败。
- 功能点 4：实现最小展示修复。验证方式：`EditDiffPreview` 在 `surface="status"` 时只渲染 header hidden summary，不再渲染 body footer more-lines；默认 surface 保持原行为。不改 CSS、不改 line budget、不改浮层定位、不改后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 status hover hidden-line clue 应放在 header，避免依赖被裁剪 body 尾部。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `EditDiffPreview` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 status hover hidden-line clue placement

- 上轮实际进展：上一轮已修正 status hover preview 的 viewport top 计算；代码复查确认 `src/components/chat/StatusPanel.tsx` 已导出 `getStatusEditPreviewTop()` 并让 `showFloatingPreview()` 使用 480px 完整浮层估算高度和 12px 安全边距。已知阻塞仍是真实 Tauri 桌面点测 hover 完整可见性、不同窗口高度/主题截图和 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的 file changes popover 是稳定可滚动容器；本项目 status hover preview 是不可滚动的临时浮层。当前 `EditDiffPreview` 在 status surface 下同时渲染 header 的 `edit-diff-hover-hidden-summary` 和 body 末尾的 `edit-diff-hover-more`。由于 body 使用 `overflow: hidden`，尾部提示很可能被裁掉，不能作为可靠信息来源。
- 本轮诊断（超越 cc-gui）：本项目 hover diff 的目标是快速审阅，不应要求用户去浮层底部找提示，也不能给出一个可能不可见的重复提示。保留 header pill，去掉 status surface 的 body footer clue，可以解决“隐藏行数提示重复但尾部经常看不到”的痛点，同时普通 transcript hover 仍保留原来的 footer clue。
- 本轮规划：只改 `EditDiffPreview` 的 hidden-line clue 放置：status surface 下只渲染 header hidden summary；非 status surface 继续渲染 `edit-diff-hover-more`。不改 CSS、不改 24 行预算、不改 floatingTop、不改 StatusPanel 数据流、不改中央 diff pane、不改后端。优先级中等：用户价值中（减少不可见/重复提示造成的认知噪声）、出现频率中（长 diff hover 时）、实现成本低（条件渲染 + 测试）、风险低（限定 status surface）。
- 本轮完成：`src/components/toolBlocks/EditDiffPreview.test.tsx` 扩展 status hidden-line clue 回归，先 RED 确认 status HTML 仍包含 `edit-diff-hover-more`，后 GREEN 锁定 status hover 有 `edit-diff-hover-hidden-summary` 但无 body footer；同时锁定普通 hover 仍保留 `edit-diff-hover-more`。`src/components/toolBlocks/EditDiffPreview.tsx` 将 body footer more-lines 条件改为 `hiddenLineCount > 0 && surface !== 'status'`。`.trellis/spec/frontend/component-guidelines.md` 已同步：status-panel hover preview 应优先 header hidden-line clue，不要在被裁剪 body 里重复 footer-only clue。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx` 先失败 1 项，失败点为 status HTML 仍包含 `edit-diff-hover-more`，符合预期。GREEN：同命令通过（1 file / 6 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 73 tests）。全量前端验证通过：`npm test`（37 files / 344 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。
- 未验证项：未启动真实 Tauri 桌面点测 status hover header hidden-line pill 在长 diff、矮窗口、浅色/深色主题下是否足够明显；未验证普通 transcript hover 的 footer clue 视觉表现；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CSS 规则复查、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造超过 24 行的文件 diff，点测 status hover header 的 hidden-line pill 是否稳定可见、不会和加减统计拥挤；2) 如果实机发现 header stats 拥挤，下一轮只调整 header stats 布局或隐藏行 pill 文案宽度，不恢复 body footer clue；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、右栏 edit tree、hover preview 可读性和隐藏行提示、central diff pane action labels、输入区 fallback；4) hover 展示层连续几轮已经覆盖背景、宽度、换行、高度、行数预算、定位和提示位置，下一轮应优先做真实桌面/Accessibility tree 验证，而不是继续扩展 hover 内容。

## 本轮 PLAN 2026-06-20 status hover tooltip association

- 目标：补齐右侧 `StatusPanel` 文件 hover/focus diff 预览的辅助技术关联。当前 `EditDiffPreview` 根节点已经使用 `role="tooltip"`，但触发它的文件行没有 `aria-describedby`，tooltip 也没有稳定 `id`，键盘/屏幕阅读器用户无法把可见预览和当前焦点行关联起来。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务上下文、前端规范和 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `StatusPanel.tsx`、`EditDiffPreview.tsx` 和 `StatusPanel.test.tsx`；读取 cc-gui `FileChangesList.tsx`，确认 cc-gui 以显式 diff 操作为主，本项目 hover tooltip 是超越项但缺少 ARIA 关联。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，断言带 `diffPreviewLines` 的文件行输出 `aria-describedby`，且页面内存在同 id 的 `role="tooltip"`；同时断言无 diff preview 的文件行没有悬空 `aria-describedby`。旧实现应因缺少该关联失败。
- 功能点 4：实现最小可访问性改动。验证方式：`EditDiffPreview` 支持可选 `id` 并透传到 tooltip 根节点；`StatusPanel` 为有 diff preview 的文件行生成稳定 id，传给触发行和 tooltip。不改 hover 内容、不改定位、不改中央 diff pane、不改后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 status-panel hover tooltip 有内容时触发器必须使用 `aria-describedby` 指向 tooltip，不能留下空引用。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `StatusPanel` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 status hover tooltip association

- 上轮实际进展：上一轮已把 status hover hidden-line clue 从被裁剪 body footer 收敛到 header；代码复查确认 `src/components/toolBlocks/EditDiffPreview.tsx` 已在 `surface="status"` 时不再渲染 `edit-diff-hover-more`，普通 hover 仍保留 footer clue。已知阻塞仍是真实 Tauri 桌面 hover/Accessibility tree 点测和 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui `FileChangesList.tsx` 以明确的 open/diff/undo 操作按钮承载文件变更审阅，减少“当前控件描述了什么”的歧义。本项目 status 文件行已经超出 cc-gui，提供 hover/focus diff preview，但触发行只暴露完整 diff action label，未通过 `aria-describedby` 关联已经渲染的 `role="tooltip"` 预览。
- 本轮诊断（超越 cc-gui）：本项目的 hover diff 是轻量快速审阅入口，键盘用户 focus 文件行时也会显示预览。把文件行和 tooltip 建立稳定 ARIA 关系，可以解决“焦点行旁边出现了预览，但辅助技术无法知道这块内容属于当前行”的痛点；无 diff 内容的行不生成引用，避免悬空 id。
- 本轮规划：只补 status hover tooltip 的关联语义：`EditDiffPreview` 增加可选 `id`，`StatusPanel` 为有 `diffPreviewLines` 的文件行生成稳定 id 并设置 `aria-describedby`，同步前端规范。不改 hover 内容、不改定位、不改 line budget、不改中央 diff pane、不改后端。优先级中等：用户价值中（键盘/辅助技术关联更完整）、出现频率中（每个有 diff 预览的文件行）、实现成本低（属性透传 + id helper + SSR 回归）、风险低（无行为和视觉变化）。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 新增 RED/GREEN 回归，锁定有 preview 的 status 文件行输出 `aria-describedby` 并指向同一个 `role="tooltip"` id，同时确保无 preview 的文件行不输出悬空引用。`src/components/toolBlocks/EditDiffPreview.tsx` 为 tooltip 根节点新增可选 `id` 透传。`src/components/chat/StatusPanel.tsx` 新增 `createStatusEditPreviewId()`，基于 edit tree node key 生成 Windows 路径安全的稳定 id，并只在 `diffPreviewLines.length > 0` 时应用。`.trellis/spec/frontend/component-guidelines.md` 已同步：status-panel hover tooltip 有内容时必须用 `aria-describedby` 关联触发行，空 preview 行不能产生 dangling reference。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为旧实现缺少 `aria-describedby`，符合预期。GREEN：同命令通过（1 file / 26 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 74 tests）。全量前端验证通过：`npm test`（37 files / 345 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出 Windows LF/CRLF 转换提示。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查结果均为不存在。
- 未验证项：未启动真实 Tauri 桌面点测文件行 focus 后 Accessibility tree 是否把 `aria-describedby` 内容按预期关联；未验证屏幕阅读器实际播报顺序；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 对标、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造含 diff preview 的 status edit tree，点测鼠标 hover、键盘 focus、Accessibility tree 中 tooltip 关联和中央 diff pane 焦点 handoff；2) 如果实机发现 tooltip 描述过长导致播报噪声，可保留 `aria-describedby` 但考虑用更短的隐藏描述文本，先以实测为依据；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、右栏 edit tree、hover preview 可读性、tooltip 语义关联、central diff pane action labels、输入区 fallback；4) hover 展示层和基础语义已连续覆盖多轮，下一轮优先做真实桌面/Accessibility tree 验证，而不是继续扩展 hover 内容。

## 本轮 PLAN 2026-06-20 status edit open-file target label

- 目标：补齐右侧 `StatusPanel` edit tree 文件行动作的一致性。文件行本体已经说明“查看完整 diff”，中央 diff pane 的 Open/Copy 按钮也已经带目标路径；但文件行内的 open-file 图标仍只宣布通用 `Open file` / `打开文件`，在多文件列表中 Tab 到图标时无法知道会打开哪个文件。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务上下文、前端规范、`package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `StatusPanel.tsx`、`StatusPanel.test.tsx` 与 cc-gui `FileChangesList.tsx`；确认 cc-gui 有文件打开入口，本项目右栏 open-file 图标仍是通用 label。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，断言 `status-edit-tree-open` 按钮输出 `Open file: <file>` / `打开文件：<file>`；旧实现应因只输出通用 `Open file` 失败。
- 功能点 4：实现最小可访问性改动。验证方式：`StatusPanel` 复用既有 `tools.openFileForPath` 英中文文案，为行内 open-file 图标提供文件级 `title` 与 `aria-label`。不改点击逻辑、不改 openFile helper、不改 hover preview、不改 central diff pane、不改后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 status edit tree 的 file action buttons 在目标文件可知时必须包含路径。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `StatusPanel` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 status edit open-file target label

- 上轮实际进展：上一轮已补齐 status hover tooltip 与触发行之间的 `aria-describedby` 关联；代码复查确认 `src/components/chat/StatusPanel.tsx` 已为有 `diffPreviewLines` 的文件行生成稳定 tooltip id，`src/components/toolBlocks/EditDiffPreview.tsx` 已支持可选 `id` 透传。已知阻塞仍是真实 Tauri 桌面 hover/focus/Accessibility tree 点测、屏幕阅读器播报顺序和 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui `FileChangesList.tsx` 有文件打开入口和 diff 操作入口，文件名本身携带目标路径 title。本项目右栏 edit tree 已把文件行本体定义为“查看完整 diff”，但同一行里的 open-file 图标仍只输出通用 `tools.openFile`，在多文件列表中不能告诉键盘/辅助技术用户会打开哪个文件。
- 本轮诊断（超越 cc-gui）：本项目右栏文件行同时有“查看完整 diff”和“打开文件”两个近邻动作。让 open-file 图标使用 `Open file: <path>` / `打开文件：<path>`，可以解决“Tab 到图标只听到打开文件，不知道目标是哪一个”的痛点，并与中央 diff pane 的文件级 Open/Copy label 保持一致。
- 本轮规划：只补 status edit tree 行内 open-file 图标的文件级 `title` / `aria-label`：复用既有 `tools.openFileForPath`，不新增 i18n；不改点击逻辑、不改 `openFile` helper、不改 hover preview、不改 central diff pane、不改后端。优先级中等：用户价值中（减少多文件误操作和辅助技术歧义）、出现频率中（每个 edit 文件行都有 open 图标）、实现成本低（label 派生 + SSR 回归）、风险低（无行为变化）。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 扩展 selected edit row 回归，先 RED 确认旧 HTML 只输出 `aria-label="打开文件"`，后 GREEN 锁定 `aria-label="Open file: src/components/chat/StatusPanel.tsx"` / `打开文件：src/components/chat/StatusPanel.tsx`。`src/components/chat/StatusPanel.tsx` 新增 `openEditedFileLabel = t('tools.openFileForPath', {file: edit.displayPath})`，并用于 `status-edit-tree-open` 按钮的 `title` 与 `aria-label`。`.trellis/spec/frontend/component-guidelines.md` 已同步：status edit tree 行级 file action button 在目标文件可知时必须使用文件级 label，而不是泛化 `Open file`。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为旧按钮缺少文件级 `aria-label`，符合预期。GREEN：同命令通过（1 file / 26 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 74 tests）。全量前端验证通过：`npm test`（37 files / 345 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出 Windows LF/CRLF 转换提示。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查结果均为不存在。
- 未验证项：未启动真实 Tauri 桌面点测右栏 open-file 图标 tooltip 与 Accessibility tree；未验证屏幕阅读器对相邻“当前完整差异”和“打开文件：<path>”的实际播报节奏；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 对标、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造多文件 edit tree，点测文件行本体、hover tooltip、open-file 图标、中央 diff pane handoff 在 Accessibility tree 中的关系是否清晰；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、右栏 edit tree、hover preview 可读性、tooltip 语义关联、行级 open-file label、central diff pane action labels、输入区 fallback；3) 如果实机发现同一文件行的 action label 太长导致读屏噪声，可保留文件级 title/aria-label，但考虑为 icon button 增加更短的上下文描述，需要以实测为依据；4) 文件审阅链路的基础语义已连续补齐，下一轮优先做真实桌面/Accessibility tree 验证，而不是继续扩展 hover 内容。

## 本轮 PLAN 2026-06-20 status edit folder action labels

- 目标：补齐右侧 `StatusPanel` edit tree 文件夹行的展开/收起动作语义。当前文件夹行是 `<button>` 且有 `aria-expanded`，但 `title` 只有路径，缺少“展开/收起哪个文件夹”的明确 action label；文件行和 open-file 图标已补齐目标语义后，文件夹行也应保持同一可访问性标准。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务上下文、前端规范、`package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `StatusPanel.tsx`、`StatusPanel.test.tsx` 与 cc-gui `FileChangesList.tsx`；确认本项目新增树形结构超出 cc-gui，但 folder row 只有路径 title 与 `aria-expanded`。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，断言展开状态文件夹输出 `Collapse folder: <path>` / `收起文件夹：<path>`，大树默认折叠文件夹输出 `Expand folder: <path>` / `展开文件夹：<path>`；旧实现应因缺少 action label 失败。
- 功能点 4：实现最小可访问性改动。验证方式：新增 `chat.layout.expandEditFolder` / `collapseEditFolder` 英中文文案；`StatusPanel` 文件夹行按 `isCollapsed` 生成文件夹级 `title` 与 `aria-label`。不改树结构、不改折叠状态、不改 hover preview、不改后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 status edit tree 文件夹行必须明确展开/收起动作和目标路径。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `StatusPanel` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 status edit folder action labels

- 上轮实际进展：上一轮已补齐 status edit tree 行内 open-file 图标的文件级 `title` / `aria-label`，代码复查确认 `src/components/chat/StatusPanel.tsx` 已复用 `tools.openFileForPath`，`src/locales/en.json` / `src/locales/zh.json` 已有文件级 open/copy/copy-done 文案。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器播报顺序和 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui 的文件变更入口以文件级操作为主；本项目已经超出 cc-gui 做了树形 edit list，但文件夹行仍只暴露 `title={node.path}` 与 `aria-expanded`。键盘或辅助技术用户能知道它是展开态，却听不到“收起哪个文件夹”或“展开哪个文件夹”的动作意图。
- 本轮诊断（超越 cc-gui）：本项目多文件编辑树支持 folder-level 折叠，适合大批量变更审阅。给 folder button 补动作级 accessible label，可以解决“同一树里文件行动作清晰、文件夹行动作却只读路径”的断层，降低键盘用户在大树中误操作的概率。
- 本轮规划：只给 status edit tree 文件夹行补动作语义：新增 `chat.layout.expandEditFolder` / `collapseEditFolder` 英中文文案；`StatusPanel` 根据 `isCollapsed` 生成 `title` 和 `aria-label`，目标取 `node.path || node.name`。不改树结构、不改默认折叠阈值、不改点击行为、不改 hover preview、不改 central diff pane、不改后端。优先级中等：用户价值中（树形导航语义完整）、出现频率中高（每个文件夹行都会出现）、实现成本低、风险低。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 已有 RED/GREEN 回归，锁定展开文件夹输出 `Collapse folder: src` / `收起文件夹：src`，大树默认折叠文件夹输出 `Expand folder: src` / `展开文件夹：src`。`src/components/chat/StatusPanel.tsx` 新增 `folderActionLabel` 并用于文件夹按钮 `title` / `aria-label`。`src/locales/en.json` / `src/locales/zh.json` 新增 `expandEditFolder` 与 `collapseEditFolder`。`.trellis/spec/frontend/component-guidelines.md` 已同步：status edit tree 文件夹行必须同时说明展开/收起动作和目标路径，不能只依赖 `aria-expanded` 与裸路径 title。
- 本轮验证：RED：交接前已运行 `npm test -- src/components/chat/StatusPanel.test.tsx`，失败 2 项，失败点为旧 folder row 只有 `title="src"` 和 `aria-expanded`，没有动作级 `aria-label`，符合预期。GREEN：`npm test -- src/components/chat/StatusPanel.test.tsx` 通过（1 file / 26 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 74 tests）。全量前端验证通过：`npm test`（37 files / 345 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面点测文件夹行 hover title、键盘 Enter/Space 展开收起、Accessibility tree 中 `aria-expanded` 与动作级 label 的组合播报；未验证屏幕阅读器实际播报节奏；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造多文件 edit tree，点测文件夹行、文件行、open-file 图标、hover tooltip 与中央 diff pane handoff 在 Accessibility tree 中是否连贯；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、右栏 edit tree、hover preview 可读性、tooltip 语义关联、文件夹 action label、central diff pane action labels 和输入区 fallback；3) 如果实机发现文件夹 label 与 `aria-expanded` 播报重复或过长，可考虑用更短的 label 文案，但必须以 Accessibility tree / screen reader 实测为依据；4) 文件审阅链路基础语义已连续补齐，下一轮优先做真实桌面/Accessibility tree 验证，而不是继续扩大 hover 内容。

## 本轮 PLAN 2026-06-20 input status edit action labels

- 目标：补齐输入区 `ChatInputStatusTabs` 的 Edits fallback 行动作语义。右侧 `StatusPanel` 文件行已经说明“查看完整 diff / 当前完整 diff”，但小屏和桌面 fallback 的输入区 Edits popover 仍只有 `title={edit.displayPath}`，键盘/辅助技术用户从输入区入口选择文件时不知道该行会切换中央 diff pane。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务上下文、前端规范、`package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `ChatInputStatusTabs.tsx`、`ChatInputStatusTabs.test.tsx`、`StatusPanel.tsx`，确认输入区 edit row 只有路径 title，而右侧 status edit row 已有 `inspectFullDiff` / `inspectCurrentFullDiff`。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ChatInputStatusTabs.test.tsx`，断言可选择的 edit row 输出 `Current full diff: <file>` / `当前完整差异：<file>`；旧实现应因缺少 `aria-label` 失败。
- 功能点 4：实现最小可访问性改动。验证方式：`ChatInputStatusTabs` 复用既有 `chat.layout.inspectFullDiff` / `inspectCurrentFullDiff` 文案，为 edit row 写入 `title` 和 `aria-label`；不改 popover 打开/关闭逻辑、不改选中数据流、不改 central diff pane、不改后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录输入区 fallback 的 edit rows 必须与 status panel 使用一致的文件级 diff action label。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `ChatInputStatusTabs` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 input status edit action labels

- 上轮实际进展：上一轮已补齐右侧 `StatusPanel` edit tree 文件夹行的展开/收起动作语义；代码复查确认 `src/components/chat/StatusPanel.tsx` 文件夹按钮已经按折叠状态输出 `Expand folder: <path>` / `Collapse folder: <path>`，英中文 locale 与前端规范也已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器播报和 cc-gui 并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮诊断（补齐 cc-gui）：cc-gui file changes 入口强调文件级操作对象；本项目右侧 status panel 已经把文件行定义为“查看完整 diff / 当前完整 diff”，但输入区 `ChatInputStatusTabs` 的 Edits fallback 仍只有裸路径 title。小屏或桌面 fallback 用户从输入区打开 edits popover 时，无法从 row 的 accessible name 判断它会切换中央 diff pane。
- 本轮诊断（超越 cc-gui）：本项目有双入口文件审阅：右侧 status panel 和输入区 fallback。让输入区 edit row 复用右侧 status panel 的文件级 diff action label，可以解决“同一个文件审阅动作在不同入口语义不一致”的痛点；同时在 i18n 未初始化时兜底输出英文 action label + 文件路径，避免 fallback UI 的上下文完全丢失。
- 本轮规划：只补 `ChatInputStatusTabs` Edits popover 行的 `title` / `aria-label`：复用已有 `chat.layout.inspectFullDiff` / `inspectCurrentFullDiff`，选中行使用 current variant；当 `t()` 返回 key 本身时用英文兜底。不同步新增 locale key，不改 popover 展开/关闭逻辑、不改 `onSelectedEditChange`、不改 central diff pane、不改后端。优先级中等偏高：用户价值中高（小屏/输入区入口语义完整）、出现频率中（status panel 隐藏或输入区 fallback 使用时）、实现成本低、风险低。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.test.tsx` 扩展 Edits popover SSR 回归，先 RED 确认旧 HTML 只有 `title="src/pages/ChatPage.tsx"`，缺少动作级 `aria-label`，后 GREEN 锁定选中 edit row 输出 `aria-label="Current full diff: src/pages/ChatPage.tsx"` / `当前完整差异：src/pages/ChatPage.tsx`。`src/components/chat/ChatInputStatusTabs.tsx` 为 edit row 新增 `inspectDiffLabelKey`、`translatedInspectDiffLabel` 和英文兜底 `inspectDiffLabel`，并用于 `title` / `aria-label`。`.trellis/spec/frontend/component-guidelines.md` 已同步：composer/input status fallback edit rows 必须与 status panel 使用一致的文件级 full-diff action label，包括 current-selected 变体。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 1 项，失败点为旧输入区 edit row 缺少 `aria-label`，符合预期。GREEN：同命令通过（1 file / 18 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 74 tests）。全量前端验证通过：`npm test`（37 files / 345 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面点测小屏或输入区 fallback 的 Edits popover，未验证点击输入区 edit row 后中央 diff pane 的焦点交接与 Accessibility tree 播报；未验证真实 i18n 初始化后的中英文文案渲染；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，分别在桌面宽度和小屏宽度点测输入区 Edits popover：tab label、row accessible name、选中后 popover dismiss、central diff pane focus handoff 是否连贯；2) 对右侧 status panel 与输入区 fallback 做同一会话的 Accessibility tree 对比，确认文件行、文件夹行、open-file 图标、tooltip、diff pane root 没有语义断层；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、输入区 fallback、右栏 edit tree、hover preview 可读性、tooltip 语义关联和 central diff pane action labels；4) 如果实机发现输入区 fallback 的文件级 label 过长，可考虑只在 `aria-label` 保留完整路径、视觉 title 用短路径，但必须以实测为依据。

## 本轮 PLAN 2026-06-20 input status tool jump fallback labels

- 目标：补齐输入区 `ChatInputStatusTabs` 的 Tasks / Subagents popover 行跳转语义兜底。上一轮 Edits 行已在 i18n 未初始化时保留 action label + 文件路径，但 task/subagent 行仍直接依赖 `t(scrollToToolTask|scrollToSubagentActivity)`；在 SSR/测试或 i18n 未就绪边界下会退化成 key，丢掉目标任务名。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis 任务上下文、前端规范、`package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `ChatInputStatusTabs.tsx`、`ChatInputStatusTabs.test.tsx`，确认 `renderToolRow()` 的 `aria-label` 直接使用翻译结果，旧测试也只锁定 key 而非目标任务名。
- 功能点 3：补 RED 测试。验证方式：更新 `src/components/chat/ChatInputStatusTabs.test.tsx`，断言 task row 输出 `Jump to tool task: npm test`，subagent row 输出 `Jump to subagent activity: Review composer status strip`；旧实现应因只输出 key 失败。
- 功能点 4：实现最小可访问性改动。验证方式：`ChatInputStatusTabs` 新增本地 helper，翻译存在时使用 i18n 文案，翻译返回 key 时用英文兜底并保留目标 summary/label。不改 click/dismiss 行为、不改 tool timeline、不改右侧 `StatusPanel`、不改后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 composer/input status fallback 的 task/subagent jump rows 也必须在 accessible label 中保留目标任务名，即使 i18n 未就绪。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `ChatInputStatusTabs` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 input status tool jump fallback labels

- 上轮进展与阻塞：上一轮已补齐输入区 Edits fallback 行的文件级 full-diff action label；代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已给 edit rows 写入 `Current full diff: <file>` / `Inspect full diff: <file>` 兜底，前端规范也已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的工具/文件变更入口会尽量让用户明确自己将跳到哪条记录或哪个文件；本项目输入区 `ChatInputStatusTabs` 的 task/subagent rows 在 i18n 正常时有 `scrollToToolTask` / `scrollToSubagentActivity`，但在 SSR、测试或 i18n 未就绪边界下直接退化为 key，丢掉 `npm test`、`Review composer status strip` 这类目标摘要。用户价值：键盘和辅助技术用户不会在 fallback UI 中听到没有业务含义的翻译 key。
- 本轮差距诊断（超越 cc-gui）：本项目把任务、subagent 和 edits 都收敛到输入区 fallback popover，适合小屏和右栏隐藏场景。让 task/subagent rows 与 edit rows 一样具备本地英文兜底，可以解决“同一个 fallback 面板里 edits 有目标上下文、tasks/subagents 却只剩 key”的断层，降低用户在多个并发工具活动间误跳的概率。
- 本轮规划：只修 `ChatInputStatusTabs` 的 tool row accessible label：翻译存在时继续使用 i18n；翻译返回 key 时兜底为 `Jump to tool task: <summary-or-label>` 或 `Jump to subagent activity: <summary-or-label>`。不改点击/关闭逻辑、不改工具时间线数据结构、不改右侧 `StatusPanel`、不改后端。优先级中等偏高：用户价值中高（fallback 面板核心跳转语义完整）、出现频率中（小屏、右栏隐藏或 SSR 边界）、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.test.tsx` 扩展并修正 SSR 回归，先 RED 确认旧实现输出 `aria-label="chat.layout.scrollToToolTask"` / `aria-label="chat.layout.scrollToSubagentActivity"`，后 GREEN 锁定 task row 输出 `Jump to tool task: npm test`、subagent row 输出 `Jump to subagent activity: Review composer status strip`，并确保 subagent 不退化为 tool-task label。`src/components/chat/ChatInputStatusTabs.tsx` 新增 `getToolJumpFallbackLabel()`，`renderToolRow()` 在 `t()` 返回 key 本身时使用英文兜底并保留目标 summary/label。`.trellis/spec/frontend/component-guidelines.md` 已同步：composer/input status fallback task 和 subagent jump rows 必须在 accessible label 中保留目标任务摘要，包括 i18n 边界兜底。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 2 项，失败点为旧 HTML 只包含 `chat.layout.scrollToToolTask` / `chat.layout.scrollToSubagentActivity`，符合预期；更新实现后又发现一条旧断言仍期待裸 key，已修正为本轮目标行为。GREEN：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 通过（1 file / 18 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 74 tests）。全量前端验证通过：`npm test`（37 files / 345 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面点测输入区 Tasks/Subagents popover 的 hover title、键盘跳转、popover dismiss 与 transcript focus handoff；未用 Accessibility tree 或屏幕阅读器验证实际播报顺序；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，在桌面宽度和小屏宽度分别点测输入区 Tasks/Subagents/Edits popover：row accessible name、跳转后 popover dismiss、transcript 或 central diff pane 焦点交接是否连贯；2) 做右侧 status panel 与输入区 fallback 的 Accessibility tree 对比，确认 task/subagent/edit 三类入口没有语义断层；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、输入区 fallback、右栏 edit tree、hover preview 可读性、tooltip 语义关联和 central diff pane action labels；4) 如果实机发现 fallback row label 过长或重复播报，再基于 Accessibility tree / screen reader 实测收敛文案，而不是提前缩短目标上下文。

## 本轮 PLAN 2026-06-20 input status tab fallback labels

- 目标：补齐输入区 `ChatInputStatusTabs` tab 按钮自身的 i18n 未就绪兜底。上一轮 task/subagent/edit 行级 accessible label 已保留目标上下文，但 tab 按钮仍直接拼接 `t(inputStatus*)` 与 `t(inputStatusProgress|inputStatusEditStats)`；在 SSR/测试或 i18n 未就绪边界下，用户在打开 popover 前只会听到 `chat.layout.inputStatusTasks chat.layout.inputStatusProgress` 这类 key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis task artifacts、前端规范、shared guides；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ChatInputStatusTabs.tsx` / test 和 cc-gui `StatusPanel/SubagentList` 相关代码，确认输入区 tabs 是本项目超越项，但 tab-level accessible label 在 i18n 边界缺少语义兜底。
- 功能点 3：补 RED 测试。验证方式：更新 `src/components/chat/ChatInputStatusTabs.test.tsx`，断言 Tasks/Subagents/Edits tab 在 SSR 边界输出 `Tasks 1/2`、`Subagents 0/1`、`Edits +5 / -2` 这类动作前入口标签；旧实现应因只输出 i18n key 失败。
- 功能点 4：实现最小可访问性改动。验证方式：`ChatInputStatusTabs` 新增本地 label/stat fallback helper，翻译存在时保留 i18n，翻译返回 key 时用英文类别和真实计数兜底。不改 tab 显示结构、不改 openTab 状态、不改 popover dismiss、不改右侧 `StatusPanel`、不改后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 composer/input status fallback tabs 也必须有类别和计数语义兜底，不能只拼接翻译 key。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `ChatInputStatusTabs` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 input status tab fallback labels

- 上轮进展与阻塞：上一轮已补齐输入区 Tasks/Subagents popover 行级跳转 label 的 i18n 边界兜底；代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已在 `renderToolRow()` 中用 `Jump to tool task: <summary>` / `Jump to subagent activity: <summary>` 兜底，`ChatInputStatusTabs.test.tsx` 与前端规范也已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的状态入口直接以真实对象列表和状态面板呈现，不会在入口层暴露翻译 key。本项目输入区 `ChatInputStatusTabs` 是自有 fallback/超越入口，但 tab 按钮本身仍把 `t('chat.layout.inputStatusTasks')` 与 `t('chat.layout.inputStatusProgress')` 直接拼接；在 SSR、测试或 i18n 未就绪边界下，用户打开 popover 前只能听到 key，无法知道这是 Tasks、Subagents 还是 Edits，以及当前计数。
- 本轮差距诊断（超越 cc-gui）：本项目把 task/subagent/edit/mcp 的状态压缩到 composer 上方，适合小屏和右栏隐藏场景。行级 label 已经补齐后，tab 级入口也应保留类别和计数兜底，解决“用户还没进入面板就已经失去上下文”的痛点，尤其是屏幕阅读器用户需要先决定打开哪个 tab。
- 本轮规划：只修 `ChatInputStatusTabs` 的 tab label/stat 派生：翻译存在时继续使用 i18n；翻译返回 key 时用 `Tasks <completed>/<total>`、`Subagents <completed>/<total>`、`Edits +<additions> / -<deletions>` 兜底。保留现有 DOM 结构、openTab 状态、popover dismiss、右侧 `StatusPanel` 和后端。优先级中等：用户价值中（入口级语义完整）、出现频率中（fallback strip 出现即触发）、实现成本低（本地 helper + 测试）、风险低（只改变标签文本兜底）。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.test.tsx` 扩展 SSR 回归，先 RED 锁定旧实现只输出 `aria-label="chat.layout.inputStatusTasks chat.layout.inputStatusProgress"`，后 GREEN 锁定 tab 入口输出 `Tasks 2/2`、`Subagents 0/1`、`Edits +5 / -2`，并明确不接受旧 key 拼接。`src/components/chat/ChatInputStatusTabs.tsx` 新增组件内 `translateWithFallback()`，用于 Tasks/Subagents/Edits tab 的 label/stat 和既有 MCP label fallback；翻译正常时保留现有 i18n 文案，翻译未就绪时用英文类别和真实计数。`.trellis/spec/frontend/component-guidelines.md` 已同步：composer/input status fallback tabs 的 `aria-label` / `title` 也必须有类别和计数语义兜底。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 1 项，失败点为旧 HTML 中 Tasks tab 仍输出 `chat.layout.inputStatusTasks chat.layout.inputStatusProgress`，符合预期。GREEN：同命令通过（1 file / 18 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 74 tests）。全量前端验证通过：`npm test`（37 files / 345 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored；并行执行时出现 package cache/build directory lock 等待但最终通过）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面点测输入区 Tasks/Subagents/Edits tabs 的 hover title、键盘展开、spinner 与计数组合播报；未用 Accessibility tree 或屏幕阅读器验证实际播报顺序；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，在桌面宽度和小屏宽度分别点测输入区 tabs 与 rows：tab accessible name、row accessible name、跳转后 popover dismiss、transcript 或 central diff pane 焦点交接是否连贯；2) 做右侧 status panel 与输入区 fallback 的 Accessibility tree 对比，确认 tab、task row、subagent row、edit row、diff pane root 没有语义断层；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、输入区 fallback、右栏 edit tree、hover preview 可读性、tooltip 语义关联和 central diff pane action labels；4) 如果实机发现 tab label 与 count pill 造成重复播报，可基于 Accessibility tree / screen reader 实测调整为更短的 `aria-label`，但不要回退到裸 key。

## 本轮 PLAN 2026-06-20 input status git branch fallback label

- 目标：补齐输入区 `ChatInputStatusTabs` Git branch chip 的 i18n 未就绪兜底。上一轮已修复 Tasks/Subagents/Edits tabs，但同一条 status strip 上的 Git chip 仍直接使用 `t('chat.layout.inputStatusGitBranch')`；SSR/测试或 i18n 未就绪时会宣布 `chat.layout.inputStatusGitBranch <branch>`。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis task artifacts、前端规范、shared guides；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `ChatInputStatusTabs.tsx` / test，确认 Git chip 与 status tabs 同属 composer status strip，但未复用 `translateWithFallback()`。
- 功能点 3：补 RED 测试。验证方式：更新 `src/components/chat/ChatInputStatusTabs.test.tsx`，断言 Git chip 输出 `aria-label="Git <branch>"`、`title="Git: <branch> · <root>"`，并不再包含 `chat.layout.inputStatusGitBranch`。
- 功能点 4：实现最小可访问性改动。验证方式：`ChatInputStatusTabs` 复用现有 `translateWithFallback()` 派生 `gitBranchLabel`，用于 Git chip 的 visible label、title 和 aria-label。不改 Git branch 数据来源、不改布局、不改 tabs/popover、不改后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 composer status strip 的 non-tab chips 也必须有 i18n key 兜底。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `ChatInputStatusTabs` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 input status git branch fallback label

- 上轮进展与阻塞：上一轮已补齐输入区 Tasks/Subagents/Edits tabs 的类别和计数兜底；代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已用 `translateWithFallback()` 输出 `Tasks 2/2`、`Subagents 0/1`、`Edits +5 / -2` 这类 tab 入口语义。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 状态入口不会在 Git/状态入口层暴露翻译 key；本项目同一条 composer status strip 上，Tasks/Subagents/Edits tabs 已经有 fallback，但 Git branch chip 仍直接用 `t('chat.layout.inputStatusGitBranch')` 拼 title、aria-label 和可见 label。SSR、测试或 i18n 未就绪时，它会宣布 `chat.layout.inputStatusGitBranch <branch>`。
- 本轮差距诊断（超越 cc-gui）：本项目把 Git branch、工具活动、subagent、edit 状态统一压缩到 composer 上方。把 Git chip 也纳入同一套 i18n 边界兜底，可以解决“同一 status strip 里 tabs 已经可读，Git chip 却仍是 key”的断层，让用户不用打开任何面板也能知道当前分支上下文。
- 本轮规划：只修 `ChatInputStatusTabs` 的 Git branch chip label 派生：复用现有 `translateWithFallback()`，翻译存在时使用 i18n，翻译返回 key 时用 `Git`；用于可见 label、`title="Git: <branch> · <root>"` 和 `aria-label="Git <branch>"`。不改 Git branch 数据来源、不改布局、不改 tabs/popover、不改后端。优先级中等：用户价值中（分支上下文入口语义完整）、出现频率中（Git workspace 中持续可见）、实现成本很低（复用 helper + 测试）、风险低（只改变 i18n 边界文本）。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.test.tsx` 扩展 Git chip SSR 回归，先 RED 锁定旧实现输出 `chat.layout.inputStatusGitBranch`，后 GREEN 锁定 `aria-label="Git feature/very-long-chat-ui-parity-branch-name"` 和 `title="Git: feature/very-long-chat-ui-parity-branch-name · C:/repo"`，并断言不再包含 key。`src/components/chat/ChatInputStatusTabs.tsx` 新增 `gitBranchLabel = translateWithFallback('chat.layout.inputStatusGitBranch', 'Git')`，并用于 Git chip 可见 label、title 与 aria-label。`.trellis/spec/frontend/component-guidelines.md` 已同步：composer status strip 的 non-tab chips 也必须有 i18n key 兜底，Git branch chip 不应暴露 `inputStatusGitBranch`。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 1 项，失败点为旧 HTML 仍输出 `aria-label="chat.layout.inputStatusGitBranch feature/very-long-chat-ui-parity-branch-name"`，符合预期。GREEN：同命令通过（1 file / 18 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 74 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 345 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored；并行执行时出现 package cache/build directory lock 等待但最终通过）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面点测 Git branch chip 的 hover title、可见 label 和 Accessibility tree；未验证真实 i18n 初始化后的中英文渲染；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，在 Git workspace 和非 Git workspace 分别点测 composer status strip：Git chip、tabs、rows 的可见文本、title、keyboard focus 和 Accessibility tree 是否一致；2) 做右侧 status panel 与输入区 fallback 的 Accessibility tree 对比，确认 branch、tab、task row、subagent row、edit row、diff pane root 没有语义断层；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、输入区 fallback、Git/context 状态、右栏 edit tree、hover preview 可读性和 central diff pane action labels；4) 如果实机发现 status strip 的 Git chip 与 tab labels 播报过长，可基于 Accessibility tree / screen reader 实测收敛文案，但不要回退到裸 key。

## 本轮 PLAN 2026-06-20 input status popover region fallback label

- 目标：补齐输入区 `ChatInputStatusTabs` 弹出详情面板 `role="region"` 的 i18n 未就绪兜底。上一轮已修复同一 status strip 上的 tabs、rows 和 Git chip，但打开后的可聚焦 popover region 仍直接使用 `t('chat.layout.inputStatusDetailsRegion')`；SSR/测试或 i18n 未就绪时会宣布翻译 key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis task artifacts、前端规范、shared guides；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ChatInputStatusTabs.tsx` / test 与 cc-gui `StatusPanel` 相关代码，确认 cc-gui 没有同等 composer fallback region，本轮属于本项目小屏/右栏隐藏体验的超越项。
- 功能点 3：补 RED 测试。验证方式：更新 `src/components/chat/ChatInputStatusTabs.test.tsx`，断言打开的 popover region 输出 `aria-label="Status details"`，并不再包含 `chat.layout.inputStatusDetailsRegion`；旧实现应因输出 key 失败。
- 功能点 4：实现最小可访问性改动。验证方式：`ChatInputStatusTabs` 复用现有 `translateWithFallback()` 派生 `statusDetailsRegionLabel`，用于 popover region 的 `aria-label`。不改 popover 定位、dismiss、tab 状态、rows、Git chip、右侧 `StatusPanel` 或后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 composer/input status fallback 的可聚焦 popover region 也必须有可读兜底标签。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `ChatInputStatusTabs` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 input status popover region fallback label

- 上轮进展与阻塞：上一轮已补齐输入区 Git branch chip 的 i18n 未就绪兜底；代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已用 `gitBranchLabel = translateWithFallback('chat.layout.inputStatusGitBranch', 'Git')` 生成可见 label、title 和 `aria-label`，测试与前端规范也已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的 `webview/src/components/StatusPanel/StatusPanel.tsx` 使用右侧 status panel tabs 打开 todo/subagent/file changes popover，入口和内容来自真实列表，不会在正常路径暴露本项目的 `chat.layout.inputStatusDetailsRegion`。本项目的 composer status strip 是自有小屏/右栏隐藏 fallback；tabs、rows、Git chip 已经有 fallback，但打开后的可聚焦 `role="region"` 仍直接用 `t('chat.layout.inputStatusDetailsRegion')`，在 SSR/测试或 i18n 未就绪边界会宣布 key。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 多了 composer 上方的 task/subagent/edit/MCP quick-detail surface，能在小屏或右栏隐藏时保持状态可达。给可聚焦 popover region 加可读兜底，解决“用户已经打开详情面板并 tab 进滚动容器时，读屏只听到翻译 key”的痛点，使 fallback surface 从入口到内容容器都具备完整语义。
- 本轮规划：只修 `ChatInputStatusTabs` 的 popover region label 派生：翻译存在时继续使用 i18n，翻译返回 key 时兜底为 `Status details`；不改 popover 定位、dismiss、tab 状态、rows、Git chip、右侧 `StatusPanel` 或后端。优先级中等：用户价值中（焦点进入详情容器时语义完整）、出现频率中（任何打开 quick-detail 并键盘滚动的场景）、实现成本很低（复用 helper + 测试）、风险低（只改 i18n 边界文本）。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.test.tsx` 扩展 popover SSR 回归，先 RED 锁定旧实现输出 `aria-label="chat.layout.inputStatusDetailsRegion"`，后 GREEN 锁定 `aria-label="Status details"` 并断言不再包含 key。`src/components/chat/ChatInputStatusTabs.tsx` 新增 `statusDetailsRegionLabel = translateWithFallback('chat.layout.inputStatusDetailsRegion', 'Status details')`，并用于 open popover region 的 `aria-label`。`.trellis/spec/frontend/component-guidelines.md` 已同步：composer/input status fallback 的可聚焦 popover region label 也必须有可读 i18n 兜底。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 1 项，失败点为旧 HTML 仍输出 `aria-label="chat.layout.inputStatusDetailsRegion"`，符合预期。GREEN：同命令通过（1 file / 18 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 74 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 345 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面点测 popover region 的键盘 focus、滚动、Escape dismiss 与 Accessibility tree；未用屏幕阅读器验证实际播报顺序；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，在桌面宽度和小屏宽度分别点测 composer status strip：Git chip、tabs、popover region、task/subagent/edit rows 的 keyboard focus、title、Escape dismiss 和 Accessibility tree 是否一致；2) 做右侧 `StatusPanel` 与输入区 fallback 的 Accessibility tree 对比，确认 branch、tab、region、task row、subagent row、edit row、diff pane root 没有语义断层；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、输入区 fallback、Git/context 状态、右栏 edit tree、hover preview 可读性和 central diff pane action labels；4) 如果实机发现 region label 与 tab/row label 组合造成重复播报，可基于 Accessibility tree / screen reader 实测收敛文案，但不要回退到裸 key。

## 本轮 PLAN 2026-06-20 input status row state fallback labels

- 目标：补齐输入区 `ChatInputStatusTabs` quick-detail rows 的状态 pill 文本兜底。上一轮已修复 popover region label，但同一个面板内的 row 状态仍直接使用 `t('tools.pending')` / `t('tools.failed')` / `t('common.success')`；SSR/测试或 i18n 未就绪时会把 `tools.pending`、`common.success` 直接显示给用户。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis task artifacts、前端规范、shared guides；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ChatInputStatusTabs.tsx` / test 与 cc-gui `StatusPanel` 相关代码，确认本项目 composer fallback rows 的 visible state pill 仍缺 i18n fallback。
- 功能点 3：补 RED 测试。验证方式：更新 `src/components/chat/ChatInputStatusTabs.test.tsx`，断言 completed / pending / error rows 分别输出 `Success` / `Pending` / `Failed`，并不再包含 `common.success`、`tools.pending`、`tools.failed`；旧实现应因输出 key 失败。
- 功能点 4：实现最小展示与可访问性改动。验证方式：`ChatInputStatusTabs` 复用现有 `translateWithFallback()` 派生 row 状态文本。翻译存在时保留 i18n；翻译返回 key 时使用英文状态。只改 row state pill 文本，不改 status class、icon、row label、popover 行为、右侧 `StatusPanel` 或后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 composer/input status fallback rows 的 visible state pill 也不能暴露 i18n key。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `ChatInputStatusTabs` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 input status row state fallback labels

- 上轮进展与阻塞：上一轮已补齐输入区 popover region 的 i18n 未就绪兜底；代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已用 `statusDetailsRegionLabel = translateWithFallback('chat.layout.inputStatusDetailsRegion', 'Status details')`，测试与前端规范也已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的 `StatusPanel` 通过真实 tab 和列表展示 task/subagent/file 状态，不会在运行态用户可见文本中显示本项目的 i18n key。本项目 composer fallback 面板里，tab、row action label、Git chip 和 region label 已补齐 fallback，但 row 内 `tool-state-pill` 仍直接显示 `t('tools.pending')` / `t('tools.failed')` / `t('common.success')`；在 SSR、测试或 i18n 未就绪边界会把 `tools.pending`、`tools.failed`、`common.success` 作为可见状态文本。
- 本轮差距诊断（超越 cc-gui）：本项目把 task/subagent/edit/MCP 状态压缩进 composer 上方 quick-detail surface，适合小屏和右栏隐藏场景。状态 pill 是用户扫描“哪个工具正在跑、哪个失败、哪个完成”的第一视觉信号；给它补 `Pending` / `Failed` / `Success` fallback，可以解决“入口和动作标签都可读，但状态本身仍是 key”的断层。
- 本轮规划：只修 `ChatInputStatusTabs` 的 row state pill 文本派生：翻译存在时继续使用 i18n，翻译返回 key 时兜底为 `Pending` / `Failed` / `Success`。不改 status class、图标、row accessible label、popover dismiss、右侧 `StatusPanel` 或后端。优先级中等：用户价值中（状态扫描可读）、出现频率中（任何打开 tasks/subagents 面板时都会看到）、实现成本很低（复用 helper + 测试）、风险低（只改 i18n 边界文本）。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.test.tsx` 新增 row state fallback 回归，先 RED 锁定旧实现输出 `common.success`、`tools.failed`、`tools.pending`，后 GREEN 锁定 completed / error / pending rows 分别输出 `Success` / `Failed` / `Pending`。`src/components/chat/ChatInputStatusTabs.tsx` 将 `statusLabel()` 改为复用 `translateWithFallback()`。`.trellis/spec/frontend/component-guidelines.md` 已同步：composer/input status fallback rows 的 visible state pill 也必须有 readable fallback，不能暴露 i18n key。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 1 项，失败点为旧 HTML 中状态 pill 仍输出 `common.success`、`tools.failed`、`tools.pending`，符合预期。GREEN：同命令通过（1 file / 19 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 75 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 346 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面点测 quick-detail rows 的可见状态文本、hover title、Accessibility tree 与中英文真实 i18n 渲染；未用屏幕阅读器验证状态 pill 在 row label 之后是否有重复播报；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，在桌面宽度和小屏宽度分别点测 composer status strip：Git chip、tabs、popover region、row state pills、task/subagent/edit rows 的 keyboard focus、title、Escape dismiss 和 Accessibility tree 是否一致；2) 做右侧 `StatusPanel` 与输入区 fallback 的 Accessibility tree 对比，确认 branch、tab、region、state pill、task row、subagent row、edit row、diff pane root 没有语义断层；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、输入区 fallback、Git/context 状态、右栏 edit tree、hover preview 可读性和 central diff pane action labels；4) 如果实机发现 state pill 和 row label 组合造成重复播报，可基于 Accessibility tree / screen reader 实测决定是否把 pill 标记为纯视觉状态或收敛 row label。

## 本轮 PLAN 2026-06-20 input status mcp row fallback labels

- 目标：补齐输入区 `ChatInputStatusTabs` MCP quick-detail server rows 的可见 metadata/state 文本兜底。上一轮已修复 task/subagent row state pill，但同一 composer fallback 面板内的 MCP server row 仍直接使用 `t('chat.layout.mcpLiveUnknown')` / `t('chat.layout.mcpEnabled')` / `t('chat.layout.mcpDisabled')`；SSR、测试或 i18n 未就绪时会把这些 key 显示给用户。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ChatInputStatusTabs.tsx` / test 与 cc-gui `StatusPanel` / MCP 相关代码，确认本项目 composer fallback MCP rows 是自有小屏/右栏隐藏体验，且仍缺 i18n fallback。
- 功能点 3：补 RED 测试。验证方式：更新 `src/components/chat/ChatInputStatusTabs.test.tsx`，构造没有 `transport` 的 enabled/disabled MCP servers，断言输出 `Unknown` / `Enabled` / `Disabled`，并不再包含 `chat.layout.mcpLiveUnknown`、`chat.layout.mcpEnabled`、`chat.layout.mcpDisabled`；旧实现应因输出 key 失败。
- 功能点 4：实现最小展示与可访问性改动。验证方式：`ChatInputStatusTabs` 复用现有 `translateWithFallback()` 派生 MCP unknown transport、enabled、disabled 标签。翻译存在时保留 i18n；翻译返回 key 时使用英文兜底。只改 MCP row 文本，不改 MCP 数据模型、live check、popover 行为、右侧 `StatusPanel` 或后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 composer/input status fallback 的 MCP server rows 也不能暴露 i18n key。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `ChatInputStatusTabs` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 input status mcp row fallback labels

- 上轮进展与阻塞：上一轮已补齐输入区 task/subagent quick-detail rows 的状态 pill 文本兜底；代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已用 `translateWithFallback()` 输出 `Pending` / `Failed` / `Success` fallback，测试与前端规范也已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 的右侧状态入口覆盖 tasks/subagents/file changes，不暴露本项目 composer fallback 的 MCP server row key；cc-gui MCP 配置界面也以真实 server card 展示状态。本项目 composer fallback 已补齐 Git、tabs、region、task/subagent row 状态，但 MCP server row 仍可能在 i18n 未就绪边界显示 `chat.layout.mcpLiveUnknown`、`chat.layout.mcpEnabled`、`chat.layout.mcpDisabled`。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 多了 composer 上方 MCP quick-detail surface，用于小屏或右栏隐藏时快速查看配置可用性。MCP row 的 transport 和 enabled/disabled pill 是用户判断“这个 MCP 服务是否可用、属于什么连接方式”的核心扫描信息；给它补 `Unknown` / `Enabled` / `Disabled` fallback，可以解决“状态面板入口可读，但 MCP 行内部仍是 key”的断层。
- 本轮规划：只修 `ChatInputStatusTabs` 的 MCP server row 文本派生：翻译存在时继续使用 i18n，翻译返回 key 时兜底为 `Unknown` / `Enabled` / `Disabled`。不改 MCP 数据模型、不改 live connectivity check、不改 popover 行为、不改右侧 `StatusPanel`、不改后端。优先级中等：用户价值中（MCP 配置可用性可扫描）、出现频率中（MCP 已配置且打开 quick-detail 时）、实现成本很低（复用 helper + 测试）、风险低（只改 i18n 边界文本）。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.test.tsx` 新增 MCP row fallback 回归，用独立空 i18n provider 模拟翻译返回 key 的边界，并构造 `transport: null` 的 enabled/disabled MCP servers，先 RED 锁定旧实现暴露 MCP key，后 GREEN 锁定输出 `Unknown` / `Enabled` / `Disabled`。`src/components/chat/ChatInputStatusTabs.tsx` 新增 `mcpUnknownTransportLabel`、`mcpEnabledLabel`、`mcpDisabledLabel` 并用于 MCP server row。`.trellis/spec/frontend/component-guidelines.md` 已同步：composer/input status fallback 的 MCP server row metadata/state labels 也必须有 readable fallback，不能暴露 MCP i18n key。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 1 项，失败点为旧 HTML 中 MCP row 仍输出 `chat.layout.mcpLiveUnknown`、`chat.layout.mcpEnabled`、`chat.layout.mcpDisabled`，符合预期。GREEN：同命令通过（1 file / 20 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 76 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 347 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建首次因测试夹具使用 `transport: undefined` 失败，根因是 `ChatMcpAvailabilityServerSummary.transport` 类型为 `string | null`；已改为真实边界 `null` 后 `npm run build` 通过。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面点测 MCP quick-detail rows 的可见文本、hover title、keyboard focus 与 Accessibility tree；未用屏幕阅读器验证 MCP row 的实际播报；未验证真实中英文 i18n 渲染截图；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 继续收敛 MCP quick-detail 面板剩余 i18n 边界，例如 `mcpConfiguredServers`、`mcpLoading`、`mcpNoServers`、`inputStatusMoreMcpServers`，确保空 i18n provider 下整个 MCP panel 不暴露 key；2) 启动真实 Tauri 桌面，在 MCP 已配置、loading、error、无 servers 场景分别点测 composer status strip 的 keyboard focus、title、Escape dismiss 和 Accessibility tree；3) 做右侧 `StatusPanel` 与输入区 fallback 的 Accessibility tree 对比，确认 branch、tab、region、state pill、MCP row、task row、subagent row、edit row、diff pane root 没有语义断层；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、输入区 fallback、MCP 配置状态、Git/context 状态、右栏 edit tree、hover preview 可读性和 central diff pane action labels。

## 本轮 PLAN 2026-06-20 input status mcp panel fallback labels

- 目标：继续收敛输入区 `ChatInputStatusTabs` MCP quick-detail panel 的 i18n 未就绪边界。上一轮已修复 MCP server row 的 `Unknown` / `Enabled` / `Disabled` fallback，但同一 panel 中 `mcpConfiguredServers`、`mcpLoading`、`mcpNoServers`、`inputStatusMoreMcpServers` 仍直接使用 `t()`；SSR、测试或 i18n 未初始化时仍会把 key 显示给用户。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ChatInputStatusTabs.tsx` / test 与 cc-gui `StatusPanel` / MCP 配置相关代码，确认本项目 composer fallback MCP panel 是自有小屏/右栏隐藏体验，且 panel header/loading/empty/more 文本仍缺 i18n fallback。
- 功能点 3：补 RED 测试。验证方式：更新 `src/components/chat/ChatInputStatusTabs.test.tsx`，用空 i18n provider 覆盖 configured servers header、loading、empty、more servers 四类 MCP panel 文本，断言输出 `Configured servers`、`Loading MCP status...`、`No MCP servers configured`、`+1 more MCP server`，并不再包含对应 i18n key；旧实现应因输出 key 失败。
- 功能点 4：实现最小展示改动。验证方式：`ChatInputStatusTabs` 复用现有 `translateWithFallback()` 派生 MCP panel 文本。翻译存在时保留 i18n；翻译返回 key 时使用英文兜底。只改 MCP panel 可见文案，不改 MCP 数据模型、live check、popover 行为、右侧 `StatusPanel` 或后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 composer/input status fallback 的 MCP panel header/loading/empty/more 文本也不能暴露 i18n key。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `ChatInputStatusTabs` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 input status mcp panel fallback labels

- 上轮进展与阻塞：上一轮已补齐输入区 MCP server row 的 `Unknown` / `Enabled` / `Disabled` fallback；代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已新增 `mcpUnknownTransportLabel`、`mcpEnabledLabel`、`mcpDisabledLabel` 并用于 server row，测试与前端规范也已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 的状态入口不会暴露本项目 composer fallback 的 MCP panel key；cc-gui MCP 配置卡片也以真实文案展示配置和状态。本项目上一轮只修了 MCP server row，但同一 panel 的 header、loading、empty、more row 仍可能显示 `chat.layout.mcpConfiguredServers`、`chat.layout.mcpLoading`、`chat.layout.mcpNoServers`、`chat.layout.inputStatusMoreMcpServers`。
- 本轮差距诊断（超越 cc-gui）：本项目的 composer MCP quick-detail panel 是小屏/右栏隐藏时的快速配置状态入口。把 panel chrome 与 server row 一起纳入 fallback，可以解决“服务器行已经可读，但 panel 顶部/空态/加载态/更多提示仍是 key”的体验断层，让用户在 MCP configured、loading、error/empty、overflow 场景都能读懂状态。
- 本轮规划：只修 `ChatInputStatusTabs` 的 MCP panel 文本派生：翻译存在时继续使用 i18n，翻译返回 key 时兜底为 `Configured servers`、`Loading MCP configuration...`、`No MCP servers configured`、`+N more MCP server(s)`。不改 MCP 数据模型、不改 live connectivity check、不改 popover 行为、不改右侧 `StatusPanel`、不改后端。优先级中等：用户价值中（完整覆盖 MCP fallback panel 的可读性）、出现频率中（MCP configured/loading/error/overflow 时）、实现成本很低（复用 helper + 测试）、风险低（只改 i18n 边界文本）。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.test.tsx` 新增 MCP panel fallback 回归，用空 i18n provider 分别覆盖 configured servers header、loading、error+empty 和 overflow more row，先 RED 锁定旧实现暴露 panel 级 MCP key，后 GREEN 锁定可读 fallback。`src/components/chat/ChatInputStatusTabs.tsx` 新增 `mcpLoadingLabel`、`mcpConfiguredServersLabel`、`mcpNoServersLabel`，并在 `renderMcpPanel()` 中按 `hiddenMcpServerCount` 生成 more row fallback。`.trellis/spec/frontend/component-guidelines.md` 已同步：composer/input status fallback 的 MCP panel chrome 与 server row labels 都必须有 readable fallback，不能暴露 MCP i18n key。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 1 项，失败点为旧 HTML 中 MCP panel header 仍输出 `chat.layout.mcpConfiguredServers`，同一测试覆盖 loading/empty/more key，符合预期。GREEN：同命令通过（1 file / 21 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 77 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 348 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面点测 MCP configured/loading/error/empty/overflow 场景的 visible text、keyboard focus、Escape dismiss 与 Accessibility tree；未用屏幕阅读器验证 MCP panel 实际播报；未验证真实中英文 i18n 渲染截图；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，在 MCP 已配置、loading、error、无 servers、超过 8 个 servers 场景分别点测 composer status strip 的 keyboard focus、title、Escape dismiss、滚动和 Accessibility tree；2) 做右侧 `StatusPanel` 与输入区 fallback 的 Accessibility tree 对比，确认 branch、tab、region、state pill、MCP panel、MCP row、task row、subagent row、edit row、diff pane root 没有语义断层；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、输入区 fallback、MCP 配置状态、Git/context 状态、右栏 edit tree、hover preview 可读性和 central diff pane action labels；4) 如果继续做代码级小步优化，优先检查 tasks/subagents/edits panel 的 more/no-data 文本是否还有同类 i18n key 泄漏，但应避免在未做真实桌面验证前继续扩大展示内容。

## 本轮 PLAN 2026-06-20 input status overflow footer fallback labels

- 目标：补齐输入区 `ChatInputStatusTabs` Tasks/Subagents/Edits quick-detail panel 的 overflow footer 文本兜底。MCP panel 已收敛，但长会话下 task/subagent/edit 列表超过 8 条时，footer 仍直接使用 `t('chat.layout.inputStatusMoreTools')` / `inputStatusMoreSubagents` / `inputStatusMoreEdits`；SSR、测试或 i18n 未初始化时会把 key 显示给用户。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取本项目 `ChatInputStatusTabs.tsx` / test，确认 tabs、rows、state pills、MCP panel 已有 fallback，但 task/subagent/edit overflow footer 仍缺 fallback。
- 功能点 3：补 RED 测试。验证方式：更新 `src/components/chat/ChatInputStatusTabs.test.tsx`，用空 i18n provider 分别构造 9 条 tasks、9 条 subagents、9 条 edits，断言 footer 输出 `+1 more tool task`、`+1 more subagent`、`+1 more edit`，并不再包含对应 i18n key；旧实现应因输出 key 失败。
- 功能点 4：实现最小展示改动。验证方式：`ChatInputStatusTabs` 复用现有 `translateWithFallback()` 派生 task/subagent/edit more footer 文本。翻译存在时保留 i18n；翻译返回 key 时使用英文兜底。只改 footer 文本，不改 `MAX_PANEL_ITEMS`、列表排序、popover 打开/关闭、右侧 `StatusPanel` 或后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 composer/input status fallback 的 task/subagent/edit overflow footer 也不能暴露 i18n key。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `ChatInputStatusTabs` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 input status overflow footer fallback labels

- 上轮进展与阻塞：上一轮已补齐输入区 MCP quick-detail panel 的 header/loading/empty/more 文本兜底；代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已新增 `mcpLoadingLabel`、`mcpConfiguredServersLabel`、`mcpNoServersLabel`，并按 `hiddenMcpServerCount` 生成 more row fallback，测试与前端规范也已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的右侧状态入口不会在长列表 footer 里暴露本项目 composer fallback 的 i18n key。本项目输入区 quick-detail 已补齐 tabs、rows、state pills、Git、region、MCP panel，但 tasks/subagents/edits 面板超过 8 条时的 overflow footer 仍可能显示 `chat.layout.inputStatusMoreTools`、`chat.layout.inputStatusMoreSubagents`、`chat.layout.inputStatusMoreEdits`。
- 本轮差距诊断（超越 cc-gui）：本项目把 tasks/subagents/edits 压缩到输入区 fallback popover，适合小屏和右栏隐藏场景。长会话中 footer 是用户判断“还有多少未显示活动”的唯一提示；给它补 `+N more tool task(s)`、`+N more subagent(s)`、`+N more edit(s)` fallback，可以解决“主要行可读，但底部剩余量提示仍是 key”的断层。
- 本轮规划：只修 `ChatInputStatusTabs` 的 task/subagent/edit overflow footer 文本派生：翻译存在时继续使用 i18n，翻译返回 key 时兜底为 `+N more tool task(s)` / `+N more subagent(s)` / `+N more edit(s)`。不改 `MAX_PANEL_ITEMS`、不改排序、不改 popover 行为、不改右侧 `StatusPanel`、不改后端。优先级中等：用户价值中（长会话剩余量可读）、出现频率中（超过 8 条时）、实现成本低（复用 helper + 测试）、风险低（只改 i18n 边界文本）。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.test.tsx` 新增 overflow footer fallback 回归，用空 i18n provider 分别构造 9 条 tasks、9 条 subagents、9 条 edits，先 RED 锁定旧实现暴露 `inputStatusMore*` key，后 GREEN 锁定可读 fallback。`src/components/chat/ChatInputStatusTabs.tsx` 新增 `formatMoreStatusItemsLabel()`、`hiddenTaskCount`、`hiddenSubagentCount`、`hiddenEditCount`，并为三个 footer 使用 `translateWithFallback()`。`.trellis/spec/frontend/component-guidelines.md` 已同步：composer/input status fallback 的 tasks/subagents/edits overflow footer 不能暴露 i18n key。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 1 项，失败点为旧 HTML 中 tasks footer 输出 `chat.layout.inputStatusMoreTools`，同一测试覆盖 subagents/edits footer，符合预期。GREEN：同命令通过（1 file / 22 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 78 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 349 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面点测超过 8 条 tasks/subagents/edits 时 footer 的可见文本、滚动位置、keyboard focus 与 Accessibility tree；未用屏幕阅读器验证 footer 在滚动区域末尾的实际播报；未验证真实中英文 i18n 渲染截图；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造超过 8 条 tasks/subagents/edits 的长会话，点测输入区 quick-detail popover 的滚动、footer 可见性、Escape dismiss、row jump 后焦点 handoff 与 Accessibility tree；2) 做右侧 `StatusPanel` 与输入区 fallback 的 Accessibility tree 对比，确认 branch、tab、region、state pill、overflow footer、MCP panel、task row、subagent row、edit row、diff pane root 没有语义断层；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 file changes 操作密度、输入区 fallback、长列表 footer、MCP 配置状态、Git/context 状态、右栏 edit tree、hover preview 可读性和 central diff pane action labels；4) 若继续小步代码优化，优先审查同一输入区 fallback 中少数理论空态 `inputStatusNoTasks` / `inputStatusNoSubagents` / `inputStatusNoEdits` 是否存在实际可达路径，再决定是否修，不要为不可达分支增加测试噪声。

## 本轮 PLAN 2026-06-20 status panel mcp fallback labels

- 目标：补齐右侧桌面 `StatusPanel` MCP 摘要与展开详情的 i18n 未就绪兜底。输入区 MCP quick-detail 已经避免暴露 key，但桌面主状态面板仍直接使用 `mcpEnabledSummary`、`mcpLoading`、`mcpConfiguredServers`、`mcpNoServers`、`mcpEnabled`、`mcpDisabled`、live status 与 check hint/button 等 key；SSR、测试或 i18n 未初始化时会把 key 显示给用户。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `ChatInputStatusTabs.tsx`、`StatusPanel.tsx`、`StatusPanel.test.tsx`；确认 `inputStatusNoTasks` / `inputStatusNoSubagents` / `inputStatusNoEdits` 受 `visibleTabs` / `activeOpenTab` 保护，当前基本不可达；本轮不为不可达空态增加测试噪声，转向真实可见的 `StatusPanel` MCP key 泄漏。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，用空 i18n provider 渲染 MCP loading、empty、server enabled/disabled、live online、manual check 等场景，断言输出可读英文 fallback 且不包含对应 i18n key；旧实现应失败。
- 功能点 4：实现最小展示改动。验证方式：在 `StatusPanel` 内新增局部 `translateWithFallback()` 与 MCP 专用 label 派生，翻译存在时继续使用 i18n，翻译返回 key 时使用英文兜底。只改展示文本，不改 MCP 数据模型、live connectivity check、展开逻辑、输入区 fallback 或后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录桌面 `StatusPanel` MCP summary/details 与 composer MCP fallback 一样不能暴露 raw i18n key。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `StatusPanel` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 status panel mcp fallback labels

- 上轮进展与阻塞：上一轮已补齐输入区 `ChatInputStatusTabs` Tasks/Subagents/Edits quick-detail panel 的 overflow footer fallback；代码复查确认 `formatMoreStatusItemsLabel()`、`hiddenTaskCount` / `hiddenSubagentCount` / `hiddenEditCount` 和三个 footer 的 `translateWithFallback()` 已存在，测试与前端规范也已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：CodeGraph 复查确认 `inputStatusNoTasks` / `inputStatusNoSubagents` / `inputStatusNoEdits` 受 `visibleTabs` 与 `activeOpenTab` 保护，当前基本不可达，因此本轮未为这些死分支增加测试噪声。真实可见差距在桌面 `StatusPanel` MCP summary/details：输入区 MCP fallback 已能避免 key 泄漏，但右侧主状态面板仍直接使用 `mcpEnabledSummary`、`mcpLoading`、`mcpConfiguredServers`、`mcpEnabled`、`mcpDisabled`、live status/latency 与 check button 文案。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 多了桌面右栏 + 输入区 fallback 双 MCP 状态入口。让两处遵守同一 fallback 规则，可以解决“输入区 MCP 已可读，但桌面主面板在 i18n 未初始化时仍显示 raw key”的断层；尤其在 loading、配置为空、手动检测、live result 场景中，用户仍能读到 `1 / 2 available`、`Configured servers`、`Online`、`Checking...` 等真实状态。
- 本轮规划：只修 `StatusPanel` 的 MCP 文本派生：新增局部 `translateWithFallback()`，为 MCP 标题、summary、loading、configured/empty、enabled/disabled、manual check、checking/check live、live status 和 latency 提供英文 fallback。翻译存在时保留现有 i18n；不改 MCP 数据模型、不改 live connectivity check、不改展开逻辑、不改输入区 fallback、不改后端。优先级中等偏高：用户价值中高（桌面主状态面板可读性）、出现频率中（MCP 配置/加载/检测时）、实现成本低、风险低。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 新增 key-only i18n SSR 回归，先 RED 确认旧实现输出 `chat.layout.mcpEnabledSummary`、`chat.layout.mcpLoading`、`chat.layout.mcpManualCheckHint`、`chat.layout.mcpLiveLatency` 等 key，后 GREEN 锁定 `MCP`、`1 / 2 available`、`Loading MCP configuration...`、`Configured servers`、`Enabled`、`Disabled`、`Online`、`24ms`、`Checking...` 与空配置 `No MCP servers configured`。`src/components/chat/StatusPanel.tsx` 新增局部 fallback helper 与 MCP label 派生，并替换 MCP summary/details 的直接 `t()` 输出。`.trellis/spec/frontend/component-guidelines.md` 已同步：桌面 `StatusPanel` MCP summary/details 与 composer MCP fallback 使用同一 readable fallback 规则。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为旧 HTML 中 MCP 区域输出 raw i18n key，符合预期。GREEN：同命令通过（1 file / 27 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 79 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 350 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面点测右侧 `StatusPanel` MCP loading/configured/empty/live-check 场景的可见文本、按钮 title、keyboard focus、展开/收起与 Accessibility tree；未用屏幕阅读器验证实际播报；未验证真实中英文 i18n 截图；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，分别构造 MCP loading、error、无 servers、enabled/disabled servers、live check online/offline/timeout/error 场景，点测右侧 `StatusPanel` 与输入区 fallback 的一致性、keyboard focus、button title 和 Accessibility tree；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 MCP 配置状态、Git/context 状态、输入区 fallback、右栏 edit tree、hover preview 和 central diff pane action labels；3) 如继续代码级小步优化，优先检查右侧 `StatusPanel` activity task/subagent rows 是否也需要 key-only i18n fallback，与输入区 task/subagent jump rows 保持一致；4) 对 `inputStatusNoTasks` / `inputStatusNoSubagents` / `inputStatusNoEdits` 只记录为当前不可达分支，除非未来打开默认 tab 逻辑变化，否则不建议继续投入。

## 本轮 PLAN 2026-06-20 status panel activity fallback labels

- 目标：补齐右侧桌面 `StatusPanel` activity task/subagent rows 的 i18n 未就绪兜底。输入区 task/subagent rows 已经有 `Jump to tool task: <summary>` / `Jump to subagent activity: <summary>` fallback，但桌面右栏同类跳转行仍直接使用 `t(getActivityJumpLabelKey(...))` 和 `t('tools.pending'|'tools.failed'|'common.success')`；SSR、测试或 i18n 未初始化时会暴露 raw key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `StatusPanel.tsx`、`StatusPanel.test.tsx` 和输入区已完成的 `ChatInputStatusTabs.tsx` 对应 helper；确认右栏 activity rows 是真实可见桌面导航目标，且缺少 key-only fallback。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，用空 i18n provider 渲染 task/subagent activity rows，断言输出 `Jump to tool task: <summary>`、`Jump to subagent activity: <summary>`、`Pending`、`Failed`、`Success`，并不包含对应 raw key；旧实现应失败。
- 功能点 4：实现最小展示改动。验证方式：在 `StatusPanel` 内复用现有 `translateWithFallback()`，新增 activity jump fallback 与 tool status fallback；不改 activity 数据来源、不改点击跳转、不改滚动/高亮、不改输入区 fallback、不改后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录桌面 `StatusPanel` activity task/subagent rows 与 composer fallback rows 使用一致的 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `StatusPanel` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 status panel activity fallback labels

- 上轮进展与阻塞：上一轮已补齐右侧桌面 `StatusPanel` MCP summary/details 的 i18n 未就绪兜底；代码复查确认 `StatusPanel.tsx` 已用局部 `translateWithFallback()` 输出 `MCP`、`1 / 2 available`、`Loading MCP configuration...`、`Configured servers`、`Enabled`、`Disabled`、`Online`、`24ms`、`Checking...` 等 fallback，测试与前端规范也已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的状态/工具入口不会把内部翻译 key 暴露给用户；本项目输入区 task/subagent rows 已有 readable fallback，但桌面右侧 `StatusPanel` activity rows 仍直接使用 `t(getActivityJumpLabelKey(...))` 和 `t('tools.pending'|'tools.failed'|'common.success')`。在 SSR、测试或 i18n 未初始化时，右栏主导航面板会输出 `chat.layout.scrollToToolTask`、`chat.layout.scrollToSubagentActivity`、`tools.pending`、`tools.failed`、`common.success`。
- 本轮差距诊断（超越 cc-gui）：本项目把 task/subagent rows 做成右栏桌面导航目标和输入区 fallback 双入口。让两处使用同一 readable fallback，可以解决“输入区跳转语义完整，桌面右栏同类入口却仍是 key”的断层；同时保留 subagent 与普通 tool task 的语义区分，避免把子代理活动宣布成普通工具任务。
- 本轮规划：只修 `StatusPanel` activity row 的跳转 label 与状态 pill 文本派生：复用组件内 `translateWithFallback()`，翻译存在时继续使用 i18n，翻译返回 key 时兜底为 `Jump to tool task: <summary>`、`Jump to subagent activity: <summary>`、`Pending`、`Failed`、`Success`。不改 activity 数据来源、不改点击跳转、不改滚动/高亮、不改输入区 fallback、不改后端。优先级中等偏高：用户价值中高（桌面主导航入口语义完整）、出现频率中高（每个任务/子代理活动行都会出现）、实现成本低、风险低。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 新增 key-only i18n SSR 回归，先 RED 确认旧实现输出 `chat.layout.scrollToToolTask`、`chat.layout.scrollToSubagentActivity`、`tools.pending`、`tools.failed`、`common.success`，后 GREEN 锁定 `Jump to tool task: npm test -- StatusPanel`、`Jump to subagent activity: write regression coverage`、`Pending`、`Failed`、`Success`。`src/components/chat/StatusPanel.tsx` 已把 `getStatusLabel()` / `getToolStatusLabel()` 改为使用 `translateWithFallback()`，并新增 `getActivityJumpFallbackLabel()` / `getActivityJumpLabel()` 用于 activity row `aria-label`。`.trellis/spec/frontend/component-guidelines.md` 已同步：桌面 `StatusPanel` activity task/subagent rows 必须和 composer fallback rows 一样有 readable fallback。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为旧 HTML 中 activity rows 输出 raw i18n key，符合预期。GREEN：同命令通过（1 file / 28 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 80 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 351 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored；并行执行时出现 package cache/build directory lock 等待但最终通过）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面点测右侧 `StatusPanel` activity task/subagent rows 的 hover title、键盘跳转、transcript 高亮、Accessibility tree 与屏幕阅读器实际播报；未验证真实中英文 i18n 渲染截图；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造普通 tool task、subagent、pending/error/completed 状态混合的长会话，点测右侧 activity rows 的 keyboard focus、跳转后高亮、scroll region 和 Accessibility tree；2) 做右侧 `StatusPanel` 与输入区 fallback 的 Accessibility tree 对比，确认 task/subagent/edit/MCP/Git 入口没有语义断层；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看状态入口密度、任务/子代理导航、MCP 配置状态、右栏 edit tree、hover preview 和 central diff pane action labels；4) 若继续代码级小步优化，可检查右栏 activity section header、hidden-count row、region label 是否也需要 key-only fallback，但应避免无限追逐不可见分支，优先转向实机/Accessibility tree 验证。

## 本轮 PLAN 2026-06-20 status panel activity chrome fallback labels

- 目标：补齐右侧桌面 `StatusPanel` activity card 容器级文本的 i18n 未就绪兜底。上一轮已修 activity row 的跳转 label 和状态 pill，但 card 标题、pending/error 计数 title、scroll region label、Tasks/Subagents 小标题、隐藏计数 row 仍直接 `t()`；SSR、测试或 i18n 未初始化时会暴露 raw key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `StatusPanel.tsx` activity card 渲染段、现有 `StatusPanel.test.tsx` 和 cc-gui `StatusPanel/SubagentList`，确认本轮缺口是真实可见的桌面 activity chrome，而不是不可达空态。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx` 的 key-only i18n SSR 回归，断言 card chrome 输出 `Current activity`、`Pending tools`、`Failed tools`、`Activity history`、`Tasks`、`Subagents`、`1 earlier tools hidden`、`1 earlier subagents hidden`，并不包含对应 raw key；旧实现应失败。
- 功能点 4：实现最小展示改动。验证方式：在 `StatusPanel` 内复用现有 `translateWithFallback()`，新增局部 label/hidden-count 派生；不改 activity 数据来源、不改点击跳转、不改滚动/高亮、不改输入区 fallback、不改后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录桌面 `StatusPanel` activity card chrome 与 rows 一样必须有 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `StatusPanel` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 status panel activity chrome fallback labels

- 上轮进展与阻塞：上一轮已补齐右侧桌面 `StatusPanel` activity task/subagent rows 的跳转 label 与状态 pill fallback；代码复查确认 `getActivityJumpLabel()`、`getStatusLabel()`、`getToolStatusLabel()` 已避免 `scrollToToolTask` / `scrollToSubagentActivity` / `tools.pending` / `tools.failed` / `common.success` 泄漏。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的 `StatusPanel` / `SubagentList` 入口以真实任务、子代理和文件变更对象呈现，不会在入口 chrome 暴露内部翻译 key。本项目上一轮只修了 activity 行级语义，但本轮 CodeGraph 复查确认右侧 `StatusPanel` activity card 的标题、pending/error 计数 title、scroll region label、Tasks/Subagents 小标题、隐藏计数 row 仍直接 `t()`，在 key-only i18n 边界会显示 `chat.layout.currentActivity`、`chat.layout.inputStatusMoreTools` 等 raw key。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 多了桌面右栏与输入区 fallback 双入口，并且右栏 activity card 有可聚焦滚动区域和隐藏历史计数。让 card chrome 与 rows 一样保留 readable fallback，可以解决“行内容可读，但用户还没进入列表前听到的区域名和列表标题仍是 key”的断层，尤其影响键盘滚动与屏幕阅读器用户决定进入哪个状态区。
- 本轮规划：只修 `StatusPanel` activity card chrome 的文本派生：翻译正常时继续使用 i18n；翻译返回 key 时用 `Current activity`、`Pending tools`、`Failed tools`、`Activity history`、`Tasks`、`Subagents`、`<count> earlier tools/subagents hidden` 兜底。不改 activity 数据来源、不改点击跳转、不改滚动/高亮、不改输入区 fallback、不改后端。优先级中等偏高：用户价值中高（桌面主状态区域可读性）、出现频率中高（activity card 出现即触发）、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 新增 key-only i18n SSR 回归，先 RED 确认旧实现输出 `chat.layout.currentActivity`、`chat.layout.pendingTools`、`chat.layout.errorTools`、`chat.layout.activityHistoryRegion`、`chat.layout.inputStatusTasks`、`chat.layout.inputStatusSubagents`、`chat.layout.inputStatusMoreTools`、`chat.layout.inputStatusMoreSubagents`，后 GREEN 锁定 readable fallback。`src/components/chat/StatusPanel.tsx` 新增 activity chrome fallback label 派生并替换对应直接 `t()` 输出。`.trellis/spec/frontend/component-guidelines.md` 已同步：桌面 `StatusPanel` activity card chrome 必须和 activity rows 一样避免 raw i18n key。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为旧 HTML 中 activity card 标题输出 `chat.layout.currentActivity`，并可见 `inputStatusMoreTools` / `inputStatusMoreSubagents` key，符合预期。GREEN：同命令通过（1 file / 29 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 81 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 352 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面点测右侧 `StatusPanel` activity card 的 hover title、keyboard focus、滚动区域命名、隐藏计数可见性、Accessibility tree 与屏幕阅读器实际播报；未验证真实中英文 i18n 渲染截图；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造普通 tool task、subagent、pending/error/completed 状态混合且超过可见上限的长会话，点测右侧 activity card 的 keyboard focus、region label、隐藏计数、行跳转后高亮与 Accessibility tree；2) 做右侧 `StatusPanel` 与输入区 fallback 的 Accessibility tree 对比，确认 task/subagent/edit/MCP/Git/Activity chrome 没有语义断层；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看状态入口密度、任务/子代理导航、MCP 配置状态、右栏 edit tree、hover preview 和 central diff pane action labels；4) 若继续代码级小步优化，优先审查 `StatusPanel` runtime/session-load/daemon chrome 是否仍有真实可见 key-only fallback 缺口，但应控制范围，避免把低频诊断文字无限扩展成大批量重构。

## 本轮 PLAN 2026-06-20 status panel top chrome fallback labels

- 目标：补齐右侧桌面 `StatusPanel` 顶部基础状态与 runtime context 的 i18n 未就绪兜底。上一轮已修 activity card chrome，但最先出现的 `Session status`、Provider/Messages/Anchors/Daemon labels、daemon 状态/reconnect、runtime context 标题与 Model/Mode/Reasoning/Workspace/SDK tiles 仍直接 `t()`；SSR、测试或 i18n 未初始化时会暴露 raw key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `StatusPanel.tsx` 顶部状态/runtime 渲染段、`StatusPanel.test.tsx` 和 cc-gui `StatusPanel`，确认本轮缺口是高频真实可见 chrome，不混入低频 session-load 详情或 edit toolbar。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，用 key-only i18n provider 渲染顶部状态与 runtime context，断言输出 `Session status`、`AI provider`、`Messages`、`Message anchors`、`Daemon`、`Reconnect daemon`、`Runtime context`、`Model`、`Permission mode`、`Auto Mode`、`Reasoning effort`、`High`、`Workspace`、`SDK`、`Installed`，并不包含对应 raw key；旧实现应失败。
- 功能点 4：实现最小展示改动。验证方式：在 `StatusPanel` 内复用现有 `translateWithFallback()`，新增顶部状态和 runtime context label 派生；翻译存在时保留 i18n，翻译返回 key 时使用英文 fallback。不改 daemon 状态判断、不改 reconnect 行为、不改 runtime context 数据来源、不改 session-load/MCP/activity/edit/后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录桌面 `StatusPanel` 顶部基础状态和 runtime context chrome 也必须有 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `StatusPanel` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 status panel top chrome fallback labels

- 上轮进展与阻塞：上一轮已补齐右侧桌面 `StatusPanel` activity card chrome 的 i18n 未就绪兜底；代码复查确认 `StatusPanel.tsx` 已输出 `Current activity`、`Pending tools`、`Failed tools`、`Activity history`、`Tasks`、`Subagents`、`<count> earlier tools/subagents hidden` fallback，测试与前端规范也已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 的右侧入口以真实 tabs 与对象计数呈现，不会在主入口暴露本项目的 `chat.layout.statusPanel`、`chat.daemon.label` 等 key。本项目上一轮只修了 activity card，而最先出现在右栏顶部的 status chrome 与 runtime context tiles 仍直接使用 `t()`，在 key-only i18n 边界会显示 `chat.layout.statusPanel`、`chat.providerLabel`、`chat.daemon.offline`、`chat.modes.bypassPermissions.label`、`chat.reasoning.high.label`、`chat.sdk.installed` 等 raw key。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 多了 provider/model/mode/reasoning/workspace/SDK/daemon 的紧凑运行就绪面板，能在发送 prompt 前给出是否可用的判断。给这些高频 chrome 加 readable fallback，可以解决“activity/MCP 已可读，但用户进入右栏第一眼仍看到 key”的断层；尤其在 daemon 离线时，`Daemon offline` 与 `Reconnect daemon` 必须可读，否则用户无法快速识别恢复路径。
- 本轮规划：只修 `StatusPanel` 顶部基础状态与 runtime context label 派生：翻译正常时继续使用 i18n；翻译返回 key 时用 `Session status`、`AI provider`、`Messages`、`Message anchors`、`Daemon`、`Daemon offline`、`Reconnect daemon`、`Runtime context`、`Model`、`Permission mode`、`Auto Mode`、`Reasoning effort`、`High`、`Workspace`、`SDK`、`Installed` 兜底。不改 daemon 状态判断、不改 reconnect 行为、不改 runtime context 数据来源、不改 session-load/MCP/activity/edit/后端。优先级中等偏高：用户价值高（判断运行可用性和恢复路径）、出现频率高（右栏出现即触发）、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 新增 key-only i18n SSR 回归，先 RED 确认旧实现输出顶部 status/runtime raw key，后 GREEN 锁定 readable fallback 并断言不再包含这些 key。`src/components/chat/StatusPanel.tsx` 新增 permission/reasoning fallback 映射和顶部 status/runtime label 派生，并替换顶部状态、daemon reconnect、runtime tiles、SDK install state 的直接 `t()` 输出。`.trellis/spec/frontend/component-guidelines.md` 已同步：桌面 `StatusPanel` 顶部基础状态和 runtime context chrome 必须避免 raw i18n key。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为旧 HTML 中顶部标题输出 `chat.layout.statusPanel`，并可见 `chat.providerLabel`、`chat.daemon.offline`、`chat.modes.bypassPermissions.label`、`chat.reasoning.high.label`、`chat.sdk.installed`，符合预期。GREEN：同命令通过（1 file / 30 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 82 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 353 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面点测右侧 `StatusPanel` 顶部 status/runtime chrome 的可见文本、reconnect button title、keyboard focus、Accessibility tree 与屏幕阅读器实际播报；未验证真实中英文 i18n 渲染截图；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造 daemon ready/offline/error/reconnecting、SDK installed/not installed、不同 permission/reasoning 的状态组合，点测右侧 status/runtime chrome 的可见文本、button title、keyboard focus 和 Accessibility tree；2) 做右侧 `StatusPanel` 与输入区 fallback 的 Accessibility tree 对比，确认 top status、activity、MCP、edit、Git、popover region 没有语义断层；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看状态入口密度、运行就绪信息、任务/子代理导航、MCP 配置状态、右栏 edit tree、hover preview 和 central diff pane action labels；4) 若继续代码级小步优化，优先检查低频 `sessionLoadMetrics` 与 edit toolbar controls 是否还有 key-only fallback 缺口，但应继续单轮聚焦，避免把诊断文字一次性批量重写。

## 本轮 PLAN 2026-06-20 status panel edit chrome fallback labels

- 目标：补齐右侧桌面 `StatusPanel` recent edits / diff toolbar 的 i18n 未就绪兜底。上一轮已修顶部 status/runtime chrome，但文件变更入口的标题、汇总、diff tree 工具栏、split/unified view、reopen 按钮和隐藏文件按钮仍有直接 `t()`；SSR、测试或 i18n 未初始化时会暴露 raw key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、当前 Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `StatusPanel.tsx` recent edits / diff toolbar 渲染段、`StatusPanel.test.tsx`，以及 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx`，确认本轮缺口对应高频 File Changes 入口，而不是低频 session-load 诊断。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx` 的 key-only i18n SSR 回归，断言 recent edits 区输出 `Recent edits`、`2 files · +3 / -1`、`Collapse edit tree`、`Diff view mode`、`Unified diff view`、`Split diff view`、`Open diff review for src/components/chat/StatusPanel.tsx`、`1 more file`，并不包含对应 raw key；旧实现应失败。
- 功能点 4：实现最小展示改动。验证方式：在 `StatusPanel` 内复用现有 `translateWithFallback()`，新增 edit chrome label/summary/helper 派生；不改 edit 数据来源、不改树结构、不改点击选择/打开文件、不改 central diff pane、不改后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录桌面 `StatusPanel` recent edits / diff toolbar chrome 必须和行级标签一样避免 raw i18n key。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `StatusPanel` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 status panel edit chrome fallback labels

- 上轮进展与阻塞：上一轮已补齐右侧桌面 `StatusPanel` 顶部 status/runtime chrome 的 i18n 未就绪兜底；代码复查确认 `StatusPanel.tsx` 已输出 `Session status`、`AI provider`、`Daemon offline`、`Runtime context`、`Auto Mode`、`High`、`Installed` 等 fallback，测试与前端规范也已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 用 Tasks/Subagents/File Changes tabs 暴露状态入口，文件变更入口不会展示本项目的 `chat.layout.recentEdits` / `chat.layout.diffViewMode` / `tools.openFileForPath` 这类 raw key。本项目右侧 recent edits tree 已经在信息密度上超过 cc-gui，但 CodeGraph 复查确认标题、编辑汇总、树展开按钮、diff mode 组、unified/split 按钮、reopen diff review、文件夹/文件行 action、Open File 与 hidden files toggle 仍有直接 `t()`，key-only i18n 下会破坏文件变更主路径。
- 本轮差距诊断（超越 cc-gui）：本项目把 recent edits 做成可折叠文件树、hover diff preview、central diff pane handoff 和 reopen affordance，能力比 cc-gui 的 popover file changes 更强。补齐 chrome/action fallback 解决的是“文件行内容可读，但进入文件变更面板时工具栏和关键按钮仍是 key”的痛点，尤其影响键盘用户判断是展开树、切换 diff 模式、打开文件，还是恢复中央 diff review。
- 本轮规划：只修 `StatusPanel` recent edits / diff toolbar 的文案派生：翻译正常时继续使用 i18n；翻译返回 key 时用 `Recent edits`、`2 files · +3 / -1`、`Collapse edit tree`、`Diff view mode`、`Unified diff view`、`Split diff view`、`Current full diff: <file>`、`Open file: <file>`、`Open diff review for <file>`、`1 more file` 等英文 fallback。不改 edit 数据来源、不改树结构、不改点击选择/打开文件、不改 central diff pane、不改后端。优先级中等偏高：用户价值高（文件变更审阅主路径语义完整）、出现频率中高（只要有 edit history 就会出现）、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 新增 key-only i18n SSR 回归，先 RED 确认旧实现输出 `chat.layout.recentEdits`、`chat.layout.editSummary`、`chat.layout.collapseEditTree`、`chat.layout.diffViewMode`、`chat.layout.diffUnifiedView`、`chat.layout.diffSplitView`、`chat.layout.inspectCurrentFullDiff`、`tools.openFileForPath`、`chat.layout.expandDiffPanelForFile`、`chat.layout.hiddenEditFiles`，后 GREEN 锁定 readable fallback。`src/components/chat/StatusPanel.tsx` 复用现有 `translateWithFallback()`，新增 recent edit chrome / action label helpers，并替换 recent edits card、edit summary、folder/file action、toolbar、diff mode、reopen diff pane、hidden file toggle 的直接 `t()` 输出。`.trellis/spec/frontend/component-guidelines.md` 已同步：桌面 recent-edits card chrome 与 diff toolbar 必须避免 raw i18n key。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为旧 HTML 中 recent edits 标题输出 `chat.layout.recentEdits`，并可见 `editSummary`、`diffViewMode`、`tools.openFileForPath` 等 key，符合预期。GREEN：同命令通过（1 file / 31 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 83 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 354 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored；并行时出现 package cache/build directory lock 等待但最终通过）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面点测 recent edits card 的 hover title、keyboard focus、folder expand/collapse、diff mode toggle、reopen diff review、Open File、hidden files 展开/收起与 Accessibility tree；未用屏幕阅读器验证实际播报顺序；未验证真实中英文 i18n 渲染截图；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造含多文件、多目录、selected edit、collapsed diff pane、hidden edit files 的会话，点测 recent edits tree 的 keyboard focus、hover preview、diff mode、reopen handoff、Open File 和 Accessibility tree；2) 做右侧 `StatusPanel` 与输入区 fallback 的 Accessibility tree 对比，确认 top status、activity、MCP、recent edits、Git、popover region 没有语义断层；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 File Changes 入口密度、可恢复 diff review、工具栏语义、hover preview 可读性和 small-screen fallback；4) 若继续代码级小步优化，优先检查低频 `sessionLoadMetrics` 是否仍需 key-only fallback，但应避免继续无限追逐诊断文字，优先转向真实桌面/Accessibility 验证。

## 本轮 PLAN 2026-06-20 status panel session load fallback labels

- 目标：补齐右侧桌面 `StatusPanel` session-load 诊断卡的 i18n 未就绪兜底。上一轮已修 recent edits / diff toolbar；当前历史会话加载诊断仍直接使用 `chat.layout.sessionLoad*` key。SSR、测试或 i18n 未初始化时会把诊断标题、状态、来源、窗口/缓存/全量/耗时标签暴露为 raw key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `StatusPanel.tsx` session-load 渲染段和 `StatusPanel.test.tsx`，并核对 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx`；确认本轮缺口是本项目新增的历史加载诊断能力，不是 cc-gui 已有组件的复制项。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，用 key-only i18n provider 渲染 window-first-paint 与 cache-hit 两种 session-load 指标，断言输出 `History load`、`Loading`、`Window first paint`、`Source`、`Window`、`Map 7ms`、`Elapsed`、`Cache hit`、`Cache` 等 readable fallback，并不包含对应 raw key；旧实现应失败。
- 功能点 4：实现最小展示改动。验证方式：在 `StatusPanel` 内复用现有 `translateWithFallback()`，新增 session-load label/helper 派生；不改 session-load 数据结构、不改展开逻辑、不改历史加载行为、不改 MCP/activity/edit/后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录桌面 `StatusPanel` session-load 诊断卡必须保持紧凑且避免 raw i18n key。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `StatusPanel` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 status panel session load fallback labels

- 上轮进展与阻塞：上一轮已补齐右侧桌面 `StatusPanel` recent edits / diff toolbar 的 i18n 未就绪兜底；代码复查确认 recent edits 标题、编辑汇总、树展开按钮、diff mode、unified/split、reopen diff review、文件夹/文件行 action、Open File 与 hidden files toggle 已使用 readable fallback，测试与前端规范也已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 以 Tasks/Subagents/File Changes tabs 呈现状态入口，不包含本项目的 session-load 诊断卡；它不会把本项目的 `chat.layout.sessionLoad*` key 暴露给用户。本项目右侧 `StatusPanel` 已补齐 top status、activity、MCP、recent edits 的 fallback，但 session-load 卡仍直接使用 `sessionLoadMetrics`、`sessionLoadLoading`、`sessionLoadWindowFirstPaint`、`sessionLoadSource`、`sessionLoadWindow`、`sessionLoadMapMs`、`sessionLoadFull`、`sessionLoadElapsed` 等 key。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 多了历史会话加载性能诊断，能解释“先显示窗口、后台补全历史、缓存命中/失败”的状态。给这个低频诊断补 readable fallback，可以解决“主状态面板其它区域都可读，但历史加载异常时诊断卡仍是 key”的断层；尤其在 loading/error 自动展开时，用户应能直接读到 `History load`、`Window first paint`、`Map 7ms`、`Load failed`。
- 本轮规划：只修 `StatusPanel` session-load 诊断卡的文案派生：翻译正常时继续使用 i18n；翻译返回 key 时用 `History load`、`Loading`、`Load failed`、`Window ready`、`Complete`、`Window first paint`、`Cache hit`、`Source`、`Window`、`Cache`、`Full`、`Elapsed`、`Map <ms>` 兜底。不改 session-load 数据结构、不改展开逻辑、不改历史加载行为、不改 MCP/activity/edit/后端。优先级中等：用户价值中（历史加载诊断可读）、出现频率低到中（打开长历史或加载失败时）、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 新增 key-only i18n SSR 回归，先 RED 确认旧实现输出 `chat.layout.sessionLoadMetrics`、`chat.layout.sessionLoadWindowFirstPaint`、`chat.layout.sessionLoadMapMs` 等 raw key；后 GREEN 锁定 window-first-paint 与 cache-hit/error 两条路径的 readable fallback。`src/components/chat/StatusPanel.tsx` 复用现有 `translateWithFallback()`，新增 session-load title/status/source/detail/map label 派生，并替换 session-load 诊断卡中的直接 `t()` 输出。`.trellis/spec/frontend/component-guidelines.md` 已同步：桌面 session-load 诊断卡必须保持紧凑，并在 i18n 返回 key 时输出 readable fallback。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为旧 HTML 中 session-load 标题输出 `chat.layout.sessionLoadMetrics`，并可见 `sessionLoadWindowFirstPaint`、`sessionLoadMapMs` 等 key，符合预期。GREEN：同命令通过（1 file / 32 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 84 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 355 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored；并行时出现 package cache/build directory lock 等待但最终通过）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0；`git status --short` 显示当前仍是累计未暂存改动，无 staged 文件。
- 未验证项：未启动真实 Tauri 桌面点测 session-load 诊断卡在 loading/windowed/complete/error/cache-hit 场景下的可见文本、展开/收起、keyboard focus、title 与 Accessibility tree；未用屏幕阅读器验证实际播报顺序；未验证真实中英文 i18n 渲染截图；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造 loading/windowed/complete/error/cache-hit 的历史会话加载场景，点测 session-load 诊断卡的展开策略、可见文本、键盘焦点和 Accessibility tree；2) 做右侧 `StatusPanel` 与输入区 fallback 的 Accessibility tree 对比，确认 top status、activity、MCP、recent edits、session-load、Git、popover region 没有语义断层；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看状态入口密度、历史加载诊断、任务/子代理导航、MCP 配置状态、File Changes 入口和 central diff pane；4) 若继续代码级小步优化，优先转向真实桌面/Accessibility 验证，除非 CodeGraph 再发现高频可见 raw-key 缺口。

## 本轮 PLAN 2026-06-20 central diff review fallback labels

- 目标：补齐中央 `ChatDiffReviewPane` 的 i18n 未就绪兜底。右侧 recent edits tree 已能用 readable labels 把用户交接到中央 diff review pane，但中央 pane 自身仍直接使用 `chat.layout.diffPanel*`、`diffViewMode`、`diffUnifiedView`、`diffSplitView`、`diffLineWrap`、`diffLineNoWrap`、`diffLineSummary`、`collapseDiffPanel`、`tools.openFileForPath`、`tools.copyPathForPath` 等 key；SSR、测试或 i18n 未初始化时会把主审阅面板操作暴露为 raw key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ChatDiffReviewPane.tsx` 与测试，并核对 cc-gui `StatusPanel` / `FileChangesList` 的 file changes popover；确认本轮缺口是本项目超越 cc-gui 的中央 diff review pane 的操作语义，不是后端或数据层问题。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ChatDiffReviewPane.test.tsx`，用 key-only i18n provider 渲染有 selected edit 的 diff pane，断言输出 `File diff: <file>`、`File diff`、`40 diff lines`、`Diff view mode`、`Unified diff view`、`Split diff view`、`Do not wrap diff lines; use horizontal scrolling`、`Open file: <file>`、`Copy path: <file>`、`Collapse file diff panel`，并不包含对应 raw key；旧实现应失败。
- 功能点 4：实现最小展示改动。验证方式：在 `ChatDiffReviewPane` 内新增局部 `translateWithFallback()` 与 diff pane label 派生；翻译存在时继续使用 i18n，翻译返回 key 时使用英文 fallback。不改 diff 数据、不改复制/打开文件行为、不改 focus handoff、不改右侧 status panel 或后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录中央 diff review pane 的 pane name、toolbar、file actions 与 collapse action 必须和 status edit tree 一样避免 raw i18n key。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `ChatDiffReviewPane` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 central diff review fallback labels

- 上轮进展与阻塞：上一轮已补齐右侧桌面 `StatusPanel` session-load 诊断卡的 i18n 未就绪兜底；代码复查确认 session-load title/status/source/detail/map label 已走 `translateWithFallback()`，测试与前端规范也已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 通过 file changes popover 和 `FileChangesList` 提供文件变更入口；不会把本项目中央 diff pane 的 `chat.layout.diffPanel*` 或 `tools.copyPathForPath` 这类 key 暴露给用户。本项目右侧 recent edits tree 已能把用户交接到中央 diff review pane，但 `ChatDiffReviewPane` 自身仍直接使用 `diffPanelForFile`、`diffPanel`、`diffLineSummary`、`diffViewMode`、`diffUnifiedView`、`diffSplitView`、`diffLineNoWrap`、`tools.openFileForPath`、`tools.copyPathForPath`、`collapseDiffPanel` 等 key。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 的 popover file changes 更进一步，提供中央 full diff review pane、wrap/no-wrap、split/unified、Open File、Copy Path、collapse handoff。给这个主审阅面板补 readable fallback，可以解决“右侧文件树可读，但交接后的核心操作栏仍是 key”的断层；尤其影响键盘和辅助技术用户识别当前文件、切换 diff 模式、打开文件、复制路径和收起面板。
- 本轮规划：只修 `ChatDiffReviewPane` 的文案派生：翻译正常时继续使用 i18n；翻译返回 key 时用 `File diff: <file>`、`File diff`、`40 diff lines`、`Diff view mode`、`Unified diff view`、`Split diff view`、`Wrap diff lines`、`Do not wrap diff lines; use horizontal scrolling`、`Open file: <file>`、`Copy path: <file>`、`Copied path: <file>`、`Collapse file diff panel` 兜底。不改 diff 数据、不改复制/打开文件行为、不改 focus handoff、不改右侧 status panel、不改后端。优先级高：用户价值高（文件审阅主路径可读）、出现频率中高（只要打开 central diff pane 就会出现）、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/ChatDiffReviewPane.test.tsx` 新增 key-only i18n SSR 回归，先 RED 确认旧实现输出 `chat.layout.diffPanelForFile`、`chat.layout.diffPanel`、`chat.layout.diffLineSummary`、`chat.layout.diffViewMode`、`tools.openFileForPath`、`tools.copyPathForPath`、`chat.layout.collapseDiffPanel` 等 raw key；后 GREEN 锁定中央 diff pane 的 readable fallback。`src/components/chat/ChatDiffReviewPane.tsx` 新增局部 `translateWithFallback()` 和 pane/toolbar/file-action/collapse label 派生，并替换对应直接 `t()` 输出。`.trellis/spec/frontend/component-guidelines.md` 已同步：中央 diff review pane 的 pane name、toolbar、file actions、copied feedback 与 collapse action 必须和 status edit tree 一样避免 raw i18n key。
- 本轮验证：RED：`npm test -- src/components/chat/ChatDiffReviewPane.test.tsx` 先失败 1 项，失败点为旧 HTML 中中央 diff pane `aria-label` 输出 `chat.layout.diffPanelForFile`，并可见 diff toolbar、open/copy/collapse raw key，符合预期。GREEN：同命令通过（1 file / 8 tests）。相邻验证通过：`npm test -- src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 85 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 356 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored；并行时出现 package cache/build directory lock 等待但最终通过）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0；`git status --short` 显示当前仍是累计未暂存改动，无 staged 文件。
- 未验证项：未启动真实 Tauri 桌面点测中央 diff review pane 的可见文本、wrap/no-wrap、split/unified、Open File、Copy Path、collapse、programmatic focus 和 Accessibility tree；未用屏幕阅读器验证实际播报顺序；未验证真实中英文 i18n 渲染截图；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、SSR 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造 selected edit + central diff pane 场景，点测 focus handoff、toolbar title/aria-label、wrap/no-wrap、split/unified、Open File、Copy Path、collapse/reopen 与 Accessibility tree；2) 做右侧 `StatusPanel` 与中央 `ChatDiffReviewPane` 的端到端键盘路径验证，确认从 edit tree 到 central pane 再 collapse/reopen 的焦点链不丢失；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 File Changes 入口、中央 diff review pane、工具栏密度、hover preview 与 small-screen fallback；4) 若继续代码级小步优化，优先用 CodeGraph 搜索其它 Chat UI 主路径的直接 `t()` raw-key 风险，不再优先追低频诊断文字。

## 本轮 PLAN 2026-06-20 diff pane reopen fallback label

- 目标：补齐页面级中央 diff pane 收起后的浮动重新打开控件的 i18n 未就绪兜底。上一轮已修 `ChatDiffReviewPane` 内部 pane/toolbar/action labels，但 `ChatPage.tsx` 的 `diffPaneReopenLabel` 仍直接使用 `t('chat.layout.expandDiffPanelForFile')` / `t('chat.layout.expandDiffPanel')`；当 i18n 返回 key 时，collapsed/reopen 主路径的 `title` 与 `aria-label` 仍可能暴露 raw key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ChatPage.tsx`、`ChatDiffReviewPane.tsx`、`chatUiBehavior.ts` 与测试，并核对 cc-gui file changes 入口；确认本轮缺口是本项目中央 diff review 超越路径的 page-level restore 控件，不是后端或 diff 数据问题。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/utils/chatUiBehavior.test.ts`，新增 `getDiffPaneReopenLabel()` 的 key-only translator 回归，断言有文件时输出 `Open file diff: <file>`，无文件时输出 `Open file diff panel`，且不暴露 `chat.layout.expandDiffPanelForFile` / `chat.layout.expandDiffPanel`；旧实现应因缺少导出或返回 key 失败。
- 功能点 4：实现最小页面行为改动。验证方式：在 `src/utils/chatUiBehavior.ts` 新增 label helper 并由 `ChatPage.tsx` 使用。翻译存在时继续使用 i18n；翻译返回 key 时使用英文 fallback。不改 diff 数据、不改 focus handoff、不改 collapse/reopen 状态、不改 `ChatDiffReviewPane` 或后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录中央 diff review 的 page-level restore/reopen 控件也必须使用 file-specific readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `chatUiBehavior` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 diff pane reopen fallback label

- 上轮进展与阻塞：上一轮已补齐中央 `ChatDiffReviewPane` 内部 pane name、toolbar、file actions、copied feedback 与 collapse action 的 readable fallback；代码复查确认 `src/components/chat/ChatDiffReviewPane.tsx` 已用局部 `translateWithFallback()` 派生对应 labels，测试与前端规范已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 与 `FileChangesList.tsx` 通过右侧状态 tab/popover 和 file-change action 进入文件 diff，不存在本项目这个 page-level floating restore 控件，也不会暴露 `chat.layout.expandDiffPanel*` 这类本项目 key。本项目上一轮只覆盖了中央 pane 内部；`ChatPage.tsx` 的右边缘 `chat-diff-pane-reopen-floating` 仍直接把 `t('chat.layout.expandDiffPanelForFile')` / `t('chat.layout.expandDiffPanel')` 用作 `title` 与 `aria-label`。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 多了“中央 diff pane 可收起且不占列宽、再用右边缘浮动按钮恢复”的审阅路径。这个控件是 collapse/reopen 主路径的唯一全局恢复入口；给它补 `Open file diff: <file>` / `Open file diff panel` fallback，可以解决“pane 内部可读，但收起后恢复按钮仍是 key”的断层，尤其影响键盘和辅助技术用户判断恢复的是哪个文件 diff。
- 本轮规划：只修 page-level restore label 派生：新增纯 helper `getDiffPaneReopenLabel()`，`ChatPage.tsx` 使用该 helper 生成浮动恢复按钮的 `title` / `aria-label`。翻译正常时继续使用现有 i18n；翻译返回 key 时输出 readable fallback。不改 diff 数据、不改 focus handoff、不改 collapse/reopen 状态、不改 `ChatDiffReviewPane` 或后端。优先级中高：用户价值高（恢复入口可读）、出现频率中（用户收起 central diff pane 后必经）、实现成本低、回归风险低。
- 本轮完成：`src/utils/chatUiBehavior.test.ts` 新增 key-only translator 回归，先 RED 确认 `getDiffPaneReopenLabel` 合同不存在；后 GREEN 锁定有文件时输出 `Open file diff: src/components/chat/ChatDiffReviewPane.tsx`，无文件时输出 `Open file diff panel`。`src/utils/chatUiBehavior.ts` 新增 `getDiffPaneReopenLabel()`，按 `displayPath` 选择 file-specific 或 generic fallback。`src/pages/ChatPage.tsx` 将 `diffPaneReopenLabel` 改为使用 helper，不再直接用 `t()` 结果。`.trellis/spec/frontend/component-guidelines.md` 已同步：page-level restore/reopen 控件也必须避免 raw i18n key。
- 本轮验证：RED：`npm test -- src/utils/chatUiBehavior.test.ts` 先失败 1 项，失败点为 `getDiffPaneReopenLabel is not a function`，符合旧实现缺少 page-level restore label 合同的预期。GREEN：同命令通过（1 file / 18 tests）。相邻验证通过：`npm test -- src/utils/chatUiBehavior.test.ts src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx`（5 files / 86 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 357 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0；`git status --short` 显示当前仍是累计未暂存改动，无 staged 文件。
- 未验证项：未启动真实 Tauri 桌面点测浮动 reopen 按钮的可见位置、hover title、keyboard focus、click 后 programmatic focus handoff 与 Accessibility tree；未用屏幕阅读器验证实际播报顺序；未验证真实中英文 i18n 渲染截图；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、cc-gui 源码核查、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造 selected edit + collapse/reopen 场景，点测右边缘 reopen 控件、StatusPanel reopen 控件、中央 pane focus handoff 和 Accessibility tree；2) 做右侧 `StatusPanel` 与中央 `ChatDiffReviewPane` 的端到端键盘路径验证，确认 edit tree -> central pane -> collapse -> floating reopen -> central pane 的焦点链不丢失；3) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 File Changes 入口、中央 diff review pane、恢复入口、工具栏密度、hover preview 与 small-screen fallback；4) 若继续代码级小步优化，优先用 CodeGraph 搜索 Chat UI 主路径剩余直接 `t()` raw-key 风险，但不要继续优先追低频诊断文字。

## 本轮 PLAN 2026-06-20 chat daemon status fallback labels

- 目标：补齐 Chat 页面顶部 daemon 状态文案的 i18n 未就绪兜底。上一轮已修复中央 diff pane 的 page-level restore label；本轮 CodeGraph 复查发现 `ChatPage.tsx` 顶部 daemon 指示器仍直接用 `t('chat.ready')`、`t('chat.daemon.offline')`、`t('chat.daemon.error')`、`t('chat.starting')` 生成可见文本，SSR、测试或 i18n 未初始化时会把 key 显示在每次打开 Chat 都能看到的顶栏。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ChatPage.tsx`、`chatDaemonStatus.ts` 与测试，并核对 cc-gui Chat/Status 入口；确认本轮缺口是本项目高频页面状态文案，不是工具块数据或后端生命周期判断问题。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/utils/chatDaemonStatus.test.ts`，新增 `getChatDaemonStatusText()` 的 key-only translator 回归，断言 ready/offline/error/starting 分别输出 `Ready`、`Offline`、`Daemon error`、`Starting`，unknown daemon status 保留原始可读状态；旧实现应因缺少导出失败。
- 功能点 4：实现最小展示改动。验证方式：在 `src/utils/chatDaemonStatus.ts` 新增 status text helper 并由 `ChatPage.tsx` 使用。翻译存在时继续使用 i18n；翻译返回 key 时使用英文 fallback。不改 daemon 状态分类、不改 reconnect 行为、不改 diagnostics、不改后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 Chat 顶部 daemon status visible text 也必须有 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `chatDaemonStatus` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat daemon reconnect fallback labels

- 上轮进展与阻塞：上一轮已补齐 Chat 页面桌面三栏 resize handles 的 i18n 未就绪兜底；代码复查确认 `src/utils/chatUiBehavior.ts` 已新增 `getPaneResizeHandleLabel()`，`src/pages/ChatPage.tsx` 已将三个 resize handle 的 `title` / `aria-label` 改为 helper 派生值，测试与前端规范已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `webview/src/components/ChatScreen.tsx` / `StatusPanel` 不包含本项目这个本地 daemon 顶栏 reconnect 控件，也不会暴露 `chat.daemon.reconnect*` key。本项目在顶栏状态文本、diagnostic tooltip 和 resize handles 已有 fallback 后，reconnect button 仍直接用 `t('chat.daemon.reconnect')`、`t('chat.daemon.reconnecting')`、`t('chat.daemon.reconnectShort')` 作为 `title`、`aria-label` 和可见短文案；key-only i18n 下会让恢复路径暴露 raw key。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 多了本地 daemon 生命周期与手动恢复入口。给顶栏 reconnect action 补 readable fallback，可以解决“状态可读但恢复按钮不可读”的断层；尤其在 daemon offline/error 这种高压路径，用户和辅助技术应直接得到 `Reconnect daemon` / `Reconnect`，而不是翻译 key。
- 本轮规划：只修顶栏 daemon reconnect label 派生：新增 `getChatDaemonReconnectLabel()` 和 `getChatDaemonReconnectShortLabel()`，`ChatPage.tsx` 使用 helper 生成 `title`、`aria-label` 和按钮短文案。翻译正常时继续使用 i18n；翻译返回 key 时输出 `Reconnect daemon`、`Reconnecting daemon`、`Reconnect`、`Reconnecting`。不改 reconnect 行为、不改 disabled/spinner、不改 daemon 状态分类、不改后端。优先级中高：用户价值中高（错误恢复路径可读）、出现频率中（daemon offline/error/reconnecting 时出现）、实现成本低、回归风险低。
- 本轮完成：`src/utils/chatDaemonStatus.test.ts` 新增 key-only translator 回归，先 RED 确认 `getChatDaemonReconnectLabel is not a function`；后 GREEN 锁定普通态 `Reconnect daemon` / `Reconnect` 和 reconnecting 态 `Reconnecting daemon` / `Reconnecting`。`src/utils/chatDaemonStatus.ts` 新增两个 reconnect label helper，保持 daemon 状态相关文案合同集中在同一纯工具文件。`src/pages/ChatPage.tsx` 将顶栏 reconnect 按钮的 `title`、`aria-label` 和短文案改为 helper 派生值，保留现有 click、disabled 与 spinner 行为。`.trellis/spec/frontend/component-guidelines.md` 已同步：top-header daemon reconnect action 的 `title` / `aria-label` / short label 必须避免 raw i18n key。
- 本轮验证：RED：`npm test -- src/utils/chatDaemonStatus.test.ts` 先失败 1 项，失败点为 `getChatDaemonReconnectLabel is not a function`，符合旧实现缺少 reconnect label 合同的预期。GREEN：同命令通过（1 file / 12 tests）。相邻验证通过：`npm test -- src/utils/chatDaemonStatus.test.ts src/utils/chatUiBehavior.test.ts src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx`（6 files / 99 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 362 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0；`git status --short` 显示当前仍是累计未暂存改动，无 staged 文件。
- 未验证项：未启动真实 Tauri 桌面点测 daemon offline/error/reconnecting 时顶栏 reconnect button 的 hover title、disabled/spinner 状态、keyboard focus、Accessibility tree 与屏幕阅读器实际播报；未验证真实中英文 i18n 渲染截图；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、cc-gui 源码核查、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，分别构造 daemon ready、starting、offline/error、ready-timeout、reconnecting 和 raw backend diagnostic 场景，点测顶栏状态、tooltip、reconnect focus/disabled/spinner 与 Accessibility tree；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看顶栏状态、状态面板入口密度、File Changes 入口、中央 diff review pane、恢复入口、三栏宽度调整和 small-screen fallback；3) 若继续代码级小步优化，优先用 CodeGraph 搜索 Chat UI page-level icon-only buttons 的剩余直接 `t()` raw-key 风险，并只挑高频控件；4) 若进入真实验证轮，优先覆盖 diff pane collapse/reopen 焦点链、resize handles、daemon diagnostic tooltip 与本轮 reconnect button。

## 迭代记录 2026-06-20 chat top chrome action fallback labels

- 上轮进展与阻塞：上一轮已补齐 Chat 顶栏 daemon reconnect action 的 i18n 未就绪兜底；代码复查确认 `src/utils/chatDaemonStatus.ts` 已新增 `getChatDaemonReconnectLabel()` / `getChatDaemonReconnectShortLabel()`，`src/pages/ChatPage.tsx` 已使用 helper 派生 reconnect 按钮的 `title`、`aria-label` 和短文案，测试与前端规范已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `webview/src/components/ChatScreen.tsx` / `ChatHeader` 不包含本项目的 SDK dependency banner，但顶部操作不会向用户暴露本项目的 `chat.sdk.manage`、`chat.clear`、`chat.sdk.missingBanner`、`chat.sdk.install` raw key。本项目上一轮修了 daemon reconnect；同一顶栏右侧的 SDK 管理、清空聊天，以及 SDK 缺失横幅和安装按钮仍直接使用 `t()` 结果。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 多了内置 SDK 依赖检测和安装恢复入口。给这些顶部 chrome action 补 readable fallback，可以解决“daemon 恢复按钮可读，但相邻依赖恢复和清空操作仍是 key”的断层；尤其 SDK 缺失时，用户需要直接读到哪个 SDK 未安装以及下一步安装动作。
- 本轮规划：只修 Chat 顶部 chrome action/banner 文案派生：新增 `getChatTopChromeActionLabel()` 和 `getSdkMissingBannerText()`，`ChatPage.tsx` 使用 helper 生成 `Manage SDKs`、`Clear chat`、`Install SDK` 和 `<SDK name> is not installed yet. Install it to start chatting.` fallback。翻译正常时继续使用 i18n；翻译返回 key 时输出英文 fallback。不改按钮行为、不改 SDK 检测、不改清空逻辑、不改后端。优先级中高：用户价值中高（依赖恢复和清空动作可读）、出现频率高（顶栏常驻，banner 在缺失时出现）、实现成本低、回归风险低。
- 本轮完成：`src/utils/chatUiBehavior.test.ts` 新增 key-only translator 回归，先 RED 确认 `getChatTopChromeActionLabel is not a function`；后 GREEN 锁定 `Manage SDKs`、`Clear chat`、`Install SDK` 和 `Claude SDK is not installed yet. Install it to start chatting.` fallback。`src/utils/chatUiBehavior.ts` 新增 `ChatTopChromeAction`、`getChatTopChromeActionLabel()` 和 `getSdkMissingBannerText()`。`src/pages/ChatPage.tsx` 将顶栏 SDK 管理、清空、SDK 缺失横幅和安装按钮改为 helper 派生文案，保留现有 onClick、disabled、SDK 检测逻辑。`.trellis/spec/frontend/component-guidelines.md` 已同步：Chat top chrome SDK/manage/clear/recovery labels 必须避免 raw i18n key。
- 本轮验证：RED：`npm test -- src/utils/chatUiBehavior.test.ts` 先失败 1 项，失败点为 `getChatTopChromeActionLabel is not a function`，符合旧实现缺少 top chrome label 合同的预期。GREEN：同命令通过（1 file / 20 tests）。相邻验证通过：`npm test -- src/utils/chatUiBehavior.test.ts src/utils/chatDaemonStatus.test.ts src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx`（6 files / 100 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 363 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。
- 未验证项：未启动真实 Tauri 桌面点测 SDK 缺失横幅、Manage SDKs、Install SDK 和 Clear chat 的真实中英文渲染、keyboard focus、Accessibility tree 与屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、cc-gui 源码核查、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，分别构造 SDK installed/missing、daemon offline/error/reconnecting、空会话/有消息会话，点测顶栏按钮、SDK 缺失横幅、reconnect button 和清空 disabled 状态的 focus 与 Accessibility tree；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看顶部状态/依赖入口、状态面板入口密度、File Changes、中央 diff review pane、恢复入口、三栏宽度调整和 small-screen fallback；3) 若继续代码级小步优化，优先用 CodeGraph 搜索 Chat UI 剩余 page-level action 的 `title` / `aria-label` / visible label 直接 `t()` 风险，并只挑高频控件；4) 若进入真实验证轮，优先覆盖 diff pane collapse/reopen 焦点链、resize handles、daemon tooltip/reconnect 和本轮 top chrome actions。

## 本轮 PLAN 2026-06-20 chat navigation control fallback labels

- 目标：补齐 Chat 对话导航控件的 i18n 未就绪兜底。CodeGraph 复查发现 `ConversationSearch.tsx`、`ScrollControl.tsx`、`MessageAnchorRail.tsx` 仍直接把 `chat.layout.searchPlaceholder`、`chat.layout.clearSearch`、`chat.layout.scrollToTop`、`chat.layout.scrollToBottom`、`chat.layout.anchorRail`、`chat.layout.currentAnchor`、`chat.layout.jumpToMessage` 用于 visible / placeholder / `title` / `aria-label`，key-only i18n 下会暴露 raw key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ConversationSearch.tsx`、`ScrollControl.tsx`、`MessageAnchorRail.tsx`、`chatUiBehavior.ts` 与测试，并核对 cc-gui `ConversationSearch/index.tsx`；确认本轮缺口是本项目对话导航主路径控件的 raw-key 风险。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/utils/chatUiBehavior.test.ts`，新增 navigation label helper 的 key-only translator 回归；扩展 `src/components/chat/MessageAnchorRail.test.tsx` 静态渲染断言，锁定锚点栏不暴露 raw key。旧实现应因缺少 helper 和渲染 raw key 失败。
- 功能点 4：实现最小展示改动。验证方式：在 `src/utils/chatUiBehavior.ts` 新增 `getChatNavigationControlLabel()`，并由 `ConversationSearch.tsx`、`ScrollControl.tsx`、`MessageAnchorRail.tsx` 使用。翻译存在时继续使用 i18n；翻译返回 key 时使用英文 fallback。不改搜索、滚动、锚点采样/定位逻辑。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 Chat navigation/search/anchor controls 的 `placeholder` / visible text / `title` / `aria-label` 必须使用 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat navigation control fallback labels

- 上轮进展与阻塞：上一轮已补齐 Chat 顶部 chrome actions / SDK recovery banner 的 i18n 未就绪兜底；代码复查确认 `src/utils/chatUiBehavior.ts` 已新增 `getChatTopChromeActionLabel()` / `getSdkMissingBannerText()`，`src/pages/ChatPage.tsx` 已使用 helper 派生 SDK 管理、清空聊天、SDK 缺失横幅和安装按钮文案，测试与前端规范已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `webview/src/components/ConversationSearch/index.tsx` 提供对话内搜索面板，并用 `defaultValue` 保证搜索控件可读。本项目也有 `ConversationSearch`、`ScrollControl`、`MessageAnchorRail`，但搜索 placeholder、清空搜索、锚点栏、当前锚点、跳转消息和滚动按钮仍直接使用 `chat.layout.*` 翻译结果；i18n 返回 key 时，用户会在高频导航控件上看到或听到 raw key。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 多了常驻左侧消息锚点栏和独立浮动滚到底部按钮。给这些导航控件补 readable fallback，可以解决“顶栏和差异面板已经可读，但对话导航主路径仍可能暴露 key”的断层；对键盘/辅助技术用户尤其重要，因为这些控件多是 icon-only 或 placeholder-only。
- 本轮规划：只修 Chat navigation control label 派生：新增 `getChatNavigationControlLabel()`，`ConversationSearch.tsx`、`ScrollControl.tsx`、`MessageAnchorRail.tsx` 使用 helper 生成 `Search this conversation`、`Clear search`、`Message timeline`、`Current message`、`Jump to message <n>`、`Scroll to top`、`Scroll to bottom` fallback。翻译正常时继续使用现有 i18n；翻译返回 key 时输出英文 fallback。不改搜索、滚动、锚点采样/定位逻辑，不改后端。优先级中高：用户价值中高（高频导航与 icon-only 控件可读）、出现频率高（每次 Chat 浏览/搜索都出现）、实现成本低、回归风险低。
- 本轮完成：`src/utils/chatUiBehavior.test.ts` 新增 key-only translator 回归，先 RED 确认 `getChatNavigationControlLabel is not a function`；后 GREEN 锁定搜索、清空、锚点栏、当前锚点、跳转消息、滚动上下的 readable fallback。`src/components/chat/MessageAnchorRail.test.tsx` 新增静态渲染回归，先 RED 确认 DOM 输出 `chat.layout.anchorRail`、`chat.layout.jumpToMessage`、`chat.layout.scrollToTop/Bottom`；后 GREEN 确认输出 `Message timeline`、`Current message`、`Jump to message 1`、`Scroll to top`、`Scroll to bottom`。`src/utils/chatUiBehavior.ts` 新增 `ChatNavigationControl` 和 `getChatNavigationControlLabel()`。`src/components/chat/ConversationSearch.tsx` 将搜索 placeholder / aria-label 和清空按钮 title / aria-label 改为 helper 派生。`src/components/chat/ScrollControl.tsx` 将浮动滚到底部按钮 title / aria-label 改为 helper 派生。`src/components/chat/MessageAnchorRail.tsx` 将锚点栏 visible/aria label、当前锚点、跳转消息、滚动上下按钮改为 helper 派生。`.trellis/spec/frontend/component-guidelines.md` 已同步：Chat navigation/search/anchor controls 必须避免 raw `chat.layout.*` key。
- 本轮验证：RED：`npm test -- src/utils/chatUiBehavior.test.ts src/components/chat/MessageAnchorRail.test.tsx` 先失败 2 项，失败点为 `getChatNavigationControlLabel is not a function` 和锚点栏输出 `chat.layout.anchorRail`，符合旧实现缺少 navigation label 合同的预期。GREEN：同命令通过（2 files / 26 tests）。相邻验证通过：`npm test -- src/utils/chatUiBehavior.test.ts src/utils/chatDaemonStatus.test.ts src/components/chat/MessageAnchorRail.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx`（7 files / 106 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 365 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored；并行时出现 package cache/build directory lock 等待但最终通过）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。目标控件复查：`rg "t\\('chat\\.layout\\.(searchPlaceholder|clearSearch|scrollToTop|scrollToBottom|anchorRail|currentAnchor|jumpToMessage)'" src/components/chat/ConversationSearch.tsx src/components/chat/ScrollControl.tsx src/components/chat/MessageAnchorRail.tsx -n` 无命中。
- 未验证项：未启动真实 Tauri 桌面点测搜索框 placeholder、清空按钮 hover/focus、左侧锚点栏、跳转消息、滚动上下按钮的真实中英文渲染、keyboard focus、Accessibility tree 与屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、cc-gui 源码核查、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，分别构造长会话、搜索命中/无命中、已滚离底部、有锚点/无锚点场景，点测搜索、锚点栏、滚动按钮、顶栏 actions、daemon reconnect 和 SDK recovery 的 focus 与 Accessibility tree；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看搜索体验、顶部状态/依赖入口、状态面板入口密度、File Changes、中央 diff review pane、恢复入口、三栏宽度调整和 small-screen fallback；3) 若继续代码级小步优化，优先用 CodeGraph 搜索 Chat composer icon-only buttons 的剩余 direct `t()` raw-key 风险（如 provider/model/reasoning/send/enhance/resize composer），但需要单轮只挑一个高频区块；4) 若进入真实验证轮，优先覆盖搜索/锚点导航、本轮 navigation labels、diff pane collapse/reopen 焦点链、resize handles、daemon tooltip/reconnect 和 top chrome actions。

## 本轮 PLAN 2026-06-20 chat navigation control fallback labels

- 目标：补齐 Chat 对话导航控件的 i18n 未就绪兜底。CodeGraph 复查发现 `ConversationSearch.tsx`、`ScrollControl.tsx`、`MessageAnchorRail.tsx` 仍直接把 `chat.layout.searchPlaceholder`、`chat.layout.clearSearch`、`chat.layout.scrollToTop`、`chat.layout.scrollToBottom`、`chat.layout.anchorRail`、`chat.layout.currentAnchor`、`chat.layout.jumpToMessage` 用于 visible / placeholder / `title` / `aria-label`，key-only i18n 下会暴露 raw key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ConversationSearch.tsx`、`ScrollControl.tsx`、`MessageAnchorRail.tsx`、`chatUiBehavior.ts` 与测试，并核对 cc-gui `ConversationSearch/index.tsx`；确认本轮缺口是本项目对话导航主路径控件的 raw-key 风险。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/utils/chatUiBehavior.test.ts`，新增 navigation label helper 的 key-only translator 回归；新增/扩展组件静态渲染断言，锁定搜索输入、清空搜索、滚动按钮、锚点栏不暴露 raw key。旧实现应因缺少 helper 或渲染 raw key 失败。
- 功能点 4：实现最小展示改动。验证方式：在 `src/utils/chatUiBehavior.ts` 新增 `getChatNavigationControlLabel()`，并由 `ConversationSearch.tsx`、`ScrollControl.tsx`、`MessageAnchorRail.tsx` 使用。翻译存在时继续使用 i18n；翻译返回 key 时使用英文 fallback。不改搜索、滚动、锚点采样/定位逻辑。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 Chat navigation/search/anchor controls 的 `placeholder` / visible text / `title` / `aria-label` 必须使用 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 本轮 PLAN 2026-06-20 chat top chrome action fallback labels

- 目标：补齐 Chat 顶部 chrome 里 SDK 管理、清空、SDK 缺失横幅和安装按钮的 i18n 未就绪兜底。上一轮已修 daemon reconnect；本轮 CodeGraph 复查发现 `ChatPage.tsx` 顶栏右侧仍直接使用 `t('chat.sdk.manage')`、`t('chat.clear')`，SDK 缺失恢复条仍直接使用 `t('chat.sdk.missingBanner')`、`t('chat.sdk.install')`。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ChatPage.tsx` 顶部 chrome 渲染段、`chatUiBehavior.ts` 与测试，并核对 cc-gui `ChatScreen` / `ChatHeader`；确认本轮缺口是本项目顶部依赖恢复与清空操作，不改 daemon、diff、status panel 或后端。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/utils/chatUiBehavior.test.ts`，新增 key-only translator 回归，断言 `Manage SDKs`、`Clear chat`、`Install SDK`、`Claude SDK is not installed yet. Install it to start chatting.` readable fallback；旧实现应因缺少导出失败。
- 功能点 4：实现最小页面展示改动。验证方式：在 `src/utils/chatUiBehavior.ts` 新增 top chrome action/banner label helpers 并由 `ChatPage.tsx` 使用。翻译存在时继续使用 i18n；翻译返回 key 时使用英文 fallback。不改按钮行为、不改 SDK 检测、不改清空逻辑。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 Chat top chrome SDK/manage/clear/recovery labels 必须使用 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `chatUiBehavior` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat daemon reconnect fallback labels

- 上轮进展与阻塞：上一轮已补齐 Chat 页面桌面三栏 resize handles 的 i18n 未就绪兜底；代码复查确认 `src/utils/chatUiBehavior.ts` 已新增 `getPaneResizeHandleLabel()`，`src/pages/ChatPage.tsx` 已将三个 resize handle 的 `title` / `aria-label` 改为 helper 派生值，测试与前端规范已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `webview/src/components/ChatScreen.tsx` / `StatusPanel` 不包含本项目这个本地 daemon 顶栏 reconnect 控件，也不会暴露 `chat.daemon.reconnect*` key。本项目在顶栏状态文本、diagnostic tooltip 和 resize handles 已有 fallback 后，reconnect button 仍直接用 `t('chat.daemon.reconnect')`、`t('chat.daemon.reconnecting')`、`t('chat.daemon.reconnectShort')` 作为 `title`、`aria-label` 和可见短文案；key-only i18n 下会让恢复路径暴露 raw key。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 多了本地 daemon 生命周期与手动恢复入口。给顶栏 reconnect action 补 readable fallback，可以解决“状态可读但恢复按钮不可读”的断层；尤其在 daemon offline/error 这种高压路径，用户和辅助技术应直接得到 `Reconnect daemon` / `Reconnect`，而不是翻译 key。
- 本轮规划：只修顶栏 daemon reconnect label 派生：新增 `getChatDaemonReconnectLabel()` 和 `getChatDaemonReconnectShortLabel()`，`ChatPage.tsx` 使用 helper 生成 `title`、`aria-label` 和按钮短文案。翻译正常时继续使用 i18n；翻译返回 key 时输出 `Reconnect daemon`、`Reconnecting daemon`、`Reconnect`、`Reconnecting`。不改 reconnect 行为、不改 disabled/spinner、不改 daemon 状态分类、不改后端。优先级中高：用户价值中高（错误恢复路径可读）、出现频率中（daemon offline/error/reconnecting 时出现）、实现成本低、回归风险低。
- 本轮完成：`src/utils/chatDaemonStatus.test.ts` 新增 key-only translator 回归，先 RED 确认 `getChatDaemonReconnectLabel is not a function`；后 GREEN 锁定普通态 `Reconnect daemon` / `Reconnect` 和 reconnecting 态 `Reconnecting daemon` / `Reconnecting`。`src/utils/chatDaemonStatus.ts` 新增两个 reconnect label helper，保持 daemon 状态相关文案合同集中在同一纯工具文件。`src/pages/ChatPage.tsx` 将顶栏 reconnect 按钮的 `title`、`aria-label` 和短文案改为 helper 派生值，保留现有 click、disabled 与 spinner 行为。`.trellis/spec/frontend/component-guidelines.md` 已同步：top-header daemon reconnect action 的 `title` / `aria-label` / short label 必须避免 raw i18n key。
- 本轮验证：RED：`npm test -- src/utils/chatDaemonStatus.test.ts` 先失败 1 项，失败点为 `getChatDaemonReconnectLabel is not a function`，符合旧实现缺少 reconnect label 合同的预期。GREEN：同命令通过（1 file / 12 tests）。相邻验证通过：`npm test -- src/utils/chatDaemonStatus.test.ts src/utils/chatUiBehavior.test.ts src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx`（6 files / 99 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 362 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0；`git status --short` 显示当前仍是累计未暂存改动，无 staged 文件。
- 未验证项：未启动真实 Tauri 桌面点测 daemon offline/error/reconnecting 时顶栏 reconnect button 的 hover title、disabled/spinner 状态、keyboard focus、Accessibility tree 与屏幕阅读器实际播报；未验证真实中英文 i18n 渲染截图；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、cc-gui 源码核查、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，分别构造 daemon ready、starting、offline/error、ready-timeout、reconnecting 和 raw backend diagnostic 场景，点测顶栏状态、tooltip、reconnect focus/disabled/spinner 与 Accessibility tree；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看顶栏状态、状态面板入口密度、File Changes 入口、中央 diff review pane、恢复入口、三栏宽度调整和 small-screen fallback；3) 若继续代码级小步优化，优先用 CodeGraph 搜索 Chat UI page-level icon-only buttons 的剩余直接 `t()` raw-key 风险，并只挑高频控件；4) 若进入真实验证轮，优先覆盖 diff pane collapse/reopen 焦点链、resize handles、daemon diagnostic tooltip 与本轮 reconnect button。

## 迭代记录 2026-06-20 chat daemon status fallback labels

- 上轮进展与阻塞：上一轮已补齐中央 diff pane 收起后的 page-level floating reopen 控件 label fallback；代码复查确认 `src/utils/chatUiBehavior.ts` 已新增 `getDiffPaneReopenLabel()`，`src/pages/ChatPage.tsx` 已使用该 helper 派生 `title` / `aria-label`，测试与前端规范已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `ChatScreen` / `StatusPanel` 以真实状态入口展示任务、文件变化和运行状态，不应向用户暴露翻译 key。本项目多个 Chat UI 区域已补齐 fallback，但 `ChatPage.tsx` 顶部 daemon 生命周期指示器是每次进入 Chat 都可见的高频状态信号，仍直接用 `t('chat.ready')`、`t('chat.daemon.offline')`、`t('chat.daemon.error')`、`t('chat.starting')` 生成可见文本；i18n 未就绪时会显示 raw key。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 多了本地 daemon 生命周期恢复逻辑和顶栏快速 reconnect 入口。让顶栏状态在 i18n 边界下仍显示 `Ready`、`Starting`、`Offline`、`Daemon error`，可以解决“右侧状态面板/输入区 fallback 可读，但页面最顶部生命周期信号仍是 key”的断层；同时 unknown daemon status 保留原始状态字符串，避免把有诊断价值的状态覆盖成泛化文案。
- 本轮规划：只修顶部 daemon visible status text 派生：新增 `getChatDaemonStatusText()`，`ChatPage.tsx` 使用该 helper。翻译正常时继续使用 i18n；翻译返回 key 时输出英文 fallback；unknown 且有 meaningful `daemonStatus` 时原样展示。不改 daemon 状态分类、不改 reconnect 行为、不改 diagnostics、不改后端。优先级高：用户价值中高（页面顶栏高频状态可读）、出现频率高（每次进入 Chat 都显示）、实现成本低、回归风险低。
- 本轮完成：`src/utils/chatDaemonStatus.test.ts` 新增 key-only translator 回归，先 RED 确认 `getChatDaemonStatusText` 合同不存在；后 GREEN 锁定 ready/offline/error/starting 分别输出 `Ready`、`Offline`、`Daemon error`、`Starting`，并锁定 meaningful unknown status 透传。`src/utils/chatDaemonStatus.ts` 新增 `getChatDaemonStatusText()`，复用既有 `getChatDaemonStatusKind()` 做分类并在翻译返回 key 时输出 fallback。`src/pages/ChatPage.tsx` 将 `daemonStatusText` 改为使用 helper，保留原有颜色、reconnect 和 diagnostic tooltip 逻辑。`.trellis/spec/frontend/component-guidelines.md` 已同步：Chat 顶部 daemon visible status text 必须避免 raw i18n key。
- 本轮验证：RED：`npm test -- src/utils/chatDaemonStatus.test.ts` 先失败 2 项，失败点为 `getChatDaemonStatusText is not a function`，符合旧实现缺少顶栏 daemon status label 合同的预期。GREEN：同命令通过（1 file / 10 tests）。相邻验证通过：`npm test -- src/utils/chatDaemonStatus.test.ts src/utils/chatUiBehavior.test.ts src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx`（6 files / 96 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 359 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0；`git status --short` 显示当前仍是累计未暂存改动，无 staged 文件。
- 未验证项：未启动真实 Tauri 桌面点测顶栏 daemon ready/starting/offline/error/unknown 状态的可见文本、hover tooltip、reconnect 按钮、keyboard focus 与 Accessibility tree；未用屏幕阅读器验证实际播报；未验证真实中英文 i18n 截图；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、cc-gui 源码核查、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，分别构造 daemon ready、starting、stdout closed/offline、start failed/error、unknown status 场景，点测顶栏和右侧 `StatusPanel` 的状态一致性、tooltip、reconnect focus 与 Accessibility tree；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看顶栏状态、状态面板入口密度、File Changes 入口、中央 diff review pane、恢复入口、工具栏密度和 small-screen fallback；3) 若继续代码级小步优化，优先检查 `CHAT_DAEMON_READY_TIMEOUT_ERROR_KEY` 这类 frontend-generated daemon diagnostic key 的 tooltip fallback，或检查 ChatPage resize handles 的 accessible label fallback；4) 若进入真实验证轮，优先覆盖上一轮未验证的 diff pane collapse/reopen 焦点链和本轮 daemon 顶栏状态。

## 本轮 PLAN 2026-06-20 chat daemon diagnostic fallback label

- 目标：补齐 Chat 顶部 daemon diagnostic tooltip 的 frontend-generated i18n key 兜底。上一轮已修可见状态文本；本轮 CodeGraph 复查发现 `ChatPage.tsx` 对 `CHAT_DAEMON_READY_TIMEOUT_ERROR_KEY` 只做 `t()`，当 i18n 返回 key 时，顶栏 title 仍会暴露 `chat.daemon.readyTimeoutError`。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `ChatPage.tsx`、`chatDaemonStatus.ts` 与 `chatDaemonStatus.test.ts`；确认本轮缺口是顶栏 tooltip 的 frontend-generated diagnostic key，不改后端/raw diagnostic。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/utils/chatDaemonStatus.test.ts`，新增 `getChatDaemonDiagnosticDisplayText()` 的 key-only translator 回归，断言 ready-timeout key 输出 readable fallback，raw backend diagnostic 原样保留，null 仍为 null；旧实现应因缺少导出失败。
- 功能点 4：实现最小展示改动。验证方式：在 `src/utils/chatDaemonStatus.ts` 新增 diagnostic display helper，并由 `ChatPage.tsx` 使用。翻译存在时继续使用 i18n；翻译返回 key 时使用英文 fallback。不改 diagnostic 生成逻辑、不改 status/reconnect 行为、不改后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 frontend-generated daemon diagnostic key 的 tooltip fallback 也不能暴露 raw key。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `chatDaemonStatus` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat daemon diagnostic fallback label

- 上轮进展与阻塞：上一轮已补齐 Chat 顶部 daemon visible status text 的 i18n 未就绪兜底；代码复查确认 `src/utils/chatDaemonStatus.ts` 已新增 `getChatDaemonStatusText()`，`src/pages/ChatPage.tsx` 已使用该 helper 派生顶栏可见状态，测试与前端规范已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui Chat/Status 入口不暴露本项目的 daemon ready-timeout 翻译 key。本项目上一轮只修了顶栏可见 status text；`ChatPage.tsx` 的 daemon diagnostic tooltip 仍对 `CHAT_DAEMON_READY_TIMEOUT_ERROR_KEY` 直接 `t()`，当 i18n 返回 key 时，顶栏 `title` 会显示 `chat.daemon.readyTimeoutError`。
- 本轮差距诊断（超越 cc-gui）：本项目有本地 daemon ready-timeout 诊断和顶栏恢复入口，这是 cc-gui 没有的生命周期透明度。让 frontend-generated diagnostic key 在 tooltip 边界兜底为 `Daemon did not become ready in time`，同时保留后端/raw diagnostic 原文，可以解决“可见状态可读，但 hover/辅助技术诊断仍是 key”的断层，并避免覆盖真实错误信息。
- 本轮规划：只修 frontend-generated daemon diagnostic display text 派生：新增纯 helper `getChatDaemonDiagnosticDisplayText()`，`ChatPage.tsx` 使用该 helper 派生顶栏 `title`。翻译正常时继续使用 i18n；翻译返回 key 时输出英文 fallback；raw backend diagnostic 原样保留；null 仍为 null。不改 diagnostic 生成逻辑、不改 status/reconnect 行为、不改后端。优先级中高：用户价值中高（高频顶栏 tooltip/辅助技术诊断可读）、出现频率中（ready-timeout 失败路径触发）、实现成本低、回归风险低。
- 本轮完成：`src/utils/chatDaemonStatus.test.ts` 新增 key-only translator 回归，先 RED 确认 `getChatDaemonDiagnosticDisplayText` 合同不存在；后 GREEN 锁定 ready-timeout key 输出 `Daemon did not become ready in time`，raw backend diagnostic `Error: node executable not found` 原样保留，null 返回 null。`src/utils/chatDaemonStatus.ts` 新增 `getChatDaemonDiagnosticDisplayText()`，只处理 `CHAT_DAEMON_READY_TIMEOUT_ERROR_KEY` 的 frontend-generated key fallback。`src/pages/ChatPage.tsx` 将 `daemonDiagnosticDisplayText` 改为使用 helper，保留原有 `title={daemonDiagnosticDisplayText ?? daemonStatusText}`、状态颜色和 reconnect 行为。`.trellis/spec/frontend/component-guidelines.md` 已同步：frontend-generated daemon diagnostic key 的 tooltip fallback 不能暴露 raw key，backend/raw errors 保留 literal diagnostic。
- 本轮验证：RED：`npm test -- src/utils/chatDaemonStatus.test.ts` 先失败 1 项，失败点为 `getChatDaemonDiagnosticDisplayText is not a function`，符合旧实现缺少顶栏 diagnostic display label 合同的预期。GREEN：同命令通过（1 file / 11 tests）。相邻验证通过：`npm test -- src/utils/chatDaemonStatus.test.ts src/utils/chatUiBehavior.test.ts src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx`（6 files / 97 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 360 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0；`git status --short` 显示当前仍是累计未暂存改动，无 staged 文件。
- 未验证项：未启动真实 Tauri 桌面点测 daemon ready-timeout tooltip 的 hover title、reconnect focus、Accessibility tree 与屏幕阅读器实际播报；未验证真实中英文 i18n 渲染截图；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，分别构造 daemon ready、starting、offline/error、ready-timeout 和 raw backend diagnostic 场景，点测顶栏可见状态、tooltip、reconnect focus 与 Accessibility tree；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看顶栏状态、状态面板入口密度、File Changes 入口、中央 diff review pane、恢复入口、工具栏密度和 small-screen fallback；3) 若继续代码级小步优化，优先检查 ChatPage resize handles 或其它 page-level controls 的 accessible label fallback，但不要继续优先追低频内部诊断文字；4) 若进入真实验证轮，优先覆盖 diff pane collapse/reopen 焦点链和本轮 daemon diagnostic tooltip。

## 本轮 PLAN 2026-06-20 chat pane resize handle fallback labels

- 目标：补齐 Chat 页面桌面三栏 resize handles 的 i18n 未就绪兜底。上一轮已修 daemon diagnostic tooltip；本轮 CodeGraph 复查发现 `ChatPage.tsx` 的 conversation-diff、diff-status、conversation-status 三个拖拽 handle 仍直接把 `t('chat.layout.resize*')` 用作 `title` / `aria-label`，当 i18n 返回 key 时会暴露 raw key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ChatPage.tsx` resize handle 渲染段、`chatUiBehavior.ts` 与测试，并核对 cc-gui `StatusPanel` / file changes 入口；确认本轮缺口是本项目三栏可调整布局的超越路径，不是 cc-gui 既有功能缺失。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/utils/chatUiBehavior.test.ts`，新增 `getPaneResizeHandleLabel()` 的 key-only translator 回归，断言三个 handle 分别输出 `Resize conversation and diff panes`、`Resize diff and right panes`、`Resize conversation and right panes`，且旧实现应因缺少导出失败。
- 功能点 4：实现最小页面展示改动。验证方式：在 `src/utils/chatUiBehavior.ts` 新增 resize handle label helper 并由 `ChatPage.tsx` 使用。翻译存在时继续使用 i18n；翻译返回 key 时使用英文 fallback。不改宽度计算、不改拖拽事件、不改布局断点、不改后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 Chat page desktop resize handles 的 `title` / `aria-label` 必须使用 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `chatUiBehavior` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat pane resize handle fallback labels

- 上轮进展与阻塞：上一轮已补齐 Chat 顶部 daemon diagnostic tooltip 的 frontend-generated i18n key 兜底；代码复查确认 `src/utils/chatDaemonStatus.ts` 已新增 `getChatDaemonDiagnosticDisplayText()`，`src/pages/ChatPage.tsx` 已用该 helper 派生顶栏 tooltip，测试与前端规范已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 与 `FileChangesList.tsx` 提供状态和文件变更入口，但没有本项目这个三栏可调 review layout，也不会暴露 `chat.layout.resize*` 这类本项目 key。本项目 `ChatPage.tsx` 的 conversation-diff、diff-status、conversation-status 三个桌面拖拽 handle 仍直接使用 `t('chat.layout.resizeConversationDiff')`、`t('chat.layout.resizeDiffStatus')`、`t('chat.layout.resizeConversationStatus')` 作为 `title` / `aria-label`；i18n 返回 key 时会让键盘和辅助技术用户听到 raw key。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 多了 conversation / central diff / status 三栏宽度可调能力。给 resize handles 补 readable fallback，可以解决“中央 diff/reopen/daemon 等主路径 labels 已可读，但调整布局的唯一无文本控件仍是 key”的断层；对只靠键盘或辅助技术定位拖拽 handle 的用户尤其重要。
- 本轮规划：只修 resize handle label 派生：新增 `getPaneResizeHandleLabel()` 和 `PaneResizeHandleEdge`，`ChatPage.tsx` 使用 helper 生成三个 handle 的 `title` / `aria-label`。翻译正常时继续使用现有 i18n；翻译返回 key 时输出 `Resize conversation and diff panes`、`Resize diff and right panes`、`Resize conversation and right panes`。不改宽度计算、不改拖拽事件、不改布局断点、不改后端。优先级中高：用户价值中（无文本控件可读）、出现频率中（桌面 review layout 一直存在）、实现成本低、回归风险低。
- 本轮完成：`src/utils/chatUiBehavior.test.ts` 新增 key-only translator 回归，先 RED 确认 `getPaneResizeHandleLabel` 合同不存在；后 GREEN 锁定三个 resize edge 的 readable fallback。`src/utils/chatUiBehavior.ts` 新增 `PaneResizeHandleEdge`、`PANE_RESIZE_HANDLE_LABELS` 和 `getPaneResizeHandleLabel()`，保持 label 合同与宽度 helper 同层。`src/pages/ChatPage.tsx` 将三个 resize handle 的 `title` / `aria-label` 改为 helper 派生值，并让 `startPaneResize()` 复用 `PaneResizeHandleEdge` 类型。`.trellis/spec/frontend/component-guidelines.md` 已同步：Chat page desktop resize handles 必须在 i18n 返回 key 时使用 readable fallback。
- 本轮验证：RED：`npm test -- src/utils/chatUiBehavior.test.ts` 先失败 1 项，失败点为 `getPaneResizeHandleLabel is not a function`，符合旧实现缺少 resize handle label 合同的预期。GREEN：同命令通过（1 file / 19 tests）。相邻验证通过：`npm test -- src/utils/chatUiBehavior.test.ts src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/utils/chatDaemonStatus.test.ts`（6 files / 98 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 361 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored；并行时出现 package cache/build directory lock 等待但最终通过）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0；`git status --short` 显示当前仍是累计未暂存改动，无 staged 文件。
- 未验证项：未启动真实 Tauri 桌面点测三种 resize handle 的 hover title、键盘焦点、拖拽行为、宽度夹取边界、Accessibility tree 与屏幕阅读器实际播报；未验证真实中英文 i18n 渲染截图；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、cc-gui 源码核查、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，重点点测 central diff pane collapse/reopen、三种 resize handle 的 keyboard focus / drag / Accessibility tree，以及 daemon tooltip；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看状态入口密度、File Changes 入口、中央 diff review pane、恢复入口、三栏宽度调整和 small-screen fallback；3) 若继续代码级小步优化，优先用 CodeGraph 搜索 Chat UI page-level icon-only buttons 的剩余直接 `t()` raw-key 风险，但避免继续追低频内部诊断文字；4) 若进入真实验证轮，优先覆盖 diff pane collapse/reopen 焦点链、本轮 resize handles 和上轮 daemon diagnostic tooltip。

## 本轮 PLAN 2026-06-20 chat daemon reconnect fallback labels

- 目标：补齐 Chat 顶栏 daemon reconnect action 的 i18n 未就绪兜底。上一轮已修 resize handle；本轮 CodeGraph 复查发现 `ChatPage.tsx` 的顶栏 reconnect 按钮仍直接把 `t('chat.daemon.reconnect')`、`t('chat.daemon.reconnecting')`、`t('chat.daemon.reconnectShort')` 用于 `title`、`aria-label` 和可见短文案，key-only i18n 下会暴露 raw key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ChatPage.tsx` reconnect 渲染段、`chatDaemonStatus.ts` 与测试，并核对 cc-gui `ChatScreen` / `StatusPanel`；确认本轮缺口是本项目本地 daemon 恢复路径的 page-level control，不是 cc-gui 既有控件复制项。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/utils/chatDaemonStatus.test.ts`，新增 reconnect label helper 的 key-only translator 回归，断言普通态输出 `Reconnect daemon` / `Reconnect`，reconnecting 态输出 `Reconnecting daemon` / `Reconnecting`；旧实现应因缺少导出失败。
- 功能点 4：实现最小页面展示改动。验证方式：在 `src/utils/chatDaemonStatus.ts` 新增 reconnect label helpers 并由 `ChatPage.tsx` 使用。翻译存在时继续使用 i18n；翻译返回 key 时使用英文 fallback。不改 reconnect 行为、不改 disabled/spinner、不改 daemon 状态分类、不改后端。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 Chat top-header daemon reconnect action 的 `title` / `aria-label` / short label 必须使用 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向 `chatDaemonStatus` 测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat daemon reconnect fallback labels

- 上轮进展与阻塞：上一轮已补齐 Chat 页面桌面三栏 resize handles 的 i18n 未就绪兜底；代码复查确认 `src/utils/chatUiBehavior.ts` 已新增 `getPaneResizeHandleLabel()`，`src/pages/ChatPage.tsx` 已将三个 resize handle 的 `title` / `aria-label` 改为 helper 派生值，测试与前端规范已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `webview/src/components/ChatScreen.tsx` / `StatusPanel` 不包含本项目这个本地 daemon 顶栏 reconnect 控件，也不会暴露 `chat.daemon.reconnect*` key。本项目在顶栏状态文本、diagnostic tooltip 和 resize handles 已有 fallback 后，reconnect button 仍直接用 `t('chat.daemon.reconnect')`、`t('chat.daemon.reconnecting')`、`t('chat.daemon.reconnectShort')` 作为 `title`、`aria-label` 和可见短文案；key-only i18n 下会让恢复路径暴露 raw key。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 多了本地 daemon 生命周期与手动恢复入口。给顶栏 reconnect action 补 readable fallback，可以解决“状态可读但恢复按钮不可读”的断层；尤其在 daemon offline/error 这种高压路径，用户和辅助技术应直接得到 `Reconnect daemon` / `Reconnect`，而不是翻译 key。
- 本轮规划：只修顶栏 daemon reconnect label 派生：新增 `getChatDaemonReconnectLabel()` 和 `getChatDaemonReconnectShortLabel()`，`ChatPage.tsx` 使用 helper 生成 `title`、`aria-label` 和按钮短文案。翻译正常时继续使用 i18n；翻译返回 key 时输出 `Reconnect daemon`、`Reconnecting daemon`、`Reconnect`、`Reconnecting`。不改 reconnect 行为、不改 disabled/spinner、不改 daemon 状态分类、不改后端。优先级中高：用户价值中高（错误恢复路径可读）、出现频率中（daemon offline/error/reconnecting 时出现）、实现成本低、回归风险低。
- 本轮完成：`src/utils/chatDaemonStatus.test.ts` 新增 key-only translator 回归，先 RED 确认 `getChatDaemonReconnectLabel is not a function`；后 GREEN 锁定普通态 `Reconnect daemon` / `Reconnect` 和 reconnecting 态 `Reconnecting daemon` / `Reconnecting`。`src/utils/chatDaemonStatus.ts` 新增两个 reconnect label helper，保持 daemon 状态相关文案合同集中在同一纯工具文件。`src/pages/ChatPage.tsx` 将顶栏 reconnect 按钮的 `title`、`aria-label` 和短文案改为 helper 派生值，保留现有 click、disabled 与 spinner 行为。`.trellis/spec/frontend/component-guidelines.md` 已同步：top-header daemon reconnect action 的 `title` / `aria-label` / short label 必须避免 raw i18n key。
- 本轮验证：RED：`npm test -- src/utils/chatDaemonStatus.test.ts` 先失败 1 项，失败点为 `getChatDaemonReconnectLabel is not a function`，符合旧实现缺少 reconnect label 合同的预期。GREEN：同命令通过（1 file / 12 tests）。相邻验证通过：`npm test -- src/utils/chatDaemonStatus.test.ts src/utils/chatUiBehavior.test.ts src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx`（6 files / 99 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 362 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0；`git status --short` 显示当前仍是累计未暂存改动，无 staged 文件。
- 未验证项：未启动真实 Tauri 桌面点测 daemon offline/error/reconnecting 时顶栏 reconnect button 的 hover title、disabled/spinner 状态、keyboard focus、Accessibility tree 与屏幕阅读器实际播报；未验证真实中英文 i18n 渲染截图；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、cc-gui 源码核查、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，分别构造 daemon ready、starting、offline/error、ready-timeout、reconnecting 和 raw backend diagnostic 场景，点测顶栏状态、tooltip、reconnect focus/disabled/spinner 与 Accessibility tree；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看顶栏状态、状态面板入口密度、File Changes 入口、中央 diff review pane、恢复入口、三栏宽度调整和 small-screen fallback；3) 若继续代码级小步优化，优先用 CodeGraph 搜索 Chat UI page-level icon-only buttons 的剩余直接 `t()` raw-key 风险，并只挑高频控件；4) 若进入真实验证轮，优先覆盖 diff pane collapse/reopen 焦点链、resize handles、daemon diagnostic tooltip 与本轮 reconnect button。

## 迭代记录 2026-06-20 chat top chrome action fallback labels

- 上轮进展与阻塞：上一轮已补齐 Chat 顶栏 daemon reconnect action 的 i18n 未就绪兜底；代码复查确认 `src/utils/chatDaemonStatus.ts` 已新增 `getChatDaemonReconnectLabel()` / `getChatDaemonReconnectShortLabel()`，`src/pages/ChatPage.tsx` 已使用 helper 派生 reconnect 按钮的 `title`、`aria-label` 和短文案，测试与前端规范已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `webview/src/components/ChatScreen.tsx` / `ChatHeader` 不包含本项目的 SDK dependency banner，但顶部操作不会向用户暴露本项目的 `chat.sdk.manage`、`chat.clear`、`chat.sdk.missingBanner`、`chat.sdk.install` raw key。本项目上一轮修了 daemon reconnect；同一顶栏右侧的 SDK 管理、清空聊天，以及 SDK 缺失横幅和安装按钮仍直接使用 `t()` 结果。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 多了内置 SDK 依赖检测和安装恢复入口。给这些顶部 chrome action 补 readable fallback，可以解决“daemon 恢复按钮可读，但相邻依赖恢复和清空操作仍是 key”的断层；尤其 SDK 缺失时，用户需要直接读到哪个 SDK 未安装以及下一步安装动作。
- 本轮规划：只修 Chat 顶部 chrome action/banner 文案派生：新增 `getChatTopChromeActionLabel()` 和 `getSdkMissingBannerText()`，`ChatPage.tsx` 使用 helper 生成 `Manage SDKs`、`Clear chat`、`Install SDK` 和 `<SDK name> is not installed yet. Install it to start chatting.` fallback。翻译正常时继续使用 i18n；翻译返回 key 时输出英文 fallback。不改按钮行为、不改 SDK 检测、不改清空逻辑、不改后端。优先级中高：用户价值中高（依赖恢复和清空动作可读）、出现频率高（顶栏常驻，banner 在缺失时出现）、实现成本低、回归风险低。
- 本轮完成：`src/utils/chatUiBehavior.test.ts` 新增 key-only translator 回归，先 RED 确认 `getChatTopChromeActionLabel is not a function`；后 GREEN 锁定 `Manage SDKs`、`Clear chat`、`Install SDK` 和 `Claude SDK is not installed yet. Install it to start chatting.` fallback。`src/utils/chatUiBehavior.ts` 新增 `ChatTopChromeAction`、`getChatTopChromeActionLabel()` 和 `getSdkMissingBannerText()`。`src/pages/ChatPage.tsx` 将顶栏 SDK 管理、清空、SDK 缺失横幅和安装按钮改为 helper 派生文案，保留现有 onClick、disabled、SDK 检测逻辑。`.trellis/spec/frontend/component-guidelines.md` 已同步：Chat top chrome SDK/manage/clear/recovery labels 必须避免 raw i18n key。
- 本轮验证：RED：`npm test -- src/utils/chatUiBehavior.test.ts` 先失败 1 项，失败点为 `getChatTopChromeActionLabel is not a function`，符合旧实现缺少 top chrome label 合同的预期。GREEN：同命令通过（1 file / 20 tests）。相邻验证通过：`npm test -- src/utils/chatUiBehavior.test.ts src/utils/chatDaemonStatus.test.ts src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx`（6 files / 100 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 363 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。
- 未验证项：未启动真实 Tauri 桌面点测 SDK 缺失横幅、Manage SDKs、Install SDK 和 Clear chat 的真实中英文渲染、keyboard focus、Accessibility tree 与屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、cc-gui 源码核查、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，分别构造 SDK installed/missing、daemon offline/error/reconnecting、空会话/有消息会话，点测顶栏按钮、SDK 缺失横幅、reconnect button 和清空 disabled 状态的 focus 与 Accessibility tree；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看顶部状态/依赖入口、状态面板入口密度、File Changes、中央 diff review pane、恢复入口、三栏宽度调整和 small-screen fallback；3) 若继续代码级小步优化，优先用 CodeGraph 搜索 Chat UI 剩余 page-level action 的 `title` / `aria-label` / visible label 直接 `t()` 风险，并只挑高频控件；4) 若进入真实验证轮，优先覆盖 diff pane collapse/reopen 焦点链、resize handles、daemon tooltip/reconnect 和本轮 top chrome actions。

## 本轮 PLAN 2026-06-20 chat navigation control fallback labels

- 目标：补齐 Chat 对话导航控件的 i18n 未就绪兜底。CodeGraph 复查发现 `ConversationSearch.tsx`、`ScrollControl.tsx`、`MessageAnchorRail.tsx` 仍直接把 `chat.layout.searchPlaceholder`、`chat.layout.clearSearch`、`chat.layout.scrollToTop`、`chat.layout.scrollToBottom`、`chat.layout.anchorRail`、`chat.layout.currentAnchor`、`chat.layout.jumpToMessage` 用于 visible / placeholder / `title` / `aria-label`，key-only i18n 下会暴露 raw key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ConversationSearch.tsx`、`ScrollControl.tsx`、`MessageAnchorRail.tsx`、`chatUiBehavior.ts` 与测试，并核对 cc-gui `ConversationSearch/index.tsx`；确认本轮缺口是本项目对话导航主路径控件的 raw-key风险。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/utils/chatUiBehavior.test.ts`，新增 navigation label helper 的 key-only translator 回归；扩展 `src/components/chat/MessageAnchorRail.test.tsx` 静态渲染断言，锁定锚点栏不暴露 raw key。旧实现应因缺少 helper 和渲染 raw key 失败。
- 功能点 4：实现最小展示改动。验证方式：在 `src/utils/chatUiBehavior.ts` 新增 `getChatNavigationControlLabel()`，并由 `ConversationSearch.tsx`、`ScrollControl.tsx`、`MessageAnchorRail.tsx` 使用。翻译存在时继续使用 i18n；翻译返回 key 时使用英文 fallback。不改搜索、滚动、锚点采样/定位逻辑。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 Chat navigation/search/anchor controls 的 `placeholder` / visible text / `title` / `aria-label` 必须使用 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat navigation control fallback labels

- 上轮进展与阻塞：上一轮已补齐 Chat 顶部 chrome actions / SDK recovery banner 的 i18n 未就绪兜底；代码复查确认 `src/utils/chatUiBehavior.ts` 已新增 `getChatTopChromeActionLabel()` / `getSdkMissingBannerText()`，`src/pages/ChatPage.tsx` 已使用 helper 派生 SDK 管理、清空聊天、SDK 缺失横幅和安装按钮文案，测试与前端规范已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报和 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `webview/src/components/ConversationSearch/index.tsx` 提供对话内搜索面板，并用 `defaultValue` 保证搜索控件可读。本项目也有 `ConversationSearch`、`ScrollControl`、`MessageAnchorRail`，但搜索 placeholder、清空搜索、锚点栏、当前锚点、跳转消息和滚动按钮仍直接使用 `chat.layout.*` 翻译结果；i18n 返回 key 时，用户会在高频导航控件上看到或听到 raw key。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 多了常驻左侧消息锚点栏和独立浮动滚到底部按钮。给这些导航控件补 readable fallback，可以解决“顶栏和差异面板已经可读，但对话导航主路径仍可能暴露 key”的断层；对键盘/辅助技术用户尤其重要，因为这些控件多是 icon-only 或 placeholder-only。
- 本轮规划：只修 Chat navigation control label 派生：新增 `getChatNavigationControlLabel()`，`ConversationSearch.tsx`、`ScrollControl.tsx`、`MessageAnchorRail.tsx` 使用 helper 生成 `Search this conversation`、`Clear search`、`Message timeline`、`Current message`、`Jump to message <n>`、`Scroll to top`、`Scroll to bottom` fallback。翻译正常时继续使用现有 i18n；翻译返回 key 时输出英文 fallback。不改搜索、滚动、锚点采样/定位逻辑，不改后端。优先级中高：用户价值中高（高频导航与 icon-only 控件可读）、出现频率高（每次 Chat 浏览/搜索都出现）、实现成本低、回归风险低。
- 本轮完成：`src/utils/chatUiBehavior.test.ts` 新增 key-only translator 回归，先 RED 确认 `getChatNavigationControlLabel is not a function`；后 GREEN 锁定搜索、清空、锚点栏、当前锚点、跳转消息、滚动上下的 readable fallback。`src/components/chat/MessageAnchorRail.test.tsx` 新增静态渲染回归，先 RED 确认 DOM 输出 `chat.layout.anchorRail`、`chat.layout.jumpToMessage`、`chat.layout.scrollToTop/Bottom`；后 GREEN 确认输出 `Message timeline`、`Current message`、`Jump to message 1`、`Scroll to top`、`Scroll to bottom`。`src/utils/chatUiBehavior.ts` 新增 `ChatNavigationControl` 和 `getChatNavigationControlLabel()`。`src/components/chat/ConversationSearch.tsx` 将搜索 placeholder / aria-label 和清空按钮 title / aria-label 改为 helper 派生。`src/components/chat/ScrollControl.tsx` 将浮动滚到底部按钮 title / aria-label 改为 helper 派生。`src/components/chat/MessageAnchorRail.tsx` 将锚点栏 visible/aria label、当前锚点、跳转消息、滚动上下按钮改为 helper 派生。`.trellis/spec/frontend/component-guidelines.md` 已同步：Chat navigation/search/anchor controls 必须避免 raw `chat.layout.*` key。
- 本轮验证：RED：`npm test -- src/utils/chatUiBehavior.test.ts src/components/chat/MessageAnchorRail.test.tsx` 先失败 2 项，失败点为 `getChatNavigationControlLabel is not a function` 和锚点栏输出 `chat.layout.anchorRail`，符合旧实现缺少 navigation label 合同的预期。GREEN：同命令通过（2 files / 26 tests）。相邻验证通过：`npm test -- src/utils/chatUiBehavior.test.ts src/utils/chatDaemonStatus.test.ts src/components/chat/MessageAnchorRail.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx`（7 files / 106 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 365 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored；并行时出现 package cache/build directory lock 等待但最终通过）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。目标控件复查：`rg "t\\('chat\\.layout\\.(searchPlaceholder|clearSearch|scrollToTop|scrollToBottom|anchorRail|currentAnchor|jumpToMessage)'" src/components/chat/ConversationSearch.tsx src/components/chat/ScrollControl.tsx src/components/chat/MessageAnchorRail.tsx -n` 无命中。
- 未验证项：未启动真实 Tauri 桌面点测搜索框 placeholder、清空按钮 hover/focus、左侧锚点栏、跳转消息、滚动上下按钮的真实中英文渲染、keyboard focus、Accessibility tree 与屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、cc-gui 源码核查、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，分别构造长会话、搜索命中/无命中、已滚离底部、有锚点/无锚点场景，点测搜索、锚点栏、滚动按钮、顶栏 actions、daemon reconnect 和 SDK recovery 的 focus 与 Accessibility tree；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看搜索体验、顶部状态/依赖入口、状态面板入口密度、File Changes、中央 diff review pane、恢复入口、三栏宽度调整和 small-screen fallback；3) 若继续代码级小步优化，优先用 CodeGraph 搜索 Chat composer icon-only buttons 的剩余 direct `t()` raw-key 风险（如 provider/model/reasoning/send/enhance/resize composer），但需要单轮只挑一个高频区块；4) 若进入真实验证轮，优先覆盖搜索/锚点导航、本轮 navigation labels、diff pane collapse/reopen 焦点链、resize handles、daemon tooltip/reconnect 和 top chrome actions。

## 本轮 PLAN 2026-06-20 chat composer toolbar fallback labels

- 目标：补齐 Chat composer toolbar 高频控件的 i18n 未就绪兜底。CodeGraph 复查发现 `ButtonArea.tsx` 的 provider、permission mode、model、reasoning selector title，以及 model refresh、enhance prompt、send、stop 的 `title` / `aria-label` 仍直接使用 `t()` 结果；key-only i18n 下会暴露 `chat.providerLabel`、`chat.send` 等 raw key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ButtonArea.tsx`、`ButtonArea.test.tsx` 与 cc-gui Chat input/header 相关源码；确认本轮缺口是 composer 高频 action/selector label raw-key 风险。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/utils/chatUiBehavior.test.ts` 和 `src/components/chat/composer/ButtonArea.test.tsx`，锁定 helper 与真实 DOM 在 key-only translator 下输出 `AI provider`、`Permission mode`、`Model`、`Reasoning effort`、`Refresh models`、`Refreshing models...`、`Enhance prompt`、`Send`、`Stop`，旧实现应失败。
- 功能点 4：实现最小展示改动。验证方式：在 `src/utils/chatUiBehavior.ts` 新增 composer toolbar label helper，并由 `ButtonArea.tsx` 使用。翻译存在时继续使用 i18n；翻译返回 key 时使用英文 fallback。不改 provider/model/mode/reasoning 选项、不改 submit/stop/refresh/enhance 行为。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 Chat composer toolbar selector/action labels 必须使用 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat composer toolbar fallback labels

- 上轮进展与阻塞：上一轮已补齐 Chat navigation/search/anchor controls 的 i18n 未就绪兜底；代码复查确认 `src/utils/chatUiBehavior.ts` 已有 `getChatNavigationControlLabel()`，`src/components/chat/ConversationSearch.tsx`、`src/components/chat/ScrollControl.tsx`、`src/components/chat/MessageAnchorRail.tsx` 已使用 helper 派生 placeholder、title、aria-label 和可见锚点文本。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报、真实中英文截图，以及 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 输入区/工具栏不会在高频 selector 和 primary action 上向用户暴露 raw i18n key。本项目 `src/components/chat/composer/ButtonArea.tsx` 仍直接把 `chat.providerLabel`、`chat.modeLabel`、`chat.modelLabel`、`chat.reasoningLabel`、`chat.modelsRefresh`、`chat.modelsRefreshing`、`chat.modelsLoading`、`chat.enhancePrompt`、`chat.send`、`chat.stop` 用作 `title` / `aria-label` / 状态 footer；i18n 返回 key 时，用户会在 composer 主路径控件上看到或听到 raw key。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 多了 provider/model 动态刷新和 prompt enhance/stop 一体化 composer 行为。给这些 compact/icon-heavy 控件补 readable fallback，可以解决“顶栏、导航已经可读，但输入工具栏主操作仍可能暴露 key”的断层；对键盘和辅助技术用户尤其重要，因为这些控件多数依赖 tooltip / aria label。
- 本轮规划：只修 Chat composer toolbar label 派生：新增 `getChatComposerToolbarLabel()`，`ButtonArea.tsx` 使用 helper 生成 `AI provider`、`Permission mode`、`Model`、`Reasoning effort`、`Refresh models`、`Refreshing models...`、`Loading models...`、`Enhance prompt`、`Send`、`Stop` fallback。翻译正常时继续使用现有 i18n；翻译返回 key 时输出英文 fallback。不改 provider/model/mode/reasoning 选项、不改 submit/stop/refresh/enhance 行为、不改 store 或后端。优先级高：用户价值高（输入区主路径与 icon-only 控件可读）、出现频率高（每次输入都会看到）、实现成本低、回归风险低。
- 本轮完成：`src/utils/chatUiBehavior.test.ts` 已新增 key-only translator 回归；本轮接续已验证 RED 失败点为 `getChatComposerToolbarLabel is not a function` 与 `ButtonArea` DOM 输出 raw key。`src/utils/chatUiBehavior.ts` 新增 `ChatComposerToolbarControl`、`CHAT_COMPOSER_TOOLBAR_LABELS` 和 `getChatComposerToolbarLabel()`。`src/components/chat/composer/ButtonArea.tsx` 将 provider/mode/model/reasoning selector title、model refresh title/aria-label、model loading footer、enhance/send/stop title/aria-label 改为 helper 派生；refresh spinning 时 aria-label 也使用 `Refreshing models...`。`src/components/chat/composer/ButtonArea.test.tsx` 和 `src/components/chat/composer/ChatComposer.render.test.tsx` 锁定 key-only i18n 下不暴露 raw key。`.trellis/spec/frontend/component-guidelines.md` 已同步：composer toolbar selector/action labels 必须使用 helper-owned readable fallback。
- 本轮验证：GREEN 定向通过：`npm test -- src/utils/chatUiBehavior.test.ts src/components/chat/composer/ButtonArea.test.tsx`（2 files / 30 tests）。集成定向通过：`npm test -- src/utils/chatUiBehavior.test.ts src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx`（3 files / 32 tests）。相邻 Chat UI 验证通过：`npm test -- src/utils/chatUiBehavior.test.ts src/utils/chatDaemonStatus.test.ts src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ChatComposer.test.ts src/components/chat/MessageAnchorRail.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx`（10 files / 125 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 366 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（Vite build passed，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored；并行时出现 package cache/build directory lock 等待但最终通过）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。目标控件复查：`rg "t\('chat\.(providerLabel|modeLabel|modelLabel|reasoningLabel|modelsRefresh|modelsRefreshing|modelsLoading|enhancePrompt|send|stop)'" src/components/chat/composer/ButtonArea.tsx` 无命中。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0；`git status --short` 显示当前仍是累计未暂存改动，无 staged 文件。
- 未验证项：未启动真实 Tauri 桌面点测 composer toolbar 的 provider/mode/model/reasoning selector tooltip、model refresh spinner/disabled 状态、enhance/send/stop hover/focus、keyboard focus、Accessibility tree 与屏幕阅读器实际播报；未验证真实中英文 i18n 渲染截图；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、Trellis 规范、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造 provider/model refresh loading/error、enhance loading、send disabled/submitting、active streaming stop 状态，点测 composer toolbar tooltip、focus、aria-label 与视觉密度；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看输入区工具栏密度、selector 菜单、发送/停止、模型刷新、顶部状态、搜索/锚点导航和中央 diff review pane；3) 若继续代码级小步优化，优先用 CodeGraph 搜索 Chat composer/menu 内部 dropdown options、completion menu 或 remaining page-level controls 的 direct `t()` raw-key 风险，并单轮只挑一个高频区块；4) 若进入真实验证轮，优先覆盖本轮 composer toolbar、上一轮 navigation labels、diff pane collapse/reopen 焦点链、resize handles、daemon tooltip/reconnect 和 top chrome actions。

## 本轮 PLAN 2026-06-20 chat composer mode reasoning value fallback labels

- 目标：补齐 Chat composer toolbar 内部 mode / reasoning selector 的可见当前值和菜单项文案 fallback。上一轮只修了 selector 外层 `title` / action `aria-label`；本轮 CodeGraph 复查发现 `ButtonArea.tsx` 仍直接用 `t('chat.modes.*')` 和 `t('chat.reasoning.*')` 生成按钮当前值、菜单 label、菜单 description。key-only i18n 下会暴露 `chat.modes.default.label`、`chat.reasoning.high.label` 等可见 raw key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ButtonArea.tsx`、`SelectorDropdown.tsx`、`constants.ts` 与 cc-gui `ModeSelect.tsx` / `types.ts`；确认本轮缺口是 selector 内部值与菜单项文案 fallback，而不是 toolbar 外层 label。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/utils/chatUiBehavior.test.ts`，新增 mode/reasoning helper 的 key-only translator 回归；扩展 `src/components/chat/composer/ButtonArea.test.tsx`，锁定静态渲染中的当前 mode/reasoning button text 不暴露 raw key。旧实现应因 helper 缺失和 DOM 输出 raw key 失败。
- 功能点 4：实现最小展示改动。验证方式：在 `src/utils/chatUiBehavior.ts` 新增 mode/reasoning option text helper，并由 `ButtonArea.tsx` 的 modeOptions、reasoningOptions、current buttonLabel 使用。翻译存在时继续使用 i18n；翻译返回 key 时使用英文 fallback。不改 dropdown open/close、onChange、过滤规则、图标、provider/model 行为。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 composer mode/reasoning selector 的当前值、option label 和 description 必须使用 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat composer mode reasoning value fallback labels

- 上轮进展与阻塞：上一轮已补齐 Chat composer toolbar 外层 selector/action labels 的 i18n 未就绪兜底；代码复查确认 `src/utils/chatUiBehavior.ts` 已新增 `getChatComposerToolbarLabel()`，`src/components/chat/composer/ButtonArea.tsx` 已使用 helper 派生 provider/mode/model/reasoning selector title、model refresh title/aria-label、model loading footer、enhance/send/stop title/aria-label。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报、真实中英文截图，以及 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `webview/src/components/ChatInputBox/selectors/ModeSelect.tsx` / `types.ts` 的 mode 和 reasoning 配置自带英文 label / description fallback，例如 `Default Mode`、`Agent Mode`、`Auto Mode`、`High`。本项目上一轮只修了外层 title/action；`ButtonArea.tsx` 的 mode/reasoning 当前按钮文本、菜单 label 和菜单 description 仍直接来自 `t('chat.modes.*')` 与 `t('chat.reasoning.*')`，key-only i18n 下会显示 `chat.modes.default.label`、`chat.reasoning.high.label` 等 raw key。
- 本轮差距诊断（超越 cc-gui）：本项目的 composer 把 provider/model/mode/reasoning 和模型动态刷新压在一条紧凑工具栏里，任何 raw key 都会直接破坏输入主路径的可读性。给当前值和菜单项补 fallback，可以解决“tooltip 可读，但按钮本体仍是 key”的断层；对频繁切换权限模式/推理强度的用户尤其重要。
- 本轮规划：只修 Chat composer mode/reasoning selector 内部文案派生：新增 `getChatComposerModeText()` 和 `getChatComposerReasoningText()`，`ButtonArea.tsx` 使用 helper 生成 mode/reasoning 当前值、option label、option description fallback。翻译正常时继续使用现有 i18n；翻译返回 key 时输出英文 fallback。不改 dropdown open/close、onChange、过滤规则、图标、provider/model 行为，不改 store 或后端。优先级高：用户价值高（输入区可见当前值可读）、出现频率高（每次 composer 渲染都显示）、实现成本低、回归风险低。
- 本轮完成：`src/utils/chatUiBehavior.test.ts` 新增 key-only translator 回归，先 RED 确认 `getChatComposerModeText is not a function`；`src/components/chat/composer/ButtonArea.test.tsx` 新增静态渲染回归，先 RED 确认 DOM 输出 `chat.modes.default.label` 与 `chat.reasoning.high.label`。`src/utils/chatUiBehavior.ts` 新增 `ChatComposerPermissionMode`、`ChatComposerReasoningEffort`、`ChatComposerOptionTextField`、mode/reasoning fallback maps，以及 `getChatComposerModeText()` / `getChatComposerReasoningText()`。`src/components/chat/composer/ButtonArea.tsx` 将 modeOptions、reasoningOptions、current mode buttonLabel、current reasoning buttonLabel 改为 helper 派生。`.trellis/spec/frontend/component-guidelines.md` 已同步：composer mode/reasoning selector 当前值、option label 和 description 必须避免 raw key。
- 本轮验证：RED：`npm test -- src/utils/chatUiBehavior.test.ts src/components/chat/composer/ButtonArea.test.tsx` 先失败 2 项，失败点为 `getChatComposerModeText is not a function` 和 DOM 未包含 `Default Mode`，符合旧实现缺少 selector 内部文案 fallback 的预期。GREEN 定向通过：同命令通过（2 files / 32 tests）。集成定向通过：`npm test -- src/utils/chatUiBehavior.test.ts src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx`（3 files / 34 tests）。相邻 Chat UI 验证通过：`npm test -- src/utils/chatUiBehavior.test.ts src/utils/chatDaemonStatus.test.ts src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ChatComposer.test.ts src/components/chat/MessageAnchorRail.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/toolBlocks/EditDiffPreview.test.tsx`（10 files / 127 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 368 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（Vite build passed，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored；并行时出现 package cache/build directory lock 等待但最终通过）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。目标控件复查：`rg "t\(`chat\.(modes|reasoning)\.\$\{[^}]+\}\.(label|description)`\)" src/components/chat/composer/ButtonArea.tsx` 无命中；helper key 只保留在 `chatUiBehavior.ts` fallback map 和测试反向断言中。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0；`git status --short` 显示当前仍是累计未暂存改动，无 staged 文件。
- 未验证项：未启动真实 Tauri 桌面点测 mode/reasoning selector 打开后的菜单项 label/description、当前按钮文本、hover/focus、keyboard navigation、Accessibility tree 与屏幕阅读器实际播报；未验证真实中英文 i18n 渲染截图；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、cc-gui 源码核查、Trellis 规范、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造 mode/reasoning selector 展开、provider 切换为 Codex 隐藏 plan、不同模型显示/隐藏 xhigh/max 的场景，点测菜单项 fallback、focus、keyboard navigation 与视觉密度；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 mode/reasoning selector 菜单、输入区工具栏密度、发送/停止、模型刷新、顶部状态、搜索/锚点导航和中央 diff review pane；3) 若继续代码级小步优化，优先用 CodeGraph 搜索 Chat composer completion menu 或 StatusPanel runtime context 中的 direct `t()` raw-key 风险，并单轮只挑一个高频区块；4) 若进入真实验证轮，优先覆盖本轮 mode/reasoning selector、上一轮 composer toolbar、navigation labels、diff pane collapse/reopen 焦点链、resize handles、daemon tooltip/reconnect 和 top chrome actions。

## 本轮 PLAN 2026-06-20 chat composer input surface fallback labels

- 目标：补齐 Chat composer input surface 的 i18n 未就绪兜底。上一轮已修 toolbar 外层和 mode/reasoning 当前值；本轮 CodeGraph 复查发现 `ChatComposer.tsx` 与 `ContextBar.tsx` 仍直接使用 `chat.attach`、`chat.removeAttachment`、`chat.collapsePanel`、`chat.expandPanel`、`chat.resizeComposer`、`chat.richPlaceholder`、`chat.completion.empty`、`chat.dropFileHint`、`chat.historyHint` 作为 title、aria-label、placeholder、空态或提示文案。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `useCompletions.ts`、`CompletionMenu.tsx`、`ChatComposer.tsx`、`ContextBar.tsx` 与 cc-gui `ChatInputBox/Dropdown/index.tsx`；确认本轮缺口是 composer 输入面主路径控件的 raw-key 风险。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/utils/chatUiBehavior.test.ts` 和 `src/components/chat/composer/ChatComposer.render.test.tsx`，锁定 key-only i18n 下 helper 与 DOM 输出 readable fallback。旧实现应因 helper 缺失和 DOM 输出 raw key 失败。
- 功能点 4：实现最小展示改动。验证方式：在 `src/utils/chatUiBehavior.ts` 新增 `getChatComposerInputLabel()`，并由 `ChatComposer.tsx` / `ContextBar.tsx` 使用。翻译存在时继续使用现有 i18n；翻译返回 key 时使用英文 fallback。不改补全、附件、resize、drop、history 或状态面板切换行为。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 composer input-surface chrome 必须使用 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 composer 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat composer input surface fallback labels

- 上轮进展与阻塞：上一轮已补齐 Chat composer mode/reasoning selector 当前值和菜单项文案 fallback；代码复查确认 `src/utils/chatUiBehavior.ts` 已新增 `getChatComposerModeText()` / `getChatComposerReasoningText()`，`src/components/chat/composer/ButtonArea.tsx` 已使用 helper 派生 mode/reasoning 当前值、option label 和 option description。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报、真实中英文截图，以及 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `webview/src/components/ChatInputBox/Dropdown/index.tsx` 给 completion dropdown 提供 loading/empty 文案路径，输入区 chrome 不应暴露 raw i18n key。本项目 `src/components/chat/composer/ChatComposer.tsx` / `ContextBar.tsx` 的附件、移除附件、状态面板开关、resize handle、textarea placeholder、completion empty、drop overlay、history hint 仍直接使用 `t()` 结果；key-only i18n 下会显示或播报 `chat.attach`、`chat.resizeComposer`、`chat.richPlaceholder`、`chat.completion.empty` 等 raw key。
- 本轮差距诊断（超越 cc-gui）：本项目 composer 比 cc-gui 多了图片附件、拖放、状态面板开关、可拖拽高度和 draft history hint。给这些输入面主路径控件补 fallback，可以解决“toolbar 可读，但输入框本体和上下文栏仍是 key”的断层；对首次打开、键盘/辅助技术用户、以及 i18n 初始化延迟场景价值高。
- 本轮规划：只修 composer input-surface label 派生：新增 `getChatComposerInputLabel()`，`ChatComposer.tsx` / `ContextBar.tsx` 使用 helper 生成 `Add attachment`、`Remove attachment`、`Collapse status panel`、`Expand status panel`、`Drag to resize the input`、`No matches`、`Drop to attach image` 等 fallback。翻译正常时继续使用现有 i18n；翻译返回 key 时输出英文 fallback。不改补全、附件、resize、drop、history 或状态面板切换行为，不改 store 或后端。优先级高：用户价值高（输入主路径可读）、出现频率高（每次 composer 渲染都显示）、实现成本低、回归风险低。
- 本轮完成：`src/utils/chatUiBehavior.test.ts` 新增 key-only translator 回归，先 RED 确认 `getChatComposerInputLabel is not a function`。`src/components/chat/composer/ChatComposer.render.test.tsx` 新增静态渲染回归，先 RED 确认 DOM 输出 `chat.attach`、`chat.collapsePanel`、`chat.resizeComposer`、`chat.richPlaceholder`。`src/utils/chatUiBehavior.ts` 新增 `ChatComposerInputControl`、`CHAT_COMPOSER_INPUT_LABELS` 和 `getChatComposerInputLabel()`。`src/components/chat/composer/ChatComposer.tsx` 将 resize handle、placeholder、completion empty、drop overlay、history hint 改为 helper 派生。`src/components/chat/composer/ContextBar.tsx` 将附件按钮、移除附件、状态面板开关改为 helper 派生，并给 icon-only 附件/状态面板按钮补 `aria-label`。`.trellis/spec/frontend/component-guidelines.md` 已同步：composer input-surface chrome 必须避免 raw key。
- 本轮验证：RED：`npm test -- src/utils/chatUiBehavior.test.ts src/components/chat/composer/ChatComposer.render.test.tsx` 先失败 2 项，失败点为 `getChatComposerInputLabel is not a function` 和 DOM 未包含 `title="Add attachment"`，符合旧实现缺少 input-surface fallback 的预期。GREEN 定向通过：同命令通过（2 files / 27 tests）。相邻 composer 验证通过：`npm test -- src/utils/chatUiBehavior.test.ts src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/ChatComposer.test.ts src/components/chat/composer/useCompletions.test.ts`（5 files / 52 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 370 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（Vite build passed，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均为不存在。目标控件复查：`rg "t\\('chat\\.(attach|removeAttachment|collapsePanel|expandPanel|resizeComposer|richPlaceholder|dropFileHint|historyHint)'" src/components/chat/composer/ChatComposer.tsx src/components/chat/composer/ContextBar.tsx` 无命中；`rg "t\\('chat\\.completion\\.empty'" src/components/chat/composer/ChatComposer.tsx src/components/chat/composer/CompletionMenu.tsx` 无命中。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0；`git status --short` 显示当前仍是累计未暂存改动，无 staged 文件。
- 未验证项：未启动真实 Tauri 桌面点测附件按钮、移除附件、状态面板开关、resize handle、completion empty、drop overlay、history hint 的真实中英文渲染、hover/focus、keyboard navigation、Accessibility tree 与屏幕阅读器实际播报；未验证真实 completion loading 视觉；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、cc-gui 源码核查、Trellis 规范、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造空输入、长输入、拖拽图片、附件移除、completion 空态、draft history、状态面板展开/收起、resize drag 场景，点测 composer input surface 的 hover/focus、aria-label 与视觉密度；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看输入区上下文栏、completion dropdown、mode/reasoning selector 菜单、发送/停止、模型刷新、顶部状态、搜索/锚点导航和中央 diff review pane；3) 若继续代码级小步优化，优先用 CodeGraph 搜索 StatusPanel runtime context 或 completion loading/empty menu chrome 中的剩余 direct `t()` / unreadable placeholder 风险，并单轮只挑一个高频区块；4) 若进入真实验证轮，优先覆盖本轮 input surface、上一轮 mode/reasoning selector、composer toolbar、navigation labels、diff pane collapse/reopen 焦点链、resize handles、daemon tooltip/reconnect 和 top chrome actions。

## 本轮 PLAN 2026-06-20 chat completion menu accessibility fallback

- 目标：补齐 Chat composer completion menu 的 loading 文案与基础可访问语义。上一轮已把 `emptyText` 从父组件接入 fallback；本轮 CodeGraph 复查发现 `CompletionMenu.tsx` loading 状态仍只渲染单字符省略号，菜单容器和选项没有 `listbox` / `option` 语义，选项 description 也没有进入 accessible label。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `CompletionMenu.tsx`、`ChatComposer.tsx`、`chatUiBehavior.ts` 与 cc-gui `ChatInputBox/Dropdown/index.tsx` / `DropdownItem.tsx`；确认本轮缺口是 completion menu chrome 的 loading/semantic accessibility，不改补全数据源或键盘消费逻辑。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/utils/chatUiBehavior.test.ts`，新增 `completion-loading` / `completion-menu` fallback 断言；扩展 `src/components/chat/composer/ChatComposer.render.test.tsx`，锁定 loading 文案、`role="listbox"`、`role="status"`、`role="option"`、`aria-selected` 和 option `aria-label`。旧实现应因 helper 缺项、loading 只显示省略号、缺少语义属性失败。
- 功能点 4：实现最小展示改动。验证方式：`CompletionMenu.tsx` 新增 `loadingText` / `menuLabel` props 并渲染 readable loading 文案、listbox/status/option 语义；`ChatComposer.tsx` 从 `getChatComposerInputLabel()` 传入文案。不改 `useCompletions`、选择、hover、高亮、滚动或文本插入逻辑。
- 功能点 5：同步 Trellis 规范和 i18n。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 completion menu listbox/status/option/accessibility 合同；更新 `src/locales/en.json` / `src/locales/zh.json` 增加 `chat.completion.label` 和 `chat.completion.loading`。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 composer 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat completion menu accessibility fallback

- 上轮进展与阻塞：上一轮已补齐 Chat composer input surface 的 i18n 未就绪兜底；代码复查确认 `src/utils/chatUiBehavior.ts` 已新增 `getChatComposerInputLabel()`，`src/components/chat/composer/ChatComposer.tsx` / `ContextBar.tsx` 已使用 helper 派生附件、移除附件、状态面板开关、resize handle、textarea placeholder、completion empty、drop overlay、history hint 文案，并给 icon-only 附件/状态面板按钮补了 `aria-label`。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报、真实中英文截图，以及 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `webview/src/components/ChatInputBox/Dropdown/index.tsx` 的 completion dropdown 使用明确的 loading/empty 文案，并通过 `DropdownItem` 呈现 label/description。本项目 `src/components/chat/composer/CompletionMenu.tsx` loading 状态只显示 `…`，没有可翻译 loading 文案；菜单容器没有名称，选项没有 `role="option"` / `aria-selected`，description 只视觉展示，辅助技术不一定能把说明和选项一起读出。
- 本轮差距诊断（超越 cc-gui）：本项目的 completion menu 同时承载 `/` 命令、`@` 文件、`#` 子代理、`!` prompt preset，且是输入效率核心路径。给 loading、区域名和选项语义补齐后，用户在异步补全等待、键盘高亮移动和读取长描述时能获得更明确反馈；这比单纯显示下拉列表更接近可操作控件，而不是视觉装饰。
- 本轮规划：只修 completion menu chrome：新增 `chat.completion.label` / `chat.completion.loading` i18n 与 fallback，`CompletionMenu` 新增 `menuLabel` / `loadingText` props，root 使用 `role="listbox"` / `aria-label`，loading 使用 `role="status"` / `aria-live="polite"`，选项使用 `role="option"` / `aria-selected` / 合并 label+description 的 `aria-label`。不改补全数据源、触发检测、键盘消费、选择插入、高亮滚动或后端命令。优先级中高：用户价值中高（异步补全与键盘/辅助技术路径更清晰）、出现频率高（所有 `/ @ # !` 补全共用）、实现成本低、回归风险低。
- 本轮完成：`src/utils/chatUiBehavior.test.ts` 新增 key-only translator 回归，先 RED 确认 `completion-loading` 缺少映射时读取 `label.key` 失败。`src/components/chat/composer/ChatComposer.render.test.tsx` 新增 `CompletionMenu` 静态渲染回归，先 RED 确认 loading DOM 没有 `role="listbox"` 且仍输出 `…`。`src/utils/chatUiBehavior.ts` 将 `ChatComposerInputControl` 扩展为 `completion-menu` / `completion-loading`，并在 fallback map 中加入 `Completion suggestions` / `Loading suggestions...`。`src/components/chat/composer/CompletionMenu.tsx` 新增 menu/loading 文案 props、listbox/status/option 语义和 option accessible label。`src/components/chat/composer/ChatComposer.tsx` 从 helper 派生并传入 completion menu label/loading text。`src/locales/en.json` / `src/locales/zh.json` 新增 `chat.completion.label`、`chat.completion.loading`。`.trellis/spec/frontend/component-guidelines.md` 已同步：completion menu 必须有 named listbox、readable loading status、option selected state 与 description-inclusive accessible labels。
- 本轮验证：RED：`npm test -- src/utils/chatUiBehavior.test.ts src/components/chat/composer/ChatComposer.render.test.tsx` 先失败 2 项，失败点为 `completion-loading` helper 缺映射和 completion menu loading DOM 缺 `role="listbox"`，符合旧实现缺口。GREEN 定向通过：同命令通过（2 files / 28 tests）。相邻 composer 验证通过：`npm test -- src/utils/chatUiBehavior.test.ts src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ButtonArea.test.tsx src/components/chat/composer/ChatComposer.test.ts src/components/chat/composer/useCompletions.test.ts`（5 files / 53 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 371 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（Vite build passed，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored；并行时出现 package cache/build directory lock 等待但最终通过）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理。目标控件复查：`rg "…|chat\\.completion\\.loading|chat\\.completion\\.label" src/components/chat/composer/CompletionMenu.tsx src/components/chat/composer/ChatComposer.tsx src/utils/chatUiBehavior.ts src/locales/en.json src/locales/zh.json -n` 显示 completion menu 不再含单字符 loading 省略号，新 key 只存在 helper/locales；`rg "<CompletionMenu" src -n` 确认生产调用点和测试调用点均已传入新 props。
- 未验证项：未启动真实 Tauri 桌面点测 `/ @ # !` completion menu loading/empty/items 的真实中英文渲染、keyboard navigation、高亮滚动、Accessibility tree 与屏幕阅读器实际播报；未验证真实 tooltip/description 在浏览器辅助技术中的读出顺序；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、cc-gui 源码核查、Trellis 规范、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造 `/` 命令加载、`@` 文件搜索、`#` 子代理、`!` prompt preset、无匹配和慢响应 loading 场景，点测 completion menu 的 loading/status、listbox/option、keyboard navigation、aria-label 与视觉密度；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 completion dropdown、输入区上下文栏、mode/reasoning selector 菜单、发送/停止、模型刷新、顶部状态、搜索/锚点导航和中央 diff review pane；3) 若继续代码级小步优化，优先用 CodeGraph 搜索 `StatusPanel` runtime context 中剩余 direct `t()` raw-key 风险，或检查 completion item description 的长文本截断/tooltip可读性；4) 若进入真实验证轮，优先覆盖本轮 completion menu、上一轮 input surface、mode/reasoning selector、composer toolbar、navigation labels、diff pane collapse/reopen 焦点链、resize handles、daemon tooltip/reconnect 和 top chrome actions。

## 本轮 PLAN 2026-06-20 chat status panel transient activity fallback labels

- 目标：补齐桌面 `StatusPanel` 的临时活动态与当前锚点标题 fallback。上一轮 completion menu 已收口；本轮 CodeGraph 复查发现 `src/components/chat/StatusPanel.tsx` 仍直接渲染 `t('chat.layout.streamingReply')`、`t('chat.layout.idle')`、`t('chat.layout.currentAnchor')`，key-only i18n 下会在右侧状态栏显示 raw key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 与 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx`；确认本轮缺口是状态面板临时活动文案边界，而不是状态面板结构或数据模型。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/StatusPanel.test.tsx`，锁定 key-only i18n 下 streaming、idle、current-anchor 三类文案输出 readable fallback，旧实现应因 DOM 输出 raw key 失败。
- 功能点 4：实现最小展示改动。验证方式：在 `StatusPanel.tsx` 复用已有 `translateWithFallback()`，新增 `streamingReplyLabel`、`idleActivityLabel`、`currentAnchorLabel` 并替换直接 `t()`。不改 activity card 展示条件、不改工具统计、不改锚点/滚动行为。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录状态面板临时活动态和当前锚点也必须走 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat status panel transient activity fallback labels

- 上轮进展与阻塞：上一轮已补齐 Chat composer completion menu 的 readable loading 文案、named listbox、status announcement、option selected state 和 description-inclusive accessible labels；代码复查确认 `src/components/chat/composer/CompletionMenu.tsx` / `ChatComposer.tsx` / `src/utils/chatUiBehavior.ts` 已落地。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报、真实中英文截图，以及 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 以明确 tab/status 文案组织任务、子代理和文件改动入口，不应在可见状态面板里暴露 raw translation key。本项目 `src/components/chat/StatusPanel.tsx` 已有大量 `translateWithFallback()`，但 streaming reply、idle、current anchor 三处仍直接 `t('chat.layout.*')`，key-only i18n 下会显示 `chat.layout.streamingReply`、`chat.layout.idle`、`chat.layout.currentAnchor`。
- 本轮差距诊断（超越 cc-gui）：本项目右侧状态面板比 cc-gui 多了 daemon、MCP、session load、runtime context、当前锚点等更丰富的桌面运行态。补齐临时活动态 fallback，可以解决“复杂状态块都可读，但短暂运行提示仍可能是 key”的断层；对 streaming 中、工具统计触发但无可列任务、长会话锚点回看三类高频场景有直接价值。
- 本轮规划：只修 `StatusPanel` 临时活动和当前锚点 label：复用组件内已有 `translateWithFallback()`，新增 `streamingReplyLabel`、`idleActivityLabel`、`currentAnchorLabel` 并替换三处直接 `t()`。不改 activity card 展示条件、不改工具统计、不改锚点/滚动行为、不改 i18n 资源键，因为 `en.json` / `zh.json` 已存在对应 key。优先级中高：用户价值明确、出现频率中高、实现成本极低、回归风险低。
- 本轮完成：`src/components/chat/StatusPanel.test.tsx` 新增 key-only i18n 回归，覆盖 streaming、idle、current-anchor 三种 DOM 输出；先 RED 确认旧实现输出 raw key。`src/components/chat/StatusPanel.tsx` 新增三项 fallback label 并替换直接 `t('chat.layout.streamingReply')`、`t('chat.layout.idle')`、`t('chat.layout.currentAnchor')`。`.trellis/spec/frontend/component-guidelines.md` 已同步：状态面板临时活动态和当前锚点 heading 必须遵循 readable fallback 合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为 DOM 输出 `chat.layout.streamingReply` / `chat.layout.currentAnchor`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 33 tests）。相邻 Chat UI 验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/MessageAnchorRail.test.tsx src/utils/chatUiBehavior.test.ts`（4 files / 84 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 372 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（Vite build passed，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均不存在。目标控件复查：`rg "t\\('chat\\.layout\\.(streamingReply|idle|currentAnchor)'" src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx` 无命中。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0；`git status --short` 显示当前仍是累计未暂存改动，无 staged 文件。
- 未验证项：未启动真实 Tauri 桌面点测右侧状态面板 streaming/idle/current-anchor 的真实中英文渲染、hover/focus、Accessibility tree 与屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、cc-gui 源码核查、Trellis 规范、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造 streaming reply、pending/error tool count 无详细任务、长会话 active anchor 三类状态，点测状态面板真实渲染和辅助技术标签；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看右侧状态面板、completion dropdown、输入区上下文栏、mode/reasoning selector 菜单、发送/停止、模型刷新、顶部状态、搜索/锚点导航和中央 diff review pane；3) 若继续代码级小步优化，优先用 CodeGraph 搜索 `StatusPanel`、`ChatInputStatusTabs`、`ChatDiffReviewPane` 中剩余 direct `t()` raw-key 风险，并单轮只挑一个高频区块；4) 若进入真实验证轮，优先覆盖本轮状态面板临时态、上一轮 completion menu、input surface、mode/reasoning selector、composer toolbar、navigation labels、diff pane collapse/reopen 焦点链、resize handles、daemon tooltip/reconnect 和 top chrome actions。

## 本轮 PLAN 2026-06-20 chat session sidebar fallback labels

- 目标：补齐桌面 Chat 会话侧栏主 chrome 的 i18n 未就绪兜底。本轮 CodeGraph 复查发现 `src/components/chat/ChatSessionSidebar.tsx` 仍直接使用 `chat.sessionPanel.*` 与 `common.refresh` / `common.loading` 作为标题、按钮 title、搜索 placeholder、分区标题和空态文案，key-only i18n 下会在首屏侧栏显示 raw key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ChatSessionSidebar.tsx`、`chatSessionSidebarUtils.test.ts` 与 cc-gui `ChatScreen.tsx` / `StatusPanel.tsx` 相关布局代码；确认本轮缺口是会话侧栏可见文案边界，而不是会话加载、缓存、选择或后端 session 协议。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/chatSessionSidebarUtils.test.ts`，用 key-only i18n 静态渲染 `ChatSessionSidebar`，锁定 title/new/search/section/empty-state 等主文案输出 readable fallback，旧实现应因 DOM 输出 raw key 失败。
- 功能点 4：实现最小展示改动。验证方式：在 `ChatSessionSidebar.tsx` 增加局部 `translateWithFallback()` 和侧栏文案常量，替换直接 `t()`。不改项目/会话加载副作用、不改缓存 key、不改选择/刷新/新建逻辑，不新增 i18n key。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 Chat session sidebar chrome 必须使用 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat session sidebar fallback labels

- 上轮进展与阻塞：上一轮已补齐 `StatusPanel` streaming/idle/current-anchor 的 readable fallback；代码复查确认 `src/components/chat/StatusPanel.tsx` 已复用 `translateWithFallback()`，测试和前端规范已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报、真实中英文截图，以及 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：本项目已经有比 cc-gui 更强的桌面会话侧栏，但 `src/components/chat/ChatSessionSidebar.tsx` 的首屏 chrome 仍直接依赖 `chat.sessionPanel.*`、`common.refresh`、`common.loading` 翻译结果。i18n 未初始化或 key 缺失时，标题、新建/刷新按钮、项目搜索、分区标题、空态、项目会话数、刷新中和 pending loading 都可能暴露 raw key。
- 本轮差距诊断（超越 cc-gui）：会话侧栏是本项目相对 cc-gui 的高价值增强，承担项目切换、历史会话浏览、刷新和新建入口。给可见文案、icon-only `title` / `aria-label`、搜索框 `aria-label` 同时补 fallback，可以解决“侧栏能工作但不可读/不可听”的断层，尤其对键盘和辅助技术用户更直接。
- 本轮规划：只修会话侧栏 chrome label 边界：在 `ChatSessionSidebar.tsx` 增加局部 `translateWithFallback()` 和集中 label 常量，替换直接 `t()`；给项目刷新、新建会话、会话刷新按钮和项目/会话搜索框接入相同 fallback `aria-label`。不改项目/会话加载副作用、不改缓存 key、不改选择/刷新/新建逻辑、不新增 i18n key。优先级高：用户价值高（首屏高频导航可读）、出现频率高、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/chatSessionSidebarUtils.test.ts` 新增 key-only i18n 静态渲染回归，锁定 `Session Management`、`New chat`、`Search projects...`、`Projects`、`No projects`、`Sessions`、`No sessions`、`Refresh`，并检查关键 `aria-label` 不退回 raw key；RED 已确认旧实现输出 `chat.sessionPanel.*` / `common.refresh`。`src/components/chat/ChatSessionSidebar.tsx` 新增 `translateWithFallback()`，并替换标题、新建/刷新按钮、搜索 placeholder、分区标题、空态、项目会话数、刷新中、无匹配和 pending loading 文案；同时为 icon-only 按钮和搜索框补 `aria-label`。`.trellis/spec/frontend/component-guidelines.md` 已记录 session sidebar chrome fallback 合同。
- 本轮验证：GREEN 定向通过：`npm test -- src/components/chat/chatSessionSidebarUtils.test.ts`（1 file / 18 tests）。目标扫描通过：`rg -n "t\\('chat\\.sessionPanel|t\\('common\\.(refresh|loading)'" src/components/chat/ChatSessionSidebar.tsx` 无命中。相邻 Chat UI 验证通过：`npm test -- src/components/chat/chatSessionSidebarUtils.test.ts src/stores/useChatStore.test.ts src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/MessageAnchorRail.test.tsx src/utils/chatUiBehavior.test.ts src/utils/chatDaemonStatus.test.ts`（7 files / 168 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 373 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（Vite build passed，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0；`git status --short` 显示当前仍是累计未暂存改动，无 staged 文件。
- 未验证项：未启动真实 Tauri 桌面点测会话侧栏项目列表、会话列表、刷新/新建按钮、pending loading、搜索框 placeholder / focus / `aria-label` 的真实中英文渲染和辅助技术输出；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、Trellis 规范、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造多项目、多会话、空项目、刷新中、pending session、项目/会话搜索无匹配场景，点测侧栏可见文案、hover title、keyboard focus 与 Accessibility tree；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看会话侧栏、右侧状态面板、completion dropdown、输入区上下文栏、mode/reasoning selector、搜索/锚点导航和中央 diff review pane；3) 若继续代码级小步优化，优先用 CodeGraph 搜索 `ChatInputStatusTabs` 直出 empty-state `t()` 的剩余 raw-key 风险，或按高频度检查 `MessageList` / `MessageItem` / `MarkdownBlock` 的 icon-only / placeholder fallback；4) 若进入真实验证轮，优先覆盖本轮 session sidebar、上一轮状态面板临时态、completion menu、input surface、navigation labels、diff pane collapse/reopen 焦点链、resize handles、daemon tooltip/reconnect 和 top chrome actions。

## 本轮 PLAN 2026-06-20 chat message list search fallback labels

- 目标：补齐 `MessageList` 搜索摘要、全历史搜索重试/加载/错误和折叠早期消息提示的 i18n 未就绪兜底。本轮 CodeGraph 先复查 `ChatInputStatusTabs`，发现 tasks/subagents/edits 空态虽然仍有 direct `t()`，但受 `visibleTabs` 门控，正常 DOM 路径几乎不可达；继续扫描同一 Chat 浏览路径后确认 `src/components/chat/MessageList.tsx` 的搜索和 reveal chrome 是更高频、更可触达的 raw-key 风险。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断。验证方式：读取 `MessageList.tsx`、相关 MessageList 测试与 cc-gui 消息列表/搜索实现；确认本轮缺口是 transcript 浏览 chrome 的 label 边界，不改搜索过滤、全历史搜索状态机、分页 reveal 或滚动保持逻辑。
- 功能点 3：补 RED 测试。验证方式：扩展 MessageList 相关测试，用 key-only i18n 静态渲染搜索有结果/无结果、全历史搜索 error/retry/loading、collapsed earlier 提示，旧实现应因 DOM 输出 raw `chat.layout.search*` / `chat.message.showEarlier` key 失败。
- 功能点 4：实现最小展示改动。验证方式：在 `MessageList.tsx` 增加局部 `translateWithFallback()` 与 label 常量/函数，替换搜索摘要、retry 按钮、loading/error 说明、show-earlier title/文本的直接 `t()`。不改消息窗口计算、不改筛选、不改 reveal 回调、不改 `MessageItem`。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 MessageList 搜索/reveal chrome 必须使用 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat message list search fallback labels

- 上轮进展与阻塞：上一轮已补齐 Chat session sidebar 主 chrome 的 readable fallback；代码复查确认 `src/components/chat/ChatSessionSidebar.tsx` 已新增局部 `translateWithFallback()`，标题、刷新/新建、项目/会话搜索、空态、项目会话数、刷新中和 pending loading 都已改为 fallback label，并给 icon-only 按钮/搜索框补 `aria-label`。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报、真实中英文截图，以及 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的消息列表/搜索体验不会在搜索摘要和历史加载提示里暴露翻译 key。本项目 `src/components/chat/MessageList.tsx` 已具备会话内搜索、完整历史搜索状态和折叠早期消息提示，但搜索结果数、无结果、完整历史加载/失败、重试按钮和折叠提示仍直接使用 `t('chat.layout.search*')` / `t('chat.message.showEarlier')`；key-only i18n 下会在 transcript 主浏览路径显示 raw key。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 多了 windowed session 与显式 full-history search 状态。给这些状态补 readable fallback，可以解决“导航侧栏/状态栏已可读，但搜索和历史浏览主路径仍可能是 key”的断层；用户在长会话查找、加载完整历史失败、滚动查看更早消息时仍能理解当前范围与下一步操作。
- 本轮规划：只修 `MessageList` 搜索/reveal chrome：新增局部 `translateWithFallback()`、搜索结果/无结果、full-history retry/loading/error 和 show-earlier label 派生，替换直接 `t()`。不改搜索过滤、不改完整历史搜索状态机、不改分页 reveal、不改滚动保持、不改 `MessageItem`。优先级中高：用户价值中高（长会话搜索和历史浏览可读）、出现频率中高、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/MessageList.test.tsx` 将 i18n mock 扩展为可切换 key-only 模式，并新增 key-only 静态渲染回归；RED 已确认旧实现输出 `chat.layout.searchNoResults`、`chat.layout.searchFullHistoryRetry`、`chat.layout.searchFullHistoryError`。`src/components/chat/MessageList.tsx` 新增局部 `translateWithFallback()` 和 label 派生函数/常量，搜索摘要、retry 按钮 `aria-label`/文本、full-history loading/error 说明和 collapsed-earlier title/文本均改为 readable fallback。`.trellis/spec/frontend/component-guidelines.md` 已记录 MessageList search/reveal chrome fallback 合同。
- 本轮验证：RED：`npm test -- src/components/chat/MessageList.test.tsx` 先失败 1 项，失败点为 DOM 输出 raw `chat.layout.searchNoResults` / `chat.layout.searchFullHistoryRetry` / `chat.layout.searchFullHistoryError`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 4 tests）。目标扫描通过：`rg -n "t\\('chat\\.layout\\.search|t\\('chat\\.message\\.showEarlier" src/components/chat/MessageList.tsx` 无命中。相邻 Chat UI 验证通过：`npm test -- src/components/chat/MessageList.test.tsx src/utils/chatNavigation.test.ts src/utils/chatUiBehavior.test.ts src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/MessageAnchorRail.test.tsx`（6 files / 102 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（37 files / 374 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（Vite build passed，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0；`git status --short` 显示当前仍是累计未暂存改动，无 staged 文件。
- 未验证项：未启动真实 Tauri 桌面点测会话内搜索、完整历史搜索 loading/error/retry、折叠早期消息提示在真实中英文、hover title、keyboard focus、Accessibility tree 与屏幕阅读器中的表现；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、Trellis 规范、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造长会话 windowed session、搜索命中/无命中、完整历史加载中、完整历史失败可重试、折叠早期消息提示场景，点测搜索摘要、retry、loading/error 和 reveal title/文本；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 transcript 搜索、长会话 reveal、会话侧栏、右侧状态面板、completion dropdown、输入区上下文栏、mode/reasoning selector 和中央 diff review pane；3) 若继续代码级小步优化，优先检查 `MessageItem` / `MarkdownBlock` / `WaitingIndicator` / `StreamingPlaceholder` 中仍直接 `t()` 的可触达 icon-only、等待态和 copy-code raw-key 风险；4) `ChatInputStatusTabs` 的 no-tasks/no-subagents/no-edits direct `t()` 可作为低优先级清理，但当前正常 DOM 路径受 visible-tab 门控，优先级低于可触达 transcript/message chrome。

## 本轮 PLAN 2026-06-20 chat message item waiting fallback labels

- 目标：补齐消息转写区主路径的 i18n 未就绪兜底。CodeGraph 复查确认 `MessageItem.tsx` 的角色、复制、空用户消息、assistant streaming chip 以及 `WaitingIndicator.tsx` / `StreamingPlaceholder.tsx` 的等待/连接提示仍直接使用 `chat.message.*` 翻译结果；key-only i18n 下会在高频 transcript 区域暴露 raw key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / locale 诊断。验证方式：读取 `MessageItem.tsx`、`WaitingIndicator.tsx`、`StreamingPlaceholder.tsx`、相关测试风格和 `src/locales/en.json` / `zh.json`；确认本轮缺口是展示层文案边界，不改复制、消息过滤、流式占位延迟或 Markdown 渲染。
- 功能点 3：补 RED 测试。验证方式：新增/扩展组件静态渲染测试，用 key-only i18n 锁定 `You`、`AI Assistant`、`System`、`Copy`、`Empty message`、`Connected, generating response...`、`Waiting for response...`，旧实现应因 DOM 输出 raw `chat.message.*` key 失败。
- 功能点 4：实现最小展示改动。验证方式：在 `MessageItem.tsx`、`WaitingIndicator.tsx`、`StreamingPlaceholder.tsx` 增加局部 `translateWithFallback()` 并替换直接 `t()`。翻译存在时继续使用 i18n；翻译返回 key 时使用英文 fallback。不改行为逻辑、不新增 i18n key。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 message transcript chrome 的 readable fallback 合同。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 chat message item waiting fallback labels

- 上轮进展与阻塞：上一轮已补齐 `MessageList` 搜索摘要、全历史搜索 loading/error/retry 和折叠早期消息提示的 readable fallback；代码复查确认 `src/components/chat/MessageList.tsx` 已新增局部 fallback 派生，测试和前端规范已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报、真实中英文截图，以及 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的消息转写区不会在角色、复制、等待或流式连接提示里暴露 raw i18n key。本项目 `src/components/chat/MessageItem.tsx` 仍直接把 `chat.message.user`、`chat.message.assistant`、`chat.message.system`、`chat.message.copy`、`chat.message.copied`、`chat.message.emptyUser`、`chat.message.streamingConnected` 用于角色、复制按钮、空态和 streaming chip；`src/components/chat/WaitingIndicator.tsx` / `StreamingPlaceholder.tsx` 仍直接使用 `chat.message.waiting` / `chat.message.streamingConnected`。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 多了更明确的 assistant streaming chip 与全局等待占位。给这些高频 transcript 状态补 readable fallback，可以解决“搜索/reveal 可读，但正在生成和复制主路径仍可能是 key”的断层；对键盘、hover tooltip 和辅助技术用户尤其直接。
- 本轮规划：只修消息转写区展示文案边界：`MessageItem.tsx`、`WaitingIndicator.tsx`、`StreamingPlaceholder.tsx` 增加局部 `translateWithFallback()`，使用 `You`、`AI Assistant`、`System`、`Copy`、`Copied`、`Empty message`、`Waiting for response...`、`Connected, generating response...` fallback。翻译正常时继续使用 i18n；翻译返回 key 时输出英文 fallback。不改复制行为、不改消息过滤、不改流式占位延迟、不改 Markdown 渲染、不新增 i18n key。优先级中高：用户价值中高（高频 transcript 主路径可读）、出现频率高、实现成本低、回归风险低。
- 本轮完成：新增 `src/components/chat/MessageItem.test.tsx`，覆盖 key-only i18n 下的用户角色、复制按钮 title/aria/可见文案、空用户消息 fallback、assistant streaming chip、`WaitingIndicator` 和 `StreamingPlaceholder` 初始等待文案；RED 已确认旧实现输出 `chat.message.user`、`chat.message.copy`、`chat.message.waiting` 等 raw key。`src/components/chat/MessageItem.tsx` 新增局部 fallback helper 并替换角色、复制、空态、streaming chip 的直接 `t()`。`src/components/chat/WaitingIndicator.tsx` 和 `src/components/chat/StreamingPlaceholder.tsx` 新增局部 fallback helper 并替换等待/连接提示。`.trellis/spec/frontend/component-guidelines.md` 已记录 message transcript chrome fallback 合同。新增测试文件已按项目规则 `git add`，其他累计改动保持未暂存。
- 本轮验证：RED：`npm test -- src/components/chat/MessageItem.test.tsx` 先失败 4 项，失败点为 DOM 输出 raw `chat.message.user`、`chat.message.copy`、`chat.message.waiting`，符合旧实现缺口；同时确认完全空 user 和 system 消息受现有 `shouldRenderChatMessage` 过滤，因此测试调整为可触达的 error user 空态和用户/assistant/等待路径。GREEN 定向通过：同命令通过（1 file / 4 tests）。目标扫描通过：`rg -n "t\\('chat\\.message\\.(user|assistant|system|copy|copied|emptyUser|waiting|streamingConnected)'" src/components/chat/MessageItem.tsx src/components/chat/WaitingIndicator.tsx src/components/chat/StreamingPlaceholder.tsx` 无命中。相邻 Chat UI 验证通过：`npm test -- src/components/chat/MessageItem.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageAnchorRail.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatUiBehavior.test.ts`（6 files / 92 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（38 files / 378 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（Vite build passed，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0；`git status --short` 显示新增 `src/components/chat/MessageItem.test.tsx` 已 staged，其余仍为累计未暂存改动。
- 未验证项：未启动真实 Tauri 桌面点测用户/assistant 消息、复制按钮 hover/focus、等待条、流式占位和 streaming chip 的真实中英文渲染、Accessibility tree 与屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、locale 文案核对、Trellis 规范、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造普通用户消息、assistant streaming、空内容发送失败、等待响应、长会话搜索/reveal 场景，点测消息区可见文案、复制 tooltip/aria、等待态和屏幕阅读器输出；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 transcript 消息区、搜索/reveal、会话侧栏、右侧状态面板、completion dropdown、输入区上下文栏和中央 diff review pane；3) 若继续代码级小步优化，优先检查 `MarkdownBlock` 的 code-copy `chat.markdown.copyCode` / `chat.markdown.copiedCode` raw-key 风险，或低优先级清理 `ChatInputStatusTabs` 空态 direct `t()`；4) 若进入真实验证轮，优先覆盖本轮消息转写区、上一轮 MessageList 搜索/reveal、session sidebar、状态面板临时态、completion menu、input surface、navigation labels、diff pane collapse/reopen 焦点链、resize handles、daemon tooltip/reconnect 和 top chrome actions。

## 本轮 PLAN 2026-06-20 markdown code copy fallback labels

- 目标：补齐 Markdown 代码块复制按钮的 i18n 未就绪兜底。CodeGraph 复查确认 `src/components/chat/MarkdownBlock.tsx` 在 DOM effect 中直接把 `chat.markdown.copyCode` / `chat.markdown.copiedCode` 写入 code-copy 按钮 `title` 和 `aria-label`；key-only i18n 下会暴露 raw key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui / locale 诊断。验证方式：读取本项目 `MarkdownBlock.tsx` 和 locale；核对 cc-gui `MarkdownBlock` code-copy title/aria 模式；确认本轮缺口是 code-copy 文案边界，不改 sanitizer、highlight、复制行为或 DOM 结构。
- 功能点 3：补 RED 测试。验证方式：新增 `MarkdownBlock.test.tsx`，用 key-only translator 锁定 helper 输出 `Copy code` / `Copied code`，旧实现应因缺少导出 helper 失败。
- 功能点 4：实现最小展示改动。验证方式：在 `MarkdownBlock.tsx` 新增 `getMarkdownCodeCopyLabels()` / 局部 fallback，并由 code-copy DOM effect 使用。翻译存在时继续使用 i18n；翻译返回 key 时使用英文 fallback。不新增 i18n key。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 Markdown code-copy `title` / `aria-label` 必须使用 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、目标扫描、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 markdown code copy fallback labels

- 上轮进展与阻塞：上一轮已补齐消息转写区角色、复制、空用户消息、等待条、流式占位和 assistant streaming chip 的 readable fallback；代码复查确认 `src/components/chat/MessageItem.tsx`、`WaitingIndicator.tsx`、`StreamingPlaceholder.tsx` 已用局部 fallback helper 替换目标 direct `t()`，`src/components/chat/MessageItem.test.tsx` 和前端规范已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报、真实中英文截图，以及 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `webview/src/components/MarkdownBlock.tsx` 的代码块复制按钮会提供可读 `title` / `aria-label`。本项目 `src/components/chat/MarkdownBlock.tsx` 已有 code-copy DOM effect，但直接把 `chat.markdown.copyCode` / `chat.markdown.copiedCode` 写入按钮 `title` 和 `aria-label`；i18n 未初始化或 key 缺失时，用户 hover 或辅助技术读取会得到 raw key。
- 本轮差距诊断（超越 cc-gui）：本项目已经把 transcript、导航、状态面板、composer 和消息区主路径逐步补齐 fallback；继续补 Markdown code-copy 可以把“读代码、复制代码”这个高频开发者动作纳入同一可访问性合同，解决“消息按钮可读，但代码块按钮不可读”的断层。
- 本轮规划：只修 `MarkdownBlock` code-copy label 派生：新增 `getMarkdownCodeCopyLabels()` 和局部 `translateWithFallback()`，DOM effect 中初始复制态、复制成功态、复制状态复位均复用 `Copy code` / `Copied code` fallback。翻译正常时继续使用现有 i18n；翻译返回 key 时输出英文 fallback。不改 sanitizer、不改 marked/highlight、不改复制行为、不改按钮 DOM 结构、不新增 i18n key。优先级中高：用户价值中高（代码复制是 Chat 开发场景高频动作）、出现频率中高、实现成本低、回归风险低。
- 本轮完成：新增 `src/components/chat/MarkdownBlock.test.tsx`，用 key-only translator 锁定 `Copy code` / `Copied code` fallback，并验证真实翻译结果仍被保留；RED 已确认旧实现缺少 `getMarkdownCodeCopyLabels` 导出。`src/components/chat/MarkdownBlock.tsx` 新增导出 helper，并将 code-copy 按钮初始 `title` / `aria-label`、copied 状态 `title` / `aria-label`、复位后的 `title` / `aria-label` 全部改为 helper 派生。`.trellis/spec/frontend/component-guidelines.md` 已记录 Markdown code-copy fallback 合同。新增测试文件已按项目规则 `git add`；上一轮 `MessageItem.test.tsx` 仍保持 staged，其他累计改动保持未暂存。
- 本轮验证：RED：`npm test -- src/components/chat/MarkdownBlock.test.tsx` 先失败 2 项，失败点为 `getMarkdownCodeCopyLabels is not a function`，符合旧实现缺少 helper 合同的预期。GREEN 定向通过：同命令通过（1 file / 2 tests）。目标扫描通过：`rg -n "t\\('chat\\.markdown\\.(copyCode|copiedCode)'" src/components/chat/MarkdownBlock.tsx` 无命中。相邻 Chat UI 验证通过：`npm test -- src/components/chat/MarkdownBlock.test.tsx src/components/chat/MessageItem.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatUiBehavior.test.ts`（6 files / 89 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（39 files / 380 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（Vite build passed，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0；`git status --short` 显示新增 `src/components/chat/MarkdownBlock.test.tsx` 与上一轮 `MessageItem.test.tsx` 已 staged，其余仍为累计未暂存改动。
- 未验证项：未启动真实 Tauri 桌面点测 Markdown 代码块复制按钮在真实 DOM effect 后的 hover title、复制成功态、复位态、键盘 focus、Accessibility tree 与屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、cc-gui 源码核查、locale 文案核对、Trellis 规范、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造包含多语言代码块的 assistant 回复，点测代码块复制按钮的 hover title、aria label、复制成功/复位状态和 clipboard 结果；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 Markdown 代码块、消息区、搜索/reveal、会话侧栏、右侧状态面板、completion dropdown 和中央 diff review pane；3) 若继续代码级小步优化，优先清理 `ChatInputStatusTabs` 的低频空态 direct `t()`，或继续 CodeGraph 搜索 Chat UI 剩余 `title` / `aria-label` direct `t()` raw-key 风险；4) 若进入真实验证轮，优先覆盖本轮 Markdown code-copy、上一轮消息转写区、MessageList 搜索/reveal、session sidebar、状态面板临时态、completion menu、input surface、navigation labels、diff pane collapse/reopen 焦点链、resize handles、daemon tooltip/reconnect 和 top chrome actions。

## 本轮 PLAN 2026-06-20 input status empty fallback labels

- 目标：收口 `ChatInputStatusTabs` 小屏 fallback 面板空态的 i18n 未就绪边界。CodeGraph 复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 仍有 `chat.layout.inputStatusNoTasks`、`chat.layout.inputStatusNoSubagents`、`chat.layout.inputStatusNoEdits` 三处 direct `t()`；虽然当前 `visibleTabs` 门控让这些空态在正常 DOM 路径中低频/近似不可达，但一旦异步数据变化或后续显隐逻辑调整触达，会暴露 raw key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / locale 诊断。验证方式：读取本项目 `ChatInputStatusTabs.tsx`、测试和 locale；确认本轮缺口是空态文案边界，不改 tab 显隐、popover、任务/子代理/编辑选择逻辑。
- 功能点 3：补 RED 测试。验证方式：扩展 `ChatInputStatusTabs.test.tsx`，新增 helper 的 key-only translator 回归，旧实现应因缺少导出 helper 失败。
- 功能点 4：实现最小展示改动。验证方式：新增 `getInputStatusEmptyPanelLabel()`，三处空态渲染复用 helper。翻译存在时继续使用 i18n；翻译返回 key 时输出英文 fallback。不新增 i18n key。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 input status empty states 必须使用 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、目标扫描、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 input status empty fallback labels

- 上轮进展与阻塞：上一轮已补齐 Markdown 代码块复制按钮 `title` / `aria-label` 的 readable fallback；代码复查确认 `src/components/chat/MarkdownBlock.tsx` 已导出并使用 `getMarkdownCodeCopyLabels()`，`src/components/chat/MarkdownBlock.test.tsx` 和前端规范已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报、真实中英文截图，以及 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的状态/详情区域不会在空态面板里向用户暴露 raw i18n key。本项目 `src/components/chat/ChatInputStatusTabs.tsx` 在小屏 fallback 详情面板中仍直接使用 `chat.layout.inputStatusNoTasks`、`chat.layout.inputStatusNoSubagents`、`chat.layout.inputStatusNoEdits`；虽然当前 `visibleTabs` 门控使这些空态低频/近似不可达，但后续显隐逻辑变化或异步状态切换时可能暴露 raw key。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 多了输入区下方任务、子代理、文件编辑聚合状态。把低频空态也纳入 readable fallback 合同，可以避免高频路径已收口、边缘状态仍露 key 的体验断层；对小屏、键盘导航和辅助技术用户更稳。
- 本轮规划：只修 `ChatInputStatusTabs` 空态文案边界：新增 `getInputStatusEmptyPanelLabel()`，使用 `No task or tool activity yet`、`No subagent calls yet`、`No file edits yet` fallback。翻译正常时继续使用 i18n；翻译返回 key 时输出英文 fallback。不改 tab 显隐、不改 popover、不改任务/子代理/编辑选择逻辑、不新增 i18n key。优先级中低但成本极低：用户价值中等、出现频率低、实现成本低、回归风险低，适合作为当前 raw-key 收口批次的尾项。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.test.tsx` 新增 helper key-only 和真实翻译回归，先 RED 确认旧实现缺少 `getInputStatusEmptyPanelLabel` 导出；后 GREEN 锁定 tasks/subagents/edits 三类空态 fallback。`src/components/chat/ChatInputStatusTabs.tsx` 新增 `ChatInputStatusEmptyPanel`、`INPUT_STATUS_EMPTY_PANEL_LABELS` 和 `getInputStatusEmptyPanelLabel()`，并将三处空态渲染改为 helper 派生。`.trellis/spec/frontend/component-guidelines.md` 已同步 input status empty panel fallback 合同。`TODO_LIST.md` 已追加本轮 PLAN 与迭代记录。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 2 项，失败点为 `getInputStatusEmptyPanelLabel is not a function`，符合旧实现缺少 helper 合同的预期。GREEN 定向通过：同命令通过（1 file / 24 tests）。目标扫描通过：`rg -n "t\\('chat\\.layout\\.inputStatusNo(Tasks|Subagents|Edits)'" src/components/chat/ChatInputStatusTabs.tsx` 无命中。相邻 Chat UI 验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/utils/chatUiBehavior.test.ts src/components/chat/MarkdownBlock.test.tsx src/components/chat/MessageItem.test.tsx src/components/chat/MessageList.test.tsx`（6 files / 91 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（39 files / 382 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（Vite build passed，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0；`git status --short` 显示新增 `src/components/chat/MarkdownBlock.test.tsx` 与 `src/components/chat/MessageItem.test.tsx` 仍 staged，其余为累计未暂存改动。
- 未验证项：未启动真实 Tauri 桌面点测小屏 input status fallback 面板的空态可见文案、focus、popover 切换、Accessibility tree 与屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、locale 文案核对、Trellis 规范、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造小屏、无任务/无子代理/无编辑、有任务、有编辑、流式生成中等场景，点测 input status tabs 的空态、popover、focus 与 Accessibility tree；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看输入区状态聚合、Markdown 代码块、消息转写区、搜索/reveal、会话侧栏、右侧状态面板、completion dropdown 和中央 diff review pane；3) 若继续代码级小步优化，优先用 CodeGraph 搜索 Chat UI 剩余 `title` / `aria-label` / placeholder direct `t()` raw-key 风险，并单轮只挑一个可触达高频区块；4) 若进入真实验证轮，优先覆盖本轮 input status empty panel、上一轮 Markdown code-copy、消息转写区、MessageList 搜索/reveal、session sidebar、completion menu、input surface、navigation labels、diff pane collapse/reopen 焦点链、resize handles、daemon tooltip/reconnect 和 top chrome actions。

## 本轮 PLAN 2026-06-20 prompt enhancer dialog fallback labels

- 目标：补齐 Chat composer prompt enhancer 弹窗的 i18n 未就绪兜底。CodeGraph 复查确认 `src/components/chat/composer/PromptEnhancerDialog.tsx` 仍直接把 `chat.enhancer.title`、`chat.enhancer.original`、`chat.enhancer.enhanced`、`chat.enhancer.loading`、`chat.enhancer.keepOriginal`、`chat.enhancer.useEnhanced` 和 `common.close` 用于可见标题、分栏标题、loading、按钮和关闭按钮 title；key-only i18n 下会在高频 prompt 优化工作流里暴露 raw key。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `PromptEnhancerDialog.tsx`、`ChatComposer.tsx` 传参和现有 composer 测试；确认本轮缺口是弹窗展示文案边界，不改 prompt enhance 请求、快捷键、portal、loading/disabled 或按钮回调。
- 功能点 3：补 RED 测试。验证方式：新增 `src/components/chat/composer/PromptEnhancerDialog.test.tsx`，用 key-only i18n 锁定弹窗标题、分栏标题、loading、关闭按钮 title 和底部按钮不暴露 raw key；旧实现应因 DOM 输出 `chat.enhancer.*` / `common.close` 失败。
- 功能点 4：实现最小展示改动。验证方式：在 `PromptEnhancerDialog.tsx` 增加局部 `translateWithFallback()` 和 label 派生。翻译存在时继续使用 i18n；翻译返回 key 时使用英文 fallback。不新增 i18n key。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 prompt enhancer modal chrome 必须使用 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、目标扫描、相邻 composer/Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 prompt enhancer dialog fallback labels

- 上轮进展与阻塞：上一轮已补齐 `ChatInputStatusTabs` 小屏 fallback 详情面板空态的 readable fallback；代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已新增并使用 `getInputStatusEmptyPanelLabel()`，`src/components/chat/ChatInputStatusTabs.test.tsx` 和前端规范已同步。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报、真实中英文截图，以及 cc-gui 真实运行并排截图未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的 prompt / input 辅助弹窗不会在标题、关闭按钮、分栏标题、loading 或操作按钮上暴露 raw i18n key。本项目 `src/components/chat/composer/PromptEnhancerDialog.tsx` 仍直接使用 `chat.enhancer.title`、`chat.enhancer.original`、`chat.enhancer.enhanced`、`chat.enhancer.loading`、`chat.enhancer.keepOriginal`、`chat.enhancer.useEnhanced` 和 `common.close`，key-only i18n 下会让 prompt 优化工作流出现 raw key。
- 本轮差距诊断（超越 cc-gui）：本项目比 cc-gui 多了内置 prompt enhance 对比弹窗。给这个弹窗补 readable fallback，可以让“输入、补全、工具栏已可读，但 prompt 优化弹窗仍露 key”的断层收口；用户在优化提示词这种高意图流程里，可以直接看到原文、增强版、保留原文、使用增强版，而不是被翻译 key 打断。
- 本轮规划：只修 prompt enhancer modal chrome label 派生：新增 `getPromptEnhancerDialogLabels()`，使用 `Enhance prompt`、`Close`、`Original prompt`、`Enhanced prompt`、`Enhancing prompt...`、`Keep original`、`Use enhanced` fallback。翻译正常时继续使用 i18n；翻译返回 key 时输出英文 fallback。不改 prompt enhance 请求、不改快捷键、不改 portal、不改 loading/disabled 或按钮回调。优先级中高：用户价值中高（prompt 优化是输入区高意图工作流）、出现频率中、实现成本低、回归风险低。
- 本轮完成：新增 `src/components/chat/composer/PromptEnhancerDialog.test.tsx`，用 key-only translator 锁定弹窗标题、关闭、原文/增强版分栏、loading 和 footer actions 的 readable fallback，并验证真实翻译仍被保留；RED 已确认旧实现缺少 `getPromptEnhancerDialogLabels` 导出。`src/components/chat/composer/PromptEnhancerDialog.tsx` 新增 `PROMPT_ENHANCER_DIALOG_LABELS` 与导出 helper，并将目标 direct `t()` 渲染改为 `labels.*`。`.trellis/spec/frontend/component-guidelines.md` 已同步 prompt enhancer modal chrome fallback 合同。新增测试文件已按项目规则 `git add`；其他累计改动保持原状态。
- 本轮验证：RED：`npm test -- src/components/chat/composer/PromptEnhancerDialog.test.tsx` 先失败 2 项，失败点为 `getPromptEnhancerDialogLabels is not a function`，符合旧实现缺少 helper 合同的预期。GREEN 定向通过：同命令通过（1 file / 2 tests）。目标扫描通过：`rg -n "t\\('(chat\\.enhancer\\.(title|original|enhanced|loading|keepOriginal|useEnhanced)|common\\.close)'" src/components/chat/composer/PromptEnhancerDialog.tsx` 无命中。相邻 composer / Chat UI 验证通过：`npm test -- src/components/chat/composer/PromptEnhancerDialog.test.tsx src/components/chat/composer/ChatComposer.render.test.tsx src/components/chat/composer/ChatComposer.test.ts src/components/chat/composer/ButtonArea.test.tsx src/utils/chatUiBehavior.test.ts src/components/chat/ChatInputStatusTabs.test.tsx`（6 files / 71 tests）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。全量前端验证通过：`npm test`（40 files / 384 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（Vite build passed，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（114 tests passed，2 doctests ignored；并行时出现 package/build lock 等待但最终通过）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已解析为工作区内路径并清理，复查结果均不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0；`git status --short` 显示新增 `src/components/chat/composer/PromptEnhancerDialog.test.tsx`、`src/components/chat/MarkdownBlock.test.tsx`、`src/components/chat/MessageItem.test.tsx` 已 staged，其余为累计未暂存改动。
- 未验证项：未启动真实 Tauri 桌面点测 prompt enhancer 弹窗的真实中英文标题、关闭按钮 title、loading 态、增强结果态、保留/使用按钮、keyboard shortcut、focus trap、Accessibility tree 与屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、Trellis 规范、RED/GREEN 回归、全量前端/后端测试和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造 prompt enhance 成功/失败/加载中、有原文/空原文、Enter/Escape 快捷键场景，点测弹窗文案、焦点、按钮 disabled 与 Accessibility tree；2) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 prompt enhancer、input toolbar、completion menu、input status、Markdown code-copy、消息转写区、搜索/reveal 和中央 diff review pane；3) 若继续代码级小步优化，优先检查 `SdkDependencyPanel`、`ToolPermissionDialog`、`ContentBlockRenderer` 中仍可触达的 direct `t()` raw-key 风险，并单轮只挑一个高频/恢复路径区块；4) 若进入真实验证轮，优先覆盖本轮 prompt enhancer、上一轮 input status empty panel、Markdown code-copy、消息转写区、MessageList 搜索/reveal、session sidebar、completion menu、navigation labels、diff pane collapse/reopen 焦点链、resize handles、daemon tooltip/reconnect 和 top chrome actions。

## 本轮 PLAN 2026-06-20 assistant raw message merge regression

- 目标：修复 Chat 中 AI 返回消息“出现后又消失、只能展示最新一段”的问题。根因初步定位在 `mergeRawChatMessage()`：assistant 结构化 raw 事件会覆盖同一条 assistant 消息旧 raw；而 `MessageItem` 一旦有 raw blocks 就优先渲染 blocks，不再渲染 `message.content`，导致前面的文本/工具块被最新 raw 遮掉。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 Trellis task artifacts、前端 state/component/quality/type 规范、`TODO_LIST.md` 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`。
- 功能点 2：CodeGraph 根因诊断。验证方式：沿 `chat://stream` / `chat://message` -> `appendToStreamingAssistant()` / `mergeRawChatMessage()` -> `MessageItem` 渲染优先级追踪，确认问题不是后端没有返回，而是前端 raw 合并覆盖。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/utils/chatMessageFlow.test.ts`，构造同一 streaming assistant 先收到文本 raw、再收到工具 raw 的场景，要求最终 raw blocks 保留两者；旧实现应只保留最新工具 raw 并失败。
- 功能点 4：实现最小修复。验证方式：`mergeRawChatMessage()` 对 assistant raw 更新时合并已有 assistant raw content 与新 raw content，而不是整体替换；保留 streaming 文本 `content`、message id、createdAt、usage/duration 等状态。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/state-management.md`，记录 assistant `chat://message` raw blocks 是增量结构化事件，必须合并到当前 assistant 消息，不能覆盖旧 raw blocks。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 store/message flow 测试、`npm test`、`npm run build`、必要后端 check、IDE build、`git diff --check` / `git diff --cached --check`。

## 迭代记录 2026-06-20 assistant raw message merge regression

- 上轮进展与阻塞：上一轮已优化大 Claude 历史会话首屏加载，但真实 Tauri 桌面耗时点测仍未完成。本轮用户反馈“聊天有 bug，返回消息又消失，AI 返回的消息不能完全展示，只能展示最新的”。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；按 Trellis inline 流程读取当前任务制品、前端 state/component/quality/type 规范和相关源码。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的 transcript 不会因为后续工具事件到达而把同一轮 assistant 早先文本/工具块从 UI 中抹掉。本项目 `MessageItem` 在有 `raw` blocks 时优先渲染 `ContentBlockRenderer`，不再渲染 `message.content`；而 `src/utils/chatMessageFlow.ts` 的 `mergeRawChatMessage()` 对 assistant raw 更新是整体替换，所以同一轮后续 `tool_use` / text raw 会覆盖早先 raw，导致看起来“只剩最新消息”。
- 本轮差距诊断（超越 cc-gui）：流式文本来自 `chat://stream` 的 `message.content`，结构化工具/文本来自 `chat://message` 的 `raw`。两条流应该在 store 层合并成同一条 assistant 消息，而不是让渲染层二选一。修复后即使第一条 raw 是工具块，也会把当前已流式显示的文本作为 fallback text block 合入 raw，避免文本被工具块遮掉。
- 本轮规划：只改 `mergeRawChatMessage()` 的 assistant raw 合并边界和相关测试；不改 MessageItem 样式、不改后端事件协议、不改 tool block 渲染、不改历史会话窗口逻辑。优先级最高：用户价值高（直接修复回复消失）、出现频率高（每次工具调用/结构化事件都可能触发）、实现成本低（纯前端 state helper）、风险可控（user/tool_result 逻辑不动）。
- 本轮完成：`src/utils/chatMessageFlow.test.ts` 新增两条回归：1) 同一 streaming assistant 先有文本 raw、后有 tool raw 时保留两者；2) streaming text 先进入 `message.content`、第一个 raw 是 tool block 时，raw 里仍保留文本 block。`src/utils/chatMessageFlow.ts` 新增 assistant raw 合并 helper：用 block key 去重累计快照，合并旧 raw blocks 和新 raw blocks；当 raw 尚无文本块但 message 已有 streamed content 时，先 seed 一个 text block，防止后续 tool raw 遮掉文本。`src/stores/useChatStore.test.ts` 更新 stale request 测试断言，确认当前 request 的 raw 现在同时保留 streamed text 与 tool block。`.trellis/spec/frontend/state-management.md` 已同步 assistant raw incremental merging 契约。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED 1：`npm test -- src/utils/chatMessageFlow.test.ts` 先失败，旧实现只得到 `[\"tool_use\"]`，缺少早先文本 raw。GREEN 1：实现 raw 合并后定向通过。RED 2：补充真实流式路径后同命令再次失败，旧实现仍只得到 `[\"tool_use\"]`，缺少 `message.content` seed。GREEN 2：补充 fallback text seed 后定向通过（1 file / 19 tests）。相邻验证通过：`npm test -- src/stores/useChatStore.test.ts src/components/chat/MessageItem.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/ContentBlockRenderer.test.tsx src/utils/chatStatusSummary.test.ts src/utils/chatMessageFlow.test.ts`（6 files / 101 tests）。全量前端验证通过：`npm test`（40 files / 386 tests，仅 Browserslist/caniuse-lite 过期提示）。构建通过：`npm run build`（Vite build passed，仅 Browserslist/caniuse-lite 过期提示）。后端快速编译通过：`cargo check --manifest-path src-tauri/Cargo.toml`。未运行完整 `cargo test`，因为本轮只改前端 state/helper/spec/TODO。
- 未验证项：未启动真实 Tauri 桌面复现一轮带文本 + tool_use + tool_result 的实时对话；未录屏确认 UI 中前文不会再消失。当前结论基于真实源码追踪、RED/GREEN 回归、相邻测试、全量前端测试和构建。
- 下一步候选：1) 启动 Tauri 桌面，发送会触发工具调用的 prompt，观察 assistant 文本、thinking/tool blocks 和最终回答是否都持续保留；2) 如果仍有“只显示最新”现象，下一轮继续查 daemon 是否发了 `[BLOCK_RESET]` 或 Codex/Claude adapter 是否发送了非增量快照；3) 增加一个 store 级事件顺序测试，直接模拟 `chat://stream` + `chat://message` + `chat://done` 的完整工具调用回合，锁定 UI 层消息数组最终形态；4) 若真实桌面确认修复，再把本轮变更单独整理提交，避免和历史 Chat UI parity 脏改混在一起。

## 本轮 PLAN 2026-06-20 large Claude history first-paint fast path

- 目标：回应“历史会话只要是大会话，就加载特别慢”。复查真实链路后确认前端 `loadSession()` 已调用 `get_unified_session_message_window` 做首屏窗口加载，但 Claude provider 的窗口实现仍会逐行解析整份 JSONL，导致大历史首屏仍是 O(full JSON parse)。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis phase context、`package.json` 和 backend spec；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，本轮无法基于该路径做 cc-gui 代码核实。
- 功能点 2：CodeGraph 根因诊断。验证方式：沿 `useChatStore.loadSession()` -> `get_unified_session_message_window` -> `session_manager::load_message_window()` -> `load_claude_message_window()` 复查真实代码，区分前端窗口策略已存在和后端窗口仍全量解析的问题。
- 功能点 3：补 RED 测试。验证方式：先运行 `cargo test --manifest-path src-tauri/Cargo.toml session_manager::providers::claude::tests::read_claude_tail_window_lines_avoids_front_matter_for_large_history`，旧实现应因缺少尾部读取 helper 失败；再补大文件尾部 meta 行回归，防止 fast path 只读尾部候选行时返回空窗口。
- 功能点 4：实现最小后端性能修复。验证方式：`src-tauri/src/session_manager/providers/claude.rs` 小文件继续走精确旧路径；大于 1 MiB 的 Claude JSONL 先按字节扫描计数行，再 seek 文件尾部读取有限 JSONL 行并只解析尾部消息；若尾部 meta/空行导致消息不足，则逐步扩大尾部行窗口直到拿满 `tailLimit` 或到达文件开头。`src-tauri/src/session_manager/mod.rs` 仅把 `normalize_message_window_limit()` 调整为 `pub(crate)` 供 provider 复用。
- 功能点 5：同步 Trellis backend spec。验证方式：更新 `.trellis/spec/backend/cross-layer-protocol.md` 的 “Chat Historical Message First-Paint Window” 契约，明确大 Claude fast path 的 `startIndex/totalCount` 可为 JSONL 源行位置/行数上界，完整精确计数仍由 full history loader 提供。
- 功能点 6：质量验证。验证方式：运行 `cargo fmt --manifest-path src-tauri/Cargo.toml`、Claude provider 定向测试、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、`npm test`、`npm run build`、`git diff --check`，并确认构建产物未进入工作树。

## 迭代记录 2026-06-20 large Claude history first-paint fast path

- 上轮进展与阻塞：上一轮已补齐 prompt enhancer 弹窗 readable fallback，新增 `src/components/chat/composer/PromptEnhancerDialog.test.tsx` 并通过前端/后端全量验证；本轮用户反馈转向“大历史会话加载慢”。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；`C:/guodevelop/jetbrains-cc-gui` 不存在，因此 cc-gui 相关对标无法在该路径代码核实，以下性能根因基于本项目真实代码。
- 本轮差距诊断（补齐 cc-gui）：本项目前端已在 `src/stores/useChatStore.ts` 的 `loadSession()` 调用 `get_unified_session_message_window`，并用 `historyWindow.startIndex` 映射首屏历史消息；后端命令也存在于 `src-tauri/src/commands/session_commands.rs`。真实缺口在 provider：`src-tauri/src/session_manager/providers/claude.rs` 的 `load_claude_message_window()` 原本仍用 `MessageWindowBuilder` 遍历并 JSON 解析整份 Claude JSONL，只在内存里保留 tail，因此大历史首屏仍会卡在全量 JSON parse。
- 本轮差距诊断（超越 cc-gui）：会话切换首屏不应等待完整历史解析。大 Claude JSONL 应先用尾部 seek + 有界 JSON 解析给出最近消息，让用户先恢复上下文；完整历史、精确计数和搜索再走 full loader。这样解决的是“点击大会话后像假死”的痛点，而不是单纯减少渲染元素。
- 本轮规划：只优化 Claude 大历史首屏窗口，不碰 Codex。优先级最高：用户价值高（直接对应反馈）、出现频率高（历史会话入口）、实现成本中低（provider 内部改动 + 单测）、风险可控。Codex 暂缓的原因是 `load_codex_message_window()` 调用 `parse_codex_history_line(&line, window.next_index())`，fallback tool id 依赖全局 parsed-message index；贸然改尾部 seek 会影响 tool result 配对和历史工具块稳定性，需要单独设计。
- 本轮完成：`src-tauri/src/session_manager/mod.rs` 将 `normalize_message_window_limit()` 改为 `pub(crate)`，避免 Claude provider 复制 `tailLimit` clamp 逻辑。`src-tauri/src/session_manager/providers/claude.rs` 新增大文件 fast path：小于 1 MiB 保持精确全量解析；大文件先计数 JSONL 行，再从文件尾部按 64 KiB chunk seek 读取有限尾部行，只解析候选尾部消息；尾部 meta/空行导致消息不足时自动扩大尾部行预算，直到拿满窗口或读到文件开头。新增 `read_claude_tail_window_lines_avoids_front_matter_for_large_history` 和 `load_claude_message_window_expands_tail_when_meta_lines_fill_suffix` 回归测试。`.trellis/spec/backend/cross-layer-protocol.md` 已记录大 Claude fast path 的源行索引/行数上界语义。`TODO_LIST.md` 已追加本轮 PLAN 与迭代记录。
- 本轮验证：RED：`cargo test --manifest-path src-tauri/Cargo.toml session_manager::providers::claude::tests::read_claude_tail_window_lines_avoids_front_matter_for_large_history` 先失败，失败点为旧实现没有 `read_claude_tail_window_lines`。GREEN 定向通过：`cargo test --manifest-path src-tauri/Cargo.toml session_manager::providers::claude::tests -- --nocapture` 通过（8 tests）。格式化通过：`cargo fmt --manifest-path src-tauri/Cargo.toml`。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored；期间出现 build/package lock 等待但最终通过）。项目命令已从 `package.json` 确认：`test` 为 `vitest run`，`build` 为 `tsc && vite build`。前端验证通过：`npm test`（40 files / 384 tests，仅 Browserslist/caniuse-lite 过期提示）、`npm run build`（Vite build passed，仅 Browserslist/caniuse-lite 过期提示）。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示。
- 未验证项：未启动真实 Tauri 桌面用超大 Claude 历史文件测量首屏耗时；未对 Codex 大历史做 fast path；未在 `C:/guodevelop/jetbrains-cc-gui` 做代码对标，因为该路径不存在。
- 下一步候选：1) 用真实 Tauri 桌面选择一个超大 Claude 历史会话，记录 `lastSessionLoadMetrics.windowLoadMs/windowMapMs` 和用户感知首屏时间，确认不再假死；2) 为 Codex 设计安全 fast path，重点先解决 fallback tool id 的全局 parsed-message index 保真，再考虑尾部 seek；3) 若 Claude 超大文件仍慢，继续把 `count_jsonl_lines()` 从全文件字节扫描优化为可接受的近似 total 或缓存 source path + file length + modified timestamp；4) 在前端会话侧栏展示首屏/完整加载进度时区分 “estimated line count” 和 “exact message count”，避免用户把大 Claude fast path 的上界 total 当成精确消息数。

## 本轮 PLAN 2026-06-20 assistant raw streaming delta sync

- 目标：继续修复用户反馈的“AI 返回消息出现后又消失、不能完全展示，只能展示最新的”。上一轮已修 `chat://message` assistant raw 覆盖问题；本轮进一步覆盖真实流式顺序：assistant raw blocks 已存在后，后续 `[CONTENT_DELTA]` 仍只更新 `message.content`，而 `MessageItem` 有 raw 时优先渲染 raw blocks，导致后续文本增量在 UI 上不可见。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端 state/component/quality/type 规范与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 原路径不可读，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 根因诊断。验证方式：沿 `useChatStore.init()` 的 `chat://stream` / `chat://message` 监听、`appendToStreamingAssistant()`、`mergeRawChatMessage()` 与 `MessageItem` raw 优先渲染边界追踪，确认本轮缺口是 raw 已存在后的 stream delta 未同步到 raw text block。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/stores/useChatStore.test.ts`，模拟完整 live turn：`send()` 后先收 `[CONTENT_DELTA]`，再收 assistant `tool_use` raw，再继续收 `[CONTENT_DELTA]`，最后 `chat://done`；旧实现应因 raw text 只停留在第一段 streamed seed 而失败。
- 功能点 4：实现最小状态层修复。验证方式：`appendToStreamingAssistant()` 在追加 `message.content` 时，如果当前 streaming assistant 已有 assistant raw，则同步 raw 中第一个 text block 为完整 streamed content；若 raw 只有工具块，则补一个 text block。不改后端协议、不改 MessageItem 渲染优先级、不改 tool_result/user raw 逻辑。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/state-management.md` 的 assistant raw incremental merging 合同，记录 stream delta 与 assistant raw 必须保持同一可渲染文本视图。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 store/message flow 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out` 构建产物。

## 迭代记录 2026-06-20 assistant raw streaming delta sync

- 上轮进展与阻塞：上一轮已修复 assistant `chat://message` raw 覆盖旧 raw blocks 的问题，并优化大 Claude 历史首屏窗口加载；但用户继续反馈聊天中“AI 返回消息不能完全展示，只能展示最新的”。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户要求的原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。已知阻塞：未启动真实 Tauri 桌面复现/录屏。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的 transcript 不会因为结构化工具块到达而让后续 assistant 文本增量从可见 UI 消失。本项目 `MessageItem` 在有 `raw` blocks 时优先渲染 `ContentBlockRenderer`，不会再渲染 `message.content`；上一轮只保证 raw 到来时把当时已有 streamed content seed 进 raw，但 raw 已存在后继续到达的 `[CONTENT_DELTA]` 仍只更新 `message.content`，导致后续文本增量隐藏在未渲染字段里。
- 本轮差距诊断（超越 cc-gui）：本项目同时接收 `chat://stream` 文本流和 `chat://message` 结构化 raw 流，状态层应持续维护一个统一可渲染视图。把后续 stream delta 同步进 assistant raw text block 后，用户在工具调用前、中、后的文本都能持续保留；这比在组件层临时拼接两个来源更稳，因为 store 才是事件时序边界。
- 本轮规划：只改 `appendToStreamingAssistant()` 的 assistant raw 同步边界和 store 级完整事件序列测试；不改后端协议、不改 `MessageItem` raw 优先渲染、不改 `mergeRawChatMessage()` 的 user/tool_result 逻辑、不改 UI 样式。优先级最高：用户价值高（直接修复可见回复丢失）、出现频率高（带工具调用的流式回复都可能触发）、实现成本低（store 层小 helper）、回归风险可控。
- 本轮完成：`src/stores/useChatStore.test.ts` 新增完整 live turn 回归：`send()` 后依次触发 `chat://stream`、assistant `tool_use` raw、后续 `chat://stream`、`chat://done`，断言最终 assistant `message.content` 和 `raw` text block 都包含完整两段文本且保留工具块。`src/stores/useChatStore.ts` 新增 `syncAssistantRawWithStreamingContent()`，在 `appendToStreamingAssistant()` 追加 delta 后，如果 streaming assistant 已有 assistant raw，则把 raw 中第一个 text block 同步为完整 streamed content；如果 raw 只有工具块，则补 text block。`.trellis/spec/frontend/state-management.md` 已补充 stream delta 与 assistant raw text block 必须同步的合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/stores/useChatStore.test.ts` 先失败 1 项，失败点为 raw text block 仍只有 `I will inspect the file.\n`，缺少后续 `It contains the raw block render path.`，符合真实缺口。GREEN 定向通过：同命令通过（1 file / 55 tests）。相邻验证通过：`npm test -- src/stores/useChatStore.test.ts src/utils/chatMessageFlow.test.ts src/components/chat/MessageItem.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/ContentBlockRenderer.test.tsx`（5 files / 95 tests）。全量前端验证通过：`npm test`（40 files / 387 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端快速编译通过：`cargo check --manifest-path src-tauri/Cargo.toml`。IDE `build_project` 通过，`problems: []`。`git diff --check` 与 `git diff --cached --check` exit 0，仅输出 Windows LF/CRLF 转换提示。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查结果均不存在。
- 未验证项：未启动真实 Tauri 桌面发送一轮带文本 + tool_use + 后续文本 + tool_result 的实时对话；未录屏确认 UI 中前文和后文都不会再消失。当前结论基于真实源码追踪、RED/GREEN 回归、相邻测试、全量前端测试、构建、后端 check 和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，发送会触发工具调用且工具后继续输出文本的 prompt，观察 assistant 前文、工具块、后文、最终回答是否持续保留；2) 如果仍有“只显示最新”现象，下一轮继续查 daemon 是否发了 `[BLOCK_RESET]` 或 provider adapter 是否发送了非增量 snapshot 并覆盖语义；3) 给 `tool_result` 到达后的 UI 状态补一条 store/component 联合回归，确保工具结果更新不会扰动 assistant raw text；4) 若真实桌面确认修复，建议把消息消失修复相关文件单独整理提交，避免和历史 Chat UI parity 脏改混在一起。
## 本轮 PLAN 2026-06-20 assistant raw text snapshot dedupe

- 目标：继续收口“AI 返回消息不能完全展示/只显示最新”的同一风险面。上一轮已让 raw 已存在后的 stream delta 同步进 raw text block；本轮覆盖后续 assistant `chat://message` 又发送 text raw 快照的顺序，避免完整文本被重复追加到工具块后面，造成 transcript 重复或顺序混乱。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端 state/quality/type 规范与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 原路径不可读，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 根因诊断。验证方式：沿 `chat://stream` / `chat://message` -> `appendToStreamingAssistant()` / `mergeRawChatMessage()` -> `mergeAssistantRaw()` 追踪，确认本轮缺口是 stream 已同步 raw 后，后续 text raw 快照仍按 text key 追加。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/stores/useChatStore.test.ts`，模拟 stream 文本、tool raw、后续 stream 文本、assistant text raw 快照、done；旧实现应出现两个 text block。
- 功能点 4：实现最小状态层修复。验证方式：让 assistant raw 合并在已有 streamed `content` 可覆盖 text raw 快照时跳过该 text block，并让 store 的 stream raw sync 只保留一个完整文本块，避免同一轮文本重复展示。不改 tool_use/tool_result 查找、不改 MessageItem 渲染。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/state-management.md`，记录 assistant text raw snapshot 与 stream content 等价时必须去重。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 store/message flow 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 assistant raw text snapshot dedupe

- 上轮进展与阻塞：上一轮已让 assistant raw 已存在后的后续 `[CONTENT_DELTA]` 同步进 raw text block，修复后续文本只停留在隐藏 `message.content` 的问题；代码复查确认 `src/stores/useChatStore.ts` 已新增 `syncAssistantRawWithStreamingContent()`，`src/stores/useChatStore.test.ts` 已有完整 live turn 回归。已知阻塞仍是真实 Tauri 桌面复现/录屏未完成。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户要求的原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui transcript 不应在同一轮里重复展示同一段 assistant 文本。本项目上一轮已把完整 streamed content 写入 raw text block；但如果 provider 随后又通过 `chat://message` 发来 assistant text raw delta/snapshot，`mergeAssistantRaw()` 会按 text 内容 key 把这段文本追加到工具块后面。真实表现可能从“消失”变成“前文 + 工具块 + 重复后文”，仍是 stream/raw 双流合并不稳定。
- 本轮差距诊断（超越 cc-gui）：本项目有 stream 文本流、assistant raw 快照、tool_use/tool_result 三条交错路径。store/helper 层应该把这些事件归一成一个稳定 transcript：一个完整 text block 加非文本结构化块，而不是把 SDK 的重复 text raw 泄漏给渲染层。这样可以减少 provider 事件顺序差异带来的 UI 闪烁、重复或顺序混乱。
- 本轮规划：只修 assistant text raw 与 streamed content 的去重边界；不改 tool_use/tool_result 查找、不改 MessageItem raw 优先渲染、不改后端协议、不改 UI 样式。优先级高：用户价值高（继续收口消息显示错乱）、出现频率中高（取决于 provider 是否同时发 stream 与 text raw）、实现成本低、回归风险低。
- 本轮完成：`src/stores/useChatStore.test.ts` 新增完整事件序列回归：stream 第一段、tool raw、stream 第二段、assistant text raw delta、done，断言最终只有一个完整 text block 和一个 tool_use block。`src/utils/chatMessageFlow.ts` 新增 `isTextAlreadyRepresentedInContent()`，在 `mergeAssistantRaw()` 合并 assistant raw 时跳过已被完整 streamed `content` 覆盖的 text raw delta/snapshot。`src/stores/useChatStore.ts` 调整 `syncAssistantRawWithStreamingContent()`，stream 同步 raw 时只保留一个完整 text block，并保留非 text blocks，避免已有重复 text block 被后续 delta 继续带下去。`.trellis/spec/frontend/state-management.md` 已同步 assistant text raw delta/snapshot 去重合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：第一次完整文本快照用例已被现有实现覆盖，随后改为更真实的“后半段 text raw delta”后，`npm test -- src/stores/useChatStore.test.ts` 失败 1 项，失败点为可渲染 blocks 长度为 3 而不是 2，符合缺口。GREEN 定向通过：`npm test -- src/stores/useChatStore.test.ts`（1 file / 56 tests）和 `npm test -- src/utils/chatMessageFlow.test.ts`（1 file / 19 tests）。相邻验证通过：`npm test -- src/stores/useChatStore.test.ts src/utils/chatMessageFlow.test.ts src/components/chat/MessageItem.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/ContentBlockRenderer.test.tsx`（5 files / 96 tests）。全量前端验证通过：`npm test`（40 files / 388 tests，仅 Browserslist/caniuse-lite 过期提示）。前端构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。`git diff --check` 与 `git diff --cached --check` exit 0，仅输出 Windows LF/CRLF 转换提示。构建/IDE 生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查结果均不存在。
- 未验证项：未启动真实 Tauri 桌面复现 provider 同时发 stream + assistant text raw delta 的实时对话；未录屏确认 UI 不重复、不消失。本轮结论基于真实源码追踪、RED/GREEN 回归、相邻测试、全量前端/后端测试、构建和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，发送会触发工具调用且工具后继续输出文本的 prompt，观察 stream/raw 混合下是否无消失、无重复；2) 给 `tool_result` 到达后的 UI 状态补联合回归，确认工具结果更新只影响工具状态，不扰动 assistant text raw；3) 如果仍有异常，继续查 daemon 是否发 `[BLOCK_RESET]` 或 provider adapter 是否发送非增量 snapshot；4) 若真实桌面确认修复，建议把消息显示修复相关文件单独整理提交，避免与历史 Chat UI parity 累计改动混在一起。
## 本轮 PLAN 2026-06-20 out-of-order tool result lookup fallback

- 目标：继续收口用户反馈的“AI 返回内容/工具状态不完整”问题。上一轮已修复 assistant raw 覆盖、stream delta 同步、重复 text raw 去重；本轮覆盖事件乱序边界：internal `tool_result` 先进入 transcript，assistant `tool_use` raw 后到时，工具块仍应解析到结果而不是长期 pending。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端 state/component/quality 规范与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不可读，实际可读对标路径沿用 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 根因诊断。验证方式：沿 `ContentBlockRenderer` -> `MessageItem.findToolResult` -> `src/utils/chatMessageFlow.ts` 的 `findToolResult()` 追踪真实代码，确认旧实现只从 assistant 消息索引向后扫描 `tool_result`。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/utils/chatMessageFlow.test.ts`，构造 user prompt、提前到达的 internal user `tool_result`、后到的 assistant `tool_use`，调用 `findToolResult(messages, toolId, assistantIndex)`；旧实现应返回 `null` 并失败。
- 功能点 4：实现最小状态流修复。验证方式：`findToolResult()` 保持向后扫描优先，找不到时再扫描 `startIndex` 之前的消息，复用同一匹配 helper；不改变消息存储顺序、不改 UI 组件、不改 tool_result raw 合并策略。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/state-management.md` 的 Chat Message Raw Event Merging 契约，明确 tool result lookup 后向优先、前向兜底的乱序兼容行为与测试要求。
- 功能点 6：质量验证。验证方式：执行 RED/GREEN 定向测试、相邻 chat flow/store/render 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、必要时 `cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out` 构建产物。

## 迭代记录 2026-06-20 out-of-order tool result lookup fallback

- 上轮进展与阻塞：上一轮已完成 assistant raw text snapshot/delta 去重，代码中确实存在 `src/utils/chatMessageFlow.ts` 的 assistant raw merge 去重和 `src/stores/useChatStore.ts` 的 raw text sync；用户反馈的“返回消息又消失、只能展示最新”核心风险已覆盖 raw 覆盖、stream delta 隐藏和 text raw 重复三条路径。已知阻塞仍是真实 Tauri 桌面实时对话未录屏验证。本轮开工确认 `C:/guodevelop/ccg-switch` 与 `.codegraph/` 可读；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的工具块不会因为前端事件乱序而长期 pending。本项目 `src/utils/chatMessageFlow.ts` 的 `findToolResult()` 旧实现只从 assistant 消息索引向后扫描，如果 internal user `tool_result` 先于 assistant `tool_use` raw 被写入消息数组，后到的工具块无法找到已存在结果。
- 本轮差距诊断（超越 cc-gui）：本项目同时合并 stream 文本、assistant raw 和 internal tool_result raw，事件顺序比单一 transcript 更复杂。结果查找应保持普通顺序优先，同时容忍乱序到达，避免用户看到“工具一直执行中”或误判 AI 输出缺失。
- 本轮规划：只改 `findToolResult()` 的查找边界和单元测试，不改消息存储顺序、不改 `MessageItem`/`ContentBlockRenderer` 结构、不改 `mergeRawChatMessage()` 的 tool_result 合并策略。优先级高：用户价值高（工具状态完整）、出现频率中（取决于 event delivery 时序）、实现成本低（helper + 回归测试）、风险低（向后查找优先保持原语义）。
- 本轮完成：`src/utils/chatMessageFlow.test.ts` 新增乱序回归，构造 prompt、提前到达的 internal `tool_result` 和后到的 assistant `tool_use`，断言 `findToolResult(messages, 'tool-early', 2)` 能解析早到结果。`src/utils/chatMessageFlow.ts` 新增 `findToolResultInRange()` 复用匹配逻辑，`findToolResult()` 现在先从 `startIndex` 向后找，找不到再扫描 `startIndex` 之前的消息。`.trellis/spec/frontend/state-management.md` 已同步 Chat Message Raw Event Merging 契约，明确后向优先、前向兜底的乱序工具结果查找规则。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/utils/chatMessageFlow.test.ts` 先失败 1 项，旧实现返回 `null`，符合缺口。GREEN 定向通过：同命令通过（1 file / 20 tests）。相邻验证通过：`npm test -- src/utils/chatMessageFlow.test.ts src/stores/useChatStore.test.ts src/components/chat/ContentBlockRenderer.test.tsx src/components/chat/MessageItem.test.tsx`（4 files / 93 tests）。全量前端验证通过：`npm test`（40 files / 389 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。`git diff --check` 与 `git diff --cached --check` exit 0；`git diff --check` 仅输出 Windows LF/CRLF 转换提示。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查均不存在。
- 未验证项：未启动真实 Tauri 桌面构造 tool_result 先到、tool_use 后到的实时事件流；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码追踪、RED/GREEN 回归、相邻测试、全量前后端测试、构建和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，发送会触发工具调用和后续文本的 prompt，观察 assistant 文本、工具块状态和 tool_result 展示是否持续完整；2) 给 `tool_result` 更新后的 component 层补一条 UI 回归，确认工具状态从 pending 到 completed/error 不扰动 assistant text block；3) 若仍出现“只显示最新”，继续追踪 daemon/provider 是否发出 reset/snapshot 覆盖语义；4) 将本轮及前几轮消息消失修复整理成独立提交，避免与历史 Chat UI parity 展示类改动混杂。

## 本轮 PLAN 2026-06-20 MessageList out-of-order tool result render fallback

- 目标：把上一轮工具层 `findToolResult()` 的乱序兼容补到 MessageList 渲染层。当前 `MessageList.tsx` 为性能预建 `toolResultById`，但只从可见窗口首条消息索引向后扫描；当 internal `tool_result` 先到、assistant `tool_use` 后到，且搜索/窗口折叠让可见起点落在 assistant 上时，UI 仍会把已完成工具显示为 pending。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、前端规范与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读路径沿用 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 根因诊断。验证方式：沿 `MessageList.toolResultById` -> `MessageItem.findToolResult` -> `ContentBlockRenderer` 追踪，确认渲染层没有调用已修复的 utility helper，而是自建只向后扫描的结果 map。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/MessageList.test.tsx`，构造较早 internal user `tool_result` 与较晚 assistant `tool_use`，用 `searchQuery` 让可见起点从 assistant 开始；旧实现应只渲染 pending 而不是 completed。
- 功能点 4：实现最小渲染层修复。验证方式：抽出 MessageList 内部 range collector，先扫描 `toolResultSearchStartIndex..messages.length`，找不到的 id 再由 `0..toolResultSearchStartIndex` 补齐；保持后向优先，不改变消息顺序、不改 tool block 组件、不改 store/raw 合并。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/state-management.md`，记录 MessageList 可见窗口结果 map 必须遵守与 utility lookup 一致的后向优先、前向兜底规则。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat render/store/message flow 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out` 构建产物。

## 迭代记录 2026-06-20 MessageList out-of-order tool result render fallback

- 上轮进展与阻塞：上一轮已修复 `src/utils/chatMessageFlow.ts` 的 `findToolResult()` 乱序查找，utility 层已能先查后续消息、再兜底早到的 internal `tool_result`。本轮代码复查确认 `src/components/chat/MessageList.tsx` 仍自建 `toolResultById` map，且只从 `visibleMessages[0]?.originalIndex` 向后扫描，因此搜索/折叠窗口下仍可能把已完成工具显示为 pending。已知阻塞仍是真实 Tauri 桌面实时事件流未录屏验证。本轮开工确认 `C:/guodevelop/ccg-switch` 与 `.codegraph/` 可读；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的工具块状态不会因为可见窗口/搜索窗口的起点变化而丢失已存在的工具结果。本项目在 `MessageList` 渲染层仍只向后收集 `tool_result`，如果 internal `tool_result` 先到、assistant `tool_use` 后到，并且当前 searchQuery 只命中 assistant 工具块，UI 会继续渲染 `tool-status-indicator pending`。
- 本轮差距诊断（超越 cc-gui）：本项目支持搜索结果窗口、折叠历史窗口和结构化工具块。结果 map 应该在保持常规后续结果优先的同时容忍早到结果，避免用户在搜索工具调用时误以为工具还没完成，或误判 AI 返回内容不完整。
- 本轮规划：只修 `MessageList` 的预计算结果 map，不改 store 事件顺序、不改 `MessageItem`/`ContentBlockRenderer` API、不改 tool block 样式。优先级高：用户价值高（工具状态完整、减少“返回消失/未完成”的误判）、出现频率中（搜索/窗口折叠 + 事件乱序时触发）、实现成本低（range collector + 组件回归）、风险低（后续结果仍优先）。
- 本轮完成：`src/components/chat/MessageList.test.tsx` 新增组件级回归，构造较早 hidden internal user `tool_result` 和较晚 assistant `Read` `tool_use`，通过 `searchQuery="package.json"` 让可见起点落在 assistant 工具块，断言工具状态为 completed、不是 pending，且 `[tool_result]` 不作为用户气泡泄漏。`src/components/chat/MessageList.tsx` 新增 `collectToolResultsInRange()`，`toolResultById` 先收集可见起点之后的结果，再回扫更早消息补缺，保持后向优先语义。`.trellis/spec/frontend/state-management.md` 已同步 MessageList 可见窗口 map 必须遵守 later-first / earlier-fallback 的合同。
- 本轮验证：RED：`npm test -- src/components/chat/MessageList.test.tsx` 先失败 1 项，旧实现输出 `tool-status-indicator pending`，缺少 `tool-status-indicator completed`。GREEN 定向通过：同命令通过（1 file / 5 tests）。相邻验证通过：`npm test -- src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/components/chat/ContentBlockRenderer.test.tsx src/utils/chatMessageFlow.test.ts src/stores/useChatStore.test.ts`（5 files / 98 tests）。全量前端验证通过：`npm test`（40 files / 390 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored；期间有 Cargo file lock 等待但最终通过）。IDE `build_project` 通过，`problems: []`。`git diff --check` 与 `git diff --cached --check` exit 0，未发现空白错误，仅 `git diff --check` 输出 Windows LF/CRLF 转换提示。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查均不存在。
- 未验证项：未启动真实 Tauri 桌面构造“tool_result 先到、tool_use 后到、搜索命中工具块”的实时事件流；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph、RED/GREEN 回归、相邻测试、全量前后端测试、生产构建和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，发送会触发工具调用和后续文本的 prompt，观察 assistant 文本、工具块 completed/error 状态和 tool_result 展示是否持续完整；2) 做真实运行并排截图审计，重点看搜索窗口、折叠历史、工具块完成态、Markdown code-copy、输入区状态和 diff review pane；3) 若仍出现“只显示最新”，继续追踪 daemon/provider 是否发出 reset/snapshot 覆盖语义；4) 将消息显示修复相关文件整理成独立提交，避免与历史 Chat UI parity 展示类累计改动混杂。

## 本轮 PLAN 2026-06-20 SDK dependency panel readable fallback

- 目标：补齐 Chat SDK 依赖管理入口的 readable i18n fallback。当前 `src/components/chat/SdkDependencyPanel.tsx` 和 `src/pages/ChatPage.tsx` SDK 弹窗标题/关闭按钮直接渲染 `t('chat.sdk.*')` / `t('common.close')`；当 i18n 未初始化或 key 缺失时会暴露 raw key，尤其在 SDK 缺失、用户需要安装依赖的恢复路径里显得不可靠。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读路径沿用 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / 源码诊断。验证方式：读取 `SdkDependencyPanel.tsx`、`ChatPage.tsx` SDK modal 片段、`useSdkStore.ts` 与 locales，确认本轮只处理 SDK dependency UI 文案 fallback，不改安装/卸载/刷新行为、不改 Tauri 命令。
- 功能点 3：补 RED 测试。验证方式：新增 `src/components/chat/SdkDependencyPanel.test.tsx`，mock `useSdkStore` 和 key-only i18n，断言标题、hint、refresh、installed/not installed、install/uninstall、installing log 均显示可读 fallback，旧实现应因输出 `chat.sdk.*` key 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `SdkDependencyPanel.tsx` 增加导出的 label helper，组件内部和 `ChatPage` SDK modal title/close 共用；翻译正常时保留真实翻译，翻译返回 key 时使用英文 fallback。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 SDK dependency panel/modal chrome 必须遵守 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out` 构建产物。

## 迭代记录 2026-06-20 SDK dependency panel readable fallback

- 上轮进展与阻塞：上一轮已完成 `MessageList` 渲染层 out-of-order `tool_result` 兜底，代码中确实存在 `collectToolResultsInRange()` 和组件级回归；工具层与渲染层都已覆盖“早到 result、后到 tool_use”的 pending 误判。已知阻塞仍是真实 Tauri 桌面实时事件流和 cc-gui 真实运行并排截图未验证。本轮开工确认 `C:/guodevelop/ccg-switch` 与 `.codegraph/` 可读；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的恢复/依赖管理入口不会在标题、按钮、状态和安装日志占位里暴露内部 i18n key。本项目 `src/components/chat/SdkDependencyPanel.tsx` 直接渲染 `chat.sdk.title`、`chat.sdk.refresh`、`chat.sdk.hint`、`chat.sdk.installed`、`chat.sdk.notInstalled`、`chat.sdk.uninstall`、`chat.sdk.installing`、`chat.sdk.install`；`src/pages/ChatPage.tsx` 的 SDK modal 外框也直接渲染 `chat.sdk.title` 和 `common.close`。如果 i18n 尚未初始化或 SDK key 缺失，用户在安装 Claude/Codex SDK 的恢复路径会看到 raw key。
- 本轮差距诊断（超越 cc-gui）：SDK 依赖安装是 Chat 不可用时的恢复路径。给这个入口做 readable fallback，不只是“文案更好看”，而是避免用户在修复环境时被内部 key 打断判断，尤其是刷新、安装中、安装/卸载这些状态和动作需要立即可懂。
- 本轮规划：只修 SDK dependency panel/modal chrome 文案派生；不改 `useSdkStore`、不改 install/uninstall/refresh 调用、不改 Tauri 命令、不改 SDK 状态模型。优先级中高：用户价值高（Chat 不能启动或 SDK 缺失时的关键恢复路径）、出现频率中（首次安装/依赖损坏/多 provider 切换时触发）、实现成本低、回归风险低。
- 本轮完成：新增并暂存 `src/components/chat/SdkDependencyPanel.test.tsx`，mock `useSdkStore` 和 key-only i18n，覆盖标题、hint、refresh、installed/not installed、install/uninstall、installing log placeholder 的 readable fallback，并验证翻译存在时保留翻译。`src/components/chat/SdkDependencyPanel.tsx` 新增 `getSdkDependencyPanelLabels()` 和 fallback 常量，组件内部所有 SDK chrome label 改为 `labels.*`，并补充按钮 `type`、`title`、`aria-label`。`src/pages/ChatPage.tsx` 的 SDK modal title/close 改为复用同一 helper。`.trellis/spec/frontend/component-guidelines.md` 已同步 SDK dependency recovery UI 的 readable fallback 合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/chat/SdkDependencyPanel.test.tsx` 先失败 2 项，失败点为 key-only 输出 `chat.sdk.*` 且 `getSdkDependencyPanelLabels is not a function`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 2 tests）。相邻验证通过：`npm test -- src/components/chat/SdkDependencyPanel.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/components/chat/ContentBlockRenderer.test.tsx src/utils/chatUiBehavior.test.ts`（5 files / 48 tests）。全量前端验证通过：`npm test`（41 files / 392 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。新增测试文件已 `git add`，仅有 Windows LF/CRLF 转换提示。
- 未验证项：未启动真实 Tauri 桌面打开 SDK 依赖管理弹窗，未真实点击 refresh/install/uninstall，未检查安装日志实时滚动、焦点链和屏幕阅读器播报；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph、RED/GREEN 回归、相邻测试、全量前后端测试、生产构建和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，覆盖 SDK modal 在 key 正常、key-only/延迟初始化、安装中、安装失败、已有日志、空日志的视觉和焦点表现；2) 继续扫描 Chat 恢复路径中的 raw-key 风险，优先 `ToolPermissionDialog`、`AskUserQuestionDialog`、`PlanApprovalDialog` 等权限/确认弹窗；3) 做真实运行并排截图审计，重点看 SDK dependency modal、工具权限弹窗、搜索/折叠窗口、工具块完成态、Markdown code-copy 和 diff review pane；4) 将消息显示修复和 readable fallback 修复按主题整理提交，避免与历史累计 UI 改动混杂。

## 本轮 PLAN 2026-06-20 tool permission dialog readable fallback

- 目标：补齐工具权限弹窗的 readable i18n fallback。当前 `src/components/chat/ToolPermissionDialog.tsx` 直接渲染 `chat.permission.*` key；当 i18n 未初始化或 key 缺失时，工具权限确认弹窗会露出内部 key。该弹窗会阻塞 daemon 等待用户放行/拒绝，因此可读性比普通提示更关键。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读路径沿用 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / 源码诊断。验证方式：读取 `ToolPermissionDialog.tsx`、现有 `ToolPermissionDialog.test.ts`、permission 类型和 locales，确认本轮只处理工具权限弹窗文案 fallback，不改快捷键、不改一次性提交保护、不改 permission response 数据流。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ToolPermissionDialog.test.ts`，导入新 label helper，断言 key-only 翻译时 title、description、parameters、raw input、cwd、deny、allowOnce 都为 readable fallback；旧实现应因 helper 不存在失败。
- 功能点 4：实现最小展示修复。验证方式：在 `ToolPermissionDialog.tsx` 增加导出的 `getToolPermissionDialogLabels()`，组件内部所有 `chat.permission.*` 文案改为 `labels.*`；翻译正常时保留真实翻译，翻译返回 key 时使用英文 fallback。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录工具权限弹窗 chrome 必须遵守 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、目标扫描、相邻权限/Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out` 构建产物。

## 迭代记录 2026-06-20 tool permission dialog readable fallback

- 上轮进展与阻塞：上一轮已补齐 SDK dependency recovery UI 的 readable fallback；代码复查确认 `src/components/chat/SdkDependencyPanel.tsx` 已导出并使用 `getSdkDependencyPanelLabels()`，`src/pages/ChatPage.tsx` 的 SDK modal title/close 也复用同一 helper，`src/components/chat/SdkDependencyPanel.test.tsx` 已新增并 staged。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能显示最新”已在前几轮分别覆盖后端 Claude JSONL tail fast path、assistant raw/stream 合并、text snapshot 去重、乱序 tool_result 查找和 MessageList 渲染层兜底；已知阻塞仍是真实 Tauri 桌面录屏/点测和 cc-gui 真实运行并排截图未完成。本轮开工确认 `C:/guodevelop/ccg-switch` 与 `.codegraph/` 可读；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径沿用 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：工具权限弹窗是阻塞 daemon 的关键确认界面，不应在标题、说明、参数标题、原始输入、工作目录、拒绝/允许按钮上暴露 `chat.permission.*` raw key。本项目 `src/components/chat/ToolPermissionDialog.tsx` 旧实现直接使用 `t('chat.permission.*')`；当 i18n 尚未初始化或 key 缺失时，用户会在必须做安全决策的界面里看到内部 key。
- 本轮差距诊断（超越 cc-gui）：本项目的权限请求覆盖 daemon 工具调用、一次性放行和拒绝路径。即使翻译资源延迟或局部缺失，权限弹窗也必须维持可读、可判断、可被辅助技术读出，避免用户因为文案不可懂而误放行、误拒绝或卡住对话。
- 本轮规划：只修工具权限弹窗 chrome 文案派生：新增 `getToolPermissionDialogLabels()` 和局部 fallback，不改快捷键、不改一次性提交保护、不改 permission response 数据流、不改按钮交互。优先级高：用户价值高（阻塞式安全决策）、出现频率中（工具调用需要确认时触发）、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/ToolPermissionDialog.test.ts` 新增 label helper 回归，覆盖 key-only 翻译时 `Tool permission required`、工具说明、`Parameters`、`Full input`、`Working directory:`、`Deny`、`Allow once` 等 fallback，并验证中文翻译存在时不被覆盖。`src/components/chat/ToolPermissionDialog.tsx` 新增 `TOOL_PERMISSION_FALLBACKS`、`translateWithFallback()` 和导出的 `getToolPermissionDialogLabels()`，组件内部所有目标 `chat.permission.*` 文案改为 `labels.*`。`.trellis/spec/frontend/component-guidelines.md` 已同步工具权限弹窗 fallback 合同。
- 本轮验证：RED：`npm test -- src/components/chat/ToolPermissionDialog.test.ts` 先失败 2 项，失败点为 `getToolPermissionDialogLabels is not a function`，符合旧实现缺少 helper 合同的预期。GREEN 定向通过：同命令通过（1 file / 6 tests）。目标扫描通过：`t('chat.permission.` 在 `src/components/chat/ToolPermissionDialog.tsx` 无命中。相邻 Chat UI 验证通过：`npm test -- src/components/chat/ToolPermissionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/components/chat/ContentBlockRenderer.test.tsx src/utils/dialogShortcuts.test.ts`（6 files / 33 tests）。全量前端验证通过：`npm test`（41 files / 394 tests）。生产构建通过：`npm run build`（`tsc && vite build`）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 通过，`problems: []`。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `C:/guodevelop/ccg-switch/out` 已确认位于工作区内并清理，复查均不存在。最终 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0；`git status --short` 显示当前工作区仍有历史累计 Chat UI/Trellis 改动和已 staged 的新增测试文件。
- 未验证项：未启动真实 Tauri 桌面触发工具权限弹窗，未真实点测 allow/deny、Enter/Escape、焦点链、hover title、Accessibility tree 与屏幕阅读器播报；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、Trellis 规范、RED/GREEN 回归、全量前端/后端测试、生产构建和 IDE build。
- 下一步候选：1) 启动真实 Tauri 桌面，构造会触发工具权限的 prompt，点测权限弹窗在 key 正常、key-only/延迟初始化、参数较长、cwd 较长、allow/deny 快捷键和重复提交保护下的表现；2) 继续扫描 Chat 恢复/确认路径中的 raw-key 风险，优先 `AskUserQuestionDialog` 和 `PlanApprovalDialog`；3) 做本项目与 `C:/guodevelop/demo/jetbrains-cc-gui` 的真实运行并排截图审计，重点看工具权限、SDK dependency modal、搜索/折叠窗口、工具块完成态、Markdown code-copy、input status 和 diff review pane；4) 将“大历史加载快路径”“消息消失修复”“readable fallback 修复”按主题整理提交，降低当前 55 个累计未提交改动的回归审查成本。

## 本轮 PLAN 2026-06-20 ask user question dialog readable fallback

- 目标：补齐 `AskUserQuestionDialog` 的 i18n 与 readable fallback。当前 `src/components/chat/AskUserQuestionDialog.tsx` 使用请求 header/question/option 数据渲染主体，但弹窗 fallback title、关闭 title、Cancel、Submit、Submitting 等 chrome 仍是硬编码文案，其中关闭按钮还是中文 `取消`。当 AskUserQuestion 作为阻塞式用户补充信息入口出现时，chrome 应与工具权限弹窗一样在 i18n 未就绪时保持可读。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径沿用 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / locale 诊断。验证方式：读取 `AskUserQuestionDialog.tsx`、`AskUserQuestionDialog.test.ts`、`types/permission.ts` 和 locales，确认本轮只处理弹窗 chrome 文案，不改问题/选项数据流、不改 answer payload、不改 Escape 逻辑、不改一次性提交保护。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/AskUserQuestionDialog.test.ts`，导入新 label helper，断言 key-only translator 下 fallback title、close、cancel、submit、submitting 保持可读；旧实现应因 helper 不存在失败。
- 功能点 4：实现最小展示修复。验证方式：在 `AskUserQuestionDialog.tsx` 新增导出的 `getAskUserQuestionDialogLabels()` 和局部 fallback，并用 `useTranslation()` 让 chrome 走 `chat.askUser.*` / `common.close`。请求 header 仍优先作为标题；缺 header 时使用 `Permission request` fallback。
- 功能点 5：同步 locale 与 Trellis 规范。验证方式：更新 `src/locales/en.json` / `zh.json` 新增 `chat.askUser.*`，并更新 `.trellis/spec/frontend/component-guidelines.md` 记录 AskUserQuestion 弹窗 readable fallback 合同。
- 功能点 6：质量验证。验证方式：执行 RED/GREEN 定向测试、目标扫描、相邻确认弹窗/Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out` 构建产物。

## 迭代记录 2026-06-20 ask user question dialog readable fallback

- 上轮进展与阻塞：上一轮已完成工具权限弹窗 readable fallback；代码中确实存在 `src/components/chat/ToolPermissionDialog.tsx` 的 `getToolPermissionDialogLabels()`，测试中确实覆盖 key-only fallback 和中文翻译保留，组件规范也已同步。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能显示最新”仍保持前几轮代码修复状态。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮开工确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径沿用 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的阻塞式确认/提问入口不会在关闭、取消、提交、提交中状态里混杂硬编码中英文或 raw key。本项目 `src/components/chat/AskUserQuestionDialog.tsx` 的主体内容来自 daemon 请求，但弹窗 chrome 仍硬编码 `Permission Request`、`取消`、`Cancel`、`Submit`、`Submitting...`，既未走 i18n，也没有 key-only fallback 合同。
- 本轮差距诊断（超越 cc-gui）：AskUserQuestion 是 AI 主动向用户补充上下文的阻塞入口。把 chrome 纳入 readable fallback 后，即使 i18n 延迟或局部缺失，用户仍能看懂关闭/取消/提交状态；请求 header/question/options 继续保持 daemon 原文，避免误改业务语义。
- 本轮规划：只修 `AskUserQuestionDialog` 弹窗 chrome：新增 `getAskUserQuestionDialogLabels()`，接入 `useTranslation()`，新增 `chat.askUser.title` / `submit` / `submitting` locale key，并复用 `common.close` / `common.cancel`。不改问题/选项数据流、不改 answer payload、不改 Escape 逻辑、不改一次性提交保护。优先级中高：用户价值高（阻塞式信息补充）、出现频率中、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/AskUserQuestionDialog.test.ts` 新增 label helper 回归，覆盖 key-only fallback、请求 header 优先作为标题、翻译存在时保留翻译。`src/components/chat/AskUserQuestionDialog.tsx` 新增 `ASK_USER_QUESTION_FALLBACKS`、`translateWithFallback()`、导出的 `getAskUserQuestionDialogLabels()`，并将标题 fallback、关闭按钮 title/aria-label、Cancel、Submit、Submitting 改为 labels 派生。`src/locales/en.json` / `src/locales/zh.json` 新增 `chat.askUser.title`、`chat.askUser.submit`、`chat.askUser.submitting`。`.trellis/spec/frontend/component-guidelines.md` 已同步 AskUserQuestion modal chrome readable fallback 合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/chat/AskUserQuestionDialog.test.ts` 先失败 3 项，失败点为 `getAskUserQuestionDialogLabels is not a function`，符合旧实现缺少 helper 合同的预期。GREEN 定向通过：同命令通过（1 file / 5 tests）。相邻确认弹窗验证通过：`npm test -- src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx`（4 files / 17 tests）。目标扫描通过：`title="取消"`、可见 `Cancel` / `Submit`、`Submitting...` 在 `AskUserQuestionDialog.tsx` 中无遗留渲染命中，唯一命中是 fallback 常量自身。全量前端验证通过：`npm test`（41 files / 397 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：IDE `build_project` 和文件级 `get_file_problems` 本轮均在工具层 120 秒超时，未能拿到 IDE problems 输出；已由 `npm run build` 覆盖 TypeScript 语法/类型门禁。未启动真实 Tauri 桌面触发 AskUserQuestion 弹窗，未真实点测多选/单选、提交、取消、Escape、焦点链、Accessibility tree 与屏幕阅读器播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 继续收口确认路径，优先 `PlanApprovalDialog` 的 hardcoded title、close、plan lines、allowed actions、cwd、deny/approve/approve-auto 文案与 `title` fallback；2) 启动真实 Tauri 桌面，构造 AskUserQuestion 单选/多选请求，点测 key 正常、key-only/延迟初始化、提交中、取消、Escape 和重复提交保护；3) 做本项目与 `C:/guodevelop/demo/jetbrains-cc-gui` 的真实运行并排截图审计，重点看 AskUserQuestion、ToolPermission、PlanApproval、SDK dependency modal、搜索/折叠窗口和工具块完成态；4) 将“大历史加载快路径”“消息消失修复”“readable fallback 修复”按主题整理提交，降低当前累计未提交改动的回归审查成本。

## 本轮 PLAN 2026-06-20 plan approval dialog readable fallback

- 目标：补齐 `PlanApprovalDialog` 的 i18n 与 readable fallback。当前 `src/components/chat/PlanApprovalDialog.tsx` 在阻塞式计划审批入口中硬编码 `Plan Approval Required`、`取消`、`Plan (n lines)`、`Allowed Actions`、`Working directory:`、`Deny`、`Approve`、`Approve & Auto` 以及 auto 模式 title。cc-gui 的真实 `PlanApprovalDialog` 使用 i18n、计划内容渲染和执行模式选择；本轮不复制其完整能力，只收口本项目现有弹窗 chrome 的可读性边界。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui / locale 诊断。验证方式：读取本项目 `PlanApprovalDialog.tsx` 与测试、cc-gui `webview/src/components/PlanApprovalDialog.tsx`、locales，确认本轮只处理现有弹窗文案派生，不改审批 payload、不改 approve/deny/auto targetMode、不引入倒计时或执行模式重构。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/PlanApprovalDialog.test.ts`，导入新 label helper，断言 key-only translator 下 title、close、plan line summary、allowed action count、cwd、deny、approve、approve-auto、approve-auto title 都保持 readable fallback；旧实现应因 helper 不存在失败。
- 功能点 4：实现最小展示修复。验证方式：在 `PlanApprovalDialog.tsx` 新增导出的 `getPlanApprovalDialogLabels()` 和局部 fallback，组件内部所有目标 hardcoded chrome 改为 labels 派生，并接入 `useTranslation()`。
- 功能点 5：同步 locale 与 Trellis 规范。验证方式：更新 `src/locales/en.json` / `zh.json` 新增 `chat.planApproval.*`，并更新 `.trellis/spec/frontend/component-guidelines.md` 记录 PlanApproval 弹窗 readable fallback 合同。
- 功能点 6：质量验证。验证方式：执行 RED/GREEN 定向测试、目标扫描、相邻确认弹窗/Chat UI 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build 或文件级 problems 检查、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out` 构建产物。

## 迭代记录 2026-06-20 plan approval dialog readable fallback

- 上轮进展与阻塞：上一轮已完成 `AskUserQuestionDialog` readable fallback；代码中确实存在 `getAskUserQuestionDialogLabels()`、`chat.askUser.*` locale key 和回归测试。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能显示最新”已在前几轮分别覆盖 Claude JSONL tail fast path、assistant raw/stream 合并、text snapshot 去重、乱序 tool_result 查找和 MessageList 渲染层兜底。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮开工确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的真实 `webview/src/components/PlanApprovalDialog.tsx` 使用 i18n、计划内容 Markdown 渲染、倒计时/折叠和执行模式选择。本项目 `src/components/chat/PlanApprovalDialog.tsx` 的现有弹窗仍硬编码 title、close title、plan line summary、allowed actions count、cwd、deny/approve/approve-auto 文案和 approve-auto title；在阻塞式计划审批场景中，i18n 缺失或延迟会让用户面对混杂语言或内部 key，影响安全决策。
- 本轮差距诊断（超越 cc-gui）：本项目先把计划审批弹窗的 chrome 纳入 helper-owned readable fallback，确保即使翻译资源缺失，用户仍能看懂关闭、拒绝、批准、批准并自动执行的含义；计划正文和 allowed prompts 仍保留 daemon 原文，避免展示层误改执行语义。
- 本轮规划：只修 `PlanApprovalDialog` chrome label 派生：新增 `getPlanApprovalDialogLabels()`，接入 `useTranslation()`，新增 `chat.planApproval.*` locale key，并替换现有硬编码文案。不改 approve/deny/auto payload，不引入 cc-gui 的倒计时、模式选择或 Markdown plan 重构。优先级中高：用户价值高（阻塞式计划审批）、出现频率中、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/PlanApprovalDialog.test.ts` 新增 label helper 回归，覆盖 key-only fallback 和中文翻译保留。`src/components/chat/PlanApprovalDialog.tsx` 新增 `PLAN_APPROVAL_FALLBACKS`、`translateWithFallback()`、导出的 `getPlanApprovalDialogLabels()`，并将标题、关闭按钮 title/aria-label、plan 行数、allowed actions 数量、cwd、deny、approve、approve-auto 和 approve-auto title 改为 labels 派生。`src/locales/en.json` / `src/locales/zh.json` 新增 `chat.planApproval.*`。`.trellis/spec/frontend/component-guidelines.md` 已同步 PlanApproval modal chrome readable fallback 合同。
- 本轮验证：RED：`npm test -- src/components/chat/PlanApprovalDialog.test.ts` 先失败 2 项，失败点为 `getPlanApprovalDialogLabels is not a function`，符合旧实现缺少 helper 合同的预期。GREEN 定向通过：同命令通过（1 file / 6 tests）。目标扫描确认旧硬编码 chrome 只剩 fallback 常量命中，渲染路径不再直接使用旧字面量。相邻确认弹窗验证通过：`npm test -- src/components/chat/PlanApprovalDialog.test.ts src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/ToolPermissionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx`（4 files / 19 tests）。全量前端验证通过：`npm test`（41 files / 399 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` 与 `git diff --cached --check` exit 0。
- 未验证项：IDE `build_project` 本轮 120 秒超时，未能拿到 IDE problems 输出；已由 `npm run build` 覆盖 TypeScript 语法/类型门禁。未启动真实 Tauri 桌面触发 PlanApproval，未真实点测 deny/approve/approve-auto、Enter/Escape、焦点链、allow auto、Accessibility tree 与屏幕阅读器播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 启动真实 Tauri 桌面，覆盖 PlanApproval / AskUserQuestion / ToolPermission 三类阻塞弹窗在 key 正常、key-only/延迟初始化、长 plan、长 cwd、deny/approve/allow 的视觉和焦点表现；2) 对比 `C:/guodevelop/demo/jetbrains-cc-gui` 的 PlanApproval 倒计时、执行模式选择和 Markdown plan 渲染，判断是否值得补齐到本项目；3) 做真实运行并排截图审计，重点看阻塞确认链路、SDK dependency modal、Markdown code-copy、input status tabs、工具块完成态和搜索/折叠窗口；4) 将“大历史加载快路径”“消息消失修复”“readable fallback 修复”按主题整理提交，降低当前累计未提交改动的回归审查成本。

## 本轮 PLAN 2026-06-20 plan approval markdown body

- 目标：补齐 `PlanApprovalDialog` 计划正文的 Markdown 渲染。cc-gui 的真实 `webview/src/components/PlanApprovalDialog.tsx` 使用 `MarkdownBlock` 渲染 `request.plan`；本项目当前仍用 `<pre>` 纯文本展示，计划里的标题、列表、代码块和链接都不能按正文语义扫读。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `PlanApprovalDialog.tsx`、`MarkdownBlock.tsx`、`PlanApprovalDialog.test.ts`，以及 cc-gui `webview/src/components/PlanApprovalDialog.tsx`，确认本轮只改计划正文展示，不改审批 payload、不改 approve/deny/auto targetMode、不引入倒计时、折叠模式或执行模式选择。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/PlanApprovalDialog.test.ts`，mock portal 与 i18n，构造含 heading、列表、TypeScript code fence 的 plan；旧实现应渲染纯 `<pre>` 并缺少 `<h2>` / `<li>` / code language class。
- 功能点 4：实现最小展示修复。验证方式：在 `PlanApprovalDialog.tsx` 复用现有 `MarkdownBlock` 渲染 expanded plan body，保留外层滚动/高度限制和现有折叠按钮；不复制 cc-gui 的倒计时/执行模式。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 PlanApproval plan body 必须复用 MarkdownBlock，不能回退成纯 `<pre>`。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻确认弹窗/Markdown 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、必要时 `cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out` 构建产物。

## 迭代记录 2026-06-20 plan approval markdown body

- 上轮进展与阻塞：上一轮已完成 `PlanApprovalDialog` readable fallback；代码中确实存在 `getPlanApprovalDialogLabels()`、`chat.planApproval.*` locale key 和回归测试。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能显示最新”仍保持前几轮代码修复状态。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮开工确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的真实 `webview/src/components/PlanApprovalDialog.tsx` 对 `request.plan` 使用 `MarkdownBlock` 渲染；本项目 `src/components/chat/PlanApprovalDialog.tsx` 仍用纯 `<pre>` 展示计划正文，导致标题、列表、链接、代码块和文件路径在审批前不易扫读。
- 本轮差距诊断（超越 cc-gui）：计划审批不是普通文本阅读，而是执行前的风险判断。把计划正文接入现有 `MarkdownBlock` 后，用户可以用结构化 heading/list/code 快速确认执行步骤和命令片段；同时继续保留 daemon 原文，不对计划语义做二次加工。
- 本轮规划：只修 PlanApproval expanded plan body 的展示方式，复用现有 `MarkdownBlock`；保留当前折叠按钮、滚动高度、allowed prompts、cwd 和 approve/deny/auto payload。不引入 cc-gui 的倒计时、折叠悬浮态或执行模式选择。优先级中高：用户价值高（审批前扫读质量）、出现频率中、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/PlanApprovalDialog.test.ts` 新增 SSR 回归，mock portal/i18n/DOMPurify，构造含 heading、列表和 TypeScript code fence 的计划，断言渲染结果包含 `<h2>`、`<li>` 和 language code class，且不再使用旧的纯 pre 容器。`src/components/chat/PlanApprovalDialog.tsx` 导入并复用 `MarkdownBlock` 渲染 expanded plan body，同时给 plan toggle 补 `type="button"` 和 `aria-expanded`。`.trellis/spec/frontend/component-guidelines.md` 已同步 PlanApproval plan body 必须复用 `MarkdownBlock` 的合同。
- 本轮验证：RED：`npm test -- src/components/chat/PlanApprovalDialog.test.ts` 先失败 1 项，旧实现输出纯 `<pre>`，缺少 `<h2>Implementation plan</h2>`。GREEN 定向通过：同命令通过（1 file / 7 tests）。相邻验证通过：`npm test -- src/components/chat/PlanApprovalDialog.test.ts src/components/chat/MarkdownBlock.test.tsx src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/ToolPermissionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx`（5 files / 22 tests）。全量前端验证通过：`npm test`（41 files / 400 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` 与 `git diff --cached --check` exit 0，只有 Windows LF/CRLF 转换提示。
- 未验证项：IDE `build_project` 和文件级 `get_file_problems` 本轮均 120 秒超时，未能拿到 IDE problems 输出；已由 `npm run build` 覆盖 TypeScript 语法/类型门禁。未启动真实 Tauri 桌面触发 PlanApproval，未视觉确认 Markdown plan 在真实 WebView 内的样式、代码复制按钮、滚动高度、焦点链和屏幕阅读器播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 启动真实 Tauri 桌面触发 PlanApproval，检查 Markdown plan 的标题/列表/代码块/链接、代码复制按钮、长计划滚动和折叠状态；2) 继续对比 `C:/guodevelop/demo/jetbrains-cc-gui` 的 PlanApproval 倒计时、执行模式选择和 collapse mini-mode，评估是否值得补齐；3) 做真实运行并排截图审计，重点看 PlanApproval、ToolPermission、AskUserQuestion、SDK dependency modal、Markdown code-copy 和搜索/折叠窗口；4) 将“大历史加载快路径”“消息消失修复”“readable fallback 修复”“PlanApproval Markdown 渲染”按主题整理提交，降低当前累计未提交改动的回归审查成本。

## 本轮 PLAN 2026-06-20 plan approval keyboard hints

- 目标：补齐 `PlanApprovalDialog` 的键盘操作提示。cc-gui 的真实 `PlanApprovalDialog` 在底部展示 `Enter` 批准、`Esc` 拒绝提示；本项目已有快捷键行为但没有可见提示，用户只能试错或依赖记忆。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `PlanApprovalDialog.tsx` / test 与 cc-gui `webview/src/components/PlanApprovalDialog.tsx`，确认本轮只补可见快捷键提示与 readable fallback，不改审批 payload、不改 approve/deny/auto targetMode。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/PlanApprovalDialog.test.ts`，断言 label helper 在 key-only translator 下提供 `Approve` / `Deny` 快捷键提示 fallback，并断言渲染输出包含 `<kbd>Enter</kbd>`、`<kbd>Esc</kbd>` 与对应提示文案。
- 功能点 4：实现最小展示修复。验证方式：扩展 `getPlanApprovalDialogLabels()`，新增 `shortcutApprove` / `shortcutDeny`；弹窗底部在按钮下方渲染紧凑 keyboard hints，不改变按钮或全局快捷键处理。
- 功能点 5：同步 locale 与 Trellis 规范。验证方式：更新 `src/locales/en.json` / `zh.json` 新增快捷键提示 key，并更新 `.trellis/spec/frontend/component-guidelines.md` 记录 PlanApproval keyboard hints 必须使用 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻确认弹窗测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、必要时 `cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 plan approval keyboard hints

- 上轮进展与阻塞：上一轮已完成 `PlanApprovalDialog` plan body Markdown 渲染；代码中确实存在 `MarkdownBlock` 复用、`PlanApprovalDialog.test.ts` Markdown 回归和组件规范合同。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能显示最新”仍保持前几轮代码修复状态。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮开工确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的真实 `webview/src/components/PlanApprovalDialog.tsx` 在底部展示 `<kbd>Enter</kbd>` 批准和 `<kbd>Esc</kbd>` 拒绝提示。本项目虽然已有 Enter/Escape 快捷键处理，但没有可见提示，用户必须猜测或记忆快捷键，尤其在长计划审批场景中不利于键盘流。
- 本轮差距诊断（超越 cc-gui）：本项目补提示时同时纳入 readable fallback 和 en/zh locale，避免 i18n 缺失时露出内部 key。提示只描述已有快捷键，不改变审批 payload 或 targetMode，因此能提升发现性而不扩大行为风险。
- 本轮规划：只补 PlanApproval 底部键盘提示和 label 派生：新增 `shortcutApprove` / `shortcutDeny` fallback 与 locale key，在按钮下方渲染紧凑 `Enter Approve` / `Esc Deny`。不改 handleApprove/handleDeny/handleApproveAuto，不引入 cc-gui 的倒计时、执行模式选择或 collapse mini-mode。优先级中高：用户价值中高（减少阻塞审批中的快捷键试错）、出现频率中、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/PlanApprovalDialog.test.ts` 扩展 label helper 断言和 SSR 渲染回归，覆盖 key-only fallback、中文翻译保留和 `<kbd>Enter</kbd>` / `<kbd>Esc</kbd>` 可见提示。`src/components/chat/PlanApprovalDialog.tsx` 新增 `shortcutApprove` / `shortcutDeny` label，并在 footer 按钮下方渲染紧凑 hints。`src/locales/en.json` / `src/locales/zh.json` 新增 `chat.planApproval.shortcutApprove` 与 `chat.planApproval.shortcutDeny`。`.trellis/spec/frontend/component-guidelines.md` 已同步 PlanApproval keyboard hints 必须使用 readable fallback 的合同。
- 本轮验证：RED：`npm test -- src/components/chat/PlanApprovalDialog.test.ts` 先失败 3 项，失败点为 `labels.shortcutApprove` / `labels.shortcutDeny` 不存在、渲染中缺少 `<kbd>Enter</kbd>`。GREEN 定向通过：同命令通过（1 file / 8 tests）。相邻验证通过：`npm test -- src/components/chat/PlanApprovalDialog.test.ts src/components/chat/MarkdownBlock.test.tsx src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/ToolPermissionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx`（5 files / 23 tests）。全量前端验证通过：`npm test`（41 files / 401 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` 与 `git diff --cached --check` exit 0，只有 Windows LF/CRLF 转换提示。
- 未验证项：IDE `build_project` 本轮 120 秒超时，未能拿到 IDE problems 输出；已由 `npm run build` 覆盖 TypeScript 语法/类型门禁。未启动真实 Tauri 桌面触发 PlanApproval，未视觉确认键盘提示在真实 WebView 内的间距、深浅色、焦点链和屏幕阅读器播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 启动真实 Tauri 桌面触发 PlanApproval，检查 Markdown plan、键盘提示、Enter/Esc 实际行为、长计划滚动和折叠状态；2) 继续对比 `C:/guodevelop/demo/jetbrains-cc-gui` 的 PlanApproval 倒计时、执行模式选择和 collapse mini-mode，评估是否值得补齐；3) 做真实运行并排截图审计，重点看 PlanApproval、ToolPermission、AskUserQuestion、SDK dependency modal、Markdown code-copy 和搜索/折叠窗口；4) 将“大历史加载快路径”“消息消失修复”“readable fallback 修复”“PlanApproval Markdown/快捷键提示”按主题整理提交，降低当前累计未提交改动的回归审查成本。

## 本轮 PLAN 2026-06-20 plan approval subtitle context

- 目标：补齐 `PlanApprovalDialog` 的 header subtitle。cc-gui 的真实 `PlanApprovalDialog` 在标题下方说明计划已准备、即将执行；本项目当前只有 `Plan approval required` 标题，缺少“为什么被阻塞 / 用户要审什么”的上下文。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `PlanApprovalDialog.tsx` / test 与 cc-gui `webview/src/components/PlanApprovalDialog.tsx`，确认本轮只补 header subtitle 和 readable fallback，不改审批 payload、不改 approve/deny/auto targetMode。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/PlanApprovalDialog.test.ts`，断言 label helper 在 key-only translator 下提供 subtitle fallback，并断言渲染输出包含 subtitle。
- 功能点 4：实现最小展示修复。验证方式：扩展 `getPlanApprovalDialogLabels()`，新增 `subtitle`；在标题下方渲染一行 compact secondary text，不改变布局结构、按钮或快捷键。
- 功能点 5：同步 locale 与 Trellis 规范。验证方式：更新 `src/locales/en.json` / `zh.json` 新增 `chat.planApproval.subtitle`，并更新 `.trellis/spec/frontend/component-guidelines.md` 记录 PlanApproval header subtitle 必须使用 readable fallback。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻确认弹窗测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、必要时 `cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 plan approval subtitle context

- 上轮进展与阻塞：上一轮已完成 `PlanApprovalDialog` keyboard hints；代码中确实存在 `shortcutApprove` / `shortcutDeny` label、en/zh locale key、footer `<kbd>Enter</kbd>` / `<kbd>Esc</kbd>` 提示和回归测试。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能显示最新”仍保持前几轮代码修复状态。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的真实 `webview/src/components/PlanApprovalDialog.tsx` 在标题下方渲染 `planApproval.subtitle`，说明计划已完成、准备执行。本项目此前只有标题和正文，用户在阻塞审批入口里缺少“为什么被打断、当前要审什么”的上下文。
- 本轮差距诊断（超越 cc-gui）：subtitle 纳入 `getPlanApprovalDialogLabels()` 的 readable fallback 和 en/zh locale，让 i18n 缺失或延迟时仍展示可读上下文；同时只补说明文本，不改变审批语义、`targetMode`、快捷键或按钮行为。
- 本轮规划：只补 PlanApproval header subtitle label 与渲染，不改 approve/deny/approve-auto payload，不引入 cc-gui 的倒计时、执行模式选择或 collapse mini-mode。优先级中高：用户价值中（减少阻塞弹窗认知成本）、出现频率中、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/PlanApprovalDialog.test.ts` 扩展 label helper 与 SSR 渲染回归，覆盖 key-only fallback、中文翻译保留和 subtitle 可见渲染。`src/components/chat/PlanApprovalDialog.tsx` 新增 `subtitle` fallback，在 header title 下方渲染 secondary text。`src/locales/en.json` / `src/locales/zh.json` 新增 `chat.planApproval.subtitle`。`.trellis/spec/frontend/component-guidelines.md` 已同步 PlanApproval modal title/subtitle readable fallback 合同。
- 本轮验证：RED：`npm test -- src/components/chat/PlanApprovalDialog.test.ts` 先失败 3 项，失败点为 subtitle label 不存在与渲染缺少 subtitle，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 9 tests）。相邻验证通过：`npm test -- src/components/chat/PlanApprovalDialog.test.ts src/components/chat/MarkdownBlock.test.tsx src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/ToolPermissionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx`（5 files / 24 tests）。本轮复跑全量前端验证通过：`npm test`（41 files / 402 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` 与 `git diff --cached --check` exit 0，仅输出 Windows LF/CRLF 转换提示。
- 未验证项：IDE `build_project` 本轮 120 秒超时，未能拿到 IDE problems 输出；已由 `npm run build` 覆盖 TypeScript 语法/类型门禁。未启动真实 Tauri 桌面触发 PlanApproval，未视觉确认 subtitle 在真实 WebView 内的间距、换行、深浅色、焦点链和屏幕阅读器播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 启动真实 Tauri 桌面触发 PlanApproval，检查 header subtitle、Markdown plan、键盘提示、Enter/Esc 实际行为、长计划滚动和折叠状态；2) 继续评估 `C:/guodevelop/demo/jetbrains-cc-gui` 的 PlanApproval 倒计时、执行模式选择和 collapse mini-mode 是否值得补齐；3) 做真实运行并排截图审计，重点看 PlanApproval、ToolPermission、AskUserQuestion、SDK dependency modal、Markdown code-copy 和搜索/折叠窗口；4) 将“大历史加载快路径”“消息消失修复”“readable fallback 修复”“PlanApproval Markdown/快捷键/subtitle”按主题整理提交，降低当前累计未提交改动的回归审查成本。

## 本轮 PLAN 2026-06-20 plan approval dialog semantics

- 目标：补齐 `PlanApprovalDialog` 的可访问 dialog 语义。当前弹窗是视觉上阻塞的 modal，但内容容器没有 `role="dialog"`、`aria-modal`、`aria-labelledby`、`aria-describedby`；屏幕阅读器和自动化检查难以把标题、副标题和审批正文关联为一个阻塞审批弹窗。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `PlanApprovalDialog.tsx` / test 与 cc-gui `webview/src/components/PlanApprovalDialog.tsx`，确认本轮只补 DOM 语义，不改审批 payload、不改 approve/deny/auto targetMode、不引入倒计时、执行模式选择或 collapse mini-mode。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/PlanApprovalDialog.test.ts`，SSR 断言弹窗根容器包含 `role="dialog"`、`aria-modal="true"`、稳定 title/subtitle id，以及 `aria-labelledby` / `aria-describedby` 指向这些 id；旧实现应因缺少这些属性失败。
- 功能点 4：实现最小语义修复。验证方式：给 dialog container 增加语义属性，给 header title/subtitle 增加稳定 id；不改变样式、按钮、快捷键、Portal 或业务回调。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 PlanApproval 阻塞弹窗必须显式声明 dialog 语义并关联 title/subtitle。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻确认弹窗测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、必要时 `cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 plan approval dialog semantics

- 上轮进展与阻塞：上一轮已补齐 `PlanApprovalDialog` header subtitle；代码中确实存在 `chat.planApproval.subtitle`、`getPlanApprovalDialogLabels().subtitle`、标题下方说明文本和回归测试。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能显示最新”仍保持前几轮代码修复状态。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的 `PlanApprovalDialog` 已有 title/subtitle、Markdown plan、keyboard hints、倒计时、折叠和执行模式选择；本项目前几轮已补齐其中的 title/subtitle、Markdown plan 和 keyboard hints。本轮发现本项目视觉上是阻塞 modal，但 DOM 容器缺少 `role="dialog"`、`aria-modal`、`aria-labelledby`、`aria-describedby`，自动化与辅助技术无法稳定把标题、说明和审批内容识别为同一个阻塞审批弹窗。
- 本轮差距诊断（超越 cc-gui）：本轮不复制 cc-gui 的高风险行为能力，而是增强本项目审批弹窗的可访问语义。用户价值是键盘和辅助技术用户能先听到“这是计划审批弹窗、为什么被阻塞”，再进入计划正文和按钮；同时不改变审批 payload、快捷键或 `targetMode`。
- 本轮规划：只给 PlanApproval dialog container 增加 `role="dialog"`、`aria-modal="true"`、`aria-labelledby`、`aria-describedby`，并给 visible title/subtitle 增加稳定 id；不改样式、不改 Portal、不改 approve/deny/auto 行为、不引入倒计时/模式选择/mini-mode。优先级中高：用户价值高（阻塞式安全决策的可访问上下文）、出现频率中、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/PlanApprovalDialog.test.ts` 新增 SSR 回归，断言 modal dialog 语义和 title/subtitle 关联。`src/components/chat/PlanApprovalDialog.tsx` 新增稳定 id 常量，在弹窗内容容器上声明 `role="dialog"`、`aria-modal="true"`、`aria-labelledby`、`aria-describedby`，并把 id 挂到可见标题和说明文本上。`.trellis/spec/frontend/component-guidelines.md` 已同步 PlanApproval 阻塞弹窗必须暴露 dialog 语义并关联 title/subtitle 的合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/chat/PlanApprovalDialog.test.ts` 先失败 1 项，失败点为 SSR 输出缺少 `role="dialog"`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 10 tests）。相邻验证通过：`npm test -- src/components/chat/PlanApprovalDialog.test.ts src/components/chat/MarkdownBlock.test.tsx src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/ToolPermissionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx`（5 files / 25 tests）。全量前端验证通过：`npm test`（41 files / 403 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` 与 `git diff --cached --check` exit 0，仅输出 Windows LF/CRLF 转换提示。
- 未验证项：IDE `build_project` 本轮 120 秒超时，未能拿到 IDE problems 输出；已由 `npm run build` 覆盖 TypeScript 语法/类型门禁。未启动真实 Tauri 桌面触发 PlanApproval，未用浏览器/屏幕阅读器检查实际 accessibility tree、焦点初始位置、焦点陷阱和播报顺序；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 启动真实 Tauri 桌面触发 PlanApproval，检查 dialog semantics 在真实 WebView accessibility tree 中是否关联 title/subtitle，并点测 Enter/Esc、长计划滚动、关闭/拒绝/批准；2) 为 ToolPermissionDialog / AskUserQuestionDialog 做同类 dialog semantics 审计，保证所有阻塞确认入口都有 `role="dialog"`、`aria-modal` 和可读 name/description；3) 继续评估 `C:/guodevelop/demo/jetbrains-cc-gui` 的 PlanApproval 倒计时、执行模式选择和 collapse mini-mode 是否值得补齐；4) 将“大历史加载快路径”“消息消失修复”“readable fallback 修复”“PlanApproval Markdown/快捷键/subtitle/语义”按主题整理提交，降低当前累计未提交改动的回归审查成本。

## 本轮 PLAN 2026-06-20 tool permission dialog semantics

- 目标：补齐 `ToolPermissionDialog` 的可访问 dialog 语义。当前弹窗会阻塞 daemon 等用户放行/拒绝工具调用，但内容容器没有 `role="dialog"`、`aria-modal`、`aria-labelledby`、`aria-describedby`；屏幕阅读器和自动化检查难以把标题、工具名/说明和安全决策按钮关联为一个工具权限弹窗。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ToolPermissionDialog.tsx` / test 与 cc-gui `webview/src/components/PermissionDialog.tsx`，确认本轮只补 DOM 语义，不改 permission response、不改快捷键、不改一次性提交保护、不引入 cc-gui 倒计时或多模式按钮。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ToolPermissionDialog.test.ts`，SSR 断言弹窗根容器包含 `role="dialog"`、`aria-modal="true"`、稳定 title/description id，以及 `aria-labelledby` / `aria-describedby` 指向这些 id；旧实现应因缺少这些属性失败。
- 功能点 4：实现最小语义修复。验证方式：给 dialog container 增加语义属性，给 header title 和 description banner 增加稳定 id；不改变样式、按钮、快捷键、Portal 或业务回调。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录工具权限弹窗必须显式声明 dialog 语义并关联 title/description。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻确认弹窗测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 tool permission dialog semantics

- 上轮进展与阻塞：上一轮已补齐 `PlanApprovalDialog` dialog semantics；代码中确实存在 `role="dialog"`、`aria-modal="true"`、`aria-labelledby` / `aria-describedby` 和 title/subtitle id 回归测试。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能显示最新”仍保持前几轮代码修复状态。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的真实 `webview/src/components/PermissionDialog.tsx` 有更完整的倒计时、多选项、命令内容 Markdown 和 resize handle；本项目 `ToolPermissionDialog` 已有 readable fallback、参数预览、raw input、cwd、deny/allow-once 和快捷键保护，但此前缺少显式 dialog 语义。本轮只补语义，不复制 cc-gui 的倒计时或多模式按钮。
- 本轮差距诊断（超越 cc-gui）：本轮发现 cc-gui 对应 PermissionDialog 也没有显式 `role="dialog"` / `aria-modal` 语义。本项目工具权限弹窗会阻塞 daemon 的安全决策，给它补 title/description 关联后，辅助技术用户能先听到“工具权限确认”和具体工具请求说明，再进入参数、raw input 和按钮，降低误放行/误拒绝风险。
- 本轮规划：只给 ToolPermission dialog container 增加 `role="dialog"`、`aria-modal="true"`、`aria-labelledby`、`aria-describedby`，并给可见 title 和 warning description banner 增加稳定 id；不改样式、不改 Portal、不改 permission response、不改 Enter/Escape、不改一次性提交保护。优先级高：用户价值高（阻塞式安全决策）、出现频率中、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/ToolPermissionDialog.test.ts` 新增 SSR 回归和 portal/i18n mock，断言工具权限弹窗暴露 dialog 语义并关联 title/description；测试夹具补齐 `ToolPermissionRequest.timestamp` 以满足 `tsc`。`src/components/chat/ToolPermissionDialog.tsx` 新增稳定 id 常量，在弹窗容器上声明 `role="dialog"`、`aria-modal="true"`、`aria-labelledby`、`aria-describedby`，并把 id 挂到可见标题和描述 banner。`.trellis/spec/frontend/component-guidelines.md` 已同步工具权限弹窗必须暴露 dialog 语义并关联 title/description 的合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/chat/ToolPermissionDialog.test.ts` 先失败 1 项，失败点为 SSR 输出缺少 `role="dialog"`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 7 tests）。相邻验证通过：`npm test -- src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx src/utils/dialogShortcuts.test.ts`（5 files / 27 tests）。全量前端验证通过：`npm test`（41 files / 404 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建第一次失败于测试夹具缺少 required `timestamp`，修复后 `npm run build` 通过（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` 与 `git diff --cached --check` exit 0，仅输出 Windows LF/CRLF 转换提示。
- 未验证项：IDE `build_project` 本轮 120 秒超时，未能拿到 IDE problems 输出；已由 `npm run build` 覆盖 TypeScript 语法/类型门禁。未启动真实 Tauri 桌面触发工具权限弹窗，未用浏览器/屏幕阅读器检查实际 accessibility tree、焦点初始位置、焦点陷阱和播报顺序；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 启动真实 Tauri 桌面触发 ToolPermissionDialog，检查 dialog semantics 在真实 WebView accessibility tree 中是否关联 title/description，并点测 Enter/Escape、deny/allow-once、长参数、长 cwd、raw input 展开；2) 为 `AskUserQuestionDialog` 做同类 dialog semantics 审计，保证所有阻塞确认/提问入口都有 `role="dialog"`、`aria-modal` 和可读 name/description；3) 继续评估 `C:/guodevelop/demo/jetbrains-cc-gui` 的 PermissionDialog 倒计时、允许一次/始终允许/拒绝三按钮和 resize handle 是否适合本项目；4) 将“大历史加载快路径”“消息消失修复”“readable fallback 修复”“dialog semantics 修复”按主题整理提交，降低当前累计未提交改动的回归审查成本。

## 本轮 PLAN 2026-06-20 ask user question dialog semantics

- 目标：补齐 `AskUserQuestionDialog` 的可访问 dialog 语义。当前弹窗会阻塞 daemon 等用户回答问题，但内容容器只有视觉 modal 与 `aria-busy`，没有 `role="dialog"`、`aria-modal`、`aria-labelledby`、`aria-describedby`；屏幕阅读器和自动化检查难以把标题、首个问题和提交/取消动作识别为同一个提问弹窗。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `AskUserQuestionDialog.tsx` / test / permission 类型与 cc-gui `webview/src/components/AskUserQuestionDialog.tsx`，确认本轮只补 DOM 语义，不改 answer payload、不改 Escape 取消、不改一次性提交保护、不引入 cc-gui 倒计时、折叠、多问题逐步导航或 Other 输入。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/AskUserQuestionDialog.test.ts`，SSR 断言弹窗根容器包含 `role="dialog"`、`aria-modal="true"`、稳定 title/description id，以及 `aria-labelledby` / `aria-describedby` 指向这些 id；旧实现应因缺少这些属性失败。
- 功能点 4：实现最小语义修复。验证方式：给 dialog container 增加语义属性，给 header title 和首个可见问题文本增加稳定 id；不改变样式、按钮、快捷键、Portal 或业务回调。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 ask-user-question 阻塞弹窗必须显式声明 dialog 语义并关联 title/首个问题。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻确认弹窗测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 ask user question dialog semantics

- 上轮进展与阻塞：上一轮已补齐 `ToolPermissionDialog` dialog semantics；代码中确实存在 `role="dialog"`、`aria-modal="true"`、`aria-labelledby` / `aria-describedby` 和 title/description id 回归测试。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能显示最新”仍保持前几轮代码修复状态：大历史快速首屏、assistant raw/stream 去重、乱序 tool_result utility 与 MessageList 渲染兜底都已在代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的真实 `webview/src/components/AskUserQuestionDialog.tsx` 有倒计时、折叠、多问题进度、Other 输入和自定义输入上限等更完整的提问体验；本项目 `AskUserQuestionDialog` 已有 readable fallback、Escape 取消、一次性提交保护和多选/单选渲染，但此前缺少显式 dialog 语义。本轮只补语义，不复制 cc-gui 的倒计时、折叠、多步骤或 Other 输入。
- 本轮差距诊断（超越 cc-gui）：本轮在已核实的 cc-gui 片段中也未看到显式 `role="dialog"` / `aria-modal`。本项目把 ask-user-question 阻塞弹窗连接到可见标题和首个问题 prompt 后，辅助技术用户能先听到“这是一个需要回答的问题弹窗”和具体问题，再进入选项与提交/取消动作，减少误答、漏答和对 daemon 阻塞状态的困惑。
- 本轮规划：只给 AskUserQuestion dialog container 增加 `role="dialog"`、`aria-modal="true"`、`aria-labelledby`、`aria-describedby`，并给可见 title 与首个问题文本增加稳定 id；不改样式、不改 Portal、不改 answer payload、不改 Escape 取消、不改一次性提交保护。优先级中高：用户价值高（阻塞式提问的可访问上下文）、出现频率中（MCP/工具提问时触发）、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/AskUserQuestionDialog.test.ts` 新增 SSR 回归和 portal/i18n mock，断言 ask-user-question 弹窗暴露 dialog 语义并关联 title/首个问题；测试夹具补齐 `AskUserQuestionRequest.timestamp` / `cwd`。`src/components/chat/AskUserQuestionDialog.tsx` 新增稳定 id 常量，在弹窗容器上声明 `role="dialog"`、`aria-modal="true"`、`aria-labelledby`、`aria-describedby`，并把 id 挂到可见标题和首个问题 label。`.trellis/spec/frontend/component-guidelines.md` 已同步 ask-user-question 阻塞弹窗必须暴露 dialog 语义并关联 title/首个问题的合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/chat/AskUserQuestionDialog.test.ts` 先失败 1 项，失败点为 SSR 输出缺少 `role="dialog"`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 6 tests）。相邻验证通过：`npm test -- src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx src/utils/dialogShortcuts.test.ts`（5 files / 28 tests，仅 Browserslist/caniuse-lite 过期提示）。全量前端验证通过：`npm test`（41 files / 405 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` 与 `git diff --cached --check` exit 0，仅输出 Windows LF/CRLF 转换提示。
- 未验证项：IDE `build_project` 本轮 120 秒超时，IDE `get_file_problems` 针对本轮两个 TS 文件也分别 120 秒超时，未能拿到 IDE problems 输出；已由 `npm run build` 覆盖 TypeScript 语法/类型门禁。未启动真实 Tauri 桌面触发 AskUserQuestionDialog，未用浏览器/屏幕阅读器检查实际 accessibility tree、焦点初始位置、焦点陷阱和播报顺序；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 启动真实 Tauri 桌面触发 AskUserQuestionDialog，检查 dialog semantics 在真实 WebView accessibility tree 中是否关联 title/首个问题，并点测 Escape、关闭按钮、单选/多选、提交后 busy 状态；2) 评估 cc-gui 的 AskUserQuestion 倒计时、折叠 mini-mode、多问题逐步导航和 Other 输入是否适合本项目，优先选择不会改变 daemon answer payload 的低风险项；3) 做真实运行并排截图审计，重点看 PlanApproval、ToolPermission、AskUserQuestion、SDK dependency modal、Markdown code-copy、搜索/折叠窗口和工具块完成态；4) 将“大历史加载快路径”“消息消失修复”“readable fallback 修复”“dialog semantics 修复”按主题整理提交，降低当前累计未提交改动的回归审查成本。

## 本轮 PLAN 2026-06-20 ask user question shortcut hint

- 目标：补齐 `AskUserQuestionDialog` 的可见 Escape 取消提示。当前弹窗已经支持全局 `Escape` 取消并避开输入控件，但 footer 只显示 Cancel/Submit 按钮，没有告诉用户键盘逃生路径；这会让阻塞式提问在长问题或用户犹豫时显得不可发现。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `AskUserQuestionDialog.tsx` / test 与 cc-gui `webview/src/components/AskUserQuestionDialog.tsx`，确认本轮只暴露现有 Escape 取消能力，不改 answer payload、不改单选/多选、不引入 cc-gui 倒计时、折叠、多问题逐步导航或 Other 输入。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/AskUserQuestionDialog.test.ts`，断言 label helper 在 key-only 翻译下提供 `shortcutCancel` fallback，SSR 输出包含 `<kbd>Esc</kbd>` 与可读取消提示；旧实现应因 label/渲染缺失失败。
- 功能点 4：实现最小展示修复。验证方式：给 `getAskUserQuestionDialogLabels()` 增加 `shortcutCancel`，在 footer 按钮区下方渲染 compact keyboard hint；不改变按钮顺序、不改变提交/取消回调、不改变快捷键解析。
- 功能点 5：同步 locale 与 Trellis 规范。验证方式：更新 `src/locales/en.json` / `zh.json` 新增 `chat.askUser.shortcutCancel`，并更新 `.trellis/spec/frontend/component-guidelines.md` 记录 ask-user-question 弹窗必须显式展示 Escape 取消提示。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻确认弹窗测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 ask user question shortcut hint

- 上轮进展与阻塞：上一轮已补齐 `AskUserQuestionDialog` dialog semantics；代码中确实存在 `role="dialog"`、`aria-modal="true"`、`aria-labelledby` / `aria-describedby` 和 title/首个问题 id 回归测试。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能显示最新”仍保持前几轮代码修复状态：大历史快速首屏、assistant raw/stream 去重、乱序 tool_result utility 与 MessageList 渲染兜底都已在代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的真实 `webview/src/components/AskUserQuestionDialog.tsx` 有倒计时、折叠、多问题进度、Other 输入和自定义输入上限等更完整的提问体验；本项目暂不复制这些高风险流程能力。本项目已经有 `Escape` 取消行为，但此前 footer 没有任何可见键盘提示，用户只能猜测如何从阻塞提问中快速退出。
- 本轮差距诊断（超越 cc-gui）：本轮聚焦“已有能力可发现”。在不改变 daemon answer payload、不增加复杂流程状态的前提下，把 `Esc Cancel` 显示在 footer，能让键盘用户和高频用户马上知道取消路径；同时用 helper-owned fallback 和 locale key 避免 i18n 未初始化时露出 raw key。
- 本轮规划：只给 AskUserQuestion footer 增加 compact `Esc` 取消提示，给 label helper 和 en/zh locale 增加 `shortcutCancel`；不改单选/多选、不改提交/取消回调、不改 Escape 解析、不引入 cc-gui 倒计时/折叠/Other 输入。优先级中高：用户价值中高（阻塞弹窗的逃生路径可发现）、出现频率中、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/AskUserQuestionDialog.test.ts` 扩展 label fallback/翻译保留测试，并新增 SSR 回归断言 `<kbd>Esc</kbd>` 与 `Cancel` hint 可见。`src/components/chat/AskUserQuestionDialog.tsx` 新增 `shortcutCancel` fallback/翻译读取，在 footer 按钮区下方渲染 compact `Esc` keyboard hint。`src/locales/en.json` / `src/locales/zh.json` 新增 `chat.askUser.shortcutCancel`。`.trellis/spec/frontend/component-guidelines.md` 已同步 ask-user-question 阻塞弹窗必须显示 Escape 取消提示的合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/chat/AskUserQuestionDialog.test.ts` 先失败 3 项，失败点为 `labels.shortcutCancel` 未定义和 SSR 输出缺少 `<kbd>Esc</kbd>`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 7 tests）。相邻验证通过：`npm test -- src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx src/utils/dialogShortcuts.test.ts`（5 files / 29 tests，仅 Browserslist/caniuse-lite 过期提示）。全量前端验证通过：`npm test`（41 files / 406 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` 与 `git diff --cached --check` exit 0，仅输出 Windows LF/CRLF 转换提示。
- 未验证项：IDE `build_project` 本轮 120 秒超时，未能拿到 IDE problems 输出；已由 `npm run build` 覆盖 TypeScript 语法/类型门禁。未启动真实 Tauri 桌面触发 AskUserQuestionDialog，未视觉确认 footer hint 在真实 WebView 中的间距、换行、深浅色和屏幕阅读器播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 启动真实 Tauri 桌面触发 AskUserQuestionDialog，检查 `Esc Cancel` hint、Escape 实际取消、关闭按钮、单选/多选、提交后 busy 状态和 dialog semantics；2) 评估 cc-gui 的 AskUserQuestion 倒计时、折叠 mini-mode、多问题逐步导航和 Other 输入是否适合本项目，优先选择不会改变 daemon answer payload 的低风险项；3) 做真实运行并排截图审计，重点看 PlanApproval、ToolPermission、AskUserQuestion、SDK dependency modal、Markdown code-copy、搜索/折叠窗口和工具块完成态；4) 将“大历史加载快路径”“消息消失修复”“readable fallback 修复”“dialog semantics/shortcut hint 修复”按主题整理提交，降低当前累计未提交改动的回归审查成本。

## 本轮 PLAN 2026-06-20 tool permission shortcut hints

- 目标：补齐 `ToolPermissionDialog` 的可见 `Enter` / `Esc` 快捷键提示。当前弹窗已经支持全局 `Enter` 允许一次、`Escape` 拒绝，并且会避开输入/按钮等控件，但 footer 只显示 Deny / Allow once 按钮，没有告诉用户可用键盘路径；cc-gui 真实 `webview/src/components/PermissionDialog.tsx` 会在选项按钮中显示 `1` / `2` / `3` 快捷键，本项目至少应把已有快捷键显式化。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guide 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ToolPermissionDialog.tsx` / test 与 cc-gui `webview/src/components/PermissionDialog.tsx`，确认本轮只暴露现有 `Enter` / `Esc` 能力，不改 permission payload、不改一次性提交保护、不引入 cc-gui 倒计时、允许始终、数字选择或 resize handle。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ToolPermissionDialog.test.ts`，断言 label helper 在 key-only 翻译下提供 `shortcutAllow` / `shortcutDeny` fallback，翻译存在时保留翻译，SSR 输出包含 `<kbd>Enter</kbd>`、`<kbd>Esc</kbd>` 和可读提示；旧实现应因 label/渲染缺失失败。
- 功能点 4：实现最小展示修复。验证方式：给 `getToolPermissionDialogLabels()` 增加快捷键提示 labels，在 footer 按钮区旁渲染 compact keyboard hints；不改变按钮顺序、不改变 deny/allow 回调、不改变快捷键解析。
- 功能点 5：同步 locale 与 Trellis 规范。验证方式：更新 `src/locales/en.json` / `zh.json` 新增 `chat.permission.shortcutAllow` / `shortcutDeny`，并更新 `.trellis/spec/frontend/component-guidelines.md` 记录工具权限弹窗必须显式展示 `Enter` / `Esc` 提示。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻确认弹窗测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 tool permission shortcut hints

- 上轮进展与阻塞：上一轮已补齐 `AskUserQuestionDialog` 的可见 `Esc Cancel` hint；代码中确实存在 `shortcutCancel` fallback/locale、footer `<kbd>Esc</kbd>` 和 SSR 回归测试。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能展示最新”仍保持前几轮代码修复状态：Claude 大历史 tail fast path、assistant raw/stream 去重、乱序 `tool_result` 查找和 MessageList 渲染兜底都已在代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 真实 `webview/src/components/PermissionDialog.tsx` 会在权限选项中显示数字快捷键 `1` / `2` / `3`，并支持键盘选择；本项目 `ToolPermissionDialog` 已有 `Enter` 允许一次和 `Escape` 拒绝的快捷键，但此前 footer 没有可见提示，用户只能猜测阻塞安全决策的键盘路径。
- 本轮差距诊断（超越 cc-gui）：本项目继续沿用 helper-owned readable fallback，把快捷键提示文案也纳入 i18n/fallback 边界。即使翻译资源未加载或局部缺失，footer 仍显示 `Enter Allow once` / `Esc Deny`，避免在安全确认场景露出 raw key 或隐藏操作含义。
- 本轮规划：只给 ToolPermission footer 增加 compact `Enter` / `Esc` 可见提示，给 label helper 和 en/zh locale 增加 `shortcutAllow` / `shortcutDeny`；不改 deny/allow-once 回调、不改 `resolveToolPermissionShortcutAction()`、不改一次性提交保护、不引入 cc-gui 的倒计时、允许始终、数字选择或 resize handle。优先级高：用户价值高（阻塞式安全决策）、出现频率中、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/ToolPermissionDialog.test.ts` 扩展 label fallback/翻译保留测试，并新增 SSR 回归断言 `<kbd>Enter</kbd>`、`Allow once`、`<kbd>Esc</kbd>`、`Deny` hint 可见。`src/components/chat/ToolPermissionDialog.tsx` 新增 `shortcutAllow` / `shortcutDeny` fallback/翻译读取，在 footer 按钮区下方渲染 compact keyboard hints。`src/locales/en.json` / `src/locales/zh.json` 新增 `chat.permission.shortcutAllow` / `chat.permission.shortcutDeny`。`.trellis/spec/frontend/component-guidelines.md` 已同步工具权限弹窗必须显示 `Enter` / `Esc` 提示的合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/chat/ToolPermissionDialog.test.ts` 先失败 3 项，失败点为 `labels.shortcutAllow` / `labels.shortcutDeny` 未定义和 SSR 输出缺少 `<kbd>Enter</kbd>`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 8 tests）。相邻验证通过：`npm test -- src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx src/utils/dialogShortcuts.test.ts`（5 files / 30 tests，仅 Browserslist/caniuse-lite 过期提示）。全量前端验证通过：`npm test`（41 files / 407 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：IDE `build_project` 本轮 120 秒超时，IDE `get_file_problems` 针对本轮两个 TS 文件也分别 120 秒超时，未能拿到 IDE problems 输出；已由 `npm run build` 覆盖 TypeScript 语法/类型门禁。未启动真实 Tauri 桌面触发 ToolPermissionDialog，未视觉确认 footer hint 在真实 WebView 中的间距、换行、深浅色、键盘实际操作和屏幕阅读器播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 启动真实 Tauri 桌面触发 ToolPermissionDialog，点测 `Enter Allow once`、`Esc Deny`、关闭按钮、overlay deny、长参数、长 cwd、raw input 展开和 dialog semantics；2) 评估 cc-gui PermissionDialog 的倒计时、允许始终、数字选择和 resize handle，优先筛选不会扩大 permission payload 风险的低成本项；3) 做真实运行并排截图审计，重点看 PlanApproval、ToolPermission、AskUserQuestion、SDK dependency modal、Markdown code-copy、搜索/折叠窗口和工具块完成态；4) 将“大历史加载快路径”“消息消失修复”“readable fallback 修复”“dialog semantics/shortcut hint 修复”按主题整理提交，降低当前累计未提交改动的回归审查成本。

## 本轮 PLAN 2026-06-20 tool permission cwd dedupe

- 目标：减少 `ToolPermissionDialog` 权限确认弹窗里的重复信息。当前 `inputs.cwd` 会优先进入参数预览，同时弹窗底部又单独显示 `Working directory:`，用户在安全决策时需要扫两遍相同路径；cc-gui 真实 `webview/src/components/PermissionDialog.tsx` 把 working directory 作为命令盒上下文单独呈现，主要内容区域聚焦命令/文本/其他参数。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ToolPermissionDialog.tsx` / test 与 cc-gui `webview/src/components/PermissionDialog.tsx`，确认本轮只去掉参数预览里的重复 cwd，不改 raw input、不改 permission payload、不改 deny/allow 回调、不改快捷键。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ToolPermissionDialog.test.ts`，导出并测试 input preview helper：当 `inputs.cwd` 与 request cwd 相同时跳过参数行；当二者不同或 cwd 是其他参数时保留，避免误删有价值差异。
- 功能点 4：实现最小展示修复。验证方式：让 input preview helper 接收 request cwd，仅过滤与 request cwd 完全相同的 `cwd` 参数；工作目录行和 raw input 保持不变。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录工具权限弹窗应避免在参数预览和工作目录行重复展示相同 cwd。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻确认弹窗测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 tool permission cwd dedupe

- 上轮进展与阻塞：上一轮已补齐 `ToolPermissionDialog` 的可见 `Enter` / `Esc` hint；代码中确实存在 `shortcutAllow` / `shortcutDeny` fallback/locale、footer `<kbd>Enter</kbd>` / `<kbd>Esc</kbd>` 和 SSR 回归测试。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能展示最新”仍保持前几轮代码修复状态：Claude 大历史 tail fast path、assistant raw/stream 去重、乱序 `tool_result` 查找和 MessageList 渲染兜底都已在代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 真实 `webview/src/components/PermissionDialog.tsx` 把 working directory 放在命令盒上下文中，主要内容区域聚焦命令/文本/其他参数。本项目 `ToolPermissionDialog` 之前会在参数预览里优先显示 `cwd`，底部又显示 `Working directory:`，安全确认时路径重复占用扫描空间。
- 本轮差距诊断（超越 cc-gui）：本轮不是简单隐藏 cwd，而是只过滤与 request-level `cwd` 完全相同的重复值；如果 `inputs.cwd` 与请求工作目录不同，仍保留在参数预览中。这样能减少噪音，同时保留异常或嵌套执行目录的风险信号；raw input 也继续保留完整 provider payload。
- 本轮规划：只调整 input preview 派生逻辑：新增可测试 helper `getToolPermissionInputPreview(inputs, cwd)`，过滤重复 cwd；不改 raw input、不改 permission response、不改 deny/allow、不改快捷键、不改样式布局。优先级中等偏高：用户价值中（减少阻塞安全弹窗噪音）、出现频率中、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/ToolPermissionDialog.test.ts` 新增 input preview 回归，覆盖重复 cwd 被省略、差异 cwd 被保留。`src/components/chat/ToolPermissionDialog.tsx` 导出 `getToolPermissionInputPreview()`，组件调用时传入 `request.cwd`，仅从参数预览中过滤同值 `cwd`；工作目录行与 raw input 保持不变。`.trellis/spec/frontend/component-guidelines.md` 已同步工具权限弹窗应避免重复展示相同 cwd、但保留差异 cwd 与 raw input 的合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/chat/ToolPermissionDialog.test.ts` 先失败 2 项，失败点为 `getToolPermissionInputPreview is not a function`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 10 tests）。相邻验证通过：`npm test -- src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx src/utils/dialogShortcuts.test.ts`（5 files / 32 tests，仅 Browserslist/caniuse-lite 过期提示）。全量前端验证通过：`npm test`（41 files / 409 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：IDE `build_project` 本轮 120 秒超时，未能拿到 IDE problems 输出；已由 `npm run build` 覆盖 TypeScript 语法/类型门禁。未启动真实 Tauri 桌面触发 ToolPermissionDialog，未视觉确认参数预览中重复 cwd 的真实消失效果、长 cwd 截断和 raw input 展开；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 启动真实 Tauri 桌面触发 ToolPermissionDialog，点测重复 cwd 去噪、差异 cwd 保留、raw input、`Enter Allow once`、`Esc Deny` 和长参数布局；2) 评估 cc-gui PermissionDialog 的主命令内容盒是否值得低风险引入本项目，让 `command` / `content` / `text` 比普通参数更突出；3) 做真实运行并排截图审计，重点看 PlanApproval、ToolPermission、AskUserQuestion、SDK dependency modal、Markdown code-copy、搜索/折叠窗口和工具块完成态；4) 将“大历史加载快路径”“消息消失修复”“readable fallback 修复”“dialog semantics/shortcut hint/cwd dedupe 修复”按主题整理提交，降低当前累计未提交改动的回归审查成本。

## 本轮 PLAN 2026-06-20 tool permission primary input preview

- 目标：让 `ToolPermissionDialog` 把 `command` / `content` / `text` 这类主输入从普通参数列表中提升为醒目的主内容预览。cc-gui 真实 `webview/src/components/PermissionDialog.tsx` 会优先用 `command` / `content` / `text` 生成 command content，并把 working directory 放在 header；本项目当前虽已去掉重复 cwd，但 `command` 仍和普通参数同级，安全确认时不够突出。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ToolPermissionDialog.tsx` / test 与 cc-gui `webview/src/components/PermissionDialog.tsx`，确认本轮只改展示层主输入提取，不改 raw input、不改 permission payload、不改 deny/allow 回调、不改快捷键。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ToolPermissionDialog.test.ts`，测试主输入 helper 按 `command` > `content` > `text` 优先级提取；SSR 断言主输入盒可见，并确认普通参数预览不再重复展示同一个 primary key。
- 功能点 4：实现最小展示修复。验证方式：新增 `getToolPermissionPrimaryInput()`，组件中渲染紧凑主输入盒；`getToolPermissionInputPreview()` 支持 omit keys，组件调用时剔除 primary key，raw input 继续完整保留。
- 功能点 5：同步 locale 与 Trellis 规范。验证方式：更新 `src/locales/en.json` / `zh.json` 新增 `chat.permission.primaryCommand` / `primaryContent` / `primaryText`，并更新 `.trellis/spec/frontend/component-guidelines.md` 记录工具权限弹窗主输入展示约定。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻确认弹窗测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 tool permission primary input preview

- 上轮进展与阻塞：上一轮已完成 `ToolPermissionDialog` 重复 cwd 去噪；代码中确实存在 `getToolPermissionInputPreview(inputs, cwd, omittedKeys?)`，并且测试覆盖了相同 cwd 被过滤、差异 cwd 保留。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能展示最新”仍保持前几轮代码修复状态：Claude 大历史 tail fast path、assistant raw/stream 去重、乱序 `tool_result` 查找和 MessageList 渲染兜底都已在代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 真实 `webview/src/components/PermissionDialog.tsx` 优先从 `command` / `content` / `text` 生成 command content，并把它放在独立内容盒中。本项目此前虽然有参数预览和 raw input，但 `command` 仍作为普通参数行展示，安全确认时用户需要在参数列表里找最关键的执行内容。
- 本轮差距诊断（超越 cc-gui）：本轮把主输入提升为独立紧凑预览盒，同时保留完整 raw input，并从普通参数预览中移除同一个 primary key，避免重复。主输入 label 也纳入 readable fallback / en / zh locale，避免 i18n 缺失时出现 raw key。
- 本轮规划：只改展示层：新增 `getToolPermissionPrimaryInput()`，按 `command` > `content` > `text` 提取主输入；渲染 dedicated preview box；参数预览 omit primary key；不改 permission payload、不改 raw input、不改 deny/allow、不改快捷键、不引入 cc-gui 倒计时/多模式/resize。优先级高：用户价值高（安全决策的核心内容更醒目）、出现频率高（Bash/写入/文本类工具常用这些字段）、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/ToolPermissionDialog.test.ts` 扩展 label fallback/翻译测试，新增 primary helper 优先级测试、参数预览 omit primary key 测试、SSR 主输入盒回归。`src/components/chat/ToolPermissionDialog.tsx` 新增 `PRIMARY_INPUT_KEYS`、导出的 `getToolPermissionPrimaryInput()`、primary label 派生，并在描述下方渲染 `data-tool-permission-primary="<key>"` 的紧凑主输入盒；组件调用 `getToolPermissionInputPreview()` 时剔除 primary key。`src/locales/en.json` / `src/locales/zh.json` 新增 `chat.permission.primaryCommand` / `primaryContent` / `primaryText`。`.trellis/spec/frontend/component-guidelines.md` 已同步工具权限弹窗主输入展示约定。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/chat/ToolPermissionDialog.test.ts` 先失败 5 项，失败点为 primary labels 未定义、`getToolPermissionPrimaryInput is not a function`、参数预览无法 omit primary key、SSR 缺少 `data-tool-permission-primary="command"`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 13 tests）。相邻验证通过：`npm test -- src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx src/utils/dialogShortcuts.test.ts`（5 files / 35 tests，仅 Browserslist/caniuse-lite 过期提示）。全量前端验证通过：`npm test`（41 files / 412 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：IDE `build_project` 本轮 120 秒超时，未能拿到 IDE problems 输出；已由 `npm run build` 覆盖 TypeScript 语法/类型门禁。未启动真实 Tauri 桌面触发 ToolPermissionDialog，未视觉确认主输入盒在真实 WebView 中的高度、换行、滚动、深浅色和长命令表现；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 启动真实 Tauri 桌面触发 ToolPermissionDialog，点测主输入盒、重复 cwd 去噪、差异 cwd 保留、raw input、`Enter Allow once`、`Esc Deny` 和长参数布局；2) 评估是否为主输入盒增加复制按钮，复用现有 copy pattern，但先确认真实使用中复制命令是否高频；3) 做真实运行并排截图审计，重点看 PlanApproval、ToolPermission、AskUserQuestion、SDK dependency modal、Markdown code-copy、搜索/折叠窗口和工具块完成态；4) 将“大历史加载快路径”“消息消失修复”“readable fallback 修复”“dialog semantics/shortcut hint/cwd dedupe/primary input 修复”按主题整理提交，降低当前累计未提交改动的回归审查成本。
## 本轮 PLAN 2026-06-20 tool permission primary input copy

- 目标：在 `ToolPermissionDialog` 的主输入预览盒中增加一键复制按钮。上一轮已把 `command` / `content` / `text` 提升为 dedicated preview；本轮继续降低安全确认后的复用成本，用户可以直接复制 Bash 命令、写入内容或文本 payload，而不必展开 raw input 再手动选中。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ToolPermissionDialog.tsx` / test、现有 copy pattern 与 cc-gui `webview/src/components/PermissionDialog.tsx`，确认本轮只增强主输入盒交互，不改 raw input、不改 permission payload、不改 deny/allow 回调、不引入 cc-gui 倒计时/允许始终/resize。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ToolPermissionDialog.test.ts`，断言 label helper 在 key-only translator 下提供 `Copy` / `Copied` fallback，中文翻译可保留；SSR 断言主输入盒内存在 copy button 的 title/aria-label 和 `data-tool-permission-primary-copy`。
- 功能点 4：实现最小交互修复。验证方式：在主输入盒 header 增加 icon-only copy button，复制完整 primary value（不复制截断预览），复制后短暂显示 copied label / check icon；按钮阻止冒泡且不影响 allow/deny 快捷键、提交状态或 raw input。
- 功能点 5：同步 locale 与 Trellis 规范。验证方式：更新 `src/locales/en.json` / `zh.json` 新增 `chat.permission.copyPrimaryInput` / `copiedPrimaryInput`，并更新 `.trellis/spec/frontend/component-guidelines.md` 记录主输入盒应提供复制 affordance。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻确认弹窗测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 tool permission primary input copy

- 上轮进展与阻塞：上一轮已完成 `ToolPermissionDialog` 主输入预览；代码中确实存在 `getToolPermissionPrimaryInput()`、`data-tool-permission-primary="<key>"`、参数预览 omit primary key、重复 cwd 去噪和 en/zh primary label。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能展示最新”仍保持前几轮代码修复状态：Claude 大历史 tail fast path、assistant raw/stream 同步、text snapshot 去重、乱序 `tool_result` 查找和 MessageList 渲染兜底都已在代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 真实 `webview/src/components/PermissionDialog.tsx` 把 `command` / `content` / `text` 作为独立 command content 展示，并复用 MarkdownBlock 的代码复制能力；本项目上一轮已有独立主输入盒，但没有一键复制，用户要复用命令或文本时仍需手动选中或展开 raw input。
- 本轮差距诊断（超越 cc-gui）：本轮不是复制 cc-gui 的倒计时、允许始终或 resize handle，而是针对本项目已提升的主输入盒补上精确复制 affordance。复制按钮只复制完整 primary value，不复制 600 字符预览截断版；同时保持 raw input 完整可查、permission payload 不变，解决的是“审批后想复用命令/内容却需要手动框选”的具体摩擦。
- 本轮规划：只改展示/交互层：给主输入盒 header 增加 icon-only copy button，新增 readable fallback 和 en/zh locale；不改 deny/allow-once 回调、不改 store 响应、不改 raw input、不引入 cc-gui 的倒计时/允许始终/数字选择/resize。优先级中高：用户价值中高（Bash/写入/文本工具复用频繁）、出现频率高、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/ToolPermissionDialog.test.ts` 新增 label fallback/翻译断言和 SSR 主输入复制按钮回归，并补全测试 mock 以兼容 `bridge.ts` 的 i18n 初始化。`src/components/chat/ToolPermissionDialog.tsx` 在 primary preview header 增加 `data-tool-permission-primary-copy="<key>"` 的复制按钮，复用 `copyToClipboard()`，复制完整 primary value，并在 1.6 秒内显示 copied label / check icon；按钮不影响 allow/deny 提交状态，也不移除 raw input。`src/locales/en.json` / `src/locales/zh.json` 新增 `chat.permission.copyPrimaryInput` 与 `chat.permission.copiedPrimaryInput`。`.trellis/spec/frontend/component-guidelines.md` 已同步主输入盒复制 affordance 合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/chat/ToolPermissionDialog.test.ts` 先失败 3 项，失败点为 `labels.copyPrimaryInput` / `labels.copiedPrimaryInput` 未定义、SSR 输出缺少 `data-tool-permission-primary-copy="command"`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 14 tests）。相邻验证通过：`npm test -- src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx src/utils/dialogShortcuts.test.ts`（5 files / 36 tests，仅 Browserslist/caniuse-lite 过期提示）。最终全量前端验证通过：`npm test`（41 files / 413 tests，仅 Browserslist/caniuse-lite 过期提示）。最终生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：IDE `build_project` 本轮 120 秒超时，未能拿到 IDE problems 输出；已由 `npm run build` 覆盖 TypeScript 语法/类型门禁。未启动真实 Tauri 桌面触发 ToolPermissionDialog，未手动点击确认复制按钮写入系统剪贴板，也未视觉确认按钮在真实 WebView 中的 hover/focus、长命令滚动、深浅色和屏幕阅读器播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 启动真实 Tauri 桌面触发 ToolPermissionDialog，点测主输入复制、主输入滚动、重复 cwd 去噪、差异 cwd 保留、raw input、`Enter Allow once`、`Esc Deny` 和长参数布局；2) 对 `PlanApprovalDialog` / `AskUserQuestionDialog` 做同类真实桌面 smoke，确认 Markdown plan、按钮 busy、可见快捷键和 ARIA 语义在 WebView 中表现一致；3) 评估 cc-gui PermissionDialog 的倒计时/数字选择/resize/allow-always，优先筛选不改变本项目 permission payload 的展示层能力；4) 将“大历史加载快路径”“消息消失修复”“readable fallback 修复”“dialog semantics/shortcut/cwd/primary/copy 修复”按主题整理提交，降低当前累计未提交改动的回归审查成本。

## 本轮 PLAN 2026-06-20 tool permission numeric shortcuts

- 目标：补齐 cc-gui 工具权限弹窗的数字键直达体验，但不引入本项目后端尚不支持的“始终允许”响应语义。
- 功能点 1：恢复上下文与对标确认。验证方式：读取 Trellis task artifacts、frontend spec、`package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，`C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断当前实现与 cc-gui。验证方式：对比本项目 `ToolPermissionDialog.tsx` 与 cc-gui `PermissionDialog.tsx`，确认 cc-gui 有 `1/2/3`、箭头与 Enter，当前项目只有 Enter/Esc；本项目后端 `ToolPermissionResponse` 只有 `allow: bool`。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ToolPermissionDialog.test.ts`，先断言 `resolveToolPermissionShortcutAction('1') === 'allow'`、`'2' === 'deny'` 且 editable target 不触发，并断言 SSR 输出可见 `1/2` 提示；旧实现应失败。
- 功能点 4：实现最小前端改动。验证方式：`resolveToolPermissionShortcutAction()` 支持数字键；按钮/提示区显示 `1 Allow once` 与 `2 Deny`，保留 Enter/Esc，不改变 `onAnswer(boolean)` 与 store/后端协议。
- 功能点 5：同步前端组件规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md` 的 tool permission 规则，记录数字键仅映射当前支持的二态决策。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 dialog 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`git diff --check` / `git diff --cached --check`；安全清理 `dist` / `out` 构建产物。

## 迭代记录 2026-06-20 tool permission numeric shortcuts

- 上轮进展与阻塞：上轮已完成 `ToolPermissionDialog` 主输入预览复制、cwd 去重、可读 fallback、ARIA dialog 语义与 Enter/Esc 可见提示，并通过定向/相邻/全量前端、构建、Rust check/test 验证；IDE build 仍在 120s 超时。本轮复查确认这些能力代码中确实存在于 `src/components/chat/ToolPermissionDialog.tsx` 与测试中。当前阻塞仍是未做真实 Tauri 桌面 smoke；工作区存在大量历史未提交修改，本轮只追加本轮相关改动。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `webview/src/components/PermissionDialog.tsx` 支持数字键 `1/2/3` 直达选项，并把编号显示在 option button 中；本项目此前只有 Enter=允许一次、Esc=拒绝，缺少数字键直达与可见编号。用户价值：阻塞式权限弹窗出现频繁时，数字键能减少鼠标移动和认知停顿。
- 本轮差距诊断（超越 cc-gui）：本项目后端 `ToolPermissionResponse` 当前只有 `{ allow: bool }`，因此本轮不复制 cc-gui 的 `Allow Always`，而是明确限制为 `1=Allow once`、`2=Deny`。用户价值：提供快捷操作，同时避免 UI 承诺一个后端不会记忆的权限策略。
- 本轮规划：选择 tool permission 数字快捷键作为本轮唯一实现项。优先级依据：用户价值中高（阻塞决策高频）、实现成本低（单组件 + 单测）、回归风险低（不改 store/backend 协议）、对标证据明确（cc-gui 真实代码核实）。
- 本轮完成项：`src/components/chat/ToolPermissionDialog.tsx` 的 `resolveToolPermissionShortcutAction()` 新增数字键映射，非编辑目标下 `1` 允许一次、`2` 拒绝、`3` 不绑定；输入框/文本域中数字键不被抢占。弹窗底部按钮与提示区新增可见 `1/2` 提示，保留 Enter/Esc。`src/components/chat/ToolPermissionDialog.test.ts` 新增 RED/GREEN 覆盖数字键映射、editable guard、SSR 可见提示。`.trellis/spec/frontend/component-guidelines.md` 记录 tool permission 数字键规则，并明确未支持 remembered permission 前不得新增 `Always allow` / `3`。
- 本轮验证：RED：`npm test -- src/components/chat/ToolPermissionDialog.test.ts` 先失败 2 项，旧实现数字键返回 `null` 且 SSR 缺少 `<kbd>1</kbd>`。GREEN：同命令通过（1 file / 16 tests）。相邻验证通过：`npm test -- src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx src/utils/dialogShortcuts.test.ts`（5 files / 38 tests）。全量前端通过：`npm test`（41 files / 415 tests，仅 Browserslist/caniuse-lite 过期提示）。构建通过：`npm run build`（Vite build passed，仅 Browserslist/caniuse-lite 过期提示）。Rust 验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`；`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE build：`mcp__idea_mcp.build_project` 120s 超时，未验证。清理：安全删除 `C:/guodevelop/ccg-switch/dist`，`out` 不存在。diff：`git diff --check` exit 0（仅 LF/CRLF 提示）；`git diff --cached --check` exit 0，无输出。
- 下一步候选：1) 做真实 Tauri 桌面 smoke，验证 ToolPermissionDialog 的主输入复制、cwd 去重、`1/2`、Enter/Esc、raw input 展开和重复提交保护；2) 评估是否设计 remembered permission 后端/桥接合同，再决定是否引入 cc-gui 的 `Allow Always`；3) 对 PlanApproval / AskUserQuestion 做同级桌面 smoke，确认 Markdown、ARIA、忙碌态和快捷键在 WebView 中实际可用；4) 继续整理多轮未提交改动，按主题拆分提交，避免把无关历史修改混在一个 commit。

## 本轮 PLAN 2026-06-20 tool permission shortcut accessible labels

- 目标：让 `ToolPermissionDialog` 的实际操作按钮也暴露快捷键上下文。上一轮 footer 已显示 `1/2`、Enter/Esc，但按钮自身的 `title` / `aria-label` 仍只是 `Allow once` / `Deny`；键盘和辅助技术用户聚焦按钮时无法直接听到同一快捷键合同。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断当前实现。验证方式：读取 `ToolPermissionDialog.tsx` / test，确认当前 footer hints 已存在，但 close/deny/allow buttons 的 `title` / `aria-label` 还没有包含 `1/2`、Enter/Esc 快捷键信息。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/ToolPermissionDialog.test.ts`，断言 SSR 输出中 allow button 的 `title` / `aria-label` 为 `Allow once (1 or Enter)`，deny button 为 `Deny (2 or Esc)`；旧实现应失败。
- 功能点 4：实现最小可访问性修复。验证方式：在组件内派生 shortcut-aware action label，并应用到 footer allow/deny buttons 的 `title` / `aria-label`；关闭按钮保留短 `Deny`，避免标题栏关闭控件过长。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 tool permission 操作按钮的 accessible label/title 也必须包含快捷键信息。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 dialog 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 tool permission shortcut accessible labels

- 上轮进展与阻塞：上一轮已完成 `ToolPermissionDialog` 的二态数字快捷键，代码中确实存在 `resolveToolPermissionShortcutAction('1') -> allow`、`'2' -> deny`、footer 可见 `1/2` 提示和相关回归测试。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能展示最新”仍保持前几轮代码修复状态：Claude 大历史 tail fast path、assistant raw/stream 同步、text snapshot 去重、乱序 `tool_result` 查找和 MessageList 渲染兜底都已在代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 真实 `webview/src/components/PermissionDialog.tsx` 将快捷键直接附在可点击选项按钮上；本项目上一轮已在按钮里显示 `kbd` 和 footer hint，但按钮本身的 `title` / `aria-label` 仍然只是 `Allow once` / `Deny`，聚焦到按钮时不会听到或看到完整快捷键上下文。
- 本轮差距诊断（超越 cc-gui）：本轮把快捷键信息写入 action button 的 accessible name / tooltip，而不只依赖旁边的视觉 hint。这样辅助技术用户、键盘用户和鼠标悬停用户都能在当前操作控件上得到同一合同：`Allow once (1 / Enter)`、`Deny (2 / Esc)`。
- 本轮规划：只改 footer action buttons 的 `title` / `aria-label`，标题栏关闭按钮保持短 `Deny`；不改 permission payload、不改回调、不改快捷键解析、不引入 remembered permission。优先级中等偏高：用户价值中（可访问性和可发现性）、出现频率中、实现成本极低、回归风险低。
- 本轮完成：`src/components/chat/ToolPermissionDialog.test.ts` 新增 SSR 回归，断言 footer allow/deny buttons 暴露 shortcut-aware `title` / `aria-label`。`src/components/chat/ToolPermissionDialog.tsx` 新增 `allowActionLabel` / `denyActionLabel`，并应用到 footer 操作按钮；可见按钮文本和 `kbd` 不变。`.trellis/spec/frontend/component-guidelines.md` 已同步操作按钮可访问名称必须包含快捷键上下文的合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/chat/ToolPermissionDialog.test.ts` 先失败 1 项，失败点为 SSR 输出缺少 `title="Allow once (1 / Enter)"`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 17 tests）。相邻验证通过：`npm test -- src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx src/utils/dialogShortcuts.test.ts`（5 files / 39 tests，仅 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（41 files / 416 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。IDE `build_project` 120 秒超时，未验证。
- 未验证项：未启动真实 Tauri 桌面触发 ToolPermissionDialog，未实际用屏幕阅读器确认按钮名称播报，未视觉确认 tooltip 在真实 WebView 中表现；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri 桌面 smoke：触发 ToolPermissionDialog，点测主输入复制、cwd 去重、`1/2`、Enter/Esc、button tooltip/aria、raw input 展开和重复提交保护；2) 对 PlanApproval / AskUserQuestion 做同类 button accessible-label 审计，确保可见 shortcut hint 和聚焦控件的名称一致；3) 评估 remembered permission 后端/桥接合同，再决定是否引入 cc-gui 的 `Allow Always`；4) 按主题整理提交，避免继续累积“大历史加载”“消息流修复”“弹窗可访问性”混合 diff。

## 本轮 PLAN 2026-06-20 approval question shortcut accessible labels

- 目标：把阻塞型审批/提问弹窗的可见快捷键提示同步到实际可聚焦操作按钮。上一轮已完成 ToolPermission footer action 的 `title` / `aria-label` 快捷键上下文；本轮补齐同类低风险缺口，避免辅助技术用户聚焦按钮时听不到同一快捷键合同。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断当前实现。验证方式：读取本项目 `PlanApprovalDialog.tsx` / `AskUserQuestionDialog.tsx` 与测试，并对照 cc-gui 对应组件，确认本轮只补 action button accessible labels，不改 approve/deny/submit/cancel 回调、不改 store/backend 协议、不引入倒计时/resize/执行模式。
- 功能点 3：补 RED 测试。验证方式：扩展 `PlanApprovalDialog.test.ts` 断言 approve / deny footer buttons 的 `title` / `aria-label` 含 `Enter` / `Esc`；扩展 `AskUserQuestionDialog.test.ts` 断言 footer cancel button 的 `title` / `aria-label` 含 `Esc`。旧实现应失败。
- 功能点 4：实现最小可访问性修复。验证方式：在组件内派生 shortcut-aware action label，并只应用到 footer 操作按钮；标题栏关闭按钮保持短 `Close`，AskUserQuestion submit 仍不声明 Enter，因为当前没有全局 Enter submit。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 PlanApproval / AskUserQuestion 的 footer shortcut 操作按钮必须让 accessible label/title 与可见 hint 保持一致。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 dialog 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 approval question shortcut accessible labels

- 上轮进展与阻塞：上一轮已完成 `ToolPermissionDialog` footer action 的 shortcut-aware `title` / `aria-label`，代码中确实存在 `Allow once (1 / Enter)` 与 `Deny (2 / Esc)` 的按钮可访问名称和回归测试。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能展示最新”仍保持前几轮代码修复状态：Claude 大历史 tail fast path、assistant raw/stream 同步、text snapshot 去重、乱序 `tool_result` 查找和 MessageList 渲染兜底都已在代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的 `PlanApprovalDialog.tsx` 和 `AskUserQuestionDialog.tsx` 都把阻塞决策的快捷键作为可见操作提示展示。本项目此前已经补了可见 `Enter` / `Esc` hint，但 `PlanApprovalDialog` 的 footer `Deny` / `Approve` 按钮和 `AskUserQuestionDialog` 的 footer `Cancel` 按钮自身仍没有 shortcut-aware `title` / `aria-label`。用户价值：键盘用户、鼠标悬停用户和辅助技术用户聚焦到真实操作按钮时，也能获得和视觉 hint 一致的快捷键合同。
- 本轮差距诊断（超越 cc-gui）：本轮不是引入 cc-gui 的倒计时、resize、collapse 或执行模式选择，而是把已经存在的快捷键提示沉到控件自身的 accessible name / tooltip。这样不改变任何 daemon 响应语义，却降低阻塞弹窗的可访问性风险和误操作成本。
- 本轮规划：只补 PlanApproval approve/deny 与 AskUserQuestion cancel footer action 的 `title` / `aria-label`；标题栏 close 保持短 `Close`，AskUserQuestion submit 不声明 Enter，因为当前没有全局 Enter submit。优先级中等偏高：用户价值中、出现频率中、实现成本极低、回归风险低。
- 本轮完成：`src/components/chat/PlanApprovalDialog.test.ts` 新增 SSR 回归，断言 footer `Deny` / `Approve` 暴露 `Deny (Esc)` 与 `Approve (Enter)` 的 `title` / `aria-label`。`src/components/chat/PlanApprovalDialog.tsx` 新增 `denyActionLabel` / `approveActionLabel` 并应用到 footer 操作按钮。`src/components/chat/AskUserQuestionDialog.test.ts` 新增 SSR 回归，断言 footer `Cancel` 暴露 `Cancel (Esc)` 的 `title` / `aria-label`。`src/components/chat/AskUserQuestionDialog.tsx` 新增 `cancelActionLabel` 并应用到 footer cancel button。`.trellis/spec/frontend/component-guidelines.md` 已同步 PlanApproval / AskUserQuestion footer shortcut action accessible label 合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/chat/PlanApprovalDialog.test.ts src/components/chat/AskUserQuestionDialog.test.ts` 先失败 2 项，失败点为 SSR 输出缺少 `title="Deny (Esc)"` 和 `title="Cancel (Esc)"`，符合旧实现缺口。GREEN 定向通过：同命令通过（2 files / 19 tests）。相邻验证通过：`npm test -- src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx src/utils/dialogShortcuts.test.ts`（5 files / 41 tests）。全量前端通过：`npm test`（41 files / 418 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 PlanApproval / AskUserQuestion，未实际用屏幕阅读器确认按钮名称播报，未视觉确认 tooltip 在真实 WebView 中表现；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri 桌面 smoke：触发 ToolPermission / PlanApproval / AskUserQuestion，点测 footer tooltip/aria、Enter/Esc、`1/2`、主输入复制、raw input 展开、重复提交保护和忙碌态；2) 对 PlanApproval 的 `Approve & Auto` 做协议审计，确认当前 `targetMode='auto'` 是否与后端/daemon 语义完全一致，再决定是否补更明确的模式说明；3) 继续评估 cc-gui 的倒计时、resize、collapse mini-mode 是否值得以不改协议的方式补齐；4) 按主题整理提交，避免继续累积“大历史加载”“消息流修复”“弹窗可访问性”混合 diff。

## 本轮 PLAN 2026-06-20 ask user question submit guard

- 目标：阻止 `AskUserQuestionDialog` 在没有任何选择时提交空答案。cc-gui 真实 `AskUserQuestionDialog.tsx` 会计算 `canProceed`，当前问题没有常规选择或有效自定义输入时禁用下一步/提交；本项目当前初始空答案也能点 Submit，会把 `{}` 写回 daemon，用户容易误提交并让后续执行基于无效答案继续。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph 诊断当前实现与 cc-gui。验证方式：读取本项目 `AskUserQuestionDialog.tsx` / test 和 cc-gui `webview/src/components/AskUserQuestionDialog.tsx`，确认本轮只增加提交门禁，不改 answer payload、不改单选/多选 state shape、不引入 cc-gui 的 Other 输入、倒计时、折叠或逐步导航。
- 功能点 3：补 RED 测试。验证方式：扩展 `AskUserQuestionDialog.test.ts`，SSR 断言初始无答案时 Submit 按钮 disabled；补纯 helper 测试覆盖单问题、多问题、单选/多选、空白答案。旧实现应失败。
- 功能点 4：实现最小门禁。验证方式：新增小型纯 helper 判断每个 daemon question 都有非空答案；`handleSubmit` 在未满足时直接返回；Submit button 在 `submitted || !canSubmit` 时 disabled；不改变 Cancel/Escape 路径。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 ask-user-question 不得提交空答案，Submit 的 disabled 状态必须和提交 guard 复用同一判断。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 dialog 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 ask user question submit guard

- 上轮进展与阻塞：上一轮已完成 PlanApproval / AskUserQuestion footer action 的 shortcut-aware `title` / `aria-label`，代码中确实存在 `Approve (Enter)`、`Deny (Esc)`、`Cancel (Esc)` 的按钮可访问名称和回归测试。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能展示最新”仍保持前几轮代码修复状态：Claude 大历史 tail fast path、assistant raw/stream 同步、text snapshot 去重、乱序 `tool_result` 查找和 MessageList 渲染兜底都已在代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 真实 `webview/src/components/AskUserQuestionDialog.tsx` 会计算 `canProceed`，当前问题无选择时禁用下一步/提交。本项目此前初始空答案也能点 Submit，可能把 `{}` 写回 daemon。用户价值：避免阻塞式提问被误提交为空答案，减少后续执行基于无效选择继续的风险。
- 本轮差距诊断（超越 cc-gui）：本项目没有复制 cc-gui 的 Other 输入、倒计时、折叠或逐步导航，而是把“不能空提交”做成纯 helper，并让按钮 disabled 和 `handleSubmit` 共用同一判断。这样即使未来新增键盘提交或其他 callback 入口，也不会绕过 UI disabled 写入空答案。
- 本轮规划：只给 AskUserQuestion 增加提交门禁：每个 daemon question 都必须有非空答案，单选/多选共用当前 `Record<question, string>` state shape；不改变 answer payload、不改变 Cancel/Escape、不新增 Other 输入或多步流程。优先级高：用户价值高（防误提交）、出现频率中、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/AskUserQuestionDialog.test.ts` 新增 RED/GREEN 覆盖，SSR 断言初始无答案时 Submit disabled，并补 `canSubmitAskUserQuestionAnswers()` helper 单元测试覆盖空问题、单问题、多问题、空白答案和多选逗号串。`src/components/chat/AskUserQuestionDialog.tsx` 新增 `canSubmitAskUserQuestionAnswers()`，`handleSubmit` 在 `!canSubmit` 时直接返回，Submit button 在 `submitted || !canSubmit` 时 disabled。`.trellis/spec/frontend/component-guidelines.md` 已同步 ask-user-question 不得提交空答案，且 disabled 状态和提交 guard 必须复用同一 helper。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/chat/AskUserQuestionDialog.test.ts` 先失败 1 项，失败点为 SSR 输出中 Submit 没有 `disabled`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 11 tests）。相邻验证通过：`npm test -- src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx src/utils/dialogShortcuts.test.ts`（5 files / 44 tests，仅 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（41 files / 421 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 AskUserQuestionDialog，未手动选择选项确认 Submit 从 disabled 切换为 enabled，未实际提交确认 daemon 收到非空 answer payload；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri 桌面 smoke：触发 AskUserQuestion，点测空答案 Submit disabled、选择后 enabled、Cancel/Escape、busy 状态、dialog semantics 和实际 answer payload；2) 对 PlanApproval 的 `Approve & Auto` 做协议审计，确认 `targetMode='auto'` 是否与后端/daemon 语义完全一致；3) 评估 cc-gui AskUserQuestion 的 Other 输入、多问题逐步导航、倒计时和 collapse mini-mode，优先选择不改变当前 answer payload 的展示层能力；4) 按主题整理提交，避免继续累积“大历史加载”“消息流修复”“弹窗可访问性/提交门禁”混合 diff。

## 本轮 PLAN 2026-06-20 ask user question custom answer

- 目标：为 `AskUserQuestionDialog` 增加低风险自定义答案路径，补齐 cc-gui `Other` 输入的核心用户价值，同时保持本项目后端已确认的 `answers: Record<string, string>` 响应格式不变。当前上一轮空答案门禁会阻止 `{}`，但如果 daemon 给出的选项不覆盖用户真实意图，或问题没有任何选项，用户只能取消，阻塞式提问会变成死路。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `AskUserQuestionDialog.tsx` / test / `useChatStore.answerAskUserQuestion` / Rust `permission_respond_ask_user_question` 与 cc-gui `webview/src/components/AskUserQuestionDialog.tsx`，确认本轮只新增自定义文本输入和 payload helper，不改 Store/Rust/Tauri 响应结构、不引入 `string[]`、倒计时、折叠或多问题逐步导航。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/AskUserQuestionDialog.test.ts`，覆盖自定义答案 label fallback、`canSubmitAskUserQuestionAnswers()` 可接受自定义答案、`buildAskUserQuestionAnswerPayload()` 对单选/多选/空选项问题生成字符串 payload，以及 SSR 输出包含自定义答案 textarea。旧实现应因 helper 参数/导出/UI 缺失失败。
- 功能点 4：实现最小功能。验证方式：组件增加 `customAnswers` 本地状态；自定义 textarea 使用 2000 字符上限；`canSubmit` 同时看常规选择和自定义答案；`handleSubmit` 通过共享 helper 构建 `Record<string, string>` payload。单选问题自定义答案优先，多选问题合并已选项与自定义答案，空选项问题只靠自定义答案提交。
- 功能点 5：同步 locale 与 Trellis 规范。验证方式：更新 `src/locales/en.json` / `zh.json` 新增自定义答案 label/placeholder/hint，更新 `.trellis/spec/frontend/component-guidelines.md` 记录 ask-user-question 自定义答案必须保持字符串 payload 并复用提交 guard/helper。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 dialog 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 ask user question custom answer

- 上轮进展与阻塞：上一轮已完成 `AskUserQuestionDialog` 空答案提交门禁，代码中确实存在 `canSubmitAskUserQuestionAnswers()`、Submit disabled 与 `handleSubmit` guard；`useChatStore.answerAskUserQuestion()` 和 Rust `permission_respond_ask_user_question` 仍保持 `answers: Record<string, string>` / `HashMap<String, String>` 合同。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能展示最新”仍保持前几轮代码修复状态。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 真实 `webview/src/components/AskUserQuestionDialog.tsx` 有 `OTHER_OPTION_MARKER`、`MAX_CUSTOM_INPUT_LENGTH = 2000` 和自定义输入，`canProceed` 会接受普通选项或有效自定义文本。本项目上一轮只做到“不能空提交”，但当选项不覆盖用户真实意图或 `options=[]` 时，用户仍只能取消。用户价值：阻塞式提问不再因为选项不完整而卡死，用户可以给出真实答案继续执行。
- 本轮差距诊断（超越 cc-gui）：本轮没有复制 cc-gui 的 `string[]` payload、多问题逐步导航、倒计时或折叠态，而是把自定义答案归一到现有字符串 map：单选自定义文本覆盖普通选择，多选自定义文本追加到逗号分隔值，空选项问题只靠自定义文本提交。这样补上核心体验，同时不改 Store/Rust/Tauri 响应文件合同。
- 本轮规划：只改 `AskUserQuestionDialog` 的本地 UI/state/helper、en/zh locale、前端组件规范和测试；不改 `useChatStore`、Rust command、permission watcher、daemon 桥接或响应文件结构。优先级高：用户价值高（解决阻塞问题）、出现频率中（工具提问/澄清场景）、实现成本低（纯前端）、回归风险可由 helper 测试覆盖。
- 本轮完成：`src/components/chat/AskUserQuestionDialog.tsx` 新增 `MAX_CUSTOM_ANSWER_LENGTH = 2000`、自定义答案 fallback labels、`customAnswers` 本地状态、textarea UI、`buildAskUserQuestionAnswerPayload()` 和扩展后的 `canSubmitAskUserQuestionAnswers()`；`handleSubmit` 通过 payload helper 写回 `Record<string, string>`。`src/components/chat/AskUserQuestionDialog.test.ts` 新增 RED/GREEN 覆盖 label fallback、optionless custom answer、单选自定义覆盖、多选自定义追加和 SSR textarea。`src/locales/en.json` / `src/locales/zh.json` 新增 `chat.askUser.customAnswerLabel`、`customAnswerPlaceholder`、`customAnswerHint`。`.trellis/spec/frontend/component-guidelines.md` 记录 ask-user-question 自定义答案的字符串 payload 合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/chat/AskUserQuestionDialog.test.ts` 先失败 7 项，失败点为自定义答案 labels 未定义、guard 不接受 custom answer、`buildAskUserQuestionAnswerPayload` 不存在、SSR 缺少 textarea，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 15 tests）。相邻验证通过：`npm test -- src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx src/utils/dialogShortcuts.test.ts`（5 files / 48 tests，仅 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（41 files / 425 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 AskUserQuestionDialog；未手动输入自定义答案确认 Submit 从 disabled 切换为 enabled；未实际提交确认 daemon 读取 `ask-user-question-response-<session>-<request>.json` 后按自定义答案继续；未检查真实 WebView 中 textarea 高度、焦点、深浅色和屏幕阅读器播报；未与 cc-gui 做运行并排截图。
- 下一步候选：1) 真实 Tauri 桌面 smoke：触发 AskUserQuestion，点测普通选项、自定义答案、空选项问题、Cancel/Escape、busy 状态、dialog semantics 和实际 response file；2) 对 PlanApproval 的 `Approve & Auto` 做协议审计，确认 `targetMode='auto'` 是否与后端/daemon 语义完全一致；3) 继续评估 cc-gui AskUserQuestion 的多问题逐步导航、倒计时和 collapse mini-mode，优先选择不改变响应合同的展示层能力；4) 按主题整理提交，避免继续累积“大历史加载”“消息流修复”“弹窗可访问性/提交门禁/自定义答案”混合 diff。

## 本轮 PLAN 2026-06-20 plan approval auto mode warning

- 目标：补强 `PlanApprovalDialog` 的 `Approve & Auto` 风险说明。CodeGraph 已核实 Rust `permission_respond_plan_approval` 接受 `target_mode: "default" / "auto" / "bypassPermissions"`，当前按钮点击 `handleApproveAuto()` 写回 `approved=true,targetMode=auto` 的协议是存在的；本轮不改协议，只让用户在点击前清楚知道 auto 会让后续操作自动放行。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `PlanApprovalDialog.tsx` / test / `useChatStore.approvePlan` / Rust `permission_respond_plan_approval` 与 cc-gui `webview/src/components/PlanApprovalDialog.tsx`，确认本轮只补 auto 模式说明与按钮可访问 label，不引入 cc-gui 的模式选择列表、倒计时、resize 或 collapse。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/PlanApprovalDialog.test.ts`，断言 label helper 有 `approveAutoHint` fallback/翻译，SSR 输出有可见 auto 风险提示，`Approve & Auto` 按钮有 `title` 与 `aria-label` 指向同一完整说明。旧实现应因 hint/aria-label 缺失失败。
- 功能点 4：实现最小展示修复。验证方式：`getPlanApprovalDialogLabels()` 新增 `approveAutoHint`；footer 在按钮下方渲染 compact warning/hint；`Approve & Auto` button 增加 `aria-label={labels.approveAutoTitle}`。不改变按钮顺序、快捷键、提交 guard、targetMode 或 Store/Rust。
- 功能点 5：同步 locale 与 Trellis 规范。验证方式：更新 `src/locales/en.json` / `zh.json` 新增 `chat.planApproval.approveAutoHint`，更新 `.trellis/spec/frontend/component-guidelines.md` 记录 auto approval 必须同时有 visible hint 和 button accessible label。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 dialog 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 plan approval auto mode warning

- 上轮进展与阻塞：上一轮已完成 `AskUserQuestionDialog` 自定义答案，代码中确实存在 `MAX_CUSTOM_ANSWER_LENGTH = 2000`、`customAnswers`、`buildAskUserQuestionAnswerPayload()` 和保持 `Record<string, string>` 的响应合同。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能展示最新”仍保持前几轮代码修复状态。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 真实 `webview/src/components/PlanApprovalDialog.tsx` 通过执行模式选择让用户先理解执行方式再批准，并带有倒计时、折叠和 resize。当前项目没有复制这些高成本流程能力，而是直接提供 `Approve` 与 `Approve & Auto`；后者虽然已写回 `approved=true,targetMode=auto`，但按钮附近此前缺少可见风险说明，用户可能不知道本次批准后后续操作会自动放行。
- 本轮差距诊断（超越 cc-gui）：本轮不引入更复杂的模式选择，也不改变协议；把 auto 风险同时放进 visible hint 和 `Approve & Auto` button 的 `title` / `aria-label`，让鼠标、键盘和辅助技术用户都能在点击前理解后果。这样保留本项目更轻量的直接决策流，同时降低误开自动模式的风险。
- 本轮规划：只改 `PlanApprovalDialog` labels/UI/locale/spec/test；不改 `useChatStore`、Rust/Tauri response、不改快捷键、不改按钮顺序、不改 `targetMode='auto'`。优先级中高：用户价值高（避免误授权后续自动放行）、出现频率中（计划审批是阻塞决策点）、实现成本低、协议回归风险低。
- 本轮完成：`src/components/chat/PlanApprovalDialog.tsx` 新增 `approveAutoHint` fallback/label，footer 渲染 amber 风险提示，并确保 `Approve & Auto` 的 `title` 与 `aria-label` 使用完整自动模式说明。`src/components/chat/PlanApprovalDialog.test.ts` 新增/更新 fallback、翻译保留和 SSR 回归，覆盖可见提示与按钮可访问名称。`src/locales/en.json` / `src/locales/zh.json` 新增 `chat.planApproval.approveAutoHint`。`.trellis/spec/frontend/component-guidelines.md` 已同步 auto approval 必须同时有 visible hint 与 button accessible label 的合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/chat/PlanApprovalDialog.test.ts` 先失败 3 项，失败点为缺少 `approveAutoHint` fallback、缺少中文翻译保留，以及 SSR 输出缺少 auto 风险提示和 `Approve & Auto` 的 `aria-label`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 12 tests）。相邻验证通过：`npm test -- src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx src/utils/dialogShortcuts.test.ts`（5 files / 49 tests）。全量前端通过：`npm test`（41 files / 426 tests）。生产构建通过：`npm run build`（`tsc && vite build`）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 PlanApproval，未手动确认 `Approve & Auto` 的 tooltip、visible hint、Enter/Esc、default/auto payload、busy state 在真实 WebView 中的表现；未用屏幕阅读器或 accessibility tree 验证播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 ToolPermission / PlanApproval / AskUserQuestion，点测 visible hint、button tooltip/aria、Enter/Esc、`1/2`、主输入复制、raw input 展开、重复提交保护和忙碌态；2) 如果真实点测仍显示 `Approve & Auto` 易误解，再评估是否引入 cc-gui 式执行模式选择，但需单独审计 `targetMode` 合同；3) 继续评估 AskUserQuestion 多问题逐步导航、collapse mini-mode、倒计时等不改变响应 payload 的体验能力；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、permission/toolblock presentation。

## 本轮 PLAN 2026-06-20 ask user question progress summary

- 目标：给 `AskUserQuestionDialog` 增加轻量问题进度与完成度提示。cc-gui 真实 `webview/src/components/AskUserQuestionDialog.tsx` 有逐题导航和 `Question current / total` 进度；本项目当前一次性渲染所有问题，虽然已经有空提交门禁和自定义答案，但多问题时用户不知道还剩几题未答，Submit 也只表现为 disabled。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `AskUserQuestionDialog.tsx` / test 与 cc-gui `webview/src/components/AskUserQuestionDialog.tsx`，确认本轮只加完成度/序号提示，不改 answer payload、不改提交 guard、不引入逐题导航、倒计时、折叠或 `string[]` 响应。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/AskUserQuestionDialog.test.ts`，新增 `countAnsweredAskUserQuestions()` helper 测试，覆盖常规答案、自定义答案、多选答案和空白答案；SSR 断言多问题弹窗显示 `0 / 2 answered`、`Question 1 of 2`、`Question 2 of 2`。旧实现应因 helper/UI 缺失失败。
- 功能点 4：实现最小展示修复。验证方式：新增完成度 helper，header 下方展示已回答数 / 总题数；多问题每题标题旁显示题号 badge；复用现有 `buildAskUserQuestionAnswerPayload()` 判断，避免 disabled 状态和完成度统计逻辑分叉。
- 功能点 5：同步 locale 与 Trellis 规范。验证方式：更新 `src/locales/en.json` / `zh.json` 新增 `chat.askUser.progressSummary` / `questionProgress`，更新 `.trellis/spec/frontend/component-guidelines.md` 记录 ask-user-question 多问题完成度提示必须不改变响应 payload。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 dialog 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 ask user question progress summary

- 上轮进展与阻塞：上一轮已完成 `PlanApprovalDialog` 的 `Approve & Auto` 风险说明，代码中确实存在 `approveAutoHint` fallback/locale、footer amber visible hint，以及 `Approve & Auto` button 的完整 `title` / `aria-label` 回归测试。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能展示最新”仍保持前几轮代码修复状态。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 真实 `webview/src/components/AskUserQuestionDialog.tsx` 通过 `currentQuestionIndex` 显示逐题流程和 `Question current / total` 进度。本项目当前一次性渲染所有问题，虽然已经有空提交门禁和自定义答案，但多问题时用户只能从 disabled Submit 推断还有未答问题，不知道完成度或每题位置。用户价值：多问题阻塞弹窗更容易扫读，减少“为什么不能提交”的认知成本。
- 本轮差距诊断（超越 cc-gui）：本轮没有引入 cc-gui 的逐题导航、倒计时、折叠或 `string[]` 响应，而是在保留全量可见问题列表的同时显示完成度和每题序号。这样用户可以跨题比较和填写，同时获得进度感；实现仍复用现有 payload helper，避免 completion 统计和提交 guard 漂移。
- 本轮规划：只改 `AskUserQuestionDialog` 的展示层、helper、locale、规范和测试；不改 `useChatStore`、Rust command、permission watcher、daemon 桥接或 `answers: Record<string, string>` 响应结构。优先级中高：用户价值中高（多问题阻塞弹窗可理解性）、出现频率中、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/AskUserQuestionDialog.tsx` 新增 `countAnsweredAskUserQuestions()`、`getAskUserQuestionProgressSummary()`、`getAskUserQuestionProgressLabel()`，header 下方显示已回答数 / 总题数，多问题时每题标题旁显示题号 badge；完成度统计复用 `buildAskUserQuestionAnswerPayload()`。`src/components/chat/AskUserQuestionDialog.test.ts` 新增 helper 单元测试和多问题 SSR 回归。`src/locales/en.json` / `src/locales/zh.json` 新增 `chat.askUser.progressSummary` / `questionProgress`。`.trellis/spec/frontend/component-guidelines.md` 已同步 ask-user-question 多问题完成度提示合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/chat/AskUserQuestionDialog.test.ts` 先失败 2 项，失败点为 `countAnsweredAskUserQuestions is not a function` 和 SSR 输出缺少 `0 / 2 answered`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 17 tests）。相邻验证通过：`npm test -- src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx src/utils/dialogShortcuts.test.ts`（5 files / 51 tests，仅 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（41 files / 428 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 AskUserQuestionDialog；未手动选择选项/输入自定义答案确认完成度从 `0 / N` 递增；未实际提交确认 daemon 读取 response file 后继续；未检查真实 WebView 中题号 badge、header 完成度、移动端换行和屏幕阅读器播报；未与 cc-gui 做运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 AskUserQuestion，点测完成度递增、普通选项、自定义答案、空选项问题、Cancel/Escape、busy 状态、dialog semantics 和实际 response file；2) 若真实多问题仍显得冗长，再评估 cc-gui 的 collapse mini-mode 或逐题导航，但必须保持 `Record<string, string>` 响应合同；3) 继续评估 PlanApproval/ToolPermission 的真实 WebView tooltip/aria/keyboard 行为；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility/progress、permission/toolblock presentation。

## 本轮 PLAN 2026-06-20 ask user question per-question status

- 目标：在 `AskUserQuestionDialog` 的多问题全列表模式里，为每题显示 `Required` / `Answered` 状态。上一轮已有 `0 / N answered` 总览和题号，但当 Submit disabled 时，用户仍需要逐题检查选项和自定义输入才能找到未答题；cc-gui 的逐题导航天然聚焦当前未完成问题，本项目保留全列表时需要更直接的未答定位。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `AskUserQuestionDialog.tsx` / test 与 cc-gui `webview/src/components/AskUserQuestionDialog.tsx`，确认本轮只加每题状态 badge，不改 answer payload、不改提交 guard、不引入逐题导航、倒计时、折叠或 `string[]` 响应。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/AskUserQuestionDialog.test.ts`，新增 `isAskUserQuestionAnswered()` helper 测试，覆盖普通选择、自定义答案、多选和空白答案；SSR 断言多问题初始状态显示 `Required` 且不显示 `Answered`。旧实现应因 helper/UI 缺失失败。
- 功能点 4：实现最小展示修复。验证方式：新增单题 answered helper，并让 `countAnsweredAskUserQuestions()` 与 UI badge 共用该 helper；多问题题号旁显示状态 badge，初始为 `Required`，答题后根据现有答案/custom answer 派生为 `Answered`。
- 功能点 5：同步 locale 与 Trellis 规范。验证方式：更新 `src/locales/en.json` / `zh.json` 新增 `chat.askUser.questionRequired` / `questionAnswered`，更新 `.trellis/spec/frontend/component-guidelines.md` 记录每题状态提示必须复用提交 guard 的答案判断。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 dialog 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 ask user question per-question status

- 上轮进展与阻塞：上一轮已完成 `AskUserQuestionDialog` 多问题完成度提示，代码中确实存在 `countAnsweredAskUserQuestions()`、`getAskUserQuestionProgressSummary()`、`getAskUserQuestionProgressLabel()`、header `0 / N answered` 与多问题 `Question X of N` badge。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能展示最新”仍保持前几轮代码修复状态。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 真实 `webview/src/components/AskUserQuestionDialog.tsx` 使用逐题导航和 `canProceed` 聚焦当前问题，用户天然知道哪一题还未完成。本项目保留全问题列表后，上一轮虽然已有 `0 / N answered` 总览和题号，但 Submit disabled 时用户仍需要逐题扫选项和自定义输入才能找到未答项。用户价值：把“为什么不能提交”定位到具体题目，降低多问题阻塞弹窗的排查成本。
- 本轮差距诊断（超越 cc-gui）：本轮没有复制 cc-gui 的逐题导航、倒计时、折叠或 `string[]` 响应，而是在保留全列表可比较优势的同时，为每题显示 `Required` / `Answered` 状态。状态由同一个 payload helper 推导，避免完成度、badge 和提交 guard 分叉。
- 本轮规划：只改 `AskUserQuestionDialog` 展示层/helper、en/zh locale、前端组件规范和测试；不改 `useChatStore`、Rust command、permission watcher、daemon 桥接或 `answers: Record<string, string>` 响应结构。优先级中高：用户价值中高（直接定位未答题）、出现频率中（多问题澄清场景）、实现成本低（纯前端展示）、回归风险低（helper 与 SSR 覆盖）。
- 本轮完成：`src/components/chat/AskUserQuestionDialog.tsx` 新增 `questionRequired` / `questionAnswered` fallback label 与 `isAskUserQuestionAnswered()`，并让 `countAnsweredAskUserQuestions()` 和多问题 UI badge 共用该 helper；多题时题号旁显示 `Required` 或 `Answered` badge。`src/components/chat/AskUserQuestionDialog.test.ts` 新增 helper 单元测试、fallback/翻译断言和多问题 SSR 回归。`src/locales/en.json` / `src/locales/zh.json` 新增 `chat.askUser.questionRequired` / `chat.askUser.questionAnswered`。`.trellis/spec/frontend/component-guidelines.md` 已同步每题状态 badge 必须复用提交 payload helper 的合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/chat/AskUserQuestionDialog.test.ts` 先失败 4 项，失败点为缺少 `questionRequired` / `questionAnswered` label、`isAskUserQuestionAnswered is not a function`、SSR 缺少 `Required`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 18 tests）。相邻验证通过：`npm test -- src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx src/utils/dialogShortcuts.test.ts`（5 files / 52 tests）。全量前端通过：`npm test`（41 files / 429 tests）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 AskUserQuestionDialog；未手动选择选项/输入自定义答案确认单题 badge 从 `Required` 切换为 `Answered`；未实际提交确认 daemon 读取 response file 后继续；未检查真实 WebView 中 badge 换行、移动端布局、焦点和屏幕阅读器播报；未与 cc-gui 做运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 AskUserQuestion，点测完成度递增、逐题 badge、普通选项、自定义答案、空选项问题、Cancel/Escape、busy 状态、dialog semantics 和实际 response file；2) 若真实多问题仍显得冗长，再评估 cc-gui 的 collapse mini-mode 或逐题导航，但必须保持 `Record<string, string>` 响应合同；3) 继续评估 PlanApproval/ToolPermission 的真实 WebView tooltip/aria/keyboard 行为；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility/progress/status、permission/toolblock presentation。

## 本轮 PLAN 2026-06-20 ask user question submit blocked hint

- 目标：在 `AskUserQuestionDialog` 中补齐 disabled Submit 的原因提示。上一轮已有 `0 / N answered` 与逐题 `Required` / `Answered`，但用户聚焦到底部 Submit 时仍只看到按钮不可点，需要回头自行关联哪个问题未答；cc-gui 逐题导航天然把“当前题未完成”放在按钮旁，本项目全列表模式需要在 footer 直接解释剩余必答题数。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `AskUserQuestionDialog.tsx` / test 与 cc-gui `webview/src/components/AskUserQuestionDialog.tsx`，确认本轮只加 submit blocked visible hint 和 button accessible label，不改 answer payload、不改提交 guard、不引入逐题导航、倒计时、折叠或 `string[]` 响应。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/AskUserQuestionDialog.test.ts`，新增 submit blocked hint fallback/translation/helper 测试；SSR 断言初始 disabled Submit 旁显示 `Answer 1 required question before submitting.`，且 Submit button 有 shortcut/blocked-aware `title` 与 `aria-label`。旧实现应因 helper/UI 缺失失败。
- 功能点 4：实现最小展示修复。验证方式：新增 `getAskUserQuestionSubmitBlockedHint()`，按剩余未答题数生成 fallback；footer 在 `!canSubmit && !submitted` 时显示 compact hint；Submit button 在 blocked 时暴露 `Submit: <hint>` 的 `title` / `aria-label`，enabled/submitting 时保持短 action label。
- 功能点 5：同步 locale 与 Trellis 规范。验证方式：更新 `src/locales/en.json` / `zh.json` 新增 `chat.askUser.submitBlockedHint`，更新 `.trellis/spec/frontend/component-guidelines.md` 记录 disabled Submit 必须提供可见原因和按钮级 accessible label，并复用已回答计数。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 dialog 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 ask user question submit blocked hint

- 上轮进展与阻塞：上一轮已完成 `AskUserQuestionDialog` 每题 `Required` / `Answered` 状态 badge，代码中确实存在 `isAskUserQuestionAnswered()`，且 `countAnsweredAskUserQuestions()` 与 UI badge 共用同一 payload helper。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能展示最新”仍保持前几轮代码修复状态。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 真实 `webview/src/components/AskUserQuestionDialog.tsx` 通过逐题导航和 `canProceed` 把未完成状态固定在当前题与下一步按钮附近。本项目保留全列表模式后，底部 Submit 只表现为 disabled；虽然上方每题已有 `Required` badge，但用户聚焦到底部按钮或使用辅助技术时，仍无法直接知道还差几题。用户价值：阻塞弹窗的失败原因在操作点就能读到，减少来回扫列表。
- 本轮差距诊断（超越 cc-gui）：本轮不引入逐题导航、倒计时、折叠或 `string[]` 响应，而是给 disabled Submit 增加可见原因和按钮级 `title` / `aria-label`。这样保留全列表填写与比较优势，同时让鼠标、键盘和辅助技术用户都能理解按钮不可用的原因。
- 本轮规划：只改 `AskUserQuestionDialog` footer 展示、helper、locale、前端组件规范和测试；不改 `canSubmitAskUserQuestionAnswers()` 语义、不改 `buildAskUserQuestionAnswerPayload()`、不改 Store/Rust/Tauri response。优先级中高：用户价值中高（解释阻塞原因）、出现频率中（多问题或空答案状态）、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/AskUserQuestionDialog.tsx` 新增 `getAskUserQuestionSubmitBlockedHint()`，按剩余未答题数生成 fallback；footer 在 `!canSubmit && !submitted` 时显示 `Answer N required question(s) before submitting.`；Submit button 在 blocked 时暴露 `Submit: <hint>` 的 `title` / `aria-label`，enabled/submitting 时保持短 action label。`src/components/chat/AskUserQuestionDialog.test.ts` 新增 fallback/translation/helper 与 SSR 回归。`src/locales/en.json` / `src/locales/zh.json` 新增 `chat.askUser.submitBlockedHint_one` / `_other`。`.trellis/spec/frontend/component-guidelines.md` 已同步 disabled Submit 必须提供可见原因和按钮级 accessible label 的合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/chat/AskUserQuestionDialog.test.ts` 先失败 3 项，失败点为 `getAskUserQuestionSubmitBlockedHint is not a function` 和 SSR 缺少 `Answer 1 required question before submitting.`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 19 tests）。相邻验证通过：`npm test -- src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx src/utils/dialogShortcuts.test.ts`（5 files / 53 tests，仅 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（41 files / 430 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 AskUserQuestionDialog；未手动确认 footer blocked hint 随选项/自定义答案消失；未用屏幕阅读器确认 Submit button 的 blocked label 播报；未检查真实 WebView 中 hint 在窄宽度、深色主题和中文复数文案下的换行表现；未与 cc-gui 做运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 AskUserQuestion，点测完成度递增、逐题 badge、Submit blocked hint、普通选项、自定义答案、空选项问题、Cancel/Escape、busy 状态、dialog semantics 和实际 response file；2) 若真实多问题仍显得冗长，再评估 cc-gui 的 collapse mini-mode 或逐题导航，但必须保持 `Record<string, string>` 响应合同；3) 继续评估 PlanApproval/ToolPermission 的真实 WebView tooltip/aria/keyboard 行为；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility/progress/status/blocked hint、permission/toolblock presentation。

## 本轮 PLAN 2026-06-20 ask user question optionless answer label

- 目标：优化 `AskUserQuestionDialog` 中无选项开放问题的自定义答案文案。上一轮已能用 blocked hint 告诉用户还差几题，但当 daemon 发送 `options: []` 的开放问题时，textarea 仍显示 `Other`，会让用户误以为存在一组选项之外的“其他”答案；真实语义其实是“直接回答”。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `AskUserQuestionDialog.tsx` / test 与 cc-gui `webview/src/components/AskUserQuestionDialog.tsx`，确认本轮只改无选项问题的 textarea label/hint，不改 payload、不改提交 guard、不引入逐题导航、倒计时、折叠或 `string[]` 响应。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/AskUserQuestionDialog.test.ts`，新增 optionless SSR 回归，断言 `options: []` 时显示 `Answer` / `Type an answer to continue.`，且不显示 `Other` / `Use this when the available options do not fit.`。旧实现应失败。
- 功能点 4：实现最小展示修复。验证方式：新增 `customAnswerRequiredLabel` / `customAnswerRequiredHint` fallback 与 locale；渲染 textarea 时根据 `q.options.length > 0` 选择 `Other` 或 `Answer` 文案；保留同一个 `customAnswers` state、2000 字符上限和 payload helper。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录有选项时叫 `Other`，无选项时叫 `Answer`，避免把开放问题误导成补充选项。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 dialog 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 ask user question optionless answer label

- 上轮进展与阻塞：上一轮已完成 `AskUserQuestionDialog` 的 Submit blocked hint，代码中确实存在 `getAskUserQuestionSubmitBlockedHint()`、footer blocked hint，以及 blocked 状态下 Submit button 的 `title` / `aria-label`。用户反馈的“大历史会话加载慢”和“AI 返回消息消失/只能展示最新”仍保持前几轮代码修复状态。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的 AskUserQuestion 对 `Other` 是“选项之外”的补充输入；本项目在 `options: []` 的开放问题里仍显示 `Other`，会让用户误以为有一组选项只是没显示。用户价值：开放问题不再被误导成“其他选项”，用户能直接理解这里要输入答案。
- 本轮差距诊断（超越 cc-gui）：本轮不改 payload，也不复制逐题导航、倒计时或折叠模式，而是按问题形态区分文案：有选项时是 `Other`，无选项时是 `Answer`。用户价值：保留本项目全列表与字符串 payload 的低风险合同，同时让开放问题语义更清楚。
- 本轮规划：只改 `AskUserQuestionDialog` label helper/render/locale/spec/test；不改 `canSubmitAskUserQuestionAnswers()`、`buildAskUserQuestionAnswerPayload()`、Store/Rust/Tauri response 或 `Record<string, string>` 响应结构。优先级中高：用户价值明确（消除开放问题误解）、出现频率中（AskUserQuestion 澄清问题可能无选项）、实现成本低、回归风险低。
- 本轮完成：`src/components/chat/AskUserQuestionDialog.tsx` 新增 `customAnswerRequiredLabel` / `customAnswerRequiredHint` fallback 与 `getAskUserQuestionCustomAnswerChrome()`，根据 `q.options.length > 0` 选择 `Other` 或 `Answer` 文案；`src/components/chat/AskUserQuestionDialog.test.ts` 新增 fallback/翻译断言和 optionless SSR 回归；`src/locales/en.json` / `src/locales/zh.json` 新增 `chat.askUser.customAnswerRequiredLabel` 与 `chat.askUser.customAnswerRequiredHint`；`.trellis/spec/frontend/component-guidelines.md` 已同步“有选项为 Other、无选项为 Answer”的组件合同；`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/chat/AskUserQuestionDialog.test.ts` 先失败 3 项，失败点为缺少 `customAnswerRequiredLabel` / `customAnswerRequiredHint`，以及 `options: []` 仍渲染 `Other` / 旧 hint，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 20 tests）。相邻 dialog 验证通过：`npm test -- src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx src/utils/dialogShortcuts.test.ts`（5 files / 54 tests）。全量前端通过：`npm test`（41 files / 431 tests）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 optionless AskUserQuestionDialog；未手动输入答案确认 label/hint、blocked hint 消失、Submit enabled；未实际提交确认 daemon 读取 response file 后继续；未检查真实 WebView/mobile/AT 播报；未与 cc-gui 做运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 ToolPermission / PlanApproval / AskUserQuestion，点测 visible hint、button tooltip/aria、Enter/Esc、`1/2`、主输入复制、raw input 展开、普通选项、自定义答案、空选项问题和 response file；2) 如果多问题仍显得冗长，再评估 cc-gui 的 collapse mini-mode 或逐题导航，但必须保持 `Record<string, string>` 响应合同；3) 继续 PlanApproval / ToolPermission 的真实 WebView tooltip、aria 和键盘行为点测；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility/progress/status/blocked hint/optionless answer label、permission/toolblock presentation。

## 本轮 PLAN 2026-06-20 ask user question per-question header tag

- 目标：补齐 `AskUserQuestionDialog` 对每题 `header` 的展示。cc-gui 真实 `webview/src/components/AskUserQuestionDialog.tsx` 在问题正文区域用 `question-tag` 显示 `currentQuestion.header`；本项目此前只把第一题 header 当作弹窗标题，多问题全列表里后续题目的短标题丢失。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `AskUserQuestionDialog.tsx` / test 与 cc-gui `webview/src/components/AskUserQuestionDialog.tsx`，确认本轮只展示每题 header tag，不改 answer payload、不改 submit guard、不引入逐题导航、倒计时、折叠或 `string[]` 响应。
- 功能点 3：补 RED 测试。验证方式：扩展 `src/components/chat/AskUserQuestionDialog.test.ts` 的多问题 SSR 回归，断言第二题 header `Confirm scope` 出现在 HTML 中。旧实现应因未渲染逐题 header 而失败。
- 功能点 4：实现最小展示修复。验证方式：在每题标题区域渲染 `q.header.trim()` 的 compact tag；空 header 不渲染；继续保留第一题 header 作为弹窗 title fallback，并保持 `aria-describedby` 仍指向第一题正文。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 question header 是 daemon-provided 上下文，应作为每题短标签展示，且不进入 answer payload。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 dialog 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 ask user question per-question header tag

- 上轮进展与阻塞：上一轮已完成 optionless AskUserQuestion 的 `Answer` 文案，代码中确实存在 `getAskUserQuestionCustomAnswerChrome()`、`customAnswerRequiredLabel` / `customAnswerRequiredHint` fallback、en/zh locale 与 SSR 回归。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮继续确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 真实 `AskUserQuestionDialog.tsx` 在问题区域渲染 `currentQuestion.header`，帮助用户理解当前问题类别。本项目多问题全列表里只显示题号和问题正文，后续题的 `header` 不可见；当 daemon 用 header 表达“确认范围 / 选择检查 / 澄清原因”等短上下文时，用户需要从长问题文本中反推分类。用户价值：多问题弹窗可以按短标签快速扫读。
- 本轮差距诊断（超越 cc-gui）：本轮不复制 cc-gui 的逐题导航或 collapse mini-mode，而是在本项目全列表模式中把每题 header 作为 compact tag 常驻展示。用户价值：保留全量问题同时可见的比较优势，又补上结构化短标题，不改变响应合同。
- 本轮规划：只改 `AskUserQuestionDialog` 展示层、对应 SSR 测试和前端组件规范；不改 `canSubmitAskUserQuestionAnswers()`、`buildAskUserQuestionAnswerPayload()`、Store/Rust/Tauri response 或 `Record<string, string>` 响应结构。优先级中高：用户价值中等（多问题扫描更清楚）、出现频率中（daemon 多问题 prompt）、实现成本低、风险低。
- 本轮完成：`src/components/chat/AskUserQuestionDialog.tsx` 在每题问题正文上方渲染非空 `q.header.trim()` 的 compact tag，空 header 跳过；`src/components/chat/AskUserQuestionDialog.test.ts` 的多问题 SSR 回归新增 `Confirm scope` 断言；`.trellis/spec/frontend/component-guidelines.md` 记录逐题 header tag 合同。未新增 locale，因为 header 是 daemon-provided 内容。
- 本轮验证：RED：`npm test -- src/components/chat/AskUserQuestionDialog.test.ts` 先失败 1 项，失败点为 SSR 输出缺少第二题 header `Confirm scope`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 20 tests）。相邻 dialog 验证通过：`npm test -- src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx src/utils/dialogShortcuts.test.ts`（5 files / 54 tests，仅 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（41 files / 431 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端编译验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发多问题 AskUserQuestionDialog；未手动确认 header tag 在窄宽度、深色主题、长 header、移动端和屏幕阅读器下的实际表现；未实际提交 response file；未与 cc-gui 做运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 ToolPermission / PlanApproval / AskUserQuestion，点测 visible hint、button tooltip/aria、Enter/Esc、`1/2`、主输入复制、raw input 展开、普通选项、自定义答案、空选项问题、逐题 header tag 和 response file；2) 如果多问题全列表仍显得冗长，再评估 cc-gui 的 collapse mini-mode 或逐题导航，但必须保持 `Record<string, string>` 响应合同；3) 继续 PlanApproval / ToolPermission 的真实 WebView tooltip、aria 和键盘行为点测；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility/progress/status/blocked hint/optionless answer/header tag、permission/toolblock presentation。

## 本轮 PLAN 2026-06-20 ask user question empty request fallback

- 目标：为 `AskUserQuestionDialog` 的 `questions: []` 边界提供可恢复的 invalid-format 状态。cc-gui 真实 `AskUserQuestionDialog.tsx` 在 normalized questions 为空时显示“问题数据格式不支持，请取消后重试”并只保留取消路径；本项目此前会显示空内容、`Answer 0 required questions before submitting.` 和 disabled Submit，用户不知道该如何恢复。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `AskUserQuestionDialog.tsx` / test 与 cc-gui `webview/src/components/AskUserQuestionDialog.tsx`，确认本轮只改空问题边界展示，不改 answer payload、不改 Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展 `AskUserQuestionDialog.test.ts`，断言 label helper 有 invalid-format fallback/translation，`questions: []` SSR 显示可读错误说明和 Cancel shortcut label，不显示 Submit 和 `Answer 0 required questions...`。旧实现应失败。
- 功能点 4：实现最小展示修复。验证方式：新增 `invalidQuestionFormat` fallback/locale；`hasQuestions=false` 时内容区显示 invalid-format message，footer 只保留 Cancel，且 `aria-describedby` 指向该 message。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录空问题请求必须是 cancel-only invalid-format 状态。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 dialog 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 ask user question empty request fallback

- 上轮进展与阻塞：上一轮已完成每题 header tag，代码中确实存在每题 `q.header.trim()` compact tag、`Confirm scope` SSR 回归和 frontend spec 记录。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮继续确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 真实 `AskUserQuestionDialog.tsx` 对无有效问题提供 invalid-format 取消路径；本项目此前会给 `questions: []` 显示空内容、`Answer 0 required questions before submitting.` 和 disabled Submit。用户价值：异常请求也有清楚恢复路径，不再把用户困在不可提交的空弹窗。
- 本轮差距诊断（超越 cc-gui）：本轮在保留本项目 dialog semantics、Esc/Cancel shortcut 与 readable i18n fallback 的前提下，把空问题请求统一为 cancel-only 状态，避免向 daemon 写入空 answer payload。用户价值：边界状态更安全、更可解释。
- 本轮规划：只改 `AskUserQuestionDialog` 空问题展示、fallback/locale、SSR 测试和前端组件规范；不改 `canSubmitAskUserQuestionAnswers()`、`buildAskUserQuestionAnswerPayload()`、Store/Rust/Tauri response 或 `Record<string, string>` 响应结构。优先级中高：用户价值明确（异常请求恢复）、出现频率低到中（协议/daemon 异常），实现成本低，风险低。
- 本轮完成：`src/components/chat/AskUserQuestionDialog.tsx` 新增 `invalidQuestionFormat` fallback label，`hasQuestions=false` 时内容区显示 invalid-format message，footer 只渲染 Cancel，不渲染 Submit 或 blocked hint，并让 `aria-describedby` 指向该 message；`src/components/chat/AskUserQuestionDialog.test.ts` 新增 label 和 SSR 回归；`src/locales/en.json` / `src/locales/zh.json` 新增 `chat.askUser.invalidQuestionFormat`；`.trellis/spec/frontend/component-guidelines.md` 记录空问题请求 cancel-only 合同。
- 本轮验证：RED：`npm test -- src/components/chat/AskUserQuestionDialog.test.ts` 先失败 3 项，失败点为 invalid-format label undefined、翻译 undefined、空问题 SSR 仍显示 `Answer 0 required questions...` 和 Submit，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 21 tests）。相邻 dialog 验证通过：`npm test -- src/components/chat/ToolPermissionDialog.test.ts src/components/chat/PlanApprovalDialog.test.ts src/components/chat/AskUserQuestionDialog.test.ts src/components/chat/SdkDependencyPanel.test.tsx src/utils/dialogShortcuts.test.ts`（5 files / 55 tests，仅 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（41 files / 432 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端编译验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 `questions: []` AskUserQuestionDialog；未手动确认 Cancel/Esc 在真实 WebView 中恢复；未实际确认 daemon 异常请求后不会收到空 answer payload；未检查真实 WebView/mobile/AT 播报；未与 cc-gui 做运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 ToolPermission / PlanApproval / AskUserQuestion，点测 visible hint、button tooltip/aria、Enter/Esc、`1/2`、主输入复制、raw input 展开、普通选项、自定义答案、空选项问题、逐题 header tag、空问题 invalid-format 和 response file；2) 如果多问题全列表仍显得冗长，再评估 cc-gui 的 collapse mini-mode 或逐题导航，但必须保持 `Record<string, string>` 响应合同；3) 继续 PlanApproval / ToolPermission 的真实 WebView tooltip、aria 和键盘行为点测；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility/progress/status/blocked hint/optionless answer/header tag/empty fallback、permission/toolblock presentation。

## 本轮 PLAN 2026-06-20 generic tool ask user question status

- 目标：修正 `GenericToolBlock` 对 `AskUserQuestion` 工具结果的状态展示。cc-gui 真实 `webview/src/components/toolBlocks/GenericToolBlock.tsx` 明确把 `AskUserQuestion` 从 `result.is_error` 错误态中排除，因为它是用户交互工具；本项目当前通用工具块会把 `is_error=true` 直接显示为 error，用户容易误以为提问流程失败。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `GenericToolBlock.tsx` / `toolPresentation.ts` 和 cc-gui `webview/src/components/toolBlocks/GenericToolBlock.tsx`，确认本轮只改展示状态，不改 `AskUserQuestionDialog`、Store/Rust/Tauri response 或 tool_result 原始数据。
- 功能点 3：补 RED 测试。验证方式：新增 `GenericToolBlock.test.tsx`，断言普通 generic 工具 `is_error=true` 仍渲染 `tool-status-indicator error`，但 `AskUserQuestion` 的 `is_error=true` 渲染为 `tool-status-indicator completed`。旧实现应失败后一项。
- 功能点 4：实现最小展示修复。验证方式：给通用工具状态计算增加“忽略 tool_result error”的可选开关；仅当工具名归一化为 `askuserquestion` / `ask_user_question` 时启用，denied 状态仍必须显示 error。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 AskUserQuestion 工具块是用户交互完成态，不得仅因 SDK 的 `is_error=true` 被标为失败。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 generic tool ask user question status

- 上轮进展与阻塞：上一轮已完成 `AskUserQuestionDialog` 空问题 invalid-format 状态，代码中确实存在 `hasQuestions=false` 时的 cancel-only 展示、`invalidQuestionFormat` fallback/locale、SSR 回归和 frontend spec 记录。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮继续确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 真实 `webview/src/components/toolBlocks/GenericToolBlock.tsx` 明确写有 `AskUserQuestion tool should never show as error - it's a user interaction tool`，即 `AskUserQuestion` 不应仅因 `tool_result.is_error=true` 显示失败。本项目 `GenericToolBlock` 此前直接使用 `getToolDisplayStatus(result, isDenied)`，会把这类用户交互完成态标成红色 error。用户价值：提问/回答流程完成后，工具时间线不会误导用户以为执行失败。
- 本轮差距诊断（超越 cc-gui）：本轮保持本项目现有 generic fallback、copy/open、结果摘要和 tool_result 原始数据不变，只把状态映射修正为“用户交互完成态”。同时保留 denied 优先级，用户主动拒绝仍显示 error。用户价值：状态解释更准确，不牺牲权限拒绝的风险提示。
- 本轮规划：只改 `GenericToolBlock` 状态判定、补 SSR 回归和前端组件规范；不改 `AskUserQuestionDialog`、`useChatStore`、Rust/Tauri response、不改 tool_result 数据或通用工具错误语义。优先级中高：用户价值明确（避免错误红态误导）、出现频率中（AskUserQuestion 是通用工具块路径）、实现成本低、风险低。
- 本轮完成：`src/components/toolBlocks/GenericToolBlock.tsx` 新增 `isAskUserQuestionTool()`，当工具名为 `AskUserQuestion` / `ask_user_question` 且已有结果、未被拒绝时渲染 `completed` 状态；普通 generic 工具 `is_error=true` 仍为 `error`。新增 `src/components/toolBlocks/GenericToolBlock.test.tsx`，覆盖普通错误态和 AskUserQuestion 完成态；该新增文件已按项目规则 `git add`。`.trellis/spec/frontend/component-guidelines.md` 记录 AskUserQuestion 工具块状态合同。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/GenericToolBlock.test.tsx` 首次因测试 mock 未隔离 `bridge` 的 i18n 初始化而报错，补 mock 后同命令失败 1 项，失败点为 `AskUserQuestion` 仍渲染 `tool-status-indicator error` 而不是 `completed`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 2 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/GenericToolBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（5 files / 44 tests，仅 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（42 files / 434 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `dist` 已确认在工作区内并清理；`out` 不存在。最终限定文件 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 AskUserQuestion tool block；未手动确认真实 transcript 中 `AskUserQuestion` 结果从红色失败态变为完成态；未用屏幕阅读器确认状态变化播报；未与 cc-gui 做运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 ToolPermission / PlanApproval / AskUserQuestion，点测 visible hint、button tooltip/aria、Enter/Esc、`1/2`、普通选项、自定义答案、空选项问题、AskUserQuestion generic tool completed 状态和 response file；2) 继续为 `BashToolBlock` / `ReadToolBlock` 补最小 SSR 覆盖，尤其是状态、copy/open 可访问 label 和路径/行号摘要；3) 审计 grouped Bash/Read/Search 对 `is_error` 的状态聚合，确认是否也存在交互工具或特殊工具误归类；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/status 修复。

## 本轮 PLAN 2026-06-20 bash read header keyboard accessibility

- 目标：让高频 `BashToolBlock` / `ReadToolBlock` 的折叠标题成为键盘和辅助技术可理解的展开控件。当前标题是带 `onClick` 的 `div`，鼠标可用，但没有 `role="button"`、`tabIndex`、`aria-expanded`，键盘用户无法聚焦并展开详情。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph 诊断。验证方式：读取本项目 `BashToolBlock.tsx` / `ReadToolBlock.tsx` / 上轮 `GenericToolBlock.tsx`，确认本轮只补单工具块 header 可访问交互，不改 tool_result、分组算法、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：新增 `BashToolBlock.test.tsx` 与 `ReadToolBlock.test.tsx`，SSR 断言 header 有 `role="button"`、`tabIndex` / `tabindex`、`aria-expanded="false"`；旧实现应失败。
- 功能点 4：实现最小交互修复。验证方式：给 Bash/Read header 增加 `role="button"`、`tabIndex={0}`、`aria-expanded={expanded}` 和 Enter/Space 键切换；保留鼠标点击行为、默认 collapsed、现有状态计算和 copy/open 行为。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录可折叠工具块 header 不得只是 click-only div，必须暴露 button 语义、键盘切换和 expanded 状态。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 bash read group item keyboard accessibility

- 上轮进展与阻塞：上一轮已完成 `EditToolBlock` 折叠 header 和 `Copy Path` 的可访问性修复，代码中确实存在 `role="button"`、`tabIndex={0}`、`aria-expanded={expanded}`、Enter/Space 切换，以及 `Copy Path` 按钮的稳定 `title` / `aria-label`；新增 `EditToolBlock.test.tsx` 已暂存。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮继续确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 和本项目都提供 group 形式让用户扫描多条 Bash/Read 工具记录。本项目 group 顶层 header 已有 button 语义，但内部每条命令/文件 row 仍是 click-only `div`，键盘用户只能展开整个 group，不能继续打开某条具体详情。用户价值：用户可以逐条打开失败命令输出或某个 read 详情，不必依赖鼠标。
- 本轮差距诊断（超越 cc-gui）：本轮没有复制 cc-gui 的列表/时间线布局，而是在本项目现有 group row 上补齐一致的 `role="button"`、`tabIndex`、`aria-expanded` 和 Enter/Space 合同。用户价值：顶层 group 和内部 item 的操作模型一致，辅助技术能解释每一层展开状态。
- 本轮规划：只改 `BashToolGroupBlock` / `ReadToolGroupBlock` 内部 item header 和对应 SSR 测试；不改分组算法、状态聚合、文件打开、复制、Store/Rust/Tauri response 或顶层 group header。优先级中高：用户价值中（group 内具体详情可键盘访问）、出现频率高（Bash/Read group 是常见长任务形态）、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/BashToolGroupBlock.tsx` 与 `src/components/toolBlocks/ReadToolGroupBlock.tsx` 为 `.task-group-item-header` 新增 `role="button"`、`tabIndex={0}`、`aria-expanded={isExpanded}` 和 Enter/Space 键切换；Read 文件名点击仍 `stopPropagation()`，不会误触发展开。新增 `src/components/toolBlocks/BashToolGroupBlock.test.tsx` 与 `src/components/toolBlocks/ReadToolGroupBlock.test.tsx` 覆盖 group 内 item row 可访问语义；两个新增测试文件已按项目规则 `git add`。`.trellis/spec/frontend/component-guidelines.md` 已同步 group 内部 item row 合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx` 先失败 2 项，失败点均为内部 `.task-group-item-header` 缺少 `role="button"` / `tabindex` / `aria-expanded`，符合旧实现缺口。GREEN 定向通过：同命令通过（2 files / 2 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（10 files / 54 tests，仅 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（47 files / 444 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `dist` 已确认在工作区内并清理；`out` 不存在。最终限定文件 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面用键盘聚焦 Bash/Read group 内部 item row；未手动确认 Enter/Space 在真实 WebView 中展开/折叠单条命令或文件详情；未用屏幕阅读器确认内部 item 的 `aria-expanded` 播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Bash/Read/Generic/Edit/Search 与 group 工具块，点测鼠标/键盘展开、copy/open、状态指示、路径/行号摘要和 AskUserQuestion generic completed 状态；2) 将 collapsible header 语义继续推广到 `SearchToolGroupBlock`、`EditToolGroupBlock`、`AgentGroupBlock` 和 `TaskExecutionBlock`，继续按组件分轮推进并补 SSR/交互测试；3) 为 group action buttons（如 Expand All / Collapse All）补稳定 `title` / `aria-label`，尤其在按钮可见文本可能变化或被图标化时；4) 审计 grouped Bash/Read/Search 对 `is_error` 的状态聚合，确认是否存在特殊工具误归类。

## 本轮 PLAN 2026-06-20 bash read group item keyboard accessibility

- 目标：把 `BashToolGroupBlock` / `ReadToolGroupBlock` 内部每个可展开的单项 header 补齐为键盘和辅助技术可理解的展开控件。上一轮已让单工具块和 group 顶层 header 可键盘操作，但 group 里的每条命令/文件 row 仍是 click-only `div`。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `BashToolGroupBlock.tsx` / `ReadToolGroupBlock.tsx` 与 cc-gui 对应 group blocks，确认本轮只补 group item header 的 `role` / `tabIndex` / `aria-expanded` / Enter/Space，不改分组算法、状态聚合、文件打开、复制、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：新增 `src/components/toolBlocks/BashToolGroupBlock.test.tsx` 与 `src/components/toolBlocks/ReadToolGroupBlock.test.tsx`，SSR 断言内部 `.task-group-item-header` 暴露 `role="button"`、`tabindex="0"`、`aria-expanded="false"`；旧实现应因属性缺失失败。
- 功能点 4：实现最小交互修复。验证方式：给 Bash/Read group item header 增加 button 语义、焦点入口、expanded 状态和 Enter/Space 切换；保留鼠标点击行为；Read 文件名点击继续 `stopPropagation()`，不触发展开。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，明确 collapsible tool-block header 规则同样适用于 group 内部可展开 item row。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 edit header keyboard accessibility

- 上轮进展与阻塞：上一轮已完成 `GenericToolBlock` 折叠 header 的键盘可达性，代码中确实存在 `role="button"`、`tabIndex={0}`、`aria-expanded` 与 Enter/Space 切换，并且无可展开内容时不暴露假按钮语义。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮继续确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 真实 `webview/src/components/toolBlocks/EditToolBlock.tsx` 提供可展开的编辑 diff 详情；本项目 `EditToolBlock` 也有同样折叠详情，但 header 仍是 click-only `div`，没有 button 语义、焦点入口或 expanded 状态。用户价值：键盘用户可以打开编辑 diff/路径详情，辅助技术用户也能理解该行是可展开控件。
- 本轮差距诊断（超越 cc-gui）：本轮没有复制 cc-gui 的额外顶部 diff/refresh 操作，而是在现有轻量 Edit 行上补齐语义化控制，并让 `Copy Path` 在可见文本短暂变成 `Copied` 时仍保留稳定 `title` / `aria-label`。用户价值：用户既能获得复制成功反馈，也不会丢失按钮目的说明。
- 本轮规划：只改 `EditToolBlock` 单工具块 header 与 `Copy Path` action、对应 SSR 测试和前端组件规范；不改 diff 解析、hover preview、open/copy 回调、分组算法、Store/Rust/Tauri response 或 group blocks。优先级中高：用户价值中（编辑工具详情可键盘访问）、出现频率中高（Edit 是核心工具块）、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/EditToolBlock.tsx` 新增 `toggleExpanded()` 和 `handleHeaderKeyDown()`，header 暴露 `role="button"`、`tabIndex={0}`、`aria-expanded={expanded}` 并支持 Enter/Space 切换；`Copy Path` 按钮新增稳定 `title` 与 `aria-label`，复制成功时仅改变可见文本为 `Copied`。新增 `src/components/toolBlocks/EditToolBlock.test.tsx` 覆盖 header 可访问语义和展开态 Copy Path 标签；该新增测试文件已按项目规则 `git add`。`.trellis/spec/frontend/component-guidelines.md` 已同步 Edit header 与 Read/Edit Copy Path 动作按钮合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/EditToolBlock.test.tsx` 先失败 2 项，失败点为旧实现缺少 `role="button"` 和 `title="tools.copyPath"`，符合本轮缺口。GREEN 定向通过：同命令通过（1 file / 2 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（8 files / 52 tests，仅 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（45 files / 442 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `dist` 已确认在工作区内并清理；`out` 不存在。最终限定文件 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面用键盘聚焦 Edit 工具块 header；未手动确认 Enter/Space 在真实 WebView 中展开/折叠 Edit diff 详情；未手动确认 Copy Path 的 tooltip、复制后 `Copied` 视觉反馈和辅助技术播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Bash/Read/Generic/Edit/Search 工具块，点测鼠标/键盘展开、copy/open、状态指示、路径/行号摘要、AskUserQuestion generic completed 状态和 response file；2) 将 collapsible header 语义继续推广到 `BashToolGroupBlock`、`ReadToolGroupBlock`、`SearchToolGroupBlock`、`EditToolGroupBlock`、`AgentGroupBlock` 和 `TaskExecutionBlock`，但继续按组件分轮推进并补 SSR/交互测试；3) 为 Generic/Edit/Group 工具块其他详情动作按钮补稳定 `title` / `aria-label` 合同；4) 审计 grouped Bash/Read/Search 对 `is_error` 的状态聚合，确认是否存在特殊工具误归类。

## 迭代记录 2026-06-20 bash read header keyboard accessibility

- 上轮进展与阻塞：上一轮已完成 `GenericToolBlock` 对 `AskUserQuestion` 用户交互工具的状态修正，代码中确实存在 `isAskUserQuestionTool()`，且 `AskUserQuestion` 有 result、未被拒绝时显示 `completed`，普通 generic 工具 `is_error=true` 仍显示 `error`。新增 `GenericToolBlock.test.tsx` 已暂存。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮继续确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的工具块对 Bash/Read 这类高频工具提供折叠/展开的详情入口；本项目当前也有折叠详情，但 `BashToolBlock` / `ReadToolBlock` 的 header 是 click-only `div`，没有 button 语义、焦点入口或 expanded 状态。用户价值：键盘用户和辅助技术用户可以识别并操作工具详情，不再只能依赖鼠标。
- 本轮差距诊断（超越 cc-gui）：本轮不改变工具块布局和协议，而是把已存在的展开交互补齐为语义化控制：`role="button"`、`tabIndex={0}`、`aria-expanded`、Enter/Space 切换。用户价值：保留现有轻量工具行，同时让交互状态在辅助技术中可解释。
- 本轮规划：只改 Bash/Read 单工具块 header 交互和 SSR 测试；不改 tool_result、分组算法、copy/open 回调、Store/Rust/Tauri response。优先级中等偏高：用户价值中（键盘可达性）、出现频率高（Bash/Read 是最常见工具块）、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/BashToolBlock.tsx` 与 `src/components/toolBlocks/ReadToolBlock.tsx` 的 header 新增 `role="button"`、`tabIndex={0}`、`aria-expanded={expanded}` 和 Enter/Space 键切换，保留鼠标 click 展开；新增 `src/components/toolBlocks/BashToolBlock.test.tsx` 与 `src/components/toolBlocks/ReadToolBlock.test.tsx` 覆盖初始 collapsed header 的可访问语义，两个新增测试文件已按项目规则 `git add`；`.trellis/spec/frontend/component-guidelines.md` 记录 collapsible tool-block header 不得只是 click-only div。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx` 先失败 2 项，失败点均为 SSR 输出缺少 `role="button"`，符合旧实现缺口。GREEN 定向通过：同命令通过（2 files / 2 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（7 files / 46 tests，仅 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（44 files / 436 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `dist` 已确认在工作区内并清理；`out` 不存在。最终限定文件 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面用键盘聚焦 Bash/Read 工具块 header；未手动确认 Enter/Space 在真实 WebView 中展开/折叠详情；未用屏幕阅读器确认 `aria-expanded` 播报；未与 cc-gui 做运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Bash/Read/Edit/Search 工具块，点测鼠标/键盘展开、copy/open、状态指示、路径/行号摘要、AskUserQuestion generic completed 状态；2) 将同样的 collapsible header 语义推广到 Generic/Edit/Agent/Task/Group 工具块，但建议按组件分轮推进并补测试；3) 为 Bash/Read copy action 补按钮级 `title` / `aria-label` 与可读 fallback；4) 审计 grouped Bash/Read/Search 对 `is_error` 的状态聚合，确认是否也存在特殊工具误归类。
## 本轮 PLAN 2026-06-20 bash read action button labels

- 目标：为高频 `BashToolBlock` / `ReadToolBlock` 的详情动作按钮补齐按钮级 `title` / `aria-label`。上一轮已让折叠 header 可键盘聚焦，但展开后的 Copy Command / Copy Output / Copy Path 仍只有可见文本，没有明确的鼠标 tooltip / 辅助技术名称合同。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `BashToolBlock.tsx` / `ReadToolBlock.tsx` / 对应测试和 cc-gui Read/Bash 相关实现，确认本轮只补按钮属性，不改 tool_result、复制/open 回调、Store/Rust/Tauri response 或折叠默认状态。
- 功能点 3：补 RED 测试。验证方式：扩展 `BashToolBlock.test.tsx` 与 `ReadToolBlock.test.tsx`，在展开态 SSR 中断言 `Copy Command`、`Copy Output`、`Copy Path` 都有 `title` 和 `aria-label`。旧实现应因属性缺失失败。
- 功能点 4：实现最小交互修复。验证方式：给 Bash/Read 详情动作按钮添加与可见动作一致的 `title` / `aria-label`；复制成功后的可见 `Copied` 状态不改变稳定的按钮语义名称。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录工具块详情动作按钮必须有稳定可访问名称和 tooltip，且复制成功态不能把语义名称降级为 `Copied`。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 bash read action button labels

- 上轮进展与阻塞：上一轮已完成 `BashToolBlock` / `ReadToolBlock` 折叠 header 的键盘可达性，代码中确实存在 `role="button"`、`tabIndex={0}`、`aria-expanded` 与 Enter/Space 切换；新增 `BashToolBlock.test.tsx` / `ReadToolBlock.test.tsx` 已暂存。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮继续确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 工具块详情入口强调可扫描的工具操作，本项目 Bash/Read 已有更直接的 Copy/Open 动作，但 `Copy Command`、`Copy Output`、`Copy Path` 此前只有按钮可见文本，缺少稳定 `title` / `aria-label`。用户价值：鼠标用户能悬停确认动作，辅助技术用户能听到明确动作名，复制成功时也不会把按钮目的误读成单纯的 `Copied`。
- 本轮差距诊断（超越 cc-gui）：本轮保留本项目比 cc-gui 更直接的复制路径/复制输出能力，并补上稳定语义标签；复制成功后的视觉反馈仍可短暂显示 `Copied`，但底层 action name 不变。用户价值：反馈和可达性不互相牺牲。
- 本轮规划：只改 Bash/Read 详情动作按钮属性、SSR 测试和前端组件规范；不改 tool_result、状态计算、复制/open 回调、Store/Rust/Tauri response 或默认折叠状态。优先级中等偏高：用户价值中（高频工具动作可发现/可读）、出现频率高（Bash/Read 最常见）、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/BashToolBlock.tsx` 为 `Copy Command` / `Copy Output` 按钮新增稳定 `title` 与 `aria-label`，并复用 label 常量，让 `Copied` 只作为可见成功反馈；`src/components/toolBlocks/ReadToolBlock.tsx` 为 `Copy Path` 按钮新增稳定 `title` 与 `aria-label`。`src/components/toolBlocks/BashToolBlock.test.tsx` / `ReadToolBlock.test.tsx` 新增展开态 SSR 回归，并用 hoisted React `useState` mock 保持测试轻量。`.trellis/spec/frontend/component-guidelines.md` 记录工具块详情动作按钮必须保留稳定可访问名称和 tooltip。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx` 首次因 ESM 不能 spy `React.useState` 暴露测试技术问题；改为 hoisted React mock 后同命令失败 2 项，失败点为 SSR 输出缺少 `title="tools.copyCommand"` / `title="tools.copyPath"`，符合旧实现缺口。GREEN 定向通过：同命令通过（2 files / 4 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（7 files / 48 tests，仅 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（44 files / 438 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `dist` 已确认在工作区内并清理；`out` 不存在。最终限定文件 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。新增测试文件已重新 `git add`。
- 未验证项：未启动真实 Tauri 桌面展开 Bash/Read 工具块；未手动确认 Copy Command / Copy Output / Copy Path 的 tooltip、复制后 `Copied` 视觉反馈和辅助技术播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Bash/Read/Edit/Search 工具块，点测鼠标/键盘展开、copy/open、状态指示、路径/行号摘要、AskUserQuestion generic completed 状态和 response file；2) 将 collapsible header 语义推广到 Generic/Edit/Agent/Task/Group 工具块，但建议按组件分轮推进并补 SSR/交互测试；3) 为 Generic/Edit/Group 工具块动作按钮补同样的稳定 `title` / `aria-label` 合同；4) 审计 grouped Bash/Read/Search 对 `is_error` 的状态聚合，确认是否存在特殊工具误归类。

## 本轮 PLAN 2026-06-20 generic header keyboard accessibility

- 目标：把 `GenericToolBlock` 的折叠 header 补齐为键盘和辅助技术可理解的展开控件。Bash/Read 已完成同类修复，但 Generic 仍是 click-only header；它覆盖 WebSearch、ToolSearch、AskUserQuestion 等所有未专用工具，缺口影响面更大。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph 诊断。验证方式：读取本项目 `GenericToolBlock.tsx` / `GenericToolBlock.test.tsx`，确认本轮只补 generic header 可访问交互，不改 tool_result 状态映射、AskUserQuestion 特例、copy/open 回调、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展 `GenericToolBlock.test.tsx`，SSR 断言有可展开内容时 header 暴露 `role="button"`、`tabIndex` / `tabindex`、`aria-expanded="false"`；同时断言无可展开内容时不暴露 button 语义。旧实现应失败第一项。
- 功能点 4：实现最小交互修复。验证方式：给有可展开内容的 Generic header 增加 `role="button"`、`tabIndex={0}`、`aria-expanded={expanded}` 和 Enter/Space 键切换；无可展开内容保持非交互 header，避免制造假按钮。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，把 collapsible tool-block header 规则明确扩展到 Generic，并注明没有详情内容的工具行不得伪装成按钮。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 generic header keyboard accessibility

- 上轮进展与阻塞：上一轮已完成 Bash/Read 详情动作按钮的稳定 `title` / `aria-label`，代码中确实存在 `copyCommandLabel`、`copyOutputLabel`、`copyPathLabel` 与展开态 SSR 回归；新增 `BashToolBlock.test.tsx` / `ReadToolBlock.test.tsx` 已暂存。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮继续确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 工具块普遍把可展开详情作为可操作入口；本项目 Bash/Read 已补 header 语义，但 `GenericToolBlock` 仍是 click-only header。Generic 覆盖 WebSearch、ToolSearch、AskUserQuestion 等未专用工具，用户价值是键盘用户和辅助技术用户也能打开这些工具的输入/输出详情。
- 本轮差距诊断（超越 cc-gui）：本轮没有盲目给所有 Generic 行加按钮语义，而是只在 `hasExpandableContent` 为真时暴露 `role="button"` / `tabIndex` / `aria-expanded`。无详情内容的工具行保持静态，避免用户聚焦到一个不能展开任何内容的假按钮。
- 本轮规划：只改 `GenericToolBlock` header 交互和 SSR 测试；不改 AskUserQuestion 状态特例、不改 tool_result、copy/open 回调、Store/Rust/Tauri response。优先级中高：用户价值中（键盘可达性）、出现频率高（所有未专用工具）、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/GenericToolBlock.tsx` 新增 `toggleExpanded()` 和 `handleHeaderKeyDown()`，有可展开内容时 header 暴露 `role="button"`、`tabIndex={0}`、`aria-expanded={expanded}` 并支持 Enter/Space 切换；无可展开内容时仍不暴露按钮语义。`src/components/toolBlocks/GenericToolBlock.test.tsx` 新增 header 可访问性 SSR 回归，覆盖可展开与不可展开两个边界；该新增测试文件已重新 `git add`。`.trellis/spec/frontend/component-guidelines.md` 已同步 Generic header 合同。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/GenericToolBlock.test.tsx` 先失败 1 项，失败点为可展开 Generic header 缺少 `role="button"`，无详情边界通过，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 4 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（7 files / 50 tests，仅 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（44 files / 440 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `dist` 已确认在工作区内并清理；`out` 不存在。最终限定文件 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面用键盘聚焦 Generic 工具块 header；未手动确认 Enter/Space 在真实 WebView 中展开/折叠 WebSearch/ToolSearch 等 generic 工具详情；未用屏幕阅读器确认 `aria-expanded` 播报；未与 cc-gui 做运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Bash/Read/Generic/Edit/Search 工具块，点测鼠标/键盘展开、copy/open、状态指示、路径/行号摘要、AskUserQuestion generic completed 状态和 response file；2) 将 collapsible header 语义继续推广到 `EditToolBlock`、`AgentGroupBlock`、`TaskExecutionBlock` 和 group blocks，但继续按组件分轮推进并补 SSR/交互测试；3) 为 Generic/Edit/Group 工具块详情动作按钮补稳定 `title` / `aria-label` 合同；4) 审计 grouped Bash/Read/Search 对 `is_error` 的状态聚合，确认是否存在特殊工具误归类。

## 本轮 PLAN 2026-06-20 edit header keyboard accessibility

- 目标：把 `EditToolBlock` 的折叠 header 补齐为键盘和辅助技术可理解的展开控件，并为详情里的 `Copy Path` 动作补稳定 `title` / `aria-label`。Bash/Read/Generic 已完成同类修复，Edit 仍是 click-only header；编辑工具又是用户最需要检查 diff 与路径的高频工具。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `EditToolBlock.tsx` 与 cc-gui `webview/src/components/toolBlocks/EditToolBlock.tsx`，确认本轮只补单个 Edit 工具块 header 与 Copy Path 语义，不改 diff 解析、hover preview、open/copy 回调、Store/Rust/Tauri response 或 group blocks。
- 功能点 3：补 RED 测试。验证方式：新增 `src/components/toolBlocks/EditToolBlock.test.tsx`，SSR 断言 Edit header 有 `role="button"`、`tabIndex` / `tabindex`、`aria-expanded="false"`，展开态 `Copy Path` 有稳定 `title` 和 `aria-label`；旧实现应因属性缺失失败。
- 功能点 4：实现最小交互修复。验证方式：给 Edit header 增加 `role="button"`、`tabIndex={0}`、`aria-expanded={expanded}` 和 Enter/Space 键切换；给 Copy Path 按钮增加稳定 label/tooltip，复制成功后的可见 `Copied` 不改变语义名称；保留点击文件名打开文件且不触发展开。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 Edit 单工具块也必须遵守 collapsible header 键盘/ARIA 合同，详情动作按钮语义标签不能被 transient copied 文案替代。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。
## 本轮 PLAN 2026-06-20 search group item keyboard accessibility

- 目标：把 `SearchToolGroupBlock` 内部每条搜索项 header 补齐为键盘和辅助技术可理解的展开控件。上一轮已完成 Bash/Read group 内部行；CodeGraph 确认 Search group 顶层 header 已有 `role/tabIndex/aria-expanded`，但内部 `.task-group-item-header` 仍是 click-only `div`。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `SearchToolGroupBlock.tsx` 与对标项目 `webview/src/components/toolBlocks/SearchToolGroupBlock.tsx`；确认本轮只补 Search group 内部行键盘语义，不改 search result parsing、file open、Generic detail 或 Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：新增 `src/components/toolBlocks/SearchToolGroupBlock.test.tsx`，SSR 断言内部 `.task-group-item-header` 暴露 `role="button"`、`tabIndex` / `tabindex`、`aria-expanded="false"`；旧实现应因属性缺失失败。
- 功能点 4：实现最小交互修复。验证方式：给 Search group 内部项 header 增加 `role="button"`、`tabIndex={0}`、`aria-expanded={isExpanded}` 和 Enter/Space 键切换；保持文件链接 `stopPropagation()` 与打开文件逻辑不变。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，把 group internal expandable row 键盘/ARIA 合同明确覆盖到搜索结果项。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 search group item keyboard accessibility

- 上轮进展与阻塞：上一轮已完成 Bash/Read group 内部可展开行的键盘可达性，代码中确实存在 `role="button"`、`tabIndex={0}`、`aria-expanded` 与 Enter/Space 切换；新增 `BashToolGroupBlock.test.tsx` / `ReadToolGroupBlock.test.tsx` 已暂存。更早的单块 Bash/Read/Generic/Edit header、详情动作按钮稳定标签、AskUserQuestion 状态/完成度、PlanApproval auto 风险提示也仍在代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 真实 `webview/src/components/toolBlocks/SearchToolGroupBlock.tsx` 提供 Search group 的折叠/展开和可扫描列表；本项目 Search group 顶层 header 已有键盘语义，但内部每个搜索项仍是 click-only `div`。用户价值：键盘用户和辅助技术用户可以进入单条搜索详情，查看匹配文件列表和原始 Generic 详情，不再只能用鼠标展开。
- 本轮差距诊断（超越 cc-gui）：本轮没有复制 cc-gui 的纯静态搜索列表，而是在本项目已有“单项可展开 + 文件可打开 + Generic 详情”的更强结构上补齐语义：每个搜索项暴露 `role="button"`、焦点入口和 `aria-expanded`，同时保留文件按钮 `stopPropagation()`。用户价值：既能快速打开首个/具体文件，又能用键盘检查每条搜索的完整输入输出。
- 本轮规划：只改 `SearchToolGroupBlock` 内部 item header、一个 SSR 测试、前端组件规范和 Trellis 记录；不改搜索结果解析、`openFile()`、`GenericToolBlock`、分组算法、Store/Rust/Tauri response。优先级中高：用户价值中（Search 是常见工具且结果需要展开查看）、出现频率中、实现成本低、回归风险低（与 Bash/Read group 同型）。
- 本轮完成：`src/components/toolBlocks/SearchToolGroupBlock.tsx` 引入 `KeyboardEvent` 类型，新增 `handleItemKeyDown()`，并给内部 `.task-group-item-header` 添加 `role="button"`、`tabIndex={0}`、`aria-expanded={isExpanded}`、Enter/Space 切换。`src/components/toolBlocks/SearchToolGroupBlock.test.tsx` 新增 SSR 回归，覆盖内部搜索项可键盘到达；新增文件已 `git add`。`.trellis/spec/frontend/component-guidelines.md` 已把 group internal expandable row 例子扩展到 Search result entries。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/SearchToolGroupBlock.test.tsx` 先失败 1 项，失败点为内部搜索项 header 缺少 `role="button"` / `tabindex` / `aria-expanded`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 1 test）。相邻验证通过：`npm test -- src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（11 files / 55 tests）。全量前端通过：`npm test`（48 files / 445 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终限定文件 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 Search group；未手动确认键盘 Tab 到内部搜索项后 Enter/Space 展开/折叠；未确认展开后 search-result file row 打开文件时不会误折叠；未用屏幕阅读器确认 `aria-expanded` 播报；未与 cc-gui 做运行并排截图。
- 下一步候选：1) `EditToolGroupBlock` 内部 edit item header 仍是 click-only `div`，适合下一轮按同样 TDD 修复，但需避开已有 `EditDiffPreview` 历史脏改；2) `AgentGroupBlock` / `TaskExecutionBlock` 顶层 header 可访问性审计，重点确认 subagent history loading 不被键盘处理改变；3) group action buttons（Expand All / Collapse All）补稳定 `title` / `aria-label`；4) 真实 Tauri desktop smoke：触发 Bash/Read/Edit/Search/Generic 工具块，点测鼠标/键盘展开、copy/open、状态指示、路径/行号摘要和 AskUserQuestion generic completed 状态。
## 本轮 PLAN 2026-06-20 edit group item keyboard accessibility

- 目标：把 `EditToolGroupBlock` 内部每个 edit item header 补齐为键盘和辅助技术可理解的展开控件。上一轮已完成 Search group 内部行；CodeGraph 确认 Edit group 顶层 header 已有 `role/tabIndex/aria-expanded`，但内部 `.task-group-item-header` 仍是 click-only `div`。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `EditToolGroupBlock.tsx` 与对标项目 `webview/src/components/toolBlocks/EditToolGroupBlock.tsx`；确认本轮只补 Edit group 内部行键盘语义，不改 edit item 收集/合并、diff preview、file open、nested EditToolBlock 或 Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：新增 `src/components/toolBlocks/EditToolGroupBlock.test.tsx`，SSR 断言内部 `.task-group-item-header` 暴露 `role="button"`、`tabIndex` / `tabindex`、`aria-expanded="false"`；旧实现应因属性缺失失败。
- 功能点 4：实现最小交互修复。验证方式：给 Edit group 内部项 header 增加 `role="button"`、`tabIndex={0}`、`aria-expanded={isExpanded}` 和 Enter/Space 键切换；保持文件路径 click `stopPropagation()`、diff hover preview 和打开文件逻辑不变。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，把 group internal expandable row 键盘/ARIA 合同明确覆盖到 Edit changed-file entries。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 edit group item keyboard accessibility

- 上轮进展与阻塞：上一轮已完成 `SearchToolGroupBlock` 内部搜索项 header 的键盘可达性，代码中确实存在 `role="button"`、`tabIndex={0}`、`aria-expanded` 与 Enter/Space 切换；新增 `SearchToolGroupBlock.test.tsx` 已暂存。更早的 Bash/Read/Edit/Generic 单块和 Bash/Read/Search group 可访问性改动也仍在代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 真实 `webview/src/components/toolBlocks/EditToolGroupBlock.tsx` 提供批量编辑文件列表，帮助用户快速扫改动文件；本项目进一步支持每个 edit item 展开到 `EditToolBlock` 和 diff preview，但内部 edit item header 仍是 click-only `div`。用户价值：键盘用户和辅助技术用户可以逐项展开检查编辑详情，不再只能用鼠标进入高风险改动细节。
- 本轮差距诊断（超越 cc-gui）：本轮没有退回 cc-gui 的静态文件列表，而是在本项目“changed-file item 可展开 + hover diff preview + 文件打开”的更强结构上补齐语义：每个 changed-file entry 暴露 `role="button"`、焦点入口和 `aria-expanded`，同时保留文件路径 click `stopPropagation()`。用户价值：既能快速打开文件，也能用键盘检查每个文件的完整编辑输入/输出。
- 本轮规划：只改 `EditToolGroupBlock` 内部 item header、一个 SSR 测试、前端组件规范和 Trellis 记录；不改 `collectEditToolItems()`、`mergeEditToolItemsByFile()`、`EditDiffPreview`、`openFile()`、nested `EditToolBlock`、分组算法、Store/Rust/Tauri response。优先级中高：用户价值高（编辑工具需要可审查）、出现频率中、实现成本低、回归风险低（与 Bash/Read/Search group 同型）。
- 本轮完成：`src/components/toolBlocks/EditToolGroupBlock.tsx` 引入 `KeyboardEvent` 类型，新增 `handleItemKeyDown()`，并给内部 `.task-group-item-header` 添加 `role="button"`、`tabIndex={0}`、`aria-expanded={isExpanded}`、Enter/Space 切换。`src/components/toolBlocks/EditToolGroupBlock.test.tsx` 新增 SSR 回归，覆盖内部 edit row 可键盘到达；新增文件已 `git add`。`.trellis/spec/frontend/component-guidelines.md` 已把 group internal expandable row 例子扩展到 Edit changed-file entries。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/EditToolGroupBlock.test.tsx` 先失败 1 项，失败点为内部 edit item header 缺少 `role="button"` / `tabindex` / `aria-expanded`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 1 test）。相邻验证通过：`npm test -- src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（12 files / 56 tests）。全量前端通过：`npm test`（49 files / 446 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终限定文件 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 Edit group；未手动确认键盘 Tab 到内部 changed-file row 后 Enter/Space 展开/折叠；未确认点击文件路径打开文件时不会误折叠；未检查 hover diff preview 与焦点样式在真实 WebView 中的共存表现；未用屏幕阅读器确认 `aria-expanded` 播报；未与 cc-gui 做运行并排截图。
- 下一步候选：1) 审计 `AgentGroupBlock` / `TaskExecutionBlock` 顶层 header 可访问性，重点确认 subagent history loading 不被键盘处理改变；2) group action buttons（Expand All / Collapse All）补稳定 `title` / `aria-label`；3) 真实 Tauri desktop smoke：触发 Bash/Read/Edit/Search/Generic 工具块，点测鼠标/键盘展开、copy/open、状态指示、路径/行号摘要、AskUserQuestion generic completed 状态；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility。

## 本轮 PLAN 2026-06-20 task execution header keyboard accessibility

- 目标：把 `TaskExecutionBlock` 顶层折叠 header 补齐为键盘和辅助技术可理解的展开控件。当前 header 是 click-only `div`，鼠标可展开 Task / 子代理详情，但键盘用户无法聚焦并触发同一条展开路径。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `TaskExecutionBlock.tsx` / `AgentGroupBlock.tsx` 与对标项目 `TaskExecutionBlock.tsx` / `AgentGroupBlock.tsx`；确认本轮只补 Task header 键盘语义，不改 `SubagentHistoryPanel`、history loading、polling、tool result、分组算法、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：新增 `src/components/toolBlocks/TaskExecutionBlock.test.tsx`，SSR 断言 `.task-header` 暴露 `role="button"`、`tabIndex` / `tabindex`、`aria-expanded="false"`；旧实现应因属性缺失失败。
- 功能点 4：实现最小交互修复。验证方式：给 Task header 增加 `role="button"`、`tabIndex={0}`、`aria-expanded={expanded}` 和 Enter/Space 键切换；鼠标点击仍走同一 toggle，展开后仍只通过 `expanded` 驱动 `SubagentHistoryPanel enabled`。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，明确 Task/Agent 顶层工具 header 也适用 collapsible header 键盘/ARIA 合同。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 task execution header keyboard accessibility

- 上轮进展与阻塞：上一轮已完成 `EditToolGroupBlock` 内部 changed-file row 的键盘可达性，代码中确实存在 `role="button"`、`tabIndex={0}`、`aria-expanded` 与 Enter/Space 切换；新增 `EditToolGroupBlock.test.tsx` 已暂存。更早的 Bash/Read/Edit/Generic 单块和 Bash/Read/Search/Edit group 可访问性改动也仍在代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 真实 `webview/src/components/toolBlocks/AgentGroupBlock.tsx` 已给子代理 group header 暴露 `role="button"`、`tabIndex`、`aria-expanded` 和 Enter/Space；本项目 `TaskExecutionBlock.tsx` 与 `AgentGroupBlock.tsx` 顶层 header 仍是 click-only `div`。用户价值：键盘用户和辅助技术用户需要能展开 Task / 子代理详情，查看 prompt、模型、agent id 和历史加载入口。
- 本轮差距诊断（超越 cc-gui）：cc-gui 的 `TaskExecutionBlock.tsx` 本身仍缺少同类键盘语义；本轮先在本项目 Task header 上补齐可聚焦 button 合同，并保持鼠标和键盘共用同一个 `expanded` 状态路径。用户价值：不会出现鼠标能触发 history panel、键盘触发另一套逻辑或无法触发的分裂体验。
- 本轮规划：只改 `TaskExecutionBlock` 顶层 header、一个 SSR 测试、前端组件规范和 Trellis 记录；不改 `SubagentHistoryPanel`、history loading / polling、`extractAgentToolMeta()`、`getAgentToolExtraParams()`、tool result、分组算法、Store/Rust/Tauri response。优先级中高：用户价值中高（Task/子代理详情需要可审查）、出现频率中、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/TaskExecutionBlock.tsx` 引入 `KeyboardEvent` 类型，新增共享 `toggleExpanded()` 与 `handleHeaderKeyDown()`，并给 `.task-header` 添加 `role="button"`、`tabIndex={0}`、`aria-expanded={expanded}`、Enter/Space 切换；`SubagentHistoryPanel enabled={expanded}` 仍由同一状态驱动。新增 `src/components/toolBlocks/TaskExecutionBlock.test.tsx`，SSR 覆盖折叠态 header 可键盘到达；新增文件已 `git add`。`.trellis/spec/frontend/component-guidelines.md` 已把 Task/Agent 顶层工具 header 纳入 collapsible header 键盘/ARIA 合同。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/TaskExecutionBlock.test.tsx` 先失败 1 项，失败点为 `.task-header` 缺少 `role="button"` / `tabindex` / `aria-expanded`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 1 test）。相邻验证通过：`npm test -- src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（13 files / 57 tests）。全量前端通过：`npm test`（50 files / 447 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终限定文件 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 TaskExecutionBlock；未手动确认键盘 Tab 到 Task header 后 Enter/Space 展开/折叠；未确认展开后 `SubagentHistoryPanel` 在真实 WebView 中加载历史的时序；未用屏幕阅读器确认 `aria-expanded` 播报；未与 cc-gui 做运行并排截图。
- 下一步候选：1) `AgentGroupBlock` 顶层 header 仍是 click-only `div`，适合下一轮按同样 TDD 修复，并继续保持 history loading 只由 `expanded` 驱动；2) group action buttons（Expand All / Collapse All）补稳定 `title` / `aria-label`；3) 真实 Tauri desktop smoke：触发 Bash/Read/Edit/Search/Generic/Task 工具块，点测鼠标/键盘展开、copy/open、状态指示、路径/行号摘要、AskUserQuestion generic completed 状态和 Task history panel；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility。

## 本轮 PLAN 2026-06-20 agent group header keyboard accessibility

- 目标：把 `AgentGroupBlock` 顶层折叠 header 补齐为键盘和辅助技术可理解的展开控件。上一轮已完成 `TaskExecutionBlock`，但 Agent header 仍是 click-only `div`，键盘用户无法聚焦并展开 agent prompt/meta/history 详情。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `AgentGroupBlock.tsx` / `TaskExecutionBlock.tsx` 与对标项目 `AgentGroupBlock.tsx`；确认本轮只补 Agent header 键盘语义，不改 `SubagentHistoryPanel`、history loading、tool result、分组算法、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：新增 `src/components/toolBlocks/AgentGroupBlock.test.tsx`，SSR 断言 `.task-header` 暴露 `role="button"`、`tabIndex` / `tabindex`、`aria-expanded="false"`；旧实现应因属性缺失失败。
- 功能点 4：实现最小交互修复。验证方式：给 Agent header 增加 `role="button"`、`tabIndex={0}`、`aria-expanded={expanded}` 和 Enter/Space 键切换；鼠标点击仍走同一 toggle，展开后仍只通过 `expanded` 驱动 `SubagentHistoryPanel enabled`。
- 功能点 5：复核 Trellis 规范。验证方式：`.trellis/spec/frontend/component-guidelines.md` 已在上一轮记录 Task/Agent 顶层工具 header 合同，本轮只在必要时补充，不重复扩写。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 agent group header keyboard accessibility

- 上轮进展与阻塞：上一轮已完成 `TaskExecutionBlock` 顶层 header 的键盘可达性，代码中确实存在 `role="button"`、`tabIndex={0}`、`aria-expanded` 与 Enter/Space 切换；新增 `TaskExecutionBlock.test.tsx` 已暂存。更早的 Bash/Read/Edit/Generic 单块和 Bash/Read/Search/Edit group 可访问性改动也仍在代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 真实 `webview/src/components/toolBlocks/AgentGroupBlock.tsx` 已给子代理 group header 暴露 `role="button"`、`tabIndex`、`aria-expanded` 和 Enter/Space；本项目 `AgentGroupBlock.tsx` 顶层 header 仍是 click-only `div`。用户价值：键盘用户和辅助技术用户可以展开 agent prompt、模型、agent id 和历史加载入口，不必依赖鼠标。
- 本轮差距诊断（超越 cc-gui）：本项目 `AgentGroupBlock` 与 `TaskExecutionBlock` 已经共享 `extractAgentToolMeta()`、`summarizeAgentToolHeader()` 和 `SubagentHistoryPanel` 的更统一展示模型；本轮让 Agent header 也复用同一 `expanded` 状态路径，避免同类 agent/task 工具出现不同的键盘模型。用户价值：Task 和 Agent 的展开操作一致，学习成本更低，也降低历史加载入口不可达的风险。
- 本轮规划：只改 `AgentGroupBlock` 顶层 header、一个 SSR 测试和 Trellis 记录；不改 `.trellis/spec/frontend/component-guidelines.md`（上一轮已覆盖 Task/Agent 合同）、不改 `SubagentHistoryPanel`、history loading、`extractAgentToolMeta()`、`summarizeAgentToolHeader()`、tool result、分组算法、Store/Rust/Tauri response。优先级中高：用户价值中高（agent 详情需要可审查）、出现频率中、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/AgentGroupBlock.tsx` 引入 `KeyboardEvent` 类型，新增共享 `toggleExpanded()` 与 `handleHeaderKeyDown()`，并给 `.task-header` 添加 `role="button"`、`tabIndex={0}`、`aria-expanded={expanded}`、Enter/Space 切换；`SubagentHistoryPanel enabled={expanded}` 仍由同一状态驱动。新增 `src/components/toolBlocks/AgentGroupBlock.test.tsx`，SSR 覆盖折叠态 header 可键盘到达；新增文件已 `git add`。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/AgentGroupBlock.test.tsx` 先失败 1 项，失败点为 `.task-header` 缺少 `role="button"` / `tabindex` / `aria-expanded`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 1 test）。相邻验证通过：`npm test -- src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（14 files / 58 tests）。全量前端通过：`npm test`（51 files / 448 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终限定文件 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 AgentGroupBlock；未手动确认键盘 Tab 到 Agent header 后 Enter/Space 展开/折叠；未确认展开后 `SubagentHistoryPanel` 在真实 WebView 中加载历史的时序；未用屏幕阅读器确认 `aria-expanded` 播报；未与 cc-gui 做运行并排截图。
- 下一步候选：1) group action buttons（Expand All / Collapse All）补稳定 `title` / `aria-label`，尤其是 Bash/Read/Edit/Search group 的批量控制；2) 真实 Tauri desktop smoke：触发 Bash/Read/Edit/Search/Generic/Task/Agent 工具块，点测鼠标/键盘展开、copy/open、状态指示、路径/行号摘要、AskUserQuestion generic completed 状态和 subagent history panel；3) 对 ToolBlock 可访问性做一次运行态扫描，核对 `role/button/tabindex/aria-expanded` 是否覆盖所有可展开入口；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility。

## 本轮 PLAN 2026-06-20 group action button accessible names

- 目标：给 Bash/Read/Edit/Search group 的批量 `Expand All` / `Collapse All` 操作补稳定 `title` / `aria-label`。当前按钮只有可见文本；当视觉样式压缩、被图标化或用户用辅助技术聚焦按钮时，控件目的不够稳固。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `BashToolGroupBlock.tsx` / `ReadToolGroupBlock.tsx` / `EditToolGroupBlock.tsx` / `SearchToolGroupBlock.tsx` 和已有测试；确认本轮只补批量按钮的稳定可访问名称，不改展开集合状态、单项 header、分组算法、文件打开、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展四个 group 测试，SSR 断言 `Expand All` / `Collapse All` 按钮同时有 `title` 和 `aria-label`；旧实现应因属性缺失失败。
- 功能点 4：实现最小交互修复。验证方式：给四个 group 操作按钮添加 `title={t('tools.expandAll')}` / `aria-label={t('tools.expandAll')}` 和对应 collapse 属性；保留现有 visible text、`onClick={() => toggleAll(...)}` 和样式。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 group 批量展开/折叠按钮必须保持稳定 `title` / `aria-label`。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 group action button accessible names

- 上轮进展与阻塞：上一轮已完成 `AgentGroupBlock` 顶层 header 的键盘可达性，代码中确实存在 `role="button"`、`tabIndex={0}`、`aria-expanded` 与 Enter/Space 切换；新增 `AgentGroupBlock.test.tsx` 已暂存。更早的 Bash/Read/Edit/Generic 单块、Bash/Read/Search/Edit group 内部 row、Task header 可访问性改动也仍在代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：本项目 Bash/Read/Edit/Search group 的批量 `Expand All` / `Collapse All` 按钮只有可见文本，缺少稳定 tooltip 和 focused accessible name；这在按钮未来压缩或图标化时会弱化控件目的。用户价值：键盘焦点、鼠标 tooltip 和辅助技术都能稳定读出批量展开/收起动作。
- 本轮差距诊断（超越 cc-gui）：本轮不改变现有 group 操作位置和文案，只把可见操作名同步到 `title` 与 `aria-label`。用户价值：保持当前扫描效率，同时提前锁住 compact / icon-only 演进时的可访问性边界。
- 本轮规划：只改 `BashToolGroupBlock` / `ReadToolGroupBlock` / `EditToolGroupBlock` / `SearchToolGroupBlock` 的批量操作按钮、四个 SSR 测试、前端组件规范和 Trellis 记录；不改展开集合状态、单项 header、分组算法、文件打开、复制、Store/Rust/Tauri response。优先级中高：用户价值明确、出现频率高、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/BashToolGroupBlock.tsx`、`src/components/toolBlocks/ReadToolGroupBlock.tsx`、`src/components/toolBlocks/EditToolGroupBlock.tsx`、`src/components/toolBlocks/SearchToolGroupBlock.tsx` 为 `Expand All` / `Collapse All` 按钮新增 `title={t('tools.expandAll')}` / `aria-label={t('tools.expandAll')}` 与对应 collapse 属性；四个 group 测试文件新增批量按钮 stable labels/tooltips SSR 断言；`.trellis/spec/frontend/component-guidelines.md` 已记录 grouped tool-block bulk action button 合同。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx` 先失败 4 项，失败点均为批量按钮缺少 `title` / `aria-label`，符合旧实现缺口。GREEN 定向通过：同命令通过（4 files / 8 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts` 通过（14 files / 62 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（51 files / 452 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终限定文件 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 Bash/Read/Edit/Search grouped tool blocks；未手动确认鼠标 hover tooltip、键盘焦点和辅助技术名称在真实 WebView 中一致；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Bash/Read/Edit/Search/Generic/Task/Agent 工具块，点测鼠标/键盘展开、批量 Expand/Collapse、copy/open、状态指示、路径/行号摘要、AskUserQuestion generic completed 状态和 subagent history panel；2) 对 ToolBlock 可访问性做一次运行态扫描，核对所有可展开入口和操作按钮的 `role/button/tabindex/aria-expanded/title/aria-label` 覆盖；3) 审计 grouped Bash/Read/Search 的状态聚合和特殊工具归类，确认是否存在更多误导性 error/pending 状态；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility。

## 本轮 PLAN 2026-06-20 search result open file accessible names

- 目标：给 `SearchToolGroupBlock` 中直接触发 `openFile()` 的搜索命中文件按钮补明确动作语义。当前这些按钮的可访问名称主要来自路径文本或 `title={file.path}`，辅助技术用户只听到路径，不知道该按钮会打开文件。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `SearchToolGroupBlock.tsx` / 测试与对标项目 `SearchToolGroupBlock.tsx`；确认本轮只补搜索文件打开按钮的 `title` / `aria-label`，不改搜索摘要、结果解析、展开集合状态、分组算法、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展 `SearchToolGroupBlock.test.tsx`，SSR 断言首个搜索命中文件按钮有 `title="tools.openFile: src/App.tsx"` 与 `aria-label="tools.openFile: src/App.tsx"`；旧实现应因只输出 `title="src/App.tsx"` 且缺少 `aria-label` 失败。
- 功能点 4：实现最小交互修复。验证方式：给 header 内首个命中文件按钮和展开结果列表中的每个文件按钮添加 `Open file: path` 形式的稳定 `title` / `aria-label`；保留 visible path、`stopPropagation()`、`openFile(path, lineStart, currentCwd)` 与样式。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录搜索结果的文件打开按钮必须把动作和目标文件同时暴露在 `title` / `aria-label` 中。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 search result open file accessible names

- 上轮进展与阻塞：上一轮已完成 Bash/Read/Edit/Search group 批量 `Expand All` / `Collapse All` 按钮的稳定 `title` / `aria-label`，代码中确实存在对应属性，四个 group 测试也已覆盖。更早的 Bash/Read/Edit/Generic 单块、Bash/Read/Search/Edit group 内部 row、Task/Agent header 可访问性改动也仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui Search group 主要展示搜索项列表，不提供本项目这种直接打开搜索命中文件的按钮；本项目新增能力更强，但按钮此前只读成文件路径或 `title={file.path}`，缺少“打开文件”的动作语义。用户价值：辅助技术用户和鼠标 hover 用户能明确知道该路径按钮会打开文件，而不是普通文本或展开行。
- 本轮差距诊断（超越 cc-gui）：保留本项目比 cc-gui 更直接的搜索命中文件跳转能力，并把 action + target 合并进 `title` / `aria-label`，例如 `Open file: src/App.tsx`。用户价值：搜索结果扫描仍然紧凑，操作含义更明确，减少误触和认知负担。
- 本轮规划：只改 `SearchToolGroupBlock` 的搜索文件打开按钮、一个 SSR 回归、前端组件规范和 Trellis 记录；不改搜索摘要、结果解析、展开集合状态、分组算法、Store/Rust/Tauri response。优先级中高：用户价值明确、出现频率中高（搜索结果文件跳转常见）、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/SearchToolGroupBlock.tsx` 新增 `formatOpenFileLabel()`，让 header 内首个命中文件按钮和展开结果列表中的每个文件按钮都使用 `title` / `aria-label` 的 `Open file: path` 语义；保留 visible path、`stopPropagation()`、`openFile(path, lineStart, currentCwd)` 与原样式。`src/components/toolBlocks/SearchToolGroupBlock.test.tsx` 新增 SSR 断言，覆盖首个命中文件按钮的动作+目标标签。`.trellis/spec/frontend/component-guidelines.md` 记录搜索结果文件打开按钮合同。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/SearchToolGroupBlock.test.tsx` 先失败 1 项，失败点为首个搜索命中文件按钮只有 `title="src/App.tsx"` 且缺少 `aria-label`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 3 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts` 通过（14 files / 63 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（51 files / 453 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终限定文件 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 Search group；未手动确认首个命中文件按钮和展开结果列表文件按钮的 tooltip、键盘焦点名称和辅助技术播报；未实际点击确认仍按正确行号打开文件；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Search group，点测首行命中文件与展开结果列表文件按钮的 tooltip、键盘焦点、点击打开路径/行号；2) 继续对 ToolBlock 可访问性做运行态扫描，尤其是 `GenericToolBlock` 详情区的 Copy/Open 按钮是否也需要 `title` / `aria-label`；3) 审计 grouped Bash/Read/Search 的状态聚合和特殊工具归类，确认是否存在更多误导性 error/pending 状态；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility。

## 本轮 PLAN 2026-06-20 generic action button accessible names

- 目标：给 `GenericToolBlock` 详情区的 `Copy Input` / `Copy Output` 按钮补稳定 `title` / `aria-label`。当前按钮只有可见文本，复制成功后还会短暂变成 `Copied`，辅助技术用户会丢失原始动作含义。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `GenericToolBlock.tsx` / `GenericToolBlock.test.tsx` 与对标项目 Generic tool 相关实现；确认本轮只补详情复制按钮的稳定可访问名称，不改 header、AskUserQuestion 状态特例、tool_result、copy/open 回调、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展 `GenericToolBlock.test.tsx`，用 hoisted React `useState` mock 展开详情，SSR 断言 `Copy Input` / `Copy Output` 同时有 `title` 和 `aria-label`；旧实现应因属性缺失失败。
- 功能点 4：实现最小交互修复。验证方式：给 Generic 详情按钮添加 `copyInputLabel` / `copyOutputLabel` 并用于 `title` / `aria-label`；复制成功后的可见 `Copied` 不改变底层语义名称；保留现有 copied 状态、按钮样式和复制内容。
- 功能点 5：复核 Trellis 规范。验证方式：`.trellis/spec/frontend/component-guidelines.md` 已有工具块详情动作按钮稳定标签合同，本轮按需补充 Generic 例子，不重复大段扩写。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 generic action button accessible names

- 上轮进展与阻塞：上一轮已完成 `SearchToolGroupBlock` 搜索命中文件打开按钮的动作+目标 `title` / `aria-label`，代码中确实存在 `formatOpenFileLabel()`，并覆盖首个命中文件按钮与展开列表文件按钮；新增测试已暂存。更早的 Bash/Read/Edit/Generic 单块、Bash/Read/Search/Edit group 内部 row、Task/Agent header、group bulk actions 可访问性改动也仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 工具块详情区强调可扫描的工具输入/输出；本项目 Generic 详情区已有 `Copy Input` / `Copy Output`，但按钮此前只有可见文本，复制成功后会变成 `Copied`。用户价值：复制反馈不会覆盖原动作语义，辅助技术用户和鼠标 hover 用户始终知道按钮是复制输入还是复制输出。
- 本轮差距诊断（超越 cc-gui）：Generic 覆盖 WebSearch、ToolSearch、AskUserQuestion 以外的所有兜底工具，本轮在不改布局和协议的前提下补稳定 action name。用户价值：未知工具也能保持和专用 Bash/Read/Edit 工具一致的可访问操作模型。
- 本轮规划：只改 `GenericToolBlock` 详情复制按钮、一个 SSR 回归和 Trellis 记录；不改 header、AskUserQuestion 状态特例、tool_result、copy/open 回调、Store/Rust/Tauri response。优先级中高：用户价值明确、出现频率高（Generic 是兜底路径）、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/GenericToolBlock.tsx` 新增 `copyInputLabel` / `copyOutputLabel`，并把它们用于 Copy Input / Copy Output 按钮的 `title` 和 `aria-label`；复制成功后的可见 `Copied` 只作为视觉反馈，底层语义名称保持不变。`src/components/toolBlocks/GenericToolBlock.test.tsx` 增加 hoisted React `useState` mock 以展开详情，并新增 SSR 断言覆盖两个复制按钮的稳定标签。`.trellis/spec/frontend/component-guidelines.md` 已有工具块详情动作按钮稳定标签合同，本轮未重复扩写。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/GenericToolBlock.test.tsx` 先失败 1 项，失败点为 Copy Input / Copy Output 按钮缺少 `title` / `aria-label`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 5 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts` 通过（14 files / 64 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（51 files / 454 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。最终限定文件 `git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面展开 Generic 工具块；未手动确认 Copy Input / Copy Output 的 tooltip、复制后 `Copied` 视觉反馈和辅助技术播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Generic/WebSearch/ToolSearch 类工具，点测 Copy Input / Copy Output 的 tooltip、键盘焦点和复制反馈；2) 继续对 ToolBlock 可访问性做运行态扫描，特别是 Generic header 内可点击文件路径仍是 `span`，是否需要调整成真正 button 或补更明确的 action 标签；3) 审计 grouped Bash/Read/Search 的状态聚合和特殊工具归类，确认是否存在更多误导性 error/pending 状态；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility。

## 本轮 PLAN 2026-06-20 generic file target button accessibility

- 目标：把 `GenericToolBlock` header 内可打开文件的目标路径从 click-only `span` 收敛为语义化文件打开控件。上一轮已完成 Generic 详情区复制按钮的稳定标签，但 header 文件目标仍只能鼠标点击，键盘用户和辅助技术用户无法明确识别其“打开文件”动作。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `GenericToolBlock.tsx` / `GenericToolBlock.test.tsx` 与对标项目 Generic tool 相关实现，确认本轮只补 header 文件目标打开控件，不改 header 展开状态、AskUserQuestion 状态特例、tool_result、复制回调、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展 `GenericToolBlock.test.tsx`，SSR 断言文件目标渲染为原生 `button type="button"`，真实文案具有 `Open file: src/App.tsx` 的 action + target 语义（单测 i18n mock 下为 `tools.openFile: src/App.tsx`）；旧实现应因仍是 `span` 且标签为裸路径失败。
- 功能点 4：实现最小交互修复。验证方式：给 Generic header 文件目标添加 `openFileLabel`，文件目标使用 `button` 保持原 class、可见路径、`stopPropagation()` 和 `openFile(target.openPath, lineStart, lineEnd, currentCwd)`；目录或非文件目标继续保持非交互文本。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录任何调用 `openFile()` 的工具块文件目标都必须是语义化控件，并暴露 action + target 的 `title` / `aria-label`。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 generic file target button accessibility

- 上轮进展与阻塞：上一轮已完成 `GenericToolBlock` 详情区 `Copy Input` / `Copy Output` 按钮的稳定 `title` / `aria-label`，代码中确实存在 `copyInputLabel` / `copyOutputLabel` 并覆盖两个按钮；新增/扩展测试仍在当前代码中存在。更早的 Bash/Read/Edit/Generic 单块、Bash/Read/Search/Edit group 内部 row、Task/Agent header、group bulk actions、Search 文件打开按钮可访问性改动也仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `GenericToolBlock` 也提供 Generic header 文件路径点击打开能力；本项目此前同样能点击打开，但实现为 click-only `span`，没有原生按钮语义、焦点入口或动作化名称。用户价值：键盘用户可以聚焦并触发 Generic 文件目标，辅助技术用户能听到这是打开文件而不是普通路径文本。
- 本轮差距诊断（超越 cc-gui）：本项目已经在 `SearchToolGroupBlock` 上建立 `Open file: path` 的 action + target 标签合同，本轮把同一合同扩展到 Generic header 文件目标，并用原生 button 替代模拟控件。用户价值：所有工具块中的文件打开入口具备一致的操作语义，减少误触和理解成本。
- 本轮规划：只改 `GenericToolBlock` header 文件目标、一个 SSR 回归、轻量 CSS reset、前端组件规范和 Trellis 记录；不改 Generic header 展开状态、AskUserQuestion 状态特例、tool_result 展示、copy 回调、Store/Rust/Tauri response。优先级中高：用户价值明确、出现频率中（Generic 覆盖兜底工具和多种文件目标）、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/GenericToolBlock.tsx` 为文件目标新增 `openFileLabel`，文件目标渲染为 `<button type="button">`，保留可见路径、原路径链接 class、`event.stopPropagation()` 和 `openFile(target.openPath, target.lineStart, target.lineEnd, currentCwd)`；非文件目标继续保持普通 summary 文本。`src/components/toolBlocks/GenericToolBlock.test.tsx` 新增 SSR 断言，覆盖 `<button>`、`type="button"`、`title` / `aria-label` 的 `tools.openFile: src/App.tsx` action + target 标签。`src/styles/toolBlocks.css` 新增 `.file-path-button` reset，避免原生按钮边框/背景/字体破坏工具行布局。`.trellis/spec/frontend/component-guidelines.md` 新增任何调用 `openFile()` 的工具块 header 文件目标必须是语义化控件的合同。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/GenericToolBlock.test.tsx` 先失败 1 项，失败点为文件目标仍输出 `<span class="tool-title-summary file-path-link clickable-file" title="src/App.tsx">src/App.tsx</span>`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 6 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts` 通过（14 files / 65 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（51 files / 455 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发带文件目标的 Generic 工具块；未手动确认文件目标按钮的键盘焦点、tooltip、点击打开路径/行号和辅助技术播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Generic/WebSearch/ToolSearch/Read/Edit/Search 类工具，点测文件目标按钮、Search 文件按钮、Copy Input/Output、Copy Path 的 tooltip、键盘焦点和点击路径/行号；2) 继续运行态扫描 ToolBlock 可访问性，重点检查 `ReadToolBlock`、`EditToolBlock`、Read/Edit group 内部文件路径是否仍有 click-only span 需要同样 button 化；3) 审计 grouped Bash/Read/Search 的状态聚合和特殊工具归类，确认是否存在更多误导性 error/pending 状态；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility。

## 本轮 PLAN 2026-06-20 read header file target button accessibility

- 目标：把 `ReadToolBlock` header 内可打开文件的路径目标从 click-only `span` 收敛为语义化文件打开控件。上一轮已完成 Generic header 文件目标 button 化，Read 仍是高频工具里最常见的文件路径入口。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ReadToolBlock.tsx` / `ReadToolBlock.test.tsx` 与对标项目 `ReadToolBlock.tsx`；确认本轮只补 header 文件路径打开控件，不改详情 `Open File` / `Copy Path`、状态计算、line info、Store/Rust/Tauri response 或分组组件。
- 功能点 3：补 RED 测试。验证方式：扩展 `ReadToolBlock.test.tsx`，SSR 断言 header 文件路径渲染为原生 `button type="button"`，真实文案具有 `Open file: src/components/toolBlocks/ReadToolBlock.tsx` 的 action + target 语义（单测 i18n mock 下为 `tools.openFile: ...`）；旧实现应因仍是 `span` 且标签为裸路径失败。
- 功能点 4：实现最小交互修复。验证方式：给 Read header 文件目标添加 `openFileLabel`，文件目标使用 `button` 保持原 class、图标、可见路径、`stopPropagation()` 和 `openFile(target.openPath, lineInfo.start, lineInfo.end, currentCwd)`；目录或非文件目标继续保持非交互文本。
- 功能点 5：复核 Trellis 规范。验证方式：`.trellis/spec/frontend/component-guidelines.md` 已有任何调用 `openFile()` 的 tool-block header target 必须是 semantic control 的合同，本轮按该合同实现，不重复扩写。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 edit header file target button accessibility

- 上轮进展与阻塞：上一轮已完成 `ReadToolBlock` header 文件目标的 button 化，代码中确实存在 `openFileLabel`、`<button type="button">`、`title` / `aria-label` 的 action + target 标签，并保留文件图标、可见路径、`stopPropagation()` 和 `openFile(...)`；`ReadToolBlock.test.tsx` 已覆盖该行为。更早的 Generic/Search 文件打开按钮、Bash/Read/Edit/Generic 单块 header、Bash/Read/Search/Edit group 内部 row、Task/Agent header、group bulk actions 可访问性改动也仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `EditToolBlock` 提供 Edit header 文件路径点击打开能力；本项目此前也能点击打开，但 header 文件路径是 click-only `span`，没有原生按钮语义、焦点入口或动作化名称。用户价值：编辑工具是用户最需要核对 diff 和路径的高频工具，键盘用户可以直接聚焦并打开编辑目标，辅助技术用户能听到这是打开文件而不是普通路径。
- 本轮差距诊断（超越 cc-gui）：本项目 Edit header 文件目标还承载 diff hover preview 和加减行统计。本轮没有移除这些能力，而是把同一块目标迁移到 `Open file: path` 的语义化 button 合同。用户价值：既保留快速预览 diff 的扫描效率，也让文件打开动作可聚焦、可理解、可复用。
- 本轮规划：只改 `EditToolBlock` header 文件目标、一个 SSR 回归和 Trellis 记录；复用已有 `.file-path-button` CSS 和 `.trellis/spec/frontend/component-guidelines.md` 合同。不改 diff 解析、hover preview 内容、详情 `Open File` / `Copy Path`、状态计算、分组组件、Store/Rust/Tauri response。优先级高：用户价值明确、出现频率高（Edit 是核心工具）、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/EditToolBlock.tsx` 新增 `openFileLabel`，有 `openPath` 时文件目标渲染为 `<button type="button">`，保留文件图标、`edit-diff-hover-label`、加减行统计、`EditDiffPreview`、原路径链接 class、`event.stopPropagation()` 和 `openFile(openPath, primaryItem?.lineStart, primaryItem?.lineEnd, currentCwd)`；无可打开路径时继续保持普通 summary 文本。`src/components/toolBlocks/EditToolBlock.test.tsx` 新增 SSR 断言，覆盖 `<button>`、`type="button"`、`title` / `aria-label` 的 `tools.openFile: src/App.tsx` action + target 标签，并确认 hover label 仍渲染。`.trellis/spec/frontend/component-guidelines.md` 已有对应合同，本轮未重复扩写。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/EditToolBlock.test.tsx` 先失败 1 项，失败点为文件目标仍输出 `<span class="tool-title-summary file-path-link clickable-file edit-diff-hover-trigger" title="src/App.tsx">...`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 3 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts` 通过（14 files / 67 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（51 files / 457 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 Edit 工具块；未手动确认 Edit header 文件目标按钮的键盘焦点、tooltip、点击打开路径/行号、diff hover preview 和辅助技术播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 审计 `ReadToolGroupBlock` / `EditToolGroupBlock` 内部文件路径是否仍有 click-only span，并按 group item 行分轮推进；2) 为详情区 `Open File` 按钮补 action + target 标签，避免只读成泛化 `Open file`；3) 真实 Tauri desktop smoke：触发 Generic/Read/Edit/Search 类工具，点测文件目标按钮、Search 文件按钮、Copy Input/Output、Copy Path 的 tooltip、键盘焦点、diff hover 和点击路径/行号；4) 审计 grouped Bash/Read/Search 的状态聚合和特殊工具归类，确认是否存在更多误导性 error/pending 状态。

## 本轮 PLAN 2026-06-20 detail open-file action target labels

- 目标：把 `ReadToolBlock` / `EditToolBlock` 展开详情区的 `Open File` 按钮从泛化 `tools.openFile` 标签升级为 `Open file: path` 的 action + target 标签。Header 文件目标已完成同类语义化，详情区按钮仍会被辅助技术读成无目标的“打开文件”。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ReadToolBlock.tsx` / `EditToolBlock.tsx` 及测试、对标项目 Read/Edit ToolBlock；确认本轮只补详情区 `Open File` 按钮标签，不改 header 文件目标、copy path、diff preview、openFile 参数、Store/Rust/Tauri response 或 group 组件。
- 功能点 3：补 RED 测试。验证方式：扩展 `ReadToolBlock.test.tsx` 与 `EditToolBlock.test.tsx`，SSR 断言展开态详情区 `Open File` 按钮的 `title` / `aria-label` 包含具体路径；旧实现应因仍为裸 `tools.openFile` 失败。
- 功能点 4：实现最小交互修复。验证方式：复用现有 `openFileLabel`，将详情区按钮 `title` / `aria-label` 改为 action + target；按钮可见文案仍保持 `tools.openFile`，避免视觉噪音。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，明确任何 `openFile()` 动作按钮（header target 或详情区按钮）都必须在 `title` / `aria-label` 中包含 action + target。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 detail open-file action target labels

- 上轮进展与阻塞：上一轮完成 `EditToolBlock` header 文件目标 button 化，代码中确实存在 `<button type="button">`、`openFileLabel`、`title` / `aria-label` 的 action + target 标签，并保留 diff hover preview、加减行统计、`event.stopPropagation()` 和 `openFile(...)`；`EditToolBlock.test.tsx` 已覆盖该行为。更早的 Read/Generic header 文件目标、Search 文件按钮、Bash/Read/Edit/Generic 单块 header、Bash/Read/Search/Edit group row、Task/Agent header、group bulk actions 等可访问性改动也仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的 Read/Edit 工具都围绕文件打开和文件参数展开展示；本项目 Header 文件目标已具备 action + target 语义，但展开详情区的 `Open File` 按钮仍只有裸 `tools.openFile` 标签。用户价值：用户在展开详情检查文件路径、行号和 diff 后，辅助技术仍能听到具体要打开哪个文件，而不是多个同名“打开文件”按钮。
- 本轮差距诊断（超越 cc-gui）：本项目已经把 header 文件目标、Search 文件按钮和 copy 动作做成稳定语义合同；本轮把详情区动作按钮也纳入同一合同。用户价值：同一屏中多个文件工具块并存时，用户可以从按钮名称直接区分目标文件，减少误打开。
- 本轮规划：只改 `ReadToolBlock` / `EditToolBlock` 展开详情区 `Open File` 按钮、对应 SSR 测试、前端组件规范和 Trellis 记录；不改 header 文件目标、copy path、diff preview、`openFile()` 参数、分组组件、Store/Rust/Tauri response。优先级中高：用户价值明确、出现频率高（Read/Edit 高频）、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/ReadToolBlock.tsx` 和 `src/components/toolBlocks/EditToolBlock.tsx` 复用已有 `openFileLabel`，将详情区 `Open File` 按钮的 `title` / `aria-label` 改为 `tools.openFile: <path>`；按钮可见文本仍保持 `tools.openFile`，避免视觉噪音。`src/components/toolBlocks/ReadToolBlock.test.tsx` 与 `src/components/toolBlocks/EditToolBlock.test.tsx` 新增展开态 SSR 回归，断言 `.btn btn-sm btn-ghost` 详情按钮包含目标路径标签。`.trellis/spec/frontend/component-guidelines.md` 已补充：任何调用 `openFile()` 的控件，包括 header target 和展开详情 action button，都必须在 `title` / `aria-label` 中包含 action + target。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx` 先失败 2 项，失败点均为详情区 `Open File` 按钮仍输出 `title="tools.openFile" aria-label="tools.openFile"`，符合旧实现缺口。GREEN 定向通过：同命令通过（2 files / 8 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（14 files / 69 tests）。全量前端通过：`npm test`（51 files / 459 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。
- 未验证项：未启动真实 Tauri 桌面触发展开态 Read/Edit 工具块；未手动确认详情区 `Open File` 按钮的 tooltip、键盘焦点、点击打开路径/行号和屏幕阅读器播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 审计 `ReadToolGroupBlock` / `EditToolGroupBlock` 内部文件路径是否仍有 click-only span 或泛化 open-file 标签，并按 group item 行分轮推进；2) 真实 Tauri desktop smoke：触发 Generic/Read/Edit/Search 类工具，点测 header 文件目标、详情 Open File、Search 文件按钮、Copy Input/Output、Copy Path 的 tooltip、键盘焦点、diff hover 和点击路径/行号；3) 审计 grouped Bash/Read/Search 的状态聚合和特殊工具归类，确认是否存在更多误导性 error/pending 状态；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility。

## 本轮 PLAN 2026-06-20 read group file target button accessibility

- 目标：把 `ReadToolGroupBlock` 内部每个可打开文件路径从 click-only `span` 收敛为语义化 `Open file: path` 控件。内部 row 已能用键盘展开，但文件路径打开动作仍没有原生按钮语义和 action + target 标签。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ReadToolGroupBlock.tsx` / `ReadToolGroupBlock.test.tsx` 和对标项目 `ReadToolGroupBlock.tsx`；确认本轮只补 Read group 内部文件路径打开控件，不改 row 展开语义、批量展开/折叠、嵌套 `ReadToolBlock`、`openFile()` 参数、分组算法、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展 `ReadToolGroupBlock.test.tsx`，SSR 断言可打开文件路径渲染为 `<button type="button">`，并带 `title` / `aria-label` 的 `tools.openFile: src/App.tsx` 标签；旧实现应因仍为 `span` 且 title 是裸路径失败。
- 功能点 4：实现最小交互修复。验证方式：为可打开文件路径新增 `openFileLabel`，渲染原生 button，保留可见路径、`event.stopPropagation()`、`openFile(target.openPath, lineInfo.start, lineInfo.end, currentCwd)`；目录或不可打开目标继续保持静态文本。
- 功能点 5：复核 Trellis 规范。验证方式：`.trellis/spec/frontend/component-guidelines.md` 已有任何调用 `openFile()` 的控件必须包含 action + target 的合同，本轮按该合同实现，不重复扩写。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 read group file target button accessibility

- 上轮进展与阻塞：上一轮完成 Read/Edit 展开详情区 `Open File` 按钮 action + target 标签，代码中确实存在 `title` / `aria-label` 的 `tools.openFile: <path>` 语义，且不改变可见文案和 `openFile()` 参数。更早的 Header 文件目标 button 化、Search 文件按钮、Bash/Read/Edit/Generic 单块 header、Bash/Read/Search/Edit group row、Task/Agent header、group bulk actions 等可访问性改动也仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `ReadToolGroupBlock` 提供批量 Read 文件列表和文件点击打开能力；本项目内部 row 已有键盘展开语义，但文件路径本身仍是 click-only `span`，`title` 只是裸路径。用户价值：键盘和辅助技术用户可以直接聚焦并理解“打开哪个文件”，而不是只能展开 row 或依赖鼠标点击路径。
- 本轮差距诊断（超越 cc-gui）：本项目在 group row 内同时提供“展开详情”和“打开文件”两个动作。本轮让文件路径成为原生 `Open file: path` button，并继续用 `event.stopPropagation()` 保持两个动作互不干扰。用户价值：用户能快速打开文件，也能按需展开详情，操作语义清晰。
- 本轮规划：只改 `ReadToolGroupBlock` 内部文件路径目标和对应 SSR 测试；不改 row 展开语义、批量展开/折叠、嵌套 `ReadToolBlock`、`openFile()` 参数、分组算法、Store/Rust/Tauri response。优先级中高：用户价值明确、出现频率中高（Read group 常见）、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/ReadToolGroupBlock.tsx` 为可打开文件路径新增 `openFileLabel`，将路径从 `<span>` 改为 `<button type="button" className="task-group-item-file file-path-button clickable-file">`，补 `title` / `aria-label` 的 `tools.openFile: <path>`；保留 `event.stopPropagation()` 和 `openFile(target.openPath, lineInfo.start, lineInfo.end, currentCwd)`。目录或不可打开目标仍保持静态 `<span>`。`src/components/toolBlocks/ReadToolGroupBlock.test.tsx` 新增 SSR 回归，覆盖内部文件路径的语义化 open-file button。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/ReadToolGroupBlock.test.tsx` 先失败 1 项，失败点为文件路径仍输出 `<span class="task-group-item-file clickable-file" title="src/App.tsx">`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 3 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（14 files / 70 tests）。全量前端通过：`npm test`（51 files / 460 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。
- 未验证项：未启动真实 Tauri 桌面触发 Read group；未手动确认 Tab 到内部文件按钮后 Enter/Space 打开文件、row Enter/Space 展开详情、二者互不误触；未用屏幕阅读器确认 `Open file: path` 和 row `aria-expanded` 播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 审计 `EditToolGroupBlock` 内部文件路径是否仍有 click-only span，并按同样方式 button 化，需保留 diff hover preview；2) 真实 Tauri desktop smoke：触发 Generic/Read/Edit/Search 类工具，点测 header 文件目标、详情 Open File、group 内部文件按钮、Search 文件按钮、Copy Input/Output、Copy Path 的 tooltip、键盘焦点、diff hover 和点击路径/行号；3) 审计 grouped Bash/Read/Search 的状态聚合和特殊工具归类，确认是否存在更多误导性 error/pending 状态；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility。

## 迭代记录 2026-06-20 read header file target button accessibility

- 上轮进展与阻塞：上一轮已完成 `GenericToolBlock` header 文件目标的 button 化，代码中确实存在 `openFileLabel`、`<button type="button">`、`title` / `aria-label` 的 action + target 标签，以及 `.file-path-button` CSS reset；`GenericToolBlock.test.tsx` 已覆盖该行为。更早的 Bash/Read/Edit/Generic 单块、Bash/Read/Search/Edit group 内部 row、Task/Agent header、group bulk actions、Search 文件打开按钮可访问性改动也仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui `ReadToolBlock` 提供 Read header 文件路径点击打开能力；本项目此前也能点击打开，但 header 文件路径是 click-only `span`，没有原生按钮语义、焦点入口或动作化名称。用户价值：Read 是最高频文件工具之一，键盘用户可以直接聚焦并打开读取目标，辅助技术用户能听到这是打开文件而不是普通路径。
- 本轮差距诊断（超越 cc-gui）：本项目已经沉淀了“任何调用 `openFile()` 的 tool-block header target 必须是 semantic control”的规范。本轮把 Read 迁移到和 Generic/Search 一致的 `Open file: path` 合同。用户价值：用户在不同工具块中遇到文件路径时，操作方式和播报语义保持一致。
- 本轮规划：只改 `ReadToolBlock` header 文件目标、一个 SSR 回归和 Trellis 记录；复用上一轮已有 `.file-path-button` CSS 和 `.trellis/spec/frontend/component-guidelines.md` 合同。不改详情 `Open File` / `Copy Path`、状态计算、line info、分组组件、Store/Rust/Tauri response。优先级高：用户价值明确、出现频率高（Read 是常用工具）、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/ReadToolBlock.tsx` 新增 `openFileLabel`，文件目标渲染为 `<button type="button">`，保留文件图标、可见路径、原路径链接 class、`event.stopPropagation()` 和 `openFile(target.openPath, lineInfo.start, lineInfo.end, currentCwd)`；目录或非文件目标继续保持普通 summary 文本。`src/components/toolBlocks/ReadToolBlock.test.tsx` 新增 SSR 断言，覆盖 `<button>`、`type="button"`、`title` / `aria-label` 的 `tools.openFile: src/components/toolBlocks/ReadToolBlock.tsx` action + target 标签。`.trellis/spec/frontend/component-guidelines.md` 已有对应合同，本轮未重复扩写。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/ReadToolBlock.test.tsx` 先失败 1 项，失败点为文件目标仍输出 `<span class="tool-title-summary file-path-link clickable-file" title="src/components/toolBlocks/ReadToolBlock.tsx">...`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 3 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts` 通过（14 files / 66 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（51 files / 456 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 Read 工具块；未手动确认 Read header 文件目标按钮的键盘焦点、tooltip、点击打开路径/行号和辅助技术播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 继续把同一 header file target button 合同迁移到 `EditToolBlock`；2) 审计 `ReadToolGroupBlock` / `EditToolGroupBlock` 内部文件路径是否仍有 click-only span，并按 group item 行分轮推进；3) 真实 Tauri desktop smoke：触发 Generic/Read/Edit/Search 类工具，点测文件目标按钮、Search 文件按钮、Copy Input/Output、Copy Path 的 tooltip、键盘焦点和点击路径/行号；4) 审计 grouped Bash/Read/Search 的状态聚合和特殊工具归类，确认是否存在更多误导性 error/pending 状态。

## 本轮 PLAN 2026-06-20 edit header file target button accessibility

- 目标：把 `EditToolBlock` header 内可打开文件的路径目标从 click-only `span` 收敛为语义化文件打开控件。Generic 和 Read 已完成同类修复，Edit 仍是高价值文件入口，并且承载 diff hover preview。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `EditToolBlock.tsx` / `EditToolBlock.test.tsx` 与对标项目 `EditToolBlock.tsx`；确认本轮只补 header 文件路径打开控件，不改 diff 解析、hover preview 内容、详情 `Open File` / `Copy Path`、状态计算、Store/Rust/Tauri response 或 group 组件。
- 功能点 3：补 RED 测试。验证方式：扩展 `EditToolBlock.test.tsx`，SSR 断言 header 文件路径渲染为原生 `button type="button"`，真实文案具有 `Open file: src/App.tsx` 的 action + target 语义（单测 i18n mock 下为 `tools.openFile: src/App.tsx`），并保留 diff hover label；旧实现应因仍是 `span` 且标签为裸路径失败。
- 功能点 4：实现最小交互修复。验证方式：给 Edit header 文件目标添加 `openFileLabel`，文件目标使用 `button` 保持原 class、图标、hover label、stats、`EditDiffPreview`、`stopPropagation()` 和 `openFile(openPath, lineStart, lineEnd, currentCwd)`；无可打开路径时保持非交互文本。
- 功能点 5：复核 Trellis 规范。验证方式：`.trellis/spec/frontend/component-guidelines.md` 已有任何调用 `openFile()` 的 tool-block header target 必须是 semantic control 的合同，本轮按该合同实现，不重复扩写。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。
- 下一步候选：1) 审计 `EditToolGroupBlock` 内部文件路径是否仍有 click-only span，并按同样方式 button 化，需保留 diff hover preview；2) 真实 Tauri desktop smoke：触发 Generic/Read/Edit/Search 类工具，点测 header 文件目标、详情 Open File、group 内部文件按钮、Search 文件按钮、Copy Input/Output、Copy Path 的 tooltip、键盘焦点、diff hover 和点击路径/行号；3) 审计 grouped Bash/Read/Search 的状态聚合和特殊工具归类，确认是否存在更多误导性 error/pending 状态；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility。

## 本轮 PLAN 2026-06-20 edit group file target button accessibility

- 目标：把 `EditToolGroupBlock` 内部 changed-file 文件路径从 click-only `span` 收敛为语义化 `Open file: path` 控件。内部 row 已具备键盘展开语义，批量按钮已有稳定标签，但文件路径打开动作仍没有原生按钮语义和 action + target 标签。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `EditToolGroupBlock.tsx` / `EditDiffPreview.tsx` 与对标项目 `EditToolGroupBlock.tsx`；确认本轮只补 Edit group 内部文件路径打开控件，不改 row 展开语义、批量展开/折叠、嵌套 `EditToolBlock`、`EditDiffPreview`、`openFile()` 参数、分组算法、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展 `EditToolGroupBlock.test.tsx`，SSR 断言可打开文件路径渲染为 `<button type="button">`，并带 `title` / `aria-label` 的 `tools.openFile: src/App.tsx` 标签，同时保留 diff hover trigger 和预览挂载；旧实现应因仍为 `span` 且 title 是裸路径失败。
- 功能点 4：实现最小交互修复。验证方式：为可打开文件路径新增 `openFileLabel`，渲染原生 button，保留可见路径、`event.stopPropagation()`、`openFile(item.openPath, item.lineStart, item.lineEnd, currentCwd)`、`edit-diff-hover-trigger` 和 `EditDiffPreview`。
- 功能点 5：复核 Trellis 规范。验证方式：`.trellis/spec/frontend/component-guidelines.md` 已有任何调用 `openFile()` 的控件必须包含 action + target 的合同，本轮按该合同实现，不重复扩写。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 edit group file target button accessibility

- 上轮进展与阻塞：上一轮完成 `ReadToolGroupBlock` 内部文件路径 button 化，代码中确实存在 `button type="button"`、`file-path-button clickable-file`、`title` / `aria-label` 的 `tools.openFile: <path>` 语义，并保留 `openFile(target.openPath, lineInfo.start, lineInfo.end, currentCwd)`。本轮开始时确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 真实 `webview/src/components/toolBlocks/EditToolGroupBlock.tsx` 有批量编辑文件列表和点击打开文件能力；本项目 `EditToolGroupBlock.tsx` 的文件路径仍是 click-only `span`，`title` 只有裸路径。用户价值：键盘用户和辅助技术用户可以直接聚焦并理解“打开哪个编辑目标文件”，减少批量编辑审查时的误操作。
- 本轮差距诊断（超越 cc-gui）：本项目 Edit group 已支持 row 展开、嵌套 `EditToolBlock` 和 hover diff preview；本轮没有退回 cc-gui 的静态列表，而是把内部文件路径升级成原生 open-file button，并保留 `EditDiffPreview`。用户价值：同一个 changed-file entry 同时提供“展开详情”和“打开文件”两个清晰动作，且键盘打开文件不会顺带切换父 row。
- 本轮规划：只改 `EditToolGroupBlock` 内部文件路径目标、对应 SSR 测试、前端组件规范和 Trellis 记录；不改 row 展开语义、批量展开/折叠、嵌套 `EditToolBlock`、`EditDiffPreview`、`openFile()` 参数、分组算法、Store/Rust/Tauri response。优先级中高：用户价值明确、出现频率中高（批量编辑常见）、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/EditToolGroupBlock.tsx` 为每个 edit item 新增 `openFileLabel`，将文件路径从 `<span>` 改为 `<button type="button" className="task-group-item-file file-path-button clickable-file edit-diff-hover-trigger">`，补 `title` / `aria-label` 的 `tools.openFile: <displayPath>`；保留可见路径、`EditDiffPreview`、`event.stopPropagation()` 和 `openFile(item.openPath, item.lineStart, item.lineEnd, currentCwd)`，并对文件按钮自身 `onKeyDown` 阻止冒泡，避免键盘打开文件同时触发 row 展开。`src/components/toolBlocks/EditToolGroupBlock.test.tsx` 新增 SSR 回归，覆盖内部文件路径的语义化 open-file button、可见路径和 diff preview 挂载。`.trellis/spec/frontend/component-guidelines.md` 已补充嵌套在 expandable row 内的 open-file 控件需要同时阻止 click / keydown 冒泡。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/EditToolGroupBlock.test.tsx` 先失败 1 项，失败点为文件路径仍输出 `<span class="task-group-item-file clickable-file edit-diff-hover-trigger" title="src/App.tsx">`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 3 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（14 files / 71 tests）。全量前端通过：`npm test`（51 files / 461 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 Edit group；未手动确认 Tab 到内部文件按钮后 Enter/Space 打开文件、row Enter/Space 展开详情、二者互不误触；未用屏幕阅读器确认 `Open file: path` 和 row `aria-expanded` 播报；未确认 hover diff preview 在真实 WebView 中与 button focus 的视觉共存；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Generic/Read/Edit/Search 类工具，点测 header 文件目标、详情 Open File、group 内部文件按钮、Search 文件按钮、Copy Input/Output、Copy Path 的 tooltip、键盘焦点、diff hover 和点击路径/行号；2) 审计 Read/Edit group 内部文件按钮键盘冒泡边界，使键盘打开文件不会同时展开父 row；3) 审计 grouped Bash/Read/Search 的状态聚合和特殊工具归类，确认是否存在更多误导性 error/pending 状态；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility。

## 本轮 PLAN 2026-06-20 group open file keydown propagation boundary

- 目标：补齐 `ReadToolGroupBlock` 与 `SearchToolGroupBlock` 中嵌套在 expandable row header 内的 open-file 按钮键盘事件边界。上一轮已在 Edit group 中发现并修复：文件按钮需要阻止 keydown 冒泡，否则聚焦文件按钮按 Enter/Space 时可能同时触发父 row 展开/折叠。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ReadToolGroupBlock.tsx` / `SearchToolGroupBlock.tsx` / `EditToolGroupBlock.tsx` 和对标项目相关 group 实现；确认 Edit 已阻止 keydown 冒泡，Read 和 Search header 内 open-file 按钮仍缺该边界。
- 功能点 3：补 RED 测试。验证方式：新增 `ToolGroupOpenFilePropagation.test.tsx`，用 mock React hooks 直接检查 Read group 文件按钮与 Search group 首个命中文件按钮的 `onKeyDown` 会调用 `stopPropagation()`；旧实现应因缺少 `onKeyDown` 失败。
- 功能点 4：实现最小交互修复。验证方式：只给 Read group 文件按钮和 Search group header 首个命中文件按钮添加 `onKeyDown={(event) => event.stopPropagation()}`；不改 openFile 参数、click 行为、row 展开逻辑、展开结果列表按钮、分组算法、Store/Rust/Tauri response。
- 功能点 5：复核 Trellis 规范。验证方式：`.trellis/spec/frontend/component-guidelines.md` 已有嵌套 open-file 控件需要阻止 click / keydown 冒泡的合同，本轮按该合同实现，不重复扩写。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 group open file keydown propagation boundary

- 上轮进展与阻塞：上一轮完成 `EditToolGroupBlock` 内部文件路径 open-file button 化，并已在代码中确认 `onKeyDown` 阻止冒泡，避免键盘打开文件时同时触发父 row 展开。上轮还把“嵌套在 expandable row 内的 open-file 控件需要阻止 click / keydown 冒泡”写入 `.trellis/spec/frontend/component-guidelines.md`。本轮开始时确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的文件列表主要依赖鼠标点击文件路径；本项目已经做得更强，提供可聚焦 open-file button 和可展开 row，但 `ReadToolGroupBlock` 与 `SearchToolGroupBlock` 的 row header 内文件按钮只阻止 click 冒泡，缺少 keydown 冒泡边界。用户价值：键盘用户聚焦文件按钮按 Enter/Space 打开文件时，不会意外展开或折叠同一行。
- 本轮差距诊断（超越 cc-gui）：本项目在同一行中同时保留“打开文件”和“展开详情”两种动作，本轮让 Read/Search 与上一轮 Edit 采用一致事件边界。用户价值：文件跳转和详情审查互不干扰，尤其适合批量 Read/Search 结果中快速逐项打开文件。
- 本轮规划：只改 `ReadToolGroupBlock` 文件按钮、`SearchToolGroupBlock` header 首个命中文件按钮和新增事件边界测试；不改 openFile 参数、click 行为、row 展开逻辑、Search 展开结果列表按钮、分组算法、Store/Rust/Tauri response。优先级中高：用户价值明确、出现频率中高、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/ReadToolGroupBlock.tsx` 的内部文件按钮新增 `onKeyDown={(event) => event.stopPropagation()}`，保留 `onClick` 的 `event.stopPropagation()` 与 `openFile(target.openPath, lineInfo.start, lineInfo.end, currentCwd)`。`src/components/toolBlocks/SearchToolGroupBlock.tsx` 的 row header 首个命中文件按钮新增同样的 keydown 冒泡拦截，保留 `openFile(firstFile.path, firstFile.lineStart, undefined, currentCwd)`；展开结果列表按钮不在 header 内，本轮不改。新增 `src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx`，通过 mock React hooks 直接调用按钮 `onKeyDown`，覆盖 Read/Search 两个嵌套 open-file 按钮的事件边界。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx` 先失败 2 项，失败点为 Read group 文件按钮和 Search group header 文件按钮均未调用 `stopPropagation()`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 2 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（15 files / 73 tests）。全量前端通过：`npm test`（52 files / 463 tests，仅 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 Read/Search group；未手动确认 Tab 到内部文件按钮后 Enter/Space 打开文件、row Enter/Space 展开详情、二者互不误触；未用屏幕阅读器确认 `Open file: path` 和 row `aria-expanded` 播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Generic/Read/Edit/Search 类工具，点测 header 文件目标、详情 Open File、group 内部文件按钮、Search 文件按钮、Copy Input/Output、Copy Path 的 tooltip、键盘焦点、diff hover 和点击路径/行号；2) 审计 tool block 详情区非文件 action button 的 copied 反馈是否都保持稳定 `title` / `aria-label`；3) 审计 grouped Bash/Read/Search 的状态聚合和特殊工具归类，确认是否存在更多误导性 error/pending 状态；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility。

## 本轮 PLAN 2026-06-20 single block open file keydown propagation boundary

- 目标：补齐 `GenericToolBlock` / `ReadToolBlock` / `EditToolBlock` 中嵌套在可展开 header 内的 open-file 按钮键盘事件边界。当前这些文件按钮已经是语义化 button 且 click 会阻止冒泡，但缺少 keydown 冒泡拦截；键盘用户聚焦文件按钮按 Enter/Space 时，事件仍可能冒泡到父 header 并触发展开/折叠。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `GenericToolBlock.tsx` / `ReadToolBlock.tsx` / `EditToolBlock.tsx` 和相关测试；确认本轮只补 header 内 open-file button 的 `onKeyDown` 冒泡边界，不改 `openFile()` 参数、click 行为、header 展开逻辑、copy 动作、分组算法、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：新增单块 open-file propagation 回归，直接检查 Generic/Read/Edit header 文件按钮的 `onKeyDown` 会调用 `stopPropagation()`；旧实现应因缺少 `onKeyDown` 失败。
- 功能点 4：实现最小交互修复。验证方式：给 Generic/Read/Edit header 内 open-file button 添加 `onKeyDown={(event) => event.stopPropagation()}`；保留 action + target `title` / `aria-label`、可见路径、diff hover preview 和原 `openFile()` 参数。
- 功能点 5：复核 Trellis 规范。验证方式：`.trellis/spec/frontend/component-guidelines.md` 已有嵌套在 expandable row 内的 open-file 控件需要阻止 click / keydown 冒泡的合同；本轮按该合同延伸到单块可展开 header，不重复扩写除非发现新约定。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 single block open file keydown propagation boundary

- 上轮进展与阻塞：上一轮完成 `ReadToolGroupBlock` 与 `SearchToolGroupBlock` 中嵌套 open-file 按钮的 keydown 冒泡边界，代码中确实存在 `onKeyDown={(event) => event.stopPropagation()}`，避免键盘打开文件时同时触发父 row 展开/折叠。更早的 Header 文件目标 button 化、详情 `Open File` action + target 标签、Search 文件按钮、Copy Input/Output/Path 稳定标签、Task/Agent header 和 group bulk actions 可访问性改动也仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的文件路径点击主要是鼠标路径入口；本项目已经把 Generic/Read/Edit header 文件目标升级成可聚焦 button，但内层 button 缺少 keydown 冒泡边界。用户价值：键盘用户聚焦文件路径按 Enter/Space 打开文件时，不会意外展开或折叠同一个工具块 header。
- 本轮差距诊断（超越 cc-gui）：本项目在单块 header 中同时保留“展开详情”和“打开文件”两个动作，本轮让单块 header 与上一轮 group row 采用同一事件边界合同。用户价值：文件跳转和详情审查互不干扰，尤其适合一屏多工具块时快速逐项打开文件。
- 本轮规划：只改 `GenericToolBlock` / `ReadToolBlock` / `EditToolBlock` header 内 open-file button、一个事件边界回归测试、前端组件规范和 Trellis 记录；不改 `openFile()` 参数、click 行为、header 展开逻辑、copy 动作、diff preview、分组算法、Store/Rust/Tauri response。优先级中高：用户价值明确、出现频率高、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/GenericToolBlock.tsx`、`src/components/toolBlocks/ReadToolBlock.tsx`、`src/components/toolBlocks/EditToolBlock.tsx` 的 header open-file button 均新增 `onKeyDown={(event) => event.stopPropagation()}`，保留 `title` / `aria-label` 的 `tools.openFile: <path>` 语义、可见路径、`openFile()` 参数和 Edit diff hover preview。新增 `src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx`，用 mock React hooks 直接调用按钮 `onKeyDown`，覆盖 Generic/Read/Edit 三个单块 header 文件按钮。`.trellis/spec/frontend/component-guidelines.md` 已把 open-file 冒泡边界从 group row 扩展为 expandable tool-block header 或 tool-group row。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx` 先失败 3 项，失败点为 Generic/Read/Edit header 文件按钮均未调用 `stopPropagation()`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 3 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（16 files / 76 tests）。全量前端通过：`npm test`（53 files / 466 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。`git diff --check` exit 0；`git diff --cached --check` exit 0；二者仅输出 Windows LF/CRLF 转换提示。
- 未验证项：未启动真实 Tauri 桌面触发 Generic/Read/Edit 单块 header；未手动确认 Tab 到 header 文件按钮后 Enter/Space 打开文件、父 header Enter/Space 展开详情、二者互不误触；未用屏幕阅读器确认 `Open file: path` 和 header `aria-expanded` 播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Generic/Read/Edit/Search 类工具，点测 header 文件目标、详情 Open File、group 内部文件按钮、Search 文件按钮、Copy Input/Output/Path 的 tooltip、键盘焦点、diff hover 和点击路径/行号；2) 对 ToolBlock 可访问性做运行态扫描，核对所有可展开入口和操作按钮的 `role/button/tabindex/aria-expanded/title/aria-label/onKeyDown` 覆盖；3) 审计 grouped Bash/Read/Search 的状态聚合和特殊工具归类，确认是否存在更多误导性 error/pending 状态；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility。

## 本轮 PLAN 2026-06-20 subagent process open file keydown boundary

- 目标：补齐 `SubagentProcessSummary` 中子代理执行过程文件按钮的 keydown 冒泡边界。当前 read file chip 和 tool row target 已经有 `title` / `aria-label` 与 click 冒泡拦截，但缺少 keydown 拦截；在 Agent/Task 详情里浏览子代理过程时，键盘打开文件不应冒泡到周边工具容器或未来行级键盘处理。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph 诊断。验证方式：读取本项目 `SubagentHistoryPanel.tsx` / `SubagentHistoryPanel.test.tsx`；确认本轮只补 `SubagentProcessSummary` open-file 按钮 `onKeyDown`，不改历史加载、process model、openFile 参数、Agent/Task 展开状态、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展 `SubagentHistoryPanel.test.tsx`，直接检查 `.subagent-file-chip` 与 `.subagent-tool-row-target` 的 `onKeyDown` 会调用 `stopPropagation()`；旧实现应因缺少 `onKeyDown` 失败。
- 功能点 4：实现最小交互修复。验证方式：给两个 open-file button 添加 `onKeyDown={(event) => event.stopPropagation()}`；保留 action + target 标签、可见路径、line range、箭头图标和原 `openFile()` 参数。
- 功能点 5：复核 Trellis 规范。验证方式：现有 `.trellis/spec/frontend/component-guidelines.md` 已约束 open-file 控件必须暴露 action + target，并在嵌套于可展开 header/group row 时阻止 click/keydown；本轮如未发现新跨组件合同，不额外扩写。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 subagent process open file keydown boundary

- 上轮进展与阻塞：上一轮完成 `GenericToolBlock` / `ReadToolBlock` / `EditToolBlock` 单块 header open-file button 的 keydown 冒泡边界，代码中确实存在 `onKeyDown={(event) => event.stopPropagation()}`，避免键盘打开文件时同时触发父 header 展开/折叠。更早的 Header 文件目标 button 化、详情 `Open File` action + target 标签、Search 文件按钮、Copy Input/Output/Path 稳定标签、Task/Agent header、group bulk actions 和 group row 冒泡边界也仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 没有本项目这种完整的子代理过程文件 chip / tool row target 展示；本项目能力更强，但 `SubagentProcessSummary` 的 read file chip 和 tool row target 只阻止 click 冒泡，缺少 keydown 冒泡边界。用户价值：键盘用户在 Agent/Task 详情里打开子代理读过或搜索到的文件时，不会触发周边工具容器或未来行级键盘处理的第二动作。
- 本轮差距诊断（超越 cc-gui）：保留本项目比 cc-gui 更丰富的子代理过程审查能力，并让子代理过程里的文件按钮采用和主 ToolBlock 一致的 action + target + keydown boundary 合同。用户价值：主消息工具块和子代理历史工具块的文件打开行为一致，用户不用学习两套键盘模型。
- 本轮规划：只改 `SubagentProcessSummary` 中 `.subagent-file-chip` 与 `.subagent-tool-row-target` 两类 open-file button、一个事件边界回归测试、前端组件规范和 Trellis 记录；不改历史加载、process model、openFile 参数、Agent/Task 展开状态、Store/Rust/Tauri response。优先级中等偏高：用户价值明确、出现频率取决于子代理使用量、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/SubagentHistoryPanel.tsx` 为 read file chip 和 tool row target open-file button 新增 `onKeyDown={(event) => event.stopPropagation()}`，保留 `title` / `aria-label` 的 `tools.subagentProcessOpenFile` 语义、可见路径、line range、箭头图标和 `openFile(..., currentCwd)` 参数。新增 `src/components/toolBlocks/SubagentProcessOpenFilePropagation.test.tsx`，mock `react-i18next` 后直接检查两个按钮的 `onKeyDown` 会调用 `stopPropagation()`。`.trellis/spec/frontend/component-guidelines.md` 已补充：子代理过程 file chip / tool-row target 这类 compact composite tool rows 也应采用同样 keydown boundary。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/SubagentProcessOpenFilePropagation.test.tsx` 先失败 1 项，失败点为 read file chip 未调用 `stopPropagation()`，符合旧实现缺口。GREEN 定向通过：`npm test -- src/components/toolBlocks/SubagentProcessOpenFilePropagation.test.tsx src/components/toolBlocks/SubagentHistoryPanel.test.tsx`（2 files / 10 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/SubagentProcessOpenFilePropagation.test.tsx src/components/toolBlocks/SubagentHistoryPanel.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（16 files / 82 tests）。全量前端通过：`npm test`（54 files / 467 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。`git diff --check` exit 0；`git diff --cached --check` exit 0；二者仅输出 Windows LF/CRLF 转换提示。
- 未验证项：未启动真实 Tauri 桌面触发 Agent/Task 子代理历史过程；未手动确认 Tab 到子代理 file chip / tool row target 后 Enter/Space 打开文件且不触发周边容器；未用屏幕阅读器确认 `subagentProcessOpenFile` 播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Agent/Task 子代理历史，点测过程文件 chip、tool row target、主工具块 header 文件目标、详情 Open File 和 group 文件按钮的 tooltip、键盘焦点、点击路径/行号；2) 对 ToolBlock 可访问性做运行态扫描，核对所有可展开入口和操作按钮的 `role/button/tabindex/aria-expanded/title/aria-label/onKeyDown` 覆盖；3) 审计 grouped Bash/Read/Search 的状态聚合和特殊工具归类，确认是否存在更多误导性 error/pending 状态；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility。

## 本轮 PLAN 2026-06-20 agent task header toggle accessible names

- 目标：给 `AgentGroupBlock` / `TaskExecutionBlock` 可展开 header 增加稳定 `title` / `aria-label`，让动作语义从“可见内容拼接出来的按钮名”收敛为明确的“展开/折叠子代理或任务详情”。cc-gui 的 Agent header 已有 toggle aria-label，本项目此前只有 `role="button"` / `tabIndex` / `aria-expanded`。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `AgentGroupBlock.tsx` / `TaskExecutionBlock.tsx` / 测试与对标项目 Agent/Task 实现；确认本轮只补 header toggle 的 `title` / `aria-label`，不改展开状态、子代理历史加载、process model、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展 `AgentGroupBlock.test.tsx` 与 `TaskExecutionBlock.test.tsx`，SSR 断言 header 同时暴露 `title` / `aria-label`，并包含动作与目标摘要；旧实现应因属性缺失失败。
- 功能点 4：实现最小交互修复。验证方式：为 Agent/Task header 计算可读 toggle label，优先使用 description / prompt / agentId / nickname 作为目标摘要；添加到 `title` / `aria-label`，保留现有 click、keydown、compact class、状态指示和详情内容。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录可展开 ToolBlock header 应暴露稳定动作标签，不能只依赖可见文本拼接。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 agent task header toggle accessible names

- 上轮进展与阻塞：上一轮已完成 `SubagentProcessSummary` 子代理过程文件 chip / tool row target 的 keydown 冒泡边界，代码中确实存在 `onKeyDown={(event) => event.stopPropagation()}`，新增 `SubagentProcessOpenFilePropagation.test.tsx` 覆盖该行为。更早的 Generic/Read/Edit 单块 header 文件目标、Read/Search group 内部文件按钮、Search 文件按钮、Copy Input/Output/Path 稳定标签、Task/Agent header `role/button/tabIndex/aria-expanded`、group bulk actions 等改动也仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的 `AgentGroupBlock` header 暴露 `aria-label` 形式的 toggle action；本项目 Agent/Task header 虽已具备键盘可达和 `aria-expanded`，但此前没有稳定 `title` / `aria-label`，辅助技术用户需要从“工具名 + 摘要 + 状态”推断它是展开详情。用户价值：用户能直接听到或 hover 到“切换子代理/任务详情：目标摘要”，降低 Agent/Task 历史审查入口的理解成本。
- 本轮差距诊断（超越 cc-gui）：本项目同时支持 Agent 与 Task 两类子代理工具，并能显示 description / prompt / agentId / nickname 等目标上下文；本轮把目标摘要合入 toggle label，而不是只给通用 `Toggle details`。用户价值：一屏多条任务或子代理调用时，键盘和读屏用户能区分当前要展开的是哪一个任务/子代理。
- 本轮规划：只改 `AgentGroupBlock` / `TaskExecutionBlock` header toggle 标签、两个 SSR 回归、i18n 文案、前端组件规范和 Trellis 记录；不改展开状态、子代理历史加载、process model、Store/Rust/Tauri response。优先级中高：用户价值明确、出现频率中高（Agent/Task 是复杂会话高价值入口）、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/AgentGroupBlock.tsx` 新增 `toggleTarget` / `toggleLabel`，用 `tools.agentDetailsToggle` 生成 `title` 和 `aria-label`，优先取 header primary/secondary summary、agentId 或工具名作为目标摘要。`src/components/toolBlocks/TaskExecutionBlock.tsx` 同步新增 `tools.taskDetailsToggle` 标签，优先取任务摘要、agentId、nickname 或工具名。`src/components/toolBlocks/AgentGroupBlock.test.tsx` 与 `src/components/toolBlocks/TaskExecutionBlock.test.tsx` 新增 SSR 断言，覆盖 header 的稳定动作标签和目标摘要。`src/locales/en.json` / `src/locales/zh.json` 新增 Agent/Task toggle 文案。`.trellis/spec/frontend/component-guidelines.md` 记录可展开 chat tool-block header 应暴露稳定 action + target 标签，不能只依赖可见文本和 `aria-expanded` 推断动作。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx` 先失败 2 项，失败点为两个 header 均缺少 `title="tools.*DetailsToggle: <target>"` / `aria-label`，符合旧实现缺口。GREEN 定向通过：同命令通过（2 files / 4 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/SubagentHistoryPanel.test.tsx src/components/toolBlocks/SubagentProcessOpenFilePropagation.test.tsx src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（18 files / 88 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（54 files / 469 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。`git diff --check` exit 0；`git diff --cached --check` exit 0；二者仅输出 Windows LF/CRLF 转换提示。
- 未验证项：未启动真实 Tauri 桌面触发 Agent/Task header；未手动确认 header tooltip、Tab 焦点、Enter/Space 展开详情和辅助技术播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Agent/Task 子代理历史，点测 header toggle label、过程文件 chip、tool row target、主工具块 header 文件目标、详情 Open File 和 group 文件按钮的 tooltip、键盘焦点、点击路径/行号；2) 对 ToolBlock 可访问性做运行态扫描，核对所有可展开入口和操作按钮的 `role/button/tabindex/aria-expanded/title/aria-label/onKeyDown` 覆盖；3) 审计 grouped Bash/Read/Search 的状态聚合和特殊工具归类，确认是否存在更多误导性 error/pending 状态；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility。

## 本轮 PLAN 2026-06-20 tool group header toggle accessible names

- 目标：给 `BashToolGroupBlock` / `ReadToolGroupBlock` / `EditToolGroupBlock` / `SearchToolGroupBlock` 的分组 header 增加稳定 `title` / `aria-label`，让分组级展开/折叠入口和上一轮 Agent/Task header 采用同一 action + target 语义。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目四个 group block 和测试、对标项目 Edit group；确认本轮只补 group header toggle 的 `title` / `aria-label` 与 i18n，不改 row 展开、bulk actions、文件打开、diff hover、分组算法、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展四个 group block 测试，SSR 断言 group header 暴露 `title` / `aria-label`，并包含具体分组目标摘要；旧实现应因属性缺失失败。
- 功能点 4：实现最小交互修复。验证方式：为 Bash/Read/Edit/Search group header 计算 toggle label，目标摘要分别使用命令摘要、读取文件摘要、编辑文件数量和搜索 query；添加到 `title` / `aria-label`，保留现有 click、keydown、compact class、状态指示和 chevron。
- 功能点 5：同步 Trellis 规范。验证方式：`.trellis/spec/frontend/component-guidelines.md` 已记录 expandable tool-block headers 的稳定 action label；本轮按该合同扩展到 tool-group headers，如需则补一句覆盖 group header。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 tool group header toggle accessible names

- 上轮进展与阻塞：上一轮已完成 `AgentGroupBlock` / `TaskExecutionBlock` 可展开 header 的稳定 `title` / `aria-label`，代码中确实存在 `tools.agentDetailsToggle` / `tools.taskDetailsToggle`，并把 description / prompt / agentId / nickname 等目标摘要合入 action label。更早的 Header 文件目标 button 化、详情 `Open File` action + target 标签、Search 文件按钮、Copy Input/Output/Path 稳定标签、group row 冒泡边界、子代理过程 file chip / tool row target 事件边界等改动也仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的可展开 Agent header 已有明确 toggle label；本项目普通 Bash/Read/Edit/Search group header 虽已有 `role="button"`、`tabIndex` 和 `aria-expanded`，但缺少稳定 `title` / `aria-label`。用户价值：读屏和 hover 用户不需要从图标、状态点、chevron 和摘要拼接推断这是分组级展开/折叠入口。
- 本轮差距诊断（超越 cc-gui）：本项目在 group header 上已有首条命令、首个文件、批量编辑目标和搜索 query 等摘要。本轮把这些摘要合进 action label，而不是只给通用 `Toggle details`。用户价值：多个工具分组连续出现时，用户能直接区分当前要展开的是哪组命令、读取、编辑或搜索结果。
- 本轮规划：只改 `BashToolGroupBlock` / `ReadToolGroupBlock` / `EditToolGroupBlock` / `SearchToolGroupBlock` 的 group header toggle 标签、四个 SSR 回归、i18n 文案、前端组件规范和 Trellis 记录；不改 row 展开、bulk actions、文件打开、diff hover、分组算法、Store/Rust/Tauri response。优先级中高：用户价值明确、出现频率高（group 是批量工具调用常见形态）、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/BashToolGroupBlock.tsx` 新增 `tools.bashGroupDetailsToggle`，目标摘要取首条命令或命令数；`ReadToolGroupBlock.tsx` 新增 `tools.readGroupDetailsToggle`，目标摘要取首个读取文件或文件数；`EditToolGroupBlock.tsx` 新增 `tools.editGroupDetailsToggle`，目标摘要取首个编辑文件；`SearchToolGroupBlock.tsx` 新增 `tools.searchGroupDetailsToggle`，目标摘要取搜索 pattern/query。四个 header 均补 `title` / `aria-label`，保留原 click、keydown、compact class、状态指示和 chevron。`src/components/toolBlocks/*GroupBlock.test.tsx` 四个测试文件分别新增 SSR 断言。`src/locales/en.json` / `src/locales/zh.json` 新增四组 toggle 文案。`.trellis/spec/frontend/component-guidelines.md` 补充 grouped tool headers 也必须使用 action + representative target 标签。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx` 先失败 4 项，失败点均为 group header 缺少 `title="tools.*GroupDetailsToggle: <target>"` / `aria-label`，符合旧实现缺口。GREEN 定向通过：同命令通过（4 files / 15 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/SubagentHistoryPanel.test.tsx src/components/toolBlocks/SubagentProcessOpenFilePropagation.test.tsx src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（18 files / 92 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（54 files / 473 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。`git diff --check` exit 0；`git diff --cached --check` exit 0；二者仅输出 Windows LF/CRLF 转换提示。
- 未验证项：未启动真实 Tauri 桌面触发 Bash/Read/Edit/Search group header；未手动确认 group header tooltip、Tab 焦点、Enter/Space 展开/折叠和辅助技术播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Bash/Read/Edit/Search/Agent/Task group 与子代理历史，点测 group header label、row 展开、文件按钮、tooltip、键盘焦点、点击路径/行号；2) 对 ToolBlock 可访问性做运行态扫描，核对所有可展开入口和操作按钮的 `role/button/tabindex/aria-expanded/title/aria-label/onKeyDown` 覆盖；3) 审计 grouped Bash/Read/Search 的状态聚合和特殊工具归类，确认是否存在更多误导性 error/pending 状态；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility。

## 本轮 PLAN 2026-06-20 tool group item row toggle accessible names

- 目标：给 `BashToolGroupBlock` / `ReadToolGroupBlock` / `EditToolGroupBlock` / `SearchToolGroupBlock` 的 group item row 展开入口增加稳定 `title` / `aria-label`，延续上一轮 group header 的 action + target 语义。当前 row 已有 `role="button"`、`tabIndex` 和 `aria-expanded`，但辅助技术用户仍需要从可见片段推断“这是切换哪条命令/文件/搜索详情”。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目四个 group block 与对标项目 group/list 实现；确认本轮只补 group item row 的 `title` / `aria-label` 与 i18n，不改 row 展开状态、bulk actions、文件打开、diff hover、搜索结果详情、分组算法、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展四个 group block 测试，SSR 断言 `.task-group-item-header` row 暴露 `title` / `aria-label`，并包含行级 action 与具体命令/文件/搜索目标；旧实现应因属性缺失失败。
- 功能点 4：实现最小交互修复。验证方式：为 Bash/Read/Edit/Search row 计算 item toggle label，目标摘要分别使用命令摘要、读取文件路径、编辑文件路径和搜索 query/结果摘要；添加到 row `title` / `aria-label`，保留现有 click、keydown、内部文件按钮、状态 pill 和 chevron。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，明确 grouped tool item rows 也必须使用 action + item target 标签，不能只依赖 `aria-expanded` 和可见行文本。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 tool group item row toggle accessible names

- 上轮进展与阻塞：上一轮完成 `BashToolGroupBlock` / `ReadToolGroupBlock` / `EditToolGroupBlock` / `SearchToolGroupBlock` 分组 header 的稳定 `title` / `aria-label`，代码中确实存在 `tools.*GroupDetailsToggle` 标签和代表性 target 摘要。更早的 Generic/Read/Edit header 文件目标 button 化、详情 `Open File` action + target 标签、Search 文件按钮、Copy Input/Output/Path 稳定标签、Task/Agent header、group row open-file 冒泡边界、子代理过程 file chip / tool row target 事件边界等改动也仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的 group/list 展示会把文件、命令或搜索目标作为主要 row 信息；本项目 group row 进一步支持逐行展开，但此前 row 只有 `role="button"` / `tabIndex` / `aria-expanded`，缺少稳定 action label。用户价值：读屏或 hover 用户进入分组后，能直接知道当前 row 是“切换哪条命令/读取/编辑/搜索详情”，不必从多个可见片段拼接推断。
- 本轮差距诊断（超越 cc-gui）：本项目拥有 cc-gui 不完全相同的可展开 row 模型，并且 row 内还可能嵌套文件打开按钮。本轮让“行级展开”和“内部文件打开”分别拥有稳定名称：`Toggle ... details: target` 与 `Open file: target`。用户价值：同一行里的两个动作边界更清楚，键盘和辅助技术用户不会把展开详情和打开文件混淆。
- 本轮规划：只改四个 group block 的 row `title` / `aria-label`、四个 SSR 回归、i18n 文案、前端组件规范和 Trellis 记录；不改 row 展开状态、bulk actions、文件打开、diff hover、搜索结果详情、分组算法、Store/Rust/Tauri response。优先级中高：用户价值明确、出现频率高（group 是批量工具调用常见形态）、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/BashToolGroupBlock.tsx` 为命令 row 新增 `tools.bashGroupItemDetailsToggle`，目标取命令摘要；`ReadToolGroupBlock.tsx` 为读取 row 新增 `tools.readGroupItemDetailsToggle`，目标取文件 display path；`EditToolGroupBlock.tsx` 为编辑 row 新增 `tools.editGroupItemDetailsToggle`，目标取编辑文件路径；`SearchToolGroupBlock.tsx` 为搜索 row 新增 `tools.searchGroupItemDetailsToggle`，目标优先取搜索 query，缺失时回退首个文件或工具名。四个 row 均补 `title` / `aria-label`，保留原 click、keydown、内部 open-file button、状态 pill 和 chevron。`src/components/toolBlocks/*GroupBlock.test.tsx` 四个测试文件分别新增 SSR 断言。`src/locales/en.json` / `src/locales/zh.json` 新增四组 row toggle 文案，并整理本轮附近 toggle key 缩进。`.trellis/spec/frontend/component-guidelines.md` 补充 grouped tool item rows 也必须使用 action + item target 标签。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx` 先失败 4 项，失败点均为 row 缺少 `title="tools.*GroupItemDetailsToggle: <target>"` / `aria-label`，符合旧实现缺口。GREEN 定向通过：同命令通过（4 files / 19 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/SubagentHistoryPanel.test.tsx src/components/toolBlocks/SubagentProcessOpenFilePropagation.test.tsx src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（18 files / 96 tests）。全量前端通过：`npm test`（54 files / 477 tests，仅既有 Browserslist/caniuse-lite 过期提示；locale 缩进整理后重新跑也通过）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示；locale 缩进整理后重新跑也通过）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。`git diff --check` exit 0；`git diff --cached --check` exit 0；二者仅输出 Windows LF/CRLF 转换提示。
- 未验证项：未启动真实 Tauri 桌面触发 Bash/Read/Edit/Search group row；未手动确认 row tooltip、Tab 焦点、Enter/Space 展开/折叠、内部文件按钮打开文件且不误触 row；未用屏幕阅读器确认 row toggle label 和内部文件按钮 label 的实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Bash/Read/Edit/Search/Agent/Task group 与子代理历史，点测 group header、group item row、内部文件按钮、tooltip、键盘焦点、点击路径/行号和 diff hover；2) 对 ToolBlock 可访问性做运行态扫描，核对所有可展开入口和操作按钮的 `role/button/tabindex/aria-expanded/title/aria-label/onKeyDown` 覆盖；3) 审计 grouped Bash/Read/Search 的状态聚合和特殊工具归类，确认是否存在更多误导性 error/pending 状态；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility。

## 本轮 PLAN 2026-06-20 single tool header toggle accessible names

- 目标：给 `GenericToolBlock` / `BashToolBlock` / `ReadToolBlock` / `EditToolBlock` 的单个工具块 header 增加稳定 `title` / `aria-label`，让单块、group header、group item row 的可展开入口都采用 action + target 语义。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目四个 single tool block 与对标项目 single block 实现；确认本轮只补 header toggle 的 `title` / `aria-label` 与 i18n，不改展开状态、文件打开、复制动作、diff hover、状态计算、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展四个 single block 测试，SSR 断言可展开 header 暴露 `title` / `aria-label`，并包含动作与具体命令/文件/搜索目标；旧实现应因属性缺失失败。
- 功能点 4：实现最小交互修复。验证方式：为 Generic/Bash/Read/Edit header 计算 toggle label，目标摘要分别使用 generic action summary、命令摘要、读取文件路径和编辑文件路径；添加到 header `title` / `aria-label`，保留现有 click、keydown、内部文件按钮、状态 indicator 和 compact class。
- 功能点 5：同步 Trellis 规范。验证方式：`.trellis/spec/frontend/component-guidelines.md` 已有 expandable chat tool-block headers 必须暴露稳定 action label 的合同；本轮如需则补充 single block examples。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 single tool header toggle accessible names

- 上轮进展与阻塞：上一轮完成 `BashToolGroupBlock` / `ReadToolGroupBlock` / `EditToolGroupBlock` / `SearchToolGroupBlock` 的 group item row `title` / `aria-label`，代码中确实存在 `tools.*GroupItemDetailsToggle` 与行级 target 摘要。更早的 group header toggle label、Generic/Read/Edit header 文件目标 button 化、详情 `Open File` action + target 标签、Search 文件按钮、Copy Input/Output/Path 稳定标签、Task/Agent header 等改动也仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的工具块 header 是用户识别工具详情入口的核心区域；本项目单个 `GenericToolBlock` / `BashToolBlock` / `ReadToolBlock` / `EditToolBlock` 虽已有 `role="button"`、`tabIndex` 和 `aria-expanded`，但父级 header 缺少稳定动作名。用户价值：读屏和 hover 用户能直接听到/看到“切换哪一个工具/命令/读取/编辑详情”，不需要从图标、状态点和内部文件按钮拼接推断。
- 本轮差距诊断（超越 cc-gui）：本项目已把 group header、group item row、内部文件打开按钮拆成不同动作语义；本轮把单块 header 也纳入同一合同。用户价值：同一屏混合单块和分组工具时，展开详情与打开文件两个动作边界一致，降低键盘和辅助技术用户误触成本。
- 本轮规划：只改 `GenericToolBlock` / `BashToolBlock` / `ReadToolBlock` / `EditToolBlock` 的父级 header toggle label、对应 SSR 测试和 i18n 文案；不改展开状态、文件打开、复制动作、diff hover、状态计算、Store/Rust/Tauri response。优先级中高：用户价值明确、出现频率高、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/GenericToolBlock.tsx` 在存在可展开内容时新增 `tools.genericDetailsToggle`，目标取 action summary 或工具名，并仅在 header 具备 button 语义时输出 `title` / `aria-label`。`src/components/toolBlocks/BashToolBlock.tsx` 新增 `tools.bashDetailsToggle`，目标取命令摘要。`src/components/toolBlocks/ReadToolBlock.tsx` 新增 `tools.readDetailsToggle`，目标取读取文件 display path。`src/components/toolBlocks/EditToolBlock.tsx` 新增 `tools.editDetailsToggle`，目标取编辑文件 display path。`src/locales/en.json` / `src/locales/zh.json` 新增四组单块 header toggle 文案。`src/components/toolBlocks/GenericToolBlock.test.tsx`、`BashToolBlock.test.tsx`、`ReadToolBlock.test.tsx`、`EditToolBlock.test.tsx` 已覆盖四个 header 的 SSR 断言。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx` 在实现前已失败 4 项，失败点均为 header 缺少 `title` / `aria-label`，符合旧实现缺口。GREEN 定向通过：同命令通过（4 files / 20 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/SubagentHistoryPanel.test.tsx src/components/toolBlocks/SubagentProcessOpenFilePropagation.test.tsx src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（18 files / 100 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（54 files / 481 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。`git diff --check` exit 0；`git diff --cached --check` exit 0；二者仅输出 Windows LF/CRLF 转换提示。
- 未验证项：未启动真实 Tauri 桌面触发 Generic/Bash/Read/Edit 单块 header；未手动确认 header tooltip、Tab 焦点、Enter/Space 展开/折叠、内部文件按钮打开文件且不误触 header；未用屏幕阅读器确认单块 header label 和内部文件按钮 label 的实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Generic/Bash/Read/Edit/Search/Agent/Task 单块与分组，点测 header、group item row、内部文件按钮、tooltip、键盘焦点、点击路径/行号和 diff hover；2) 对 ToolBlock 可访问性做运行态扫描，核对所有可展开入口和操作按钮的 `role/button/tabindex/aria-expanded/title/aria-label/onKeyDown` 覆盖；3) 审计 grouped Bash/Read/Search 的状态聚合和特殊工具归类，确认是否存在更多误导性 error/pending 状态；4) 按主题整理提交：大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility。

## 本轮 PLAN 2026-06-20 single read edit copy path target labels

- 目标：把 `ReadToolBlock` / `EditToolBlock` 详情区 `Copy Path` 按钮从泛化 `tools.copyPath` 标签升级为 `Copy path: path` 的 action + target 标签。cc-gui 的 Read/Edit 单块主要提供文件点击；本项目已经超越出详情区 Copy Path，但当前多个文件工具块同屏时按钮名称不可区分。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ReadToolBlock.tsx` / `EditToolBlock.tsx` 与对标项目 Read/Edit 单块；确认本轮只补详情区 Copy Path 的 `title` / `aria-label`，不改复制内容、按钮可见文案、Open File、header toggle、文件目标按钮、diff hover、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：更新 `ReadToolBlock.test.tsx` 与 `EditToolBlock.test.tsx`，SSR 断言展开态详情区 Copy Path 按钮的 `title` / `aria-label` 包含具体路径；旧实现应因仍为裸 `tools.copyPath` 失败。
- 功能点 4：实现最小交互修复。验证方式：复用已有 `copyPathForPath` locale key，新增目标化 `copyPathLabel` 用于 `title` / `aria-label`，按钮可见文案仍使用 `tools.copyPath`，复制值仍为原 `filePath`。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，明确复制路径类动作按钮也必须在 `title` / `aria-label` 中包含 action + target，但可见文案可以保持紧凑。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 single read edit copy path target labels

- 上轮进展与阻塞：上一轮完成 `GenericToolBlock` / `BashToolBlock` / `ReadToolBlock` / `EditToolBlock` 单块 header 的稳定 `title` / `aria-label`，代码中确实存在 `tools.genericDetailsToggle` / `tools.bashDetailsToggle` / `tools.readDetailsToggle` / `tools.editDetailsToggle` 与具体 target 摘要。更早的 header 文件目标 button 化、详情 `Open File` action + target 标签、group header 和 group item row toggle 标签、Read/Edit/Search group 内部 open-file 事件边界也仍在代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui Read/Edit 单块围绕文件目标提供清晰可点击入口；本项目已经额外提供详情区 `Copy Path`，但按钮 `title` / `aria-label` 仍是裸 `tools.copyPath`。用户价值：展开多个 Read/Edit 工具块时，用户能直接区分要复制哪个文件路径，避免从多个同名按钮中猜测。
- 本轮差距诊断（超越 cc-gui）：本项目的详情区同时有 `Open File` 和 `Copy Path` 两种文件动作，本轮让复制动作也采用 action + target 语义，和 `Open file: path` 对齐。用户价值：键盘、hover 和辅助技术用户在不阅读周围上下文的情况下，也能确定当前按钮的具体目标。
- 本轮规划：只改 `ReadToolBlock` / `EditToolBlock` 详情区 `Copy Path` 的 `title` / `aria-label`、两个 SSR 回归和前端组件规范；不改复制内容、按钮可见文案、Open File、header toggle、文件目标按钮、diff hover、Store/Rust/Tauri response。优先级中高：用户价值明确、Read/Edit 出现频率高、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/ReadToolBlock.tsx` 新增 `copyPathButtonLabel` 与 `copyPathActionLabel`，按钮可见文案保持 `tools.copyPath`，`title` / `aria-label` 改为 `tools.copyPathForPath: <displayPath>`，复制值仍为原 `filePath`。`src/components/toolBlocks/EditToolBlock.tsx` 做同样处理，目标取 `displayPath || filePath`。`src/components/toolBlocks/ReadToolBlock.test.tsx` 与 `src/components/toolBlocks/EditToolBlock.test.tsx` 更新 i18n mock 支持 `{ file }`，并覆盖目标化 Copy Path 标签。`.trellis/spec/frontend/component-guidelines.md` 补充：复制文件路径类动作按钮也必须在 `title` / `aria-label` 中包含 action + target，可见文案可以保持紧凑。`TODO_LIST.md` 已追加本轮 PLAN 与记录。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx` 先失败 2 项，失败点均为详情区 Copy Path 仍输出 `title="tools.copyPath" aria-label="tools.copyPath"`，符合旧实现缺口。GREEN 定向通过：同命令通过（2 files / 10 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/SubagentHistoryPanel.test.tsx src/components/toolBlocks/SubagentProcessOpenFilePropagation.test.tsx src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（18 files / 100 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（54 files / 481 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 Read/Edit 单块详情区；未手动确认 Copy Path tooltip、复制结果和 copied 反馈；未用屏幕阅读器确认 `Copy path: path` 实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 审计 Generic/Bash 的 Copy Input/Copy Output/Copy Command 是否也需要 action + representative target（例如命令摘要或工具名）以降低同屏多按钮歧义；2) 真实 Tauri desktop smoke：触发 Generic/Bash/Read/Edit/Search/Agent/Task 单块与分组，点测 header、group item row、内部文件按钮、Copy Path、Copy Input/Output、tooltip、键盘焦点、点击路径/行号和 diff hover；3) 对 ToolBlock 可访问性做运行态扫描，核对所有可展开入口和操作按钮的 `role/button/tabindex/aria-expanded/title/aria-label/onKeyDown` 覆盖；4) 审计 grouped Bash/Read/Search 的状态聚合和特殊工具归类，确认是否存在更多误导性 error/pending 状态。

## 本轮 PLAN 2026-06-20 bash copy action target labels

- 目标：把 `BashToolBlock` 详情区 `Copy Command` / `Copy Output` 按钮从泛化标签升级为包含命令 target 的 action label，例如 `Copy command: npm test` / `Copy output from command: npm test`。上一轮已完成 Read/Edit `Copy Path: path`，Bash 是同类高频操作。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `BashToolBlock.tsx` / `BashToolBlock.test.tsx` 与对标项目 Bash 单块；确认本轮只补 Bash 详情区 Copy Command / Copy Output 的 `title` / `aria-label` 和 i18n，不改复制内容、按钮可见文案、header toggle、命令摘要、结果解析、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：更新 `BashToolBlock.test.tsx`，SSR 断言展开态 copy command / copy output 按钮的 `title` / `aria-label` 包含命令摘要 `npm test`；旧实现应因仍为裸 `tools.copyCommand` / `tools.copyOutput` 失败。
- 功能点 4：实现最小交互修复。验证方式：新增 `copyCommandForCommand` / `copyOutputForCommand` i18n key；组件内拆分可见按钮文案和 action label，`title` / `aria-label` 用目标化 label，按钮可见文案仍保持 `tools.copyCommand` / `tools.copyOutput`，复制值不变。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，明确复制命令/输出类动作按钮也应在可识别 target 存在时包含 action + representative target。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 bash copy action target labels

- 上轮进展与阻塞：上一轮完成 `ReadToolBlock` / `EditToolBlock` 详情区 `Copy Path` 的目标化 `title` / `aria-label`，代码中确实存在 `tools.copyPathForPath: <path>`，且复制值和可见按钮文案保持不变。更早的单块 header toggle label、group header / group item row toggle label、文件打开 action + target 标签和 open-file 冒泡边界也仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的 Bash 单块在 `webview/src/components/toolBlocks/BashToolBlock.tsx` 中以命令和输出为核心展示详情；本项目已额外提供 `Copy Command` / `Copy Output`，但此前按钮 `title` / `aria-label` 仍是裸 `tools.copyCommand` / `tools.copyOutput`。用户价值：多个 Bash 工具块同屏展开时，用户能直接知道复制的是哪条命令或哪条命令的输出。
- 本轮差距诊断（超越 cc-gui）：本项目已有命令摘要 `summarizeCommand()`，本轮把该摘要复用于复制动作的可访问名称，同时保留紧凑可见文案。用户价值：键盘、hover 和辅助技术用户无需阅读周边命令块，也能从按钮自身获得“动作 + 目标”。
- 本轮规划：只改 `BashToolBlock` 详情区 `Copy Command` / `Copy Output` 的 `title` / `aria-label`、一个 SSR 回归、英文/中文 i18n key、前端组件规范和 Trellis 记录；不改复制内容、按钮可见文案、header toggle、命令摘要、Bash result 解析、Store/Rust/Tauri response。优先级中高：用户价值明确、Bash 高频、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/BashToolBlock.tsx` 新增 `copyCommandButtonLabel` / `copyOutputButtonLabel` 与 `copyCommandActionLabel` / `copyOutputActionLabel`，按钮可见文案仍为 `tools.copyCommand` / `tools.copyOutput`，复制成功时仍显示 `tools.copied`；`title` / `aria-label` 改为 `tools.copyCommandForCommand: <command target>` 与 `tools.copyOutputForCommand: <command target>`，复制的命令和输出字符串不变。`src/components/toolBlocks/BashToolBlock.test.tsx` 的 i18n mock 支持 `{ target }`，并覆盖目标化 Copy Command / Copy Output 标签。`src/locales/en.json` / `src/locales/zh.json` 新增 `copyCommandForCommand` 与 `copyOutputForCommand`。`.trellis/spec/frontend/component-guidelines.md` 已补充 Bash 命令/输出复制动作也应带代表性命令 target。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/BashToolBlock.test.tsx` 在实现前已失败 1 项，失败点为详情区按钮仍输出 `title="tools.copyCommand" aria-label="tools.copyCommand"` 和 `title="tools.copyOutput" aria-label="tools.copyOutput"`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 3 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/SubagentHistoryPanel.test.tsx src/components/toolBlocks/SubagentProcessOpenFilePropagation.test.tsx src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（18 files / 100 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（54 files / 481 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 Bash 单块详情区；未手动确认 Copy Command / Copy Output tooltip、复制结果和 copied 反馈；未用屏幕阅读器确认 `Copy command: <target>` / `Copy output from command: <target>` 实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 审计 `GenericToolBlock` 的 `Copy Input` / `Copy Output` 是否也需要 action + representative target，例如 `Copy input for tool: AskUserQuestion` / `Copy output from tool: ToolName`，避免多个 generic 工具同屏时按钮同名；2) 真实 Tauri desktop smoke：触发 Generic/Bash/Read/Edit/Search/Agent/Task 单块与分组，点测 header、group item row、内部文件按钮、Copy Path、Copy Command、Copy Input/Output、tooltip、键盘焦点、点击路径/行号和 diff hover；3) 对 ToolBlock 可访问性做运行态扫描，核对所有可展开入口和操作按钮的 `role/button/tabindex/aria-expanded/title/aria-label/onKeyDown` 覆盖；4) 审计 grouped Bash/Read/Search 的状态聚合和特殊工具归类，确认是否存在更多误导性 error/pending 状态。

## 本轮 PLAN 2026-06-20 generic copy action target labels

- 目标：把 `GenericToolBlock` 详情区 `Copy Input` / `Copy Output` 按钮从泛化标签升级为包含代表性 target 的 action label，例如 `Copy input for tool: chat ui parity` / `Copy output from tool: chat ui parity`。上一轮已完成 Bash `Copy Command` / `Copy Output`，Generic 是同类剩余高频入口。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `GenericToolBlock.tsx` / `GenericToolBlock.test.tsx` 与对标项目 Generic 单块；确认本轮只补 Generic 详情区 Copy Input / Copy Output 的 `title` / `aria-label` 和 i18n，不改复制内容、按钮可见文案、header toggle、文件打开、结果解析、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：更新 `GenericToolBlock.test.tsx`，SSR 断言展开态 Copy Input / Copy Output 按钮的 `title` / `aria-label` 包含代表性 target `chat ui parity`；旧实现应因仍为裸 `tools.copyInput` / `tools.copyOutput` 失败。
- 功能点 4：实现最小交互修复。验证方式：新增 `copyInputForTool` / `copyOutputForTool` i18n key；组件内拆分可见按钮文案和 action label，`title` / `aria-label` 用目标化 label，按钮可见文案仍保持 `tools.copyInput` / `tools.copyOutput`，复制值不变。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，明确 Generic Copy Input / Copy Output 这类工具输入/输出复制动作也应在可识别 target 存在时包含 action + representative target。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 generic copy action target labels

- 上轮进展与阻塞：上一轮完成 `BashToolBlock` 详情区 `Copy Command` / `Copy Output` 的目标化 `title` / `aria-label`，代码中确实存在 `tools.copyCommandForCommand: <target>` 与 `tools.copyOutputForCommand: <target>`，复制值和可见按钮文案保持不变。更早的 Read/Edit `Copy Path` 目标化标签、单块 header toggle label、group header / group item row toggle label、文件打开 action + target 标签和 open-file 冒泡边界也仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的 Generic 工具块核心是把工具名、输入和输出清楚分区；本项目已额外提供 Copy Input / Copy Output，但此前按钮 `title` / `aria-label` 仍是裸 `tools.copyInput` / `tools.copyOutput`。用户价值：多个 Generic 工具块同屏展开时，用户能直接知道复制的是哪个工具目标的输入或输出。
- 本轮差距诊断（超越 cc-gui）：本项目已经用 `summarizeGenericTool()` 提取 query、file target、command 或工具名摘要。本轮把同一代表性 target 复用于复制动作可访问名称，而不是只依赖周边上下文。用户价值：键盘、hover 和辅助技术用户在不阅读参数表的情况下，也能从按钮自身获得“动作 + 目标”。
- 本轮规划：只改 `GenericToolBlock` 详情区 `Copy Input` / `Copy Output` 的 `title` / `aria-label`、一个 SSR 回归、英文/中文 i18n key、前端组件规范和 Trellis 记录；不改复制内容、按钮可见文案、header toggle、文件打开、结果解析、Store/Rust/Tauri response。优先级中高：用户价值明确、Generic 工具覆盖 AskUserQuestion/WebSearch/未知工具等高频兜底场景、实现成本低、回归风险低。
- 本轮完成：`src/components/toolBlocks/GenericToolBlock.tsx` 复用 `headerToggleTarget` 生成 `copyInputActionLabel` 与 `copyOutputActionLabel`，按钮可见文案仍为 `tools.copyInput` / `tools.copyOutput`，复制成功时仍显示 `tools.copied`；`title` / `aria-label` 改为 `tools.copyInputForTool: <target>` 与 `tools.copyOutputForTool: <target>`，复制的 JSON input 和格式化 output 字符串不变。`src/components/toolBlocks/GenericToolBlock.test.tsx` 更新 Copy Input / Copy Output SSR 断言。`src/locales/en.json` / `src/locales/zh.json` 新增 `copyInputForTool` 与 `copyOutputForTool`。`.trellis/spec/frontend/component-guidelines.md` 已补充 Generic 工具输入/输出复制动作也应带 header representative target。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/GenericToolBlock.test.tsx` 先失败 1 项，失败点为详情区按钮仍输出 `title="tools.copyInput" aria-label="tools.copyInput"` 和 `title="tools.copyOutput" aria-label="tools.copyOutput"`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 7 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/SubagentHistoryPanel.test.tsx src/components/toolBlocks/SubagentProcessOpenFilePropagation.test.tsx src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（18 files / 100 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（54 files / 481 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 Generic 单块详情区；未手动确认 Copy Input / Copy Output tooltip、复制结果和 copied 反馈；未用屏幕阅读器确认 `Copy input for tool: <target>` / `Copy output from tool: <target>` 实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Generic/Bash/Read/Edit/Search/Agent/Task 单块与分组，点测 header、group item row、内部文件按钮、Copy Path、Copy Command、Copy Input/Output、tooltip、键盘焦点、点击路径/行号和 diff hover；2) 对 ToolBlock 可访问性做运行态扫描，核对所有可展开入口和操作按钮的 `role/button/tabindex/aria-expanded/title/aria-label/onKeyDown` 覆盖；3) 审计 grouped Bash/Read/Search 的状态聚合和特殊工具归类，确认是否存在更多误导性 error/pending 状态；4) 开始收束同主题提交边界：把大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility 拆成可审查提交批次。

## 本轮 PLAN 2026-06-20 search group status summary

- 目标：让 `SearchToolGroupBlock` 在分组 header 折叠态也展示状态构成，补上 Bash group 已有的成功/失败/pending 摘要能力。当前 Search group header 只有搜索词和一个状态点；当批量搜索部分完成、部分失败或仍有 pending 时，用户必须展开逐项检查。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `SearchToolGroupBlock.tsx` / `BashToolGroupBlock.tsx` / `toolGrouping.ts` 与 cc-gui group 实现；确认本轮只补 Search group header 状态摘要与 target label，不改分组算法、搜索结果解析、文件打开、GenericToolBlock 展开详情、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：更新 `SearchToolGroupBlock.test.tsx`，断言混合状态下 header 可见 `tools.success · 1 tools.failed · 1 tools.pending`，且 `title` / `aria-label` 的 target 包含搜索词和状态摘要；旧实现应因缺少摘要失败。
- 功能点 4：实现最小展示修复。验证方式：在组件内计算 completed/error/pending 计数，复用既有 i18n key 组合成 header secondary summary，并把 group toggle target 改为 `search target · status summary`；不新增依赖、不改变复制或打开文件行为。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，明确 tool group header 对混合状态应在折叠态提供可读计数摘要，避免只靠颜色点表达状态。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 search group status summary

- 上轮进展与阻塞：上一轮完成 `GenericToolBlock` 详情区 `Copy Input` / `Copy Output` 的目标化 `title` / `aria-label`，代码中确实存在 `tools.copyInputForTool: <target>` 与 `tools.copyOutputForTool: <target>`，复制值和可见按钮文案保持不变。更早的 Read/Edit `Copy Path`、Bash `Copy Command` / `Copy Output`、单块/group header toggle label、文件打开 action + target 标签和 open-file 冒泡边界也仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的 Search group 代码（`webview/src/components/toolBlocks/SearchToolGroupBlock.tsx`）能在展开列表中呈现每项 pending/completed/error 状态；本项目也有每行状态 pill 和 group status dot，但 Search group header 折叠态此前只显示搜索词，混合状态下需要展开逐项扫描。用户价值：批量搜索部分失败或仍在执行时，用户无需展开就能判断是否需要介入。
- 本轮差距诊断（超越 cc-gui）：本项目已经为 Bash group header 展示成功/失败/pending 摘要，本轮把同一折叠态扫描能力补到 Search group，并把状态摘要纳入 header toggle 的 `title` / `aria-label` target。用户价值：视觉用户、键盘用户和辅助技术用户都能从同一个 header 获得“搜索目标 + 执行构成”，减少只靠颜色点判断状态的歧义。
- 本轮规划：只改 `SearchToolGroupBlock` header 状态摘要、`SearchToolGroupBlock.test.tsx` SSR 回归、前端组件规范和 Trellis 记录；不改分组算法、搜索结果解析、文件打开、GenericToolBlock 展开详情、Store/Rust/Tauri response。优先级中高：批量 Search 高频，折叠态误判成本较高，实现成本低，回归风险低。
- 本轮完成：`src/components/toolBlocks/SearchToolGroupBlock.tsx` 新增 `summarizeSearchGroupStatus()` 与 `formatSearchGroupStatusSummary()`，按结果计算 completed/error/pending 计数；header 新增 `tool-title-secondary-summary`，混合状态示例显示 `tools.success · 1 tools.failed · 1 tools.pending`；group header `title` / `aria-label` 的 target 改为 `useState · tools.success · 1 tools.failed · 1 tools.pending`，让聚焦和 hover 时也能获得状态构成。`src/components/toolBlocks/SearchToolGroupBlock.test.tsx` 的 mock 数据改为 1 成功、1 失败、1 pending，并新增 header 状态摘要断言。`.trellis/spec/frontend/component-guidelines.md` 已补充 grouped tool header 混合状态不能只依赖颜色点，应提供可读计数摘要。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/SearchToolGroupBlock.test.tsx` 先失败 2 项，失败点为 header 仍输出 `tools.searchGroupDetailsToggle: useState` 且没有 `tool-title-secondary-summary`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 6 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/SubagentHistoryPanel.test.tsx src/components/toolBlocks/SubagentProcessOpenFilePropagation.test.tsx src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（18 files / 101 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（54 files / 482 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 Search group；未手动确认折叠态 header 状态摘要、tooltip、键盘聚焦播报与屏幕阅读器实际输出；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Generic/Bash/Read/Edit/Search/Agent/Task 单块与分组，点测 header、group item row、内部文件按钮、Copy Path、Copy Command、Copy Input/Output、Search 状态摘要、tooltip、键盘焦点、点击路径/行号和 diff hover；2) 对 ToolBlock 可访问性做运行态扫描，核对所有可展开入口和操作按钮的 `role/button/tabindex/aria-expanded/title/aria-label/onKeyDown` 覆盖；3) 审计 Read/Edit group header 是否也应该像 Bash/Search 一样暴露 mixed status count；4) 开始收束同主题提交边界：把大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility 拆成可审查提交批次。

## 本轮 PLAN 2026-06-20 read edit group status summary

- 目标：把上一轮 Search group 的折叠态混合状态摘要推广到 `ReadToolGroupBlock` / `EditToolGroupBlock`，并把状态计数逻辑收敛到共享工具，避免多个 group block 复制本地 completed/error/pending 计数。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `ReadToolGroupBlock.tsx` / `EditToolGroupBlock.tsx` / `SearchToolGroupBlock.tsx` / `toolGrouping.ts` 与 cc-gui Read/Edit group；确认本轮只补 group header 状态摘要和共享计数工具，不改 row 展开、bulk actions、文件打开、diff hover、分组算法、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展 `ReadToolGroupBlock.test.tsx` / `EditToolGroupBlock.test.tsx`，让 mock 数据覆盖 1 completed、1 error、1 pending，断言 header 可见状态摘要并纳入 group toggle `title` / `aria-label`；旧实现应因缺少摘要失败。给 `toolGrouping.test.ts` 补共享状态计数测试。
- 功能点 4：实现最小展示修复。验证方式：在 `toolGrouping.ts` 新增 status summary helper；Read group 基于 raw blocks 计数并把 `3 files · Success · 1 Failed · 1 Pending` 显示在 secondary summary；Edit group 基于 `editItems` 这些可见文件行计数并新增 secondary summary。Search group 改用共享 helper，保留上一轮行为。
- 功能点 5：复核 Trellis 规范。验证方式：`.trellis/spec/frontend/component-guidelines.md` 已有 mixed group header 状态摘要合同；本轮如只落地同一合同，不重复扩写。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 read edit group status summary

- 上轮进展与阻塞：上一轮完成 `SearchToolGroupBlock` 折叠态混合状态摘要，代码中确实存在 header `tool-title-secondary-summary` 和 `title` / `aria-label` 的 `search target · status summary` 组合。更早的 Generic/Bash/Read/Edit copy action target 标签、单块/group header toggle label、文件打开 action + target 标签和 open-file 冒泡边界也仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报、cc-gui 真实运行并排截图未完成，以及 IDE build 120 秒超时。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的 Read/Edit group 在展开列表中能呈现每项状态；本项目 Read/Edit group 也有每行状态 pill 和 group 状态点，但折叠态此前只显示文件/数量或 diff 汇总，混合状态下需要展开逐项扫描。用户价值：批量 Read/Edit 中如果部分失败或仍 pending，用户不用展开列表就能判断是否需要处理。
- 本轮差距诊断（超越 cc-gui）：本项目已具备比 cc-gui 更强的 group header action label、文件按钮、diff hover 和 Search/Bash 状态摘要。本轮把相同折叠态扫描能力推广到 Read/Edit，并把状态计数逻辑抽成共享工具，避免 Search/Read/Edit 分别复制本地计数。用户价值：所有高频工具分组的状态表达一致，用户在连续工具调用中不需要切换阅读模型。
- 本轮规划：只改 `toolGrouping.ts` 状态摘要工具、`ReadToolGroupBlock` / `EditToolGroupBlock` / `SearchToolGroupBlock` 的 header 状态摘要接入、三个测试文件和 Trellis 记录；不改 row 展开、bulk actions、文件打开、diff hover、分组算法、Store/Rust/Tauri response。优先级中高：用户价值明确，Read/Edit 分组出现频率高，复用 Search 的既定合同，回归风险低。
- 本轮完成：`src/utils/toolGrouping.ts` 新增 `ToolExecutionStatusSummary`、`summarizeToolExecutionStatuses()`、`summarizeToolResultStatuses()` 和 `formatToolExecutionStatusSummary()`；`ReadToolGroupBlock.tsx` 在 header secondary summary 中显示 `3 files · tools.success · 1 tools.failed · 1 tools.pending`，并把同样摘要纳入 group toggle `title` / `aria-label` target；`EditToolGroupBlock.tsx` 基于 `editItems` 可见文件行计数，新增 `tool-title-secondary-summary` 和 header target 状态摘要；`SearchToolGroupBlock.tsx` 移除本地计数 helper，改用共享工具并保持上一轮行为。`ReadToolGroupBlock.test.tsx` / `EditToolGroupBlock.test.tsx` 的 mock 数据扩展为 1 completed、1 error、1 pending，并新增 header 状态摘要断言。`toolGrouping.test.ts` 新增共享状态计数和格式化测试。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/utils/toolGrouping.test.ts` 先失败 6 项，失败点为共享 helper 未导出，Read/Edit header 仍缺状态摘要，符合旧实现缺口。GREEN 定向通过：`npm test -- src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/utils/toolGrouping.test.ts`（4 files / 21 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/SubagentHistoryPanel.test.tsx src/components/toolBlocks/SubagentProcessOpenFilePropagation.test.tsx src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（18 files / 105 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（54 files / 486 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。`git diff --check` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。
- 未验证项：未启动真实 Tauri 桌面触发 Read/Edit/Search group；未手动确认折叠态 header 状态摘要、tooltip、键盘聚焦播报与屏幕阅读器实际输出；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Generic/Bash/Read/Edit/Search/Agent/Task 单块与分组，点测 header、group item row、内部文件按钮、Copy Path、Copy Command、Copy Input/Output、Read/Edit/Search 状态摘要、tooltip、键盘焦点、点击路径/行号和 diff hover；2) 对 ToolBlock 可访问性做运行态扫描，核对所有可展开入口和操作按钮的 `role/button/tabindex/aria-expanded/title/aria-label/onKeyDown` 覆盖；3) 审计 Bash group header 是否应从“完成时只显示 Success”升级为和共享 helper 一致的 completed 计数显示；4) 开始收束同主题提交边界：把大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility 拆成可审查提交批次。

## 本轮 PLAN 2026-06-20 bash group status summary

- 目标：审计并统一 `BashToolGroupBlock` 折叠态状态摘要，让 Bash group 和 Read/Edit/Search group 使用同一类可读执行构成表达；当多个命令成功、部分失败或 pending 时，header 和 toggle label 都能直接说明状态构成。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `BashToolGroupBlock.tsx` / `BashToolGroupBlock.test.tsx` / `toolGrouping.ts` 与 cc-gui Bash group；确认本轮只改 header 状态摘要和 toggle target，不改命令解析、row 展开、bulk actions、复制、分组算法、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展 `BashToolGroupBlock.test.tsx`，构造 2 completed、1 error、1 pending，断言 header 可见 `2 tools.success · 1 tools.failed · 1 tools.pending` 且纳入 group toggle `title` / `aria-label`；扩展 `toolGrouping.test.ts` 覆盖 completedCount 大于 1 时的格式化。
- 功能点 4：实现最小展示修复。验证方式：让 `formatToolExecutionStatusSummary()` 对多个成功项显示成功计数，并让 `BashToolGroupBlock` 使用 `summarizeToolResultStatuses()` / `formatToolExecutionStatusSummary()` 生成 secondary summary 与 toggle target；保留单成功时既有 `tools.success` 风格，避免 Read/Edit/Search 单成功摘要变化。
- 功能点 5：复核 Trellis 规范。验证方式：`.trellis/spec/frontend/component-guidelines.md` 已有 grouped tool header 混合状态摘要合同；本轮如只落地同一合同，不重复扩写。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 bash group status summary

- 上轮进展与阻塞：上一轮完成 `ReadToolGroupBlock` / `EditToolGroupBlock` / `SearchToolGroupBlock` 的折叠态混合状态摘要，并抽出 `src/utils/toolGrouping.ts` 的共享状态计数和格式化 helper；代码中确实存在 `summarizeToolResultStatuses()` / `formatToolExecutionStatusSummary()`，Read/Edit/Search group header 已使用这些 helper 展示状态构成。更早的 Generic/Bash/Read/Edit copy action target 标签、单块/group header toggle label、文件打开 action + target 标签和 open-file 冒泡边界也仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报、cc-gui 真实运行并排截图未完成，以及 IDE build 120 秒超时。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 的 `webview/src/components/toolBlocks/BashToolGroupBlock.tsx` 会在 Bash batch header 展示进度摘要，例如失败数、全部完成或 completed/total；本项目 Bash group 已有状态点和 secondary summary，但多个成功命令仍只显示一次 `tools.success`，且 header toggle 的 `title` / `aria-label` 只包含首条命令，不包含状态构成。用户价值：批量命令出现“多个成功 + 部分失败 + pending”时，用户不用展开列表即可判断当前批处理是否需要介入。
- 本轮差距诊断（超越 cc-gui）：本项目已有比 cc-gui 更完整的 group header 可访问标签和 Read/Edit/Search 统一状态摘要；本轮让 Bash group 也纳入同一 shared helper，并在多个成功项时显示成功计数。用户价值：视觉用户、键盘用户和辅助技术用户都能从同一 header 获得“代表命令 + 执行构成”，降低只靠颜色点或展开列表确认状态的成本。
- 本轮规划：只改 `BashToolGroupBlock` header 状态摘要和 toggle target、`toolGrouping` 状态摘要格式化、两个测试文件和 Trellis 记录；不改命令解析、row 展开、bulk actions、复制、分组算法、Store/Rust/Tauri response。优先级中高：Bash group 高频且状态风险高，实现成本低，复用既有共享 helper，回归面可用组件测试覆盖。
- 本轮完成：`src/utils/toolGrouping.ts` 的 `formatToolExecutionStatusSummary()` 在 `completedCount > 1` 时输出 `2 tools.success` 这类计数，`completedCount === 1` 仍保持原来的 `tools.success`，避免单成功场景文案扰动。`src/components/toolBlocks/BashToolGroupBlock.tsx` 改用 `summarizeToolResultStatuses()` / `formatToolExecutionStatusSummary()` 生成 `statusSummary`，可见 header summary 和 `tools.bashGroupDetailsToggle` 的 target 都包含状态构成，例如 `npm test · 2 tools.success · 1 tools.failed · 1 tools.pending`。`src/components/toolBlocks/BashToolGroupBlock.test.tsx` 扩展为 2 completed、1 error、1 pending 的混合状态断言。`src/utils/toolGrouping.test.ts` 新增多个成功项格式化断言。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/BashToolGroupBlock.test.tsx src/utils/toolGrouping.test.ts` 先失败 3 项，失败点为 shared formatter 未显示成功计数，Bash group header toggle target 缺少状态构成，header 可见摘要仍为 `tools.success · 1 tools.failed · 1 tools.pending`，符合旧实现缺口。GREEN 定向通过：同命令通过（2 files / 9 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/SubagentHistoryPanel.test.tsx src/components/toolBlocks/SubagentProcessOpenFilePropagation.test.tsx src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（18 files / 107 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（54 files / 488 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。`git diff --check` exit 0；`git diff --cached --check` exit 0；二者仅输出 Windows LF/CRLF 转换提示。
- 未验证项：未启动真实 Tauri 桌面触发 Bash group；未手动确认折叠态 Bash header 状态摘要、tooltip、键盘聚焦播报与屏幕阅读器实际输出；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Generic/Bash/Read/Edit/Search/Agent/Task 单块与分组，点测 header、group item row、内部文件按钮、Copy Path、Copy Command、Copy Input/Output、Bash/Read/Edit/Search 状态摘要、tooltip、键盘焦点、点击路径/行号和 diff hover；2) 对 ToolBlock 可访问性做运行态扫描，核对所有可展开入口和操作按钮的 `role/button/tabindex/aria-expanded/title/aria-label/onKeyDown` 覆盖；3) 审计 all-success group 的状态摘要是否需要在所有 group 中加入总数或耗时，避免“全部成功但规模未知”时仍需依赖数量 chip；4) 开始收束同主题提交边界：把大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility 拆成可审查提交批次。

## 本轮 PLAN 2026-06-20 group bulk action target labels

- 目标：给 Bash/Read/Edit/Search group 的 `Expand all` / `Collapse all` 批量操作补充目标化 `title` / `aria-label`。当前这些按钮可见文本和可访问名称都是通用 `tools.expandAll` / `tools.collapseAll`；一屏多个工具分组时，hover 或读屏用户无法知道按钮会作用于哪一组。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目四个 group block、测试和 locale；确认 cc-gui 可读实现没有本项目这种 group-level bulk action，本轮属于超越项；确认只改 bulk action 的 `title` / `aria-label` 与 i18n，不改按钮可见文案、展开状态、row 行为、文件打开、状态摘要、分组算法、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展四个 group block 测试，断言 `Expand all` / `Collapse all` 按钮的 `title` / `aria-label` 包含 action 和 group target；旧实现应因仍使用通用 label 失败。
- 功能点 4：实现最小交互修复。验证方式：新增 i18n key `tools.expandAllInGroup` / `tools.collapseAllInGroup`，四个 group block 使用既有 group target 生成 action label；按钮可见文本仍保留 `tools.expandAll` / `tools.collapseAll`。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 group-level bulk action 必须使用 action + group target 标签，避免一屏多组时重复的通用按钮名。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 group bulk action target labels

- 上轮进展与阻塞：上一轮完成 `BashToolGroupBlock` 折叠态状态摘要，代码中确实存在 `summarizeToolResultStatuses()` / `formatToolExecutionStatusSummary()` 接入，Bash group header 和 toggle target 已能显示 `npm test · 2 tools.success · 1 tools.failed · 1 tools.pending` 这类执行构成。更早的 group header toggle label、group item row toggle label、Read/Edit/Search 状态摘要、Copy/Open File action + target 标签、Agent/Task header 标签和事件冒泡边界也仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读代码中没有本项目这种 group-level `Expand all` / `Collapse all` 批量按钮，因此本轮没有直接 cc-gui 缺口要补齐；已核实的对标结论是“无同类控件可复制”。用户价值：避免为了追平不存在的控件而扩大范围，保持本轮聚焦在本项目真实高频交互上。
- 本轮差距诊断（超越 cc-gui）：本项目 Bash/Read/Edit/Search group 在展开后有批量展开/折叠按钮，但此前这些按钮的可见文本、`title` 和 `aria-label` 都是通用 `tools.expandAll` / `tools.collapseAll`。一屏多个工具分组时，hover、键盘焦点和读屏用户无法知道按钮会作用于哪组命令、文件、编辑或搜索。用户价值：按钮保持紧凑，但辅助技术和 tooltip 能直接暴露“展开此分组全部详情：npm test · 2 Success · 1 Failed · 1 Pending”这类目标上下文，降低误操作和来回确认成本。
- 本轮规划：只改 `BashToolGroupBlock` / `ReadToolGroupBlock` / `EditToolGroupBlock` / `SearchToolGroupBlock` 的 group bulk action `title` / `aria-label`、四个 SSR 回归、i18n 文案、前端组件规范和 Trellis 记录；不改按钮可见文案、展开状态、row 行为、文件打开、状态摘要、分组算法、Store/Rust/Tauri response。优先级中高：用户价值明确、group 批量操作在长工具列表中出现频率高、实现成本低，且回归面可由现有组件测试覆盖。
- 本轮完成：`src/components/toolBlocks/BashToolGroupBlock.tsx` 新增 `expandAllLabel` / `collapseAllLabel`，复用包含命令与状态构成的 `groupToggleTarget` 生成批量按钮标签。`src/components/toolBlocks/ReadToolGroupBlock.tsx` / `EditToolGroupBlock.tsx` / `SearchToolGroupBlock.tsx` 同步把既有 group target 用于 `Expand all` / `Collapse all` 的 `title` 和 `aria-label`，可见按钮文本仍保留 `tools.expandAll` / `tools.collapseAll`。`src/components/toolBlocks/BashToolGroupBlock.test.tsx`、`ReadToolGroupBlock.test.tsx`、`EditToolGroupBlock.test.tsx`、`SearchToolGroupBlock.test.tsx` 新增/更新 SSR 断言，覆盖目标化批量操作标签，同时确认可见文本仍紧凑。`src/locales/en.json` / `src/locales/zh.json` 新增 `tools.expandAllInGroup` / `tools.collapseAllInGroup`。`.trellis/spec/frontend/component-guidelines.md` 补充 grouped bulk action 必须使用 action + group target 标签。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx` 先失败 4 项，失败点为 bulk action 仍使用通用 `title="tools.expandAll"` / `aria-label="tools.expandAll"` 和 `tools.collapseAll`，符合旧实现缺口。GREEN 定向通过：同命令通过（4 files / 23 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/SubagentHistoryPanel.test.tsx src/components/toolBlocks/SubagentProcessOpenFilePropagation.test.tsx src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（18 files / 107 tests）。本轮收尾重新验证：`npm test` 通过（54 files / 488 tests，仅既有 Browserslist/caniuse-lite 过期提示）；`npm run build` 通过（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）；`cargo check --manifest-path src-tauri/Cargo.toml` 通过；`cargo test --manifest-path src-tauri/Cargo.toml` 通过（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。`git diff --check` exit 0；`git diff --cached --check` exit 0；二者仅输出 Windows LF/CRLF 转换提示。
- 未验证项：未启动真实 Tauri 桌面触发 Bash/Read/Edit/Search group bulk action；未手动确认按钮 hover tooltip、Tab 焦点、读屏播报和点击后展开/折叠全部 row 的运行态表现；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发 Generic/Bash/Read/Edit/Search/Agent/Task 单块与分组，点测 header、group item row、group bulk action、内部文件按钮、Copy Path、Copy Command、Copy Input/Output、状态摘要、tooltip、键盘焦点、点击路径/行号和 diff hover；2) 对 ToolBlock 可访问性做运行态扫描，核对所有可展开入口和操作按钮的 `role/button/tabindex/aria-expanded/title/aria-label/onKeyDown` 覆盖；3) 审计 all-success group 的状态摘要是否需要在所有 group 中加入总数或耗时，避免“全部成功但规模未知”时仍需依赖数量 chip；4) 开始收束同主题提交边界：把大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility 拆成可审查提交批次。

## 本轮 PLAN 2026-06-20 group bulk action disabled states

- 目标：给 Bash/Read/Edit/Search group 的 `Expand all` / `Collapse all` 批量操作增加无效状态反馈。当前按钮即使所有子项都未展开或已全部展开仍可点击；用户在长分组中容易重复触发没有效果的动作。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目四个 group block 和测试、对标项目 group 实现；确认 cc-gui 可读实现没有本项目这种 group-level bulk action，本轮属于超越项；确认只改 bulk action 的 `disabled` 状态，不改按钮文案、标签、展开算法、row 行为、文件打开、状态摘要、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展四个 group block SSR 测试，断言初始无子项展开时 `Collapse all` 带 `disabled`，`Expand all` 不带 `disabled`；旧实现应因 collapse 按钮未禁用失败。
- 功能点 4：实现最小交互修复。验证方式：四个 group block 计算 `allItemsExpanded` / `noItemsExpanded`，分别禁用 `Expand all` / `Collapse all`；保留上一轮目标化 `title` / `aria-label`，让禁用控件仍能被上下文解释。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 grouped bulk action 在目标状态已满足时必须禁用，避免无效按钮看起来仍可执行。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 group bulk action disabled states

- 上轮进展与阻塞：上一轮完成 Bash/Read/Edit/Search group 的 `Expand all` / `Collapse all` 目标化 `title` / `aria-label`，代码中确实存在 `tools.expandAllInGroup` / `tools.collapseAllInGroup`，可见按钮文本仍保持 `tools.expandAll` / `tools.collapseAll`。更早的 Copy/Open File action + target 标签、group header / item row toggle label、Read/Edit/Search/Bash 状态摘要、Agent/Task header 标签和事件冒泡边界也仍在当前代码中存在。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报，以及 cc-gui 真实运行并排截图未完成。本轮确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读实现没有本项目这种 group-level `Expand all` / `Collapse all` 批量按钮，因此没有同类 cc-gui 控件可直接补齐。用户价值：本轮不臆测对标能力，只基于本项目已有批量控件做真实体验改进。
- 本轮差距诊断（超越 cc-gui）：本项目 group bulk action 在目标状态已满足时仍可点击，例如初始没有任何子项展开时 `Collapse all` 仍像有效按钮。用户价值：禁用无效动作能减少长工具分组中的重复点击和“是不是没响应”的误解；辅助技术用户也能通过 disabled 状态知道该动作当前不可执行。
- 本轮规划：只改 Bash/Read/Edit/Search group bulk action 的 `disabled` 状态、四个 SSR 回归、前端组件规范和 Trellis 记录；不改按钮可见文案、上一轮目标化标签、展开算法、row 行为、文件打开、状态摘要、Store/Rust/Tauri response。优先级中高：用户价值明确、实现成本低、回归面小，且属于当前 toolblock 可访问性与交互连续打磨中的自然下一步。
- 本轮完成：`src/components/toolBlocks/BashToolGroupBlock.tsx`、`ReadToolGroupBlock.tsx`、`EditToolGroupBlock.tsx`、`SearchToolGroupBlock.tsx` 新增有效展开项计数，过滤越界 index 后计算 `allItemsExpanded` / `noItemsExpanded`；`Expand all` 在全部有效行已展开时禁用，`Collapse all` 在无有效展开行时禁用。四个按钮继续保留 action + group target 的 `title` / `aria-label`。`src/components/toolBlocks/BashToolGroupBlock.test.tsx`、`ReadToolGroupBlock.test.tsx`、`EditToolGroupBlock.test.tsx`、`SearchToolGroupBlock.test.tsx` 新增初始状态 SSR 断言，覆盖 `Collapse all disabled` 和 `Expand all enabled`。`.trellis/spec/frontend/component-guidelines.md` 记录 grouped bulk action 在目标状态已满足时必须禁用。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx` 先失败 4 项，失败点均为 `Collapse all` 按钮缺少 `disabled`，符合旧实现缺口。GREEN 定向通过：同命令通过（4 files / 27 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/SubagentHistoryPanel.test.tsx src/components/toolBlocks/SubagentProcessOpenFilePropagation.test.tsx src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（18 files / 111 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（54 files / 492 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。`git diff --check` exit 0；`git diff --cached --check` exit 0；二者仅输出 Windows LF/CRLF 转换提示。
- 未验证项：未启动真实 Tauri 桌面触发 Bash/Read/Edit/Search group bulk action；未手动确认点击 `Expand all` 后 `Expand all` 变 disabled、`Collapse all` 变 enabled 的运行态状态切换；未用屏幕阅读器确认 disabled 状态播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 给 group bulk action 增加交互级测试，覆盖点击 `Expand all` 后禁用状态互换、点击 `Collapse all` 后恢复初始状态；2) 真实 Tauri desktop smoke：触发 Generic/Bash/Read/Edit/Search/Agent/Task 单块与分组，点测 header、group item row、group bulk action disabled 状态、内部文件按钮、Copy Path、Copy Command、Copy Input/Output、状态摘要、tooltip、键盘焦点、点击路径/行号和 diff hover；3) 对 ToolBlock 可访问性做运行态扫描，核对所有可展开入口和操作按钮的 `role/button/tabindex/aria-expanded/title/aria-label/onKeyDown/disabled` 覆盖；4) 开始收束同主题提交边界：把大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility 拆成可审查提交批次。

## 本轮 PLAN 2026-06-20 group bulk action state helper

- 目标：把上一轮 Bash/Read/Edit/Search group 中重复的 `Expand all` / `Collapse all` disabled 状态判断收敛到共享纯 helper，并补足“全展开后 Expand 禁用、部分展开两者可用、越界 index 不影响判断”的单元测试。当前验证只覆盖初始 SSR 状态，四个组件各自复制状态计算，后续维护容易漂移。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph 诊断与实现边界。验证方式：读取 `toolGrouping.ts`、四个 group block、四个 group test 和 cc-gui group 实现；确认本轮只抽取状态判断 helper 和测试，不改可见文案、上一轮 disabled 行为、展开/折叠状态更新、文件打开、状态摘要、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展 `toolGrouping.test.ts`，断言新 helper 在 none/some/all/invalid-index 场景下返回正确的 `allItemsExpanded` / `noItemsExpanded`；旧实现应因 helper 未导出失败。
- 功能点 4：实现共享 helper 并接入组件。验证方式：在 `toolGrouping.ts` 新增 `getToolGroupBulkActionState(itemCount, expandedIndices)`，四个 group block 改用该 helper；保留上一轮 SSR disabled 断言。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 group bulk action disabled 状态应复用共享 helper，避免四个 group block 分叉。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 group bulk action state helper

- 上轮进展与阻塞：上一轮完成 Bash/Read/Edit/Search group 的 `Expand all` / `Collapse all` disabled 状态，代码中确实存在 `disabled={allItemsExpanded}` 与 `disabled={noItemsExpanded}`，并保留上一轮 action + group target 的 `title` / `aria-label`。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 运行态并排截图，以及 IDE build 超时未验证。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读实现没有本项目这种 group-level `Expand all` / `Collapse all` 批量按钮，因此本轮没有直接 cc-gui 控件缺口可补齐。用户价值：继续避免臆测对标能力，把精力放在本项目真实存在且高频使用的批量操作上。
- 本轮差距诊断（超越 cc-gui）：上一轮已经让无效 bulk action 进入 disabled 状态，但 Bash/Read/Edit/Search 四个组件各自复制有效 index 过滤与状态判断。用户价值：共享纯 helper 可以防止后续漂移，尤其 Edit group 的行数来自可见 edit rows，而不是 raw `tool_use` 数量；这能降低未来加新 group 或改编辑项聚合时出现“按钮状态与可见行不一致”的回归风险。
- 本轮规划：只抽取 `getToolGroupBulkActionState(itemCount, expandedIndices)` 并接入四个 group block，补 helper 单元测试和 spec 约定；不改按钮文案、上一轮 disabled 行为、展开/折叠状态更新、文件打开、状态摘要、Store/Rust/Tauri response。优先级中高：用户价值来自降低回归风险，出现频率覆盖所有工具分组，实现成本低，且纯函数测试能覆盖边界条件。
- 本轮完成：`src/utils/toolGrouping.ts` 新增 `ToolGroupBulkActionState` 与 `getToolGroupBulkActionState()`，统一过滤负数和越界 index 后返回 `allItemsExpanded` / `noItemsExpanded`。`src/utils/toolGrouping.test.ts` 新增 none / some / all / invalid-index 四类断言。`src/components/toolBlocks/BashToolGroupBlock.tsx`、`ReadToolGroupBlock.tsx`、`SearchToolGroupBlock.tsx` 改为按 `blocks.length` 调用共享 helper；`src/components/toolBlocks/EditToolGroupBlock.tsx` 改为按 `editItems.length` 调用共享 helper，保持“可见编辑行”语义。`.trellis/spec/frontend/component-guidelines.md` 已记录 group bulk action disabled 状态必须复用共享 helper，且 Edit group 必须传可见 edit-row count。
- 本轮验证：RED：`npm test -- src/utils/toolGrouping.test.ts` 先失败，失败点为 `TypeError: getToolGroupBulkActionState is not a function`，符合旧实现 helper 未导出的缺口。GREEN 定向通过：`npm test -- src/utils/toolGrouping.test.ts src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx`（5 files / 32 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/SubagentHistoryPanel.test.tsx src/components/toolBlocks/SubagentProcessOpenFilePropagation.test.tsx src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（18 files / 112 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（54 files / 493 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未验证。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。`git diff --check` exit 0；`git diff --cached --check` exit 0；二者仅输出 Windows LF/CRLF 转换提示。
- 未验证项：未启动真实 Tauri 桌面触发 Bash/Read/Edit/Search group bulk action；未手动确认点击 `Expand all` 后按钮 disabled 状态互换、点击 `Collapse all` 后恢复初始状态；未用屏幕阅读器确认 disabled 状态播报；未与 cc-gui 做真实运行并排截图；IDE build 因 120 秒超时未验证。
- 下一步候选：1) 若项目后续引入 DOM 交互测试环境（当前未配置 jsdom/happy-dom，也没有 Testing Library 依赖），补点击 `Expand all` / `Collapse all` 后 disabled 状态互换的交互级测试；2) 真实 Tauri desktop smoke：触发 Generic/Bash/Read/Edit/Search/Agent/Task 单块与分组，点测 header、group item row、group bulk action disabled 状态、内部文件按钮、Copy Path、Copy Command、Copy Input/Output、状态摘要、tooltip、键盘焦点、点击路径/行号和 diff hover；3) 对 ToolBlock 可访问性做运行态扫描，核对所有可展开入口和操作按钮的 `role/button/tabindex/aria-expanded/title/aria-label/onKeyDown/disabled` 覆盖；4) 开始收束同主题提交边界：把大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility 拆成可审查提交批次。

## 本轮 PLAN 2026-06-20 group expanded index helpers

- 目标：把 Bash/Read/Edit/Search group 中重复的 `toggleAll` / `toggleItem` 展开集合更新逻辑收敛到共享纯 helper。上一轮已统一 disabled 状态判断，但四个组件仍各自创建全量 index Set、复制当前 Set、增删单个 index，后续维护容易再次漂移。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph 诊断与实现边界。验证方式：读取 `toolGrouping.ts`、四个 group block 和 cc-gui group 实现；确认本轮只抽取展开集合 helper 和测试，不改可见文案、disabled 行为、group/header/row 标签、文件打开、状态摘要、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展 `toolGrouping.test.ts`，断言全量展开集合会按 item count 生成有效 index，负数/零 count 返回空集合；单项 toggle 会新增/移除有效 index，并忽略负数、越界和非整数 index。旧实现应因 helper 未导出失败。
- 功能点 4：实现共享 helper 并接入组件。验证方式：在 `toolGrouping.ts` 新增 `getToolGroupExpandedIndices(itemCount)` 与 `toggleToolGroupExpandedIndex(itemCount, expandedIndices, index)`；四个 group block 改用 helper，Edit 继续传 `editItems.length`，其余传 `blocks.length`。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 group item 展开集合更新应复用共享 helper，避免 bulk action disabled 判断与实际集合更新分叉。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-20 group expanded index helpers

- 上轮进展与阻塞：上一轮完成 `getToolGroupBulkActionState()` 抽取，代码中确实存在 Bash/Read/Edit/Search group 统一复用该 helper 的 disabled 状态判断；Edit group 继续按可见 `editItems.length` 计算，而不是 raw `tool_use` 数量。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 运行态并排截图，以及 IDE build 超时未验证。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读实现没有本项目这种 group-level `Expand all` / `Collapse all` 批量按钮，因此本轮没有直接 cc-gui 控件缺口可补齐。用户价值：继续避免臆测对标能力，只基于代码中真实存在的交互面补稳定性。
- 本轮差距诊断（超越 cc-gui）：disabled 状态已统一，但实际展开集合更新仍在 Bash/Read/Edit/Search 四个组件中重复维护。用户价值：把 bulk expand 和单行 toggle 收敛到共享纯 helper，可以防止未来 disabled 判断与真实展开集合的边界条件分叉，尤其 Edit group 的可见行数和 raw tool 数不一致时更容易出错。
- 本轮规划：只抽取 `getToolGroupExpandedIndices(itemCount)` 与 `toggleToolGroupExpandedIndex(itemCount, expandedIndices, index)` 并接入四个 group block，补 helper 单元测试和 spec 约定；不改可见文案、上一轮 disabled 行为、group/header/row 标签、文件打开、状态摘要、Store/Rust/Tauri response。优先级中高：覆盖所有工具分组，降低维护回归风险，实现成本低，纯函数边界可被单元测试稳定覆盖。
- 本轮完成：`src/utils/toolGrouping.ts` 新增 `getToolGroupExpandedIndices()` 与 `toggleToolGroupExpandedIndex()`；前者按有效 item count 生成全量 index set，后者返回新 `Set`、切换有效整数 index，并忽略负数、越界和非整数 index。`src/utils/toolGrouping.test.ts` 新增全量展开集合、无突变单项 toggle、非法 index 忽略三类测试。`src/components/toolBlocks/BashToolGroupBlock.tsx`、`ReadToolGroupBlock.tsx`、`SearchToolGroupBlock.tsx` 改为按 `blocks.length` 调用共享 helper；`src/components/toolBlocks/EditToolGroupBlock.tsx` 改为按 `editItems.length` 调用共享 helper，保持“可见编辑行”语义。`.trellis/spec/frontend/component-guidelines.md` 已记录 grouped tool components 应复用共享 expanded-index helper。
- 本轮验证：RED：`npm test -- src/utils/toolGrouping.test.ts` 先失败 3 项，失败点为 `TypeError: getToolGroupExpandedIndices is not a function` 与 `TypeError: toggleToolGroupExpandedIndex is not a function`，符合旧实现 helper 未导出的缺口。GREEN 定向通过：`npm test -- src/utils/toolGrouping.test.ts src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx`（5 files / 35 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/SubagentHistoryPanel.test.tsx src/components/toolBlocks/SubagentProcessOpenFilePropagation.test.tsx src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（18 files / 115 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（54 files / 496 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。`git diff --check` 与 `git diff --cached --check` 收尾执行。
- 未验证项：未启动真实 Tauri 桌面触发 Bash/Read/Edit/Search group bulk action；未手动确认点击 `Expand all` 后所有行展开、按钮 disabled 状态互换、点击 `Collapse all` 后恢复初始状态；未用屏幕阅读器确认 disabled / expanded 状态播报；未与 cc-gui 做真实运行并排截图；IDE build 本轮未运行，上一轮 120 秒超时结果仍不能视为通过。
- 下一步候选：1) 若项目后续引入 DOM 交互测试环境，补点击 `Expand all` / `Collapse all` 后展开行和 disabled 状态互换的交互级测试；2) 真实 Tauri desktop smoke：触发 Generic/Bash/Read/Edit/Search/Agent/Task 单块与分组，点测 header、group item row、group bulk action disabled 状态、内部文件按钮、Copy Path、Copy Command、Copy Input/Output、状态摘要、tooltip、键盘焦点、点击路径/行号和 diff hover；3) 对 ToolBlock 可访问性做运行态扫描，核对所有可展开入口和操作按钮的 `role/button/tabindex/aria-expanded/title/aria-label/onKeyDown/disabled` 覆盖；4) 开始收束同主题提交边界：把大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility 拆成可审查提交批次。

## 本轮 PLAN 2026-06-20 group toggle activation helper

- 目标：把 Bash/Read/Edit/Search group 中重复的 Enter/Space 键盘 toggle 判定收敛到共享纯 helper。前两轮已经统一 bulk disabled 状态和展开 index 集合更新，但 group header 与 item row 仍各自写 `event.key === 'Enter' || event.key === ' '`，后续维护容易和当前 `role="button"` 键盘合同分叉。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取 `toolGrouping.ts`、四个 group block、`toolGrouping.test.ts` 与 cc-gui group 实现；确认本轮只抽取键盘激活 key helper 和测试，不改 visible UI、ARIA 文案、展开状态语义、文件打开、状态摘要、Store/Rust/Tauri response。
- 功能点 3：补 RED 测试。验证方式：扩展 `toolGrouping.test.ts`，断言新 helper 仅把 `Enter` 与空格键识别为 group toggle activation key，忽略 `Escape`、箭头键和普通字符；旧实现应因 helper 未导出失败。
- 功能点 4：实现共享 helper 并接入组件。验证方式：在 `toolGrouping.ts` 新增 `isToolGroupToggleActivationKey(key)`；四个 group block 的 group header 和 item row keydown 都改用该 helper，保留 `preventDefault()` 和现有 toggle 行为。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 `role="button"` 的 grouped tool header/item row 键盘激活必须复用共享 helper。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 tool/chat 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。真实 Tauri 与屏幕阅读器仍单独标注未验证。

## 迭代记录 2026-06-20 group toggle activation helper

- 上轮进展与阻塞：上一轮完成 Bash/Read/Edit/Search group 的 expanded-index helper 抽取，代码中确实存在 `getToolGroupExpandedIndices()` 与 `toggleToolGroupExpandedIndex()`，四个 group block 已复用共享 helper；Edit group 继续按可见 `editItems.length` 计算。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 运行态并排截图，以及 IDE build 超时未验证。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 group 实现多为 click-driven header/list item，未看到本项目这种完整 `role="button"` + `tabIndex` + `aria-expanded` + Enter/Space 合同，因此本轮没有直接 cc-gui 缺口可补。用户价值：继续基于真实对标代码判断，不把当前项目的增强交互误判成 cc-gui parity 缺失。
- 本轮差距诊断（超越 cc-gui）：当前项目已经让 group header/item row 支持键盘展开，但 Bash/Read/Edit/Search 各自复制 Enter/Space 判定。用户价值：共享 `isToolGroupToggleActivationKey()` 能让四类工具分组保持一致的键盘激活边界，后续如果需要支持更多键或调整 Space 语义，只需改一处，降低可访问性回归风险。
- 本轮规划：只抽取 `isToolGroupToggleActivationKey(key)` 并接入四个 group block 的 header 与 item row keydown，补 helper 单元测试和 spec 约定；不改 visible UI、ARIA 文案、展开状态语义、文件打开、状态摘要、Store/Rust/Tauri response。优先级中高：覆盖所有工具分组键盘入口，用户价值来自稳定可访问性合同，实现成本低且测试边界明确。
- 本轮完成：`src/utils/toolGrouping.ts` 新增 `TOOL_GROUP_TOGGLE_ACTIVATION_KEYS` 与 `isToolGroupToggleActivationKey()`，只识别 `Enter` 与空格键。`src/utils/toolGrouping.test.ts` 新增键盘激活 key 测试，覆盖 `Enter`、` `、`Escape`、`ArrowDown` 和普通字符。`src/components/toolBlocks/BashToolGroupBlock.tsx`、`ReadToolGroupBlock.tsx`、`EditToolGroupBlock.tsx`、`SearchToolGroupBlock.tsx` 的 group header 与 item row keydown 判定均改用共享 helper，保留原有 `preventDefault()` 和 toggle 行为。`.trellis/spec/frontend/component-guidelines.md` 已记录 grouped tool custom `role="button"` toggle 必须复用共享 keyboard-activation helper。
- 本轮验证：RED：`npm test -- src/utils/toolGrouping.test.ts` 先失败 1 项，失败点为 `TypeError: isToolGroupToggleActivationKey is not a function`，符合旧实现 helper 未导出的缺口。GREEN 定向通过：`npm test -- src/utils/toolGrouping.test.ts src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx`（5 files / 36 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/SubagentHistoryPanel.test.tsx src/components/toolBlocks/SubagentProcessOpenFilePropagation.test.tsx src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/components/chat/MessageList.test.tsx src/components/chat/MessageItem.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（18 files / 116 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（54 files / 497 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。固定字符串搜索确认四个 group 组件不再保留本轮目标内的重复 `event.key` 判定；剩余命中在单块/Agent/Task，属于后续独立候选。
- 未验证项：未启动真实 Tauri 桌面触发 Bash/Read/Edit/Search group keyboard toggle；未手动确认 Tab 到 group header/item row 后 Enter/Space 展开、Escape/箭头键不触发展开；未用屏幕阅读器确认 `aria-expanded` 与目标化 label 播报；未与 cc-gui 做真实运行并排截图；IDE build 本轮未运行，上一轮 120 秒超时结果仍不能视为通过。
- 下一步候选：1) 将同样的 keyboard-activation helper 评估推广到 Bash/Read/Edit/Generic 单块和 Agent/Task header，前提是确认它们的键盘合同完全一致；2) 若项目后续引入 DOM 交互测试环境，补 group header/item row 的 Enter/Space 实际展开交互测试；3) 真实 Tauri desktop smoke：触发 Generic/Bash/Read/Edit/Search/Agent/Task 单块与分组，点测 header、group item row、bulk action disabled、键盘焦点、Open File、Copy Input/Output、状态摘要和 diff hover；4) 对 ToolBlock 可访问性做运行态扫描，核对所有可展开入口和操作按钮的 `role/button/tabindex/aria-expanded/title/aria-label/onKeyDown/disabled` 覆盖。

## 本轮 PLAN 2026-06-21 status edit inspect keyboard helper

- 目标：把 `StatusPanel` 最近编辑文件行的 diff 检查键盘激活从手写 `Enter` / Space 判断收敛到共享 activation helper，和 ToolBlock header / row 的自定义 `role="button"` 合同保持一致。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / 搜索诊断。验证方式：读取 `StatusPanel.tsx`、`StatusPanelOpenFilePropagation.test.tsx` 与 `toolGrouping.ts`；确认 ToolBlock/Agent/Task 已复用 `isToolBlockToggleActivationKey()`，剩余真实手写 Enter/Space 命中在 `StatusPanel` 最近编辑 diff 检查行。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanelOpenFilePropagation.test.tsx`，要求导出 `isStatusEditInspectActivationKey()`，并断言最近编辑 diff 行只响应 Enter/Space，Escape 不触发选择；旧实现应因 helper 未导出失败。
- 功能点 4：实现最小交互修复。验证方式：`StatusPanel.tsx` 导出 `isStatusEditInspectActivationKey = isToolBlockToggleActivationKey`，并让 `handleEditedFileKeyDown()` 复用该 helper；不改打开文件、选择 diff、预览、i18n、状态摘要、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 StatusPanel 最近编辑 diff 检查行必须复用共享 keyboard activation helper。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/tool 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-21 status edit inspect keyboard helper

- 上轮进展与阻塞：上一轮完成 Bash/Read/Edit/Search group 的 Enter/Space 键盘 toggle helper 抽取，代码中确实存在 `isToolBlockToggleActivationKey()` 与 `isToolGroupToggleActivationKey`，并且 ToolBlock 单块、Agent、Task、group header/item row 已复用该 helper。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 运行态并排截图，以及 IDE build 未重新验证。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读实现没有本项目这种右侧 `StatusPanel` 最近编辑 diff 检查树，因此本轮没有直接 cc-gui 控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目扩展面误判为 cc-gui parity 缺失。
- 本轮差距诊断（超越 cc-gui）：`StatusPanel` 最近编辑文件行是自定义 `role="button"`，用于打开中央 diff 检查；它此前直接写 `event.key !== 'Enter' && event.key !== ' '`，和 ToolBlock 已收敛的键盘合同分叉。用户价值：复用共享 helper 后，键盘用户在 ToolBlock 与右侧编辑树之间得到一致的 Enter/Space 激活边界，未来修改键盘合同时不会漏掉状态面板。
- 本轮规划：只改 `StatusPanel` 最近编辑 diff 行键盘激活 helper、一个组件级回归、前端组件规范和 Trellis 记录；不改文件打开按钮、diff 选择逻辑、hover preview、可见 UI、i18n、状态摘要、Store/Rust/Tauri response。优先级中高：用户价值来自一致键盘合同和降低回归风险，出现频率覆盖所有有文件编辑的对话，实现成本低。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增导出 `isStatusEditInspectActivationKey`，复用 `isToolBlockToggleActivationKey`，并让 `handleEditedFileKeyDown()` 改用该 helper。`src/components/chat/StatusPanelOpenFilePropagation.test.tsx` 新增 helper 键集合测试和最近编辑 diff 行 keydown 行为测试，覆盖 Escape 不触发、Enter 触发 `preventDefault()` 与 `onSelectedEditChange`。`.trellis/spec/frontend/component-guidelines.md` 已记录 StatusPanel 最近编辑 diff 检查行必须复用共享 keyboard activation helper。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanelOpenFilePropagation.test.tsx` 先失败 1 项，失败点为 `TypeError: isStatusEditInspectActivationKey is not a function`，符合旧实现 helper 未导出的缺口。GREEN 定向通过：同命令通过（1 file / 3 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/toolBlocks/ToolGroupOpenFilePropagation.test.tsx src/utils/toolGrouping.test.ts`（6 files / 81 tests）。全量前端通过：`npm test`（55 files / 502 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。
- 未验证项：未启动真实 Tauri 桌面触发最近编辑 diff 检查行；未手动确认 Tab 到最近编辑文件行后 Enter/Space 打开中央 diff、Escape/箭头键不触发；未用屏幕阅读器确认 `aria-label` / `aria-current` 播报；未与 cc-gui 做真实运行并排截图；IDE build 本轮未运行。
- 下一步候选：1) 对 `StatusPanel` 其他手写键盘处理做同类审计，优先区分 Escape-only 关闭逻辑和自定义 `role="button"` 激活逻辑；2) 真实 Tauri desktop smoke：触发文件编辑后点测右侧最近编辑树、中央 diff review pane、open-file inner button、hover preview、键盘焦点和 Enter/Space/Escape 行为；3) 对 ToolBlock + StatusPanel 可访问性做运行态扫描，核对所有自定义 button-like rows 的 `role/tabIndex/aria-label/aria-current/aria-expanded/onKeyDown`；4) 开始收束同主题提交边界：把大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility、StatusPanel a11y 拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 mcp live check action state label

- 目标：让 `StatusPanel` 的手动 MCP live-check 按钮在检查中状态下同步更新 `title` / `aria-label`，避免可见文本显示 `Checking...`，但聚焦控件仍只暴露静态 `Check live` 或缺少可访问名称。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / 搜索诊断。验证方式：读取 `StatusPanel.tsx` / `StatusPanel.test.tsx`，并搜索 `StatusPanel` 中紧凑按钮的 `title` / `aria-label` 覆盖；确认 MCP summary toggle、diff view、reopen diff、daemon reconnect 等已有 action-specific 标签，真实缺口是 `status-mcp-check` 只有静态 `title` 且没有 `aria-label`。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 MCP live-check 用例，断言检查中按钮输出 `aria-label="Checking..."` / `aria-label="检测中…"` 和同状态 `title`；旧实现应因缺少 `aria-label` 且 `title` 仍为静态检查动作失败。
- 功能点 4：实现最小交互修复。验证方式：在 `StatusPanel.tsx` 新增 `mcpCheckActionLabel`，空闲时使用 `mcpCheckLiveLabel`，检查中使用 `mcpCheckingLabel`，并同步到 live-check button 的 `title` / `aria-label`；不改 MCP 数据、按钮 disabled 条件、点击处理、服务器列表、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 MCP manual live-check 按钮的 `title` / `aria-label` 必须跟随 checking 状态。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/MCP/tool 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-21 mcp live check action state label

- 上轮进展与阻塞：上一轮完成 `StatusPanel` 最近编辑 diff 检查行键盘激活 helper，代码中确实存在 `isStatusEditInspectActivationKey = isToolBlockToggleActivationKey`，并且最近编辑 diff 行已经复用该 helper。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 运行态并排截图，以及 IDE build 未重新验证。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读实现没有本项目这种 MCP live-check 状态面板控件，因此本轮没有直接 cc-gui 控件缺口可补齐。用户价值：继续把对标限制在真实代码，不凭印象假设 cc-gui 具备同类 MCP 检查能力。
- 本轮差距诊断（超越 cc-gui）：本项目 MCP details 已能展示 configured/enabled/live latency，并提供手动 live check；但检查中时按钮可见文本变为 `Checking...`，旧实现没有 `aria-label`，`title` 也仍是静态动作名。用户价值：键盘焦点、tooltip 和辅助技术用户能知道检查请求正在进行，而不是误以为按钮仍可立即发起新检查。
- 本轮规划：只改 `StatusPanel` MCP live-check button 的状态化 `title` / `aria-label`、一个 SSR 回归、前端组件规范和 Trellis 记录；不改 MCP 可用性计算、连接检测数据、按钮 disabled 条件、点击逻辑、Store/Rust/Tauri response。优先级中高：MCP live check 是诊断高频入口，实现成本低，回归面小。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `mcpCheckActionLabel`，在 `mcpConnectivity?.checking` 时使用 `mcpCheckingLabel`，否则使用 `mcpCheckLiveLabel`；`status-mcp-check` 现在同步设置 `title` 与 `aria-label`。`src/components/chat/StatusPanel.test.tsx` 的 MCP live-check SSR 用例新增检查中 `aria-label` / `title` 断言。`.trellis/spec/frontend/component-guidelines.md` 已记录 MCP manual live-check button 的状态化 label 合同。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为检查中按钮缺少 `aria-label="Checking..."` / `aria-label="检测中…"` 且 `title` 仍为静态检查动作，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 33 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（6 files / 75 tests）。全量前端通过：`npm test`（55 files / 502 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已确认在工作区内并清理；`out` 不存在。
- 未验证项：未启动真实 Tauri 桌面触发 MCP live check；未手动确认检查中按钮 tooltip、disabled 状态和读屏播报；未与 cc-gui 做真实运行并排截图；IDE build 本轮未运行。
- 下一步候选：1) 真实 Tauri desktop smoke：打开 MCP details，点测 live-check 空闲/检查中/成功/失败状态的可见文本、tooltip、disabled 和服务器状态 pill；2) 继续审计 `StatusPanel` 内状态化按钮是否有“可见状态变化但 aria/title 不变”的同类问题；3) 对 ToolBlock + StatusPanel 可访问性做运行态扫描，核对所有紧凑按钮和自定义 row 的 `title/aria-label/disabled/onKeyDown`；4) 开始收束同主题提交边界：把大历史加载快路径、消息消失修复、dialog readable fallback/accessibility、toolblock presentation/accessibility、StatusPanel a11y 拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 daemon reconnect action state label

- 目标：让 `StatusPanel` 的 daemon reconnect 按钮在重连中状态下同步更新 `title` / `aria-label`，避免按钮 disabled 且图标旋转时仍暴露空闲态 `Reconnect daemon`。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / 搜索诊断。验证方式：读取 `StatusPanel.tsx` / `StatusPanel.test.tsx` 与 `chatDaemonStatus.ts`；确认 `getChatDaemonReconnectLabel()` 已有 reconnecting 合同，但 `StatusPanel` 的 `status-daemon-reconnect` 仍使用静态 `chat.daemon.reconnect` 标签。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx`，断言 `daemonReconnecting` 时按钮 disabled、图标旋转，并输出 `title="Reconnecting daemon"` / `aria-label="Reconnecting daemon"`；旧实现应因仍输出 `Reconnect daemon` 失败。
- 功能点 4：实现最小交互修复。验证方式：`StatusPanel.tsx` 接入既有 `getChatDaemonReconnectLabel({ daemonReconnecting, translate })`，只同步按钮 `title` / `aria-label`；不改 daemon 状态计算、disabled 条件、点击处理、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 daemon reconnect disabled/in-flight 状态也必须暴露状态化 action label。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/daemon/MCP/tool 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-21 daemon reconnect action state label

- 上轮进展与阻塞：上一轮完成 `StatusPanel` 手动 MCP live-check 按钮的状态化 `title` / `aria-label`，代码中确实存在 `mcpCheckActionLabel`，检查中使用 `mcpCheckingLabel`，空闲时使用 `mcpCheckLiveLabel`。更早的最近编辑 diff 行键盘 helper、open-file 冒泡边界、ToolBlock/group action label 也仍在当前代码中存在。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 运行态并排截图，以及 IDE build 超时未返回结果。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读实现没有本项目这种 daemon reconnect 状态面板控件，因此本轮没有直接 cc-gui 控件缺口可补齐。用户价值：继续基于真实代码判断，不凭印象把本项目 daemon 自愈控制误当作 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目已有 `getChatDaemonReconnectLabel()`，并在工具函数测试中约定重连中 fallback 为 `Reconnecting daemon`，但 `StatusPanel` 的 `status-daemon-reconnect` 在 disabled 且图标旋转时仍暴露空闲态 `Reconnect daemon`。用户价值：键盘焦点、tooltip 和辅助技术用户能知道重连请求正在进行，而不是误以为按钮仍可发起新重连。
- 本轮规划：只改 `StatusPanel` daemon reconnect button 的状态化 `title` / `aria-label`、一个 SSR 回归、前端组件规范和 Trellis 记录；不改 daemon 状态计算、重连可用性判断、按钮 disabled 条件、点击处理、Store/Rust/Tauri response。优先级中高：daemon 恢复是故障场景高价值入口，实现成本低，且复用现有 helper 降低回归风险。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 引入并使用 `getChatDaemonReconnectLabel({ daemonReconnecting, translate })`，让 `status-daemon-reconnect` 在重连中输出状态化 `title` / `aria-label`；按钮显示、disabled 条件和 `onClick` 不变。`src/components/chat/StatusPanel.test.tsx` 新增 SSR 回归，覆盖 `daemonReconnecting` 时按钮 disabled、图标旋转、`title` / `aria-label` 为 `Reconnecting daemon`，且不再暴露 `aria-label="Reconnect daemon"`。`.trellis/spec/frontend/component-guidelines.md` 已记录 daemon reconnect disabled/in-flight 状态也必须暴露状态化 action label。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为重连中按钮仍输出 `title="Reconnect daemon"` / `aria-label="Reconnect daemon"`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（7 files / 88 tests）。全量前端通过：`npm test`（55 files / 503 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未返回编译结果。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在。`git diff --check` / `git diff --cached --check` exit 0；前者仅打印 Windows LF/CRLF 转换提示，后者无输出。
- 未验证项：未启动真实 Tauri 桌面触发 daemon reconnect；未手动确认重连中按钮 tooltip、disabled 状态和读屏播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：断开/重启 daemon 后点测 offline/error/reconnecting/ready 状态、reconnect 按钮 tooltip、disabled 和图标旋转；2) 继续审计 `StatusPanel` 内 remaining 状态化按钮，尤其是 recent edit hidden-files toggle 是否需要 `aria-label` 跟随 show-all / collapse 状态；3) 对 ToolBlock + StatusPanel 做一次运行态可访问性扫描，核对所有紧凑按钮和自定义 row 的 `title/aria-label/disabled/onKeyDown`；4) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 recent edit overflow action label

- 目标：让 `StatusPanel` 最近编辑隐藏文件切换按钮的可见文本、`title` 和 `aria-label` 随当前状态一致。折叠态显示/暴露隐藏文件数量，展开态显示/暴露返回最近编辑列表的动作，避免 tooltip 或辅助名称停留在过期状态。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且对标项目有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 和对标项目 `StatusPanel` / `FileChangesList`；确认 cc-gui 没有本项目同构的最近编辑隐藏文件切换按钮，本轮属于本项目增强项。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 recent edit fallback 用例，断言隐藏文件切换按钮有 `aria-label="1 more file"`；旧实现应因只有 `title` 且没有 `aria-label` 失败。
- 功能点 4：实现最小交互修复。验证方式：在 `StatusPanel.tsx` 新增 `hiddenEditFilesActionLabel`，根据 `showAllEdits` 在 `showRecentEditFilesLabel` 与 `hiddenEditFilesLabel` 间切换，并同步到按钮可见文本、`title` 和 `aria-label`；不改点击处理、编辑树、diff 选择、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 recent-edit overflow controls 的 visible text / title / aria-label 必须跟随当前 action。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check` / `git diff --cached --check`，并清理 `dist` / `out`。

## 迭代记录 2026-06-21 recent edit overflow action label

- 上轮进展与阻塞：上一轮完成 `StatusPanel` daemon reconnect 按钮的 in-flight 状态化 `title` / `aria-label`，代码中确实存在 `getChatDaemonReconnectLabel({ daemonReconnecting, translate })` 接入，重连中不再暴露空闲态 `Reconnect daemon`。更早的 MCP live-check 状态化 label、最近编辑 diff 行 keyboard helper、open-file 冒泡边界、ToolBlock/group action label 也仍在当前代码中存在。本轮复查确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径仍为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 运行态并排截图，以及 IDE build 超时未返回结果。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `StatusPanel` / `FileChangesList` 使用 tabs/popover 展示文件变更，没有本项目这种右侧最近编辑树的隐藏文件切换按钮，因此本轮没有直接 cc-gui 控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目增强型编辑树误判为 cc-gui 已有控件。
- 本轮差距诊断（超越 cc-gui）：本项目 `StatusPanel` 最近编辑树在隐藏文件存在时会显示底部切换按钮；旧实现只有 `title={hiddenEditFilesLabel}`，没有 `aria-label`，且展开后可见文本会变为 `Show recent edits`，但 tooltip 仍会停留在隐藏数量。用户价值：键盘焦点、tooltip 和辅助技术用户能拿到和当前可见动作一致的标签，减少“显示更多/返回最近”状态误读。
- 本轮规划：只改最近编辑隐藏文件切换按钮的可见文本 / `title` / `aria-label` 同步逻辑、一个 SSR 回归、前端组件规范和 Trellis 记录；不改编辑树、diff 选择、hover preview、文件打开、Store/Rust/Tauri response。优先级中等：该按钮只在有历史编辑溢出时出现，但实现成本很低，且与近期状态化 action-label 主题一致。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `hiddenEditFilesActionLabel`，根据 `showAllEdits` 在 `showRecentEditFilesLabel` 与 `hiddenEditFilesLabel` 之间切换，并同步用于按钮可见文本、`title` 和 `aria-label`；点击逻辑仍是原来的 `setShowAllEdits((value) => !value)`。`src/components/chat/StatusPanel.test.tsx` 在 recent edit fallback 用例中新增 `aria-label="1 more file"` 回归断言。`.trellis/spec/frontend/component-guidelines.md` 已记录 recent-edit overflow controls 的 visible text / title / aria-label 必须跟随当前 action。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为 hidden edit overflow button 缺少 `aria-label="1 more file"`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。全量前端通过：`npm test`（55 files / 503 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。IDE `build_project` 120 秒超时，未返回编译结果。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在。`git diff --check` / `git diff --cached --check` exit 0；前者仅打印 Windows LF/CRLF 转换提示，后者无输出。
- 未验证项：未启动真实 Tauri 桌面触发最近编辑隐藏文件切换；未手动确认展开后按钮 tooltip / `aria-label` 切换为返回最近编辑列表；未用屏幕阅读器确认播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：触发文件编辑溢出后点测最近编辑树、隐藏文件切换、diff review pane、open-file inner button、hover preview、键盘焦点和 Enter/Space/Escape 行为；2) 继续审计 `StatusPanel` 中非按钮但有 `title` 的状态 pill / diagnostic 是否需要补充可访问名称或 region 语义；3) 对 ToolBlock + StatusPanel 做一次运行态可访问性扫描，核对所有紧凑按钮和自定义 row 的 `title/aria-label/disabled/onKeyDown`；4) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。
## 本轮 PLAN 2026-06-21 input status row status pill target label

- 目标：让 `ChatInputStatusTabs` 小屏 composer status strip 的 task / subagent row 状态 pill 在保持紧凑 `Success` / `Pending` / `Failed` 可见文本的同时，向辅助技术和 hover tooltip 暴露 `工具类型: 工具目标 · 状态`，与桌面 `StatusPanel` current activity / activity history 的目标化状态 pill 规则保持一致。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx` 的 `renderToolRow` 与输入区状态 strip 测试；确认 row 级 button 已有 jump label，但 row 内 status pill 仍缺少目标化 `title` / `aria-label`。
- 功能点 3：补 RED 测试。验证方式：扩展 `ChatInputStatusTabs.test.tsx` 的 task row、subagent row 与 key-only i18n fallback 用例，断言 row 内 status pill 输出 `npm: npm test · Success`、`Agent: Review composer status strip · Pending`、`npm: npm test --watch · Pending` 的 `title` / `aria-label`；旧实现应因状态 pill 缺少目标化标签失败。
- 功能点 4：实现最小交互修复。验证方式：在 `ChatInputStatusTabs.tsx` 中新增局部 helper，复用已有 `statusLabel()`，并给 `renderToolRow()` 内 `tool-state-pill` 写入 `title` / `aria-label`；不改 row click/jump、popover close、任务/子代理分组、编辑选择、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 small-screen composer fallback 的 task/subagent row status pill 必须匹配桌面 target-label 行为。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。
## 迭代记录 2026-06-21 input status row status pill target label

- 上轮进展与阻塞：上一轮记录称已完成桌面 `StatusPanel` activity history row 状态 pill 的目标化 `title` / `aria-label`；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 的 `renderActivityTaskRow()` 已复用 `getToolStatusTargetLabel(tool)`，`StatusPanel.test.tsx` 和 `.trellis/spec/frontend/component-guidelines.md` 也已有对应断言/规范。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 使用 todo/files/subagents tabs + popover 展示任务、文件变更和子代理状态；本项目 `ChatInputStatusTabs` 已把 tasks/subagents/edits/MCP 做成小屏 composer fallback，但 task/subagent row 内的 `Pending` / `Failed` / `Success` 状态 pill 仍缺少独立目标标签。用户价值：小屏用户不需要依赖右侧桌面面板，也能在状态行中获得可理解的任务状态语义。
- 本轮差距诊断（超越 cc-gui）：本项目的小屏 composer strip 是比 cc-gui 更贴近输入主路径的快速入口；旧实现 row button 已有 `Jump to tool task: <summary>` / `Jump to subagent activity: <summary>`，但内部短状态 pill 被单独 hover 或读屏聚焦时仍只是孤立状态词。用户价值：辅助技术、tooltip 和自动化检查能直接获得 `npm: npm test · Success` 或 `Agent: Review composer status strip · Pending`，窄屏状态入口与桌面 `StatusPanel` 保持一致。
- 本轮规划：只改 `ChatInputStatusTabs.tsx` 的 task/subagent row status pill、`ChatInputStatusTabs.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 row click/jump、popover close、任务/子代理分组、编辑选择、Store/Rust/Tauri response。优先级高：小屏 fallback 出现在输入主路径，出现频率高；实现成本低，回归面集中在静态 DOM markup。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.tsx` 新增局部 `getToolStatusTargetLabel(tool)`，复用已有 `statusLabel()`，并给 `renderToolRow()` 内的 `tool-state-pill` 写入 `title` / `aria-label`；可见状态文本和 row 级 jump label 保持不变。`src/components/chat/ChatInputStatusTabs.test.tsx` 扩展 task row、subagent row 与 key-only i18n fallback 用例，覆盖目标化标签和可见短状态文本。`.trellis/spec/frontend/component-guidelines.md` 已记录 small-screen composer fallback 的 task/subagent row status pill 必须匹配桌面 target-label 行为。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 3 项，失败点为 task/subagent row status pill 缺少 `npm: npm test · Success`、`Agent: Review composer status strip · Pending`、`npm: npm test --watch · Pending` 的目标化标签。GREEN 定向通过：同命令通过（1 file / 24 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。全量前端通过：`npm test`（55 files / 503 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在。`git diff --check -- src/components/chat/ChatInputStatusTabs.tsx src/components/chat/ChatInputStatusTabs.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx` 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察小屏 composer status strip 的 hover/focus/tooltip；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 对 composer strip MCP server row 的 enabled/disabled status pill 做 target-label 收束，例如 `Filesystem: Enabled` / `GitHub: Disabled`，与桌面 MCP status pill 规则一致；2) 对 `StatusPanel` current-activity pending/error 数字 pill 做目标化 `aria-label` 点测，确认 `Pending tools: 3` / `Failed tools: 1` 已覆盖；3) 启动真实 Tauri desktop smoke，构造小屏 tasks/subagents/edits/MCP popover，点测 tooltip、焦点顺序、Escape 关闭和跳转后 popover 关闭；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 todo/files/subagents、composer strip fallback、状态 pill、文件变更列表/树和 MCP 可用性。

## 本轮 PLAN 2026-06-21 input status mcp server status pill target label

- 目标：让 `ChatInputStatusTabs` 小屏 composer status strip 的 MCP server row 状态 pill 在保持紧凑 `Enabled` / `Disabled` 可见文本的同时，向辅助技术和 hover tooltip 暴露 `服务器名: 状态`，与桌面 `StatusPanel` MCP server-row status pill target-label 规则一致。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx` 的 `renderMcpServerRow` 与对标项目 `StatusPanel`；确认本项目小屏 MCP row 状态 pill 只有短状态文本，缺少 server target `title` / `aria-label`。
- 功能点 3：补 RED 测试。验证方式：扩展 `ChatInputStatusTabs.test.tsx` 的 MCP server details 与 key-only i18n fallback 用例，断言状态 pill 输出 `Filesystem: Enabled` / `GitHub: Disabled` 的 `title` / `aria-label`；旧实现应因状态 pill 缺少目标化标签失败。
- 功能点 4：实现最小交互修复。验证方式：在 `ChatInputStatusTabs.tsx` 中新增局部 MCP server status label helper，并给 `renderMcpServerRow()` 内 `tool-state-pill` 写入 `title` / `aria-label`；不改 MCP 配置读取、live connectivity、server row 结构、popover 行为、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 small-screen composer fallback 的 MCP server status pill 必须包含 server name 和 enabled/disabled 状态。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 input status mcp server status pill target label

- 上轮进展与阻塞：上一轮完成 `ChatInputStatusTabs` 小屏 composer status strip 的 task / subagent row 状态 pill 目标化标签；本轮代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已存在 `getToolStatusTargetLabel(tool)`，`renderToolRow()` 内 `tool-state-pill` 已写入 `title` / `aria-label`，`src/components/chat/ChatInputStatusTabs.test.tsx` 和 `.trellis/spec/frontend/component-guidelines.md` 也已有 task/subagent 对应断言与规范。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 使用 todo/files/subagents tabs + popover 展示任务、文件变更和子代理状态，没有本项目同构的小屏 composer MCP fallback server row。因此本轮没有直接 cc-gui 同类控件缺口可补齐；已核实的结论是本项目拥有额外的小屏 MCP 快速面板，需要按本项目现有桌面 `StatusPanel` MCP 规则补足语义。
- 本轮差距诊断（超越 cc-gui）：本项目 `ChatInputStatusTabs` 的 MCP quick-detail 面板会在输入主路径展示多个 MCP server，每一行右侧都有紧凑 `Enabled` / `Disabled` 状态 pill。旧实现状态 pill 只有短状态词，脱离视觉行上下文后无法表达归属服务器。用户价值：hover、键盘/辅助技术和自动化检查能直接获得 `Filesystem: Enabled` 或 `GitHub: Disabled`，多 server 场景下不需要从相邻文本推断状态归属。
- 本轮规划：只改 `ChatInputStatusTabs.tsx` 的 MCP server row status pill、`ChatInputStatusTabs.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 MCP 配置读取、live connectivity、server row 结构、popover 行为、Store/Rust/Tauri response。优先级中高：小屏 composer strip 在输入主路径出现，MCP server row 可重复出现；实现成本低，回归面集中在静态 DOM markup。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.tsx` 新增局部 `getMcpServerStatusLabel(server)` / `getMcpServerStatusTargetLabel(server)`，并在 `renderMcpServerRow()` 的 `tool-state-pill` 上写入 `title` / `aria-label`，可见文本仍保持 `Enabled` / `Disabled`。`src/components/chat/ChatInputStatusTabs.test.tsx` 在 MCP server details 和 key-only i18n fallback 用例中新增 `Filesystem: Enabled` / `GitHub: Disabled` 的 `title` / `aria-label` 断言。`.trellis/spec/frontend/component-guidelines.md` 已记录 small-screen composer fallback MCP server status pill 必须包含 server name 和 enabled/disabled 状态。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 2 项，失败点为 MCP server row status pill 缺少 `Filesystem: Enabled` / `GitHub: Disabled` 的目标化标签，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 24 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。全量前端通过：`npm test`（55 files / 503 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在。`git diff --check -- src/components/chat/ChatInputStatusTabs.tsx src/components/chat/ChatInputStatusTabs.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx` 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察小屏 composer status strip MCP popover；未手动确认 hover tooltip、焦点顺序、Escape 关闭和屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 对 `StatusPanel` current-activity pending/error 数字 pill 做目标化 `aria-label` 点测，确认 `Pending tools: 3` / `Failed tools: 1` 已覆盖且在 key-only i18n 下不泄漏 key；2) 启动真实 Tauri desktop smoke，构造小屏 tasks/subagents/edits/MCP popover，点测 tooltip、焦点顺序、Escape 关闭和跳转后 popover 关闭；3) 对 ToolBlock + StatusPanel 做一次运行态可访问性扫描，核对所有紧凑按钮和自定义 row 的 `title/aria-label/disabled/onKeyDown`；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 todo/files/subagents、composer strip fallback、状态 pill、文件变更列表/树和 MCP 可用性；5) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 session load error detail target label

- 目标：让 `StatusPanel` history session-load details 中的错误详情行在文本被截断时，通过 `title` / `aria-label` 同时暴露“History load: Load failed · 错误详情”，避免错误文本脱离诊断来源。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 session-load metrics 渲染与测试；确认 pending/error 数字 pill 已有目标化 `aria-label`，本轮真实缺口是 session-load error detail 只有 raw error `title` 且没有 `aria-label`。对标项目 cc-gui 可读代码没有本项目 history session-load diagnostic card。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 key-only history session-load fallback 用例，断言 cache error 行输出 `title="History load: Load failed · Cache read failed"` 与 `aria-label`；旧实现应因 title 只有 raw error 且缺少 `aria-label` 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中基于既有 `sessionLoadStatusAriaLabel` 生成 `sessionLoadErrorDetailLabel`，并同步到错误详情行的 `title` / `aria-label`；不改 session-load 状态计算、自动展开逻辑、指标明细、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 session-load error detail 必须携带诊断来源和失败状态的 target label。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 session load error detail target label

- 上轮进展与阻塞：上一轮完成 `ChatInputStatusTabs` 小屏 composer status strip 的 MCP server row 状态 pill 目标化标签；本轮代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已存在 `getMcpServerStatusTargetLabel(server)`，`renderMcpServerRow()` 内 `tool-state-pill` 已写入 `title` / `aria-label`，`src/components/chat/ChatInputStatusTabs.test.tsx` 和 `.trellis/spec/frontend/component-guidelines.md` 也已有 `Filesystem: Enabled` / `GitHub: Disabled` 对应断言与规范。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 与 `webview/src/components/StatusPanel/FileChangesList.tsx` 以 todo/files/subagents tabs + popover 展示任务和文件变化，没有本项目同构的 history session-load diagnostic card。因此本轮没有直接 cc-gui 同类控件缺口可补齐；已核实的结论是本项目额外提供历史加载诊断，需要按本项目已有可访问性规则补足语义。
- 本轮差距诊断（超越 cc-gui）：本项目 `StatusPanel` 的 history session-load details 在错误状态下会自动展开并显示具体错误，但旧实现错误详情行只有 raw error 文本和 raw error `title`，没有 `aria-label`，也没有说明这个错误属于 `History load: Load failed`。用户价值：当错误文本被右侧紧凑面板截断，或被辅助技术单独读取时，用户能直接获得“诊断来源 + 失败状态 + 具体错误”，减少把错误误认为 daemon、MCP 或普通工具失败的成本。
- 本轮规划：只改 `StatusPanel.tsx` 的 session-load error detail `title` / `aria-label`、`StatusPanel.test.tsx` 的 key-only i18n SSR 回归断言、前端组件规范和 Trellis 记录；不改 session-load 状态计算、自动展开逻辑、指标明细、Store/Rust/Tauri response。优先级中等：该 UI 只在历史加载异常时出现，但故障场景诊断价值高，实现成本低且回归面集中。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `sessionLoadErrorDetailLabel`，基于既有 `sessionLoadStatusAriaLabel` 与 raw error 组合为 `History load: Load failed · Cache read failed`，并同步到错误详情行的 `title` / `aria-label`；可见错误文本保持 raw error 不变。`src/components/chat/StatusPanel.test.tsx` 在 key-only history session-load fallback 用例中新增上下文化 `title` / `aria-label` 断言。`.trellis/spec/frontend/component-guidelines.md` 已记录 session-load error detail 必须携带诊断来源和失败状态的 target label。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为错误详情行仍输出 `title="Cache read failed"` 且缺少 `aria-label="History load: Load failed · Cache read failed"`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。全量前端通过：`npm test`（55 files / 503 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面触发 history session-load error card；未手动确认错误详情 tooltip、Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：构造 session-load loading/windowed/error/cache-hit 状态，点测 history-load 诊断卡片的自动展开、tooltip、状态 pill、错误详情和键盘焦点；2) 继续审计 `StatusPanel` 中非交互但会截断的诊断值，确认是否都具备上下文化 `title` / `aria-label`；3) 对 ToolBlock + StatusPanel 做运行态可访问性扫描，核对所有紧凑按钮、自定义 row、状态 pill 和诊断值的 `title/aria-label/disabled/onKeyDown`；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 todo/files/subagents、composer strip fallback、状态 pill、文件变更列表/树、MCP 可用性和历史加载诊断；5) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 mcp diagnostic error target labels

- 目标：让 `StatusPanel` MCP details 中的配置错误与 live-check 错误详情在文本被截断时，通过 `title` / `aria-label` 暴露诊断来源，例如 `MCP: Configuration error · Failed to load mcp.json` 与 `MCP: Live check failed · Request timed out`。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 MCP details 渲染与测试；确认 MCP summary toggle、live-check button、server row status pills 已有目标化标签，本轮真实缺口是 `status-mcp-diagnostic` 与 `status-mcp-live-error` 只有 raw error `title` 且没有 `aria-label`。对标项目 cc-gui 可读代码没有同构 MCP diagnostics。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 key-only MCP fallback 用例，断言配置错误行输出 `MCP: Configuration error · ...`，live-check 错误行输出 `MCP: Live check failed · ...` 的 `title` / `aria-label`；旧实现应因 title 只有 raw error 且缺少 `aria-label` 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中复用 `mcpStatusLabel` 生成配置错误和 live-check 错误的上下文 label，并同步到对应错误详情行；不改 MCP 状态计算、自动展开逻辑、live-check 点击逻辑、server row、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 MCP diagnostic error rows 必须携带 MCP 来源和错误类别的 target label。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 mcp diagnostic error target labels

- 上轮进展与阻塞：上一轮完成 `StatusPanel` history session-load error detail 的上下文化 `title` / `aria-label`；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已存在 `sessionLoadErrorDetailLabel`，错误详情行已同步写入 `title` / `aria-label`，`StatusPanel.test.tsx` 和 `.trellis/spec/frontend/component-guidelines.md` 也已有 `History load: Load failed · Cache read failed` 对应断言与规范。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 没有本项目同构的 MCP diagnostics / live-check 控件，因此本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目 MCP 可用性诊断误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 `StatusPanel` MCP details 已能展示配置错误、live-check 错误、检查中状态和 server live 结果；但旧实现中 `status-mcp-diagnostic` 与 `status-mcp-live-error` 只有 raw error 文本和 raw error `title`，没有 `aria-label`，也没有说明错误类别。用户价值：当错误文本被右侧紧凑面板截断，或被辅助技术单独读取时，用户能直接区分“配置错误”和“实时检查失败”，不用从 MCP 面板上下文推断。
- 本轮规划：只改 `StatusPanel.tsx` 的 MCP 配置错误 / live-check 错误详情 `title` / `aria-label`、`StatusPanel.test.tsx` 的 SSR 回归断言、`src/locales/en.json` / `src/locales/zh.json` 两个新错误类别文案、前端组件规范和 Trellis 记录；不改 MCP 状态计算、自动展开逻辑、live-check 点击逻辑、server row、Store/Rust/Tauri response。优先级中等偏高：MCP 诊断是排障入口，错误场景价值高，实现成本低且回归面集中。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `mcpConfigurationErrorLabel` / `mcpLiveCheckFailedLabel`，并生成 `mcpConfigurationErrorDetailLabel` / `mcpLiveCheckErrorDetailLabel`；MCP summary error title、配置错误行、live-check 错误行现在都带有 `MCP: Configuration error · ...` 或 `MCP: Live check failed · ...` 上下文，错误行可见文本保持 raw error 不变。`src/components/chat/StatusPanel.test.tsx` 扩展 MCP loading/error、live-check 和 key-only fallback 用例，覆盖普通 locale 与 key-only i18n。`src/locales/en.json` / `src/locales/zh.json` 新增 `mcpConfigurationError` 与 `mcpLiveCheckFailed`。`.trellis/spec/frontend/component-guidelines.md` 已记录 MCP diagnostic error rows 必须携带 MCP 来源和错误类别的 target label。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 3 项，失败点为 MCP 配置错误和 live-check 错误仍只有 raw error `title`，缺少 `MCP: Configuration error · ...` / `MCP: Live check failed · ...` 的上下文化 `title` / `aria-label`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。全量前端通过：`npm test`（55 files / 503 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx src/locales/en.json src/locales/zh.json .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面触发 MCP 配置错误或 live-check 错误；未手动确认错误详情 tooltip、Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：打开 MCP details，点测配置加载错误、live-check error/checking/success、server live result 和 disabled server status pill 的 tooltip、aria 与视觉状态；2) 继续审计 `StatusPanel` 中剩余 title-only 截断值，例如 daemon diagnostic、active tool summary/detail、recent-edit summary、active anchor 是否需要上下文化 label；3) 对 ToolBlock + StatusPanel 做运行态可访问性扫描，核对所有紧凑按钮、自定义 row、状态 pill 和诊断值的 `title/aria-label/disabled/onKeyDown`；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 todo/files/subagents、composer strip fallback、状态 pill、文件变更列表/树、MCP 可用性和历史加载诊断；5) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 daemon diagnostic target label

- 目标：让 `StatusPanel` daemon failure diagnostic 截断行通过 `title` / `aria-label` 暴露诊断来源和当前状态，例如 `Daemon: Daemon error · Error: node executable not found`，避免 raw error 脱离 daemon 区域上下文。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 daemon diagnostic 渲染与测试；确认 daemon status value 和 reconnect button 已有目标化标签，本轮真实缺口是 `status-daemon-diagnostic` 只有 raw error `title` 且没有 `aria-label`。对标项目 cc-gui 可读 `StatusPanel` 没有同构 daemon diagnostics。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 daemon failure diagnostics 用例，断言 diagnostic 行输出 `Daemon: Daemon error · ...` 的 `title` / `aria-label`；旧实现应因 title 只有 raw error 且缺少 `aria-label` 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中基于既有 `daemonStatusAriaLabel` 生成 `daemonDiagnosticDetailLabel`，并同步到 diagnostic 行；不改 daemon 状态分类、诊断文本截断、重连按钮、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 daemon diagnostic row 必须携带 daemon 来源和当前状态的 target label。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 daemon diagnostic target label

- 上轮进展与阻塞：上一轮完成 `StatusPanel` MCP 配置错误与 live-check 错误详情的上下文化 `title` / `aria-label`；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已存在 `mcpConfigurationErrorDetailLabel` / `mcpLiveCheckErrorDetailLabel`，对应错误行已写入 `title` / `aria-label`，`StatusPanel.test.tsx`、`src/locales/en.json`、`src/locales/zh.json` 和 `.trellis/spec/frontend/component-guidelines.md` 也已有 `MCP: Configuration error · ...` / `MCP: Live check failed · ...` 的断言、文案和规范。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 是 todo/files/subagents tabs + popover，没有本项目同构的 daemon diagnostics 或 reconnect recovery row，因此本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：避免把本项目额外诊断能力误判为 cc-gui parity 项，继续基于真实代码判断。
- 本轮差距诊断（超越 cc-gui）：本项目 `StatusPanel` daemon 区域已有状态值目标化 `aria-label` 和手动 reconnect button，但 `status-daemon-diagnostic` 截断行旧实现只有 raw error `title` 且没有 `aria-label`。用户价值：当 daemon error 文本在紧凑右侧面板被截断或被辅助技术单独读取时，用户能直接知道这是 `Daemon: Daemon error` 下的诊断，而不是 MCP、history load 或普通工具错误。
- 本轮规划：只改 `StatusPanel.tsx` 的 daemon diagnostic row `title` / `aria-label`、`StatusPanel.test.tsx` 的回归断言、前端组件规范和 Trellis 记录；不改 daemon 状态分类、诊断文本截断、重连按钮、Store/Rust/Tauri response。优先级中等：该行只在 daemon 故障时出现，但故障恢复场景价值高，实现成本低且回归面集中。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `daemonDiagnosticDetailLabel`，基于既有 `daemonStatusAriaLabel` 与 raw diagnostic 组合为 `Daemon: Daemon error · Error: node executable not found` / `守护进程: 守护进程异常 · Error: node executable not found`，并同步到 diagnostic 行的 `title` / `aria-label`；可见诊断文本保持 raw error 不变。`src/components/chat/StatusPanel.test.tsx` 扩展 daemon failure diagnostics 用例，覆盖 contextual `title` / `aria-label`。`.trellis/spec/frontend/component-guidelines.md` 已记录 daemon diagnostic rows 必须携带 daemon 来源和当前状态的 target label。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为 `status-daemon-diagnostic` 仍输出 `title="Error: node executable not found"` 且缺少 `aria-label="Daemon: Daemon error · Error: node executable not found"` / 中文等价标签，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。全量前端通过：`npm test`（55 files / 503 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面触发 daemon error diagnostic；未手动确认 daemon diagnostic tooltip、Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：构造 daemon shutdown/error/ready-timeout 状态，点测 status value、diagnostic row、reconnect idle/reconnecting 的 tooltip、aria 与视觉状态；2) 继续审计 `StatusPanel` 中剩余 title-only 截断值，例如 active tool summary/detail、recent-edit summary、active anchor 是否需要上下文化 label；3) 对 ToolBlock + StatusPanel 做运行态可访问性扫描，核对所有紧凑按钮、自定义 row、状态 pill 和诊断值的 `title/aria-label/disabled/onKeyDown`；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 todo/files/subagents、composer strip fallback、状态 pill、文件变更列表/树、MCP 可用性、历史加载诊断和 daemon 诊断；5) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 active anchor target label

- 目标：让 `StatusPanel` 当前消息锚点卡片在长文本被截断或被辅助技术读取时，通过 `title` / `aria-label` 暴露 `Current message: <anchor>`，避免只读出孤立的锚点文本。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 active anchor 渲染与测试；确认 current anchor label 已有 readable fallback，但卡片容器只有 raw `title` 且没有 `aria-label`。对标项目 cc-gui 可读 `StatusPanel` 没有同构 current-anchor card。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 key-only transient activity / anchor fallback 用例，断言输出 `title="Current message: Refactor status panel labels"` 与同值 `aria-label`；旧实现应因 title 只有 raw anchor 且缺少 `aria-label` 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中基于既有 `currentAnchorLabel` 与 `activeAnchorLabel` 生成 `activeAnchorTargetLabel`，并同步到当前消息卡片的 `title` / `aria-label`；不改 anchor 计算、可见文本、消息滚动、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 active/current anchor cards 必须携带 label + value 的 target label。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 active anchor target label

- 上轮进展与阻塞：上一轮完成 `StatusPanel` daemon diagnostic row 的上下文化 `title` / `aria-label`；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已存在 `daemonDiagnosticDetailLabel`，`status-daemon-diagnostic` 行已同步写入 `title` / `aria-label`，`StatusPanel.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有 `Daemon: Daemon error · ...` 对应断言和规范。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 是 todo/files/subagents tabs + popover，没有本项目同构的 current-message anchor card，因此本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目的定位增强能力误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 `StatusPanel` 会在右侧底部显示当前消息锚点，但旧实现中该卡片只有 raw anchor `title`，没有 `aria-label`，也没有说明它是 `Current message`。用户价值：当 anchor 文本较长或被辅助技术单独读取时，用户能直接知道它代表当前消息定位，而不是一段孤立摘要文本。
- 本轮规划：只改 `StatusPanel.tsx` 的 active anchor card `title` / `aria-label`、`StatusPanel.test.tsx` 的 key-only fallback 回归断言、前端组件规范和 Trellis 记录；不改 anchor 计算、可见文本、消息滚动、Store/Rust/Tauri response。优先级中等：该卡片只在存在 active anchor 时出现，但它是 transcript 定位反馈，用户价值明确，实现成本低且回归面集中。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `activeAnchorTargetLabel`，基于既有 `currentAnchorLabel` 与 `activeAnchorLabel` 组合为 `Current message: Refactor status panel labels`，并同步到当前消息卡片的 `title` / `aria-label`；可见 anchor 文本保持 raw label 不变。`src/components/chat/StatusPanel.test.tsx` 扩展 key-only transient activity / anchor fallback 用例，覆盖 contextual `title` / `aria-label`。`.trellis/spec/frontend/component-guidelines.md` 已记录 current-message anchor card 必须携带 label + value 的 target label。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为 current-anchor 卡片仍输出 `title="Refactor status panel labels"` 且缺少 `aria-label="Current message: Refactor status panel labels"`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。全量前端通过：`npm test`（55 files / 503 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面触发 active anchor card；未手动确认当前消息卡片 tooltip、Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：滚动/跳转消息后点测 current-message anchor card 的可见文本、tooltip、aria 与右侧状态面板定位反馈；2) 继续审计 `StatusPanel` 中剩余 title-only 截断值，例如 active tool summary/detail、recent-edit summary 是否需要上下文化 label；3) 对 ToolBlock + StatusPanel 做运行态可访问性扫描，核对所有紧凑按钮、自定义 row、状态 pill 和诊断值的 `title/aria-label/disabled/onKeyDown`；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 todo/files/subagents、composer strip fallback、状态 pill、文件变更列表/树、MCP 可用性、历史加载诊断、daemon 诊断和当前消息定位；5) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 active tool text target labels

- 目标：让 `StatusPanel` current activity active tool 的 summary/detail 截断行通过 `title` / `aria-label` 暴露工具类型上下文，例如 `Bash: npm run build` 与 `Bash: Running build verification`，避免只读出孤立命令或详情文本。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 active tool 渲染与测试；确认 active tool status pill 已有目标化标签，本轮真实缺口是 summary/detail 截断行只有 raw `title` 且没有 `aria-label`。对标项目 cc-gui 可读 `StatusPanel` 没有同构 current-activity active tool card。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 current activity header 用例，断言 active summary 输出 `title="Bash: npm run build"` / `aria-label`，detail 输出 `title="Bash: Running build verification"` / `aria-label`；旧实现应因 title 只有 raw 文本且缺少 `aria-label` 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中新增 active tool summary/detail target label helper，并同步到对应行；不改 active tool 选择、状态 pill、activity history、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 active tool summary/detail 截断文本必须携带工具类型上下文的 target label。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 active tool text target labels

- 上轮进展与阻塞：上一轮完成 `StatusPanel` current-message anchor card 的上下文化 `title` / `aria-label`；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已存在 `activeAnchorTargetLabel`，当前消息卡片已同步写入 `title` / `aria-label`，`StatusPanel.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有 `Current message: ...` 断言和规范。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 是 todo/files/subagents tabs + popover，没有本项目同构的 current-activity active tool summary/detail card，因此本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，避免把本项目额外的运行中工具可视化能力误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 `StatusPanel` current activity active tool 卡片已能显示工具类型 chip、状态 pill、summary 和 detail；但旧实现中 summary/detail 截断行只有 raw `title`，没有 `aria-label`，也没有说明文本属于哪个工具类型。用户价值：当右侧面板宽度较窄、命令或详情被截断，或辅助技术单独读取该行时，用户能直接听到/看到 `Bash: npm run build` 与 `Bash: Running build verification`，不用从相邻 chip 推断上下文。
- 本轮规划：只改 `StatusPanel.tsx` 的 active tool summary/detail `title` / `aria-label`、`StatusPanel.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 active tool 选择、状态 pill、activity history、Store/Rust/Tauri response。优先级中等偏高：运行中工具卡片出现在高频等待/观察路径，目标标签成本低，且延续近期状态面板可访问语义收束。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `getToolSummaryTargetLabel(tool)` 与 `getToolDetailTargetLabel(tool)`，将 active tool summary/detail 的 `title` / `aria-label` 从 raw 文本升级为 `工具类型: 文本`，可见文本保持 raw summary/detail 不变。`src/components/chat/StatusPanel.test.tsx` 扩展 current activity header 用例，覆盖 `title="Bash: npm run build"`、`aria-label="Bash: npm run build"`、`title="Bash: Running build verification"` 与同值 `aria-label`。`.trellis/spec/frontend/component-guidelines.md` 已记录 active tool summary/detail 截断文本必须携带工具类型上下文。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为 active tool summary 仍输出 `title="npm run build"` 且缺少 `aria-label="Bash: npm run build"`，detail 缺少 `aria-label="Bash: Running build verification"`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。接手后复核全量前端通过：`npm test`（55 files / 503 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面触发 active tool current-activity card；未手动确认 summary/detail tooltip、Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：运行会产生 active Bash/Task/Subagent 的 chat 流，点测 current activity 卡片的状态 pill、summary/detail tooltip、aria 与 activity history 同步；2) 继续审计 `StatusPanel` 中剩余非交互截断值，例如 recent-edit summary 是否需要补充 `aria-label` 或更明确的上下文；3) 对 ToolBlock + StatusPanel 做运行态可访问性扫描，核对所有紧凑按钮、自定义 row、状态 pill 和诊断值的 `title/aria-label/disabled/onKeyDown`；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 todo/files/subagents、composer strip fallback、状态 pill、文件变更列表/树、MCP 可用性、历史加载诊断、daemon 诊断、当前消息定位和 current activity active tool；5) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 mcp transport target label

- 目标：让 `StatusPanel` MCP server row 中被截断的 transport 值（例如 `stdio` / `sse`）在保留可见短文本的同时，通过 `title` / `aria-label` 暴露 `MCP server transport: filesystem · stdio` 这样的目标标签，避免密集 server 列表里出现无上下文的协议短词。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 MCP server row 渲染与测试；确认 transport 旧实现是裸 `<span className="truncate">stdio</span>`，没有 `title` / `aria-label`。对标项目 cc-gui 可读 `ServerCard` 在 MCP 设置页展开后用 label/value 展示 command/url，没有本项目 Chat 右侧 live-check server row 同构控件。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 MCP connectivity 与 key-only fallback 用例，断言 transport 输出 `title="MCP server transport: filesystem · stdio"` / `aria-label="MCP server transport: filesystem · stdio"`（中文环境允许 `MCP 服务器传输：filesystem · stdio`）；旧实现应因缺少这些属性失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 增加 `getMcpServerTransportLabel(serverName, transport)` 并同步到 transport span 的 `title` / `aria-label`；新增英文/中文 i18n key；不改 server row 布局、live-check 状态、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 MCP server-row transport 截断值必须携带 server + transport 的 target label。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 mcp transport target label

- 上轮进展与阻塞：上一轮完成 `StatusPanel` MCP server row inline latency 的 `aria-hidden` 收束；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已将 `liveLatencyText` 的视觉副本文本标记为 `aria-hidden="true"`，右侧 `status-mcp-live-result` pill 继续通过 `title` / `aria-label` 暴露 `filesystem: Online · 24ms`，`StatusPanel.test.tsx` 和 `.trellis/spec/frontend/component-guidelines.md` 也已有对应断言与规范。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/mcp/ServerCard.tsx` 在 MCP 设置页通过展开卡片的 label/value 行展示 command/url，`webview/src/components/StatusPanel/StatusPanel.tsx` 是 todo/files/subagent tabs + popover；未发现与本项目 Chat 右侧 `StatusPanel` MCP live-check server row 同构的 transport 短文本。因此本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的紧凑 MCP 排障面板误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 MCP server row 的 transport 值旧实现只显示裸 `stdio` / `sse`，没有 `title` / `aria-label`；这些值不是 live-result latency 那样的重复信息，而是用户判断 MCP 连接方式的重要诊断字段。用户价值：在密集 server 列表、截断 tooltip、自动化检查或辅助技术读取时，用户能直接知道 `stdio` 属于哪个 MCP server 的 transport，而不是听到孤立协议词。
- 本轮规划：只改 `StatusPanel.tsx` 的 transport target label、`StatusPanel.test.tsx` 的 SSR 回归断言、英文/中文 i18n key、前端组件规范和 Trellis 记录；不改 server row 布局、live-check 状态、MCP 状态计算、Store/Rust/Tauri response。优先级中等：transport 在每个已配置 MCP server row 高频出现，实现成本低，且完成本轮 MCP server row name/latency/transport 语义收束。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `getMcpServerTransportLabel(serverName, transport)`，使用 `chat.layout.mcpServerTransport` 和 fallback `MCP server transport: <server> · <transport>`，并同步写入 transport span 的 `title` / `aria-label`；可见 `stdio` / `sse` 文本保持不变。`src/components/chat/StatusPanel.test.tsx` 在 MCP connectivity 与 key-only fallback 用例中新增 transport target label 断言。`src/locales/en.json` 新增 `mcpServerTransport: "MCP server transport: {{server}} · {{transport}}"`，`src/locales/zh.json` 新增 `mcpServerTransport: "MCP 服务器传输：{{server}} · {{transport}}"`。`.trellis/spec/frontend/component-guidelines.md` 已记录 MCP server-row transport 截断值必须携带 server + transport 的 target label。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为旧实现仍输出裸 `<span class="truncate">stdio</span>` / `<span class="truncate">sse</span>`，缺少 `MCP server transport: ...` / `MCP 服务器传输：...` 的 `title` / `aria-label`。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。全量前端通过：`npm test`（55 files / 504 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx src/locales/en.json src/locales/zh.json .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx`、`StatusPanel.test.tsx`、`src/locales/en.json`、`src/locales/zh.json` 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面触发 MCP server row；未手动确认 transport tooltip、Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke：构造 MCP 可用、MCP loading、MCP config error、live-check failed、live-result online/offline/timeout/error 状态，点测 summary tooltip、toggle action label、server row server name/transport/latency/status pill 和 live-check button；2) 对 `StatusPanel` 剩余 title-only 或裸紧凑值做一次收尾扫描，确认是否还有未上下文化的截断值；3) 对 ToolBlock + StatusPanel 做运行态可访问性扫描，核对所有紧凑按钮、自定义 row、状态 pill、诊断值和 diff preview 的 `title/aria-label/aria-hidden/disabled/onKeyDown`；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 files popover、recent edits、diff preview、current activity、MCP 和 daemon 诊断；5) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 mcp latency duplicate readout

- 目标：让 `StatusPanel` MCP server row 中作为视觉辅助的 inline latency（例如 `24ms`）在 live-result pill 已经通过 `title` / `aria-label` 暴露 `filesystem: Online · 24ms` 时不再被辅助技术重复朗读，保持视觉扫描效率，同时避免无上下文的裸延迟值进入可访问名称流。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 MCP server row 渲染与测试；确认 `liveLatencyText` 同时出现在 inline 文本和 live-result pill target label。对标项目 cc-gui 可读 `StatusPanel` / MCP 设置组件没有本项目同构的 Chat 右侧 MCP live-check server row。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 MCP connectivity 与 key-only fallback 用例，断言 inline latency 输出 `aria-hidden="true"`；旧实现应因缺少该属性失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中给 inline `liveLatencyText` span 增加 `aria-hidden="true"`；不改 live-result pill、server row 布局、MCP 状态计算、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 MCP server-row inline latency 是 live-result pill 的视觉重复值，应在 pill 已提供完整 target label 时设为 `aria-hidden`。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 mcp latency duplicate readout

- 上轮进展与阻塞：上一轮完成 `StatusPanel` MCP server name 的目标标签补齐；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已存在 `getMcpServerNameLabel(server.name)`，并已将 `MCP server: filesystem` / `MCP 服务器：filesystem` 同步写入 server name 的 `title` 与 `aria-label`，`StatusPanel.test.tsx`、`src/locales/en.json`、`src/locales/zh.json` 和 `.trellis/spec/frontend/component-guidelines.md` 也已有对应断言、文案和规范。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 使用 todo/files/subagent tabs + popover，MCP 设置区另有 server card / tools panel；未发现与本项目右侧 Chat `StatusPanel` MCP live-check server row 同构的 inline latency 展示。因此本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，避免把本项目额外 MCP 排障能力误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 MCP server row 的 live-result pill 已通过 `title` / `aria-label` 暴露 `filesystem: 在线 · 24ms`，但同行左侧还显示裸 `24ms` inline latency。旧实现会让辅助技术在同一行重复读到完整 live-result 后又读一个无上下文 `24ms`。用户价值：保留视觉扫描速度，同时让可访问输出更接近用户真正需要的诊断语义，减少重复和歧义。
- 本轮规划：只改 `StatusPanel.tsx` 的 inline `liveLatencyText` span、`StatusPanel.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 live-result pill、server row 布局、MCP 状态计算、Store/Rust/Tauri response。优先级中等：MCP live-check 排障路径高频，修复成本极低，且与近期 MCP target-label 收束一致。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 给 MCP server row 的 inline latency span 增加 `aria-hidden="true"`，可见 `24ms` 保持不变，完整语义继续由右侧 `status-mcp-live-result` pill 的 `title` / `aria-label` 提供。`src/components/chat/StatusPanel.test.tsx` 在 MCP connectivity 与 key-only fallback 用例中新增 `class="flex-shrink-0" aria-hidden="true">24ms</span>` 断言。`.trellis/spec/frontend/component-guidelines.md` 已记录 inline latency 是 live-result pill 的视觉重复值，应该在完整 target label 已存在时 `aria-hidden`。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为旧实现输出 `<span class="flex-shrink-0">24ms</span>`，缺少 `aria-hidden="true"`。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。全量前端通过：`npm test`（55 files / 504 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` / `.trellis/spec/frontend/component-guidelines.md` 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面触发 MCP server row；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 对 `StatusPanel` MCP server transport inline 文本做剩余审计，判断是否需要 `MCP server transport: <server> · <transport>` 的 target label；2) 做真实 Tauri desktop smoke：构造 MCP 可用、MCP loading、MCP config error、live-check failed、live-result online/offline/timeout/error 状态，点测 summary tooltip、toggle action label、server row server name/status pill/live-check button；3) 对 ToolBlock + StatusPanel 做运行态可访问性扫描，核对所有紧凑按钮、自定义 row、状态 pill、诊断值和 diff preview 的 `title/aria-label/aria-hidden/disabled/onKeyDown`；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 files popover、recent edits、diff preview、current activity、MCP 和 daemon 诊断；5) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 recent edit summary target label

- 目标：让 `StatusPanel` 最近编辑卡片顶部的 edit summary 截断值在保持可见短文本 `N files · +A / -D` 的同时，通过 `title` / `aria-label` 暴露同一摘要，避免该紧凑统计在辅助技术或自动化检查中只有 hover tooltip、没有可访问名称。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 recent edit summary 渲染与测试；确认 `editSummaryLabel` 仅用于 `title`，缺少 `aria-label`。对标项目 cc-gui 可读 `StatusPanel` / `FileChangesList` 没有同构的右侧最近编辑 summary 行。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 recent edit fallback 用例，断言 summary 行输出 `title="2 files · +3 / -1"` 与 `aria-label="2 files · +3 / -1"`；旧实现应因缺少 `aria-label` 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中把 `editSummaryLabel` 同步写入 summary 行的 `aria-label`；不改统计计算、最近编辑树、diff 选择、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 recent-edit summary 的 `title` / `aria-label` 必须与可见摘要保持一致。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 recent edit summary target label

- 上轮进展与阻塞：上一轮完成 `StatusPanel` current activity active tool summary/detail 的上下文化 `title` / `aria-label`；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已存在 `getToolSummaryTargetLabel(tool)` / `getToolDetailTargetLabel(tool)`，active tool summary/detail 已同步写入 `title` 与 `aria-label`，`StatusPanel.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有 `Bash: npm run build` / `Bash: Running build verification` 断言和规范。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 与 `webview/src/components/StatusPanel/FileChangesList.tsx` 使用 files tab + popover + file row stats 展示文件变化，没有本项目同构的右侧最近编辑树顶部 summary 截断行。因此本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，避免把本项目额外的最近编辑概览误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 `StatusPanel` recent edits 卡片顶部显示 `N files · +A / -D` 紧凑摘要；旧实现可见文本和 `title` 都正确，但缺少 `aria-label`。用户价值：当该摘要作为紧凑统计被辅助技术或自动化检查读取时，仍能获得和 hover tooltip 一致的完整摘要，不依赖相邻 “Recent edits” 标题推断含义。
- 本轮规划：只改 `StatusPanel.tsx` 的 recent edit summary `aria-label`、`StatusPanel.test.tsx` 的 key-only i18n SSR 回归断言、前端组件规范和 Trellis 记录；不改统计计算、最近编辑树、diff 选择、Store/Rust/Tauri response。优先级中等：该 summary 在有文件改动时高频出现，实现成本极低，且延续近期状态面板 target-label 收束。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 将 `editSummaryLabel` 同步写入 recent edit summary 行的 `aria-label`，可见文本和 `title` 保持原样。`src/components/chat/StatusPanel.test.tsx` 在 recent edit fallback 用例中新增 `title="2 files · +3 / -1"` 与 `aria-label="2 files · +3 / -1"` 断言。`.trellis/spec/frontend/component-guidelines.md` 已记录 compact edit summary value 的 `title` / `aria-label` 必须与可见摘要一致。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为 recent edit summary 只有 `title="2 files · +3 / -1"`，缺少 `aria-label="2 files · +3 / -1"`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。全量前端通过：`npm test`（55 files / 503 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面触发 recent edits summary；未手动确认 summary tooltip、Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：构造 recent edits、hidden edit overflow、diff pane collapsed/expanded、folder collapse、open-file inner button，点测 tooltip、aria、焦点顺序和键盘行为；2) 对 `StatusPanel` 做一次剩余 title-only 值扫描，确认非交互截断值是否仍有只给 tooltip 不给 `aria-label` 的遗漏；3) 对 ToolBlock + StatusPanel 做运行态可访问性扫描，核对所有紧凑按钮、自定义 row、状态 pill 和诊断值的 `title/aria-label/disabled/onKeyDown`；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 todo/files/subagents、composer strip fallback、状态 pill、文件变更列表/树、MCP 可用性、历史加载诊断、daemon 诊断、当前消息定位和 current activity active tool；5) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 diff preview header target labels

- 目标：让 `EditDiffPreview` hover / panel header 中的文件路径、变更摘要、隐藏行数提示在保留现有可见文本和 `title` 的同时，也通过 `aria-label` 暴露同一目标值，避免 `StatusPanel` recent edit hover preview 和工具块 diff preview 的关键信息只存在于鼠标 tooltip。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `EditDiffPreview.tsx` / `EditDiffPreview.test.tsx` 与 cc-gui `StatusPanel` / `FileChangesList`；确认本项目 diff preview header 的 `edit-diff-hover-path`、`edit-diff-hover-summary`、status hidden-line clue 只有 `title`，缺少 `aria-label`。cc-gui 可读文件变化 popover没有本项目同构的 status hover diff preview。
- 功能点 3：补 RED 测试。验证方式：扩展 `EditDiffPreview.test.tsx`，断言 path / summary / status hidden-line clue 输出对应 `aria-label`；旧实现应因缺少这些 `aria-label` 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `EditDiffPreview.tsx` 给 path、summary、status hidden-line clue 同步写入 `aria-label`；不改行预算、截断、wrap、tooltip id、StatusPanel row `aria-describedby`、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 diff preview header 中 title-only 的路径/摘要/隐藏行提示必须同步 `aria-label`。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/toolblock/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 diff preview header target labels

- 上轮进展与阻塞：上一轮完成 `StatusPanel` 最近编辑 summary 的 `aria-label` 补齐；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已将 `editSummaryLabel` 同步写入 recent edit summary 的 `aria-label`，`StatusPanel.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有 `2 files · +3 / -1` 的 `title` / `aria-label` 断言和规范。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 与 `webview/src/components/StatusPanel/FileChangesList.tsx` 使用 files tab + popover + file row stats / diff action 展示文件变化，没有本项目同构的 `EditDiffPreview` hover/status header。因此本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的 hover diff preview 误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 `EditDiffPreview` 被 `StatusPanel` recent edit hover preview、`ChatDiffReviewPane` 和 Edit tool blocks 复用；旧实现中 header 的文件路径、行变更摘要、status surface 的隐藏行提示已有 `title`，但没有同值 `aria-label`。用户价值：当用户通过键盘、辅助技术或自动化检查理解 transient diff preview 时，关键上下文不会只停留在鼠标 tooltip 里。
- 本轮规划：只改 `EditDiffPreview.tsx` 的 header path / summary / status hidden-line clue `aria-label`，补 `EditDiffPreview.test.tsx` SSR 回归断言，更新前端组件规范和 Trellis 记录；不改行预算、截断、wrap、tooltip id、StatusPanel row `aria-describedby`、Store/Rust/Tauri response。优先级中等偏高：该组件复用面广，实现成本极低，且与近期 Chat UI target-label 收束一致。
- 本轮完成：`src/components/toolBlocks/EditDiffPreview.tsx` 给 `edit-diff-hover-path`、`edit-diff-hover-summary`、`edit-diff-hover-hidden-summary` 分别同步写入 `aria-label={filePath}`、`aria-label={previewLabel}`、`aria-label={moreLinesLabel}`，可见文本和 `title` 保持原样。`src/components/toolBlocks/EditDiffPreview.test.tsx` 增加 path / summary / hidden-line clue 的 `aria-label` 回归断言。`.trellis/spec/frontend/component-guidelines.md` 已记录 diff preview header 的路径、行变更摘要和 status-surface hidden-line clue 不能只是 tooltip-only 文本。
- 本轮验证：RED（交接时已完成）：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx` 先失败 2 项，失败点为缺少 `aria-label="src/example.ts"`、`aria-label="3 added, 1 removed"` 和 `aria-label="3 more lines"`。GREEN 定向通过：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx`（1 file / 6 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx`（6 files / 63 tests）。全量前端通过：`npm test`（55 files / 503 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/toolBlocks/EditDiffPreview.tsx src/components/toolBlocks/EditDiffPreview.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `EditDiffPreview.tsx` / `EditDiffPreview.test.tsx` 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面触发 recent edit hover preview 或工具块 diff preview；未手动确认 diff preview header tooltip、Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：构造 edit tool / apply_patch / recent edits，点测 hover diff preview、status hover preview、central diff review pane 的 header tooltip、aria、line budget 和 viewport positioning；2) 对 `EditDiffPreview` 的非 status hidden-line footer `edit-diff-hover-more` 做可访问性审计，确认是否也需要 `aria-label` 或上下文化说明；3) 做 ToolBlock + StatusPanel 运行态可访问性扫描，核对所有紧凑按钮、自定义 row、状态 pill、诊断值和 diff preview header 的 `title/aria-label/disabled/onKeyDown`；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 files popover、recent edits、diff preview、current activity、MCP 和 daemon 诊断；5) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 diff preview footer target label

- 目标：让 `EditDiffPreview` 默认 hover footer 的隐藏行提示 `edit-diff-hover-more` 在保留现有可见文本的同时，通过 `title` / `aria-label` 暴露同一提示，补齐上一轮 header target-label 后同组件剩余的 tooltip/accessible-name 缺口。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `EditDiffPreview.tsx` / `EditDiffPreview.test.tsx` 与 cc-gui `StatusPanel` / `FileChangesList`；确认本项目默认 hover footer `edit-diff-hover-more` 只有可见文本，缺少 `title` / `aria-label`。cc-gui 可读文件变化 popover没有本项目同构的 hover diff preview footer。
- 功能点 3：补 RED 测试。验证方式：扩展 `EditDiffPreview.test.tsx` 的默认 line-budget 用例，断言 footer 输出 `title="9 more lines"` 与 `aria-label="9 more lines"`；旧实现应因缺少这些属性失败。
- 功能点 4：实现最小展示修复。验证方式：在 `EditDiffPreview.tsx` 给 `edit-diff-hover-more` 同步写入 `title` / `aria-label`；不改行预算、header、status surface、tooltip id、wrap、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 diff preview footer hidden-line clue 和 header hidden-line clue 一样不能只是可见文本或 tooltip-only 文本。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/toolblock/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 diff preview footer target label

- 上轮进展与阻塞：上一轮完成 `EditDiffPreview` header path / summary / status hidden-line clue 的 `aria-label` 补齐；本轮代码复查确认 `src/components/toolBlocks/EditDiffPreview.tsx` 已给 `edit-diff-hover-path`、`edit-diff-hover-summary`、`edit-diff-hover-hidden-summary` 写入同值 `aria-label`，`EditDiffPreview.test.tsx` 和 `.trellis/spec/frontend/component-guidelines.md` 也已有对应断言与规范。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 与 `webview/src/components/StatusPanel/FileChangesList.tsx` 使用 files tab + popover + file row stats / diff action 展示文件变化，没有本项目同构的 hover diff preview footer。因此本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的 diff preview 信息密度误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 `EditDiffPreview` 默认 hover surface 在超过 16 行时会显示底部 `N more lines` footer；旧实现 footer 只有可见文本，没有 `title` / `aria-label`，而上一轮已经让 status surface 的 hidden-line clue 具备同值可访问标签。用户价值：默认 hover preview 和 status hover preview 的截断提示语义一致，用户不会因为 surface 不同而丢失隐藏行数的可访问名称。
- 本轮规划：只改 `EditDiffPreview.tsx` 的默认 footer hidden-line clue `title` / `aria-label`、`EditDiffPreview.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改行预算、header、status surface、tooltip id、wrap、Store/Rust/Tauri response。优先级中等：该 footer 在长 diff hover 里高频出现，修复成本极低，且完成同组件 target-label 收束。
- 本轮完成：`src/components/toolBlocks/EditDiffPreview.tsx` 给 `edit-diff-hover-more` 同步写入 `title={moreLinesLabel}` 和 `aria-label={moreLinesLabel}`，可见文本保持 `moreLinesLabel` 不变。`src/components/toolBlocks/EditDiffPreview.test.tsx` 在默认 line-budget 用例中新增 `title="9 more lines"` 与 `aria-label="9 more lines"` 断言。`.trellis/spec/frontend/component-guidelines.md` 已记录默认 hover footer hidden-line clue 与 header hidden-line clue 使用同一 `title` / `aria-label` 规则。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx` 先失败 1 项，失败点为 `edit-diff-hover-more` 仍输出 `<span class="edit-diff-hover-more">9 more lines</span>`，缺少 `title="9 more lines"` 与 `aria-label="9 more lines"`。GREEN 定向通过：同命令通过（1 file / 6 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx`（6 files / 63 tests）。全量前端通过：`npm test`（55 files / 503 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/toolBlocks/EditDiffPreview.tsx src/components/toolBlocks/EditDiffPreview.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `EditDiffPreview.tsx` / `EditDiffPreview.test.tsx` 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面触发默认 hover diff preview footer；未手动确认 footer tooltip、Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke：构造超过 16 行和超过 24 行的 edit/apply_patch diff，点测默认 hover footer、status hidden-line header、central diff pane 行预算和 viewport positioning；2) 对 `EditDiffPreview` 的 `edit-stat-added` / `edit-stat-deleted` 做审计，确认是否需要单独 `aria-hidden` 或依赖 summary 即可，避免重复播报；3) 做 ToolBlock + StatusPanel 运行态可访问性扫描，核对所有紧凑按钮、自定义 row、状态 pill、诊断值和 diff preview 的 `title/aria-label/disabled/onKeyDown`；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 files popover、recent edits、diff preview、current activity、MCP 和 daemon 诊断；5) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 diff preview stat pills aria hidden

- 目标：让 `EditDiffPreview` header 中紧邻 summary 的 `+N` / `-N` 视觉统计 pill 对辅助技术隐藏，避免读屏先读 `3 added, 1 removed` summary 后又重复读出 `+3` / `-1` 裸数字；视觉速读信息保持不变。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `EditDiffPreview.tsx` / `EditDiffPreview.test.tsx` 与 cc-gui `StatusPanel` / `FileChangesList`；确认本项目 summary 已通过 `title` / `aria-label` 暴露完整行变更语义，`edit-stat-added` / `edit-stat-deleted` 只是重复视觉 pill，旧实现未 `aria-hidden`。cc-gui 可读文件变化 popover没有本项目同构的 hover diff preview stats header。
- 功能点 3：补 RED 测试。验证方式：扩展 `EditDiffPreview.test.tsx` 的 compact diff summary 用例，断言 `edit-stat-added` / `edit-stat-deleted` 输出 `aria-hidden="true"`；旧实现应因缺少这些属性失败。
- 功能点 4：实现最小展示修复。验证方式：在 `EditDiffPreview.tsx` 给 `edit-stat-added` / `edit-stat-deleted` 加 `aria-hidden="true"`；不改可见文本、summary label、line budget、status surface、tooltip id、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 diff preview header 的 `+N` / `-N` visual stat pills 必须由 summary 承担可访问语义，pill 自身应 `aria-hidden`。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/toolblock/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 diff preview stat pills aria hidden

- 上轮进展与阻塞：上一轮完成 `EditDiffPreview` 默认 hover footer hidden-line clue 的 `title` / `aria-label` 补齐；本轮代码复查确认 `src/components/toolBlocks/EditDiffPreview.tsx` 已给 `edit-diff-hover-more` 写入 `title={moreLinesLabel}` 与 `aria-label={moreLinesLabel}`，`EditDiffPreview.test.tsx` 和 `.trellis/spec/frontend/component-guidelines.md` 也已有 `9 more lines` 对应断言与规范。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 与 `webview/src/components/StatusPanel/FileChangesList.tsx` 使用 files tab + popover + file row stats / diff action 展示文件变化，没有本项目同构的 hover diff preview stats header。因此本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的 diff preview header 误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 `EditDiffPreview` header 已通过 `edit-diff-hover-summary` 暴露完整 `3 added, 1 removed` 的可访问摘要，旁边的 `+3` / `-1` pill 只是视觉速读辅助。旧实现未隐藏这些视觉 pill，辅助技术可能先读完整摘要再重复读出裸数字。用户价值：读屏输出更干净，保留视觉扫描速度，同时避免同一变更统计被重复播报。
- 本轮规划：只改 `EditDiffPreview.tsx` 的 `edit-stat-added` / `edit-stat-deleted` 可访问性属性、`EditDiffPreview.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改可见文本、summary label、line budget、status surface、tooltip id、Store/Rust/Tauri response。优先级中等：该 header 在 edit hover/status/panel 相关路径复用，修复成本极低，且完成同组件 diff preview 语义收束。
- 本轮完成：`src/components/toolBlocks/EditDiffPreview.tsx` 给 `edit-stat-added` 和 `edit-stat-deleted` 写入 `aria-hidden="true"`，可见 `+N` / `-N` 文本保持不变，完整语义继续由 `edit-diff-hover-summary` 的 `title` / `aria-label` 承担。`src/components/toolBlocks/EditDiffPreview.test.tsx` 在 compact diff summary 用例中新增 `class="edit-stat-added" aria-hidden="true"` 与 `class="edit-stat-deleted" aria-hidden="true"` 断言。`.trellis/spec/frontend/component-guidelines.md` 已记录 diff preview visual stat pills 应由 summary 承担可访问语义，pill 自身应 `aria-hidden`。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx` 先失败 1 项，失败点为旧实现仍输出 `<span class="edit-stat-added">+3</span>` 与 `<span class="edit-stat-deleted">-1</span>`，缺少 `aria-hidden="true"`，符合预期缺口。GREEN 定向通过：同命令通过（1 file / 6 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx`（6 files / 63 tests）。全量前端通过：`npm test`（55 files / 503 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。本轮已清理构建产物并确认 `{"dist":false,"out":false}`。`git diff --check -- src/components/toolBlocks/EditDiffPreview.tsx src/components/toolBlocks/EditDiffPreview.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `EditDiffPreview.tsx` / `EditDiffPreview.test.tsx` 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面触发 hover diff preview 或 status diff preview；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 真实 Tauri desktop smoke：构造超过 16 行和超过 24 行的 edit/apply_patch diff，点测默认 hover footer、status hidden-line header、central diff pane 行预算和 viewport positioning；2) 对 `EditDiffPreview` 的 diff line number / marker / content 语义做一次专门审计，判断哪些是可读内容、哪些是纯布局辅助，避免误隐藏真实 diff；3) 做 ToolBlock + StatusPanel 运行态可访问性扫描，核对所有紧凑按钮、自定义 row、状态 pill、诊断值和 diff preview 的 `title/aria-label/aria-hidden/disabled/onKeyDown`；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 files popover、recent edits、diff preview、current activity、MCP 和 daemon 诊断；5) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 diff preview line row semantic labels

- 目标：让 `EditDiffPreview` 的每条 diff 行在保留现有视觉行号、`+` / `-` 标记和代码文本的同时，向辅助技术暴露上下文化行语义，例如 `Context line 10: keep before`、`Removed line 11: remove me`、`Added line 11: add me`，避免读屏只听到裸行号、标记和代码片段。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `EditDiffPreview.tsx` / `EditDiffPreview.test.tsx` 与 cc-gui `StatusPanel` / `FileChangesList`；确认本项目 diff 行当前没有上下文化 `aria-label`，cc-gui 可读文件变化 popover没有同构 hover diff preview 行级语义。
- 功能点 3：补 RED 测试。验证方式：扩展 `EditDiffPreview.test.tsx` 的 compact summary 用例，断言 body 是 `role="list"`，行是 `role="listitem"`，且三类行分别输出 `Context line 10: keep before`、`Removed line 11: remove me`、`Added line 11: add me`；旧实现应因缺少这些语义失败。
- 功能点 4：实现最小展示修复。验证方式：在 `EditDiffPreview.tsx` 增加本地行标签 helper，把 body 标记为 list，并给 unified / split 两种模式的行写入 `role="listitem"` 与 `aria-label`；同步 `src/locales/en.json` / `src/locales/zh.json` 的行级标签文案；不隐藏代码内容，不改可见行号、marker、line budget、tooltip id、StatusPanel row、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 diff preview 行级可访问名称必须包含变更类型、行号和代码文本，且不能把真实 diff 内容当作装饰隐藏。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/toolblock/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 diff preview line row semantic labels

- 上轮进展与阻塞：上一轮完成 `EditDiffPreview` header 中 `+N` / `-N` visual stat pills 的 `aria-hidden` 收束；本轮代码复查确认 `src/components/toolBlocks/EditDiffPreview.tsx` 已给 `edit-stat-added` 和 `edit-stat-deleted` 写入 `aria-hidden="true"`，`EditDiffPreview.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应断言和规范。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 与 `webview/src/components/StatusPanel/FileChangesList.tsx` 使用 files tab + popover + file row stats / diff action 展示文件变化，没有本项目同构的 hover diff preview 行级语义。因此本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的 hover/panel diff preview 误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 `EditDiffPreview` 现在会出现在工具块 hover、status hover 和中央 diff pane；旧实现的行内容只有裸行号、`+` / `-` marker 和代码文本，读屏可能把 `11 - remove me` 读成难以理解的碎片。用户价值：每条 diff 行都能直接表达 `Context/Removed/Added + line + code text`，用户不用从孤立符号推断变更类型，同时真实 diff 文本继续可见，不把代码内容误当装饰隐藏。
- 本轮规划：只改 `EditDiffPreview.tsx` 的 body/line 语义、`EditDiffPreview.test.tsx` 的 SSR 回归断言、`src/locales/en.json` / `src/locales/zh.json` 的行级标签文案、前端组件规范和 Trellis 记录；不改可见行号、marker、代码文本、line budget、tooltip id、StatusPanel row、Store/Rust/Tauri response。优先级中等偏高：该组件复用面广，长 diff 行高频出现；实现成本低，回归面集中在静态 markup 和 i18n 文案。
- 本轮完成：`src/components/toolBlocks/EditDiffPreview.tsx` 新增本地 line-label helper，把 `edit-diff-hover-body` 标记为 `role="list"`，并给 unified / split 两种模式的 diff 行写入 `role="listitem"` 与上下文化 `aria-label`。行标签对 context / removed / added 分别输出 `Context line <n>: <text>`、`Removed line <n>: <text>`、`Added line <n>: <text>`，空行回退为 `blank line`。可见行号、marker 和代码文本保持不变。`src/components/toolBlocks/EditDiffPreview.test.tsx` 新增 unified 三类行与 split mode 的回归断言。`src/locales/en.json` / `src/locales/zh.json` 新增 `diffPreviewBlankLine`、`diffPreviewContextLine`、`diffPreviewRemovedLine`、`diffPreviewAddedLine`。`.trellis/spec/frontend/component-guidelines.md` 已记录 diff preview 行级可访问名称必须包含变更类型、行号和代码文本，且不要把真实 diff 内容当装饰隐藏。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx` 先失败 1 项，失败点为旧实现 `edit-diff-hover-body` 缺少 `role="list"`，diff 行缺少 `role="listitem"` 和 `Context line 10: keep before` / `Removed line 11: remove me` / `Added line 11: add me` 的 `aria-label`，符合预期缺口。GREEN 定向通过：同命令通过（1 file / 7 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx`（6 files / 64 tests）。全量前端通过：`npm test`（55 files / 504 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/toolBlocks/EditDiffPreview.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/locales/en.json src/locales/zh.json .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `EditDiffPreview.tsx`、`EditDiffPreview.test.tsx`、`src/locales/en.json`、`src/locales/zh.json` 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面触发 hover diff preview、status diff preview 或中央 diff pane；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke：构造超过 16 行和超过 24 行的 edit/apply_patch diff，点测默认 hover footer、status hidden-line header、line row labels、central diff pane 行预算和 viewport positioning；2) 对 `EditDiffPreview` split mode 的 old/new cell 是否需要 column-level 辅助标签做专门审计，避免在双栏阅读时丢失旧/新列上下文；3) 做 ToolBlock + StatusPanel 运行态可访问性扫描，核对所有紧凑按钮、自定义 row、状态 pill、诊断值和 diff preview 的 `title/aria-label/aria-hidden/disabled/onKeyDown`；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 files popover、recent edits、diff preview、current activity、MCP 和 daemon 诊断；5) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 diff preview line row tooltip labels

- 目标：让 `EditDiffPreview` 的每条 diff 行在已有上下文化 `aria-label` 的基础上，同步提供同文案 `title`，使鼠标 hover tooltip 与辅助技术读出的行语义一致；可见行号、`+` / `-` marker 和代码文本保持不变。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `EditDiffPreview.tsx` / `EditDiffPreview.test.tsx` 与对标项目 `StatusPanel` / `FileChangesList`；确认本项目 diff 行已有 `role="listitem"` 和上下文化 `aria-label`，但旧实现没有行级 `title`。cc-gui 可读文件变化 popover 没有本项目同构 hover diff preview 行级 tooltip 语义。
- 功能点 3：补 RED 测试。验证方式：扩展 `EditDiffPreview.test.tsx` 的 compact summary 与 split mode 用例，断言三类 diff 行输出与 `aria-label` 一致的 `title`；旧实现应因缺少这些 `title` 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `EditDiffPreview.tsx` 中把 `getLineAriaLabel(line)` 的结果局部缓存为 `lineLabel`，并同步写入 unified / split 两种模式行节点的 `title`、`role="listitem"`、`aria-label`；不改 i18n key、可见文本、line budget、tooltip id、StatusPanel row、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 hoverable/truncated diff preview line rows 的 `title` 应与上下文化可访问名称保持一致。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/toolblock/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 diff preview line row tooltip labels

- 上轮进展与阻塞：上一轮完成 `EditDiffPreview` 的 diff 行级 `role="list"` / `role="listitem"` / 上下文化 `aria-label`；本轮代码复查确认 `src/components/toolBlocks/EditDiffPreview.tsx` 已存在 `getLineAriaLabel(line)`，unified / split 两种行节点已写入 `aria-label`，`src/components/toolBlocks/EditDiffPreview.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有行级语义断言和规范。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 与 `webview/src/components/StatusPanel/FileChangesList.tsx` 使用 files tab + popover + file row stats / diff action 展示文件变化，没有本项目同构的 hover diff preview 行级 tooltip 语义。因此本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的 diff preview hover/panel 能力误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 `EditDiffPreview` 的 diff 行已经能向辅助技术暴露 `Context/Removed/Added + line + code text`，但旧实现没有同文案 `title`。鼠标用户在 hover 到被截断或符号密集的 diff 行时，只能看到拆散的行号、marker、代码片段。用户价值：视觉 hover tooltip 与辅助技术名称保持一致，用户不用从 `11 - remove me` 这类碎片推断变更类型。
- 本轮规划：只改 `EditDiffPreview.tsx` 的 unified / split 行节点 `title`、`EditDiffPreview.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 i18n key、可见行号、marker、代码文本、line budget、tooltip id、StatusPanel row、Store/Rust/Tauri response。优先级中等：该组件复用在工具块 hover、status hover 和中央 diff pane，行级 tooltip 修复成本低且回归面集中。
- 本轮完成：`src/components/toolBlocks/EditDiffPreview.tsx` 在 unified / split 两种 `previewLines.map()` 中局部缓存 `lineLabel = getLineAriaLabel(line)`，并同步写入行节点 `title={lineLabel}`、`role="listitem"`、`aria-label={lineLabel}`；可见 diff 内容保持不变。`src/components/toolBlocks/EditDiffPreview.test.tsx` 在 compact diff summary 和 split mode 用例中新增 `title` 与 `aria-label` 同步断言。`.trellis/spec/frontend/component-guidelines.md` 已记录 hoverable/truncated diff preview line rows 的 `title` 应与上下文化可访问名称保持一致。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx` 先失败 2 项，失败点为 unified 三类行和 split removed/added 行缺少与 `aria-label` 一致的 `title`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 7 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx`（6 files / 64 tests）。全量前端通过：`npm test`（55 files / 504 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/toolBlocks/EditDiffPreview.tsx src/components/toolBlocks/EditDiffPreview.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `EditDiffPreview.tsx` / `EditDiffPreview.test.tsx` 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面触发 hover diff preview、status diff preview 或中央 diff pane；未手动确认浏览器原生 tooltip、Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke：构造超过 16 行和超过 24 行的 edit/apply_patch diff，点测默认 hover footer、status hidden-line header、line row tooltip、central diff pane 行预算和 viewport positioning；2) 对 `EditDiffPreview` split mode 的 old/new cell 是否需要 column-level 辅助标签做专门审计，避免双栏阅读时丢失旧/新列上下文；3) 做 ToolBlock + StatusPanel 运行态可访问性扫描，核对所有紧凑按钮、自定义 row、状态 pill、诊断值和 diff preview 的 `title/aria-label/aria-hidden/disabled/onKeyDown`；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 files popover、recent edits、diff preview、current activity、MCP 和 daemon 诊断；5) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 diff preview split cell target labels

- 目标：让 `EditDiffPreview` split 模式的 old/new 单元格在已有行级 `title` / `aria-label` 基础上，补充列上下文，例如 `Old side: Removed line 11: remove me`、`New side: no content for Removed line 11: remove me`，避免双栏阅读时只知道行变化、不知道当前内容属于旧列还是新列。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `EditDiffPreview.tsx` / `EditDiffPreview.test.tsx` 与对标项目 `StatusPanel` / `FileChangesList`；确认本项目 split row 已有行级 label，split cell 仍缺 old/new column target label。cc-gui 可读文件变化 popover没有本项目同构的 split diff cell 语义。
- 功能点 3：补 RED 测试。验证方式：扩展 `EditDiffPreview.test.tsx` 的 split mode 用例，断言 old/new 非空 cell 与 empty cell 都输出 `title` / `aria-label`，旧实现应因 cell 缺少列级属性失败。
- 功能点 4：实现最小展示修复。验证方式：在 `EditDiffPreview.tsx` 增加 split cell label helper，给 old/new 两列单元格写入 `title` / `aria-label`；新增英文/中文 i18n key；不隐藏真实 diff 内容，不改行级 label、可见行号、marker、代码文本、line budget、tooltip id、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 split diff cell 必须携带 old/new 列上下文，empty side 也要说明该列无对应内容。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/toolblock/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 diff preview split cell target labels

- 上轮进展与阻塞：上一轮完成 `EditDiffPreview` diff 行级 tooltip label 同步；本轮代码复查确认 `src/components/toolBlocks/EditDiffPreview.tsx` 已将 `getLineAriaLabel(line)` 结果同步到 unified / split 行节点 `title`、`role="listitem"`、`aria-label`，`src/components/toolBlocks/EditDiffPreview.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有行级 tooltip 断言和规范。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 与 `webview/src/components/StatusPanel/FileChangesList.tsx` 使用 files tab + popover + file row stats / diff action 展示文件变化，没有本项目同构的 split diff hover/panel cell 语义。因此本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的 split diff 预览能力误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 `EditDiffPreview` split 模式会同时展示 old/new 两列；行级标签已经能表达 `Removed line 11: remove me`，但旧实现中 old/new cell 本身只有 class，没有列级 `title` / `aria-label`。用户价值：当用户逐列查看双栏 diff，或 hover 到新增/删除行的空白侧时，可以直接知道这是旧列还是新列，以及空白代表该列无对应内容。
- 本轮规划：只改 `EditDiffPreview.tsx` 的 split cell `title` / `aria-label`、`EditDiffPreview.test.tsx` 的 SSR 回归断言、`src/locales/en.json` / `src/locales/zh.json` 的列级标签文案、前端组件规范和 Trellis 记录；不改行级 label、可见行号、marker、代码文本、line budget、tooltip id、Store/Rust/Tauri response。优先级中等：split diff 是高价值阅读模式，列级标签成本低，且完成同组件 line/cell 语义收束。
- 本轮完成：`src/components/toolBlocks/EditDiffPreview.tsx` 新增本地 `getSplitCellLabel(lineLabel, side, isEmpty)` helper，并给 split old/new cell 写入列级 `title` / `aria-label`。非空列输出 `Old side: <lineLabel>` / `New side: <lineLabel>`；新增/删除造成的空列输出 `Old side: no content for <lineLabel>` / `New side: no content for <lineLabel>`。`src/components/toolBlocks/EditDiffPreview.test.tsx` 扩展 split mode 用例，覆盖 removed old cell、removed new empty cell、added old empty cell、added new cell。`src/locales/en.json` / `src/locales/zh.json` 新增 `diffPreviewOldCell`、`diffPreviewNewCell`、`diffPreviewOldEmptyCell`、`diffPreviewNewEmptyCell`。`.trellis/spec/frontend/component-guidelines.md` 已记录 split diff cell 必须携带 old/new 列上下文，empty side 也要说明该列无对应内容。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx` 先失败 1 项，失败点为 split cell 缺少 `Old side: Removed line 11: remove me` / `New side: no content for Removed line 11: remove me` 等列级 `title` / `aria-label`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 7 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/EditDiffPreview.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx`（6 files / 64 tests）。全量前端通过：`npm test`（55 files / 504 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/toolBlocks/EditDiffPreview.tsx src/components/toolBlocks/EditDiffPreview.test.tsx src/locales/en.json src/locales/zh.json .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `EditDiffPreview.tsx`、`EditDiffPreview.test.tsx`、`src/locales/en.json`、`src/locales/zh.json` 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面触发 split diff hover preview、status diff preview 或中央 diff pane；未手动确认 split cell tooltip、Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke：构造新增、删除、上下文混合的 edit/apply_patch diff，点测 split old/new cell tooltip、row tooltip、status hover preview 和 central diff pane 行预算；2) 对 `EditDiffPreview` split cell 的上下文行是否需要更短视觉 tooltip 文案做实际运行审计，避免长代码行 tooltip 过宽；3) 做 ToolBlock + StatusPanel 运行态可访问性扫描，核对所有紧凑按钮、自定义 row、状态 pill、诊断值和 diff preview 的 `title/aria-label/aria-hidden/disabled/onKeyDown`；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 files popover、recent edits、diff preview、current activity、MCP 和 daemon 诊断；5) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 mcp summary target label

- 目标：让 `StatusPanel` 的 `status-mcp-summary` 摘要容器在已有 `title` 的基础上同步输出同值 `aria-label`，让 MCP 可用性摘要或配置错误不只依赖 hover tooltip，也能被辅助技术和自动化检查直接读取。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 MCP 摘要渲染与测试；确认 `status-mcp-summary` 旧实现只有 `title`，内部 toggle 的 `aria-label` 只描述展开/收起动作，不表达当前 MCP 可用性或配置错误摘要。cc-gui 可读 `StatusPanel` 没有同构的 MCP summary 容器。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 MCP availability / error / key-only fallback 用例，断言 summary 容器输出与 `title` 相同的 `aria-label`；旧实现应因 summary 容器缺少该 `aria-label` 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中新增 `mcpSummaryTargetLabel`，把普通摘要与错误摘要统一同步到 `title` / `aria-label`；不改 MCP 状态计算、auto-expand、server row、live-check、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 MCP summary 容器若通过 `title` 暴露可用性/配置错误，必须同值写入 `aria-label`；内部 toggle 仍保留动作型展开/收起标签。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 mcp server name target label

- 上轮进展与阻塞：上一轮完成 `StatusPanel` MCP live-check result pill 的 `title` / `aria-label` 同源化；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已将 `status-mcp-live-result` 的 `title` 优先绑定到 `liveStatusTargetLabel`，`aria-label` 同值，`StatusPanel.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有 `filesystem: Online · 24ms` / `filesystem: 在线 · 24ms` 的同源断言和规范。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx`、`webview/src/components/StatusPanel/FileChangesList.tsx` 与 MCP 设置组件没有本项目同构的 Chat 右侧 MCP live-check server row；本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的 MCP server 诊断行误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 `StatusPanel` MCP server row 中 server name 是截断文本；旧实现只给 `title="filesystem"` / `title="browser"`，没有 `aria-label`，也没有说明这是 MCP server 名称。用户价值：在密集 server 列表里，hover、辅助技术和自动化检查都能直接读到 `MCP server: filesystem` 或 `MCP 服务器：filesystem`，不用从上方 “Configured servers” 标题和相邻状态 pill 推断该值的身份。
- 本轮规划：只改 `StatusPanel.tsx` 的 MCP server name `title` / `aria-label`、`StatusPanel.test.tsx` 的 SSR 回归断言、`src/locales/en.json` / `src/locales/zh.json` 的 `mcpServerName` 文案、前端组件规范和 Trellis 记录；不改 server row 布局、live-check 状态、Store/Rust/Tauri response。优先级中等：server row 在 MCP 配置与排障路径高频出现，实现成本低，且延续 MCP 详情 target-label 收束。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `getMcpServerNameLabel(serverName)`，通过 `chat.layout.mcpServerName` 和 fallback `MCP server: <server>` 生成目标标签，并同步写入 server name 的 `title` / `aria-label`；可见 server name 文本保持不变。`src/components/chat/StatusPanel.test.tsx` 扩展 MCP connectivity 与 key-only fallback 用例，覆盖中文环境和 key-only fallback 下的 server name target label。`src/locales/en.json` 新增 `mcpServerName: "MCP server: {{server}}"`，`src/locales/zh.json` 新增 `mcpServerName: "MCP 服务器：{{server}}"`。`.trellis/spec/frontend/component-guidelines.md` 已记录 MCP server-row name 截断值必须携带 target label，不应只暴露裸 server id。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为旧实现仍输出 `<div ... title="filesystem">filesystem</div>` 与 `<div ... title="browser">browser</div>`，缺少 `MCP server: ...` / `MCP 服务器：...` 的 `title` / `aria-label`，符合本轮缺口。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。全量前端通过：`npm test`（55 files / 504 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx src/locales/en.json src/locales/zh.json .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx`、`StatusPanel.test.tsx`、`src/locales/en.json`、`src/locales/zh.json` 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面触发 MCP server row；未手动确认 server name tooltip、Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke：构造 MCP 可用、MCP loading、MCP config error、live-check failed、live-result online/offline/timeout/error 状态，点测 summary tooltip、toggle action label、server row server name/status pill 和 live-check button；2) 对 `StatusPanel` MCP server transport / latency inline 文本做剩余审计，判断这些布局辅助值是否应 `aria-hidden` 或需要 contextual title；3) 对 ToolBlock + StatusPanel 做运行态可访问性扫描，核对所有紧凑按钮、自定义 row、状态 pill、诊断值和 diff preview 的 `title/aria-label/aria-hidden/disabled/onKeyDown`；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 files popover、recent edits、diff preview、current activity、MCP 和 daemon 诊断；5) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 mcp server name target label

- 目标：让 `StatusPanel` MCP server row 中被截断的 server name 不只输出裸 `title="filesystem"`，而是通过 `title` / `aria-label` 暴露 `MCP server: filesystem` 这样的目标标签，帮助用户在密集 server 列表里直接理解该值的身份。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 MCP server row 渲染与测试；确认 server name 旧实现只有裸 `title={server.name}` 且没有 `aria-label`。对标项目 cc-gui 可读 `StatusPanel` / MCP 设置组件没有本项目同构的 Chat 右侧 MCP live-check server row。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 MCP connectivity 与 key-only fallback 用例，断言 server name 输出 `title="MCP server: filesystem"` / `aria-label="MCP server: filesystem"`（中文环境允许 `MCP 服务器：filesystem`）；旧实现应因 title 裸值且缺少 `aria-label` 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 增加 `getMcpServerNameLabel(serverName)` 并同步到 server name 的 `title` / `aria-label`；新增英文/中文 i18n key；不改 server row 布局、live-check 状态、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 MCP server name 截断值必须携带 `MCP server: <name>` 这类 target label。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 mcp live-result title label

- 上轮进展与阻塞：上一轮完成 `StatusPanel` MCP summary 容器的同源 `title` / `aria-label`；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已存在 `mcpSummaryTargetLabel`，并已同步写入 `status-mcp-summary` 容器 `title` 与 `aria-label`，`StatusPanel.test.tsx` 和 `.trellis/spec/frontend/component-guidelines.md` 也已有 summary 容器状态摘要与 inner toggle action label 分离的断言和规范。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx`、`webview/src/components/StatusPanel/FileChangesList.tsx` 与 MCP 设置组件没有本项目同构的 MCP live-check result pill；本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的 live-check 诊断能力误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 `StatusPanel` MCP live-check result pill 已通过 `aria-label` 暴露 `filesystem: 在线 · 24ms`，但旧实现的 `title` 只来自 latency/message 片段，在线延迟场景 hover 只显示裸 `24ms`。用户价值：鼠标 hover 用户和辅助技术用户获得同一条完整诊断语义，能直接知道延迟属于哪个 MCP server、当前 live status 是什么，不用从相邻行上下文拼回含义。
- 本轮规划：只改 `StatusPanel.tsx` 的 MCP live-result pill `title` 来源、`StatusPanel.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 live-check 数据结构、状态计算、server row、Store/Rust/Tauri response。优先级中等：live-check 是 MCP 排障路径关键反馈，tooltip 语义修复成本极低，且延续 MCP 详情 target-label 收束。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 将 `liveStatusAriaLabel` 更名为同源语义更准确的 `liveStatusTargetLabel`，并让 `status-mcp-live-result` 的 `title` 优先使用该完整目标标签，`aria-label` 保持同值；保留 `liveTitle` 作为无状态标签时的 fallback。`src/components/chat/StatusPanel.test.tsx` 在 MCP connectivity 用例中新增 live-result pill `title="filesystem: 在线 · 24ms"` / `aria-label="filesystem: 在线 · 24ms"` 同源断言，兼容英文 fallback。`.trellis/spec/frontend/component-guidelines.md` 已记录 MCP live-result pill 的 `title` / `aria-label` 必须同源表达 server + live status + latency/message。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为旧实现仍输出 `class="status-mcp-live-result ..."` 且 `title="24ms"`、`aria-label="filesystem: 在线 · 24ms"`，符合本轮缺口。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。全量前端通过：`npm test`（55 files / 504 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面触发 MCP live-check result pill；未手动确认 live-result tooltip、Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke：构造 MCP 可用、MCP loading、MCP config error、live-check failed、live-result online/offline/timeout/error 状态，点测 summary tooltip、toggle action label、server row status pill 和 live-check button；2) 对 `StatusPanel` MCP server name/transport/latency 文本做剩余 title-only 审计，判断哪些是重复布局文本、哪些需要 target label；3) 对 ToolBlock + StatusPanel 做运行态可访问性扫描，核对所有紧凑按钮、自定义 row、状态 pill、诊断值和 diff preview 的 `title/aria-label/aria-hidden/disabled/onKeyDown`；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 files popover、recent edits、diff preview、current activity、MCP 和 daemon 诊断；5) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 迭代记录 2026-06-21 mcp summary target label

- 上轮进展与阻塞：上一轮完成 `EditDiffPreview` split 模式 old/new 单元格列级 `title` / `aria-label`；本轮代码复查确认 `src/components/toolBlocks/EditDiffPreview.tsx` 已存在 `getSplitCellLabel(lineLabel, side, isEmpty)`，split old/new 非空与空白单元格已输出列级目标标签，`src/components/toolBlocks/EditDiffPreview.test.tsx`、`src/locales/en.json`、`src/locales/zh.json` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应断言、文案和规范。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/StatusPanel.tsx` 与 `webview/src/components/StatusPanel/FileChangesList.tsx` 没有本项目同构的 MCP summary 容器；本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的 MCP 可用性摘要能力误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 `StatusPanel` 的 `status-mcp-summary` 容器旧实现已经通过 `title` 暴露 `2 / 5 可用` 或 `MCP: 配置错误 · failed to load mcp config`，但容器自身没有同值 `aria-label`；内部 `status-mcp-toggle` 的 `aria-label` 只表达 `展开/收起 MCP 详情`，不表达当前状态摘要。用户价值：辅助技术和自动化检查可以直接读取 MCP 可用性或配置错误摘要，同时保留展开按钮的动作语义，避免把状态和动作混成一个标签。
- 本轮规划：只改 `StatusPanel.tsx` 的 MCP summary `title` / `aria-label` 同源目标标签、`StatusPanel.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 MCP 状态计算、auto-expand、server row、live-check、Store/Rust/Tauri response。优先级中等偏高：MCP 摘要在启用 MCP 或配置异常时高频出现，缺口修复成本极低，且延续近期状态面板 target-label 收束。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `mcpSummaryTargetLabel`，普通状态使用 `mcpSummaryText`，错误状态使用 `mcpConfigurationErrorDetailLabel`，并同步写入 `status-mcp-summary` 容器 `title` 与 `aria-label`；内部 `status-mcp-toggle` 仍保留展开/收起动作标签。`src/components/chat/StatusPanel.test.tsx` 扩展 MCP availability、error diagnostics、key-only fallback 用例，断言 summary 容器自身包含与 `title` 同值的 `aria-label`。`.trellis/spec/frontend/component-guidelines.md` 已记录 MCP summary 容器的 state summary 与 inner toggle action label 必须区分。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 3 项，失败点分别为普通 MCP summary、错误 MCP summary、key-only fallback summary 容器缺少同值 `aria-label`，符合旧实现缺口。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 96 tests）。全量前端通过：`npm test`（55 files / 504 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面触发 MCP summary 的 collapsed / expanded / error 状态；未手动确认 MCP summary tooltip、Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke：构造 MCP 可用、MCP loading、MCP config error、live-check failed 四种状态，点测 summary tooltip、toggle action label、server row status pill 和 live-check button；2) 对 `StatusPanel` 做一次剩余 title-only 值扫描，确认非交互截断值是否仍有只给 tooltip 不给 `aria-label` 的遗漏；3) 对 ToolBlock + StatusPanel 做运行态可访问性扫描，核对所有紧凑按钮、自定义 row、状态 pill、诊断值和 diff preview 的 `title/aria-label/aria-hidden/disabled/onKeyDown`；4) 做 cc-gui 与本项目真实运行画面并排截图审计，重点看 files popover、recent edits、diff preview、current activity、MCP 和 daemon 诊断；5) 开始收束同主题提交边界：把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 mcp live-result title label

- 目标：让 `StatusPanel` MCP live-check 结果 pill 的 hover `title` 与现有 `aria-label` 使用同一上下文化状态文案，例如 `filesystem: Online · 24ms`，避免鼠标用户只能看到裸 `24ms`。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 MCP live-result 渲染与测试；确认 `status-mcp-live-result` 已有 `aria-label="filesystem: Online · 24ms"`，但 `title` 只来自 latency/message 片段。对标项目 cc-gui 可读 `StatusPanel` / MCP 组件没有本项目同构的 live-check result pill。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 MCP connectivity 用例，断言 live-result pill 输出 `title="filesystem: Online · 24ms"` 与同值 `aria-label`；旧实现应因 `title="24ms"` 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中让 live-result pill 的 `title` 优先使用与 `aria-label` 同源的 `liveStatusTargetLabel`，保留 message/detail fallback；不改 live-check 数据结构、状态计算、server row、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 MCP live-result pill 的 `title` / `aria-label` 必须同源表达 server + live status + latency/message。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。
## 本轮 PLAN 2026-06-21 small-screen edit row described stats

- 目标：让 `ChatInputStatusTabs` 小屏 fallback 的 edit row 在整行按钮获得焦点时，也能通过 `aria-describedby` 关联同一行 `+N` / `-N` stats 的目标化说明，保持可见 UI 不变，同时让“Inspect full diff: <file>”动作和“Edit stats: <file> · +N / -N”规模信息处于同一可访问上下文。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx` 的 edits panel，并读取可读 cc-gui `FileChangesList.tsx`；确认本项目 stats group 已有独立 `title` / `aria-label`，但整行 diff 按钮尚未通过 `aria-describedby` 连接该说明。
- 功能点 3：补 RED 测试。验证方式：扩展 `ChatInputStatusTabs.test.tsx` 的 edits panel 用例和 key-only fallback 用例，断言 edit row button 的 `aria-describedby` 指向 stats group 的稳定 `id`；旧实现应因缺少该连接失败。
- 功能点 4：实现最小交互修复。验证方式：在 `ChatInputStatusTabs.tsx` 为 edit stats group 生成稳定 DOM id，并把该 id 写入整行按钮 `aria-describedby`；不改 diff selection、popover close、edit stats 计算、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录小屏 fallback edit row 的 action button 应连接 stats group 描述。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 small-screen edit row described stats

- 上轮进展与阻塞：上轮记录称已完成小屏 edit row stats 的目标化 `title` / `aria-label`；本轮代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已存在 `getEditStatsTargetLabel(edit)`，stats group 已输出 `Edit stats: <file> · +N / -N`，`src/components/chat/ChatInputStatusTabs.test.tsx` 和 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、Accessibility tree / 屏幕阅读器实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目也有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：cc-gui 可读 `webview/src/components/StatusPanel/FileChangesList.tsx` 的 file change row 会把文件名和 `+N` / `-N` 放在同一行扫描语境里；本项目小屏 `ChatInputStatusTabs` 已有 `Inspect full diff: <file>` 行按钮和独立 stats group 标签，但行按钮焦点没有通过 `aria-describedby` 连接 stats group，辅助技术用户聚焦整行操作时仍可能只听到动作，不听到变更规模。用户价值：让文件变更入口在同一焦点上下文里同时暴露“动作”和“规模”，补齐文件变更列表的可访问语义。
- 本轮差距诊断（超越 cc-gui）：本项目小屏 composer strip 是比 cc-gui 更靠近输入动作的 transient quick-detail surface。把整行 full-diff action 与 stats group 绑定后，用户不用在视觉相邻关系和单独 stats tooltip 之间切换，就能在一个焦点位置获得 `Inspect full diff: src/pages/ChatPage.tsx` + `Edit stats: src/pages/ChatPage.tsx · +5 / -2`。用户价值：窄屏、键盘和读屏路径下比较多文件编辑时更少上下文切换。
- 本轮规划：只改小屏 edit row 的可访问关系：为 stats group 生成稳定 DOM id，并在整行 diff button 上写入 `aria-describedby`；补 SSR 回归测试和前端组件规范。优先级中等偏高：多文件 edits 高频出现，用户价值明确，实现成本低，回归面集中在 SSR markup；不改 diff selection、popover 关闭、edit stats 计算、Store/Rust/Tauri response。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.tsx` 新增 `getEditStatsDescriptionId(edit)`，基于 `getChatStatusEditKey(edit)` 生成安全 id；edit row button 新增 `aria-describedby={editStatsDescriptionId}`；stats group 新增同 id，保留既有 `title` / `aria-label` 和可见 `+N` / `-N`。`src/components/chat/ChatInputStatusTabs.test.tsx` 扩展 edits panel 与 key-only fallback 用例，断言 `aria-describedby` 指向实际存在的 stats group id。`.trellis/spec/frontend/component-guidelines.md` 已记录小屏 fallback edit row action button 应连接 stats group 描述。`TODO_LIST.md` 已记录本轮 PLAN 与迭代结论。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 2 项，失败点均为找不到 edit row button 的 `aria-describedby`。GREEN 定向通过：同命令通过（1 file / 25 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/chatStatusSummary.test.ts`（6 files / 79 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理，`out` 不存在，复查结果 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/ChatInputStatusTabs.tsx src/components/chat/ChatInputStatusTabs.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察小屏 composer strip edits popover 的 hover/focus/tooltip；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。本轮结论基于真实源码、CodeGraph 复查、Trellis 规范、RED/GREEN 回归、全量前端/后端测试和 CLI build。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造多文件 edits、MCP 配置、task/subagent activity、recent edit tree、history load，点测小屏 popover 和右侧 `StatusPanel` 的 tooltip、焦点顺序和可访问名称；2) 审计 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact numeric/token surfaces，确认 remaining count、history load、daemon/MCP diagnostic、edit stats 是否都已带目标；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、MCP 配置、运行状态、状态 pill、文件改动列表/树和小屏 fallback；4) 若继续代码级小步优化，优先用 CodeGraph 搜索 Chat composer/menu 内部 dropdown options 或 remaining page-level controls 的 direct `t()` raw-key 风险，单轮只挑一个高频区块。
## 本轮 PLAN 2026-06-21 current activity count pill title labels

- 目标：让 `StatusPanel` 当前活动卡片右上角的 pending / failed 数字 pill 在保留紧凑数字视觉的同时，`title` 与现有 `aria-label` 一样暴露 `Pending tools: 3` / `Failed tools: 1` 这类完整语义，避免鼠标悬停只能看到泛化的 `Pending tools` / `Failed tools`。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 current activity count 渲染与测试；确认本项目已给数字 pill 写入 count-aware `aria-label`，但 `title` 仍只使用类别标签。对标项目 cc-gui 可读代码未发现同构的 current-activity 数字 pill。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 current activity count 用例，断言 pending / failed pill 的 `title` 也包含对应数量；旧实现应因 `title="Pending tools"` / `title="Failed tools"` 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中让 pending / failed count pill 的 `title` 使用现有 `pendingToolCountLabel` / `errorToolCountLabel`；不改可见数字、count 计算、活动列表、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 numeric-only current-activity count pill 的 `title` / `aria-label` 都应包含 count meaning。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 current activity count pill title labels

- 上轮进展与阻塞：上一轮完成 `ChatInputStatusTabs` 小屏 edit row 的 `aria-describedby` 关联；本轮代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已存在 `getEditStatsDescriptionId(edit)`，edit row button 已关联 stats group id，`ChatInputStatusTabs.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui 代码未发现本项目同构的 right-panel current-activity pending/failed 数字 pill；本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目更密集的状态面板能力误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 `StatusPanel` 当前活动卡片的 pending / failed 数字 pill 已通过 `aria-label` 暴露 `Pending tools: 3` / `Failed tools: 1`，但旧实现 hover `title` 仍只是 `Pending tools` / `Failed tools`，鼠标用户看不到具体数量语义。用户价值：在紧凑状态面板里，视觉数字、鼠标 tooltip 和辅助技术名称表达同一件事，减少用户在密集状态 token 之间反复对照。
- 本轮规划：只改 `StatusPanel.tsx` 的 current activity count pill `title`、`StatusPanel.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改可见数字、count 计算、活动列表、Store/Rust/Tauri response。优先级中等偏高：该控件在流式执行和多工具并发中高频出现，实现成本极低，回归面集中在静态 markup。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 将 pending / failed count pill 的 `title` 从 `pendingToolsLabel` / `errorToolsLabel` 改为现有 `pendingToolCountLabel` / `errorToolCountLabel`，与 `aria-label` 同源；可见文本仍是紧凑数字。`src/components/chat/StatusPanel.test.tsx` 更新 current activity count 用例，断言 `title="Pending tools: 3"` / `title="Failed tools: 1"` 或中文等价文案。`.trellis/spec/frontend/component-guidelines.md` 已记录 numeric-only current-activity count pill 的 `title` / `aria-label` 都应包含 count meaning。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为旧实现仍输出 `title="进行中工具"` / `title="失败工具"`，缺少数量，符合预期缺口。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts`（8 files / 97 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 current activity count pill hover tooltip；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 pending/error tools、active tool、MCP live-check、recent edits 和小屏 composer strip，点测 tooltip、焦点顺序和可访问名称；2) 对 `StatusPanel` 剩余 title-only 或 compact token surface 做一次目标化清单收束，重点看 history load、anchor、daemon/MCP diagnostic 是否仍有鼠标 tooltip 与 aria-label 不一致；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。
## 本轮 PLAN 2026-06-21 session load source target label

- 目标：让 `StatusPanel` 的 `status-session-load-metrics` 容器在保留历史会话 source path tooltip 的同时，通过 `title` / `aria-label` 暴露 `History load: Source · <path>` 这类目标化来源标签，避免辅助技术或自动化检查只能读到裸文件路径，或完全读不到容器来源。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 session-load 渲染与测试；确认 session-load status pill、toggle、error row 已有目标标签，但外层 source-path 容器仍只有裸 `title={sourcePath}`。对标项目 cc-gui 可读代码没有本项目同构的历史加载诊断卡。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 history session load metrics 用例和 key-only fallback 用例，断言 `status-session-load-metrics` 容器输出 `title` / `aria-label` 同源的 `History load: Source · <path>`；旧实现应因 `title` 仍是裸路径且缺少 `aria-label` 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中新增 `sessionLoadSourceTargetLabel`，组合现有 `sessionLoadMetricsLabel`、`sessionLoadSourceDetailLabel` 和 `sessionLoadMetrics.sourcePath`，并写入容器 `title` / `aria-label`；不改 session-load 状态计算、展开逻辑、历史加载数据结构、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 session-load diagnostic card 的 source-path container 不应只暴露裸路径，必须包含 `History load` + `Source` 上下文。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/session-load 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 session load source target label

- 上轮进展与阻塞：上一轮完成 `StatusPanel` current-activity pending / failed 数字 pill 的 count-aware `title`；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已将 pending / failed count pill 的 `title` 和 `aria-label` 同源绑定到 `Pending tools: 3` / `Failed tools: 1`，`StatusPanel.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `StatusPanel` 使用 todo/files/subagents tabs 与 popover，不包含本项目同构的历史会话 load metrics 诊断卡；本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的长历史加载诊断能力误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 `StatusPanel` 的 session-load 诊断区域已经给 toggle、status pill 和 error row 提供了目标化标签，但外层 `status-session-load-metrics` 容器旧实现只有裸 `title={sourcePath}`，没有 `aria-label`，也没有说明该 JSONL 路径代表历史加载来源。用户价值：长历史加载排障时，hover、辅助技术和自动化检查都能直接读到 `History load: Source · C:/.../session.jsonl`，不用从裸路径倒推语境。
- 本轮规划：只改 `StatusPanel.tsx` 的 session-load source target label、`StatusPanel.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 session-load 状态计算、展开逻辑、历史加载数据结构、Store/Rust/Tauri response。优先级中等：长历史会话加载和缓存排障中高价值，出现频率不如 activity pill 但修复成本极低，回归面集中在静态 markup。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `sessionLoadSourceTargetLabel`，组合现有 `sessionLoadMetricsLabel`、`sessionLoadSourceDetailLabel` 与 `sessionLoadMetrics.sourcePath`，并同步写入 `status-session-load-metrics` 容器 `title` / `aria-label`；可见 UI 与展开内容保持不变。`src/components/chat/StatusPanel.test.tsx` 扩展 completed metrics 与 key-only fallback 用例，覆盖中文/英文环境下 `History load: Source · <path>` 的同源 `title` / `aria-label`。`.trellis/spec/frontend/component-guidelines.md` 已记录 session-load diagnostic card source-path container 不应只暴露裸路径，必须包含 `History load` + `Source` 上下文。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为旧实现仍输出裸 `title="C:/Users/Administrator/.codex/sessions/session-1.jsonl"` 且缺少 `aria-label`，符合预期缺口。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts src/stores/useChatStore.test.ts`（9 files / 153 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 session-load diagnostic card 的 collapsed / expanded / error 状态；未手动确认 source-path tooltip、Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造长历史 session windowed/complete/cache/error 状态，点测 history load source tooltip、toggle、status pill、error row 和展开明细；2) 对 `StatusPanel` 剩余 title/action 不一致点做一轮更严格审计，重点看 activity row button 的 `title` 是否应从 summary/detail 改成 jump action label 或补 `aria-describedby`；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。
## 本轮 PLAN 2026-06-21 activity row action title labels

- 目标：让 `StatusPanel` 当前活动历史行的 button `title` 与 `aria-label` 使用同一个跳转动作标签，例如 `Jump to tool task: npm test -- StatusPanel` / `Jump to subagent activity: write regression coverage`，避免鼠标 tooltip 表达 summary/detail 而焦点语义表达 jump action。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 current activity row 渲染与测试；确认本项目 row `aria-label` 已是 transcript jump action，但 `title` 仍是 summary/detail。对标项目 cc-gui 可读 `StatusPanel` 使用 todo/files/subagents tabs 与 subagent row 展开，没有本项目同构的 transcript jump activity row。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 task row 与 subagent row 用例，断言 activity row button 的 `title` 与 `aria-label` 都使用 jump action label；旧实现应因 `title` 仍是 summary/detail 失败。
- 功能点 4：实现最小交互语义修复。验证方式：在 `StatusPanel.tsx` 中让 `renderActivityTaskRow` 的 button `title` 使用 `getActivityJumpLabel(tool)`；不改可见 summary/detail、status pill target label、tool selection、activity ordering、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 current-activity transcript jump row 的 button `title` / `aria-label` 必须描述跳转动作，summary/detail 继续作为可见行内容。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP/store 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 activity row action title labels

- 上轮进展与阻塞：上一轮完成 `StatusPanel` session-load source 容器目标化标签；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已有 `sessionLoadSourceTargetLabel`，`status-session-load-metrics` 容器已同步 `title` / `aria-label` 到 `History load: Source · <path>`，`StatusPanel.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 与 `SubagentList.tsx` 使用 todo/files/subagents tabs、popover 和 subagent row 展开，不包含本项目同构的 right-panel transcript jump activity row。本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目更密集的跳转型 activity history 误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 `StatusPanel` 当前活动历史行是一个可点击/可聚焦的 transcript jump button；旧实现 `aria-label` 已表达 `Jump to tool task` / `Jump to subagent activity`，但 `title` 仍是 summary/detail。用户价值：鼠标 hover、自动化检查和辅助技术名称表达同一个动作，用户不会把可点击行误解成只展示摘要的静态信息行。
- 本轮规划：只改 `StatusPanel.tsx` 的 activity row button `title`、`StatusPanel.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改可见 summary/detail、status pill target label、tool selection、activity ordering、Store/Rust/Tauri response。优先级中等偏高：activity history 在流式多工具任务中高频出现，实现成本极低，回归面集中在静态 markup。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 将 `renderActivityTaskRow` 的 button `title` 从 `[tool.summary, tool.detail].join('\n')` 改为 `getActivityJumpLabel(tool)`，与 `aria-label` 同源；可见 summary/detail 仍在行内展示，状态 pill 仍用 `Tool: target · Status`。`src/components/chat/StatusPanel.test.tsx` 扩展 task row 与 subagent row 用例，覆盖 `title="Jump to tool task: ..."` / `title="Jump to subagent activity: ..."`。`.trellis/spec/frontend/component-guidelines.md` 已记录 transcript jump row 的 `title` / `aria-label` 必须描述跳转动作，summary/detail 不应作为控制 tooltip。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为旧实现仍输出 `title="npm test -- StatusPanel\n targeted regression"` 与 `title="write regression coverage\n test-runner · sonnet"`，符合预期缺口。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts src/stores/useChatStore.test.ts`（9 files / 153 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 current activity task/subagent row 的 hover tooltip 和 jump 后高亮；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 pending/error tools、subagent activity、recent edits、session-load 和 MCP live-check，点测 tooltip、焦点顺序、跳转高亮和可访问名称；2) 对 `StatusPanel` 剩余 button/action `title` 与 `aria-label` 做收束审计，重点看 daemon reconnect、MCP toggle、recent edit overflow、diff panel reopen 是否都已同源；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。
## 本轮 PLAN 2026-06-21 composer activity row action title labels

- 目标：让小屏 `ChatInputStatusTabs` tasks/subagents quick-detail row 的 button `title` 与 `aria-label` 使用同一个跳转动作标签，例如 `Jump to tool task: npm test` / `Jump to subagent activity: Review composer status strip`，避免桌面 `StatusPanel` 已修复而小屏 fallback 仍把鼠标 tooltip 留在 summary/detail。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx`；确认小屏 row `aria-label` 已是 transcript jump action，但 `title` 仍是 summary/detail。对标项目 cc-gui 可读 `StatusPanel` 使用 tabs/popover，没有本项目同构的 composer quick-detail fallback row。
- 功能点 3：补 RED 测试。验证方式：扩展 `ChatInputStatusTabs.test.tsx` 的 task row 与 subagent row 用例，断言 row button 的 `title` 与 `aria-label` 都使用 jump action label；旧实现应因 `title` 仍是 summary/detail 失败。
- 功能点 4：实现最小交互语义修复。验证方式：在 `ChatInputStatusTabs.tsx` 中让 `renderToolRow` 的 button `title` 使用 `toolJumpLabel`；不改可见 summary/detail、status pill target label、popover 关闭、tool selection、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 composer quick-detail task/subagent jump row 的 button `title` / `aria-label` 必须描述跳转动作，summary/detail 继续作为可见行内容。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 composer activity row action title labels

- 上轮进展与阻塞：上一轮完成桌面 `StatusPanel` current activity task/subagent row 的 action-title 同源化；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已将 `renderActivityTaskRow` 的 button `title` 与 `aria-label` 同源绑定到 `getActivityJumpLabel(tool)`，`StatusPanel.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 使用 todo/files/subagents tabs 与 popover，没有本项目同构的 composer quick-detail fallback row。本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的小屏 fallback 状态入口误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目小屏 `ChatInputStatusTabs` tasks/subagents quick-detail row 是右侧 `StatusPanel` 隐藏时的主跳转入口；旧实现 `aria-label` 已表达 `Jump to tool task` / `Jump to subagent activity`，但 `title` 仍是 summary/detail。用户价值：窄屏或桌面右栏隐藏时，鼠标 hover、辅助技术名称和自动化检查都表达同一个 transcript jump 动作，和桌面状态面保持一致。
- 本轮规划：只改 `ChatInputStatusTabs.tsx` 的 task/subagent row button `title`、`ChatInputStatusTabs.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改可见 summary/detail、status pill target label、popover 关闭、tool selection、Store/Rust/Tauri response。优先级中等偏高：该入口是小屏高频状态路径，实现成本极低，回归面集中在静态 markup。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.tsx` 将 `renderToolRow` 的 button `title` 从 `[tool.summary, tool.detail].join('\n')` 改为 `toolJumpLabel`，与 `aria-label` 同源；可见 summary/detail 仍在行内展示，状态 pill 仍用 `Tool: target · Status`。`src/components/chat/ChatInputStatusTabs.test.tsx` 扩展 task row 与 subagent row 用例，覆盖 `title="Jump to tool task: npm test"` / `title="Jump to subagent activity: Review composer status strip"`。`.trellis/spec/frontend/component-guidelines.md` 已记录 composer quick-detail task/subagent jump row 的 `title` / `aria-label` 必须描述跳转动作，summary/detail 不应作为控制 tooltip。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 2 项，失败点为旧实现仍输出 `title="npm test\nTests passed"` 与 `title="Review composer status strip\nfrontend-reviewer · claude-sonnet-provider-20260601"`，符合预期缺口。GREEN 定向通过：同命令通过（1 file / 25 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/chatStatusSummary.test.ts`（8 files / 94 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/ChatInputStatusTabs.tsx src/components/chat/ChatInputStatusTabs.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅输出 Windows LF/CRLF 转换提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 small-screen composer status strip 的 task/subagent row hover tooltip、popover close 和 jump 后高亮；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 task/subagent/edit/MCP 状态，点测小屏 composer strip 的 tooltip、焦点顺序、popover close、跳转高亮和可访问名称；2) 对 `ChatInputStatusTabs` MCP server row 外层 `title={server.id}` 是否需要目标化 `aria-label` 做审计，避免外层容器和内部 server name/transport/status 语义重复或缺失；3) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact row 的 `title` / `aria-label` 做一轮自动化快照清单；4) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；5) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 composer mcp server name target label

- 目标：让小屏 `ChatInputStatusTabs` MCP quick-detail row 的 server name 不再只依赖外层裸 `title={server.id}`，而是在可见 server name 上复用 `MCP server: <name>` / `MCP 服务器：<name>` 目标标签，并避免外层容器 tooltip 与内部 transport/status 语义冲突。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx` 和桌面 `StatusPanel.tsx` 的 MCP server name 实现；确认桌面已有 `getMcpServerNameLabel`，小屏 MCP row 仍用外层裸 `title={server.id}`，可见 server name 没有 `title` / `aria-label`。可读 cc-gui MCP 设置卡只展示 server name/status/actions，没有本项目同构的小屏 Chat composer quick-detail MCP row。
- 功能点 3：补 RED 测试。验证方式：扩展 `ChatInputStatusTabs.test.tsx` 的 MCP server details 与 key-only fallback 用例，断言 visible server name 输出 `title="MCP server: Filesystem"` / `aria-label="MCP server: Filesystem"`，并且外层 row 不再输出裸 `title="filesystem"`；旧实现应失败。
- 功能点 4：实现最小展示修复。验证方式：在 `ChatInputStatusTabs.tsx` 增加 `getMcpServerNameTargetLabel(server)`，复用既有 `chat.layout.mcpServerName` i18n key 与 fallback，把标签写到 server name div；移除外层 row 的裸 id `title`；不改 MCP status/transport 计算、popover、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 composer MCP quick-detail server name 必须携带目标化 `title` / `aria-label`，外层 row 不应只暴露裸 server id。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 composer mcp server name target label

- 上轮进展与阻塞：上一轮完成小屏 `ChatInputStatusTabs` tasks/subagents quick-detail row 的 action-title 同源化；本轮代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已将 `renderToolRow` 的 button `title` 与 `aria-label` 同源绑定到 `toolJumpLabel`，`ChatInputStatusTabs.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 使用 todo/files/subagents tabs 与 popover；`webview/src/components/mcp/ServerCard.tsx` 是 MCP 设置卡，不是 Chat composer quick-detail surface。本轮没有直接 cc-gui 同构控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的小屏 MCP 状态入口误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目小屏 `ChatInputStatusTabs` MCP quick-detail row 中 server name 是截断文本；旧实现只在外层 row 放裸 `title={server.id}`，可见 server name 没有 `title` / `aria-label`。用户价值：窄屏 MCP 排障时，hover、辅助技术和自动化检查都能直接读到 `MCP server: Filesystem`，不会把外层裸 id `filesystem` 误解成整行摘要，也不会与内部 transport/status 标签混淆。
- 本轮规划：只改 `ChatInputStatusTabs.tsx` 的 MCP server name target label、`ChatInputStatusTabs.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 MCP status/transport 计算、popover 行为、Store/Rust/Tauri response。优先级中等：MCP quick-detail 是小屏排障高频入口，实现成本低，且桌面 `StatusPanel` 已有同款 `mcpServerName` 语义可复用。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.tsx` 新增 `getMcpServerNameTargetLabel(server)`，复用既有 `chat.layout.mcpServerName` i18n key 和 fallback `MCP server: <name>`，把 `title` / `aria-label` 写到可见 server name div，并移除外层 row 的裸 `title={server.id}`。`src/components/chat/ChatInputStatusTabs.test.tsx` 扩展 MCP server details 与 key-only fallback 用例，覆盖 `title="MCP server: Filesystem"` / `aria-label="MCP server: Filesystem"`，并断言不再输出 `title="filesystem"` / `title="github"`。`.trellis/spec/frontend/component-guidelines.md` 已记录 composer MCP quick-detail server name 必须携带目标化 `title` / `aria-label`，外层 row 不应只暴露裸 server id。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 2 项，失败点为旧实现仍输出外层 `title="filesystem"` / `title="github"`，且缺少 `title="MCP server: Filesystem"` / `aria-label="MCP server: Filesystem"`。GREEN 定向通过：同命令通过（1 file / 25 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/chatStatusSummary.test.ts`（8 files / 94 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 small-screen composer MCP row 的 server-name tooltip、transport/status label 组合和 popover 关闭；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 MCP 可用、MCP loading、MCP config error、task/subagent/edit 状态，点测小屏 composer strip 的 tooltip、焦点顺序、popover close 和可访问名称；2) 对 `ChatInputStatusTabs` MCP error row `title={mcpStatus.error}` 是否也需要 `aria-label` 与 `MCP: Configuration error · <message>` 目标化标签做审计；3) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact row 的 `title` / `aria-label` 做一轮自动化快照清单；4) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；5) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 composer mcp error target label

- 目标：让小屏 `ChatInputStatusTabs` MCP quick-detail error row 不再只暴露裸 `title={mcpStatus.error}`，而是通过 `title` / `aria-label` 同源输出 `MCP: Configuration error · <message>`，使错误来源和类型在 hover、辅助技术和自动化检查中都可读。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx` 和桌面 `StatusPanel.tsx` 的 MCP error 实现；确认桌面已有 `MCP: Configuration error · <message>` 目标标签，小屏 error row 仍只有裸错误 `title` 且没有 `aria-label`。可读 cc-gui 没有本项目同构的小屏 Chat composer MCP error row。
- 功能点 3：补 RED 测试。验证方式：扩展 `ChatInputStatusTabs.test.tsx` 的 MCP panel key-only fallback 用例，断言 error row 输出 `title="MCP: Configuration error · Unable to read MCP configuration"` 与同值 `aria-label`；旧实现应因裸 title 且缺少 aria-label 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `ChatInputStatusTabs.tsx` 增加 `mcpConfigurationErrorLabel` 和 `getMcpConfigurationErrorTargetLabel(error)`，复用既有 `chat.layout.mcpConfigurationError` i18n key 与 fallback；不改 MCP loading/error 数据结构、popover、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 composer MCP quick-detail error row 必须携带 `MCP: Configuration error · <message>` 目标化 `title` / `aria-label`。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 composer mcp error target label

- 上轮进展与阻塞：上一轮完成小屏 `ChatInputStatusTabs` MCP server name target label；本轮代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已新增 `getMcpServerNameTargetLabel(server)`，server name div 已输出 `title` / `aria-label` 到 `MCP server: <name>`，外层 row 的裸 `title={server.id}` 已移除，`ChatInputStatusTabs.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 使用 todo/files/subagents tabs 与 popover；`webview/src/components/mcp/ServerCard.tsx` 是 MCP 设置卡，没有本项目同构的小屏 Chat composer MCP error row。本轮没有直接 cc-gui 同构控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的小屏 MCP 错误提示误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目小屏 `ChatInputStatusTabs` MCP quick-detail error row 旧实现只给裸错误 `title="Unable to read MCP configuration"`，没有 `aria-label`，也没有说明这是 MCP 配置错误。用户价值：窄屏 MCP 配置排障时，hover、辅助技术和自动化检查都能直接读到 `MCP: Configuration error · Unable to read MCP configuration`，不用从 MCP tab 和错误颜色反推来源。
- 本轮规划：只改 `ChatInputStatusTabs.tsx` 的 MCP error row target label、`ChatInputStatusTabs.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 MCP loading/error 数据结构、popover 行为、Store/Rust/Tauri response。优先级中等：配置错误是 MCP 入口的关键失败态，出现频率低于正常 server row，但修复成本极低，且与桌面 `StatusPanel` 的 MCP configuration error 语义保持一致。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.tsx` 新增 `mcpConfigurationErrorLabel` 和 `getMcpConfigurationErrorTargetLabel(error)`，复用既有 `chat.layout.mcpConfigurationError` i18n key 与 fallback `Configuration error`；MCP error row 现在将 `title` / `aria-label` 同源设置为 `MCP: Configuration error · <message>`，可见错误文本保持原样。`src/components/chat/ChatInputStatusTabs.test.tsx` 扩展 key-only fallback 用例，覆盖 `title="MCP: Configuration error · Unable to read MCP configuration"` 与同值 `aria-label`，并防止裸 key 泄露。`.trellis/spec/frontend/component-guidelines.md` 已记录 composer MCP quick-detail error row 必须携带来源与错误类别目标标签。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 1 项，失败点为旧实现仍输出裸 `title="Unable to read MCP configuration"`，且缺少 `aria-label="MCP: Configuration error · Unable to read MCP configuration"`。GREEN 定向通过：同命令通过（1 file / 25 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/chatStatusSummary.test.ts`（8 files / 94 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 small-screen composer MCP error row 的 hover tooltip、可访问名称和 popover 行为；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 MCP 可用、MCP loading、MCP config error、task/subagent/edit 状态，点测小屏 composer strip 的 tooltip、焦点顺序、popover close 和可访问名称；2) 对 `ChatInputStatusTabs` loading row 是否需要 `title` / `aria-label="MCP: Loading MCP configuration..."` 做审计，确认是否会与可见 loading text 重复；3) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact row 的 `title` / `aria-label` 做一轮自动化快照清单；4) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；5) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 composer mcp loading target label

- 目标：让小屏 `ChatInputStatusTabs` MCP quick-detail loading row 在保留可见 `Loading MCP configuration...` 的同时，通过 `title` / `aria-label` 同源输出 `MCP: Loading MCP configuration...`，使短暂加载态在 hover、辅助技术和自动化快照中都带有 MCP 来源。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx` 的 MCP loading row；确认 loading row 旧实现只有可见 loading text，没有 `title` / `aria-label`。可读 cc-gui 没有本项目同构的小屏 Chat composer MCP loading row。
- 功能点 3：补 RED 测试。验证方式：扩展 `ChatInputStatusTabs.test.tsx` 的 MCP panel key-only fallback 用例，断言 loading row 输出 `title="MCP: Loading MCP configuration..."` 与同值 `aria-label`；旧实现应因缺少这些属性失败。
- 功能点 4：实现最小展示修复。验证方式：在 `ChatInputStatusTabs.tsx` 增加 loading target label 并同步到 loading row 的 `title` / `aria-label`；不改 MCP loading/error 数据结构、popover、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 composer MCP quick-detail loading row 必须携带来源化 `title` / `aria-label`。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 composer mcp loading target label

- 上轮进展与阻塞：上一轮完成小屏 `ChatInputStatusTabs` MCP error row target label；本轮代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已通过 `getMcpConfigurationErrorTargetLabel(error)` 将 error row 的 `title` / `aria-label` 同源设置为 `MCP: Configuration error · <message>`，`ChatInputStatusTabs.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 使用 todo/files/subagents tabs 与 popover；`webview/src/components/mcp/ServerCard.tsx` 是 MCP 设置卡，没有本项目同构的小屏 Chat composer MCP loading row。本轮没有直接 cc-gui 同构控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的小屏 MCP loading 状态误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目小屏 `ChatInputStatusTabs` MCP quick-detail loading row 旧实现只有可见 `Loading MCP configuration...`，没有 `title` / `aria-label`，也没有把短暂加载态明确归属到 MCP 面板。用户价值：窄屏 MCP 配置读取期间，hover、辅助技术和自动化检查都能直接读到 `MCP: Loading MCP configuration...`，与 error/server rows 的目标标签保持一致。
- 本轮规划：只改 `ChatInputStatusTabs.tsx` 的 MCP loading row target label、`ChatInputStatusTabs.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 MCP loading/error 数据结构、popover 行为、Store/Rust/Tauri response。优先级中等：loading 是短暂态但发生在 MCP 面板入口打开时，实现成本极低，且完成小屏 MCP quick-detail 状态行的 target-label 收束。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.tsx` 新增 `mcpLoadingTargetLabel = \`${mcpTabLabel}: ${mcpLoadingLabel}\``，并把 MCP loading row 的 `title` / `aria-label` 同源设置为该值，可见 loading 文案保持原样。`src/components/chat/ChatInputStatusTabs.test.tsx` 扩展 key-only fallback MCP panel 用例，覆盖 `title="MCP: Loading MCP configuration..."` 与同值 `aria-label`，并继续防止裸 `chat.layout.mcpLoading` 泄露。`.trellis/spec/frontend/component-guidelines.md` 已记录 composer MCP quick-detail loading row 必须保留可见 loading text，同时通过 `title` / `aria-label` 暴露 MCP 来源。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 1 项，失败点为旧实现的 loading row 缺少 `title="MCP: Loading MCP configuration..."` / `aria-label="MCP: Loading MCP configuration..."`。GREEN 定向通过：同命令通过（1 file / 25 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/chatStatusSummary.test.ts`（8 files / 94 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/ChatInputStatusTabs.tsx src/components/chat/ChatInputStatusTabs.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 small-screen composer MCP loading row 的 hover tooltip、可访问名称和 popover 行为；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 MCP 可用、MCP loading、MCP config error、task/subagent/edit 状态，点测小屏 composer strip 的 tooltip、焦点顺序、popover close 和可访问名称；2) 对 `ChatInputStatusTabs` MCP tab button 在 loading/error/server 混合状态下的 `aria-label` 是否需要区分 `MCP 0 / 0` 与错误/加载摘要做审计；3) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact row 的 `title` / `aria-label` 做一轮自动化快照清单；4) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；5) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 composer mcp tab state label

- 目标：让小屏 `ChatInputStatusTabs` 的 MCP tab button 在 loading/error 状态下不再只暴露 `MCP 0 / 0` 这类计数标签，而是把入口按钮的 `title` / `aria-label` 同源切换为 `MCP: Loading MCP configuration...` 或 `MCP: Configuration error · <message>`，与面板内 loading/error row 保持一致。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx` 的 tab button 与 MCP panel 渲染；确认面板内 loading/error row 已有目标标签，但 MCP tab button 仍用 `label + stat`。可读 cc-gui 没有本项目同构的小屏 composer MCP tab。
- 功能点 3：补 RED 测试。验证方式：扩展 `ChatInputStatusTabs.test.tsx` 的 key-only MCP panel 用例，用 button 级 regex 断言 `chat-input-status-tab-mcp` 的 `title` / `aria-label` 在 loading/error 时分别输出目标标签；旧实现应因 tab button 仍是 `MCP 0 / 0` 失败。
- 功能点 4：实现最小展示修复。验证方式：给 `renderTabButton` 增加可选 accessible label override，并只在 MCP tab 传入 loading/error 优先的目标标签；不改可见 stat、spinner、popover、MCP status 数据结构、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 composer MCP tab button 在 loading/error 状态下应复用对应 row 的目标标签，而普通配置态保留计数摘要。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build / file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 composer mcp tab state label

- 上轮进展与阻塞：上一轮完成小屏 `ChatInputStatusTabs` MCP loading row target label；本轮代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已将 loading row 的 `title` / `aria-label` 同源设置为 `MCP: Loading MCP configuration...`，`ChatInputStatusTabs.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 使用 todo/files/subagents tabs 与 popover；没有本项目同构的小屏 Chat composer MCP tab button。本轮没有直接 cc-gui 同构控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的小屏 MCP fallback 入口误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目小屏 `ChatInputStatusTabs` MCP panel 内部 loading/error row 已有明确目标标签，但入口 tab button 旧实现仍只输出 `MCP 0 / 0`。用户价值：窄屏用户还没打开或正聚焦 MCP tab 时，就能从 hover、辅助技术和自动化检查里读到当前是 `MCP: Loading MCP configuration...` 或 `MCP: Configuration error · <message>`，不用先进入面板再确认状态来源。
- 本轮规划：只改 `ChatInputStatusTabs.tsx` 的 MCP tab button accessible label override、`ChatInputStatusTabs.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改可见 stat、spinner、popover、MCP status 数据结构、Store/Rust/Tauri response。优先级中等：该入口是小屏 MCP 状态的第一焦点，loading/error 出现频率低于正常配置态，但实现成本极低，且与面板内 row 语义收束。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.tsx` 给 `renderTabButton` 增加可选 `accessibleLabelOverride`，并为 MCP tab 计算 `mcpTabAccessibleLabel`：error 优先使用 `MCP: Configuration error · <message>`，loading 使用 `MCP: Loading MCP configuration...`，普通配置态继续使用 `MCP enabled / total` 计数摘要。`src/components/chat/ChatInputStatusTabs.test.tsx` 用 button 级 regex 覆盖 `chat-input-status-tab-mcp` 在 loading/error 时的 `title` / `aria-label`，避免误匹配面板内 row。`.trellis/spec/frontend/component-guidelines.md` 已记录 composer MCP tab button 在 loading/error 状态下应复用对应 row 的目标标签。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 1 项，失败点为旧实现的 MCP tab button 仍输出 `aria-label="MCP 0 / 0"` / `title="MCP 0 / 0"`，符合预期缺口。GREEN 定向通过：同命令通过（1 file / 25 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/chatStatusSummary.test.ts`（8 files / 94 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/ChatInputStatusTabs.tsx src/components/chat/ChatInputStatusTabs.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 small-screen composer MCP tab 的 hover tooltip、focus announcement 和 popover 行为；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 MCP loading/error/configured、task/subagent/edit 状态，点测小屏 composer strip 的 tooltip、焦点顺序、popover close 和可访问名称；2) 对 `ChatInputStatusTabs` 普通 configured MCP tab 是否需要 `MCP: 1 / 2 available` 这类更语义化计数做审计，避免过度改动当前短标签；3) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact row 的 `title` / `aria-label` 做一轮自动化快照清单；4) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；5) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 composer mcp configured tab summary label

- 目标：让小屏 `ChatInputStatusTabs` 普通 configured MCP tab 在保留可见 `1 / 2` count pill 的同时，通过按钮 `title` / `aria-label` 输出 `MCP: 1 / 2 available`，避免读屏、hover 和自动化快照看到的比例脱离“可用服务器数 / 总服务器数”的含义。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx` / `StatusPanel.tsx` 的 MCP summary 渲染；确认右侧 `StatusPanel` 已复用 `mcpEnabledSummary` 输出 `1 / 2 available`，但小屏 MCP tab 普通态仍只输出 `MCP 1 / 2`。可读 cc-gui 没有本项目同构的小屏 composer MCP tab。
- 功能点 3：补 RED 测试。验证方式：扩展 `ChatInputStatusTabs.test.tsx` 普通 configured MCP tab 和 key-only fallback configured MCP panel 用例，断言 tab button 自身输出 `MCP: 1 / 2 available` / `MCP: 1 / 9 available`；旧实现应因 `MCP 1 / 2` 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `ChatInputStatusTabs.tsx` 复用 `chat.layout.mcpEnabledSummary` 与 fallback `${enabled} / ${total} available` 生成 MCP configured summary，只覆盖 MCP tab 的 `title` / `aria-label`，可见 count pill、spinner、popover、MCP 数据结构、Store/Rust/Tauri response 不变。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 ordinary configured-server state 下 MCP tab 可见 count pill 仍保持短计数，但 `title` / `aria-label` 应用 `MCP: 1 / 2 available` 这类上下文化摘要。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 desktop mcp summary source label

- 上轮进展与阻塞：上一轮完成 desktop `StatusPanel` MCP live-check result pill message target label；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已将 live result detail 从单一 latency 改为 message + latency，`StatusPanel.test.tsx` 已覆盖 `filesystem: Online/在线 · Handshake complete · 24ms`。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 是 todo/files/subagents tabs 与 popover，没有本项目同构的 MCP summary card。本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外 MCP 可用性摘要误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 desktop `StatusPanel` loading/empty/manual hint/error MCP rows 已经带 `MCP:` 来源标签，但非错误 summary 容器旧实现仍是裸 `2 / 5 available` / `1 / 2 available`。用户价值：折叠状态下 hover、读屏和自动化快照能直接读到 `MCP: 2 / 5 available`，不用依赖左侧图标或视觉标题推断这是 MCP 可用性摘要。
- 本轮规划：只改 `StatusPanel.tsx` 的 MCP summary target label、`StatusPanel.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 visible summary、展开状态、server rows、connectivity check、Store/Rust/Tauri response。优先级中等：MCP summary 在桌面状态面板常驻，用户价值明确，实现成本极低，并与既有 MCP source-label 规范收束。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 将 `mcpSummaryTargetLabel` 的非错误分支改为 `${mcpStatusLabel}: ${mcpSummaryText}`，错误分支继续保持 `MCP: Configuration error · ...`；可见 summary 和 toggle action label 不变。`src/components/chat/StatusPanel.test.tsx` 扩展 collapsed summary 与 key-only fallback 用例，覆盖 `MCP: 2 / 5 available` / `MCP: 1 / 2 available`。`.trellis/spec/frontend/component-guidelines.md` 已记录 desktop MCP summary 容器应保持可见 summary 紧凑，同时通过 `title` / `aria-label` 暴露 MCP 来源。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为旧实现 summary 容器 `title` / `aria-label` 仍是裸 `2 / 5 可用` 和 `1 / 2 available`。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/stores/useChatStore.test.ts`（6 files / 123 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 MCP summary 的 hover tooltip、折叠/展开行为和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 MCP summary loading/error/configured/live-check、no-server、current-message anchor、recent edit tree、daemon ready/offline/error/reconnecting、session-load、task/subagent/edit 状态，点测 tooltip、焦点顺序和可访问名称；2) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact numeric/token surfaces 做自动化快照清单，优先 session-load source/status rows、current activity hidden-count rows、recent edit overflow；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次；5) 若继续代码级小步优化，优先审计仍只有可见短文本、缺少 `title` / `aria-label` 或 `aria-describedby` 的 transient diagnostic rows。

## 迭代记录 2026-06-21 composer mcp configured tab summary label

- 上轮进展与阻塞：上一轮完成小屏 `ChatInputStatusTabs` MCP tab 在 loading/error 状态下复用面板 row 的目标标签；本轮代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已存在 `mcpTabAccessibleLabel`，loading 输出 `MCP: Loading MCP configuration...`，error 输出 `MCP: Configuration error · <message>`，`ChatInputStatusTabs.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 使用 todo/files/subagents tabs 与 popover，没有本项目同构的小屏 Chat composer MCP tab button。本轮没有直接 cc-gui 同构控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的小屏 MCP fallback 能力误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目右侧 `StatusPanel` MCP summary 已通过 `chat.layout.mcpEnabledSummary` 说明 `1 / 2 available`，但小屏 composer MCP tab 普通 configured 状态旧实现仍只输出 `MCP 1 / 2`。用户价值：窄屏用户聚焦或 hover MCP tab 时能直接理解 `1 / 2` 是可用服务器比例，而不是脱离上下文的裸计数；可见 UI 仍保持短 pill，不增加底部 composer 密度。
- 本轮规划：只改 `ChatInputStatusTabs.tsx` 的 MCP configured tab accessible label、`ChatInputStatusTabs.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改可见 stat、spinner、popover、MCP status 数据结构、Store/Rust/Tauri response。优先级中等：普通 configured MCP tab 比 loading/error 更高频出现，语义修复成本极低，且与右侧 `StatusPanel` 的 summary label 对齐。
- 本轮完成：`src/components/chat/ChatInputStatusTabs.tsx` 新增 `mcpConfiguredSummaryLabel`，复用 `chat.layout.mcpEnabledSummary` 与 fallback `${enabled} / ${total} available`；新增 `mcpTabStat` 保持可见 count pill `enabled / total`；`mcpTabAccessibleLabel` 在非 loading/error 的普通 configured 状态下输出 `${mcpTabLabel}: ${mcpConfiguredSummaryLabel}`。`src/components/chat/ChatInputStatusTabs.test.tsx` 扩展普通 configured tab 与 key-only fallback configured panel 用例，断言 MCP tab button 自身 `title` / `aria-label` 为 `MCP: 1 / 2 available` / `MCP: 1 / 9 available`。`.trellis/spec/frontend/component-guidelines.md` 已记录普通 configured MCP tab 的可见短计数与可访问语义摘要应分层处理。
- 本轮验证：RED：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx` 先失败 2 项，失败点为旧实现输出 `aria-label="MCP 1 / 2"` / `title="MCP 1 / 2"` 和 `aria-label="MCP 1 / 9"` / `title="MCP 1 / 9"`。GREEN 定向通过：同命令通过（1 file / 25 tests）。相邻验证通过：`npm test -- src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/chatStatusSummary.test.ts`（8 files / 94 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/ChatInputStatusTabs.tsx src/components/chat/ChatInputStatusTabs.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，仅 LF/CRLF 提示。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `ChatInputStatusTabs.tsx` / `ChatInputStatusTabs.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 small-screen composer MCP configured tab 的 hover tooltip、focus announcement 和 popover 行为；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 MCP loading/error/configured、task/subagent/edit 状态，点测小屏 composer strip 的 tooltip、焦点顺序、popover close 和可访问名称；2) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact numeric/token surfaces 做自动化快照清单，优先 remaining count、history load、daemon/MCP diagnostic、recent edit overflow；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次；5) 若继续代码级小步优化，优先审计 `ChatInputStatusTabs` / `StatusPanel` 中仍只通过 `title` 暴露上下文、但没有 `aria-label` 或 `aria-describedby` 的短值。

## 本轮 PLAN 2026-06-21 runtime context title target labels

- 目标：让右侧 `StatusPanel` runtime context tile 的紧凑值在 `title` 中保留来源标签，例如 `Model: claude-sonnet-4-6`、`Workspace: C:/...`、`SDK: C:/deps/claude-sdk`，避免 hover tooltip 只显示裸值或裸路径，而 `aria-label` 才有上下文。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 runtime context 渲染与测试；确认 active tool summary/detail 与 activity row action title 已有目标化标签，但 runtime context tile 的 `title` 仍是裸值。对标项目 cc-gui 可读 `StatusPanel` 使用 todo/files/subagents tabs 与 popover，没有本项目同构的 runtime context tile grid。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 runtime context 与 key-only fallback 用例，断言 model、permission mode、reasoning effort、workspace、SDK 的 `title` 输出带来源标签；旧实现应因 `title` 仍是裸值失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中复用现有 `getRuntimeContextValueLabel(label, value)`，把 runtime context value 的 `title` 与现有 `aria-label` 语义对齐；SDK tooltip 保留安装路径但前缀 `SDK:`；不改可见 UI、runtime context 数据结构、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 runtime context tile values 的 `title` / `aria-label` 都应带来源标签，SDK 路径 tooltip 也不能是裸路径。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP/store 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 runtime context title target labels

- 上轮进展与阻塞：上一轮完成小屏 `ChatInputStatusTabs` 普通 configured MCP tab summary label；本轮代码复查确认 `src/components/chat/ChatInputStatusTabs.tsx` 已输出 `MCP: 1 / 2 available` / `MCP: 1 / 9 available`，对应测试与 `.trellis/spec/frontend/component-guidelines.md` 也已有覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 与 `SubagentList.tsx` 使用 todo/files/subagents tabs、popover 与 subagent row 展开，没有本项目同构的 runtime context tile grid。本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的运行上下文密度能力误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 `StatusPanel` runtime context tile 的可见值很紧凑，`aria-label` 已能表达 `Model: ...` / `Workspace: ...`，但旧实现 hover `title` 仍是裸模型、裸权限模式、裸推理强度、裸工作区路径或裸 SDK 路径。用户价值：鼠标 hover、辅助技术名称和自动化快照表达同一语义，用户在密集状态面板中无需从相邻小标题反推裸值来源。
- 本轮规划：只改 `StatusPanel.tsx` 的 runtime context value `title`、`StatusPanel.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改可见 tile 布局、状态计算、runtime context 数据结构、Store/Rust/Tauri response。优先级中等：runtime context 在每个聊天会话都可见，路径/模型值经常截断，实现成本极低，回归面集中在静态 markup。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 将 model、permission mode、reasoning effort、workspace、SDK tile value 的 `title` 改为 `getRuntimeContextValueLabel(...)`；SDK tooltip 在保留安装路径可检查性的同时输出 `SDK: C:/deps/claude-sdk`，可见值仍是 `Claude Code SDK · Installed` / `Not installed`。`src/components/chat/StatusPanel.test.tsx` 扩展 runtime context 与 key-only fallback 用例，覆盖 `title="Model: claude-sonnet-4-6"`、`title="Permission mode: Auto Mode"`、`title="Reasoning effort: High"`、`title="Workspace: C:/guodevelop/ccg-switch"`、`title="SDK: C:/deps/claude-sdk"` 及中文等价文案。`.trellis/spec/frontend/component-guidelines.md` 已记录 runtime context tile values 的 `title` / `aria-label` 必须带来源标签，SDK 路径 tooltip 也要保留 `SDK:` 上下文。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为旧实现仍输出裸 `title="claude-sonnet-4-6"`、`title="Auto Mode"`、`title="High"`、`title="C:/guodevelop/ccg-switch"`、`title="C:/deps/claude-sdk"`。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts src/stores/useChatStore.test.ts`（9 files / 153 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 runtime context tile 的 hover tooltip、截断展示和焦点语义；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 runtime context、MCP loading/error/configured、task/subagent/edit、session-load 状态，点测 tooltip、焦点顺序、popover close 和可访问名称；2) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact numeric/token surfaces 做自动化快照清单，优先 remaining count、daemon/MCP diagnostic、recent edit overflow、anchor labels；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次；5) 若继续代码级小步优化，优先审计仍只有 `title`、缺少 `aria-label` 或 `aria-describedby` 的短值。

## 本轮 PLAN 2026-06-21 desktop mcp loading target label

- 目标：让桌面右侧 `StatusPanel` 展开后的 MCP loading 行在保留紧凑可见文案的同时，通过 `title` / `aria-label` 暴露 `MCP: Loading MCP configuration...`，与小屏 composer MCP loading row 的语义一致。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 与 `ChatInputStatusTabs.tsx` 的 MCP loading 渲染；确认小屏 composer MCP loading row 已有目标标签，但桌面 `StatusPanel` expanded MCP details loading row 仍只有裸文本。对标项目 cc-gui 可读 `StatusPanel` 没有本项目同构的 MCP 配置 loading 诊断行。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 MCP loading/error diagnostics 用例和 key-only fallback MCP 用例，断言 loading row 有 `status-mcp-loading`、`title` 和 `aria-label`；旧实现应因缺少这些属性失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中新增 `mcpLoadingTargetLabel = \`${mcpStatusLabel}: ${mcpLoadingLabel}\``，并写入 MCP loading row 的 `title` / `aria-label`；不改 MCP summary、展开逻辑、server row、live check、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 desktop MCP loading row 应 mirror small-screen MCP loading row 的 target-label 规则。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP/store 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 desktop mcp loading target label

- 上轮进展与阻塞：上一轮完成 `StatusPanel` runtime context tile `title` target labels；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已将 model、permission mode、reasoning effort、workspace、SDK tile value 的 `title` 改为 `getRuntimeContextValueLabel(...)`，测试与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 使用 todo/files/subagents tabs 与 popover，没有本项目同构的 MCP 配置 loading 诊断行。本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的 MCP 配置诊断能力误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目小屏 `ChatInputStatusTabs` MCP loading row 已输出 `MCP: Loading MCP configuration...`，但桌面 `StatusPanel` expanded MCP details 里的 loading row 旧实现只有裸 `Loading MCP configuration...` / `正在加载 MCP 配置…` 文本，没有 `title` 或 `aria-label`。用户价值：桌面用户在 MCP 配置加载的短暂状态中，hover、辅助技术和自动化快照都能直接读到来源是 MCP，而不是一个脱离详情卡上下文的加载短句。
- 本轮规划：只改 `StatusPanel.tsx` 的 MCP loading row target label、`StatusPanel.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 MCP summary、展开逻辑、server row、live check、MCP 数据结构、Store/Rust/Tauri response。优先级中等：loading 是短暂态但发生在 MCP 入口打开时，实现成本极低，并能收束 desktop 与 small-screen 的同一状态语义。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `mcpLoadingTargetLabel`，并给 expanded MCP details 的 loading row 添加 `status-mcp-loading` class、`title={mcpLoadingTargetLabel}` 和 `aria-label={mcpLoadingTargetLabel}`；可见文案保持原样。`src/components/chat/StatusPanel.test.tsx` 扩展 MCP loading/error diagnostics 与 key-only fallback 用例，覆盖中文资源和英文 fallback 下的 `MCP: Loading MCP configuration...` 目标标签。`.trellis/spec/frontend/component-guidelines.md` 已记录 desktop MCP loading row 应与 small-screen composer MCP loading row 保持 target-label 语义一致。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为旧实现缺少 `status-mcp-loading`、`title="MCP: Loading MCP configuration..."` / 中文等价、`aria-label="MCP: Loading MCP configuration..."` / 中文等价。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts src/stores/useChatStore.test.ts`（9 files / 153 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 MCP loading row 的 hover tooltip、expanded/collapsed 过渡和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 MCP loading/error/configured/live-check、runtime context、task/subagent/edit、session-load 状态，点测 tooltip、焦点顺序、popover close 和可访问名称；2) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact numeric/token surfaces 做自动化快照清单，优先 MCP no-server row、MCP manual hint row、daemon diagnostic、recent edit overflow、anchor labels；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次；5) 若继续代码级小步优化，优先审计仍只有可见短文本、缺少 `title` / `aria-label` 或 `aria-describedby` 的 transient diagnostic rows。

## 本轮 PLAN 2026-06-21 desktop mcp empty target label

- 目标：让桌面右侧 `StatusPanel` 展开后的 MCP empty/no-server 行在保留可见 `No MCP servers configured` 的同时，通过 `title` / `aria-label` 暴露 `MCP: No MCP servers configured`，避免空配置状态被读成泛化空文本。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 MCP details empty row；确认 desktop MCP loading/error/server rows 已有目标标签，但 no-server row 仍只有裸文本。对标项目 cc-gui 可读 `StatusPanel` 没有本项目同构的 MCP 空配置诊断行。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 key-only fallback MCP empty 用例，断言 no-server row 有 `status-mcp-empty`、`title="MCP: No MCP servers configured"`、`aria-label="MCP: No MCP servers configured"`；旧实现应因缺少这些属性失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中新增 `mcpNoServersTargetLabel = \`${mcpStatusLabel}: ${mcpNoServersLabel}\``，并写入 empty row 的 `title` / `aria-label`；不改 MCP summary、展开逻辑、server row、live check、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 desktop MCP empty row 应遵循 MCP source-label 规则。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP/store 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 desktop mcp empty target label

- 上轮进展与阻塞：上一轮完成 desktop `StatusPanel` MCP loading row target label；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已新增 `mcpLoadingTargetLabel`，expanded MCP details loading row 已输出 `status-mcp-loading`、`title` 与 `aria-label` 到 `MCP: Loading MCP configuration...`，`StatusPanel.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 使用 todo/files/subagents tabs 与 popover，没有本项目同构的 MCP 空配置诊断行。本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外 MCP 配置可用性诊断误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 desktop `StatusPanel` expanded MCP details 中 loading、error、server name、transport、status pill 已经有来源化标签，但 no-server row 旧实现仍只有裸 `No MCP servers configured` 文本。用户价值：当用户排查 MCP 配置为空或配置读取失败时，hover、辅助技术和自动化快照都能直接读到 `MCP: No MCP servers configured`，不用从面板位置和周边错误文本反推来源。
- 本轮规划：只改 `StatusPanel.tsx` 的 MCP empty row target label、`StatusPanel.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 MCP summary、展开逻辑、server row、live check、MCP 数据结构、Store/Rust/Tauri response。优先级中等：空配置状态是 MCP 首次配置和错误排查中的关键状态，实现成本极低，且与同面板 loading/error/server rows 的语义一致。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `mcpNoServersTargetLabel`，并给 expanded MCP details 的 empty row 添加 `status-mcp-empty` class、`title={mcpNoServersTargetLabel}` 和 `aria-label={mcpNoServersTargetLabel}`；可见 `No MCP servers configured` 文案保持原样。`src/components/chat/StatusPanel.test.tsx` 扩展 key-only fallback MCP empty 用例，覆盖 `MCP: No MCP servers configured` 目标标签。`.trellis/spec/frontend/component-guidelines.md` 已记录 desktop MCP empty row 应保留可见空状态文案，同时通过 `title` / `aria-label` 暴露 MCP 来源。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为旧实现的 no-server row 只有裸 `No MCP servers configured`，缺少 `status-mcp-empty`、`title="MCP: No MCP servers configured"`、`aria-label="MCP: No MCP servers configured"`。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts src/stores/useChatStore.test.ts`（9 files / 153 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 MCP empty/no-server row 的 hover tooltip、expanded/collapsed 过渡和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 MCP no-server/loading/error/configured/live-check、runtime context、task/subagent/edit、session-load 状态，点测 tooltip、焦点顺序、popover close 和可访问名称；2) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact numeric/token surfaces 做自动化快照清单，优先 MCP manual hint row、daemon diagnostic、recent edit overflow、anchor labels；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次；5) 若继续代码级小步优化，优先审计仍只有可见短文本、缺少 `title` / `aria-label` 或 `aria-describedby` 的 transient diagnostic rows。

## 本轮 PLAN 2026-06-21 desktop mcp manual hint target label

- 目标：让桌面右侧 `StatusPanel` 展开后的 MCP 手动 live-check 提示行在保留紧凑可见文案的同时，通过 `title` / `aria-label` 暴露 `MCP: Run a live check for enabled MCP servers.` 或 `MCP: No enabled MCP servers to check.`，避免提示短句脱离 MCP 详情上下文。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 MCP manual check hint 实现；确认 desktop MCP loading/error/empty/server rows 已有目标标签，但 manual hint row 仍只有裸可见短句。对标项目 cc-gui 可读 `StatusPanel` 没有本项目同构的 MCP live-check hint row。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 MCP live-check 与 key-only fallback 用例，断言提示行有 `status-mcp-check-hint`、`title` 和 `aria-label`；旧实现应因缺少这些属性失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中提取 `mcpManualHintText`，新增 `mcpManualHintTargetLabel = \`${mcpStatusLabel}: ${mcpManualHintText}\``，并写入 manual hint row 的 `title` / `aria-label`；不改 live-check 行为、button 状态、MCP summary、server row、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 desktop MCP manual hint row 应遵循 MCP source-label 规则。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP/store 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 desktop mcp manual hint target label

- 上轮进展与阻塞：上一轮完成 desktop `StatusPanel` MCP empty/no-server row target label；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已新增 `mcpNoServersTargetLabel`，expanded MCP details empty row 已输出 `status-mcp-empty`、`title` 与 `aria-label` 到 `MCP: No MCP servers configured`，`StatusPanel.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 使用 todo/files/subagents tabs 与 popover，没有本项目同构的 MCP 手动 live-check 提示行。本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外 MCP live-check 诊断能力误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 desktop `StatusPanel` expanded MCP details 中 loading、error、empty、server name、transport、status/live result pill 已经有来源化标签，但 manual live-check hint row 旧实现仍只有裸 `Run a live check for enabled MCP servers.` / `No enabled MCP servers to check.` 文本。用户价值：用户在桌面侧排查 MCP 连通性时，hover、辅助技术和自动化快照都能直接读到这是 MCP live-check helper，而不是脱离详情卡上下文的泛化说明句。
- 本轮规划：只改 `StatusPanel.tsx` 的 MCP manual hint target label、`StatusPanel.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 live-check 行为、button 状态、MCP summary、server row、MCP 数据结构、Store/Rust/Tauri response。优先级中等：该提示只在 MCP details 展开且有手动检测入口时出现，但实现成本极低，并收束 desktop MCP details 内剩余裸短句。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `mcpManualHintText` 与 `mcpManualHintTargetLabel`，并给 expanded MCP details 的 manual hint row 添加 `status-mcp-check-hint` class、`title={mcpManualHintTargetLabel}` 和 `aria-label={mcpManualHintTargetLabel}`；可见文案和 check button 行为保持原样。`src/components/chat/StatusPanel.test.tsx` 扩展 MCP live-check 与 key-only fallback 用例，覆盖 enabled 分支的 `MCP: Run a live check for enabled MCP servers.`，以及 no-enabled 分支的 `MCP: No enabled MCP servers to check.`。`.trellis/spec/frontend/component-guidelines.md` 已记录 desktop MCP manual hint row 应保留紧凑可见文本，同时通过 `title` / `aria-label` 暴露 MCP 来源。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为旧实现的 manual hint row 只有裸文本，缺少 `status-mcp-check-hint`、`title`、`aria-label`。GREEN 定向通过：同命令通过（1 file / 34 tests）。补 no-enabled 边界后重新验证：`npm test -- src/components/chat/StatusPanel.test.tsx` 通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts src/stores/useChatStore.test.ts`（9 files / 153 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 MCP manual hint row 的 hover tooltip、expanded/collapsed 过渡和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 MCP no-server/loading/error/configured/live-check、runtime context、task/subagent/edit、session-load 状态，点测 tooltip、焦点顺序、popover close 和可访问名称；2) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact numeric/token surfaces 做自动化快照清单，优先 daemon diagnostic、recent edit overflow、anchor labels、session-load source/status rows；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次；5) 若继续代码级小步优化，优先审计仍只有可见短文本、缺少 `title` / `aria-label` 或 `aria-describedby` 的 transient diagnostic rows。

## 本轮 PLAN 2026-06-21 desktop session-load detail value labels

- 目标：让桌面右侧 `StatusPanel` 的 history session-load 展开详情值不再只是裸数字/短句，而是在 `title` / `aria-label` 中暴露 `History load: <field> · <value>`，例如 `History load: Window · 120 / 5000 · 40ms · Map 7ms`。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 session-load details；确认 container/source/status/error 已有目标标签，但 details 内 Source/Window/Cache/Full/Elapsed 的值列仍只有裸可见短值。对标项目 cc-gui 可读 `StatusPanel` 没有本项目同构的 history load diagnostic card。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 session-load loading/cache fallback 用例，断言 Source/Window/Full/Elapsed/Cache 值列输出 `title` 与 `aria-label`；旧实现应因缺少这些属性失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中提取 session-load detail value text，并通过 `History load: <field> · <value>` 写入每个值列的 `title` / `aria-label`；不改 metrics 数据结构、展开逻辑、状态计算、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 session-load expanded detail value rows 必须携带 field + value target labels。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/session-load 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 desktop session-load detail value labels

- 上轮进展与阻塞：上一轮完成 desktop `StatusPanel` MCP manual live-check hint row target label；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已新增 `mcpManualHintText` 与 `mcpManualHintTargetLabel`，manual hint row 已输出 `status-mcp-check-hint`、`title` 与 `aria-label` 到 `MCP: Run a live check for enabled MCP servers.` / `MCP: No enabled MCP servers to check.`，测试与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 使用 todo/files/subagents tabs 与 popover，没有本项目同构的 history session-load diagnostic card。本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外历史加载性能诊断能力误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 desktop `StatusPanel` session-load diagnostic card 的 source-path container、toggle、status pill 和 error row 已有目标标签，但 expanded details 内 Source/Window/Cache/Full/Elapsed 的值列旧实现仍是裸 `Window first paint`、`120 / 5000 · 40ms · Map 7ms`、`5000 · 2ms` 等短值。用户价值：历史会话加载排障时，截断值、hover tooltip、辅助技术和自动化快照都能直接读到字段来源，例如 `History load: Window · 120 / 5000 · 40ms · Map 7ms`，不用从左侧短标签和面板位置拼上下文。
- 本轮规划：只改 `StatusPanel.tsx` 的 session-load detail value target labels、`StatusPanel.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 metrics 数据结构、展开逻辑、状态计算、Store/Rust/Tauri response。优先级中等：session-load 只在历史会话加载诊断中出现，但这些值是性能排障关键数据，且实现成本低、回归面集中在静态 markup。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `getSessionLoadDetailValueLabel(...)`，并提取 `sessionLoadCacheValueText`、`sessionLoadWindowValueText`、`sessionLoadFullValueText`、`sessionLoadElapsedValueText`，让 Source/Window/Cache/Full/Elapsed 值列的可见文本与 `title` / `aria-label` 同源。`src/components/chat/StatusPanel.test.tsx` 扩展 key-only fallback session-load 用例，覆盖 `History load: Source · Window first paint`、`History load: Window · 120 / 5000 · 40ms · Map 7ms`、`History load: Full · 5000 · 50ms · Map 11ms`、`History load: Elapsed · 161ms`、`History load: Cache · 5000 · 2ms`。`.trellis/spec/frontend/component-guidelines.md` 已记录 expanded session-load detail value rows 必须携带 field + value target labels。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为旧实现的 session-load details value rows 只有裸可见短值，缺少 `title` / `aria-label`。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts src/stores/useChatStore.test.ts`（9 files / 153 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 session-load details 的 hover tooltip、expanded/collapsed 过渡和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 session-load loading/windowed/cache/error、MCP no-server/loading/error/configured/live-check、runtime context、task/subagent/edit 状态，点测 tooltip、焦点顺序、popover close 和可访问名称；2) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact numeric/token surfaces 做自动化快照清单，优先 current activity hidden-count rows、anchor card、session-load summary text、recent edit overflow；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次；5) 若继续代码级小步优化，优先审计仍只有可见短文本、缺少 `title` / `aria-label` 或 `aria-describedby` 的 transient diagnostic rows。

## 本轮 PLAN 2026-06-21 desktop session-load summary target label

- 目标：让桌面右侧 `StatusPanel` 的 history session-load 折叠摘要值在保留紧凑可见文本的同时，通过 `title` / `aria-label` 暴露 `History load: <summary>`，例如 `History load: Window first paint · 120 / 5000 · 161ms`。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 session-load summary；确认 source/status/details value 已有目标标签，但折叠摘要值仍只有裸可见短句。对标项目 cc-gui 可读 `StatusPanel` 没有本项目同构的 history load diagnostic card。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 completed/windowed/cache/key-only session-load 用例，断言折叠摘要值输出 `title` 与 `aria-label`；旧实现应因缺少这些属性失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中新增 `sessionLoadSummaryTargetLabel = \`${sessionLoadMetricsLabel}: ${sessionLoadSummaryText}\``，并写入折叠摘要值的 `title` / `aria-label`；不改 metrics 数据结构、展开逻辑、状态计算、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 collapsed session-load summary value 必须携带来源化目标标签。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/session-load 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 desktop session-load summary target label

- 上轮进展与阻塞：上一轮完成 desktop `StatusPanel` session-load expanded detail value rows target labels；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已通过 `getSessionLoadDetailValueLabel(...)` 给 Source/Window/Full/Elapsed/Cache 值列输出 `History load: <field> · <value>` 的 `title` / `aria-label`，`StatusPanel.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 使用 todo/files/subagents tabs 与 popover，没有本项目同构的 history session-load diagnostic card。本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外历史加载性能诊断能力误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 desktop `StatusPanel` session-load diagnostic card 的 source-path container、toggle、status pill、error row 和 expanded detail values 已有目标标签，但 collapsed summary value 旧实现仍只有裸 `Window first paint · 120 / 5000 · 161ms` / `Cache hit · 5000 · 2ms` 可见短句，且可能独立截断。用户价值：历史会话加载排障时，hover、辅助技术和自动化快照都能直接读到 `History load: <summary>`，不用从父容器 source-path 或相邻状态 pill 拼上下文。
- 本轮规划：只改 `StatusPanel.tsx` 的 session-load collapsed summary target label、`StatusPanel.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 metrics 数据结构、展开逻辑、状态计算、Store/Rust/Tauri response。优先级中等：session-load 只在历史加载诊断中出现，但摘要值是折叠态唯一性能概览，实现成本低且与上一轮 detail value labels 收束。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `sessionLoadSummaryTargetLabel = \`${sessionLoadMetricsLabel}: ${sessionLoadSummaryText}\``，并把它写入 collapsed summary span 的 `title` / `aria-label`；可见摘要、toggle、status pill 和 expanded details 保持原样。`src/components/chat/StatusPanel.test.tsx` 扩展 completed/windowed/cache/key-only fallback session-load 用例，覆盖 `History load: Window first paint · 120 / 5000 · 161ms`、`History load: Window first paint · 120 / 5000 · 57ms` 和 `History load: Cache hit · 5000 · 2ms`。`.trellis/spec/frontend/component-guidelines.md` 已记录 collapsed session-load summary value 必须通过 `title` / `aria-label` 暴露 `History load: <summary>`。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 4 项，失败点为旧实现的 collapsed summary span 缺少 `title` / `aria-label`。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts src/stores/useChatStore.test.ts`（9 files / 153 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 session-load collapsed summary 的 hover tooltip、expanded/collapsed 过渡和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 session-load loading/windowed/cache/error、MCP no-server/loading/error/configured/live-check、runtime context、task/subagent/edit 状态，点测 tooltip、焦点顺序、popover close 和可访问名称；2) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact numeric/token surfaces 做自动化快照清单，优先 current activity hidden-count rows、anchor card、recent edit overflow、daemon diagnostic；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次；5) 若继续代码级小步优化，优先审计仍只有可见短文本、缺少 `title` / `aria-label` 或 `aria-describedby` 的 transient diagnostic rows。

## 本轮 PLAN 2026-06-21 desktop activity hidden-count target labels

- 目标：让桌面右侧 `StatusPanel` current activity 的 clipped-history footer 在保留可见 `<count> earlier tools/subagents hidden` 文案的同时，通过 `title` / `aria-label` 暴露同源文本，避免窄右栏中被截断的 footer 只剩裸短句。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 activity hidden-count footer；确认 row/button/status pill 已有目标标签，但 hidden-count footer 仍只有可见短文本。对标项目 cc-gui 可读 `StatusPanel` 使用 tab/popover，没有本项目同构的 clipped-history footer。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 recent desktop task list 与 subagent activity 用例，断言 `status-activity-task-hidden-count` / `status-activity-subagent-hidden-count` 输出同源 `title` 与 `aria-label`；旧实现应因缺少这些属性失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中提取 `hiddenActivityTaskLabel` / `hiddenActivitySubagentLabel`，并写入对应 footer 的 `title` / `aria-label`；不改裁剪数量、排序、跳转、活动摘要、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 clipped-history footers 必须镜像可见 readable fallback 到 `title` / `aria-label`。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/activity 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 desktop activity hidden-count target labels

- 上轮进展与阻塞：上一轮完成 desktop `StatusPanel` session-load collapsed summary target label；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已将 collapsed summary span 的 `title` / `aria-label` 同源设置为 `History load: <summary>`，`StatusPanel.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 使用 todo/files/subagents tabs 与 popover，没有本项目同构的 desktop current-activity clipped-history footer。本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的右侧活动历史列表误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 desktop `StatusPanel` current activity 的 task/subagent row buttons、状态 pill、scroll region 已经有较完整的 `title` / `aria-label`，但 hidden-count footer 旧实现只有裸 `1 earlier tools hidden` / `1 earlier subagents hidden` 可见短句。用户价值：当右侧活动历史被裁剪时，hover、辅助技术和自动化快照都能读到完整 footer 文案，窄栏截断后仍能知道还有更早工具或子代理未显示。
- 本轮规划：只改 `StatusPanel.tsx` 的 activity hidden-count footer target labels、`StatusPanel.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改裁剪数量、排序、跳转、活动摘要、Store/Rust/Tauri response。优先级中等偏低但成本极低：该 footer 只在活动历史超过可见上限时出现，但一旦出现就直接影响用户理解历史是否被截断。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `hiddenActivityTaskLabel` / `hiddenActivitySubagentLabel`，并把 `status-activity-task-hidden-count` 与 `status-activity-subagent-hidden-count` 的可见文本、`title`、`aria-label` 同源。`src/components/chat/StatusPanel.test.tsx` 扩展 recent desktop task list 与 subagent activity 用例，覆盖 tools/subagents hidden-count footer 的 `title` / `aria-label`。`.trellis/spec/frontend/component-guidelines.md` 已记录 clipped-history footers 必须把 readable footer text 镜像到 `title` / `aria-label`。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为旧实现的 tools/subagents hidden-count footer 缺少 `title` / `aria-label`。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts src/stores/useChatStore.test.ts`（9 files / 153 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 activity hidden-count footer 的 hover tooltip、scroll region 截断表现和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 current activity 长列表、session-load loading/windowed/cache/error、MCP no-server/loading/error/configured/live-check、runtime context、task/subagent/edit 状态，点测 tooltip、焦点顺序、popover close 和可访问名称；2) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact numeric/token surfaces 做自动化快照清单，优先 anchor card、recent edit overflow、daemon diagnostic、activity section ratio labels；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次；5) 若继续代码级小步优化，优先审计仍只有可见短文本、缺少 `title` / `aria-label` 或 `aria-describedby` 的 transient diagnostic rows。

## 本轮 PLAN 2026-06-21 desktop activity section ratio target labels

- 目标：让桌面右侧 `StatusPanel` current activity 的 task/subagent section ratio 在保留可见 `5 / 6`、`2 / 3` 短计数的同时，通过 `title` / `aria-label` 暴露 `Tasks: 5 / 6`、`Subagents: 2 / 3`，避免比例值脱离左侧 section heading 后成为裸数字。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 activity section header ratio；确认 hidden-count footer 已有目标标签，但 section ratio 仍只有裸可见短计数。对标项目 cc-gui 可读 `StatusPanel` 有 tab progress，但没有本项目同构的 activity section ratio。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 recent desktop task list 与 subagent activity 用例，断言 task/subagent ratio span 输出同源 `title` 与 `aria-label`；旧实现应因缺少这些属性失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中新增 task/subagent section ratio labels，并写入 ratio span 的 `title` / `aria-label`；不改可见短计数、裁剪数量、排序、跳转、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 current activity section ratios 必须在 `title` / `aria-label` 中携带 section heading。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/activity 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 desktop activity section ratio target labels

- 上轮进展与阻塞：上一轮完成 desktop `StatusPanel` current activity hidden-count footer target labels；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已将 `status-activity-task-hidden-count` / `status-activity-subagent-hidden-count` 的 visible text、`title`、`aria-label` 同源设置，测试与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 有 tab progress，例如 tasks/subagents 的 completed/total，但没有本项目同构的 desktop current-activity section ratio。本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的右侧活动历史分区计数误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 desktop `StatusPanel` current activity 的 task/subagent section header 旧实现只显示裸 `5 / 6`、`2 / 3`，没有 `title` / `aria-label`，离开左侧 `Tasks` / `Subagents` heading 后无法独立说明含义。用户价值：窄右栏、hover、辅助技术和自动化快照都能直接读到 `Tasks: 5 / 6` 或 `Subagents: 2 / 3`，同时可见 UI 仍保持短计数。
- 本轮规划：只改 `StatusPanel.tsx` 的 activity section ratio target labels、`StatusPanel.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改可见短计数、裁剪数量、排序、跳转、Store/Rust/Tauri response。优先级中等偏低但成本极低：该 ratio 在活动历史存在时高频显示，且与上一轮 hidden-count footer 标签形成同一区域收束。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `activityTaskRatioLabel` / `activitySubagentRatioLabel`，并让 `status-activity-task-ratio` 与 `status-activity-subagent-ratio` 的 `title` / `aria-label` 输出 `Tasks: visible / total`、`Subagents: visible / total`，可见文本仍为短 `visible / total`。`src/components/chat/StatusPanel.test.tsx` 扩展 recent desktop task list 与 subagent activity 用例，覆盖 task/subagent ratio span 的 `title` / `aria-label`。`.trellis/spec/frontend/component-guidelines.md` 已记录 current activity section ratio values 必须在 `title` / `aria-label` 中携带 section heading。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为旧实现的 task/subagent ratio span 缺少 `class`、`title`、`aria-label`。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts src/stores/useChatStore.test.ts`（9 files / 153 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 activity section ratio 的 hover tooltip、scroll region 截断表现和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 current activity 长列表、session-load loading/windowed/cache/error、MCP no-server/loading/error/configured/live-check、runtime context、task/subagent/edit 状态，点测 tooltip、焦点顺序、popover close 和可访问名称；2) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact numeric/token surfaces 做自动化快照清单，优先 anchor card、recent edit overflow、daemon diagnostic、MCP detail latency/status pills；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次；5) 若继续代码级小步优化，优先审计仍只有可见短文本、缺少 `title` / `aria-label` 或 `aria-describedby` 的 transient diagnostic rows。

## 本轮 PLAN 2026-06-21 desktop mcp configured count target label

- 目标：让桌面右侧 `StatusPanel` expanded MCP details 的 configured servers 数量在保留可见裸数字 `2` 的同时，通过 `title` / `aria-label` 暴露 `Configured servers: 2`，避免详情分组右侧短计数脱离左侧 label 后成为裸数字。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 MCP details；确认 server name、transport、enabled/live pills、loading/empty/manual hint 已有目标标签，但 configured servers header count 仍只有裸数字。对标项目 cc-gui 可读 `StatusPanel` 有 tab progress，但没有本项目同构的 expanded MCP configured server count row。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 MCP live-check 与 key-only fallback MCP 用例，断言 configured server count span 有 `status-mcp-configured-server-count`、`title` 和 `aria-label`；旧实现应因缺少这些属性失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中新增 `mcpConfiguredServersCountLabel`，并写入 configured servers count span 的 `title` / `aria-label`；不改 MCP summary、展开逻辑、server row、live check、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 expanded MCP configured servers count 必须在目标标签中携带分组 label。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP/store 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 desktop mcp configured count target label

- 上轮进展与阻塞：上一轮完成 desktop `StatusPanel` current activity section ratio target labels；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已新增 `activityTaskRatioLabel` / `activitySubagentRatioLabel`，`status-activity-task-ratio` / `status-activity-subagent-ratio` 已输出 `Tasks: visible / total`、`Subagents: visible / total` 的 `title` / `aria-label`，测试与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 有 todo/files/subagents tab progress，但没有本项目同构的 expanded MCP configured server count row。本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外 MCP 配置诊断面误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 desktop `StatusPanel` expanded MCP details 中 loading、empty、manual hint、server name、transport、enabled/live result pills 已有目标标签，但 `Configured servers` header 右侧旧实现仍只是裸 `2`。用户价值：MCP 详情展开后，hover、辅助技术和自动化快照都能直接读到 `Configured servers: 2`，不用依赖左侧 label 或 DOM 相邻关系推断数字含义。
- 本轮规划：只改 `StatusPanel.tsx` 的 MCP configured server count target label、`StatusPanel.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 MCP summary、展开逻辑、server row、live check、MCP 数据结构、Store/Rust/Tauri response。优先级中等偏低但成本极低：该 count 只在 MCP details 展开且有 configured servers 时出现，但它是同一 details 面里最后几个裸短值之一。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `mcpConfiguredServersCountLabel`，并让 `status-mcp-configured-server-count` 的 `title` / `aria-label` 输出 `Configured servers: <count>`，可见文本仍为短数字。`src/components/chat/StatusPanel.test.tsx` 扩展 MCP live-check 与 key-only fallback MCP 用例，覆盖中文资源和英文 fallback 下的 configured server count target label。`.trellis/spec/frontend/component-guidelines.md` 已记录 expanded MCP details header count 可见值可保持裸数字，但 `title` / `aria-label` 必须携带 group label。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为旧实现的 configured servers count 仍是裸 `<span>2</span>`，缺少 `status-mcp-configured-server-count`、`title`、`aria-label`。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts src/stores/useChatStore.test.ts`（9 files / 153 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 expanded MCP details configured server count 的 hover tooltip、展开/折叠过渡和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 MCP no-server/loading/error/configured/live-check、current activity 长列表、session-load loading/windowed/cache/error、runtime context、task/subagent/edit 状态，点测 tooltip、焦点顺序、popover close 和可访问名称；2) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact numeric/token surfaces 做自动化快照清单，优先 daemon ready/offline status title、top provider/message/anchor values、recent edit tree stats、MCP summary source label；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次；5) 若继续代码级小步优化，优先审计仍只有可见短文本、缺少 `title` / `aria-label` 或 `aria-describedby` 的 transient diagnostic rows。

## 本轮 PLAN 2026-06-21 desktop top status value target labels

- 目标：让桌面右侧 `StatusPanel` 顶部状态区的 provider、message count、anchor count 在保留可见短值 `claude`、`4`、`3` 的同时，通过 `title` / `aria-label` 暴露 `AI provider: claude`、`Messages: 4`、`Message anchors: 3`，避免高频状态值脱离左侧 label 后成为裸值。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` 顶部状态区与 `StatusPanel.test.tsx` fallback 用例；确认 runtime context values、MCP details count 已有目标标签，但 top provider/message/anchor values 仍只有裸可见值。对标项目 cc-gui 可读 `StatusPanel` 是 todo/files/subagents tabs，没有本项目同构的 provider/message/anchor top status values。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 key-only fallback top status 用例，断言 `status-top-provider-value`、`status-top-message-count`、`status-top-anchor-count` 有 `title` 和 `aria-label`；旧实现应因缺少这些属性失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中新增 top status value labels，并写入三个值 span 的 `title` / `aria-label`；不改可见布局、计数来源、daemon/MCP/runtime context、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 top status provider/message/anchor values 必须在目标标签中携带左侧 label。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP/store 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 desktop top status value target labels

- 上轮进展与阻塞：上一轮完成 desktop `StatusPanel` expanded MCP details configured server count target label；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已新增 `mcpConfiguredServersCountLabel`，`status-mcp-configured-server-count` 已输出 `Configured servers: <count>` 的 `title` / `aria-label`，测试与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 是 todo/files/subagents tabs 与 popover，tab 内有 progress；没有本项目同构的 provider/message/anchor top status values。本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的桌面运行状态顶栏误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 desktop `StatusPanel` 顶部状态区的 label 已能在 i18n 缺失时 fallback 为 `AI provider`、`Messages`、`Message anchors`，但右侧值旧实现仍是裸 `claude`、`4`、`3`。用户价值：这些高频状态值在 hover、辅助技术和自动化快照中都能直接读到 `AI provider: claude`、`Messages: 4`、`Message anchors: 3`，不用依赖视觉相邻关系。
- 本轮规划：只改 `StatusPanel.tsx` 的 top provider/message/anchor value target labels、`StatusPanel.test.tsx` 的 key-only fallback SSR 回归断言、前端组件规范和 Trellis 记录；不改可见布局、计数来源、daemon/MCP/runtime context、Store/Rust/Tauri response。优先级中等：这三个值在每个 desktop chat 会话都出现，实现成本极低，回归面集中在静态 markup。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `providerTargetLabel` / `messageCountTargetLabel` / `anchorCountTargetLabel`，并让 `status-top-provider-value`、`status-top-message-count`、`status-top-anchor-count` 的 `title` / `aria-label` 分别输出左侧 label + 当前值；可见短值保持原样。`src/components/chat/StatusPanel.test.tsx` 扩展 key-only fallback top status 用例，覆盖 `AI provider: claude`、`Messages: 4`、`Message anchors: 3`。`.trellis/spec/frontend/component-guidelines.md` 已记录 top status provider/message/anchor values 必须在目标标签中携带左侧 label。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为旧实现的 provider/message/anchor values 仍是裸 span，缺少 `status-top-provider-value` / `status-top-message-count` / `status-top-anchor-count` 以及对应 `title` / `aria-label`。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts src/stores/useChatStore.test.ts`（9 files / 153 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 top status provider/message/anchor values 的 hover tooltip 和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 top status、runtime context、MCP no-server/loading/error/configured/live-check、current activity 长列表、session-load loading/windowed/cache/error、task/subagent/edit 状态，点测 tooltip、焦点顺序、popover close 和可访问名称；2) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact numeric/token surfaces 做自动化快照清单，优先 daemon ready/offline status title、recent edit tree stats、MCP summary source label、active-anchor inner value；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次；5) 若继续代码级小步优化，优先审计仍只有可见短文本、缺少 `title` / `aria-label` 或 `aria-describedby` 的 transient diagnostic rows。

## 本轮 PLAN 2026-06-21 desktop daemon status title target label

- 目标：让桌面右侧 `StatusPanel` 顶部 daemon 状态值在无诊断详情时也通过 `title` 暴露 `Daemon: <status>`，与现有 `aria-label` 保持一致；有诊断详情时继续保留 `Daemon: <status> · <detail>`。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` 顶部 daemon 状态渲染与 `StatusPanel.test.tsx` fallback 用例；确认 provider/message/anchor values 已有目标标签，但 daemon status value 的 `title` 在无诊断详情时仍是裸 `Success` / `Daemon offline`。对标项目 cc-gui 可读 `StatusPanel` 是 tabs/popover，没有本项目同构的 daemon status value。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 key-only fallback top status 用例，断言 `status-top-daemon-value` 有 `title="Daemon: Daemon offline"` 和 `aria-label="Daemon: Daemon offline"`；旧实现应因 `title` 仍是裸状态失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中新增 `daemonStatusTargetLabel = daemonDiagnosticDetailLabel || daemonStatusAriaLabel`，并写入 daemon status span 的 `title`；不改 daemon 状态计算、reconnect 按钮、诊断 row、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 top daemon status value 的 `title` / `aria-label` 必须携带 daemon label；诊断详情可追加在同一 title 中。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP/store 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 desktop daemon status title target label

- 上轮进展与阻塞：上一轮完成 desktop `StatusPanel` top provider/message/anchor value target labels；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已新增 `providerTargetLabel` / `messageCountTargetLabel` / `anchorCountTargetLabel`，`status-top-provider-value`、`status-top-message-count`、`status-top-anchor-count` 已输出 `AI provider: <provider>`、`Messages: <count>`、`Message anchors: <count>` 的 `title` / `aria-label`，测试与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 是 todo/files/subagents tabs 与 popover，没有本项目同构的 top daemon status value。本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外 daemon 运行状态顶栏误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 desktop `StatusPanel` top daemon status value 旧实现的 `aria-label` 已是 `Daemon: Daemon offline`，但 `title` 在无诊断详情时仍是裸 `Daemon offline` / `Success`；hover tooltip 与辅助语义不一致。用户价值：用户 hover daemon 状态或在自动化快照中读取时能得到与可访问名称一致的 `Daemon: <status>`，有诊断详情时仍保留 `Daemon: <status> · <detail>`。
- 本轮规划：只改 `StatusPanel.tsx` 的 daemon status title target label、`StatusPanel.test.tsx` 的 key-only fallback SSR 回归断言、前端组件规范和 Trellis 记录；不改 daemon 状态计算、reconnect 按钮、诊断 row、Store/Rust/Tauri response。优先级中等：daemon 状态每个 desktop chat 会话都出现，实现成本极低，且与刚完成的 top status values 语义收束。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `daemonStatusTargetLabel = daemonDiagnosticDetailLabel || daemonStatusAriaLabel`，并让 daemon status span 使用 `status-top-daemon-value` class，`title={daemonStatusTargetLabel}`、`aria-label={daemonStatusAriaLabel}`；无诊断时 title 为 `Daemon: <status>`，有诊断时 title 为 `Daemon: <status> · <detail>`。`src/components/chat/StatusPanel.test.tsx` 扩展 key-only fallback top status 用例，覆盖 `Daemon: Daemon offline`。`.trellis/spec/frontend/component-guidelines.md` 已记录 top daemon status value 的 `title` / `aria-label` 必须携带 daemon label。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为旧实现 daemon status span 的 `title` 仍是裸 `Daemon offline`，且缺少 `status-top-daemon-value` class。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts src/stores/useChatStore.test.ts`（9 files / 153 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 daemon status hover tooltip 和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 daemon ready/offline/error/reconnecting、top status、runtime context、MCP no-server/loading/error/configured/live-check、current activity、session-load、task/subagent/edit 状态，点测 tooltip、焦点顺序和可访问名称；2) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact numeric/token surfaces 做自动化快照清单，优先 recent edit tree stats、MCP summary source label、active-anchor inner value；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次；5) 若继续代码级小步优化，优先审计仍只有可见短文本、缺少 `title` / `aria-label` 或 `aria-describedby` 的 transient diagnostic rows。

## 本轮 PLAN 2026-06-21 desktop edit tree stats target labels

- 目标：让桌面右侧 `StatusPanel` recent edits 树里的文件夹/文件 `+N / -M` 统计在保留紧凑可见数字的同时，通过 `title` / `aria-label` 暴露 `Edit stats: <target> · +N / -M`，避免聚合树中的裸增删数脱离文件或文件夹上下文。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` / `StatusPanel.test.tsx` 的 edit tree stats；确认文件/文件夹行 action label、状态 pill、open file button 已有目标标签，但 `status-edit-tree-stats` 仍只有裸 `+N` / `-M`。对标项目 cc-gui 可读 `StatusPanel` files tab 只有总量 tab stats，没有本项目同构的 recent edit tree stats。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 key-only fallback recent edits 与 scrollable tree 用例，断言文件/文件夹 `status-edit-tree-stats` 输出 `Edit stats: <target> · +N / -M` 的 `title` 与 `aria-label`；旧实现应因缺少这些属性失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中新增 edit stats target label helper，并写入 folder/file stats group 的 `title` / `aria-label`；不改树构建、聚合数量、排序、折叠、hover preview、open file、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 status edit tree stats 必须携带目标路径和增删数字。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/diff/edit 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 desktop edit tree stats target labels

- 上轮进展与阻塞：上一轮完成 desktop `StatusPanel` daemon status title target label；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已新增 `daemonStatusTargetLabel`，`status-top-daemon-value` 的 `title` 已与 `aria-label` 语义对齐为 `Daemon: <status>`，有诊断详情时保留 `Daemon: <status> · <detail>`，测试与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 的 files tab 只显示总量 additions/deletions tab stats，并通过 popover 渲染文件变化列表；没有本项目同构的 recent edit tree folder/file stats。本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外树形 recent edits 状态栏误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 desktop `StatusPanel` recent edit tree 的 folder/file row action label、file status pill、open-file button 和 hover diff preview 已有目标语义，但 `status-edit-tree-stats` 旧实现仍只是裸 `+N` / `-M`。用户价值：用户在窄右栏、hover tooltip、辅助技术或自动化快照里读取聚合目录/文件增删数时，能直接知道这些数字属于哪个 target，例如 `Edit stats: src/components · +5 / -1` 或 `Edit stats: src/components/chat/StatusPanel.tsx · +2 / -1`，不用依赖相邻 DOM 或视觉位置推断。
- 本轮规划：只改 `StatusPanel.tsx` 的 edit tree stats target label、`StatusPanel.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改树构建、聚合数量、排序、折叠、hover preview、open file、Store/Rust/Tauri response。优先级中等：recent edits 是用户审查文件变更的高价值区域，`+N/-M` 重复出现且实现成本极低。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `getEditStatsTargetLabel(...)`，复用 `chat.layout.inputStatusEditFileStats` 语义，把 folder 和 file `status-edit-tree-stats` 的 `title` / `aria-label` 设为 `Edit stats: <target> · +N / -M`（中文环境为 `编辑统计：<target> · +N / -M`），可见 `+N` / `-M` 保持紧凑。`src/components/chat/StatusPanel.test.tsx` 扩展 key-only fallback recent edits 用例，覆盖文件 stats 的英文 fallback；扩展 scrollable tree 用例，覆盖 folder aggregate stats 和 file stats 的英中文 target label。`.trellis/spec/frontend/component-guidelines.md` 已记录 status edit tree stats 必须携带目标路径和 change scale。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为旧实现的 `status-edit-tree-stats` 缺少 `title` / `aria-label`。实现后首次 GREEN 调整中发现普通测试环境返回中文 `编辑统计：...`，将断言改为英中文兼容。GREEN 定向通过：`npm test -- src/components/chat/StatusPanel.test.tsx`（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts src/stores/useChatStore.test.ts`（9 files / 153 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 recent edit tree stats 的 hover tooltip、目录折叠/展开状态和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 recent edit tree 多层目录、daemon ready/offline/error/reconnecting、top status、runtime context、MCP no-server/loading/error/configured/live-check、current activity、session-load、task/subagent/edit 状态，点测 tooltip、焦点顺序和可访问名称；2) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact numeric/token surfaces 做自动化快照清单，优先 MCP summary source label、active-anchor inner value、MCP detail latency/status pills；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次；5) 若继续代码级小步优化，优先审计仍只有可见短文本、缺少 `title` / `aria-label` 或 `aria-describedby` 的 transient diagnostic rows。

## 本轮 PLAN 2026-06-21 desktop active anchor value target label

- 目标：让桌面右侧 `StatusPanel` 的 current-message anchor 卡片内部 value 行不再只是裸 anchor 文本，而是在该值自身通过 `title` / `aria-label` 暴露 `Current message: <anchor>`，与外层卡片语义一致。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` active-anchor JSX 与 `StatusPanel.test.tsx` fallback 用例；确认外层 card 已有 `Current message: <anchor>`，但内部 value 行仍只有裸文本。对标项目 cc-gui 可读 `StatusPanel` 没有本项目同构的 active-anchor summary card。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 key-only fallback active-anchor 用例，断言内部 value 行有稳定 class、`title` 与 `aria-label`；旧实现应因缺少内部 value 属性失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中给内部 visible anchor value 添加 `status-active-anchor-value` class、`title={activeAnchorTargetLabel}` 和 `aria-label={activeAnchorTargetLabel}`；不改 anchor rail、active anchor 数据流、滚动定位、Store/Rust/Tauri response。
- 功能点 5：规范同步复查。验证方式：确认 `.trellis/spec/frontend/component-guidelines.md` 已记录 current-message anchor card 的 label + value 规则；如已存在则不重复追加。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/anchor 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 desktop active anchor value target label

- 上轮进展与阻塞：上一轮完成 desktop `StatusPanel` recent edit tree folder/file stats target labels；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已有 `getEditStatsTargetLabel(...)`，folder/file `status-edit-tree-stats` 已输出 `Edit stats: <target> · +N / -M` 的 `title` / `aria-label`，`StatusPanel.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 也已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 是 todo/files/subagents tabs 与 popover，没有本项目同构的 current-message active-anchor summary card。本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的当前消息锚点摘要误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 desktop `StatusPanel` active-anchor 外层 card 已有 `Current message: <anchor>`，但内部可见 anchor value 旧实现仍是裸文本。用户价值：当 anchor 文案较长、被截断、被 hover 或被辅助技术单独读取时，用户和自动化快照都能直接获得 `Current message: Refactor status panel labels`，不用依赖外层 card 或视觉标题推断上下文。
- 本轮规划：只改 `StatusPanel.tsx` 的 active-anchor inner value target label、`StatusPanel.test.tsx` 的 key-only fallback SSR 回归断言和 Trellis 记录；不改 anchor rail、active anchor 数据流、滚动定位、Store/Rust/Tauri response。优先级中等：当前消息锚点在长会话定位中高价值，缺口实现成本极低，并且 `.trellis/spec/frontend/component-guidelines.md` 已要求 current-message anchor card 遵循 label + value 规则。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 给 active-anchor 内部 visible value 行添加 `status-active-anchor-value` class、`title={activeAnchorTargetLabel}` 和 `aria-label={activeAnchorTargetLabel}`；外层 card、可见 `Current message` 标题和 anchor 文案保持不变。`src/components/chat/StatusPanel.test.tsx` 扩展 key-only fallback active-anchor 用例，断言内部 value 行输出 `Current message: Refactor status panel labels` 的目标标签。`.trellis/spec/frontend/component-guidelines.md` 已存在 current-message anchor card 规则，本轮复查确认后未重复追加。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为旧实现内部 value 行缺少 `status-active-anchor-value`、`title` 与 `aria-label`。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/components/chat/MessageAnchorRail.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts src/stores/useChatStore.test.ts`（10 files / 158 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 current-message anchor card 的 hover tooltip、截断表现和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 current-message anchor、recent edit tree 多层目录、daemon ready/offline/error/reconnecting、top status、runtime context、MCP no-server/loading/error/configured/live-check、current activity、session-load、task/subagent/edit 状态，点测 tooltip、焦点顺序和可访问名称；2) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact numeric/token surfaces 做自动化快照清单，优先 MCP summary source label、MCP detail latency/status pills、session-load source/status rows；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次；5) 若继续代码级小步优化，优先审计仍只有可见短文本、缺少 `title` / `aria-label` 或 `aria-describedby` 的 transient diagnostic rows。

## 本轮 PLAN 2026-06-21 desktop mcp live result message target label

- 目标：让桌面右侧 `StatusPanel` MCP live-check result pill 在已有 server + status + latency 标签基础上，同时携带 live result message，例如 `filesystem: Timed out · Process detection timed out (5s) · 5000ms`，避免诊断原因只停留在不可见数据或被普通 tooltip 分支丢弃。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` MCP live result JSX 与 `StatusPanel.test.tsx` live-check 用例；确认 latency inline helper 已 `aria-hidden`，live result pill 已有 server + status + latency，但 message 存在时未纳入 `title` / `aria-label`。对标项目 cc-gui 可读 `StatusPanel` 没有本项目同构的 MCP live-check result pill。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 MCP live-check 用例，加入带 message 的 timeout result，断言 result pill 的 `title` 与 `aria-label` 同时包含 server、status、message、latency；旧实现应因 message 缺失失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中把 live result detail 从单一 latency 改为 `[message, latency]`，并继续通过 `getMcpServerStateLabel(server.name, liveStatusLabel, detail)` 生成 pill 标签；不改 connectivity result 数据结构、检测触发、server enabled 逻辑、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：复查 `.trellis/spec/frontend/component-guidelines.md` 已记录 MCP live-check result pill 应携带 latency/message target label；如已存在则不重复追加。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 desktop mcp live result message target label

- 上轮进展与阻塞：上一轮完成 desktop `StatusPanel` active-anchor inner value target label；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已给 active-anchor 内部 visible value 行添加 `status-active-anchor-value` class、`title={activeAnchorTargetLabel}` 与 `aria-label={activeAnchorTargetLabel}`，`StatusPanel.test.tsx` 已覆盖 `Current message: Refactor status panel labels`。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 是 todo/files/subagents tabs 与 popover，没有本项目同构的 MCP live-check result pill。本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外 MCP 连通性诊断能力误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 desktop `StatusPanel` MCP live result pill 已有 server + status + latency target label，且旁边 inline latency 已 `aria-hidden`，但当 live result 返回 `message` 时，旧实现会因为 `liveStatusTargetLabel` 优先级而丢掉 message，只输出 `filesystem: Online · 24ms`。用户价值：用户排查握手失败、超时或认证问题时，hover、读屏和自动化快照能直接获得 `filesystem: Online · Handshake complete · 24ms` 或同类 message，不需要打开底层日志或依赖全局 error row。
- 本轮规划：只改 `StatusPanel.tsx` 的 MCP live result pill target label detail 拼接、`StatusPanel.test.tsx` 的 live-check SSR 回归断言和 Trellis 记录；不改 connectivity result 数据结构、检测触发、server enabled 逻辑、Store/Rust/Tauri response。优先级中等：live-check 是诊断路径中的高价值状态，message 出现频率低于 latency，但实现成本极低，并与 `.trellis/spec/frontend/component-guidelines.md` 的 latency/message 规则对齐。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 将 MCP live result detail 从单一 `liveLatencyText` 改为 `liveDetailText = [message, latency].filter(Boolean).join(' · ')`，并继续用 `getMcpServerStateLabel(server.name, liveStatusLabel, liveDetailText)` 生成 `title` / `aria-label`；无 message 时仍保持原有 `server: status · latency`。`src/components/chat/StatusPanel.test.tsx` 把 live-check fixture 改成带 `message: 'Handshake complete'`，并断言 live result pill 输出 `filesystem: Online/在线 · Handshake complete · 24ms`。`.trellis/spec/frontend/component-guidelines.md` 已存在 MCP live result pill latency/message 规则，本轮复查确认后未重复追加。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为旧实现 live result pill 仍输出 `filesystem: 在线 · 24ms`，缺少 `Handshake complete`。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/stores/useChatStore.test.ts`（6 files / 123 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 MCP live-check result pill 的 hover tooltip、message 截断和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 MCP live-check online/offline/timeout/error with message、no-server/loading/error/configured、current-message anchor、recent edit tree、daemon ready/offline/error/reconnecting、session-load、task/subagent/edit 状态，点测 tooltip、焦点顺序和可访问名称；2) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact numeric/token surfaces 做自动化快照清单，优先 MCP summary source label、session-load source/status rows、current activity hidden-count rows；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次；5) 若继续代码级小步优化，优先审计仍只有可见短文本、缺少 `title` / `aria-label` 或 `aria-describedby` 的 transient diagnostic rows。

## 本轮 PLAN 2026-06-21 desktop mcp summary source label

- 目标：让桌面右侧 `StatusPanel` 的 MCP summary 外层 `title` / `aria-label` 在非错误状态下也携带来源，例如 `MCP: 2 / 5 available`，避免折叠摘要被读成脱离上下文的裸可用数量。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` MCP summary 计算与 `StatusPanel.test.tsx` summary 断言；确认 loading/empty/manual hint/error 已有 `MCP:` 来源标签，但非错误 summary 仍是裸 `1 / 2 available`。对标项目 cc-gui 可读 `StatusPanel` 没有本项目同构的 MCP summary card。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 MCP collapsed summary 与 key-only fallback 用例，断言 summary 外层 `title` / `aria-label` 输出 `MCP: <summary>`；旧实现应因仍输出裸 summary 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中让 `mcpSummaryTargetLabel` 的非错误分支变为 `${mcpStatusLabel}: ${mcpSummaryText}`；错误分支继续保留 `MCP: Configuration error · ...`，不改 visible summary、展开状态、server rows、connectivity check、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 desktop MCP summary 外层 source label 规则。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 本轮 PLAN 2026-06-21 desktop session load card composite target label

- 目标：让桌面右侧 `StatusPanel` 的 History load 外层卡片 `title` / `aria-label` 同时暴露折叠摘要和 source path，例如 `History load: Window first paint · 120 / 5000 · 161ms · Source · C:/.../session-1.jsonl`，避免外层卡片只读到路径、却读不到用户正在扫视的加载状态摘要。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` session-load 渲染和 `StatusPanel.test.tsx` session-load 用例；确认 summary span、status pill、detail values 已有目标标签，但外层 `status-session-load-metrics` 仍只用 source path。对标项目 cc-gui 可读 `StatusPanel` 没有本项目同构 History load diagnostic card。
- 功能点 3：补 RED 测试。验证方式：扩展 completed history load 与 key-only fallback 用例，断言外层卡片输出 `History load: <summary> · Source · <path>`；旧实现应因仍输出 `History load: Source · <path>` 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中新增 session-load card composite target label，并写入外层 card 的 `title` / `aria-label`；保留 summary span、status pill、detail rows 原有标签，不改 session-load 数据、展开逻辑、缓存/窗口判断、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 session-load 外层卡片应同时携带 compact summary 与 source path。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/store 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 desktop session load card composite target label

- 上轮进展与阻塞：上一轮完成 desktop `StatusPanel` MCP summary source label；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已让 `mcpSummaryTargetLabel` 的非错误分支输出 `${mcpStatusLabel}: ${mcpSummaryText}`，`StatusPanel.test.tsx` 已覆盖 `MCP: 2 / 5 available` 和 key-only fallback 的 `MCP: 1 / 2 available`。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 是 todo/files/subagents tabs 与 popover，没有本项目同构的 History load diagnostic card。本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的历史加载性能诊断误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 desktop `StatusPanel` 的 History load summary span、status pill、detail value rows 已有目标标签，但外层 `status-session-load-metrics` 旧实现仍只暴露 `History load: Source · <path>`。用户价值：折叠卡片 hover、读屏和自动化快照能直接读到 `History load: Window first paint · 120 / 5000 · 161ms · Source · <path>`，同时知道加载状态摘要和原始 session 文件来源。
- 本轮规划：只改 `StatusPanel.tsx` 的 session-load 外层 card target label、`StatusPanel.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 session-load 数据采集、展开逻辑、缓存/窗口判断、Store/Rust/Tauri response。优先级中等：History load 是长会话高价值诊断面板，外层标签经常被 hover/读屏读取，实现成本低，风险集中在静态 markup。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 新增 `sessionLoadCardTargetLabel = ${sessionLoadSummaryTargetLabel} · ${sessionLoadSourceDetailLabel} · ${sessionLoadMetrics.sourcePath}`，并写入 `status-session-load-metrics` 外层容器的 `title` / `aria-label`；删除不再使用的 `sessionLoadSourceTargetLabel`，避免 TS strict noUnusedLocals 失败。`src/components/chat/StatusPanel.test.tsx` 扩展 completed history load 与 key-only fallback 用例，覆盖 window/cache 两种外层卡片 composite target label。`.trellis/spec/frontend/component-guidelines.md` 已记录 session-load 外层卡片应同时携带 compact summary 与 source path。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 2 项，失败点为旧实现外层 card 仍输出 `History load/历史加载: Source/来源 · <path>`。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts src/stores/useChatStore.test.ts`（9 files / 153 tests）。首次生产构建失败：`npm run build` 报 `TS6133: 'sessionLoadSourceTargetLabel' is declared but its value is never read`；删除死变量后复跑通过。全量前端最终通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建最终通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 History load 外层卡片 hover tooltip、折叠/展开行为和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 session-load loading/windowed/cache/error、MCP summary loading/error/configured/live-check、daemon ready/offline/error/reconnecting、current-message anchor、recent edit tree、task/subagent/edit 状态，点测 tooltip、焦点顺序和可访问名称；2) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact numeric/token surfaces 做自动化快照清单，优先 current activity hidden-count rows、recent edit overflow、MCP detail latency/status pills；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次；5) 若继续代码级小步优化，优先审计仍只有可见短文本、缺少 `title` / `aria-label` 或 `aria-describedby` 的 transient diagnostic rows。

## 本轮 PLAN 2026-06-21 desktop recent edit overflow readable fallback

- 目标：让桌面右侧 `StatusPanel` recent edit overflow 在 i18n key-only / 缺失翻译时不再退化为裸 `1 more file`，而是 fallback 为 `1 more file not shown in this list` / `N more files not shown in this list`，与真实 en/zh 翻译的语义一致。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` recent edit overflow 计算、`StatusPanel.test.tsx` key-only fallback 用例，以及可读 cc-gui `StatusPanel` files tab；确认 cc-gui 没有同构 overflow button，本项目真实 i18n 已清晰但 fallback 裸值仍不够。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 key-only recent edit chrome 用例，断言 `title` / `aria-label` 输出 `1 more file not shown in this list`；旧实现应因仍输出 `1 more file` 失败。
- 功能点 4：实现最小展示修复。验证方式：只改 `getHiddenEditFilesLabel` 的英文 fallback，保留现有 i18n key、真实 en/zh 文案、recent edit list cap、showAllEdits state、tree/diff/open-file 行为不变。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 recent edit overflow fallback 也必须保留“not shown in this list”的上下文。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/edit 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 desktop recent edit overflow readable fallback

- 上轮进展与阻塞：上一轮完成 desktop `StatusPanel` History load card composite target label；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已新增 `sessionLoadCardTargetLabel` 并写入 `status-session-load-metrics` 外层 `title` / `aria-label`，`StatusPanel.test.tsx` 已覆盖 window/cache 两种 `History load: <summary> · Source · <path>`。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 的 files tab 显示总量 additions/deletions stats，并通过 popover 展示文件变化；没有本项目同构的 recent edit overflow show-all/collapse button。本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的 right sidebar capped recent-edit list 误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目真实 en/zh 翻译已让 hidden edit overflow 说明“not shown in this list / 未在列表中显示”，但 key-only / 缺失翻译 fallback 旧实现会退化为裸 `1 more file`。用户价值：在 i18n 未初始化、测试快照或异常语言包场景中，用户仍能读到 `1 more file not shown in this list`，明确这是当前 recent edits 列表的隐藏项入口，而不是孤立文件计数。
- 本轮规划：只改 `StatusPanel.tsx` 的 `getHiddenEditFilesLabel` 英文 fallback、`StatusPanel.test.tsx` 的 key-only SSR 回归断言、前端组件规范和 Trellis 记录；不改现有 en/zh locale、showAllEdits 状态、recent edit cap、tree/diff/open-file 行为、Store/Rust/Tauri response。优先级中等：overflow 行在长工具链编辑后常见，fallback 改动极低风险，并补齐规范一致性。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 将 hidden edit files fallback 从 `${count} more file(s)` 改为 `${count} more file(s) not shown in this list`，真实 i18n key 和翻译文件不变。`src/components/chat/StatusPanel.test.tsx` 的 key-only recent edit chrome 用例改为断言 `title` / `aria-label` 输出 `1 more file not shown in this list`，并明确不接受裸 `title="1 more file"`。`.trellis/spec/frontend/component-guidelines.md` 已把 recent-edit hidden-file fallback 示例更新为 `1 more file not shown in this list`，并说明裸 `1 more file` 会丢失 overflow 控件上下文。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 1 项，失败点为旧实现仍输出 `title="1 more file"` / `aria-label="1 more file"`。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts src/stores/useChatStore.test.ts`（9 files / 153 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored；期间出现等待 cargo package/build directory lock 的非失败提示）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx` / `StatusPanel.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 recent edit overflow tooltip、点击展开全部/收起回最近列表行为和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 recent edit overflow、session-load loading/windowed/cache/error、MCP summary loading/error/configured/live-check、daemon ready/offline/error/reconnecting、current-message anchor、task/subagent/edit 状态，点测 tooltip、焦点顺序和可访问名称；2) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact numeric/token surfaces 做自动化快照清单，优先 MCP detail latency/status pills、current activity hidden-count rows、tool token/action labels；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次；5) 若继续代码级小步优化，优先审计仍只有可见短文本、缺少 `title` / `aria-label` 或 `aria-describedby` 的 transient diagnostic rows。

## 本轮 PLAN 2026-06-21 desktop activity history hidden footer context

- 目标：让桌面右侧 `StatusPanel` current activity 的 clipped task/subagent footer 使用 activity-history 专属上下文文案，例如 `Earlier tool tasks hidden from activity history: 1`，避免复用 composer input-status overflow 的泛化 `1 earlier tools hidden`。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `StatusPanel.tsx` activity hidden-count helper 与 `StatusPanel.test.tsx` current activity 用例；确认 hidden footer 已把 visible text、`title`、`aria-label` 同源，但旧文案仍缺少 activity-history 裁剪上下文。可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 是 todo/files/subagents tabs 与 popover，没有同构 clipped activity-history footer。
- 功能点 3：补 RED 测试。验证方式：扩展 `StatusPanel.test.tsx` 的 recent desktop task/subagent activity 和 key-only i18n 用例，断言真实 i18n 输出 `Earlier tool tasks hidden from activity history: 1` / `活动历史中还有 1 个更早工具任务未显示`，key-only fallback 输出 `1 earlier tool task hidden from activity history` / `1 earlier subagent hidden from activity history`；旧实现应失败。
- 功能点 4：实现最小展示修复。验证方式：在 `StatusPanel.tsx` 中把 hidden task/subagent footer 改用 `activityHistoryMoreTools` / `activityHistoryMoreSubagents` 专属 key，并补充 en/zh locale；不改 activity ordering、row jump、scroll region、composer `ChatInputStatusTabs` overflow、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 desktop activity-history clipped footers 不应复用 generic input-status overflow strings。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 chat/status/MCP/edit/store 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 desktop activity history hidden footer context

- 上轮进展与阻塞：上一轮完成 desktop `StatusPanel` recent edit overflow readable fallback；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 的 `getHiddenEditFilesLabel()` 已回退到 `1 more file not shown in this list`，`StatusPanel.test.tsx` 和 `.trellis/spec/frontend/component-guidelines.md` 已有对应覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/StatusPanel/StatusPanel.tsx` 是 todo/files/subagents tabs 与 popover，没有本项目同构的 right-sidebar current activity clipped footer。本轮没有直接 cc-gui 同类控件缺口可补齐。用户价值：继续基于真实代码判断，不把本项目额外的桌面 activity history 误判为 cc-gui parity 项。
- 本轮差距诊断（超越 cc-gui）：本项目 desktop `StatusPanel` current activity hidden footer 已把 visible text、`title`、`aria-label` 同源，但旧文案 `1 earlier tools hidden` / `1 earlier subagents hidden` 与 composer input-status overflow 共用语义，缺少“这是 activity history 被裁剪的更早项目”上下文。用户价值：长工具链会话中，用户 hover、读屏或看自动化快照时能直接知道隐藏项属于右侧活动历史，而不是输入区任务列表或普通计数。
- 本轮规划：只改 `StatusPanel.tsx` 的 hidden activity task/subagent label key 与 fallback、`StatusPanel.test.tsx` 的 SSR 回归断言、`src/locales/en.json` / `src/locales/zh.json` 新增 activity-history 专属文案、前端组件规范和 Trellis 记录；不改 activity ordering、row jump、scroll region、composer overflow、Store/Rust/Tauri response。优先级中等：activity history 在多工具任务中常见，语义改进明确，实现成本低，回归面集中在静态 markup 和 locale。
- 本轮完成：`src/components/chat/StatusPanel.tsx` 将 `getHiddenActivityTaskLabel()` / `getHiddenActivitySubagentLabel()` 从 `inputStatusMoreTools` / `inputStatusMoreSubagents` 切换到 `activityHistoryMoreTools` / `activityHistoryMoreSubagents`，并提供 key-only fallback `1 earlier tool task hidden from activity history` / `1 earlier subagent hidden from activity history`。`src/locales/en.json` 新增 `Earlier tool tasks hidden from activity history: {{count}}` 与 `Earlier subagents hidden from activity history: {{count}}`；`src/locales/zh.json` 新增 `活动历史中还有 {{count}} 个更早工具任务未显示` 与 `活动历史中还有 {{count}} 个更早子代理未显示`。`src/components/chat/StatusPanel.test.tsx` 更新 task/subagent hidden footer 与 key-only fallback 断言，并确认不暴露新旧 raw i18n key。`.trellis/spec/frontend/component-guidelines.md` 已记录 desktop activity-history clipped footer 应使用 activity-specific key，避免复用 generic input-status overflow strings。
- 本轮验证：RED：`npm test -- src/components/chat/StatusPanel.test.tsx` 先失败 3 项，失败点分别为 task footer 仍输出 `还有 1 个更早工具未显示`、subagent footer 仍输出 `还有 1 个更早子代理未显示`、key-only fallback 仍输出 `1 earlier tools hidden` / `1 earlier subagents hidden`。GREEN 定向通过：同命令通过（1 file / 34 tests）。相邻验证通过：`npm test -- src/components/chat/StatusPanel.test.tsx src/components/chat/StatusPanelOpenFilePropagation.test.tsx src/components/chat/ChatDiffReviewPane.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/chatDaemonStatus.test.ts src/utils/chatMcpStatus.test.ts src/utils/chatMcpConnectivity.test.ts src/utils/toolGrouping.test.ts src/stores/useChatStore.test.ts`（9 files / 153 tests）。全量前端通过：`npm test`（55 files / 505 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `out` 已清理，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/chat/StatusPanel.tsx src/components/chat/StatusPanel.test.tsx src/locales/en.json src/locales/zh.json .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `StatusPanel.tsx`、`StatusPanel.test.tsx`、`src/locales/en.json`、`src/locales/zh.json` 均 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 activity history hidden footer 的窄栏截断、hover tooltip 和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 current activity task/subagent overflow、recent edit overflow、session-load loading/windowed/cache/error、MCP summary loading/error/configured/live-check、daemon ready/offline/error/reconnecting、current-message anchor 状态，点测 tooltip、焦点顺序和可访问名称；2) 对 `StatusPanel` / `ChatInputStatusTabs` 剩余 compact numeric/token surfaces 做自动化快照清单，优先 MCP detail latency/status pills、tool token/action labels、session-load detail value rows；3) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 todo/files/subagents、运行状态、文件改动列表/树、MCP 配置和小屏 fallback；4) 开始收束同主题提交边界，把状态面板 a11y、toolblock action-label、键盘/冒泡边界拆成可审查提交批次；5) 若继续代码级小步优化，优先审计仍只有可见短文本、缺少 `title` / `aria-label` 或 `aria-describedby` 的 transient diagnostic rows。

## 本轮 PLAN 2026-06-21 task execution header summary aria labels

- 目标：让 `TaskExecutionBlock` 折叠标题里的任务 summary、身份 summary 和 runtime summary 在保留紧凑可见文本的同时，把同源文本写入 `aria-label`，避免长任务描述、模型/agent id 摘要被截断或被辅助技术单独读取时丢失语义。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `TaskExecutionBlock.tsx` 与 `TaskExecutionBlock.test.tsx`；确认 header 已有 keyboard/toggle label 的未提交改动，但 header 内部 `task-summary-text`、`tool-title-secondary-summary`、`tool-title-runtime-summary` 仍只有 `title`，没有同源 `aria-label`。对标 cc-gui 有 agent/task 工具卡片摘要，但本项目的 React 组件需要按本地 a11y 规范继续强化。
- 功能点 3：补 RED 测试。验证方式：扩展 `TaskExecutionBlock.test.tsx`，构造 description、result summary、model、agent id，断言三个 header summary span 都有 `title` 与 `aria-label`；旧实现应因缺少 `aria-label` 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `TaskExecutionBlock.tsx` 的三个 summary span 上添加同源 `aria-label`；不改 `extractAgentToolMeta()`、`summarizeAgentToolHeader()`、展开内容、SubagentHistoryPanel、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 Agent-like tool block header summary spans 的 `title` / `aria-label` 同源规则。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 toolblock/chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 尾部恢复摘要 2026-06-21 search expanded result snippets

- 本轮已完成并验证：`SearchToolGroupBlock` 展开结果行现在显示从 `path:line[:column]:snippet` 解析出的 snippet，`SearchResultFile` 增加可选 `snippet`，结果行 button label 为 `Open search result: <path> · L<line> · <snippet>`，CSS 保持 path/snippet 截断且 line 固定在右侧。
- 修改文件：`src/utils/toolPresentation.ts`、`src/utils/toolPresentation.test.ts`、`src/components/toolBlocks/SearchToolGroupBlock.tsx`、`src/components/toolBlocks/SearchToolGroupBlock.test.tsx`、`src/styles/toolBlocks.css`、`.trellis/spec/frontend/component-guidelines.md`、`TODO_LIST.md`。
- 验证通过：RED/GREEN 定向（2 files / 42 tests）、相邻验证（15 files / 173 tests）、`npm test`（55 files / 522 tests）、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）、dist/out 清理确认 `{"dist":false,"out":false}`、`git diff --check` / `git diff --cached --check`。IDE build/file problems 仍 120 秒超时。
- 下一步候选：真实 Tauri desktop smoke；审计 Search result dedupe 是否应展示同文件多匹配多行；收束同主题提交边界；与可读 cc-gui 做真实运行并排截图。

## 本轮 PLAN 2026-06-21 search same-file multi-match rows

- 目标：让 `SearchToolGroupBlock` 展开后的搜索结果列表在同一文件有多条命中时显示多条可打开行，而不是只保留该文件的第一条命中；同时保持 `matchCount` 与 `fileCount` 摘要语义不变。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `summarizeSearchResultText`、`SearchToolGroupBlock.tsx` 与相关测试；确认当前 `files` 按 lowercased path 去重，只保留同文件第一条命中。对标 cc-gui 未提供 snippet/多行命中展开，因此本轮属于本项目超越项。
- 功能点 3：补 RED 测试。验证方式：扩展 `toolPresentation.test.ts` 断言同文件两条不同 line/snippet 都进入 `files`，但 `fileCount` 仍为 1；扩展 `SearchToolGroupBlock.test.tsx` 断言展开区渲染同文件两条不同 result row 与可访问 label。旧实现应因只保留第一条失败。
- 功能点 4：实现最小展示修复。验证方式：调整 `summarizeSearchResultText`，用独立 unique path 集合计算 `fileCount`，用 path+line+snippet 去重可见 result rows，并继续最多展示 8 条；不改 openFile 参数、matchCount、summary 文案、GenericToolBlock、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 expanded Search result rows 的 `files` 展示语义为可打开命中行，`fileCount` 仍表示唯一文件数。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 toolblock/chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 search same-file multi-match rows

- 上轮进展与阻塞：上一轮完成 Search 展开结果 snippet 展示；本轮复查代码确认 `SearchResultFile.snippet`、`parseSearchResultLine()` 的 snippet 解析、`SearchToolGroupBlock` 展开行 snippet、combined `Open search result: <path> · L<line> · <snippet>` label 和三列 CSS 布局均已存在。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。本轮用 CodeGraph 读取了本项目 `SearchToolGroupBlock.tsx`、`summarizeSearchResultText()`、`parseSearchResultLine()` 与相关测试。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui Search group 展示 batch header、pattern/path/status 和滚动聚合，没有展开 snippet 或同文件多匹配行。因此本轮没有直接 cc-gui 缺口需要补齐。
- 本轮差距诊断（超越 cc-gui）：本项目上一轮已显示 snippet，但 `summarizeSearchResultText()` 仍按 lowercased path 去重 `files`，导致同一文件 `src/App.tsx:12:...`、`src/App.tsx:48:...` 只显示第一条。用户价值：搜索一个高频符号时，同文件多行命中很常见；展开列表显示多条命中行后，用户能直接打开目标行，不用读 raw output 或先跳到第一条再手动查找。
- 本轮规划与完成：只改 `src/utils/toolPresentation.ts`、`src/utils/toolPresentation.test.ts`、`src/components/toolBlocks/SearchToolGroupBlock.test.tsx`、`.trellis/spec/frontend/component-guidelines.md` 和 `TODO_LIST.md`。实现上将 `summarizeSearchResultText()` 的 `fileCount` 改为独立 unique path 集合计算，将可见 `files` 改为 path + line + snippet 维度去重并保留最多 8 条；不改 `matchCount`、explicit matches/files 解析、openFile 参数、Search 组件 DOM 结构、GenericToolBlock、Store/Rust/Tauri response。测试新增同文件 L12/L48 两条 snippet 的解析与展开 DOM 断言，并同步旧测试中已写死的 file-level dedupe 期望。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/utils/toolPresentation.test.ts` 先失败 2 项，失败点为 `files` 只返回 L12，展开 UI 未渲染 L48。GREEN 定向通过：同命令通过（2 files / 44 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/utils/toolPresentation.test.ts src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/chat/MessageItem.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/toolGrouping.test.ts`（15 files / 175 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（55 files / 524 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored；Cargo 等待 package cache/build directory file lock 后正常完成）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/utils/toolPresentation.ts src/utils/toolPresentation.test.ts .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `toolPresentation.ts`、`toolPresentation.test.ts`、`SearchToolGroupBlock.test.tsx`、`.trellis/spec/frontend/component-guidelines.md` 均 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察同文件多匹配 Search 展开行的实际截断、hover tooltip、焦点顺序和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，覆盖 expanded Search 多行 snippet、single Bash/Generic 多复制按钮、grouped Search/Read/Bash、Edit stats、Task/Agent、状态面板和 daemon/MCP 状态；2) 审计 Search 展开列表 8 条上限是否需要显示“更多结果在 raw output 中”的 compact footer；3) 收束同主题提交边界，把 toolblock a11y/action-label、搜索结果片段、多匹配行、复制反馈隔离拆成可审查提交批次；4) 与可读 cc-gui 做真实运行并排截图审计，重点看 grouped Search/Read、generic fallback、agent/task 工具块、todo/files/subagents、文件改动列表/树和小屏 fallback。

## 尾部恢复摘要 2026-06-21 search same-file multi-match rows

- 本轮已完成并验证：`summarizeSearchResultText()` 现在用 unique path 计算 `fileCount`，用 path + line + snippet 去重可见 result rows；同一文件多条 `path:line[:column]:snippet` 命中会在 Search 展开区显示为多条可打开行。
- 修改文件：`src/utils/toolPresentation.ts`、`src/utils/toolPresentation.test.ts`、`src/components/toolBlocks/SearchToolGroupBlock.test.tsx`、`.trellis/spec/frontend/component-guidelines.md`、`TODO_LIST.md`。
- 验证通过：RED/GREEN 定向（2 files / 44 tests）、相邻验证（15 files / 175 tests）、`npm test`（55 files / 524 tests）、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）、dist/out 清理确认 `{"dist":false,"out":false}`、`git diff --check` / `git diff --cached --check`。IDE build/file problems 仍 120 秒超时。
- 下一步候选：真实 Tauri desktop smoke；Search 展开列表 8 条上限 footer；收束同主题提交边界；与可读 cc-gui 做真实运行并排截图。

## 迭代记录 2026-06-21 toolblock copy feedback isolation

- 上轮进展与阻塞：上一轮完成 `SearchToolGroupBlock` header query/status summary、row pattern/result summary、row status pill 和 expanded result line chip 的 `title` / `aria-label` 目标化；本轮代码复查确认 `src/components/toolBlocks/SearchToolGroupBlock.tsx`、`SearchToolGroupBlock.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 已覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。本轮用 CodeGraph 读取了本项目 `BashToolBlock.tsx` / `GenericToolBlock.tsx` / 测试，以及可读 cc-gui `webview/src/components/toolBlocks/BashToolBlock.tsx` / `GenericToolBlock.tsx`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui 的 Bash/Generic tool block 没有复制命令/输入/输出按钮，因此不存在需要复制的 cc-gui 缺口。本项目已经在复制能力上超出 cc-gui，但新增能力需要避免错误成功反馈。
- 本轮差距诊断（超越 cc-gui）：本项目 `BashToolBlock` 使用单个 `copied` 布尔状态同时驱动“复制命令”和“复制输出”，`GenericToolBlock` 也用单个 `copied` 布尔状态同时驱动“复制输入”和“复制输出”。用户价值：当用户只复制命令或输入时，另一个按钮不应同时显示 `Copied`，否则会让用户误以为输出也已复制，尤其在排查命令结果或 MCP/raw tool payload 时容易粘贴错内容。
- 本轮规划：只改 `BashToolBlock.tsx` / `GenericToolBlock.tsx` 的 copied feedback state、对应 SSR 回归测试、`SingleToolBlockOpenFilePropagation.test.tsx` 的 `useRef` test mock、前端组件规范和 Trellis 记录；不改复制内容、header/action label、展开折叠、open file、tool result parsing、Store/Rust/Tauri response。
- 本轮完成：`src/components/toolBlocks/BashToolBlock.tsx` 将 `copied` 改为 `copiedTarget: 'command' | 'output' | null`，复制命令只让命令按钮显示 `tools.copied`，复制输出只让输出按钮显示 `tools.copied`，并通过 `copiedResetTimerRef` 清掉上一次 reset timer，避免快速连续复制时旧 timer 抢先清空新反馈。`src/components/toolBlocks/GenericToolBlock.tsx` 同样改为 `copiedTarget: 'input' | 'output' | null`。`BashToolBlock.test.tsx` 和 `GenericToolBlock.test.tsx` 新增 SSR 回归断言，模拟 command/input copied 状态时只有对应按钮显示成功态。`SingleToolBlockOpenFilePropagation.test.tsx` 的 React mock 增加 `useRef`，保持直接调用 memo 组件的既有测试策略。`.trellis/spec/frontend/component-guidelines.md` 已记录同一工具块多个复制按钮必须使用目标化 copied state，禁止共享单个布尔状态驱动多个成功态。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx` 先失败 2 项，失败点为旧实现会让两个按钮同时显示 `tools.copied`。GREEN 定向通过：同命令通过（2 files / 14 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/chat/MessageItem.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（15 files / 172 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端首次因 `SingleToolBlockOpenFilePropagation.test.tsx` 直接调用组件但未 mock `useRef` 失败；补齐 test mock 后 `npm test -- src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx` 通过（1 file / 3 tests），随后 `npm test` 通过（55 files / 521 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored；期间 Cargo 等待 package cache/build directory file lock 后正常完成）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/toolBlocks/BashToolBlock.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/SingleToolBlockOpenFilePropagation.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `BashToolBlock.tsx`、`GenericToolBlock.tsx`、`BashToolBlock.test.tsx`、`GenericToolBlock.test.tsx` 均 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 Bash/Generic detail action 的 copied feedback、快速连续复制 timer 行为、hover tooltip、焦点顺序和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 grouped Search/Read/Bash mixed status、single Bash/Generic 多复制按钮、single/grouped Edit stats、Read/Edit/Bash/Generic/Task/Agent header summary、diff hover、current activity overflow、recent edit overflow、MCP live-check、session-load、daemon 状态，点测 tooltip、焦点顺序和可访问名称；2) 审计 expanded search result rows 是否需要把匹配片段摘要也引入可见/可访问上下文，而不仅是 path/line；3) 继续收束同主题提交边界，把状态面板 a11y、toolblock header/action-label、键盘/冒泡边界拆成可审查提交批次；4) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 grouped Search/Read、generic fallback、agent/task 工具块、todo/files/subagents、文件改动列表/树和小屏 fallback。

## 迭代记录 2026-06-21 task execution header summary aria labels

- 上轮进展与阻塞：上一轮完成 desktop `StatusPanel` current activity clipped task/subagent footer 的 activity-history 专属上下文文案；本轮代码复查确认 `src/components/chat/StatusPanel.tsx` 已使用 `activityHistoryMoreTools` / `activityHistoryMoreSubagents`，`src/locales/en.json` / `src/locales/zh.json` 已有对应文案，`StatusPanel.test.tsx` 和 `.trellis/spec/frontend/component-guidelines.md` 已覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描本轮 120 秒超时未返回结果。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/toolBlocks/TaskExecutionBlock.tsx` 与 `AgentGroupBlock.tsx` 有 agent/task header summary、模型和 agent id 展示，但未见本项目同等的 summary span `aria-label` 规则。本轮没有直接 cc-gui parity 缺口可补齐。用户价值：继续基于真实代码判断，把 cc-gui 的任务摘要能力映射到本项目已有组件，并按本项目更严格的可访问语义补强。
- 本轮差距诊断（超越 cc-gui）：本项目 `TaskExecutionBlock` header 已有 keyboard/toggle label 的未提交改动，能读作 `Toggle task details: <target>`，但内部 `task-summary-text`、`tool-title-secondary-summary`、`tool-title-runtime-summary` 旧实现只有 `title`。用户价值：长任务描述、result 摘要、模型/agent id 等紧凑 header 文本被截断、hover 或被辅助技术单独读取时，仍能获取完整同源文本，不依赖父级 toggle action label 代替内容语义。
- 本轮规划：只改 `TaskExecutionBlock.tsx` 的三个 header summary span、`TaskExecutionBlock.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 `extractAgentToolMeta()`、`summarizeAgentToolHeader()`、展开内容、SubagentHistoryPanel、Store/Rust/Tauri response。优先级中等：任务工具块是长会话和子代理流的高频扫描对象，summary span 可访问标签成本极低，风险集中在静态 markup。
- 本轮完成：`src/components/toolBlocks/TaskExecutionBlock.tsx` 给 `task-summary-text`、`tool-title-secondary-summary`、`tool-title-runtime-summary` 三类 header summary span 添加与 `title` 同源的 `aria-label`。`src/components/toolBlocks/TaskExecutionBlock.test.tsx` 扩展 fixture，加入 completed result summary，并新增断言覆盖 primary description、secondary result summary、runtime `sonnet · agent-12…` 三类 span 的 `title` / `aria-label`。`.trellis/spec/frontend/component-guidelines.md` 已记录 Agent-like tool block compact header summaries 应镜像 displayed text 到 `title` / `aria-label`，父 header 保持 action-specific toggle label。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/TaskExecutionBlock.test.tsx` 先失败 1 项，失败点为旧实现三个 summary span 只有 `title`，缺少 `aria-label`。GREEN 定向通过：同命令通过（1 file / 3 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/chat/MessageItem.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts src/stores/useChatStore.test.ts`（9 files / 173 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（55 files / 506 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 与 `out` 已清理，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/toolBlocks/TaskExecutionBlock.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `TaskExecutionBlock.tsx` / `TaskExecutionBlock.test.tsx` 均 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 Task 工具 header summary 的截断、hover tooltip 和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 Task/Agent 工具 header summary、subagent history、current activity task/subagent overflow、recent edit overflow、MCP live-check、session-load、daemon 状态，点测 tooltip、焦点顺序和可访问名称；2) 对 `AgentGroupBlock` 做同类 header summary 审计，尤其 `agentType` / `summary` 是否也需要 `title` / `aria-label` 同源以及 action-specific toggle label；3) 对 ToolBlock copy/action buttons 做目标化审计，优先 Bash/Edit/Read/Generic 的 Copy Input / Copy Output / Copy Path visible-state 切换；4) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 agent/task 工具块、todo/files/subagents、运行状态、文件改动列表/树和小屏 fallback；5) 开始收束同主题提交边界，把状态面板 a11y、toolblock header/action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 agent group header summary aria labels

- 目标：让 `AgentGroupBlock` 折叠标题里的任务 summary、身份/result summary 和 runtime summary 与 `TaskExecutionBlock` 保持同一可访问语义：可见文本继续紧凑显示，同时同源写入 `title` / `aria-label`，避免长子代理描述、结果摘要、模型/agent id 摘要在截断、hover 或辅助技术读取时丢失。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `AgentGroupBlock.tsx` 与 `AgentGroupBlock.test.tsx`，并读取可读 cc-gui `webview/src/components/toolBlocks/AgentGroupBlock.tsx`；确认本项目 header 已有目标化 toggle 的未提交改动，但 header 内部 `task-summary-text`、`tool-title-secondary-summary`、`tool-title-runtime-summary` 仍只有 `title`，没有同源 `aria-label`；cc-gui 有 agent type / summary 展示，但 toggle 仍是通用 `Toggle agent group details`。
- 功能点 3：补 RED 测试。验证方式：扩展 `AgentGroupBlock.test.tsx`，构造 description、result summary、model、agent id，断言三个 header summary span 都有 `title` 与 `aria-label`；旧实现应因缺少 `aria-label` 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `AgentGroupBlock.tsx` 的三个 summary span 上添加同源 `aria-label`；不改 `extractAgentToolMeta()`、`summarizeAgentToolHeader()`、展开内容、SubagentHistoryPanel、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：复查 `.trellis/spec/frontend/component-guidelines.md` 的 Agent-like tool block 规则；如已有通用规则则补充 AgentGroupBlock 已纳入执行记录，不重复制造冲突段落。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 toolblock/chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 agent group header summary aria labels

- 上轮进展与阻塞：上一轮完成 `TaskExecutionBlock` header summary 的 `aria-label` 同源补齐；本轮代码复查确认 `src/components/toolBlocks/TaskExecutionBlock.tsx` 已给 `task-summary-text`、`tool-title-secondary-summary`、`tool-title-runtime-summary` 写入与 `title` 同源的 `aria-label`，`TaskExecutionBlock.test.tsx` 已覆盖 primary / secondary / runtime 三类 summary，`.trellis/spec/frontend/component-guidelines.md` 已包含 Agent-like tool block compact header summaries 的同源规则。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/toolBlocks/AgentGroupBlock.tsx` 有 agent type / summary 展示，summary 带 `title`，但 header toggle 仍是通用 `Toggle agent group details`。本项目 `AgentGroupBlock` 已有未提交的目标化 header toggle 和键盘激活改动，本轮补齐 summary 自身的可访问名称。用户价值：保留 cc-gui 的 agent 摘要扫描能力，同时让本项目在可访问语义上更完整。
- 本轮差距诊断（超越 cc-gui）：本项目 `AgentGroupBlock` 折叠 header 已能读作 `tools.agentDetailsToggle: Review frontend accessibility`，但内部 description、result summary、runtime `model · agent id` 三类紧凑 span 旧实现只有 `title`。用户价值：长子代理描述、执行结果摘要、模型/agent id 被截断、hover 或被辅助技术单独读取时，仍能获得完整同源文本，不依赖父级 toggle action label 代替内容语义。
- 本轮规划：只改 `AgentGroupBlock.tsx` 的三个 header summary span、`AgentGroupBlock.test.tsx` 的 SSR 回归断言和 Trellis 记录；不改 `extractAgentToolMeta()`、`summarizeAgentToolHeader()`、展开内容、SubagentHistoryPanel、Store/Rust/Tauri response。优先级中等偏高：Agent/Task 工具块是长会话和子代理流的高频扫描对象，和上一轮 Task 规则对齐成本极低，风险集中在静态 markup。
- 本轮完成：`src/components/toolBlocks/AgentGroupBlock.tsx` 给 `task-summary-text`、`tool-title-secondary-summary`、`tool-title-runtime-summary` 三类 header summary span 添加与 `title` 同源的 `aria-label`。`src/components/toolBlocks/AgentGroupBlock.test.tsx` 将 fixture 改为 completed result，并新增断言覆盖 primary description、secondary result summary、runtime `sonnet · agent-re…` 三类 span 的 `title` / `aria-label`。`.trellis/spec/frontend/component-guidelines.md` 已存在覆盖 `AgentGroupBlock` / `TaskExecutionBlock` 的 Agent-like 通用规则，本轮未重复追加冲突段落。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/AgentGroupBlock.test.tsx` 先失败 1 项，失败点为旧实现三个 summary span 只有 `title`，缺少 `aria-label`。GREEN 定向通过：同命令通过（1 file / 3 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/chat/MessageItem.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts src/stores/useChatStore.test.ts`（9 files / 174 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（55 files / 507 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/toolBlocks/AgentGroupBlock.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `AgentGroupBlock.tsx` / `AgentGroupBlock.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 Agent 工具 header summary 的截断、hover tooltip 和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 Task/Agent 工具 header summary、subagent history、current activity task/subagent overflow、recent edit overflow、MCP live-check、session-load、daemon 状态，点测 tooltip、焦点顺序和可访问名称；2) 对 ToolBlock copy/action buttons 做目标化审计，优先 Bash/Edit/Read/Generic 的 Copy Input / Copy Output / Copy Path visible-state 切换；3) 对 `AgentGroupBlock` 展开区的 description、agent id、model、reasoning rows 做目标标签审计，确认长 agent id / prompt 截断场景是否仍只依赖 code/title；4) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 agent/task 工具块、todo/files/subagents、运行状态、文件改动列表/树和小屏 fallback；5) 开始收束同主题提交边界，把状态面板 a11y、toolblock header/action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 generic tool header summary aria labels

- 目标：让 `GenericToolBlock` header 中的非交互紧凑摘要 span（目录/路径摘要、命令摘要、通用 action summary、result summary）在保留可见短文本和 `title` 的同时，同源写入 `aria-label`，避免 WebSearch、apply_patch fallback、命令类 generic 工具的摘要被截断或单独读取时丢失语义。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `GenericToolBlock.tsx` / `GenericToolBlock.test.tsx` 与可读 cc-gui `GenericToolBlock.tsx`；确认本项目 detail copy/action buttons 已有目标化标签，但 header 非交互 summary span 仍只有 `title`，没有同源 `aria-label`。
- 功能点 3：补 RED 测试。验证方式：扩展 `GenericToolBlock.test.tsx` 的 header accessibility 用例，断言 action summary 和 result summary 同时有 `title` 与 `aria-label`；旧实现应因缺少 `aria-label` 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `GenericToolBlock.tsx` 的非交互 summary span 上添加同源 `aria-label`；保留现有 open-file button、copy input/output label、AskUserQuestion 状态、header toggle keyboard 行为，不改 `summarizeGenericTool()`、result formatting、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 generic tool header compact summary spans 的 `title` / `aria-label` 同源规则，并与 action button 规则区分。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 toolblock/chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 generic tool header summary aria labels

- 上轮进展与阻塞：上一轮完成 `AgentGroupBlock` header summary 的 `aria-label` 同源补齐；本轮代码复查确认 `src/components/toolBlocks/AgentGroupBlock.tsx` 已给 `task-summary-text`、`tool-title-secondary-summary`、`tool-title-runtime-summary` 写入同源 `aria-label`，`AgentGroupBlock.test.tsx` 已覆盖 primary / secondary / runtime 三类 summary，`.trellis/spec/frontend/component-guidelines.md` 已包含 Agent-like tool block compact header summaries 的同源规则。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/toolBlocks/GenericToolBlock.tsx` 有通用工具 header、文件/命令/搜索摘要和状态指示，但没有本项目当前这套更严格的 header action label 与 summary span `aria-label` 规则。本项目 detail copy/action buttons 已有目标化标签，本轮不重复改 copy 逻辑。用户价值：继续保留 cc-gui 通用工具可扫描能力，同时补齐本项目在紧凑摘要可访问语义上的一致性。
- 本轮差距诊断（超越 cc-gui）：本项目 `GenericToolBlock` header 已有目标化 toggle、键盘激活、file target semantic button、copy input/output 目标化标签，但非交互的目录/路径摘要、命令摘要、通用 action summary、result summary 旧实现仍只有 `title`。用户价值：WebSearch、命令 fallback、非文件路径和 result 摘要在窄栏截断、hover 或被辅助技术单独读取时，仍能获得完整同源文本，不依赖父级 toggle action label 或视觉邻近关系。
- 本轮规划：只改 `GenericToolBlock.tsx` 的非交互 summary span、`GenericToolBlock.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；保留已有 AskUserQuestion completed 状态、header keyboard/toggle label、file target button、copy input/output labels，不改 `summarizeGenericTool()`、result formatting、Store/Rust/Tauri response。优先级中等偏高：generic tools 是 fallback 面，出现类型多且高频，summary span 补齐成本低、回归面集中在静态 markup。
- 本轮完成：`src/components/toolBlocks/GenericToolBlock.tsx` 给非交互 path/directory summary、command summary、generic action summary、result summary 添加与 `title` 同源的 `aria-label`；同时把 secondary summary className 改为数组过滤拼接，避免无意义尾随空格影响 SSR markup。`src/components/toolBlocks/GenericToolBlock.test.tsx` 新增 `mirrors compact header summaries into accessible labels` 用例，覆盖 `chat ui parity` action summary 和 `Search completed` result summary。`.trellis/spec/frontend/component-guidelines.md` 已记录 generic tool header compact summary spans 的同源规则，并明确 file targets 仍应保持 semantic open-file button。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/GenericToolBlock.test.tsx` 先失败 1 项，失败点为旧实现 action/result summary 只有 `title`，缺少 `aria-label`。GREEN 定向通过：同命令通过（1 file / 8 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/chat/MessageItem.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（11 files / 132 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（55 files / 508 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/toolBlocks/GenericToolBlock.tsx src/components/toolBlocks/GenericToolBlock.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `GenericToolBlock.tsx` / `GenericToolBlock.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 Generic 工具 header summary 的截断、hover tooltip 和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 Generic/WebSearch/apply_patch fallback、Bash/Read/Edit/Task/Agent header summary、current activity overflow、recent edit overflow、MCP live-check、session-load、daemon 状态，点测 tooltip、焦点顺序和可访问名称；2) 对 Bash/Read/Edit header compact spans 做同类收束，优先 Bash command/result summary、Read line-info、Edit file/change stats 是否也需要 `title` / `aria-label` 同源；3) 对 ToolBlock copy/action buttons 做二次审计，确认 `Copied` 状态下多个按钮共享 copied state 是否会让 Copy Command / Copy Output 同时变绿并造成误解；4) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 generic fallback、agent/task 工具块、todo/files/subagents、文件改动列表/树和小屏 fallback；5) 开始收束同主题提交边界，把状态面板 a11y、toolblock header/action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 bash tool header summary aria labels

- 目标：让 `BashToolBlock` header 中的命令摘要和结果摘要在保留紧凑可见文本与 `title` 的同时，同源写入 `aria-label`，避免长命令或执行结果首行被截断、hover 或辅助技术单独读取时丢失语义。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `BashToolBlock.tsx` / `BashToolBlock.test.tsx` 与可读 cc-gui `BashToolBlock.tsx`；确认本项目 header toggle 与 copy command/output 已有目标化标签的未提交改动，但 header 命令摘要和结果摘要仍只有 `title`，没有同源 `aria-label`。
- 功能点 3：补 RED 测试。验证方式：扩展 `BashToolBlock.test.tsx`，断言 command summary `npm test` 和 result summary `Tests passed` 同时有 `title` 与 `aria-label`；旧实现应因缺少 `aria-label` 失败。
- 功能点 4：实现最小展示修复。验证方式：在 `BashToolBlock.tsx` 的 command summary 和 result summary span 上添加同源 `aria-label`；保留现有 header keyboard/toggle label、copy command/output labels、Bash result parsing、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 Bash tool header compact command/result summary spans 的 `title` / `aria-label` 同源规则。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 toolblock/chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 bash tool header summary aria labels

- 上轮进展与阻塞：上一轮完成 `GenericToolBlock` header 非交互 summary 的 `aria-label` 同源补齐；本轮代码复查确认 `src/components/toolBlocks/GenericToolBlock.tsx` 已给 path/directory、command、generic action、result summary 写入同源 `aria-label`，`GenericToolBlock.test.tsx` 已覆盖 action/result summary，`.trellis/spec/frontend/component-guidelines.md` 已记录 generic header compact summary 规则。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。本轮用 CodeGraph 读取了本项目 `src/components/toolBlocks/BashToolBlock.tsx` / `BashToolBlock.test.tsx` 和可读 cc-gui `webview/src/components/toolBlocks/BashToolBlock.tsx`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/toolBlocks/BashToolBlock.tsx` 有 Bash header、命令描述与展开输出，但 header 仍是 click-only 容器，未见本项目当前的目标化 toggle label、键盘激活、copy command/output 目标化标签或 summary span `aria-label`。本轮没有直接 cc-gui 缺口需要复制；本项目已在交互语义上继续超越。
- 本轮差距诊断（超越 cc-gui）：本项目 `BashToolBlock` header 已有未提交的 `role="button"`、`tabIndex`、`aria-expanded`、`tools.bashDetailsToggle: <command>` 和 Copy Command / Copy Output 目标化标签，但命令摘要与结果摘要旧实现只有 `title`；成功态 result summary 还会渲染 `class="tool-title-secondary-summary "` 尾随空格。用户价值：长命令或执行结果首行在窄栏截断、hover 或被辅助技术单独读取时，仍能获得完整同源文本；SSR markup 更稳定，减少只因空格导致的测试脆弱性。
- 本轮规划：只改 `BashToolBlock.tsx` 的 command/result summary span、`BashToolBlock.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；保留现有 header keyboard/toggle label、copy command/output target labels、Bash result parsing、Store/Rust/Tauri response。优先级中等偏高：Bash 是高频工具块，命令与输出首行是用户扫描执行状态的核心信息，补齐成本低且回归面集中在静态 markup。
- 本轮完成：`src/components/toolBlocks/BashToolBlock.tsx` 给 command summary 添加 `aria-label={command}`，给 result summary 添加 `aria-label={resultSummary}`，并将 result summary `className` 改成数组过滤拼接，避免非错误态尾随空格。`src/components/toolBlocks/BashToolBlock.test.tsx` 新增 `mirrors compact header summaries into accessible labels` 用例，覆盖 `npm test` 命令摘要与 `Tests passed` 结果摘要的 `title` / `aria-label`。`.trellis/spec/frontend/component-guidelines.md` 已记录 Bash header command/result summary 的 compact-summary 同源规则。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/BashToolBlock.test.tsx` 先失败 1 项，失败点为旧实现 command/result summary 只有 `title`，缺少 `aria-label`，且 result summary class 有尾随空格。GREEN 定向通过：同命令通过（1 file / 4 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/chat/MessageItem.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（11 files / 133 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（55 files / 509 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/toolBlocks/BashToolBlock.tsx src/components/toolBlocks/BashToolBlock.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `BashToolBlock.tsx` / `BashToolBlock.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 Bash 工具 header command/result summary 的截断、hover tooltip 和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 Bash/Generic/WebSearch/apply_patch fallback、Read/Edit/Task/Agent header summary、current activity overflow、recent edit overflow、MCP live-check、session-load、daemon 状态，点测 tooltip、焦点顺序和可访问名称；2) 对 Read/Edit header compact spans 做同类收束，优先 Read line-info、Edit file/change stats 的 `title` / `aria-label` 同源和目标上下文；3) 对 ToolBlock copy/action buttons 做二次审计，确认 `Copied` 状态下多个按钮共享 copied state 是否会让 Copy Command / Copy Output 同时变绿并造成误解；4) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 Bash/generic fallback、agent/task 工具块、todo/files/subagents、文件改动列表/树和小屏 fallback；5) 开始收束同主题提交边界，把状态面板 a11y、toolblock header/action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 read tool header line summary aria labels

- 目标：让 `ReadToolBlock` header 的行号短标签和非文件 target summary 在保留紧凑可见文本的同时，同源写入 `title` / `aria-label`，避免 `L11-L30` 这类短标签或目录/非文件路径在截断、hover 或辅助技术单独读取时丢失上下文。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `ReadToolBlock.tsx` / `ReadToolBlock.test.tsx` 与可读 cc-gui `ReadToolBlock.tsx`；确认本项目文件 target 已是 semantic open-file button，但 line-info span 仍无 `title` / `aria-label`，非文件 target span 只有 `title` 没有 `aria-label`。cc-gui 有 file target 与 line info 展示，但没有本项目当前更严格的 header action/summary 语义。
- 功能点 3：补 RED 测试。验证方式：扩展 `ReadToolBlock.test.tsx`，断言 line-info `L11-L30` 同时有 `title` 与 `aria-label`；旧实现应因缺少这两个属性失败。
- 功能点 4：实现最小展示修复。验证方式：在 `ReadToolBlock.tsx` 中提取一次 `lineRangeLabel = formatLineRange(lineInfo)`，line-info span 写入 `title` / `aria-label`；非文件 target span 同步 `aria-label={filePath}`。保留现有 open-file button、copy path label、line calculation、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 Read header line-info 和非交互 target summary 的 `title` / `aria-label` 同源规则，文件 target 仍保持 semantic open-file button。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 toolblock/chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 read tool header line summary aria labels

- 上轮进展与阻塞：上一轮完成 `BashToolBlock` header command/result summary 的 `aria-label` 同源补齐；本轮代码复查确认 `src/components/toolBlocks/BashToolBlock.tsx` 已给 command summary 写入 `aria-label={command}`、给 result summary 写入 `aria-label={resultSummary}`，并清理 result summary class 尾随空格，`BashToolBlock.test.tsx` 已覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`。本轮用 CodeGraph 读取了本项目 `src/components/toolBlocks/ReadToolBlock.tsx` / `ReadToolBlock.test.tsx`、`formatLineRange()` / `getToolLineInfo()`，以及可读 cc-gui `webview/src/components/toolBlocks/ReadToolBlock.tsx`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/toolBlocks/ReadToolBlock.tsx` 有 Read file target 与 line info 展示，并可点击打开文件，但旧实现仍是 click-only header，没有本项目当前的目标化 toggle label、keyboard activation、copy path target label 或 summary span `aria-label` 规则。本轮没有直接 cc-gui 缺口需要复制；本项目继续在交互语义上超越。
- 本轮差距诊断（超越 cc-gui）：本项目 `ReadToolBlock` 文件 target 已是 semantic open-file button，并带 `tools.openFile: <path>` 的 `title` / `aria-label`；detail open/copy path 按钮也已有目标化标签。但 header 的 `line-info` 旧实现只显示 `L11-L30`，没有 `title` / `aria-label`；非文件 target span 旧实现只有 `title={filePath}`，没有同源 `aria-label`。用户价值：行号短标签或目录/非文件 target 在窄栏截断、hover 或被辅助技术单独读取时，仍能获取完整同源文本，不依赖相邻文件名或父级 toggle label。
- 本轮规划：只改 `ReadToolBlock.tsx` 的 line-info span 与非文件 target span、`ReadToolBlock.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；保留现有 open-file button、copy path label、line calculation、Store/Rust/Tauri response。优先级中等偏高：Read 是高频工具块，行号范围是定位阅读上下文的核心信息，改动极低风险且与前几轮 header summary 规则一致。
- 本轮完成：`src/components/toolBlocks/ReadToolBlock.tsx` 新增 `lineRangeLabel = formatLineRange(lineInfo)`，将 header line-info span 改为同时输出 `title={lineRangeLabel}` 和 `aria-label={lineRangeLabel}`；非文件 target summary span 增加 `aria-label={filePath}`，与既有 `title={filePath}` 同源。`src/components/toolBlocks/ReadToolBlock.test.tsx` 新增 `mirrors compact line ranges into accessible labels` 用例，覆盖 `offset: 10, limit: 20` 派生的 `L11-L30`。`.trellis/spec/frontend/component-guidelines.md` 已记录 Read line-range summary 和非交互 target summary 的同源规则，文件 target 仍保持 semantic open-file button。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/ReadToolBlock.test.tsx` 先失败 1 项，失败点为旧实现 `line-info` 只有可见 `L11-L30`，缺少 `title` / `aria-label`。GREEN 定向通过：同命令通过（1 file / 6 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/chat/MessageItem.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（11 files / 134 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（55 files / 510 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/toolBlocks/ReadToolBlock.tsx src/components/toolBlocks/ReadToolBlock.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `ReadToolBlock.tsx` / `ReadToolBlock.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 Read 工具 header line-info 和非文件 target summary 的截断、hover tooltip 和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 Read file/directory/line-range、Edit file/change stats、Bash/Generic/Task/Agent header summary、current activity overflow、recent edit overflow、MCP live-check、session-load、daemon 状态，点测 tooltip、焦点顺序和可访问名称；2) 对 `EditToolBlock` header compact spans 做同类收束，优先非 open-file target span、edit change stats `+N/-M`、diff hover trigger 的 `title` / `aria-label` 同源和目标上下文；3) 对 ToolBlock copy/action buttons 做二次审计，确认 `Copied` 状态下多个按钮共享 copied state 是否会让 Copy Command / Copy Output 或 open/copy 操作同时变绿并造成误解；4) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 Read/Edit/generic fallback、agent/task 工具块、todo/files/subagents、文件改动列表/树和小屏 fallback；5) 开始收束同主题提交边界，把状态面板 a11y、toolblock header/action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 edit tool header stats labels

- 目标：让 `EditToolBlock` header 中可见的 `+N/-M` 变更统计具备目标化 `title` / `aria-label`，并把内部裸数字设为 `aria-hidden`，避免文件编辑规模只能靠视觉判断或被辅助技术读成脱离文件上下文的 `+1 -1`。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `EditToolBlock.tsx` / `EditToolBlock.test.tsx`、`EditDiffPreview.tsx` 与可读 cc-gui `EditToolBlock.tsx`；确认本项目 header toggle、open-file button、copy path 已有目标化标签，但 header 的 `edit-item-stats` 仍是裸 `+N/-M`。
- 功能点 3：补 RED 测试。验证方式：扩展 `EditToolBlock.test.tsx`，断言 header stats wrapper 输出 `Edit stats: src/App.tsx · +1 / -1` 的 `title` / `aria-label`，且内部 `+1` / `-1` pill 为 `aria-hidden`；旧实现应失败。
- 功能点 4：实现最小展示修复。验证方式：在 `EditToolBlock.tsx` 复用既有 `chat.layout.inputStatusEditFileStats` 语义生成 `editStatsLabel`，写入 open-file 与非 open-file 分支的 stats wrapper；内部数字 pill 加 `aria-hidden="true"`；非 open-file target span 同步 `aria-label={filePath}`。不改 diff preview、openFile、copy path、collectEditToolItems、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：复查 `.trellis/spec/frontend/component-guidelines.md` 已有 stats pill 规则，如需补充 EditToolBlock header 执行规则则追加，不重复制造冲突段落。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 toolblock/chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 edit tool header stats labels

- 上轮进展与阻塞：上一轮完成 `ReadToolBlock` header line-info 与非文件 target summary 的 `title` / `aria-label` 同源补齐；本轮代码复查确认 `src/components/toolBlocks/ReadToolBlock.tsx` 已将 `L11-L30` 这类 line range 写入 `title` / `aria-label`，非文件 target span 也已有 `aria-label={filePath}`。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`，`webview/src/components/toolBlocks/EditToolBlock.tsx` 可读。本轮用 CodeGraph 读取了本项目 `src/components/toolBlocks/EditToolBlock.tsx` / `EditToolBlock.test.tsx` 相关源码，以及可读 cc-gui `webview/src/components/toolBlocks/EditToolBlock.tsx`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/toolBlocks/EditToolBlock.tsx` 有编辑文件 target、line info、diff action、refresh action 和 `+N/-M` 统计展示，但 header 仍是 click-only 容器，stats span 也没有本项目当前采用的 target label / `aria-hidden` 规则。本轮没有需要直接复制的 cc-gui 缺口；本项目继续在键盘、目标化标签和辅助技术语义上超越。
- 本轮差距诊断（超越 cc-gui）：本项目 `EditToolBlock` header toggle、open-file button、copy path button 已有目标化标签，但 `edit-item-stats` 旧实现只显示裸 `+1` / `-1`。用户价值：用户在窄栏、hover、读屏或自动化快照中能直接获得 `Edit stats: src/App.tsx · +1 / -1`，不会把无上下文的数字误解成独立状态；内部可见数字设为 `aria-hidden` 后也避免重复播报。
- 本轮规划：只改 `EditToolBlock.tsx` 的 header stats wrapper 与非 open-file target span、`EditToolBlock.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 diff preview、openFile、copy path、`collectEditToolItems`、Store/Rust/Tauri response。优先级中等偏高：Edit 是高频工具块，变更统计是用户扫描风险的核心信息，实现成本低且回归面集中在静态 markup。
- 本轮完成：`src/components/toolBlocks/EditToolBlock.tsx` 新增 `hasEditStats`、`editStatsFallbackLabel`、`translatedEditStatsLabel` 和 `editStatsLabel`，复用 `chat.layout.inputStatusEditFileStats` 生成 `Edit stats: <file> · +N / -M` 语义标签；open-file 与非 open-file 分支的 `.edit-item-stats` 均写入 `title` / `aria-label`，内部 `.edit-stat-added` / `.edit-stat-deleted` 均写入 `aria-hidden="true"`；非 open-file target summary span 增加 `aria-label={filePath}`。`src/components/toolBlocks/EditToolBlock.test.tsx` 扩展 i18n mock 并新增 `mirrors compact edit stats into an accessible target label` 用例。`.trellis/spec/frontend/component-guidelines.md` 已记录 Edit tool header stat summaries 应通过 wrapper 暴露目标化 stats label，并隐藏内部重复数字。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/EditToolBlock.test.tsx` 先失败 1 项，失败点为旧实现 `.edit-item-stats` 没有 `title` / `aria-label` 且内部数字未 `aria-hidden`。GREEN 定向通过：同命令通过（1 file / 6 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/chat/MessageItem.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（11 files / 135 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（55 files / 511 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/toolBlocks/EditToolBlock.tsx src/components/toolBlocks/EditToolBlock.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `EditToolBlock.tsx` / `EditToolBlock.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 Edit stats 的 hover tooltip、截断表现、焦点顺序和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 Read/Edit/Bash/Generic/Task/Agent header summary、Edit stats、diff hover、current activity overflow、recent edit overflow、MCP live-check、session-load、daemon 状态，点测 tooltip、焦点顺序和可访问名称；2) 对 `EditToolGroupBlock` 的 group total stats 与 item stats 做同类收束，优先 `edit-total-stats` 和 `task-group-item-badge edit-item-stats` 的目标化 `title` / `aria-label` 与内部 `aria-hidden`；3) 对 ToolBlock copy/action buttons 做二次审计，确认共享 `copied` state 是否会让多个复制按钮同时显示成功并造成误解；4) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 Read/Edit/generic fallback、agent/task 工具块、todo/files/subagents、文件改动列表/树和小屏 fallback；5) 开始收束同主题提交边界，把状态面板 a11y、toolblock header/action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 edit group stats target labels

- 目标：让 `EditToolGroupBlock` header 的总计 `+N/-M` 和每个 edit row 的 `+N/-M` badge 都具备目标化 `title` / `aria-label`，并把内部裸数字设为 `aria-hidden`，避免分组编辑规模被读成脱离文件或分组上下文的裸数字。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `EditToolGroupBlock.tsx` / `EditToolGroupBlock.test.tsx` 与可读 cc-gui `EditToolGroupBlock.tsx`；确认本项目 group toggle、item toggle、open-file button、bulk actions 已有目标化标签，但 `edit-total-stats` 与 item `edit-item-stats` 仍是裸 `+N/-M`。
- 功能点 3：补 RED 测试。验证方式：扩展 `EditToolGroupBlock.test.tsx`，断言 group total stats 输出 `Edit stats: src/App.tsx · 3 files · +3 / -3` 的 `title` / `aria-label`，item stats 输出 `Edit stats: src/App.tsx · +1 / -1` 的 `title` / `aria-label`，且内部 `+N` / `-M` pill 为 `aria-hidden`；旧实现应失败。
- 功能点 4：实现最小展示修复。验证方式：在 `EditToolGroupBlock.tsx` 复用 `chat.layout.inputStatusEditFileStats` 语义生成 item stats label，并为 group total 使用同一语义加 `N files` target；写入 group 与 item stats wrapper，内部数字 pill 加 `aria-hidden="true"`。不改 `mergeEditToolItemsByFile`、聚合数量、展开/折叠、diff hover、open file、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 Edit group total/item stats 的 wrapper target label 与内部数字隐藏规则。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 toolblock/chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 edit group stats target labels

- 上轮进展与阻塞：上一轮完成 `EditToolBlock` header stats wrapper 的 `title` / `aria-label` 目标化和内部数字 `aria-hidden`；本轮代码复查确认 `src/components/toolBlocks/EditToolBlock.tsx` 已在 open-file 与非 open-file 分支为 `.edit-item-stats` 输出 `Edit stats: <file> · +N / -M`，`EditToolBlock.test.tsx` 已覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`，`webview/src/components/toolBlocks/EditToolGroupBlock.tsx` 可读。本轮用 CodeGraph 读取了本项目 `src/components/toolBlocks/EditToolGroupBlock.tsx` / `EditToolGroupBlock.test.tsx` 相关源码，以及可读 cc-gui `webview/src/components/toolBlocks/EditToolGroupBlock.tsx` 的 `EditToolGroupBlock` / `EditFileItem`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/toolBlocks/EditToolGroupBlock.tsx` 有 batch edit header、总计 `+N/-M`、文件列表、单项 stats、diff/refresh action 和自动刷新，但 header 仍是 click-only 容器，stats 也没有目标化 `title` / `aria-label` 或内部数字 `aria-hidden`。本轮没有需要直接复制的 cc-gui 缺口；本项目在 group keyboard、open-file action labels、bulk action labels 和 stats 语义上继续超越。
- 本轮差距诊断（超越 cc-gui）：本项目 `EditToolGroupBlock` 已有 group header toggle、item row toggle、open-file button、bulk expand/collapse 的目标化标签，但 `edit-total-stats` 和 row-level `task-group-item-badge edit-item-stats` 旧实现仍是裸 `+N/-M`。用户价值：用户在批量编辑中 hover、读屏或查看自动化快照时，能区分总计 `Edit stats: src/App.tsx · 3 files · +3 / -3` 与单文件 `Edit stats: src/App.tsx · +1 / -1`，不会把重复数字当成无上下文状态。
- 本轮规划：只改 `EditToolGroupBlock.tsx` 的 group total/item stats wrapper、`EditToolGroupBlock.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 `mergeEditToolItemsByFile`、聚合数量、排序、展开/折叠、diff hover、open file、Store/Rust/Tauri response。优先级中等偏高：批量 Edit 在长任务中常见，stats 是扫描变更规模的核心信息，改动成本低且继承上轮单个 Edit 的同一语义规则。
- 本轮完成：`src/components/toolBlocks/EditToolGroupBlock.tsx` 新增 `hasTotalEditStats`、`getEditStatsLabel(...)`、`groupStatsTarget` 和 `totalStatsLabel`，让 group header `.edit-total-stats` 输出 `title` / `aria-label`，并将内部 `.edit-stat-added` / `.edit-stat-deleted` 设为 `aria-hidden="true"`；每个 edit row 计算 `hasItemStats` / `itemStatsLabel`，让 `.task-group-item-badge.edit-item-stats` 输出文件级 stats label，并隐藏内部数字。`src/components/toolBlocks/EditToolGroupBlock.test.tsx` 扩展 i18n mock，新增 group total stats 和 row stats 两个回归用例；fixture 继续保留 completed/pending/failed 混合状态，覆盖 3 个文件。`.trellis/spec/frontend/component-guidelines.md` 已记录 Edit group total/item stats 的 wrapper target label 和内部数字隐藏规则。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/EditToolGroupBlock.test.tsx` 先失败 2 项，失败点为旧实现 group total stats 和 row stats 都缺少 `title` / `aria-label`，内部数字也未 `aria-hidden`。GREEN 定向通过：同命令通过（1 file / 9 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/chat/MessageItem.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（12 files / 144 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（55 files / 513 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/toolBlocks/EditToolGroupBlock.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `EditToolGroupBlock.tsx` / `EditToolGroupBlock.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 Edit group total/item stats 的 hover tooltip、截断表现、焦点顺序和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造单个/分组 Edit stats、Read/Edit/Bash/Generic/Task/Agent header summary、diff hover、current activity overflow、recent edit overflow、MCP live-check、session-load、daemon 状态，点测 tooltip、焦点顺序和可访问名称；2) 继续审计 grouped tool compact stats，优先 `BashToolGroupBlock` 命令状态/计数、`ReadToolGroupBlock` 行号与文件计数、`SearchToolGroupBlock` result count 是否仍有裸数字或只靠相邻文本解释；3) 对 ToolBlock copy/action buttons 做二次审计，确认共享 `copied` state 是否会让多个复制按钮同时显示成功并造成误解；4) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 grouped Edit/Bash/Read、generic fallback、agent/task 工具块、todo/files/subagents、文件改动列表/树和小屏 fallback；5) 开始收束同主题提交边界，把状态面板 a11y、toolblock header/action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 bash group compact summary labels

- 目标：让 `BashToolGroupBlock` 的命令数量 chip、header command/status summary、row command/result summary 和 row status pill 都具备同源 `title` / `aria-label`，避免分组 Bash 的重复短文本或状态 pill 被读成脱离命令上下文的裸数字、裸状态。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `BashToolGroupBlock.tsx` / `BashToolGroupBlock.test.tsx` 与可读 cc-gui `BashToolGroupBlock.tsx`；确认本项目 group/item toggle 与 bulk actions 已有目标化标签，但 header count chip、header summary、row command/result summary 和 row status pill 仍未完整同源暴露 `aria-label`。
- 功能点 3：补 RED 测试。验证方式：扩展 `BashToolGroupBlock.test.tsx`，断言 command count chip 输出 `tools.commandCount:4` 的 `title` / `aria-label`，header primary/status summary 同源 `title` / `aria-label`，row command/result summary 同源 `title` / `aria-label`，row status pill 输出 `Bash: npm test · tools.success`；旧实现应失败。
- 功能点 4：实现最小展示修复。验证方式：在 `BashToolGroupBlock.tsx` 为上述 compact spans/pills 添加同源标签；状态 pill label 使用 `Bash: <command target> · <status label>`，不改 `summarizeBashGroupHeader`、`summarizeCommand`、`summarizeGroupBashItemResult`、分组数量、展开/折叠、BashToolBlock、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 Bash group compact count/summary/status pill 的 target-label 规则。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 toolblock/chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 bash group compact summary labels

- 上轮进展与阻塞：上一轮完成 `EditToolGroupBlock` group total/item stats 的目标化 `title` / `aria-label` 与内部数字 `aria-hidden`；本轮代码复查确认 `src/components/toolBlocks/EditToolGroupBlock.tsx` 已为 `.edit-total-stats` 和 `.task-group-item-badge.edit-item-stats` 输出 contextual stats label，`EditToolGroupBlock.test.tsx` 已覆盖 3 文件 mixed status fixture。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`，`webview/src/components/toolBlocks/BashToolGroupBlock.tsx` 可读。本轮用 CodeGraph 读取了本项目 `src/components/toolBlocks/BashToolGroupBlock.tsx` / `BashToolGroupBlock.test.tsx` 相关源码，以及可读 cc-gui `webview/src/components/toolBlocks/BashToolGroupBlock.tsx`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/toolBlocks/BashToolGroupBlock.tsx` 有 batch run header、progress summary、timeline item、单项展开详情和状态点，但 header 与 timeline row 仍是 click-only/视觉状态，没有本项目当前的 keyboard toggle、bulk action target labels 或 compact summary/status pill `aria-label`。本轮没有需要直接复制的 cc-gui 缺口；本项目继续在键盘和可访问语义上超越。
- 本轮差距诊断（超越 cc-gui）：本项目 `BashToolGroupBlock` 的 group/item toggle 与 bulk actions 已有目标化标签，但 header count chip、header primary/status summary、row command/result summary 和 row status pill 旧实现仍只靠可见文本或 `title`。用户价值：批量 Bash 中会重复出现 `Success` / `Failed` / `Pending` 和短命令 chip；补齐后 hover、读屏和自动化快照能直接读到 `Bash: npm test · Success`、`4 commands`、完整命令和结果摘要，用户不用依赖相邻 DOM 或视觉行位置推断。
- 本轮规划：只改 `BashToolGroupBlock.tsx` 的 compact summary/status pill 属性、`BashToolGroupBlock.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 `summarizeBashGroupHeader`、`summarizeCommand`、`summarizeGroupBashItemResult`、分组数量、展开/折叠、BashToolBlock、Store/Rust/Tauri response。优先级中等偏高：Bash group 是高频执行面，重复短状态很多，改动成本低且与前几轮 toolblock a11y 模式一致。
- 本轮完成：`src/components/toolBlocks/BashToolGroupBlock.tsx` 新增 `commandCountLabel` 并写入 header count chip 的 `title` / `aria-label`；header primary summary 与 status summary 现在都有同源 `title` / `aria-label`；row command summary 使用 `commandLabel` 同源输出 `title` / `aria-label`；row result summary 同源输出 `title` / `aria-label`；row status pill 新增 `itemStatusText` / `itemStatusLabel`，输出 `Bash: <command target> · <status>` 的 `title` / `aria-label`。`src/components/toolBlocks/BashToolGroupBlock.test.tsx` 新增 header compact summary、row command/result summary、row status pill 三组断言。`.trellis/spec/frontend/component-guidelines.md` 已记录 Bash group compact count/summary/status pill 的 target-label 规则。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/BashToolGroupBlock.test.tsx` 先失败 3 项，失败点为旧实现缺少 command count chip label、header summary `aria-label`、row command/result `aria-label` 和 row status pill target label。GREEN 定向通过：同命令通过（1 file / 8 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/chat/MessageItem.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（13 files / 152 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（55 files / 515 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored；期间 Cargo 等待 package cache/build directory file lock 后正常完成）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/toolBlocks/BashToolGroupBlock.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `BashToolGroupBlock.tsx` / `BashToolGroupBlock.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 Bash group count/summary/status pill 的 hover tooltip、截断表现、焦点顺序和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 grouped Bash mixed status、single/grouped Edit stats、Read/Edit/Bash/Generic/Task/Agent header summary、diff hover、current activity overflow、recent edit overflow、MCP live-check、session-load、daemon 状态，点测 tooltip、焦点顺序和可访问名称；2) 继续审计 grouped tool compact stats，优先 `ReadToolGroupBlock` 行号/文件计数/status pill、`SearchToolGroupBlock` result count 和 row status 是否仍有裸数字或只靠相邻文本解释；3) 对 ToolBlock copy/action buttons 做二次审计，确认共享 `copied` state 是否会让多个复制按钮同时显示成功并造成误解；4) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 grouped Bash/Read/Search、generic fallback、agent/task 工具块、todo/files/subagents、文件改动列表/树和小屏 fallback；5) 开始收束同主题提交边界，把状态面板 a11y、toolblock header/action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 read group compact summary labels

- 目标：让 `ReadToolGroupBlock` 的 header primary/secondary summary、row line summary 和 row status pill 都具备同源 `title` / `aria-label` 或目标化 label，避免 grouped Read 中 `3 files`、`L11-30`、`Success` / `Failed` / `Pending` 被读成脱离文件上下文的短文本。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `ReadToolGroupBlock.tsx` / `ReadToolGroupBlock.test.tsx` 与可读 cc-gui `ReadToolGroupBlock.tsx`；确认本项目 group/item toggle、open-file button、bulk actions 已有目标化标签，但 header summary、row line summary 与 row status pill 仍未完整暴露同源 `aria-label` 或目标上下文。
- 功能点 3：补 RED 测试。验证方式：扩展 `ReadToolGroupBlock.test.tsx`，断言 header primary/secondary summary 同源 `title` / `aria-label`，row line summary 输出 `Read lines: src/App.tsx · L11-30`，row status pill 输出 `Read: src/App.tsx · tools.success` / `Read: src/error.tsx · tools.failed`；旧实现应失败。
- 功能点 4：实现最小展示修复。验证方式：在 `ReadToolGroupBlock.tsx` 为 compact summaries 添加同源标签；row line label 使用 `Read lines: <target> · <line>`，status pill label 使用 `Read: <target> · <status>`。不改 `summarizeReadGroupHeader`、`getToolLineInfo`、分组数量、展开/折叠、open file、`ReadToolBlock`、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 Read group compact summary/line/status pill 的 target-label 规则。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 toolblock/chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 read group compact summary labels

- 上轮进展与阻塞：上一轮完成 `BashToolGroupBlock` command count、header summary、row command/result summary 与 row status pill 的同源 `title` / `aria-label`；本轮代码复查确认 `src/components/toolBlocks/BashToolGroupBlock.tsx` 已有 `commandCountLabel`、header/row summary aria-label、`Bash: <command> · <status>` 状态 pill label，`BashToolGroupBlock.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 已覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`，`webview/src/components/toolBlocks/ReadToolGroupBlock.tsx` 可通过 CodeGraph 读取。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/toolBlocks/ReadToolGroupBlock.tsx` 有 Read batch header、文件列表、行号、状态点和自动滚动，但没有本项目当前的 group/item keyboard toggle、bulk expand/collapse action labels、open-file action labels 或 compact summary/status pill `aria-label`。本轮没有需要直接复制的 cc-gui 缺口；本项目继续在键盘和可访问语义上超越。
- 本轮差距诊断（超越 cc-gui）：本项目 `ReadToolGroupBlock` 的 group/item toggle、open-file button 和 bulk actions 已有目标化标签，但 header primary/secondary summary、row line summary 与 row status pill 旧实现仍只靠可见短文本或 `title`。用户价值：批量 Read 中常见 `3 files`、`L11-30`、`Success` / `Failed` / `Pending` 重复短文本；补齐后 hover、读屏和自动化快照能直接读到 `src/App.tsx`、`Read lines: src/App.tsx · L11-30`、`Read: src/App.tsx · Success`，不用依赖相邻 DOM 或视觉行位置推断。
- 本轮规划：只改 `ReadToolGroupBlock.tsx` 的 compact summary/line/status pill 属性、`ReadToolGroupBlock.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 `summarizeReadGroupHeader`、`getToolLineInfo`、分组数量、展开/折叠、open file、`ReadToolBlock`、Store/Rust/Tauri response。优先级中等偏高：Read group 是高频文件查看面，行号和状态是扫描重点，改动成本低且延续 Bash/Edit 同一模式。
- 本轮完成：`src/components/toolBlocks/ReadToolGroupBlock.tsx` 为 header `.tool-title-summary` / `.tool-title-secondary-summary` 镜像同源 `aria-label`；为非文件 target span 镜像 `aria-label`；为 row line summary 输出 `Read lines: <target> · <line>` 的 `title` / `aria-label`；为 row status pill 输出 `Read: <target> · <status>` 的 `title` / `aria-label`，并复用同一个 `itemStatusText` 保持可见文本与标签来源一致。`src/components/toolBlocks/ReadToolGroupBlock.test.tsx` 新增 header summary aria、line summary target label、row status pill target label 三类断言。`.trellis/spec/frontend/component-guidelines.md` 已记录 Read group compact summary/line/status pill target-label 规则。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/ReadToolGroupBlock.test.tsx` 先失败 3 项，失败点为 header summary 缺 `aria-label`、line summary 仍是裸 `title="L11-30"`、row status pill 仍是裸 `tools.success` / `tools.failed`。GREEN 定向通过：同命令通过（1 file / 9 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/chat/MessageItem.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（14 files / 161 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（55 files / 517 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/toolBlocks/ReadToolGroupBlock.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `ReadToolGroupBlock.tsx` / `ReadToolGroupBlock.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 Read group header summary、line badge 和 status pill 的 hover tooltip、截断表现、焦点顺序和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 grouped Read/Bash/Search mixed status、single/grouped Edit stats、Read/Edit/Bash/Generic/Task/Agent header summary、diff hover、current activity overflow、recent edit overflow、MCP live-check、session-load、daemon 状态，点测 tooltip、焦点顺序和可访问名称；2) 继续审计 grouped tool compact stats，优先 `SearchToolGroupBlock` result count、row status pill、query/first matched file summary 是否仍有裸数字或只靠相邻文本解释；3) 对 ToolBlock copy/action buttons 做二次审计，确认共享 `copied` state 是否会让多个复制按钮同时显示成功并造成误解；4) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 grouped Read/Search、generic fallback、agent/task 工具块、todo/files/subagents、文件改动列表/树和小屏 fallback；5) 开始收束同主题提交边界，把状态面板 a11y、toolblock header/action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 search group compact summary labels

- 目标：让 `SearchToolGroupBlock` 的 header query/status summary、row pattern/result summary、row status pill 和展开结果行号都具备同源 `title` / `aria-label` 或目标化 label，避免 grouped Search 中 `useState`、`1 match · 1 file`、`L12`、`Success` / `Failed` / `Pending` 被读成脱离搜索上下文的短文本。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `SearchToolGroupBlock.tsx` / `SearchToolGroupBlock.test.tsx` 与可读 cc-gui `SearchToolGroupBlock.tsx`；确认本项目 group/item toggle、open-file buttons、bulk actions 已有目标化标签，但 header summary、row pattern/result summary、row status pill 和 result line chip 仍未完整暴露同源 `aria-label` 或目标上下文。
- 功能点 3：补 RED 测试。验证方式：扩展 `SearchToolGroupBlock.test.tsx`，断言 header query/status summary 同源 `title` / `aria-label`，row pattern/result summary 同源 `title` / `aria-label`，row status pill 输出 `Search: useState · tools.success` / `Search: failed query · tools.failed`，展开结果行号输出 `Search result line: src/App.tsx · L12`；旧实现应失败。
- 功能点 4：实现最小展示修复。验证方式：在 `SearchToolGroupBlock.tsx` 为 compact summaries 添加同源标签；row status label 使用 `Search: <query or file target> · <status>`，result line label 使用 `Search result line: <path> · L<line>`。不改 `summarizeSearchInput`、`summarizeSearchResultText`、`summarizeSearchGroupHeader`、分组数量、展开/折叠、open file、`GenericToolBlock`、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 Search group compact summary/result/status pill 的 target-label 规则。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 toolblock/chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 search group compact summary labels

- 上轮进展与阻塞：上一轮完成 `ReadToolGroupBlock` header summary、row line summary 与 row status pill 的同源 `title` / `aria-label` 或目标化 label；本轮代码复查确认 `src/components/toolBlocks/ReadToolGroupBlock.tsx` 已为 header `.tool-title-summary` / `.tool-title-secondary-summary` 镜像 `aria-label`，并为 line/status 输出 `Read lines: <target> · <line>` 与 `Read: <target> · <status>`，`ReadToolGroupBlock.test.tsx` 与 `.trellis/spec/frontend/component-guidelines.md` 已覆盖。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui`，且该对标项目有 `.codegraph/`，`webview/src/components/toolBlocks/SearchToolGroupBlock.tsx` 可通过 CodeGraph 读取。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui `webview/src/components/toolBlocks/SearchToolGroupBlock.tsx` 有 search batch header、搜索项列表、pattern/path、状态点和自动滚动，但没有本项目当前的 group/item keyboard toggle、bulk expand/collapse action labels、open-file action labels 或 compact summary/status pill `aria-label`。本轮没有需要直接复制的 cc-gui 缺口；本项目继续在键盘和可访问语义上超越。
- 本轮差距诊断（超越 cc-gui）：本项目 `SearchToolGroupBlock` 的 group/item toggle、open-file buttons 和 bulk actions 已有目标化标签，但 header query/status summary、row pattern/result summary、row status pill 和展开 result line chip 旧实现仍只靠可见短文本或裸 `title`。用户价值：批量 Search 中常见 `useState`、`1 match · 1 file`、`L12`、`Success` / `Failed` / `Pending` 重复短文本；补齐后 hover、读屏和自动化快照能直接读到 `Search query: useState`、`Search results for useState: 1 match · 1 file`、`Search: useState · Success`，不用依赖相邻 DOM 或视觉行位置推断。
- 本轮规划：只改 `SearchToolGroupBlock.tsx` 的 compact summary/result/status pill/line chip 属性、`SearchToolGroupBlock.test.tsx` 的 SSR 回归断言、前端组件规范和 Trellis 记录；不改 `summarizeSearchInput`、`summarizeSearchResultText`、`summarizeSearchGroupHeader`、分组数量、展开/折叠、open file、`GenericToolBlock`、Store/Rust/Tauri response。优先级中等偏高：Search group 是高频定位面，query、匹配数量、首个结果文件和状态是扫描重点，改动成本低且延续 Bash/Read/Edit 同一模式。
- 本轮完成：`src/components/toolBlocks/SearchToolGroupBlock.tsx` 为 header `.tool-title-summary` / `.tool-title-secondary-summary` 镜像同源 `aria-label`；为 row `.task-group-item-pattern` 输出 `Search query: <query>` / `Search tool: <tool>` 的 `title` / `aria-label`；为 row `.task-group-item-secondary` 输出 `Search results for <target>: <summary>`；为 row status pill 输出 `Search: <target> · <status>`；为展开结果行号 `.search-result-file-line` 输出 `Search result line: <path> · L<line>` 的 `title` / `aria-label`。`src/components/toolBlocks/SearchToolGroupBlock.test.tsx` 新增 header summary aria、row pattern/result summary target label、row status pill target label 三类断言。`.trellis/spec/frontend/component-guidelines.md` 已记录 Search group compact summary/result/status pill/line chip target-label 规则。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/SearchToolGroupBlock.test.tsx` 先失败 3 项，失败点为 header summary 缺 `aria-label`、row pattern/result summary 仍是裸 `title="useState"` / `title="1 match · 1 file"`、row status pill 仍是裸 `tools.success` / `tools.failed`。GREEN 定向通过：同命令通过（1 file / 9 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/chat/MessageItem.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/toolPresentation.test.ts src/utils/toolGrouping.test.ts`（15 files / 170 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（55 files / 519 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/toolBlocks/SearchToolGroupBlock.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `SearchToolGroupBlock.tsx` / `SearchToolGroupBlock.test.tsx` 也 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 Search group header summary、row pattern/result summary、status pill 和 expanded result line chip 的 hover tooltip、截断表现、焦点顺序和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，构造 grouped Search/Read/Bash mixed status、single/grouped Edit stats、Read/Edit/Bash/Generic/Task/Agent header summary、diff hover、current activity overflow、recent edit overflow、MCP live-check、session-load、daemon 状态，点测 tooltip、焦点顺序和可访问名称；2) 继续审计 ToolBlock copy/action buttons，确认共享 `copied` state 是否会让多个复制按钮同时显示成功并造成误解；3) 审计 expanded search result rows 是否需要把匹配片段摘要也引入可见/可访问上下文，而不仅是 path/line；4) 与可读 cc-gui 项目做真实运行画面并排截图审计，重点看 grouped Search/Read、generic fallback、agent/task 工具块、todo/files/subagents、文件改动列表/树和小屏 fallback；5) 开始收束同主题提交边界，把状态面板 a11y、toolblock header/action-label、键盘/冒泡边界拆成可审查提交批次。

## 本轮 PLAN 2026-06-21 toolblock copy feedback isolation

- 目标：让单个工具块内的多复制按钮反馈互相隔离，避免 `BashToolBlock` 的“复制命令/复制输出”和 `GenericToolBlock` 的“复制输入/复制输出”在任一按钮点击后同时显示 `Copied` 成功态。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `BashToolBlock.tsx` / `GenericToolBlock.tsx` 与可读 cc-gui 对应组件；确认本项目新增复制按钮存在共享 `copied` 布尔状态，而 cc-gui 对应组件没有复制按钮，因此本轮属于本项目体验增强的回归风险修复。
- 功能点 3：补 RED 测试。验证方式：扩展 `BashToolBlock.test.tsx` 与 `GenericToolBlock.test.tsx`，模拟 command/input copied 状态时只对应按钮显示 `tools.copied`，另一个按钮仍显示原始 copy label；旧实现应失败。
- 功能点 4：实现最小交互修复。验证方式：将 `copied: boolean` 改为窄类型 copy target（`command` / `output` / `input` / `null`），按钮 class 与文案只根据自身 target 判断，不改复制内容、header、展开折叠、open file、tool result parsing、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录同一工具块内多个复制按钮必须使用目标化 copied state，禁止共享单个布尔状态驱动多个按钮成功态。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 toolblock/chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 toolblock copy feedback isolation

- 上轮进展与阻塞：上一轮完成 `SearchToolGroupBlock` compact summary / result / status / line chip 的目标化标签；本轮复查代码与测试确认已存在。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；`C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。本轮用 CodeGraph 读取本项目 `BashToolBlock` / `GenericToolBlock` 与可读 cc-gui 对应组件。
- 本轮差距诊断：cc-gui 对应 Bash/Generic tool block 没有复制按钮，本轮不是补齐 cc-gui；本项目已有复制能力但 `BashToolBlock` 与 `GenericToolBlock` 使用共享 `copied` 布尔状态，导致任一复制按钮点击后兄弟按钮也显示 `Copied`。用户价值：避免用户误以为输出也已复制，从而粘贴错内容。
- 本轮规划与完成：只改 `src/components/toolBlocks/BashToolBlock.tsx`、`src/components/toolBlocks/GenericToolBlock.tsx`、`BashToolBlock.test.tsx`、`GenericToolBlock.test.tsx`、`SingleToolBlockOpenFilePropagation.test.tsx`、`.trellis/spec/frontend/component-guidelines.md` 和 `TODO_LIST.md`。实现上将 Bash copied state 改为 `command | output | null`，Generic 改为 `input | output | null`，按钮只根据自身 target 显示成功态，并用 ref 清理上一次 reset timer；测试补充 command/input copied 状态只影响对应按钮；规范记录同一工具块多个复制按钮禁止共享布尔 copied state。
- 本轮验证：RED 定向 `npm test -- src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx` 先失败 2 项，失败原因为两个按钮同时显示 `tools.copied`；GREEN 同命令通过（2 files / 14 tests）。相邻验证通过（15 files / 172 tests）。`npm test` 通过（55 files / 521 tests，仅既有 Browserslist/caniuse-lite 过期提示）。`npm run build` 通过（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml` 通过（116 tests passed，2 doctests ignored）。构建产物已清理，确认 `{"dist":false,"out":false}`。`git diff --check` / `git diff --cached --check` 通过，仅 LF/CRLF 提示。IDE `build_project` 与相关 `get_file_problems` 均 120 秒超时，IDE 验证未完成。
- 下一步候选：1) 做真实 Tauri desktop smoke，覆盖 single Bash/Generic 多复制按钮、grouped Search/Read/Bash、Edit stats、Task/Agent、状态面板和 daemon/MCP 状态；2) 审计 expanded search result rows 是否需要匹配片段摘要；3) 收束同主题提交边界；4) 与可读 cc-gui 做真实运行并排截图审计。

## 迭代记录 2026-06-21 search expanded result snippets

- 上轮进展与阻塞：上一轮完成 `BashToolBlock` / `GenericToolBlock` 多复制按钮 copied feedback 隔离；本轮代码复查确认 `copiedTarget` 已分别按 `command | output | null` 和 `input | output | null` 区分，相关测试与前端规范已存在。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；`C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。本轮用 CodeGraph 读取了本项目 `SearchToolGroupBlock.tsx` / `SearchToolGroupBlock.test.tsx` / `toolPresentation.ts`，以及可读 cc-gui `webview/src/components/toolBlocks/SearchToolGroupBlock.tsx`。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui 的 Search group 展示 batch header、pattern/path/status 和自动滚动，不展示匹配片段；本项目此前展开结果也只显示 path + line。因此本轮没有直接补齐 cc-gui 的缺口。
- 本轮差距诊断（超越 cc-gui）：搜索结果原始文本常是 `path:line[:column]:snippet`，用户定位结果时不仅需要知道文件和行号，也需要知道该行为什么匹配。此前 `summarizeSearchResultText` 丢弃 snippet，展开行只显示 path + `L12`。用户价值：当同一文件或多文件有多个匹配时，展开行直接显示 `const [value, setValue] = useState(false);` 这类片段，用户无需展开 Generic raw output 或打开文件才能判断哪条结果值得点开。
- 本轮规划与完成：只改 `src/utils/toolPresentation.ts`、`src/utils/toolPresentation.test.ts`、`src/components/toolBlocks/SearchToolGroupBlock.tsx`、`src/components/toolBlocks/SearchToolGroupBlock.test.tsx`、`src/styles/toolBlocks.css`、`.trellis/spec/frontend/component-guidelines.md` 和 `TODO_LIST.md`。实现上给 `SearchResultFile` 增加可选 `snippet`；`parseSearchResultLine` 从 `path:line[:column]:snippet` 提取并截断 snippet；展开结果行新增 `.search-result-file-snippet`；结果按钮的 `title` / `aria-label` 改为 `Open search result: <path> · L<line> · <snippet>`；CSS 使用三列 grid 保持 path/snippet 截断且 line 固定在右侧。不改 match/file count、dedupe、排序、openFile 参数、GenericToolBlock、Store/Rust/Tauri response。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/utils/toolPresentation.test.ts` 先失败 3 项，失败点为 parser 未输出 snippet，展开 result row 未渲染 snippet / contextual label。GREEN 定向通过：同命令通过（2 files / 42 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/utils/toolPresentation.test.ts src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/chat/MessageItem.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/toolGrouping.test.ts`（15 files / 173 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（55 files / 522 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored；期间 Cargo 等待 package cache/build directory file lock 后正常完成）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/toolBlocks/SearchToolGroupBlock.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/utils/toolPresentation.ts src/utils/toolPresentation.test.ts src/styles/toolBlocks.css .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `SearchToolGroupBlock.tsx`、`toolPresentation.ts`、`SearchToolGroupBlock.test.tsx`、`toolPresentation.test.ts` 均 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 expanded Search result snippet 的实际截断、hover tooltip、焦点顺序和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，覆盖 expanded Search snippet、single Bash/Generic 多复制按钮、grouped Search/Read/Bash、Edit stats、Task/Agent、状态面板和 daemon/MCP 状态；2) 审计 Search result dedupe 是否应在同一文件多匹配时显示多行而不是只保留首个文件行；3) 收束同主题提交边界，把 toolblock a11y/action-label、搜索结果片段、复制反馈隔离拆成可审查提交批次；4) 与可读 cc-gui 做真实运行并排截图审计。

## 本轮 PLAN 2026-06-21 search expanded result snippets

- 目标：让 `SearchToolGroupBlock` 展开后的 result rows 不只显示文件路径和行号，还显示从 `path:line[:column]:snippet` 中提取的匹配片段，并把片段并入该行的 `title` / `aria-label`。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `SearchToolGroupBlock.tsx` / `SearchToolGroupBlock.test.tsx` / `summarizeSearchResultText` 与可读 cc-gui `SearchToolGroupBlock.tsx`；确认 cc-gui 展示 pattern/path/status，没有展开匹配片段；本项目当前展开结果也只显示 path + line。
- 功能点 3：补 RED 测试。验证方式：扩展 `SearchToolGroupBlock.test.tsx` 和 `toolPresentation.test.ts`，断言 `src/App.tsx:12:const ...` 会解析为 `snippet`，展开行可见 snippet，button title/aria 包含 `Search result: src/App.tsx · L12 · const ...`；旧实现应失败。
- 功能点 4：实现最小展示修复。验证方式：给 `SearchResultFile` 增加可选 `snippet`，在 `parseSearchResultLine` 中提取并截断，`SearchToolGroupBlock` 展开行增加 snippet span 和上下文 label；不改 match/file count、dedupe、排序、openFile 参数、GenericToolBlock、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 expanded Search result rows 的 path/line/snippet 展示与 label 规则。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 toolblock/chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 尾部恢复摘要 2026-06-21 search expanded result snippets

- 本轮已完成并验证：`SearchToolGroupBlock` 展开结果行现在显示从 `path:line[:column]:snippet` 解析出的 snippet，`SearchResultFile` 增加可选 `snippet`，结果行 button label 为 `Open search result: <path> · L<line> · <snippet>`，CSS 保持 path/snippet 截断且 line 固定在右侧。
- 修改文件：`src/utils/toolPresentation.ts`、`src/utils/toolPresentation.test.ts`、`src/components/toolBlocks/SearchToolGroupBlock.tsx`、`src/components/toolBlocks/SearchToolGroupBlock.test.tsx`、`src/styles/toolBlocks.css`、`.trellis/spec/frontend/component-guidelines.md`、`TODO_LIST.md`。
- 验证通过：RED/GREEN 定向（2 files / 42 tests）、相邻验证（15 files / 173 tests）、`npm test`（55 files / 522 tests）、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）、dist/out 清理确认 `{"dist":false,"out":false}`、`git diff --check` / `git diff --cached --check`。IDE build/file problems 仍 120 秒超时。
- 下一步候选：真实 Tauri desktop smoke；审计 Search result dedupe 是否应展示同文件多匹配多行；收束同主题提交边界；与可读 cc-gui 做真实运行并排截图。

## 迭代记录 2026-06-21 search same-file multi-match rows

- 上轮进展与阻塞：上一轮完成 Search 展开结果 snippet 展示；本轮复查代码确认 `SearchResultFile.snippet`、`parseSearchResultLine()` 的 snippet 解析、`SearchToolGroupBlock` 展开行 snippet、combined `Open search result: <path> · L<line> · <snippet>` label 和三列 CSS 布局均已存在。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。本轮用 CodeGraph 读取了本项目 `SearchToolGroupBlock.tsx`、`summarizeSearchResultText()`、`parseSearchResultLine()` 与相关测试。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui Search group 展示 batch header、pattern/path/status 和滚动聚合，没有展开 snippet 或同文件多匹配行。因此本轮没有直接 cc-gui 缺口需要补齐。
- 本轮差距诊断（超越 cc-gui）：本项目上一轮已显示 snippet，但 `summarizeSearchResultText()` 仍按 lowercased path 去重 `files`，导致同一文件 `src/App.tsx:12:...`、`src/App.tsx:48:...` 只显示第一条。用户价值：搜索一个高频符号时，同文件多行命中很常见；展开列表显示多条命中行后，用户能直接打开目标行，不用读 raw output 或先跳到第一条再手动查找。
- 本轮规划与完成：只改 `src/utils/toolPresentation.ts`、`src/utils/toolPresentation.test.ts`、`src/components/toolBlocks/SearchToolGroupBlock.test.tsx`、`.trellis/spec/frontend/component-guidelines.md` 和 `TODO_LIST.md`。实现上将 `summarizeSearchResultText()` 的 `fileCount` 改为独立 unique path 集合计算，将可见 `files` 改为 path + line + snippet 维度去重并保留最多 8 条；不改 `matchCount`、explicit matches/files 解析、openFile 参数、Search 组件 DOM 结构、GenericToolBlock、Store/Rust/Tauri response。测试新增同文件 L12/L48 两条 snippet 的解析与展开 DOM 断言，并同步旧测试中已写死的 file-level dedupe 期望。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/utils/toolPresentation.test.ts` 先失败 2 项，失败点为 `files` 只返回 L12，展开 UI 未渲染 L48。GREEN 定向通过：同命令通过（2 files / 44 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/utils/toolPresentation.test.ts src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/chat/MessageItem.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/toolGrouping.test.ts`（15 files / 175 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（55 files / 524 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建通过：`npm run build`（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored；Cargo 等待 package cache/build directory file lock 后正常完成）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/utils/toolPresentation.ts src/utils/toolPresentation.test.ts .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `toolPresentation.ts`、`toolPresentation.test.ts`、`SearchToolGroupBlock.test.tsx`、`.trellis/spec/frontend/component-guidelines.md` 均 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察同文件多匹配 Search 展开行的实际截断、hover tooltip、焦点顺序和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，覆盖 expanded Search 多行 snippet、single Bash/Generic 多复制按钮、grouped Search/Read/Bash、Edit stats、Task/Agent、状态面板和 daemon/MCP 状态；2) 审计 Search 展开列表 8 条上限是否需要显示“更多结果在 raw output 中”的 compact footer；3) 收束同主题提交边界，把 toolblock a11y/action-label、搜索结果片段、多匹配行、复制反馈隔离拆成可审查提交批次；4) 与可读 cc-gui 做真实运行并排截图审计，重点看 grouped Search/Read、generic fallback、agent/task 工具块、todo/files/subagents、文件改动列表/树和小屏 fallback。

## 尾部恢复摘要 2026-06-21 search same-file multi-match rows

- 本轮已完成并验证：`summarizeSearchResultText()` 现在用 unique path 计算 `fileCount`，用 path + line + snippet 去重可见 result rows；同一文件多条 `path:line[:column]:snippet` 命中会在 Search 展开区显示为多条可打开行。
- 修改文件：`src/utils/toolPresentation.ts`、`src/utils/toolPresentation.test.ts`、`src/components/toolBlocks/SearchToolGroupBlock.test.tsx`、`.trellis/spec/frontend/component-guidelines.md`、`TODO_LIST.md`。
- 验证通过：RED/GREEN 定向（2 files / 44 tests）、相邻验证（15 files / 175 tests）、`npm test`（55 files / 524 tests）、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）、dist/out 清理确认 `{"dist":false,"out":false}`、`git diff --check` / `git diff --cached --check`。IDE build/file problems 仍 120 秒超时。
- 下一步候选：真实 Tauri desktop smoke；Search 展开列表 8 条上限 footer；收束同主题提交边界；与可读 cc-gui 做真实运行并排截图。

## 本轮 PLAN 2026-06-21 search result limit footer

- 目标：当 `SearchToolGroupBlock` 展开结果超过当前最多 8 条可见行时，显示一个 compact footer，明确还有多少条搜索结果未在快捷列表中展示，避免用户误以为搜索只返回了当前 8 条。
- 功能点 1：恢复上下文与路径确认。验证方式：读取 `TODO_LIST.md`、Trellis task artifacts、frontend spec、shared guides 与 `package.json`；确认 `C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`，用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在，实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。
- 功能点 2：CodeGraph / cc-gui 诊断。验证方式：用 CodeGraph 读取本项目 `summarizeSearchResultText()`、`SearchToolGroupBlock.tsx` 与相关测试；确认当前结果列表固定 `files.slice(0, 8)`，没有任何 omitted/overflow 提示。对标 cc-gui 没有 snippet/多行展开 footer，因此本轮属于本项目超越项。
- 功能点 3：补 RED 测试。验证方式：扩展 `toolPresentation.test.ts` 断言 10 条解析结果只展示 8 条并输出 `omittedResultCount: 2`；扩展 `SearchToolGroupBlock.test.tsx` 断言展开区渲染 footer，并带有 `title` / `aria-label`。
- 功能点 4：实现最小展示修复。验证方式：给 `SearchResultSummary` 增加可选 `omittedResultCount`；`summarizeSearchResultText()` 保持最多 8 条 visible rows，同时计算 omitted count；`SearchToolGroupBlock` 在 result rows 后显示 compact footer。不改 openFile 参数、match/file count、排序、GenericToolBlock、Store/Rust/Tauri response。
- 功能点 5：同步 Trellis 规范。验证方式：更新 `.trellis/spec/frontend/component-guidelines.md`，记录 expanded Search result rows 有可见上限时必须显示 omitted footer，并说明 raw output 仍在 Generic details 中可查。
- 功能点 6：质量验证。验证方式：运行 RED/GREEN 定向测试、相邻 toolblock/chat/status 测试、`npm test`、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`、IDE build/file problems（若超时则标注未验证）、`git diff --check` / `git diff --cached --check`，并清理 `dist` / 确认 `out` 不存在。

## 迭代记录 2026-06-21 search result limit footer

- 上轮进展与阻塞：上一轮完成 Search 同文件多匹配多行展示；本轮代码复查确认 `summarizeSearchResultText()` 已用 unique path 计算 `fileCount`、用 path + line + snippet 去重可见 result rows，`SearchToolGroupBlock` 展开区能显示同文件 L12/L48 多条可打开行，相关测试与前端规范已存在。已知阻塞仍是真实 Tauri 桌面点测、屏幕阅读器 / Accessibility tree 实际播报、cc-gui 真实运行并排截图，以及 IDE build / IDE 文件问题扫描 120 秒超时。
- 本轮路径确认：`C:/guodevelop/ccg-switch` 可读且有 `.codegraph/`；用户原始对标路径 `C:/guodevelop/jetbrains-cc-gui` 不存在；实际可读对标路径为 `C:/guodevelop/demo/jetbrains-cc-gui` 且有 `.codegraph/`。本轮用 CodeGraph 读取了本项目 `SearchToolGroupBlock.tsx`、`summarizeSearchResultText()`、`SearchResultSummary` 与相关测试。
- 本轮差距诊断（补齐 cc-gui）：可读 cc-gui Search group 展示 batch header、pattern/path/status 和滚动聚合，没有 snippet、多行命中展开或结果上限 footer。因此本轮没有直接 cc-gui 缺口需要补齐。
- 本轮差距诊断（超越 cc-gui）：本项目展开 quick list 最多显示 8 条，但摘要可显示 10 matches / 12 matches；旧实现没有任何 overflow 提示，用户可能误以为只返回了可见的 8 条。用户价值：当搜索结果较多时，footer 明确“原始输出中还有 N 条结果”，用户能理解可见列表是快捷入口，并知道需要展开 raw output 查看剩余内容。
- 本轮规划与完成：只改 `src/utils/toolPresentation.ts`、`src/utils/toolPresentation.test.ts`、`src/components/toolBlocks/SearchToolGroupBlock.tsx`、`src/components/toolBlocks/SearchToolGroupBlock.test.tsx`、`src/styles/toolBlocks.css`、`src/locales/en.json`、`src/locales/zh.json`、`.trellis/spec/frontend/component-guidelines.md` 和 `TODO_LIST.md`。实现上给 `SearchResultSummary` 增加可选 `omittedResultCount`；`summarizeSearchResultText()` 保持最多 8 条 visible rows，同时计算 omitted count；`SearchToolGroupBlock` 在 result rows 后渲染 `.search-result-files-footer`，并给 footer 同源 `title` / `aria-label`；新增中英文 i18n key `tools.searchMoreResults`；CSS 将 footer 设计成非交互的轻量提示。不改 openFile 参数、match/file count、排序、GenericToolBlock、Store/Rust/Tauri response。
- 本轮验证：RED：`npm test -- src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/utils/toolPresentation.test.ts` 先失败 2 项，失败点为 `omittedResultCount` 是 `undefined` 且展开 DOM 没有 `.search-result-files-footer`。GREEN 定向通过：同命令通过（2 files / 46 tests）。相邻验证通过：`npm test -- src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/utils/toolPresentation.test.ts src/components/toolBlocks/ReadToolGroupBlock.test.tsx src/components/toolBlocks/ReadToolBlock.test.tsx src/components/toolBlocks/BashToolGroupBlock.test.tsx src/components/toolBlocks/BashToolBlock.test.tsx src/components/toolBlocks/EditToolGroupBlock.test.tsx src/components/toolBlocks/EditToolBlock.test.tsx src/components/toolBlocks/GenericToolBlock.test.tsx src/components/toolBlocks/TaskExecutionBlock.test.tsx src/components/toolBlocks/AgentGroupBlock.test.tsx src/components/chat/MessageItem.test.tsx src/components/chat/StatusPanel.test.tsx src/components/chat/ChatInputStatusTabs.test.tsx src/utils/toolGrouping.test.ts`（15 files / 177 tests，仅既有 Browserslist/caniuse-lite 过期提示）。全量前端通过：`npm test`（55 files / 526 tests，仅既有 Browserslist/caniuse-lite 过期提示）。生产构建第一次失败于测试代码使用 `Array.prototype.at()`，当前 TS lib 不支持；已改成索引访问后 `npm test -- src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/utils/toolPresentation.test.ts` 通过（2 files / 46 tests），`npm run build` 通过（`tsc && vite build`，仅既有 Browserslist/caniuse-lite 过期提示），并再次运行 `npm test` 通过（55 files / 526 tests）。后端验证通过：`cargo check --manifest-path src-tauri/Cargo.toml` 与 `cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored；Cargo 等待 package cache/shared cache/build directory file lock 后正常完成）。构建生成的 `C:/guodevelop/ccg-switch/dist` 已清理；`out` 不存在，确认结果为 `{"dist":false,"out":false}`。`git diff --check -- src/components/toolBlocks/SearchToolGroupBlock.tsx src/components/toolBlocks/SearchToolGroupBlock.test.tsx src/utils/toolPresentation.ts src/utils/toolPresentation.test.ts src/styles/toolBlocks.css src/locales/en.json src/locales/zh.json .trellis/spec/frontend/component-guidelines.md TODO_LIST.md` exit 0，仅 LF/CRLF 提示；`git diff --cached --check` exit 0，无输出。IDE `build_project` 120 秒超时；IDE `get_file_problems` 对 `SearchToolGroupBlock.tsx`、`SearchToolGroupBlock.test.tsx`、`toolPresentation.ts`、`toolPresentation.test.ts` 均 120 秒超时，IDE 验证未完成。
- 未验证项：未启动真实 Tauri 桌面观察 Search overflow footer 的实际截断、hover tooltip、焦点顺序和可访问名称；未手动确认 Accessibility tree 或屏幕阅读器实际播报；未与 cc-gui 做真实运行并排截图。
- 下一步候选：1) 做真实 Tauri desktop smoke，覆盖 expanded Search 多行 snippet/overflow footer、single Bash/Generic 多复制按钮、grouped Search/Read/Bash、Edit stats、Task/Agent、状态面板和 daemon/MCP 状态；2) 审计 Search footer 是否需要交互式“展开 raw output”快捷入口，但这会扩大到 GenericToolBlock 展开控制，需单独设计；3) 收束同主题提交边界，把 toolblock a11y/action-label、搜索结果片段、多匹配行、overflow footer、复制反馈隔离拆成可审查提交批次；4) 与可读 cc-gui 做真实运行并排截图审计，重点看 grouped Search/Read、generic fallback、agent/task 工具块、todo/files/subagents、文件改动列表/树和小屏 fallback。

## 尾部恢复摘要 2026-06-21 search result limit footer

- 本轮已完成并验证：Search 展开 quick list 超过 8 条时，`summarizeSearchResultText()` 输出 `omittedResultCount`，`SearchToolGroupBlock` 在结果列表底部显示非交互 footer（中英文 i18n、`title` / `aria-label`），提示剩余结果在 raw output 中。
- 修改文件：`src/utils/toolPresentation.ts`、`src/utils/toolPresentation.test.ts`、`src/components/toolBlocks/SearchToolGroupBlock.tsx`、`src/components/toolBlocks/SearchToolGroupBlock.test.tsx`、`src/styles/toolBlocks.css`、`src/locales/en.json`、`src/locales/zh.json`、`.trellis/spec/frontend/component-guidelines.md`、`TODO_LIST.md`。
- 验证通过：RED/GREEN 定向（2 files / 46 tests）、相邻验证（15 files / 177 tests）、`npm test`（55 files / 526 tests）、`npm run build`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo test --manifest-path src-tauri/Cargo.toml`（116 tests passed，2 doctests ignored）、dist/out 清理确认 `{"dist":false,"out":false}`、`git diff --check` / `git diff --cached --check`。IDE build/file problems 仍 120 秒超时。
- 下一步候选：真实 Tauri desktop smoke；是否给 Search footer 增加展开 raw output 快捷入口；收束同主题提交边界；与可读 cc-gui 做真实运行并排截图。
